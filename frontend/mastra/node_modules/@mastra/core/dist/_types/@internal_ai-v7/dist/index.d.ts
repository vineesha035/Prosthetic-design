import { JSONSchema7 } from 'json-schema';
import { ServerResponse } from 'node:http';
import { ServerResponse as ServerResponse_2 } from 'http';
import { WORKFLOW_DESERIALIZE } from '@workflow/serde';
import { WORKFLOW_SERIALIZE } from '@workflow/serde';
import { z } from 'zod/v4';
import * as z3 from 'zod/v3';
import * as z4 from 'zod/v4';

export declare abstract class AbstractChat<UI_MESSAGE extends UIMessage> {
    readonly id: string;
    readonly generateId: IdGenerator;
    protected state: ChatState<UI_MESSAGE>;
    private messageMetadataSchema;
    private dataPartSchemas;
    private readonly transport;
    private onError?;
    private onToolCall?;
    private onFinish?;
    private onData?;
    private sendAutomaticallyWhen?;
    private activeResponse;
    private jobExecutor;
    constructor({ generateId, id, transport, messageMetadataSchema, dataPartSchemas, state, onError, onToolCall, onFinish, onData, sendAutomaticallyWhen, }: Omit<ChatInit<UI_MESSAGE>, 'messages'> & {
        state: ChatState<UI_MESSAGE>;
    });
    /**
     * Hook status:
     *
     * - `submitted`: The message has been sent to the API and we're awaiting the start of the response stream.
     * - `streaming`: The response is actively streaming in from the API, receiving chunks of data.
     * - `ready`: The full response has been received and processed; a new user message can be submitted.
     * - `error`: An error occurred during the API request, preventing successful completion.
     */
    get status(): ChatStatus;
    protected setStatus({ status, error, }: {
        status: ChatStatus;
        error?: Error;
    }): void;
    get error(): Error | undefined;
    get messages(): UI_MESSAGE[];
    get lastMessage(): UI_MESSAGE | undefined;
    set messages(messages: UI_MESSAGE[]);
    /**
     * Appends or replaces a user message to the chat list. This triggers the API call to fetch
     * the assistant's response.
     *
     * If a messageId is provided, the message will be replaced.
     */
    sendMessage: (message?: (CreateUIMessage<UI_MESSAGE> & {
        text?: never;
        files?: never;
        messageId?: string;
    }) | {
        text: string;
        files?: FileList | FileUIPart[];
        metadata?: InferUIMessageMetadata<UI_MESSAGE>;
        parts?: never;
        messageId?: string;
    } | {
        files: FileList | FileUIPart[];
        metadata?: InferUIMessageMetadata<UI_MESSAGE>;
        parts?: never;
        messageId?: string;
    }, options?: ChatRequestOptions) => Promise<void>;
    /**
     * Regenerate the assistant message with the provided message id.
     * If no message id is provided, the last assistant message will be regenerated.
     */
    regenerate: ({ messageId, ...options }?: {
        messageId?: string;
    } & ChatRequestOptions) => Promise<void>;
    /**
     * Attempt to resume an ongoing streaming response.
     */
    resumeStream: (options?: ChatRequestOptions) => Promise<void>;
    /**
     * Clear the error state and set the status to ready if the chat is in an error state.
     */
    clearError: () => void;
    addToolApprovalResponse: ChatAddToolApproveResponseFunction;
    addToolOutput: ChatAddToolOutputFunction<UI_MESSAGE>;
    /** @deprecated Use addToolOutput */
    addToolResult: ChatAddToolOutputFunction<UI_MESSAGE>;
    /**
     * Abort the current request immediately, keep the generated tokens if any.
     */
    stop: () => Promise<void>;
    private shouldSendAutomatically;
    private makeRequest;
}

/**
 * Tool names that are enabled for a generation step.
 *
 * `undefined` means no tool restriction is applied. Tool names are object keys
 * at runtime, so the type is restricted to the string keys of the configured
 * tool set.
 */
export declare type ActiveTools<TOOLS extends ToolSet> = ReadonlyArray<keyof TOOLS & string> | undefined;

declare type ActiveToolSubset<TOOLS extends ToolSet | undefined, ACTIVE_TOOL_NAMES extends ActiveTools<NonNullable<TOOLS>>> = TOOLS extends undefined ? undefined : [ACTIVE_TOOL_NAMES] extends [NonNullable<ActiveTools<NonNullable<TOOLS>>>] ? Pick<NonNullable<TOOLS>, ACTIVE_TOOL_NAMES[number]> : TOOLS;

/**
 * Middleware that appends input examples to tool descriptions.
 *
 * This is useful for providers that don't natively support the `inputExamples`
 * property. The middleware serializes examples into the tool's description text.
 *
 * @param options - Configuration options for the middleware.
 * @param options.prefix - A prefix to prepend before the examples. Default: 'Input Examples:'
 * @param options.format - Optional custom formatter for each example.
 *   Receives the example object and its index. Default: JSON.stringify(example.input)
 * @param options.remove - Whether to remove the inputExamples property
 *   after adding them to the description. Default: true
 *
 * @example
 * ```ts
 * import { wrapLanguageModel, addToolInputExamplesMiddleware } from 'ai';
 *
 * const model = wrapLanguageModel({
 *   model: yourModel,
 *   middleware: addToolInputExamplesMiddleware(),
 * });
 * ```
 */
export declare function addToolInputExamplesMiddleware({ prefix, format, remove, }?: {
    /**
     * A prefix to prepend before the examples.
     */
    prefix?: string;
    /**
     * Optional custom formatter for each example.
     * Receives the example object and its index.
     * Default: JSON.stringify(example.input)
     */
    format?: (example: {
        input: JSONObject_2;
    }, index: number) => string;
    /**
     * Whether to remove the inputExamples property after adding them to the description.
     * Default: true
     */
    remove?: boolean;
}): LanguageModelMiddleware;

/**
 * An Agent receives a prompt (text or messages) and generates or streams an output
 * that consists of steps, tool calls, data parts, etc.
 *
 * You can implement your own Agent by implementing the `Agent` interface,
 * or use the `ToolLoopAgent` class.
 */
export declare interface Agent<CALL_OPTIONS = never, TOOLS extends ToolSet = {}, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = never> {
    /**
     * The specification version of the agent interface. This will enable
     * us to evolve the agent interface and retain backwards compatibility.
     */
    readonly version: 'agent-v1';
    /**
     * The id of the agent.
     */
    readonly id: string | undefined;
    /**
     * The tools that the agent can use.
     */
    readonly tools: TOOLS;
    /**
     * Generates an output from the agent (non-streaming).
     */
    generate(options: AgentCallParameters<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT>): PromiseLike<GenerateTextResult<TOOLS, RUNTIME_CONTEXT, OUTPUT>>;
    /**
     * Streams an output from the agent (streaming).
     */
    stream(options: AgentStreamParameters<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT>): PromiseLike<StreamTextResult<TOOLS, RUNTIME_CONTEXT, OUTPUT>>;
}

/**
 * Parameters for calling an agent.
 */
export declare type AgentCallParameters<CALL_OPTIONS, TOOLS extends ToolSet = {}, RUNTIME_CONTEXT extends Context_2 = Context_2> = ([CALL_OPTIONS] extends [never] ? {
    options?: never;
} : {
    options: CALL_OPTIONS;
}) & ({
    /**
     * A prompt. It can be either a text prompt or a list of messages.
     *
     * You can either use `prompt` or `messages` but not both.
     */
    prompt: string | Array<ModelMessage>;
    /**
     * A list of messages.
     *
     * You can either use `prompt` or `messages` but not both.
     */
    messages?: never;
} | {
    /**
     * A list of messages.
     *
     * You can either use `prompt` or `messages` but not both.
     */
    messages: Array<ModelMessage>;
    /**
     * A prompt. It can be either a text prompt or a list of messages.
     *
     * You can either use `prompt` or `messages` but not both.
     */
    prompt?: never;
}) & {
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Timeout in milliseconds. Can be specified as a number or as an object
     * with total, per-step, first-content, inter-content, and tool timeouts.
     * First-content and inter-content timeouts are only enforced by streaming
     * calls.
     */
    timeout?: TimeoutConfiguration<TOOLS>;
    /**
     * Callback that is called when the agent operation begins, before any LLM calls.
     */
    onStart?: GenerateTextOnStartCallback<TOOLS, RUNTIME_CONTEXT>;
    /**
     * Callback that is called when the agent operation begins, before any LLM calls.
     *
     * @deprecated Use `onStart` instead.
     */
    experimental_onStart?: GenerateTextOnStartCallback<TOOLS, RUNTIME_CONTEXT>;
    /**
     * Callback that is called when a step (LLM call) begins, before the provider is called.
     */
    onStepStart?: GenerateTextOnStepStartCallback<TOOLS, RUNTIME_CONTEXT>;
    /**
     * Callback that is called when a step (LLM call) begins, before the provider is called.
     *
     * @deprecated Use `onStepStart` instead.
     */
    experimental_onStepStart?: GenerateTextOnStepStartCallback<TOOLS, RUNTIME_CONTEXT>;
    /**
     * Callback that is called before each tool execution begins.
     */
    onToolExecutionStart?: OnToolExecutionStartCallback<TOOLS>;
    /**
     * Callback that is called before each tool execution begins.
     *
     * @deprecated Use `onToolExecutionStart` instead.
     */
    experimental_onToolCallStart?: OnToolExecutionStartCallback<TOOLS>;
    /**
     * Callback that is called after each tool execution completes.
     */
    onToolExecutionEnd?: OnToolExecutionEndCallback<TOOLS>;
    /**
     * Callback that is called after each tool execution completes.
     *
     * @deprecated Use `onToolExecutionEnd` instead.
     */
    experimental_onToolCallFinish?: OnToolExecutionEndCallback<TOOLS>;
    /**
     * Callback that is called when each step (LLM call) ends, including intermediate steps.
     */
    onStepEnd?: GenerateTextOnStepEndCallback<TOOLS, RUNTIME_CONTEXT>;
    /**
     * Callback that is called when each step (LLM call) ends, including intermediate steps.
     *
     * @deprecated Use `onStepEnd` instead.
     */
    onStepFinish?: GenerateTextOnStepFinishCallback<TOOLS, RUNTIME_CONTEXT>;
    /**
     * Callback that is called when all steps are finished and the response is complete.
     */
    onEnd?: GenerateTextOnEndCallback<TOOLS, RUNTIME_CONTEXT>;
    /**
     * Callback that is called when all steps are finished and the response is complete.
     *
     * @deprecated Use `onEnd` instead.
     */
    onFinish?: GenerateTextOnEndCallback<TOOLS, RUNTIME_CONTEXT>;
    /**
     * The sandbox environment that is passed through to tool execution.
     */
    experimental_sandbox?: Experimental_SandboxSession;
};

/**
 * Parameters for streaming an output from an agent.
 */
export declare type AgentStreamParameters<CALL_OPTIONS, TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = AgentCallParameters<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT> & {
    /**
     * Optional stream transformations.
     * They are applied in the order they are provided.
     * The stream transformations must maintain the stream structure for streamText to work correctly.
     */
    experimental_transform?: Arrayable_2<StreamTextTransform<TOOLS>>;
};

declare namespace _ai_sdk_provider_utils {
    export {
        getErrorMessage,
        WORKFLOW_DESERIALIZE,
        WORKFLOW_SERIALIZE,
        EventSourceMessage,
        EventSourceParserStream,
        Arrayable,
        AssistantContent_2 as AssistantContent,
        AssistantModelMessage_2 as AssistantModelMessage,
        Context,
        CustomPart,
        DEFAULT_MAX_DOWNLOAD_SIZE,
        DataContent_2 as DataContent,
        DelayedPromise,
        DownloadError_2 as DownloadError,
        DynamicTool,
        TRANSCRIPTION_STREAM_AUDIO_DONE_FRAME_TYPE as EXPERIMENTAL_TRANSCRIPTION_STREAM_AUDIO_DONE_FRAME_TYPE,
        TRANSCRIPTION_STREAM_START_FRAME_TYPE as EXPERIMENTAL_TRANSCRIPTION_STREAM_START_FRAME_TYPE,
        ExecutableTool,
        SandboxProcess as Experimental_SandboxProcess,
        SandboxSession as Experimental_SandboxSession,
        TranscriptionStreamClientFrame as Experimental_TranscriptionStreamClientFrame,
        TranscriptionStreamStartFrame as Experimental_TranscriptionStreamStartFrame,
        FetchFunction,
        FileData,
        FileDataData,
        FileDataReference,
        FileDataText,
        FileDataUrl,
        FilePart_2 as FilePart,
        FlexibleSchema_2 as FlexibleSchema,
        FunctionTool,
        HasRequiredKey,
        IdGenerator_2 as IdGenerator,
        ImagePart_2 as ImagePart,
        InferSchema_2 as InferSchema,
        InferToolContext,
        InferToolInput_2 as InferToolInput,
        InferToolOutput_2 as InferToolOutput,
        InferToolSetContext,
        LazySchema,
        MaybePromiseLike,
        ModelMessage_2 as ModelMessage,
        ParseResult,
        ProviderDefinedTool,
        ProviderDefinedToolFactory,
        ProviderDefinedToolFactoryWithOutputSchema,
        ProviderExecutedTool,
        ProviderExecutedToolFactory,
        ProviderOptions,
        ProviderReference_2 as ProviderReference,
        ReasoningFilePart,
        ReasoningPart,
        Resolvable,
        ResponseHandler,
        RetryDelayProvider,
        RetryErrorFactory,
        RetryErrorReason,
        RetryFunction,
        Schema_2 as Schema,
        SerializationError,
        ShouldRetryFunction,
        StreamingToolCallDelta,
        StreamingToolCallTracker,
        StreamingToolCallTrackerOptions,
        SystemModelMessage_2 as SystemModelMessage,
        TextPart_2 as TextPart,
        Tool_2 as Tool,
        ToolApprovalRequest_2 as ToolApprovalRequest,
        ToolApprovalResponse_2 as ToolApprovalResponse,
        ToolCall,
        ToolCallPart_2 as ToolCallPart,
        ToolContent_2 as ToolContent,
        ToolExecuteFunction_2 as ToolExecuteFunction,
        ToolExecutionOptions_2 as ToolExecutionOptions,
        ToolModelMessage_2 as ToolModelMessage,
        ToolNameMapping,
        ToolNeedsApprovalFunction,
        ToolResult,
        ToolResultOutput,
        ToolResultPart_2 as ToolResultPart,
        ToolSet_2 as ToolSet,
        UserContent_2 as UserContent,
        UserModelMessage_2 as UserModelMessage,
        VERSION,
        ValidationResult,
        WebSocketConnection,
        WebSocketConstructor,
        WebSocketLike,
        asArray,
        asSchema_2 as asSchema,
        cancelResponseBody,
        combineHeaders,
        connectToWebSocket,
        convertAsyncIteratorToReadableStream,
        convertBase64ToUint8Array,
        convertImageModelFileToDataUri,
        convertInlineFileDataToUint8Array,
        convertToBase64,
        convertToFormData,
        convertUint8ArrayToBase64,
        createBinaryResponseHandler,
        createEventSourceResponseHandler,
        createIdGenerator_2 as createIdGenerator,
        createJsonErrorResponseHandler,
        createJsonResponseHandler,
        createProviderDefinedToolFactory,
        createProviderDefinedToolFactoryWithOutputSchema,
        createProviderExecutedToolFactory,
        createStatusCodeErrorResponseHandler,
        createToolNameMapping,
        delay,
        detectMediaType,
        downloadBlob,
        dynamicTool_2 as dynamicTool,
        executeTool,
        parseTranscriptionStreamClientFrame as experimental_parseTranscriptionStreamClientFrame,
        parseTranscriptionStreamPart as experimental_parseTranscriptionStreamPart,
        serializeTranscriptionStreamPart as experimental_serializeTranscriptionStreamPart,
        extractLines,
        extractResponseHeaders,
        fetchWithValidatedRedirects,
        filterNullable,
        generateId_2 as generateId,
        getFromApi,
        getRuntimeEnvironmentUserAgent,
        getTopLevelMediaType,
        getWebSocketConstructor,
        injectJsonInstructionIntoMessages,
        isAbortError,
        isBrowserRuntime,
        isBuffer,
        isCustomReasoning,
        isExecutableTool,
        isFullMediaType,
        isNonNullable,
        isParsableJson,
        isProviderReference,
        isSameOrigin,
        isUrlSupported,
        jsonSchema_2 as jsonSchema,
        lazySchema,
        loadApiKey,
        loadOptionalSetting,
        loadSetting,
        mapReasoningToProviderBudget,
        mapReasoningToProviderEffort,
        mediaTypeToExtension,
        normalizeHeaders,
        parseJSON,
        parseJsonEventStream_2 as parseJsonEventStream,
        parseProviderOptions,
        postFormDataToApi,
        postJsonToApi,
        postToApi,
        readResponseWithSizeLimit,
        readWebSocketMessageText,
        removeUndefinedEntries,
        resolve,
        resolveFullMediaType,
        resolveProviderReference,
        retryWithExponentialBackoff,
        safeParseJSON,
        safeValidateTypes,
        secureJsonParse,
        serializeModelOptions,
        stripFileExtension,
        toWebSocketUrl,
        tool_2 as tool,
        validateBaseURL,
        validateDownloadUrl,
        validateTypes,
        waitForWebSocketBufferDrain,
        withUserAgentSuffix,
        withoutTrailingSlash,
        zodSchema_2 as zodSchema,
        StandardJSONSchemaV1,
        StandardSchemaV1,
        StandardTypedV1
    }
}

declare namespace _ai_sdk_provider_utils_2 {
    export {
        getErrorMessage,
        WORKFLOW_DESERIALIZE,
        WORKFLOW_SERIALIZE,
        EventSourceMessage,
        EventSourceParserStream,
        Arrayable_2 as Arrayable,
        AssistantContent,
        AssistantModelMessage,
        Context_2 as Context,
        CustomPart_2 as CustomPart,
        DEFAULT_MAX_DOWNLOAD_SIZE_2 as DEFAULT_MAX_DOWNLOAD_SIZE,
        DataContent,
        DelayedPromise_2 as DelayedPromise,
        DownloadError,
        DynamicTool_2 as DynamicTool,
        TRANSCRIPTION_STREAM_AUDIO_DONE_FRAME_TYPE_2 as EXPERIMENTAL_TRANSCRIPTION_STREAM_AUDIO_DONE_FRAME_TYPE,
        TRANSCRIPTION_STREAM_START_FRAME_TYPE_2 as EXPERIMENTAL_TRANSCRIPTION_STREAM_START_FRAME_TYPE,
        ExecutableTool_2 as ExecutableTool,
        Experimental_SandboxProcess,
        Experimental_SandboxSession,
        ToolCallerDefinition as Experimental_ToolCallerDefinition,
        Experimental_ToolCallerTool,
        TranscriptionStreamClientFrame_2 as Experimental_TranscriptionStreamClientFrame,
        TranscriptionStreamStartFrame_2 as Experimental_TranscriptionStreamStartFrame,
        FetchFunction_2 as FetchFunction,
        FileData_2 as FileData,
        FileDataData_2 as FileDataData,
        FileDataReference_2 as FileDataReference,
        FileDataText_2 as FileDataText,
        FileDataUrl_2 as FileDataUrl,
        FilePart,
        FlexibleSchema,
        FunctionTool_2 as FunctionTool,
        HasRequiredKey_2 as HasRequiredKey,
        IdGenerator,
        ImagePart,
        InferSchema,
        InferToolContext_2 as InferToolContext,
        InferToolInput,
        InferToolOutput,
        InferToolSetContext_2 as InferToolSetContext,
        LazySchema_2 as LazySchema,
        MaybePromiseLike_2 as MaybePromiseLike,
        ModelMessage,
        ParseResult_2 as ParseResult,
        ProviderDefinedTool_2 as ProviderDefinedTool,
        ProviderDefinedToolFactory_2 as ProviderDefinedToolFactory,
        ProviderDefinedToolFactoryWithOutputSchema_2 as ProviderDefinedToolFactoryWithOutputSchema,
        ProviderExecutedTool_2 as ProviderExecutedTool,
        ProviderExecutedToolFactory_2 as ProviderExecutedToolFactory,
        ProviderOptions_2 as ProviderOptions,
        ProviderReference_3 as ProviderReference,
        ReasoningFilePart_2 as ReasoningFilePart,
        ReasoningPart_2 as ReasoningPart,
        Resolvable_2 as Resolvable,
        ResponseHandler_2 as ResponseHandler,
        RetryDelayProvider_2 as RetryDelayProvider,
        RetryErrorFactory_2 as RetryErrorFactory,
        RetryErrorReason_3 as RetryErrorReason,
        RetryFunction_2 as RetryFunction,
        Schema,
        SerializationError_2 as SerializationError,
        ShouldRetryFunction_2 as ShouldRetryFunction,
        StreamingToolCallDelta_2 as StreamingToolCallDelta,
        StreamingToolCallTracker_2 as StreamingToolCallTracker,
        StreamingToolCallTrackerOptions_2 as StreamingToolCallTrackerOptions,
        SystemModelMessage,
        TextPart,
        Tool,
        ToolApprovalRequest,
        ToolApprovalResponse,
        ToolCall_2 as ToolCall,
        ToolCallPart,
        ToolContent,
        ToolExecuteFunction,
        ToolExecutionOptions,
        ToolModelMessage,
        ToolNameMapping_2 as ToolNameMapping,
        ToolNeedsApprovalFunction_2 as ToolNeedsApprovalFunction,
        ToolResult_2 as ToolResult,
        ToolResultOutput_2 as ToolResultOutput,
        ToolResultPart,
        ToolSet,
        UserContent,
        UserModelMessage,
        VERSION_2 as VERSION,
        ValidationResult_2 as ValidationResult,
        WebSocketConnection_2 as WebSocketConnection,
        WebSocketConstructor_2 as WebSocketConstructor,
        WebSocketLike_2 as WebSocketLike,
        asArray_2 as asArray,
        asSchema,
        cancelResponseBody_2 as cancelResponseBody,
        combineHeaders_2 as combineHeaders,
        connectToWebSocket_2 as connectToWebSocket,
        convertAsyncIteratorToReadableStream_2 as convertAsyncIteratorToReadableStream,
        convertBase64ToUint8Array_2 as convertBase64ToUint8Array,
        convertImageModelFileToDataUri_2 as convertImageModelFileToDataUri,
        convertInlineFileDataToUint8Array_2 as convertInlineFileDataToUint8Array,
        convertToBase64_2 as convertToBase64,
        convertToFormData_2 as convertToFormData,
        convertUint8ArrayToBase64_2 as convertUint8ArrayToBase64,
        createBinaryResponseHandler_2 as createBinaryResponseHandler,
        createEventSourceResponseHandler_2 as createEventSourceResponseHandler,
        createIdGenerator,
        createJsonErrorResponseHandler_2 as createJsonErrorResponseHandler,
        createJsonResponseHandler_2 as createJsonResponseHandler,
        createProviderDefinedToolFactory_2 as createProviderDefinedToolFactory,
        createProviderDefinedToolFactoryWithOutputSchema_2 as createProviderDefinedToolFactoryWithOutputSchema,
        createProviderExecutedToolFactory_2 as createProviderExecutedToolFactory,
        createStatusCodeErrorResponseHandler_2 as createStatusCodeErrorResponseHandler,
        createToolNameMapping_2 as createToolNameMapping,
        delay_2 as delay,
        detectMediaType_2 as detectMediaType,
        downloadBlob_2 as downloadBlob,
        dynamicTool,
        executeTool_2 as executeTool,
        getToolCaller as experimental_getToolCaller,
        parseTranscriptionStreamClientFrame_2 as experimental_parseTranscriptionStreamClientFrame,
        parseTranscriptionStreamPart_2 as experimental_parseTranscriptionStreamPart,
        serializeTranscriptionStreamPart_2 as experimental_serializeTranscriptionStreamPart,
        experimental_toolCaller,
        extractLines_2 as extractLines,
        extractResponseHeaders_2 as extractResponseHeaders,
        fetchWithValidatedRedirects_2 as fetchWithValidatedRedirects,
        filterNullable_2 as filterNullable,
        generateId,
        getFromApi_2 as getFromApi,
        getRuntimeEnvironmentUserAgent_2 as getRuntimeEnvironmentUserAgent,
        getTopLevelMediaType_2 as getTopLevelMediaType,
        getWebSocketConstructor_2 as getWebSocketConstructor,
        injectJsonInstructionIntoMessages_2 as injectJsonInstructionIntoMessages,
        isAbortError_2 as isAbortError,
        isBrowserRuntime_2 as isBrowserRuntime,
        isBuffer_2 as isBuffer,
        isCustomReasoning_2 as isCustomReasoning,
        isExecutableTool_2 as isExecutableTool,
        isFullMediaType_2 as isFullMediaType,
        isNonNullable_2 as isNonNullable,
        isParsableJson_2 as isParsableJson,
        isProviderReference_2 as isProviderReference,
        isRecord,
        isSameOrigin_2 as isSameOrigin,
        isUrlSupported_2 as isUrlSupported,
        jsonSchema,
        lazySchema_2 as lazySchema,
        loadApiKey_2 as loadApiKey,
        loadOptionalSetting_2 as loadOptionalSetting,
        loadSetting_2 as loadSetting,
        mapReasoningToProviderBudget_2 as mapReasoningToProviderBudget,
        mapReasoningToProviderEffort_2 as mapReasoningToProviderEffort,
        mediaTypeToExtension_2 as mediaTypeToExtension,
        normalizeHeaders_2 as normalizeHeaders,
        parseJSON_2 as parseJSON,
        parseJsonEventStream,
        parseProviderOptions_2 as parseProviderOptions,
        postFormDataToApi_2 as postFormDataToApi,
        postJsonToApi_2 as postJsonToApi,
        postToApi_2 as postToApi,
        readResponseWithSizeLimit_2 as readResponseWithSizeLimit,
        readWebSocketMessageText_2 as readWebSocketMessageText,
        removeUndefinedEntries_2 as removeUndefinedEntries,
        resolve_2 as resolve,
        resolveFullMediaType_2 as resolveFullMediaType,
        resolveProviderReference_2 as resolveProviderReference,
        retryWithExponentialBackoff_2 as retryWithExponentialBackoff,
        safeParseJSON_2 as safeParseJSON,
        safeValidateTypes_2 as safeValidateTypes,
        secureJsonParse_2 as secureJsonParse,
        serializeModelOptions_2 as serializeModelOptions,
        stripFileExtension_2 as stripFileExtension,
        toWebSocketUrl_2 as toWebSocketUrl,
        tool,
        validateBaseURL_2 as validateBaseURL,
        validateDownloadUrl_2 as validateDownloadUrl,
        validateTypes_2 as validateTypes,
        waitForWebSocketBufferDrain_2 as waitForWebSocketBufferDrain,
        withUserAgentSuffix_2 as withUserAgentSuffix,
        withoutTrailingSlash_2 as withoutTrailingSlash,
        zodSchema,
        StandardJSONSchemaV1,
        StandardSchemaV1,
        StandardTypedV1
    }
}

export declare const AI_SDK_TELEMETRY_TRACING_CHANNEL = "ai:telemetry";

/**
 * Custom error class for AI SDK related errors.
 * @extends Error
 */
export declare class AISDKError extends Error {
    private readonly [symbol$e];
    /**
     * The underlying cause of the error, if any.
     */
    readonly cause?: unknown;
    /**
     * Creates an AI SDK Error.
     *
     * @param {Object} params - The parameters for creating the error.
     * @param {string} params.name - The name of the error.
     * @param {string} params.message - The error message.
     * @param {unknown} [params.cause] - The underlying cause of the error.
     */
    constructor({ name, message, cause, }: {
        name: string;
        message: string;
        cause?: unknown;
    });
    /**
     * Checks if the given error is an AI SDK Error.
     * @param {unknown} error - The error to check.
     * @returns {boolean} True if the error is an AI SDK Error, false otherwise.
     */
    static isInstance(error: unknown): error is AISDKError;
    protected static hasMarker(error: unknown, marker: string): boolean;
}

/**
 * Custom error class for AI SDK related errors.
 * @extends Error
 */
declare class AISDKError_2 extends Error {
    private readonly [symbol$e_2];
    /**
     * The underlying cause of the error, if any.
     */
    readonly cause?: unknown;
    /**
     * Creates an AI SDK Error.
     *
     * @param {Object} params - The parameters for creating the error.
     * @param {string} params.name - The name of the error.
     * @param {string} params.message - The error message.
     * @param {unknown} [params.cause] - The underlying cause of the error.
     */
    constructor({ name, message, cause, }: {
        name: string;
        message: string;
        cause?: unknown;
    });
    /**
     * Checks if the given error is an AI SDK Error.
     * @param {unknown} error - The error to check.
     * @returns {boolean} True if the error is an AI SDK Error, false otherwise.
     */
    static isInstance(error: unknown): error is AISDKError_2;
    protected static hasMarker(error: unknown, marker: string): boolean;
}

export declare class APICallError extends AISDKError {
    private readonly [symbol$d];
    readonly url: string;
    readonly requestBodyValues: unknown;
    readonly statusCode?: number;
    readonly responseHeaders?: Record<string, string>;
    readonly responseBody?: string;
    readonly isRetryable: boolean;
    readonly data?: unknown;
    constructor({ message, url, requestBodyValues, statusCode, responseHeaders, responseBody, cause, isRetryable, // server error
        data, }: {
        message: string;
        url: string;
        requestBodyValues: unknown;
        statusCode?: number;
        responseHeaders?: Record<string, string>;
        responseBody?: string;
        cause?: unknown;
        isRetryable?: boolean;
        data?: unknown;
    });
    static isInstance(error: unknown): error is APICallError;
}

declare class APICallError_2 extends AISDKError_2 {
    private readonly [symbol$d_2];
    readonly url: string;
    readonly requestBodyValues: unknown;
    readonly statusCode?: number;
    readonly responseHeaders?: Record<string, string>;
    readonly responseBody?: string;
    readonly isRetryable: boolean;
    readonly data?: unknown;
    constructor({ message, url, requestBodyValues, statusCode, responseHeaders, responseBody, cause, isRetryable, // server error
        data, }: {
        message: string;
        url: string;
        requestBodyValues: unknown;
        statusCode?: number;
        responseHeaders?: Record<string, string>;
        responseBody?: string;
        cause?: unknown;
        isRetryable?: boolean;
        data?: unknown;
    });
    static isInstance(error: unknown): error is APICallError_2;
}

/**
 * A value that can be provided either as a single item, an array of items,
 * or be left undefined.
 */
declare type Arrayable<T> = T | T[] | undefined;

/**
 * A value that can be provided either as a single item, an array of items,
 * or be left undefined.
 */
declare type Arrayable_2<T> = T | T[] | undefined;

/**
 * Normalizes a possibly undefined or non-array value into an array.
 */
declare function asArray<T>(value: Arrayable<T>): T[];

/**
 * Normalizes a possibly undefined or non-array value into an array.
 */
declare function asArray_2<T>(value: Arrayable_2<T>): T[];

export declare function asSchema<OBJECT>(schema: FlexibleSchema<OBJECT> | undefined): Schema<OBJECT>;

declare function asSchema_2<OBJECT>(schema: FlexibleSchema_2<OBJECT> | undefined): Schema_2<OBJECT>;

/**
 * Content of an assistant message.
 * It can be a string or an array of text, image, reasoning, redacted reasoning, and tool call parts.
 */
export declare type AssistantContent = string | Array<TextPart | CustomPart_2 | FilePart | ReasoningPart_2 | ReasoningFilePart_2 | ToolCallPart | ToolResultPart | ToolApprovalRequest>;

/**
 * Content of an assistant message.
 * It can be a string or an array of text, image, reasoning, redacted reasoning, and tool call parts.
 */
declare type AssistantContent_2 = string | Array<TextPart_2 | CustomPart | FilePart_2 | ReasoningPart | ReasoningFilePart | ToolCallPart_2 | ToolResultPart_2 | ToolApprovalRequest_2>;

/**
 * An assistant message. It can contain text, tool calls, or a combination of text and tool calls.
 */
export declare type AssistantModelMessage = {
    role: 'assistant';
    content: AssistantContent;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
};

/**
 * An assistant message. It can contain text, tool calls, or a combination of text and tool calls.
 */
declare type AssistantModelMessage_2 = {
    role: 'assistant';
    content: AssistantContent_2;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
};

export declare const assistantModelMessageSchema: z.ZodType<AssistantModelMessage>;

declare type asUITool<TOOL extends UITool | Tool> = TOOL extends Tool ? InferUITool<TOOL> : TOOL;

/**
 * A type that combines AsyncIterable and ReadableStream.
 * This allows a ReadableStream to be consumed using for-await-of syntax.
 */
export declare type AsyncIterableStream<T> = AsyncIterable<T> & ReadableStream<T>;

/**
 * Common properties shared by function-style tools.
 */
declare type BaseFunctionTool<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context | unknown | never = any> = BaseTool<INPUT, OUTPUT, CONTEXT> & {
    /**
     * Optional description of what the tool does.
     *
     * Included in the tool definition sent to the language model so it can
     * decide when and how to call the tool.
     *
     * Provide a string for a fixed description, or a function that returns a
     * string from the current `context` (and optional `experimental_sandbox`) when the
     * description should vary per call.
     */
    description?: string | ((options: {
        context: NoInfer<CONTEXT>;
        experimental_sandbox?: SandboxSession;
    }) => string);
    /**
     * Strict mode setting for the tool.
     *
     * Providers that support strict mode will use this setting to determine
     * how the input should be generated. Strict mode will always produce
     * valid inputs, but it might limit what input schemas are supported.
     */
    strict?: boolean;
    /**
     * An optional list of input examples that show the language
     * model what the input should look like.
     */
    inputExamples?: Array<{
        input: NoInfer<INPUT>;
    }>;
    id?: never;
    isProviderExecuted?: never;
    args?: never;
    supportsDeferredResults?: never;
};

/**
 * Common properties shared by function-style tools.
 */
declare type BaseFunctionTool_2<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context_2 | unknown | never = any> = BaseTool_2<INPUT, OUTPUT, CONTEXT> & {
    /**
     * Optional description of what the tool does.
     *
     * Included in the tool definition sent to the language model so it can
     * decide when and how to call the tool.
     *
     * Provide a string for a fixed description, or a function that returns a
     * string from the current `context` (and optional `experimental_sandbox`) when the
     * description should vary per call.
     */
    description?: string | ((options: {
        context: NoInfer<CONTEXT>;
        experimental_sandbox?: Experimental_SandboxSession;
    }) => string);
    /**
     * Strict mode setting for the tool.
     *
     * Providers that support strict mode will use this setting to determine
     * how the input should be generated. Strict mode will always produce
     * valid inputs, but it might limit what input schemas are supported.
     */
    strict?: boolean;
    /**
     * An optional list of input examples that show the language
     * model what the input should look like.
     */
    inputExamples?: Array<{
        input: NoInfer<INPUT>;
    }>;
    id?: never;
    isProviderExecuted?: never;
    args?: never;
    supportsDeferredResults?: never;
};

/**
 * Common properties shared by provider tools.
 */
declare type BaseProviderTool<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context | unknown | never = any> = BaseTool<INPUT, OUTPUT, CONTEXT> & {
    type: 'provider';
    /**
     * The ID of the tool. Must follow the format `<provider-name>.<unique-tool-name>`.
     */
    id: `${string}.${string}`;
    /**
     * The arguments for configuring the tool. Must match the expected arguments defined by the provider for this tool.
     */
    args: Record<string, unknown>;
    description?: never;
    strict?: never;
    inputExamples?: never;
};

/**
 * Common properties shared by provider tools.
 */
declare type BaseProviderTool_2<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context_2 | unknown | never = any> = BaseTool_2<INPUT, OUTPUT, CONTEXT> & {
    type: 'provider';
    /**
     * The ID of the tool. Must follow the format `<provider-name>.<unique-tool-name>`.
     */
    id: `${string}.${string}`;
    /**
     * The arguments for configuring the tool. Must match the expected arguments defined by the provider for this tool.
     */
    args: Record<string, unknown>;
    description?: never;
    strict?: never;
    inputExamples?: never;
};

/**
 * Common properties shared by all tool kinds.
 */
declare type BaseTool<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context | unknown | never = any> = {
    /**
     * An optional title of the tool.
     *
     * @deprecated Use `providerMetadata` for source-specific tool display metadata.
     */
    title?: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
    /**
     * Optional metadata about the tool itself (e.g. its source).
     *
     * Unlike `providerOptions`, this metadata is not sent to the language
     * model. Instead, it is propagated onto the resulting tool call's
     * `toolMetadata` so consumers can read it from tool call / result parts
     * and UI message parts. This is useful for sources of dynamic tools (e.g.
     * an MCP server) to identify themselves.
     */
    metadata?: JSONObject;
    /**
     * The schema of the input that the tool expects.
     * The language model will use this to generate the input.
     * It is also used to validate the output of the language model.
     *
     * You can use descriptions on the schema properties to make the input understandable for the language model.
     */
    inputSchema: FlexibleSchema_2<INPUT>;
    /**
     * An optional schema describing the context that the tool expects.
     *
     * The context is passed to execute function as part of the execution options.
     */
    contextSchema?: FlexibleSchema_2<CONTEXT>;
    /**
     * Whether the tool needs approval before it can be executed.
     *
     * @deprecated Tool approval is handled on a `generateText` / `streamText` level now.
     */
    needsApproval?: boolean | ToolNeedsApprovalFunction<[
    INPUT
    ] extends [never] ? unknown : INPUT, NoInfer<CONTEXT>>;
    /**
     * Optional function that is called when the model starts generating the tool input.
     * In non-streaming contexts, it is called immediately before `onInputAvailable`.
     */
    onInputStart?: (options: ToolExecutionOptions_2<NoInfer<CONTEXT>>) => void | PromiseLike<void>;
    /**
     * Optional function that is called when an argument streaming delta is available.
     * Only called when the tool is used in a streaming context.
     */
    onInputDelta?: (options: {
        inputTextDelta: string;
    } & ToolExecutionOptions_2<NoInfer<CONTEXT>>) => void | PromiseLike<void>;
    /**
     * Optional function that is called when a tool call can be started,
     * even if the execute function is not provided.
     */
    onInputAvailable?: (options: {
        input: [INPUT] extends [never] ? unknown : INPUT;
    } & ToolExecutionOptions_2<NoInfer<CONTEXT>>) => void | PromiseLike<void>;
    /**
     * Optional conversion function that maps the tool result to an output that can be used by the language model.
     *
     * If not provided, the tool result will be sent as a JSON object.
     *
     * This function is invoked on the server by `convertToModelMessages`, so ensure that you pass the same "tools" (ToolSet) to both "convertToModelMessages" and "streamText" (or other generation APIs).
     */
    toModelOutput?: (options: {
        /**
         * The ID of the tool call. You can use it e.g. when sending tool-call related information with stream data.
         */
        toolCallId: string;
        /**
         * The input of the tool call.
         */
        input: [INPUT] extends [never] ? unknown : INPUT;
        /**
         * The output of the tool call.
         */
        output: 0 extends 1 & OUTPUT ? any : [OUTPUT] extends [never] ? any : NoInfer<OUTPUT>;
    }) => ToolResultOutput | PromiseLike<ToolResultOutput>;
} & ToolOutputProperties<INPUT, OUTPUT, NoInfer<CONTEXT>>;

/**
 * Common properties shared by all tool kinds.
 */
declare type BaseTool_2<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context_2 | unknown | never = any> = {
    /**
     * An optional title of the tool.
     *
     * @deprecated Use `providerMetadata` for source-specific tool display metadata.
     */
    title?: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Optional metadata about the tool itself (e.g. its source).
     *
     * Unlike `providerOptions`, this metadata is not sent to the language
     * model. Instead, it is propagated onto the resulting tool call's
     * `toolMetadata` so consumers can read it from tool call / result parts
     * and UI message parts. This is useful for sources of dynamic tools (e.g.
     * an MCP server) to identify themselves.
     */
    metadata?: JSONObject;
    /**
     * The schema of the input that the tool expects.
     * The language model will use this to generate the input.
     * It is also used to validate the output of the language model.
     *
     * You can use descriptions on the schema properties to make the input understandable for the language model.
     */
    inputSchema: FlexibleSchema<INPUT>;
    /**
     * An optional schema describing the context that the tool expects.
     *
     * The context is passed to execute function as part of the execution options.
     */
    contextSchema?: FlexibleSchema<CONTEXT>;
    /**
     * Whether the tool needs approval before it can be executed.
     *
     * @deprecated Tool approval is handled on a `generateText` / `streamText` level now.
     */
    needsApproval?: boolean | ToolNeedsApprovalFunction_2<[
    INPUT
    ] extends [never] ? unknown : INPUT, NoInfer<CONTEXT>>;
    /**
     * Optional function that is called when the model starts generating the tool input.
     * In non-streaming contexts, it is called immediately before `onInputAvailable`.
     */
    onInputStart?: (options: ToolExecutionOptions<NoInfer<CONTEXT>>) => void | PromiseLike<void>;
    /**
     * Optional function that is called when an argument streaming delta is available.
     * Only called when the tool is used in a streaming context.
     */
    onInputDelta?: (options: {
        inputTextDelta: string;
    } & ToolExecutionOptions<NoInfer<CONTEXT>>) => void | PromiseLike<void>;
    /**
     * Optional function that is called when a tool call can be started,
     * even if the execute function is not provided.
     */
    onInputAvailable?: (options: {
        input: [INPUT] extends [never] ? unknown : INPUT;
    } & ToolExecutionOptions<NoInfer<CONTEXT>>) => void | PromiseLike<void>;
    /**
     * Optional conversion function that maps the tool result to an output that can be used by the language model.
     *
     * If not provided, the tool result will be sent as a JSON object.
     *
     * This function is invoked on the server by `convertToModelMessages`, so ensure that you pass the same "tools" (ToolSet) to both "convertToModelMessages" and "streamText" (or other generation APIs).
     */
    toModelOutput?: (options: {
        /**
         * The ID of the tool call. You can use it e.g. when sending tool-call related information with stream data.
         */
        toolCallId: string;
        /**
         * The input of the tool call.
         */
        input: [INPUT] extends [never] ? unknown : INPUT;
        /**
         * The output of the tool call.
         */
        output: 0 extends 1 & OUTPUT ? any : [OUTPUT] extends [never] ? any : NoInfer<OUTPUT>;
    }) => ToolResultOutput_2 | PromiseLike<ToolResultOutput_2>;
} & ToolOutputProperties_2<INPUT, OUTPUT, NoInfer<CONTEXT>>;

declare type BaseToolCall = {
    type: 'tool-call';
    toolCallId: string;
    providerExecuted?: boolean;
    providerMetadata?: ProviderMetadata;
    toolMetadata?: JSONObject_2;
};

declare type BaseToolExecutionEndFields = {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /** Execution time of the tool call in milliseconds. */
    readonly toolExecutionMs: number;
    /**
     * Messages that were sent to the language model to initiate the response that contained the tool call.
     * The messages **do not** include the system prompt nor the assistant response that contained the tool call.
     */
    readonly messages: ModelMessage[];
};

declare type BaseToolExecutionStartFields = {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /**
     * Messages that were sent to the language model to initiate the response that contained the tool call.
     * The messages **do not** include the system prompt nor the assistant response that contained the tool call.
     */
    readonly messages: ModelMessage[];
};

/**
 * A callback function that can be used with `notify`.
 */
declare type Callback<EVENT> = (event: EVENT) => PromiseLike<void> | void;

export declare function callCompletionApi({ api, prompt, credentials, headers, body, streamProtocol, setCompletion, setLoading, setError, setAbortController, onFinish, onError, fetch, }: {
    api: string;
    prompt: string;
    credentials: RequestCredentials | undefined;
    headers: HeadersInit | undefined;
    body: Record<string, any>;
    streamProtocol: 'data' | 'text' | undefined;
    setCompletion: (completion: string) => void;
    setLoading: (loading: boolean) => void;
    setError: (error: Error | undefined) => void;
    setAbortController: (abortController: AbortController | null) => void;
    onFinish: ((prompt: string, completion: string) => void) | undefined;
    onError: ((error: Error) => void) | undefined;
    fetch: ReturnType<typeof getOriginalFetch> | undefined;
}): Promise<string | null | undefined>;

/** @deprecated Use `LanguageModelCallOptions` combined with `RequestOptions` instead. */
export declare type CallSettings = LanguageModelCallOptions & Omit<RequestOptions, 'timeout'>;

/**
 * Warning from the model provider for this call. The call will proceed, but e.g.
 * some settings might not be supported, which can lead to suboptimal results.
 */
export declare type CallWarning = SharedV4Warning_2;

/**
 * Cancels a response body to release the underlying connection.
 *
 * When a fetch Response is rejected without consuming its body (e.g. a failed
 * status code, an open-redirect rejection, or a Content-Length that exceeds the
 * size limit), the underlying TCP socket is not returned to the connection pool
 * and may stay open until the process runs out of file descriptors. Cancelling
 * the body avoids this leak.
 *
 * Errors thrown while cancelling are ignored: the body may already be locked,
 * disturbed, or absent, none of which should mask the original rejection.
 */
declare function cancelResponseBody(response: Response): Promise<void>;

/**
 * Cancels a response body to release the underlying connection.
 *
 * When a fetch Response is rejected without consuming its body (e.g. a failed
 * status code, an open-redirect rejection, or a Content-Length that exceeds the
 * size limit), the underlying TCP socket is not returned to the connection pool
 * and may stay open until the process runs out of file descriptors. Cancelling
 * the body avoids this leak.
 *
 * Errors thrown while cancelling are ignored: the body may already be locked,
 * disturbed, or absent, none of which should mask the original rejection.
 */
declare function cancelResponseBody_2(response: Response): Promise<void>;

/**
 * Function that can be called to add a tool approval response to the chat.
 */
export declare type ChatAddToolApproveResponseFunction = ({ id, approved, reason, options, }: {
    id: string;
    /**
     * Flag indicating whether the approval was granted or denied.
     */
    approved: boolean;
    /**
     * Optional reason for the approval or denial.
     */
    reason?: string;
    /**
     * Optional request options to be used if `sendAutomaticallyWhen` callback returns true.
     */
    options?: ChatRequestOptions;
}) => void | PromiseLike<void>;

/**
 * Function that can be called to add a tool output to the chat.
 */
export declare type ChatAddToolOutputFunction<UI_MESSAGE extends UIMessage> = <TOOL extends keyof InferUIMessageTools<UI_MESSAGE>>({ state, tool, toolCallId, output, errorText, options, }: {
    /**
     * Name of the tool that was called.
     */
    tool: TOOL;
    /**
     * Identifier of the tool call to add output for.
     */
    toolCallId: string;
    /**
     * Optional request options to be used if `sendAutomaticallyWhen` callback returns true.
     */
    options?: ChatRequestOptions;
} & ({
    state?: 'output-available';
    output: InferUIMessageTools<UI_MESSAGE>[TOOL]['output'];
    errorText?: never;
} | {
    state: 'output-error';
    output?: never;
    errorText: string;
})) => void | PromiseLike<void>;

export declare interface ChatInit<UI_MESSAGE extends UIMessage> {
    /**
     * A unique identifier for the chat. If not provided, a random one will be
     * generated.
     */
    id?: string;
    messageMetadataSchema?: FlexibleSchema<InferUIMessageMetadata<UI_MESSAGE>>;
    dataPartSchemas?: UIDataTypesToSchemas<InferUIMessageData<UI_MESSAGE>>;
    messages?: UI_MESSAGE[];
    /**
     * A way to provide a function that is going to be used for ids for messages and the chat.
     * If not provided the default AI SDK `generateId` is used.
     */
    generateId?: IdGenerator;
    transport?: ChatTransport<UI_MESSAGE>;
    /**
     * Callback function to be called when an error is encountered.
     */
    onError?: ChatOnErrorCallback;
    /**
     * Optional callback function that is invoked when a tool call is received.
     * Intended for automatic client-side tool execution.
     *
     * To add the tool output, call `addToolOutput` without awaiting it inside
     * this callback. The callback's return value is not used.
     */
    onToolCall?: ChatOnToolCallCallback<UI_MESSAGE>;
    /**
     * Function that is called when the assistant response has finished streaming.
     */
    onFinish?: ChatOnFinishCallback<UI_MESSAGE>;
    /**
     * Optional callback function that is called when a data part is received.
     *
     * @param data The data part that was received.
     */
    onData?: ChatOnDataCallback<UI_MESSAGE>;
    /**
     * When provided, this function will be called when the stream is finished or a tool call is added
     * to determine if the current messages should be resubmitted.
     */
    sendAutomaticallyWhen?: (options: {
        messages: UI_MESSAGE[];
    }) => boolean | PromiseLike<boolean>;
}

export declare type ChatOnDataCallback<UI_MESSAGE extends UIMessage> = (dataPart: DataUIPart<InferUIMessageData<UI_MESSAGE>>) => void;

export declare type ChatOnErrorCallback = (error: Error) => void;

/**
 * Function that is called when the assistant response has finished streaming.
 *
 * @param message The assistant message that was streamed.
 * @param messages The full chat history, including the assistant message.
 *
 * @param isAbort Indicates whether the request has been aborted.
 * @param isDisconnect Indicates whether the request has been ended by a network error.
 * @param isError Indicates whether the request has been ended by an error.
 * @param finishReason The reason why the generation finished.
 */
export declare type ChatOnFinishCallback<UI_MESSAGE extends UIMessage> = (options: {
    message: UI_MESSAGE;
    messages: UI_MESSAGE[];
    isAbort: boolean;
    isDisconnect: boolean;
    isError: boolean;
    finishReason?: FinishReason;
}) => void;

export declare type ChatOnToolCallCallback<UI_MESSAGE extends UIMessage = UIMessage> = (options: {
    toolCall: InferUIMessageToolCall<UI_MESSAGE>;
}) => void | PromiseLike<void>;

export declare type ChatRequestOptions = {
    /**
     * Additional headers that should be to be passed to the API endpoint.
     */
    headers?: Record<string, string> | Headers;
    /**
     * Additional body JSON properties that should be sent to the API endpoint.
     */
    body?: object;
    metadata?: unknown;
};

export declare interface ChatState<UI_MESSAGE extends UIMessage> {
    status: ChatStatus;
    error: Error | undefined;
    messages: UI_MESSAGE[];
    pushMessage: (message: UI_MESSAGE) => void;
    popMessage: () => void;
    replaceMessage: (index: number, message: UI_MESSAGE) => void;
    snapshot: <T>(thing: T) => T;
}

export declare type ChatStatus = 'submitted' | 'streaming' | 'ready' | 'error';

/**
 * Transport interface for handling chat message communication and streaming.
 *
 * The `ChatTransport` interface provides fine-grained control over how messages
 * are sent to API endpoints and how responses are processed. This enables
 * alternative communication protocols like WebSockets, custom authentication
 * patterns, or specialized backend integrations.
 *
 * @template UI_MESSAGE - The UI message type extending UIMessage
 */
export declare interface ChatTransport<UI_MESSAGE extends UIMessage> {
    /**
     * Sends messages to the chat API endpoint and returns a streaming response.
     *
     * This method handles both new message submission and message regeneration.
     * It supports real-time streaming of responses through UIMessageChunk events.
     *
     * @param options - Configuration object containing:
     * @param options.trigger - The type of message submission:
     *   - `'submit-message'`: Submitting a new user message
     *   - `'regenerate-message'`: Regenerating an assistant response
     * @param options.chatId - Unique identifier for the chat session
     * @param options.messageId - ID of the message to regenerate (for regenerate-message trigger) or undefined for new messages
     * @param options.messages - Array of UI messages representing the conversation history
     * @param options.abortSignal - Signal to abort the request if needed
     * @param options.headers - Additional HTTP headers to include in the request
     * @param options.body - Additional JSON properties to include in the request body
     * @param options.metadata - Custom metadata to attach to the request
     *
     * @returns Promise resolving to a ReadableStream of UIMessageChunk objects.
     *   The stream emits various chunk types like:
     *   - `text-start`, `text-delta`, `text-end`: For streaming text content
     *   - `tool-input-start`, `tool-input-delta`, `tool-input-available`: For tool calls
     *   - `data-part-start`, `data-part-delta`, `data-part-available`: For data parts
     *   - `error`: For error handling
     *
     * @throws Error when the API request fails or response is invalid
     */
    sendMessages: (options: {
        /** The type of message submission - either new message or regeneration */
        trigger: 'submit-message' | 'regenerate-message';
        /** Unique identifier for the chat session */
        chatId: string;
        /** ID of the message to regenerate, or undefined for new messages */
        messageId: string | undefined;
        /** Array of UI messages representing the conversation history */
        messages: UI_MESSAGE[];
        /** Signal to abort the request if needed */
        abortSignal: AbortSignal | undefined;
    } & ChatRequestOptions) => Promise<ReadableStream<UIMessageChunk>>;
    /**
     * Reconnects to an existing streaming response for the specified chat session.
     *
     * This method is used to resume streaming when a connection is interrupted
     * or when resuming a chat session. It's particularly useful for maintaining
     * continuity in long-running conversations or recovering from network issues.
     *
     * @param options - Configuration object containing:
     * @param options.chatId - Unique identifier for the chat session to reconnect to
     * @param options.headers - Additional HTTP headers to include in the reconnection request
     * @param options.body - Additional JSON properties to include in the request body
     * @param options.metadata - Custom metadata to attach to the request
     *
     * @returns Promise resolving to:
     *   - `ReadableStream<UIMessageChunk>`: If an active stream is found and can be resumed
     *   - `null`: If no active stream exists for the specified chat session (e.g., response already completed)
     *
     * @throws Error when the reconnection request fails or response is invalid
     */
    reconnectToStream: (options: {
        /** Unique identifier for the chat session to reconnect to */
        chatId: string;
    } & ChatRequestOptions) => Promise<ReadableStream<UIMessageChunk> | null>;
}

/**
 * Detects the first chunk in a buffer.
 *
 * @param buffer - The buffer to detect the first chunk in.
 *
 * @returns The first detected chunk, or `undefined` if no chunk was detected.
 */
export declare type ChunkDetector = (buffer: string) => string | undefined | null;

declare function combineHeaders(...headers: Array<Record<string, string | undefined> | undefined>): Record<string, string | undefined>;

declare function combineHeaders_2(...headers: Array<Record<string, string | undefined> | undefined>): Record<string, string | undefined>;

export declare type CompletionRequestOptions = {
    /**
     * An optional object of headers to be passed to the API endpoint.
     */
    headers?: Record<string, string> | Headers;
    /**
     * An optional object to be passed to the API endpoint.
     */
    body?: object;
};

/**
 * Opens a WebSocket for a provider model, owning the transport-generic layer
 * (analogous to `postToApi` for HTTP): constructor resolution, header hygiene,
 * abort wiring, and message decoding. Callers own the URL, the auth channel
 * (subprotocols vs headers), and the wire protocol.
 */
declare function connectToWebSocket({ url, protocols, headers, webSocket, abortSignal, onOpen, onMessageText, onProcessingError, onSocketError, onClose, onAbort, }: {
    url: string | URL;
    protocols?: string | string[];
    headers?: Record<string, string | undefined>;
    webSocket?: WebSocketConstructor;
    abortSignal?: AbortSignal;
    onOpen?: (socket: WebSocketLike) => void;
    /** One decoded message. Throws and rejections go to `onProcessingError`. */
    onMessageText: (text: string) => void | PromiseLike<void>;
    /** Constructor throws and message decoding/processing failures. */
    onProcessingError: (error: unknown) => void;
    onSocketError?: () => void;
    /**
     * Receives the close code and reason when the transport provides them
     * (native `CloseEvent` / `ws` close event).
     */
    onClose?: (info: {
        code?: number;
        reason?: string;
    }) => void;
    /** Also called (without opening a socket) when the signal is already aborted. */
    onAbort?: (reason: unknown) => void;
}): WebSocketConnection;

/**
 * Opens a WebSocket for a provider model, owning the transport-generic layer
 * (analogous to `postToApi` for HTTP): constructor resolution, header hygiene,
 * abort wiring, and message decoding. Callers own the URL, the auth channel
 * (subprotocols vs headers), and the wire protocol.
 */
declare function connectToWebSocket_2({ url, protocols, headers, webSocket, abortSignal, onOpen, onMessageText, onProcessingError, onSocketError, onClose, onAbort, }: {
    url: string | URL;
    protocols?: string | string[];
    headers?: Record<string, string | undefined>;
    webSocket?: WebSocketConstructor_2;
    abortSignal?: AbortSignal;
    onOpen?: (socket: WebSocketLike_2) => void;
    /** One decoded message. Throws and rejections go to `onProcessingError`. */
    onMessageText: (text: string) => void | PromiseLike<void>;
    /** Constructor throws and message decoding/processing failures. */
    onProcessingError: (error: unknown) => void;
    onSocketError?: () => void;
    /**
     * Receives the close code and reason when the transport provides them
     * (native `CloseEvent` / `ws` close event).
     */
    onClose?: (info: {
        code?: number;
        reason?: string;
    }) => void;
    /** Also called (without opening a socket) when the signal is already aborted. */
    onAbort?: (reason: unknown) => void;
}): WebSocketConnection_2;

/**
 * Consumes a ReadableStream until it's fully read.
 *
 * This function reads the stream chunk by chunk until the stream is exhausted.
 * It doesn't process or return the data from the stream; it simply ensures
 * that the entire stream is read.
 *
 * @param options - The options for consuming the stream.
 * @param options.stream - The ReadableStream to be consumed.
 * @param options.onError - Optional callback to handle errors that occur during consumption.
 * @returns A promise that resolves when the stream is fully consumed.
 */
export declare function consumeStream({ stream, onError, }: {
    stream: ReadableStream;
    onError?: (error: unknown) => void;
}): Promise<void>;

declare type ConsumeStreamOptions = {
    onError?: ErrorHandler;
};

export declare type ContentPart<TOOLS extends ToolSet> = {
    type: 'text';
    text: string;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'custom';
    kind: `${string}.${string}`;
    providerMetadata?: ProviderMetadata;
} | ReasoningOutput | ReasoningFileOutput | ({
    type: 'source';
} & Source) | {
    type: 'file';
    file: GeneratedFile;
    providerMetadata?: ProviderMetadata;
} | ({
    type: 'tool-call';
} & TypedToolCall<TOOLS> & {
    providerMetadata?: ProviderMetadata;
}) | ({
    type: 'tool-result';
} & TypedToolResult<TOOLS> & {
    providerMetadata?: ProviderMetadata;
}) | ({
    type: 'tool-error';
} & TypedToolError<TOOLS> & {
    providerMetadata?: ProviderMetadata;
}) | ToolApprovalRequestOutput<TOOLS> | ToolApprovalResponseOutput<TOOLS>;

/**
 * A context object that is passed into tool execution.
 */
declare type Context = Record<string, unknown>;

/**
 * A context object that is passed into tool execution.
 */
declare type Context_2 = Record<string, unknown>;

/**
 * Converts an AsyncIterator to a ReadableStream.
 *
 * @template T - The type of elements produced by the AsyncIterator.
 * @param { <T>} iterator - The AsyncIterator to convert.
 * @returns {ReadableStream<T>} - A ReadableStream that provides the same data as the AsyncIterator.
 */
declare function convertAsyncIteratorToReadableStream<T>(iterator: AsyncIterator<T>): ReadableStream<T>;

/**
 * Converts an AsyncIterator to a ReadableStream.
 *
 * @template T - The type of elements produced by the AsyncIterator.
 * @param { <T>} iterator - The AsyncIterator to convert.
 * @returns {ReadableStream<T>} - A ReadableStream that provides the same data as the AsyncIterator.
 */
declare function convertAsyncIteratorToReadableStream_2<T>(iterator: AsyncIterator<T>): ReadableStream<T>;

declare function convertBase64ToUint8Array(base64String: string): Uint8Array<ArrayBuffer>;

declare function convertBase64ToUint8Array_2(base64String: string): Uint8Array<ArrayBuffer>;

/**
 * Converts data content to a base64-encoded string.
 *
 * @param content - Data content to convert.
 * @returns Base64-encoded string.
 */
export declare function convertDataContentToBase64String(content: DataContent): string;

export declare function convertFileListToFileUIParts(files: FileList | undefined): Promise<Array<FileUIPart>>;

/**
 * Convert an ImageModelV4File to a URL or data URI string.
 *
 * If the file is a URL, it returns the URL as-is.
 * If the file is base64 data, it returns a data URI with the base64 data.
 * If the file is a Uint8Array, it converts it to base64 and returns a data URI.
 */
declare function convertImageModelFileToDataUri(file: ImageModelV4File): string;

/**
 * Convert an ImageModelV4File to a URL or data URI string.
 *
 * If the file is a URL, it returns the URL as-is.
 * If the file is base64 data, it returns a data URI with the base64 data.
 * If the file is a Uint8Array, it converts it to base64 and returns a data URI.
 */
declare function convertImageModelFileToDataUri_2(file: ImageModelV4File): string;

/**
 * Converts inline file data (a tagged `data` or `text` shape) into raw bytes.
 *
 * - `{ type: 'text', text }` → UTF-8 encoded bytes
 * - `{ type: 'data', data: Uint8Array | Buffer }` → returned as-is
 * - `{ type: 'data', data: ArrayBuffer }` → wrapped in a `Uint8Array`
 * - `{ type: 'data', data: string }` → decoded as base64
 */
declare function convertInlineFileDataToUint8Array(data: InlineFileData): Uint8Array;

/**
 * Converts inline file data (a tagged `data` or `text` shape) into raw bytes.
 *
 * - `{ type: 'text', text }` → UTF-8 encoded bytes
 * - `{ type: 'data', data: Uint8Array | Buffer }` → returned as-is
 * - `{ type: 'data', data: ArrayBuffer }` → wrapped in a `Uint8Array`
 * - `{ type: 'data', data: string }` → decoded as base64
 */
declare function convertInlineFileDataToUint8Array_2(data: InlineFileData_2): Uint8Array;

declare function convertToBase64(value: string | Uint8Array): string;

declare function convertToBase64_2(value: string | Uint8Array): string;

/**
 * Converts an input object to FormData for multipart/form-data requests.
 *
 * Handles the following cases:
 * - `null` or `undefined` values are skipped
 * - Arrays with a single element are appended as a single value
 * - Arrays with multiple elements are appended with `[]` suffix (e.g., `image[]`)
 *   unless `useArrayBrackets` is set to `false`
 * - All other values are appended directly
 *
 * @param input - The input object to convert. Use a generic type for type validation.
 * @param options - Optional configuration object.
 * @param options.useArrayBrackets - Whether to add `[]` suffix for multi-element arrays.
 *   Defaults to `true`. Set to `false` for APIs that expect repeated keys without brackets.
 * @returns A FormData object containing the input values.
 *
 * @example
 * ```ts
 * type MyInput = {
 *   model: string;
 *   prompt: string;
 *   images: Blob[];
 * };
 *
 * const formData = convertToFormData<MyInput>({
 *   model: 'gpt-image-1',
 *   prompt: 'A cat',
 *   images: [blob1, blob2],
 * });
 * ```
 */
declare function convertToFormData<T extends Record<string, unknown>>(input: T, options?: {
    useArrayBrackets?: boolean;
}): FormData;

/**
 * Converts an input object to FormData for multipart/form-data requests.
 *
 * Handles the following cases:
 * - `null` or `undefined` values are skipped
 * - Arrays with a single element are appended as a single value
 * - Arrays with multiple elements are appended with `[]` suffix (e.g., `image[]`)
 *   unless `useArrayBrackets` is set to `false`
 * - All other values are appended directly
 *
 * @param input - The input object to convert. Use a generic type for type validation.
 * @param options - Optional configuration object.
 * @param options.useArrayBrackets - Whether to add `[]` suffix for multi-element arrays.
 *   Defaults to `true`. Set to `false` for APIs that expect repeated keys without brackets.
 * @returns A FormData object containing the input values.
 *
 * @example
 * ```ts
 * type MyInput = {
 *   model: string;
 *   prompt: string;
 *   images: Blob[];
 * };
 *
 * const formData = convertToFormData<MyInput>({
 *   model: 'gpt-image-1',
 *   prompt: 'A cat',
 *   images: [blob1, blob2],
 * });
 * ```
 */
declare function convertToFormData_2<T extends Record<string, unknown>>(input: T, options?: {
    useArrayBrackets?: boolean;
}): FormData;

/**
 * Converts an array of UI messages from useChat into an array of ModelMessages that can be used
 * with the AI functions (e.g. `streamText`, `generateText`).
 *
 * @param messages - The UI messages to convert.
 * @param options.tools - The tools to use.
 * @param options.ignoreIncompleteToolCalls - Whether to ignore incomplete tool calls. Default is `false`.
 * @param options.convertDataPart - Optional function to convert data parts to text or file model message parts. Returns `undefined` if the part should be ignored.
 *
 * @returns An array of ModelMessages.
 */
export declare function convertToModelMessages<UI_MESSAGE extends UIMessage>(messages: Array<Omit<UI_MESSAGE, 'id'>>, options?: {
    tools?: ToolSet;
    ignoreIncompleteToolCalls?: boolean;
    convertDataPart?: (part: DataUIPart<InferUIMessageData<UI_MESSAGE>>) => TextPart | FilePart | undefined;
}): Promise<ModelMessage[]>;

declare function convertUint8ArrayToBase64(array: Uint8Array): string;

declare function convertUint8ArrayToBase64_2(array: Uint8Array): string;

/**
 * Calculates the cosine similarity between two vectors. This is a useful metric for
 * comparing the similarity of two vectors such as embeddings.
 *
 * @param vector1 - The first vector.
 * @param vector2 - The second vector.
 *
 * @returns The cosine similarity between vector1 and vector2, or 0 if either vector is the zero vector.
 *
 * @throws {InvalidArgumentError} If the vectors do not have the same length.
 */
export declare function cosineSimilarity(vector1: number[], vector2: number[]): number;

/**
 * Runs the agent and stream the output as a UI message stream.
 *
 * @param agent - The agent to run.
 * @param uiMessages - The input UI messages.
 * @param abortSignal - The abort signal. Optional.
 * @param timeout - Timeout in milliseconds. Optional.
 * @param experimental_sandbox - The sandbox environment that is passed through to tool execution. Optional.
 * @param options - The options for the agent.
 * @param experimental_transform - The stream transformations. Optional.
 * @param onStepEnd - Callback that is called when each step ends. Optional.
 * @param onStepFinish - Deprecated alias for `onStepEnd`. Optional.
 *
 * @returns The UI message stream.
 */
export declare function createAgentUIStream<CALL_OPTIONS = never, TOOLS extends ToolSet = {}, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = never, MESSAGE_METADATA = unknown, UI_MESSAGE extends UIMessage<MESSAGE_METADATA, never, InferUITools<TOOLS>> = UIMessage<MESSAGE_METADATA, never, InferUITools<TOOLS>>>({ agent, uiMessages, options, abortSignal, timeout, experimental_sandbox: sandbox, experimental_transform, onStepEnd, onStepFinish, ...uiMessageStreamOptions }: {
    agent: Agent<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT, OUTPUT>;
    uiMessages: unknown[];
    abortSignal?: AbortSignal;
    timeout?: TimeoutConfiguration<TOOLS>;
    experimental_sandbox?: Experimental_SandboxSession;
    options?: CALL_OPTIONS;
    experimental_transform?: Arrayable_2<StreamTextTransform<TOOLS>>;
    onStepEnd?: GenerateTextOnStepEndCallback<TOOLS>;
    /** @deprecated Use `onStepEnd` instead. */
    onStepFinish?: GenerateTextOnStepFinishCallback<TOOLS>;
} & UIMessageStreamOptions<UI_MESSAGE>): Promise<AsyncIterableStream<InferUIMessageChunk<UI_MESSAGE>>>;

/**
 * Runs the agent and returns a response object with a UI message stream.
 *
 * @param agent - The agent to run.
 * @param uiMessages - The input UI messages.
 * @param abortSignal - Abort signal. Optional.
 * @param timeout - Timeout in milliseconds. Optional.
 * @param experimental_sandbox - The sandbox environment that is passed through to tool execution. Optional.
 * @param options - The options for the agent. Optional.
 * @param experimental_transform - Stream transformations. Optional.
 * @param onStepEnd - Callback that is called when each step ends. Optional.
 * @param onStepFinish - Deprecated alias for `onStepEnd`. Optional.
 * @param headers - Additional headers for the response. Optional.
 * @param status - The status code for the response. Optional.
 * @param statusText - The status text for the response. Optional.
 * @param consumeSseStream - Whether to consume the SSE stream. Optional.
 *
 * @returns The response object.
 */
export declare function createAgentUIStreamResponse<CALL_OPTIONS = never, TOOLS extends ToolSet = {}, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = never, MESSAGE_METADATA = unknown>({ headers, status, statusText, consumeSseStream, ...options }: {
    agent: Agent<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT, OUTPUT>;
    uiMessages: unknown[];
    abortSignal?: AbortSignal;
    timeout?: TimeoutConfiguration<TOOLS>;
    experimental_sandbox?: Experimental_SandboxSession;
    options?: CALL_OPTIONS;
    experimental_transform?: Arrayable_2<StreamTextTransform<TOOLS>>;
    onStepEnd?: GenerateTextOnStepEndCallback<TOOLS>;
    /** @deprecated Use `onStepEnd` instead. */
    onStepFinish?: GenerateTextOnStepFinishCallback<TOOLS>;
} & UIMessageStreamResponseInit & UIMessageStreamOptions<UIMessage<MESSAGE_METADATA, never, InferUITools<TOOLS>>>): Promise<Response>;

declare const createBinaryResponseHandler: () => ResponseHandler<Uint8Array>;

declare const createBinaryResponseHandler_2: () => ResponseHandler_2<Uint8Array>;

/**
 * Creates a download function with configurable options.
 *
 * @param options - Configuration options for the download function.
 * @param options.maxBytes - Maximum allowed download size in bytes. Default: 2 GiB.
 * @returns A download function that can be passed to `transcribe()` or `experimental_generateVideo()`.
 */
export declare function createDownload(options?: {
    maxBytes?: number;
}): ({ url, abortSignal }: {
    url: URL;
    abortSignal?: AbortSignal;
}) => Promise<{
    data: Uint8Array<ArrayBufferLike>;
    mediaType: string | undefined;
}>;

declare const createEventSourceResponseHandler: <T>(chunkSchema: FlexibleSchema_2<T>) => ResponseHandler<ReadableStream<ParseResult<T>>>;

declare const createEventSourceResponseHandler_2: <T>(chunkSchema: FlexibleSchema<T>) => ResponseHandler_2<ReadableStream<ParseResult_2<T>>>;

/**
 * Create a remote provider instance.
 */
export declare function createGateway(options?: GatewayProviderSettings): GatewayProvider;

/**
 * Creates an ID generator.
 * The total length of the ID is the sum of the prefix, separator, and random part length.
 * Not cryptographically secure.
 *
 * @param alphabet - The alphabet to use for the ID. Default: '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz'.
 * @param prefix - The prefix of the ID to generate. Optional.
 * @param separator - The separator between the prefix and the random part of the ID. Default: '-'.
 * @param size - The size of the random part of the ID to generate. Default: 16.
 */
export declare const createIdGenerator: ({ prefix, size, alphabet, separator, }?: {
    prefix?: string;
    separator?: string;
    size?: number;
    alphabet?: string;
}) => IdGenerator;

/**
 * Creates an ID generator.
 * The total length of the ID is the sum of the prefix, separator, and random part length.
 * Not cryptographically secure.
 *
 * @param alphabet - The alphabet to use for the ID. Default: '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz'.
 * @param prefix - The prefix of the ID to generate. Optional.
 * @param separator - The separator between the prefix and the random part of the ID. Default: '-'.
 * @param size - The size of the random part of the ID to generate. Default: 16.
 */
declare const createIdGenerator_2: ({ prefix, size, alphabet, separator, }?: {
    prefix?: string;
    separator?: string;
    size?: number;
    alphabet?: string;
}) => IdGenerator_2;

declare const createJsonErrorResponseHandler: <T>({ errorSchema, errorToMessage, isRetryable, }: {
    errorSchema: FlexibleSchema_2<T>;
    errorToMessage: (error: T) => string;
    isRetryable?: (response: Response, error?: T) => boolean;
}) => ResponseHandler<APICallError_2>;

declare const createJsonErrorResponseHandler_2: <T>({ errorSchema, errorToMessage, isRetryable, }: {
    errorSchema: FlexibleSchema<T>;
    errorToMessage: (error: T) => string;
    isRetryable?: (response: Response, error?: T) => boolean;
}) => ResponseHandler_2<APICallError_2>;

declare const createJsonResponseHandler: <T>(responseSchema: FlexibleSchema_2<T>) => ResponseHandler<T>;

declare const createJsonResponseHandler_2: <T>(responseSchema: FlexibleSchema<T>) => ResponseHandler_2<T>;

declare function createProviderDefinedToolFactory<INPUT, ARGS extends object, CONTEXT extends Context = {}>({ id, inputSchema, }: {
    id: `${string}.${string}`;
    inputSchema: FlexibleSchema_2<INPUT>;
}): ProviderDefinedToolFactory<INPUT, ARGS, CONTEXT>;

declare function createProviderDefinedToolFactory_2<INPUT, ARGS extends object, CONTEXT extends Context_2 = {}>({ id, inputSchema, }: {
    id: `${string}.${string}`;
    inputSchema: FlexibleSchema<INPUT>;
}): ProviderDefinedToolFactory_2<INPUT, ARGS, CONTEXT>;

declare function createProviderDefinedToolFactoryWithOutputSchema<INPUT, OUTPUT, ARGS extends object, CONTEXT extends Context = {}>({ id, inputSchema, outputSchema, }: {
    id: `${string}.${string}`;
    inputSchema: FlexibleSchema_2<INPUT>;
    outputSchema: FlexibleSchema_2<OUTPUT>;
}): ProviderDefinedToolFactoryWithOutputSchema<INPUT, OUTPUT, ARGS, CONTEXT>;

declare function createProviderDefinedToolFactoryWithOutputSchema_2<INPUT, OUTPUT, ARGS extends object, CONTEXT extends Context_2 = {}>({ id, inputSchema, outputSchema, }: {
    id: `${string}.${string}`;
    inputSchema: FlexibleSchema<INPUT>;
    outputSchema: FlexibleSchema<OUTPUT>;
}): ProviderDefinedToolFactoryWithOutputSchema_2<INPUT, OUTPUT, ARGS, CONTEXT>;

declare function createProviderExecutedToolFactory<INPUT, OUTPUT, ARGS extends object, CONTEXT extends Context = {}>({ id, inputSchema, outputSchema, supportsDeferredResults, }: {
    id: `${string}.${string}`;
    inputSchema: FlexibleSchema_2<INPUT>;
    outputSchema: FlexibleSchema_2<OUTPUT>;
    /**
     * Whether this provider-executed tool supports deferred results.
     *
     * When true, the tool result may not be returned in the same turn as the
     * tool call (e.g., when using programmatic tool calling where a server tool
     * triggers a client-executed tool, and the server tool's result is deferred
     * until the client tool is resolved).
     *
     * @default false
     */
    supportsDeferredResults?: boolean;
}): ProviderExecutedToolFactory<INPUT, OUTPUT, ARGS, CONTEXT>;

declare function createProviderExecutedToolFactory_2<INPUT, OUTPUT, ARGS extends object, CONTEXT extends Context_2 = {}>({ id, inputSchema, outputSchema, supportsDeferredResults, }: {
    id: `${string}.${string}`;
    inputSchema: FlexibleSchema<INPUT>;
    outputSchema: FlexibleSchema<OUTPUT>;
    /**
     * Whether this provider-executed tool supports deferred results.
     *
     * When true, the tool result may not be returned in the same turn as the
     * tool call (e.g., when using programmatic tool calling where a server tool
     * triggers a client-executed tool, and the server tool's result is deferred
     * until the client tool is resolved).
     *
     * @default false
     */
    supportsDeferredResults?: boolean;
}): ProviderExecutedToolFactory_2<INPUT, OUTPUT, ARGS, CONTEXT>;

/**
 * Creates a registry for the given providers with optional middleware functionality.
 * This function allows you to register multiple providers and optionally apply middleware
 * to all language models from the registry, enabling you to transform parameters, wrap generate
 * operations, and wrap stream operations for every language model accessed through the registry.
 *
 * @param providers - A record of provider instances to be registered in the registry.
 * @param options - Configuration options for the provider registry.
 * @param options.separator - The separator used between provider ID and model ID in the combined identifier. Defaults to ':'.
 * @param options.languageModelMiddleware - Optional middleware to be applied to all language models from the registry. When multiple middlewares are provided, the first middleware will transform the input first, and the last middleware will be wrapped directly around the model.
 * @param options.imageModelMiddleware - Optional middleware to be applied to all image models from the registry. When multiple middlewares are provided, the first middleware will transform the input first, and the last middleware will be wrapped directly around the model.
 * @returns A new ProviderRegistryProvider instance that provides access to all registered providers with optional middleware applied to language and image models.
 */
export declare function createProviderRegistry<PROVIDERS extends Record<string, ProviderV4_2 | ProviderV3>, SEPARATOR extends string = ':'>(providers: PROVIDERS, { separator, languageModelMiddleware, imageModelMiddleware, }?: {
    separator?: SEPARATOR;
    languageModelMiddleware?: LanguageModelMiddleware | LanguageModelMiddleware[];
    imageModelMiddleware?: ImageModelMiddleware | ImageModelMiddleware[];
}): ProviderRegistryProvider<PROVIDERS, SEPARATOR>;

declare const createStatusCodeErrorResponseHandler: () => ResponseHandler<APICallError_2>;

declare const createStatusCodeErrorResponseHandler_2: () => ResponseHandler_2<APICallError_2>;

/**
 * Creates a Response object from a text stream.
 * Each text chunk is encoded as UTF-8 and sent as a separate chunk.
 * Sets a `Content-Type` header to `text/plain; charset=utf-8`.
 *
 * @param options - The options for creating the response.
 * @param options.status - Optional HTTP status code (default: 200).
 * @param options.statusText - Optional HTTP status text.
 * @param options.headers - Optional response headers.
 * @param options.stream - The text stream to send.
 * @returns A Response object with the text stream body.
 */
export declare function createTextStreamResponse({ status, statusText, headers, stream, }: ResponseInit & {
    stream: ReadableStream<string>;
}): Response;

/**
 * @param tools - Tools that were passed to the language model.
 * @param providerToolNames - Maps the provider tool ids to the provider tool names.
 */
declare function createToolNameMapping({ tools, providerToolNames, }: {
    /**
     * Tools that were passed to the language model.
     */
    tools: Array<LanguageModelV4FunctionTool | LanguageModelV4ProviderTool> | undefined;
    /**
     * Maps the provider tool ids to the provider tool names.
     */
    providerToolNames: Record<`${string}.${string}`, string>;
}): ToolNameMapping;

/**
 * @param tools - Tools that were passed to the language model.
 * @param providerToolNames - Maps the provider tool ids to the provider tool names.
 */
declare function createToolNameMapping_2({ tools, providerToolNames, }: {
    /**
     * Tools that were passed to the language model.
     */
    tools: Array<LanguageModelV4FunctionTool | LanguageModelV4ProviderTool> | undefined;
    /**
     * Maps the provider tool ids to the provider tool names.
     */
    providerToolNames: Record<`${string}.${string}`, string>;
}): ToolNameMapping_2;

export declare type CreateUIMessage<UI_MESSAGE extends UIMessage> = Omit<UI_MESSAGE, 'id' | 'role'> & {
    id?: UI_MESSAGE['id'];
    role?: UI_MESSAGE['role'];
};

/**
 * Creates a UI message stream that can be used to send messages to the client.
 *
 * @param options.execute - A function that is called with a writer to write UI message chunks to the stream.
 * @param options.onError - A function that extracts an error message from an error. Defaults to `() => 'An error occurred.'` so server-side error details are not leaked to the client; supply your own to surface richer messages.
 * @param options.originalMessages - The original messages. If provided, persistence mode is assumed
 *   and a message ID is provided for the response message.
 * @param options.onStepEnd - A callback that is called when each step ends. Useful for persisting intermediate messages.
 * @param options.onStepFinish - Deprecated alias for `onStepEnd`.
 * @param options.onEnd - A callback that is called when the stream ends.
 * @param options.onFinish - Deprecated alias for `onEnd`.
 * @param options.generateId - A function that generates a unique ID. Defaults to the built-in ID generator.
 *
 * @returns A `ReadableStream` of UI message chunks.
 */
export declare function createUIMessageStream<UI_MESSAGE extends UIMessage>({ execute, onError, // prevent leaking server error details to the client by default
    originalMessages, onStepEnd, onStepFinish, onEnd, onFinish, generateId, }: {
    execute: (options: {
        writer: UIMessageStreamWriter<UI_MESSAGE>;
    }) => Promise<void> | void;
    onError?: (error: unknown) => string;
    /**
     * The original messages. If they are provided, persistence mode is assumed,
     * and a message ID is provided for the response message.
     */
    originalMessages?: UI_MESSAGE[];
    /**
     * Callback that is called when each step ends during multi-step agent runs.
     */
    onStepEnd?: UIMessageStreamOnStepEndCallback<UI_MESSAGE>;
    /**
     * Callback that is called when each step ends during multi-step agent runs.
     *
     * @deprecated Use `onStepEnd` instead.
     */
    onStepFinish?: UIMessageStreamOnStepFinishCallback<UI_MESSAGE>;
    onEnd?: UIMessageStreamOnEndCallback<UI_MESSAGE>;
    /**
     * @deprecated Use `onEnd` instead.
     */
    onFinish?: UIMessageStreamOnEndCallback<UI_MESSAGE>;
    generateId?: IdGenerator;
}): ReadableStream<InferUIMessageChunk<UI_MESSAGE>>;

/**
 * Creates a Response object from a UI message stream.
 * The stream is transformed to Server-Sent Events (SSE) format.
 *
 * @param options.status - The HTTP status code for the response.
 * @param options.statusText - The HTTP status text for the response.
 * @param options.headers - Additional HTTP headers to include in the response.
 * @param options.stream - The UI message chunk stream to send.
 * @param options.consumeSseStream - Optional callback to consume a copy of the SSE stream independently.
 *
 * @returns A `Response` object with the UI message stream as the body.
 */
export declare function createUIMessageStreamResponse({ status, statusText, headers, stream, consumeSseStream, }: UIMessageStreamResponseInit & {
    stream: ReadableStream<UIMessageChunk>;
}): Response;

/**
 * A provider-specific part of a message.
 */
export declare type CustomContentUIPart = {
    type: 'custom';
    /**
     * The kind of custom content, in the format `{provider}.{provider-type}`.
     */
    kind: `${string}.${string}`;
    /**
     * The provider metadata.
     */
    providerMetadata?: ProviderMetadata;
};

/**
 * Custom content part of a prompt. It contains no standardized payload beyond
 * provider-specific options.
 */
declare interface CustomPart {
    type: 'custom';
    /**
     * The kind of custom content, in the format `{provider}.{provider-type}`.
     */
    kind: `${string}.${string}`;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
}

/**
 * Custom content part of a prompt. It contains no standardized payload beyond
 * provider-specific options.
 */
declare interface CustomPart_2 {
    type: 'custom';
    /**
     * The kind of custom content, in the format `{provider}.{provider-type}`.
     */
    kind: `${string}.${string}`;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
}

/**
 * Creates a custom provider with specified language models, text embedding models, image models, transcription models, speech models, file APIs, skill APIs, and an optional fallback provider.
 *
 * @param {Object} options - The options for creating the custom provider.
 * @param {Record<string, LanguageModel>} [options.languageModels] - A record of language models, where keys are model IDs and values are language model instances.
 * @param {Record<string, EmbeddingModel>} [options.embeddingModels] - A record of text embedding models, where keys are model IDs and values are embedding model instances.
 * @param {Record<string, ImageModel>} [options.imageModels] - A record of image models, where keys are model IDs and values are image model instances.
 * @param {Record<string, TranscriptionModel>} [options.transcriptionModels] - A record of transcription models, where keys are model IDs and values are transcription model instances.
 * @param {Record<string, SpeechModel>} [options.speechModels] - A record of speech models, where keys are model IDs and values are speech model instances.
 * @param {Record<string, RerankingModel>} [options.rerankingModels] - A record of reranking models, where keys are model IDs and values are reranking model instances.
 * @param {Record<string, VideoModel>} [options.videoModels] - A record of video models, where keys are model IDs and values are video model instances.
 * @param {FilesV4} [options.files] - A files interface for uploading files.
 * @param {SkillsV4} [options.skills] - A skills interface for uploading skills.
 * @param {ProviderV2 | ProviderV3 | ProviderV4} [options.fallbackProvider] - An optional fallback provider to use when a requested model is not found in the custom provider.
 * @returns {ProviderV4} A ProviderV4 object with languageModel, embeddingModel, imageModel, transcriptionModel, speechModel, rerankingModel, and videoModel methods.
 *
 * @throws {NoSuchModelError} Throws when a requested model is not found and no fallback provider is available.
 */
export declare function customProvider<LANGUAGE_MODELS extends Record<string, LanguageModel>, EMBEDDING_MODELS extends Record<string, EmbeddingModel>, IMAGE_MODELS extends Record<string, ImageModel>, TRANSCRIPTION_MODELS extends Record<string, TranscriptionModel>, SPEECH_MODELS extends Record<string, SpeechModel>, RERANKING_MODELS extends Record<string, RerankingModel>, VIDEO_MODELS extends Record<string, VideoModel>, FILES extends FilesV4_2 | undefined = undefined, SKILLS extends SkillsV4_2 | undefined = undefined, FALLBACK extends ProviderV2 | ProviderV3 | ProviderV4_2 | undefined = undefined>({ languageModels, embeddingModels, imageModels, transcriptionModels, speechModels, rerankingModels, videoModels, files, skills, fallbackProvider: fallbackProviderArg, }: {
    languageModels?: LANGUAGE_MODELS;
    embeddingModels?: EMBEDDING_MODELS;
    imageModels?: IMAGE_MODELS;
    transcriptionModels?: TRANSCRIPTION_MODELS;
    speechModels?: SPEECH_MODELS;
    rerankingModels?: RERANKING_MODELS;
    videoModels?: VIDEO_MODELS;
    files?: FILES;
    skills?: SKILLS;
    fallbackProvider?: FALLBACK;
}): ProviderV4_2 & {
    languageModel(modelId: ExtractModelId<LANGUAGE_MODELS>): LanguageModelV4_2;
    embeddingModel(modelId: ExtractModelId<EMBEDDING_MODELS>): EmbeddingModelV4_2;
    imageModel(modelId: ExtractModelId<IMAGE_MODELS>): ImageModelV4_2;
    transcriptionModel(modelId: ExtractModelId<TRANSCRIPTION_MODELS>): TranscriptionModelV4_2;
    rerankingModel(modelId: ExtractModelId<RERANKING_MODELS>): RerankingModelV4_2;
    speechModel(modelId: ExtractModelId<SPEECH_MODELS>): SpeechModelV4_2;
    videoModel(modelId: ExtractModelId<VIDEO_MODELS>): VideoModelV4_2;
} & (FILES extends FilesV4_2 ? {
    files(): FilesV4_2;
} : [FALLBACK] extends [{
    files: () => FilesV4_2;
}] ? {
    files(): FilesV4_2;
} : {
    files?(): FilesV4_2;
}) & (SKILLS extends SkillsV4_2 ? {
    skills(): SkillsV4_2;
} : [FALLBACK] extends [{
    skills: () => SkillsV4_2;
}] ? {
    skills(): SkillsV4_2;
} : {
    skills?(): SkillsV4_2;
});

/**
 * Data content. Can either be a base64-encoded string, a Uint8Array, an ArrayBuffer, or a Buffer.
 */
export declare type DataContent = string | Uint8Array | ArrayBuffer | Buffer;

/**
 * Data content. Can either be a base64-encoded string, a Uint8Array, an ArrayBuffer, or a Buffer.
 */
declare type DataContent_2 = string | Uint8Array | ArrayBuffer | Buffer;

declare type DataUIMessageChunk<DATA_TYPES extends UIDataTypes> = ValueOf<{
    [NAME in keyof DATA_TYPES & string]: {
        type: `data-${NAME}`;
        id?: string;
        data: DATA_TYPES[NAME];
        transient?: boolean;
    };
}>;

export declare type DataUIPart<DATA_TYPES extends UIDataTypes> = ValueOf<{
    [NAME in keyof DATA_TYPES & string]: {
        type: `data-${NAME}`;
        id?: string;
        data: DATA_TYPES[NAME];
    };
}>;

/**
 * Create a type from an object with all keys and nested keys set to optional.
 * The helper supports normal objects and schemas (which are resolved automatically).
 * It always recurses into arrays.
 *
 * Adopted from [type-fest](https://github.com/sindresorhus/type-fest/tree/main) PartialDeep.
 */
export declare type DeepPartial<T> = T extends FlexibleSchema ? DeepPartialInternal<InferSchema<T>> : DeepPartialInternal<T>;

declare type DeepPartialInternal<T> = T extends null | undefined | string | number | boolean | symbol | bigint | void | Date | RegExp | ((...arguments_: any[]) => unknown) | (new (...arguments_: any[]) => unknown) ? T : T extends Map<infer KeyType, infer ValueType> ? PartialMap<KeyType, ValueType> : T extends Set<infer ItemType> ? PartialSet<ItemType> : T extends ReadonlyMap<infer KeyType, infer ValueType> ? PartialReadonlyMap<KeyType, ValueType> : T extends ReadonlySet<infer ItemType> ? PartialReadonlySet<ItemType> : T extends object ? T extends ReadonlyArray<infer ItemType> ? ItemType[] extends T ? readonly ItemType[] extends T ? ReadonlyArray<DeepPartialInternal<ItemType | undefined>> : Array<DeepPartialInternal<ItemType | undefined>> : PartialObject<T> : PartialObject<T> : unknown;

/**
 * Default maximum download size: 2 GiB.
 *
 * `fetch().arrayBuffer()` has ~2x peak memory overhead (undici buffers the
 * body internally, then creates the JS ArrayBuffer), so very large downloads
 * risk exceeding the default V8 heap limit on 64-bit systems and terminating
 * the process with an out-of-memory error.
 *
 * Setting this limit converts an unrecoverable OOM crash into a catchable
 * `DownloadError`.
 */
declare const DEFAULT_MAX_DOWNLOAD_SIZE: number;

/**
 * Default maximum download size: 2 GiB.
 *
 * `fetch().arrayBuffer()` has ~2x peak memory overhead (undici buffers the
 * body internally, then creates the JS ArrayBuffer), so very large downloads
 * risk exceeding the default V8 heap limit on 64-bit systems and terminating
 * the process with an out-of-memory error.
 *
 * Setting this limit converts an unrecoverable OOM crash into a catchable
 * `DownloadError`.
 */
declare const DEFAULT_MAX_DOWNLOAD_SIZE_2: number;

export declare class DefaultChatTransport<UI_MESSAGE extends UIMessage> extends HttpChatTransport<UI_MESSAGE> {
    constructor(options?: HttpChatTransportInitOptions<UI_MESSAGE>);
    protected processResponseStream(stream: ReadableStream<Uint8Array<ArrayBufferLike>>): ReadableStream<UIMessageChunk>;
}

/**
 * Applies default settings for an embedding model.
 */
export declare function defaultEmbeddingSettingsMiddleware({ settings, }: {
    settings: Partial<{
        headers?: EmbeddingModelV4CallOptions_2['headers'];
        providerOptions?: EmbeddingModelV4CallOptions_2['providerOptions'];
    }>;
}): EmbeddingModelMiddleware;

export declare class DefaultGeneratedFile implements GeneratedFile {
    private base64Data;
    private uint8ArrayData;
    readonly mediaType: string;
    constructor({ data, mediaType, }: {
        data: string | Uint8Array;
        mediaType: string;
    });
    get base64(): string;
    get uint8Array(): Uint8Array<ArrayBufferLike>;
}

/**
 * Applies default settings for a language model.
 */
export declare function defaultSettingsMiddleware({ settings, }: {
    settings: Partial<{
        maxOutputTokens?: LanguageModelV4CallOptions_2['maxOutputTokens'];
        temperature?: LanguageModelV4CallOptions_2['temperature'];
        stopSequences?: LanguageModelV4CallOptions_2['stopSequences'];
        topP?: LanguageModelV4CallOptions_2['topP'];
        topK?: LanguageModelV4CallOptions_2['topK'];
        presencePenalty?: LanguageModelV4CallOptions_2['presencePenalty'];
        frequencyPenalty?: LanguageModelV4CallOptions_2['frequencyPenalty'];
        responseFormat?: LanguageModelV4CallOptions_2['responseFormat'];
        seed?: LanguageModelV4CallOptions_2['seed'];
        tools?: LanguageModelV4CallOptions_2['tools'];
        toolChoice?: LanguageModelV4CallOptions_2['toolChoice'];
        headers?: LanguageModelV4CallOptions_2['headers'];
        providerOptions?: LanguageModelV4CallOptions_2['providerOptions'];
    }>;
}): LanguageModelMiddleware;

/**
 * Creates a Promise that resolves after a specified delay
 * @param delayInMs - The delay duration in milliseconds. If null or undefined, resolves immediately.
 * @param signal - Optional AbortSignal to cancel the delay
 * @returns A Promise that resolves after the specified delay
 * @throws {DOMException} When the signal is aborted
 */
declare function delay(delayInMs?: number | null, options?: {
    abortSignal?: AbortSignal;
}): Promise<void>;

/**
 * Creates a Promise that resolves after a specified delay
 * @param delayInMs - The delay duration in milliseconds. If null or undefined, resolves immediately.
 * @param signal - Optional AbortSignal to cancel the delay
 * @returns A Promise that resolves after the specified delay
 * @throws {DOMException} When the signal is aborted
 */
declare function delay_2(delayInMs?: number | null, options?: {
    abortSignal?: AbortSignal;
}): Promise<void>;

/**
 * Delayed promise. It is only constructed once the value is accessed.
 * This is useful to avoid unhandled promise rejections when the promise is created
 * but not accessed.
 */
declare class DelayedPromise<T> {
    private status;
    private _promise;
    private _resolve;
    private _reject;
    get promise(): Promise<T>;
    resolve(value: T): void;
    reject(error: unknown): void;
    isResolved(): boolean;
    isRejected(): boolean;
    isPending(): boolean;
}

/**
 * Delayed promise. It is only constructed once the value is accessed.
 * This is useful to avoid unhandled promise rejections when the promise is created
 * but not accessed.
 */
declare class DelayedPromise_2<T> {
    private status;
    private _promise;
    private _resolve;
    private _reject;
    get promise(): Promise<T>;
    resolve(value: T): void;
    reject(error: unknown): void;
    isResolved(): boolean;
    isRejected(): boolean;
    isPending(): boolean;
}

/**
 * Detect the IANA media type of a file from its raw bytes or base64 string.
 *
 * - When `topLevelType` is omitted, every known signature is considered
 *   (image, audio, video, and application). Returns `undefined` when the
 *   bytes do not match any known signature.
 * - When `topLevelType` is provided, only signatures for that top-level
 *   segment are considered. Returns `undefined` for unsupported segments
 *   (e.g. `"text"`) or when no signature matches.
 */
declare function detectMediaType({ data, topLevelType, }: {
    data: Uint8Array | string;
    topLevelType?: string;
}): string | undefined;

/**
 * Detect the IANA media type of a file from its raw bytes or base64 string.
 *
 * - When `topLevelType` is omitted, every known signature is considered
 *   (image, audio, video, and application). Returns `undefined` when the
 *   bytes do not match any known signature.
 * - When `topLevelType` is provided, only signatures for that top-level
 *   segment are considered. Returns `undefined` for unsupported segments
 *   (e.g. `"text"`) or when no signature matches.
 */
declare function detectMediaType_2({ data, topLevelType, }: {
    data: Uint8Array | string;
    topLevelType?: string;
}): string | undefined;

/**
 * Pure diff of two fingerprint maps produced by {@link fingerprintTools}.
 * `added`/`removed` are tools present in only one map; `changed` are tools whose
 * pinned definition differs. Uses own-property lookups so a tool literally named
 * `constructor` or `toString` diffs correctly.
 */
export declare function detectToolDrift(current: Record<string, string>, baseline: Record<string, string>): {
    added: string[];
    removed: string[];
    changed: string[];
};

/**
 * A transport that directly communicates with an Agent in-process,
 * without going through HTTP. This is useful for:
 * - Server-side rendering scenarios
 * - Testing without network
 * - Single-process applications
 *
 * @example
 * ```tsx
 * import { useChat } from '@ai-sdk/react';
 * import { DirectChatTransport } from 'ai';
 * import { myAgent } from './my-agent';
 *
 * const { messages, sendMessage } = useChat({
 *   transport: new DirectChatTransport({ agent: myAgent }),
 * });
 * ```
 */
export declare class DirectChatTransport<CALL_OPTIONS = never, TOOLS extends ToolSet = {}, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = never, UI_MESSAGE extends UIMessage<unknown, never, InferUITools<TOOLS>> = UIMessage<unknown, never, InferUITools<TOOLS>>> implements ChatTransport<UI_MESSAGE> {
    private readonly agent;
    private readonly agentOptions;
    private readonly uiMessageStreamOptions;
    constructor({ agent, options, ...uiMessageStreamOptions }: DirectChatTransportOptions<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT, OUTPUT, UI_MESSAGE>);
    sendMessages({ messages, abortSignal, }: Parameters<ChatTransport<UI_MESSAGE>['sendMessages']>[0]): Promise<ReadableStream<UIMessageChunk>>;
    /**
     * Direct transport does not support reconnection since there is no
     * persistent server-side stream to reconnect to.
     *
     * @returns Always returns `null`
     */
    reconnectToStream(_options: Parameters<ChatTransport<UI_MESSAGE>['reconnectToStream']>[0]): Promise<ReadableStream<UIMessageChunk> | null>;
}

/**
 * Options for the `DirectChatTransport` class.
 */
export declare type DirectChatTransportOptions<CALL_OPTIONS, TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2, OUTPUT extends Output_2, UI_MESSAGE extends UIMessage<unknown, never, InferUITools<TOOLS>>> = {
    /**
     * The agent to use for generating responses.
     */
    agent: Agent<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT, OUTPUT>;
    /**
     * Options to pass to the agent when calling it.
     */
    options?: CALL_OPTIONS;
} & Omit<UIMessageStreamOptions<UI_MESSAGE>, 'onFinish'>;

/**
 * Download a file from a URL and return it as a Blob.
 *
 * @param url - The URL to download from.
 * @param options - Optional settings for the download.
 * @param options.maxBytes - Maximum allowed download size in bytes. Defaults to 100 MiB.
 * @param options.abortSignal - An optional abort signal to cancel the download.
 * @returns A Promise that resolves to the downloaded Blob.
 *
 * @throws DownloadError if the download fails or exceeds maxBytes.
 */
declare function downloadBlob(url: string, options?: {
    maxBytes?: number;
    abortSignal?: AbortSignal;
}): Promise<Blob>;

/**
 * Download a file from a URL and return it as a Blob.
 *
 * @param url - The URL to download from.
 * @param options - Optional settings for the download.
 * @param options.maxBytes - Maximum allowed download size in bytes. Defaults to 100 MiB.
 * @param options.abortSignal - An optional abort signal to cancel the download.
 * @returns A Promise that resolves to the downloaded Blob.
 *
 * @throws DownloadError if the download fails or exceeds maxBytes.
 */
declare function downloadBlob_2(url: string, options?: {
    maxBytes?: number;
    abortSignal?: AbortSignal;
}): Promise<Blob>;

export declare class DownloadError extends AISDKError_2 {
    private readonly [symbol$1_4];
    readonly url: string;
    readonly statusCode?: number;
    readonly statusText?: string;
    constructor({ url, statusCode, statusText, cause, message, }: {
        url: string;
        statusCode?: number;
        statusText?: string;
        message?: string;
        cause?: unknown;
    });
    static isInstance(error: unknown): error is DownloadError;
}

declare class DownloadError_2 extends AISDKError_2 {
    private readonly [symbol$1_2];
    readonly url: string;
    readonly statusCode?: number;
    readonly statusText?: string;
    constructor({ url, statusCode, statusText, cause, message, }: {
        url: string;
        statusCode?: number;
        statusText?: string;
        message?: string;
        cause?: unknown;
    });
    static isInstance(error: unknown): error is DownloadError_2;
}

/**
 * Tool that is defined at runtime.
 * The types of input and output are not known at development time.
 *
 * For example, MCP tools that are not known at development time.
 */
declare type DynamicTool<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context | unknown | never = any> = BaseFunctionTool<INPUT, OUTPUT, CONTEXT> & {
    type: 'dynamic';
};

/**
 * Define a dynamic tool.
 */
export declare function dynamicTool(tool: Omit<DynamicTool_2<unknown, unknown, Context_2>, 'type'>): DynamicTool_2<unknown, unknown, Context_2>;

/**
 * Tool that is defined at runtime.
 * The types of input and output are not known at development time.
 *
 * For example, MCP tools that are not known at development time.
 */
declare type DynamicTool_2<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context_2 | unknown | never = any> = BaseFunctionTool_2<INPUT, OUTPUT, CONTEXT> & {
    type: 'dynamic';
};

/**
 * Define a dynamic tool.
 */
declare function dynamicTool_2(tool: Omit<DynamicTool<unknown, unknown, Context>, 'type'>): DynamicTool<unknown, unknown, Context>;

/**
 * A tool call whose `toolName` is only known at runtime, such as an invalid
 * or otherwise untyped call that cannot be matched to the declared tool set.
 */
export declare type DynamicToolCall = BaseToolCall & {
    toolName: string;
    input: unknown;
    dynamic: true;
    title?: string;
    /**
     * True if this is caused by an unparsable tool call or
     * a tool that does not exist.
     */
    invalid?: boolean;
    /**
     * The error that caused the tool call to be invalid.
     */
    error?: unknown;
};

export declare type DynamicToolError = {
    type: 'tool-error';
    toolCallId: string;
    toolName: string;
    input: unknown;
    error: unknown;
    providerExecuted?: boolean;
    providerMetadata?: ProviderMetadata;
    toolMetadata?: JSONObject_2;
    dynamic: true;
    title?: string;
};

/**
 * End event shape for dynamic or untyped tool calls.
 */
declare type DynamicToolExecutionEndEvent<TOOLS extends ToolSet> = BaseToolExecutionEndFields & {
    readonly toolCall: DynamicToolCall;
    readonly toolContext: unknown;
    readonly toolOutput: ToolOutput<TOOLS>;
};

/**
 * Start event shape for dynamic or untyped tool calls.
 */
declare type DynamicToolExecutionStartEvent = BaseToolExecutionStartFields & {
    readonly toolCall: DynamicToolCall;
    readonly toolContext: unknown;
};

export declare type DynamicToolResult = {
    type: 'tool-result';
    toolCallId: string;
    toolName: string;
    input: unknown;
    output: unknown;
    providerExecuted?: boolean;
    providerMetadata?: ProviderMetadata;
    toolMetadata?: JSONObject_2;
    dynamic: true;
    preliminary?: boolean;
    title?: string;
};

export declare type DynamicToolUIPart = {
    type: 'dynamic-tool';
    /**
     * Name of the tool that is being called.
     */
    toolName: string;
    /**
     * ID of the tool call.
     */
    toolCallId: string;
    title?: string;
    toolMetadata?: JSONObject_2;
    /**
     * Whether the tool call was executed by the provider.
     */
    providerExecuted?: boolean;
} & ({
    state: 'input-streaming';
    input?: unknown;
    output?: never;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    approval?: never;
} | {
    state: 'input-available';
    input: unknown;
    output?: never;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    approval?: never;
} | {
    state: 'approval-requested';
    input: unknown;
    output?: never;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    approval: {
        id: string;
        approved?: never;
        reason?: never;
        isAutomatic?: boolean;
        signature?: string;
    };
} | {
    state: 'approval-responded';
    input: unknown;
    output?: never;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    approval: {
        id: string;
        approved: boolean;
        reason?: string;
        isAutomatic?: boolean;
        signature?: string;
    };
} | {
    state: 'output-available';
    input: unknown;
    output: unknown;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    resultProviderMetadata?: ProviderMetadata;
    preliminary?: boolean;
    approval?: {
        id: string;
        approved: true;
        reason?: string;
        isAutomatic?: boolean;
        signature?: string;
    };
} | {
    state: 'output-error';
    input: unknown;
    output?: never;
    errorText: string;
    callProviderMetadata?: ProviderMetadata;
    resultProviderMetadata?: ProviderMetadata;
    approval?: {
        id: string;
        approved: true;
        reason?: string;
        isAutomatic?: boolean;
        signature?: string;
    };
} | {
    state: 'output-denied';
    input: unknown;
    output?: never;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    approval: {
        id: string;
        approved: false;
        reason?: string;
        isAutomatic?: boolean;
        signature?: string;
    };
});

/**
 * Embed a value using an embedding model. The type of the value is defined by the embedding model.
 *
 * @param model - The embedding model to use.
 * @param value - The value that should be embedded.
 *
 * @param maxRetries - Maximum number of retries. Set to 0 to disable retries. Default: 2.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param headers - Additional HTTP headers to be sent with the request. Only applicable for HTTP-based providers.
 *
 * @param telemetry - Optional telemetry configuration.
 *
 * @param providerOptions - Additional provider-specific options. They are passed through
 * to the provider from the AI SDK and enable provider-specific
 * functionality that can be fully encapsulated in the provider.
 *
 * @returns A result object that contains the embedding, the value, and additional information.
 */
export declare function embed({ model: modelArg, value, providerOptions, maxRetries: maxRetriesArg, abortSignal, headers, experimental_telemetry, telemetry, onStart, experimental_onStart, onEnd, experimental_onEnd, _internal: { generateCallId }, }: {
    /**
     * The embedding model to use.
     */
    model: EmbeddingModel;
    /**
     * The value that should be embedded.
     */
    value: string;
    /**
     * Maximum number of retries per embedding model call. Set to 0 to disable retries.
     *
     * @default 2
     */
    maxRetries?: number;
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional headers to include in the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string>;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Optional telemetry configuration.
     */
    telemetry?: TelemetryOptions;
    /**
     * Optional telemetry configuration.
     *
     * @deprecated Use `telemetry` instead. This alias will be removed in a future major release.
     */
    experimental_telemetry?: TelemetryOptions;
    /**
     * Callback that is called when the embed operation begins,
     * before the embedding model is called.
     */
    onStart?: Callback<EmbedStartEvent>;
    /**
     * Callback that is called when the embed operation begins,
     * before the embedding model is called.
     *
     * @deprecated Use `onStart` instead.
     */
    experimental_onStart?: Callback<EmbedStartEvent>;
    /**
     * Callback that is called when the embed operation completes,
     * after the embedding model returns.
     */
    onEnd?: Callback<EmbedEndEvent>;
    /**
     * Callback that is called when the embed operation completes,
     * after the embedding model returns.
     *
     * @deprecated Use `onEnd` instead.
     */
    experimental_onEnd?: Callback<EmbedEndEvent>;
    /**
     * Internal. For test use only. May change without notice.
     */
    _internal?: {
        generateCallId?: () => string;
    };
}): Promise<EmbedResult>;

/**
 * Embedding.
 */
export declare type Embedding = EmbeddingModelV4Embedding_2;

/**
 * Embedding model that is used by the AI SDK.
 */
export declare type EmbeddingModel = string | EmbeddingModelV4_2 | EmbeddingModelV3 | EmbeddingModelV2<string>;

/**
 * Event fired when an individual embedding model call (doEmbed) completes.
 *
 * Contains the embeddings, usage, and any warnings from the model response.
 */
export declare type EmbeddingModelCallEndEvent = {
    /** Unique identifier for this embed call, used to correlate events. */
    readonly callId: string;
    /** Unique identifier for this individual doEmbed invocation, used to correlate start/finish within parallel chunks. */
    readonly embedCallId: string;
    /** Identifies the inner operation (e.g. 'ai.embed.doEmbed' or 'ai.embedMany.doEmbed'). */
    readonly operationId: string;
    /** The provider identifier. */
    readonly provider: string;
    /** The specific model identifier. */
    readonly modelId: string;
    /** The values that were embedded in this particular model call. */
    readonly values: Array<string>;
    /** The resulting embeddings from the model call. */
    readonly embeddings: Array<Embedding>;
    /** Token usage for this model call. */
    readonly usage: EmbeddingModelUsage;
};

/**
 * Event fired when an individual embedding model call (inner operation doEmbed) begins.
 *
 * For `embed`, there is one call. For `embedMany`, there may be multiple
 * calls when values are chunked.
 */
export declare type EmbeddingModelCallStartEvent = {
    /** Unique identifier for this embed call, used to correlate events. */
    readonly callId: string;
    /** Unique identifier for this individual doEmbed invocation, used to correlate start/finish within parallel chunks. */
    readonly embedCallId: string;
    /** Identifies the inner operation (e.g. 'ai.embed.doEmbed' or 'ai.embedMany.doEmbed'). */
    readonly operationId: string;
    /** The provider identifier. */
    readonly provider: string;
    /** The specific model identifier. */
    readonly modelId: string;
    /** The values being embedded in this particular model call. */
    readonly values: Array<string>;
};

/**
 * Middleware for embedding models.
 * Accepts both V3 and V4 middleware types for backward compatibility.
 *
 * Uses EmbeddingModelV4Middleware as the base but relaxes specificationVersion
 * to accept any string (including 'v3') and makes it optional.
 */
export declare type EmbeddingModelMiddleware = Omit<EmbeddingModelV4Middleware, 'specificationVersion'> & {
    readonly specificationVersion?: string;
};

/**
 * Represents the number of tokens used in an embedding.
 */
export declare type EmbeddingModelUsage = {
    /**
     * The number of tokens used in the embedding.
     */
    tokens: number;
};

/**
 * Specification for an embedding model that implements the embedding model
 * interface version 2.
 *
 * VALUE is the type of the values that the model can embed.
 * This will allow us to go beyond text embeddings in the future,
 * e.g. to support image embeddings
 */
declare type EmbeddingModelV2<VALUE> = {
    /**
     * The embedding model must specify which embedding model interface
     * version it implements. This will allow us to evolve the embedding
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v2';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many embeddings can be generated in a single API call.
     *
     * Use Infinity for models that do not have a limit.
     */
    readonly maxEmbeddingsPerCall: PromiseLike<number | undefined> | number | undefined;
    /**
     * True if the model can handle multiple embedding calls in parallel.
     */
    readonly supportsParallelCalls: PromiseLike<boolean> | boolean;
    /**
     * Generates a list of embeddings for the given input text.
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     */
    doEmbed(options: {
        /**
         * List of values to embed.
         */
        values: Array<VALUE>;
        /**
         * Abort signal for cancelling the operation.
         */
        abortSignal?: AbortSignal;
        /**
         * Additional provider-specific options. They are passed through
         * to the provider from the AI SDK and enable provider-specific
         * functionality that can be fully encapsulated in the provider.
         */
        providerOptions?: SharedV2ProviderOptions;
        /**
         * Additional HTTP headers to be sent with the request.
         * Only applicable for HTTP-based providers.
         */
        headers?: Record<string, string | undefined>;
    }): PromiseLike<{
        /**
         * Generated embeddings. They are in the same order as the input values.
         */
        embeddings: Array<EmbeddingModelV2Embedding>;
        /**
         * Token usage. We only have input tokens for embeddings.
         */
        usage?: {
            tokens: number;
        };
        /**
         * Additional provider-specific metadata. They are passed through
         * from the provider to the AI SDK and enable provider-specific
         * results that can be fully encapsulated in the provider.
         */
        providerMetadata?: SharedV2ProviderMetadata;
        /**
         * Optional response information for debugging purposes.
         */
        response?: {
            /**
             * Response headers.
             */
            headers?: SharedV2Headers_2;
            /**
             * The response body.
             */
            body?: unknown;
        };
    }>;
};

/**
 * An embedding is a vector, i.e. an array of numbers.
 * It is e.g. used to represent a text as a vector of word embeddings.
 */
declare type EmbeddingModelV2Embedding = Array<number>;

/**
 * Specification for an embedding model that implements the embedding model
 * interface version 3.
 *
 * It is specific to text embeddings.
 */
declare type EmbeddingModelV3 = {
    /**
     * The embedding model must specify which embedding model interface
     * version it implements. This will allow us to evolve the embedding
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v3';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many embeddings can be generated in a single API call.
     *
     * Use Infinity for models that do not have a limit.
     */
    readonly maxEmbeddingsPerCall: PromiseLike<number | undefined> | number | undefined;
    /**
     * True if the model can handle multiple embedding calls in parallel.
     */
    readonly supportsParallelCalls: PromiseLike<boolean> | boolean;
    /**
     * Generates a list of embeddings for the given input text.
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     */
    doEmbed(options: EmbeddingModelV3CallOptions): PromiseLike<EmbeddingModelV3Result>;
};

declare type EmbeddingModelV3CallOptions = {
    /**
     * List of text values to generate embeddings for.
     */
    values: Array<string>;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV3ProviderOptions;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: SharedV3Headers;
};

/**
 * An embedding is a vector, i.e. an array of numbers.
 * It is e.g. used to represent a text as a vector of word embeddings.
 */
declare type EmbeddingModelV3Embedding = Array<number>;

/**
 * The result of a embedding model doEmbed call.
 */
declare type EmbeddingModelV3Result = {
    /**
     * Generated embeddings. They are in the same order as the input values.
     */
    embeddings: Array<EmbeddingModelV3Embedding>;
    /**
     * Token usage. We only have input tokens for embeddings.
     */
    usage?: {
        tokens: number;
    };
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV3ProviderMetadata;
    /**
     * Optional response information for debugging purposes.
     */
    response?: {
        /**
         * Response headers.
         */
        headers?: SharedV3Headers;
        /**
         * The response body.
         */
        body?: unknown;
    };
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: Array<SharedV3Warning>;
};

/**
 * Specification for an embedding model that implements the embedding model
 * interface version 4.
 *
 * It is specific to text embeddings.
 */
declare type EmbeddingModelV4 = {
    /**
     * The embedding model must specify which embedding model interface
     * version it implements. This will allow us to evolve the embedding
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many embeddings can be generated in a single API call.
     *
     * Use Infinity for models that do not have a limit.
     */
    readonly maxEmbeddingsPerCall: PromiseLike<number | undefined> | number | undefined;
    /**
     * True if the model can handle multiple embedding calls in parallel.
     */
    readonly supportsParallelCalls: PromiseLike<boolean> | boolean;
    /**
     * Generates a list of embeddings for the given input text.
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     */
    doEmbed(options: EmbeddingModelV4CallOptions): PromiseLike<EmbeddingModelV4Result>;
};

/**
 * Specification for an embedding model that implements the embedding model
 * interface version 4.
 *
 * It is specific to text embeddings.
 */
declare type EmbeddingModelV4_2 = {
    /**
     * The embedding model must specify which embedding model interface
     * version it implements. This will allow us to evolve the embedding
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many embeddings can be generated in a single API call.
     *
     * Use Infinity for models that do not have a limit.
     */
    readonly maxEmbeddingsPerCall: PromiseLike<number | undefined> | number | undefined;
    /**
     * True if the model can handle multiple embedding calls in parallel.
     */
    readonly supportsParallelCalls: PromiseLike<boolean> | boolean;
    /**
     * Generates a list of embeddings for the given input text.
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     */
    doEmbed(options: EmbeddingModelV4CallOptions_2): PromiseLike<EmbeddingModelV4Result_2>;
};

declare type EmbeddingModelV4CallOptions = {
    /**
     * List of text values to generate embeddings for.
     */
    values: Array<string>;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: SharedV4Headers;
};

declare type EmbeddingModelV4CallOptions_2 = {
    /**
     * List of text values to generate embeddings for.
     */
    values: Array<string>;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: SharedV4Headers_2;
};

/**
 * An embedding is a vector, i.e. an array of numbers.
 * It is e.g. used to represent a text as a vector of word embeddings.
 */
declare type EmbeddingModelV4Embedding = Array<number>;

/**
 * An embedding is a vector, i.e. an array of numbers.
 * It is e.g. used to represent a text as a vector of word embeddings.
 */
declare type EmbeddingModelV4Embedding_2 = Array<number>;

/**
 * Middleware for EmbeddingModelV4.
 * This type defines the structure for middleware that can be used to modify
 * the behavior of EmbeddingModelV4 operations.
 */
declare type EmbeddingModelV4Middleware = {
    /**
     * Middleware specification version. Use `v4` for the current version.
     */
    readonly specificationVersion: 'v4';
    /**
     * Override the provider name if desired.
     * @param options.model - The embedding model instance.
     */
    overrideProvider?: (options: {
        model: EmbeddingModelV4_2;
    }) => string;
    /**
     * Override the model ID if desired.
     * @param options.model - The embedding model instance.
     */
    overrideModelId?: (options: {
        model: EmbeddingModelV4_2;
    }) => string;
    /**
     * Override the limit of how many embeddings can be generated in a single API call if desired.
     * @param options.model - The embedding model instance.
     */
    overrideMaxEmbeddingsPerCall?: (options: {
        model: EmbeddingModelV4_2;
    }) => PromiseLike<number | undefined> | number | undefined;
    /**
     * Override support for handling multiple embedding calls in parallel, if desired..
     * @param options.model - The embedding model instance.
     */
    overrideSupportsParallelCalls?: (options: {
        model: EmbeddingModelV4_2;
    }) => PromiseLike<boolean> | boolean;
    /**
     * Transforms the parameters before they are passed to the embed model.
     * @param options - Object containing the type of operation and the parameters.
     * @param options.params - The original parameters for the embedding model call.
     * @returns A promise that resolves to the transformed parameters.
     */
    transformParams?: (options: {
        params: EmbeddingModelV4CallOptions_2;
        model: EmbeddingModelV4_2;
    }) => PromiseLike<EmbeddingModelV4CallOptions_2>;
    /**
     * Wraps the embed operation of the embedding model.
     *
     * @param options - Object containing the embed function, parameters, and model.
     * @param options.doEmbed - The original embed function.
     * @param options.params - The parameters for the embed call. If the
     * `transformParams` middleware is used, this will be the transformed parameters.
     * @param options.model - The embedding model instance.
     * @returns A promise that resolves to the result of the generate operation.
     */
    wrapEmbed?: (options: {
        doEmbed: () => ReturnType<EmbeddingModelV4_2['doEmbed']>;
        params: EmbeddingModelV4CallOptions_2;
        model: EmbeddingModelV4_2;
    }) => Promise<Awaited<ReturnType<EmbeddingModelV4_2['doEmbed']>>>;
};

/**
 * The result of a embedding model doEmbed call.
 */
declare type EmbeddingModelV4Result = {
    /**
     * Generated embeddings. They are in the same order as the input values.
     */
    embeddings: Array<EmbeddingModelV4Embedding>;
    /**
     * Token usage. We only have input tokens for embeddings.
     */
    usage?: {
        tokens: number;
    };
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV4ProviderMetadata;
    /**
     * Optional response information for debugging purposes.
     */
    response?: {
        /**
         * Response headers.
         */
        headers?: SharedV4Headers;
        /**
         * The response body.
         */
        body?: unknown;
    };
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: Array<SharedV4Warning>;
};

/**
 * The result of a embedding model doEmbed call.
 */
declare type EmbeddingModelV4Result_2 = {
    /**
     * Generated embeddings. They are in the same order as the input values.
     */
    embeddings: Array<EmbeddingModelV4Embedding_2>;
    /**
     * Token usage. We only have input tokens for embeddings.
     */
    usage?: {
        tokens: number;
    };
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
    /**
     * Optional response information for debugging purposes.
     */
    response?: {
        /**
         * Response headers.
         */
        headers?: SharedV4Headers_2;
        /**
         * The response body.
         */
        body?: unknown;
    };
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: Array<SharedV4Warning_2>;
};

/**
 * Event passed to the `onEnd` callback for embed and embedMany operations.
 *
 * Called when the operation completes, after the embedding model returns.
 */
export declare type EmbedEndEvent = {
    /** Unique identifier for this embed call, used to correlate events. */
    readonly callId: string;
    /** Identifies the operation type (e.g. 'ai.embed' or 'ai.embedMany'). */
    readonly operationId: string;
    /** The provider identifier (e.g., 'openai', 'anthropic'). */
    readonly provider: string;
    /** The specific model identifier (e.g., 'text-embedding-3-small'). */
    readonly modelId: string;
    /** The value(s) that were embedded. A string for embed, an array for embedMany. */
    readonly value: string | Array<string>;
    /** The resulting embedding(s). A single vector for embed, an array for embedMany. */
    readonly embedding: Embedding | Array<Embedding>;
    /** Token usage for the embedding operation. */
    readonly usage: EmbeddingModelUsage;
    /** Warnings from the embedding model, e.g. unsupported settings. */
    readonly warnings: Array<Warning>;
    /** Optional provider-specific metadata. */
    readonly providerMetadata: ProviderMetadata | undefined;
    /** Response data including headers and body. A single response for embed, an array for embedMany. */
    readonly response: {
        headers?: Record<string, string>;
        body?: unknown;
    } | Array<{
        headers?: Record<string, string>;
        body?: unknown;
    } | undefined> | undefined;
};

/**
 * Embed several values using an embedding model. The type of the value is defined
 * by the embedding model.
 *
 * `embedMany` automatically splits large requests into smaller chunks if the model
 * has a limit on how many embeddings can be generated in a single call.
 *
 * @param model - The embedding model to use.
 * @param values - The values that should be embedded.
 *
 * @param maxRetries - Maximum number of retries. Set to 0 to disable retries. Default: 2.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param headers - Additional HTTP headers to be sent with the request. Only applicable for HTTP-based providers.
 *
 * @param maxParallelCalls - Maximum number of concurrent requests. Default: Infinity.
 *
 * @param telemetry - Optional telemetry configuration.
 *
 * @param providerOptions - Additional provider-specific options. They are passed through
 * to the provider from the AI SDK and enable provider-specific
 * functionality that can be fully encapsulated in the provider.
 *
 * @returns A result object that contains the embeddings, the value, and additional information.
 */
export declare function embedMany({ model: modelArg, values, maxParallelCalls, maxRetries: maxRetriesArg, abortSignal, headers, providerOptions, experimental_telemetry, telemetry, onStart, experimental_onStart, onEnd, experimental_onEnd, _internal: { generateCallId }, }: {
    /**
     * The embedding model to use.
     */
    model: EmbeddingModel;
    /**
     * The values that should be embedded.
     */
    values: Array<string>;
    /**
     * Maximum number of retries per embedding model call. Set to 0 to disable retries.
     *
     * @default 2
     */
    maxRetries?: number;
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional headers to include in the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string>;
    /**
     * Optional telemetry configuration.
     */
    telemetry?: TelemetryOptions;
    /**
     * Optional telemetry configuration.
     *
     * @deprecated Use `telemetry` instead. This alias will be removed in a future major release.
     */
    experimental_telemetry?: TelemetryOptions;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Maximum number of concurrent requests.
     *
     * @default Infinity
     */
    maxParallelCalls?: number;
    /**
     * Callback that is called when the embedMany operation begins,
     * before the embedding model is called.
     */
    onStart?: Callback<EmbedStartEvent>;
    /**
     * Callback that is called when the embedMany operation begins,
     * before the embedding model is called.
     *
     * @deprecated Use `onStart` instead.
     */
    experimental_onStart?: Callback<EmbedStartEvent>;
    /**
     * Callback that is called when the embedMany operation completes,
     * after all embedding model calls return.
     */
    onEnd?: Callback<EmbedEndEvent>;
    /**
     * Callback that is called when the embedMany operation completes,
     * after all embedding model calls return.
     *
     * @deprecated Use `onEnd` instead.
     */
    experimental_onEnd?: Callback<EmbedEndEvent>;
    /**
     * Internal. For test use only. May change without notice.
     */
    _internal?: {
        generateCallId?: () => string;
    };
}): Promise<EmbedManyResult>;

/**
 * The result of an `embedMany` call.
 * It contains the embeddings, the values, and additional information.
 */
export declare interface EmbedManyResult {
    /**
     * The values that were embedded.
     */
    readonly values: Array<string>;
    /**
     * The embeddings. They are in the same order as the values.
     */
    readonly embeddings: Array<Embedding>;
    /**
     * The embedding token usage.
     */
    readonly usage: EmbeddingModelUsage;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    readonly warnings: Array<Warning>;
    /**
     * Optional provider-specific metadata.
     */
    readonly providerMetadata?: ProviderMetadata;
    /**
     * Optional raw response data.
     */
    readonly responses?: Array<{
        /**
         * Response headers.
         */
        headers?: Record<string, string>;
        /**
         * The response body.
         */
        body?: unknown;
    } | undefined>;
}

/**
 * The result of an `embed` call.
 * It contains the embedding, the value, and additional information.
 */
export declare interface EmbedResult {
    /**
     * The value that was embedded.
     */
    readonly value: string;
    /**
     * The embedding of the value.
     */
    readonly embedding: Embedding;
    /**
     * The embedding token usage.
     */
    readonly usage: EmbeddingModelUsage;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    readonly warnings: Array<Warning>;
    /**
     * Optional provider-specific metadata.
     */
    readonly providerMetadata?: ProviderMetadata;
    /**
     * Optional response data.
     */
    readonly response?: {
        /**
         * Response headers.
         */
        headers?: Record<string, string>;
        /**
         * The response body.
         */
        body?: unknown;
    };
}

/**
 * Event passed to the `onStart` callback for embed and embedMany operations.
 *
 * Called when the operation begins, before the embedding model is called.
 */
export declare type EmbedStartEvent = {
    /** Unique identifier for this embed call, used to correlate events. */
    readonly callId: string;
    /** Identifies the operation type (e.g. 'ai.embed' or 'ai.embedMany'). */
    readonly operationId: string;
    /** The provider identifier (e.g., 'openai', 'anthropic'). */
    readonly provider: string;
    /** The specific model identifier (e.g., 'text-embedding-3-small'). */
    readonly modelId: string;
    /** The value(s) being embedded. A string for embed, an array for embedMany. */
    readonly value: string | Array<string>;
    /** Maximum number of retries for failed requests. */
    readonly maxRetries: number;
    /** Additional HTTP headers sent with the request. */
    readonly headers: Record<string, string | undefined> | undefined;
    /** Additional provider-specific options. */
    readonly providerOptions: ProviderOptions_2 | undefined;
};

export declare class EmptyResponseBodyError extends AISDKError {
    private readonly [symbol$c];
    constructor({ message }?: {
        message?: string;
    });
    static isInstance(error: unknown): error is EmptyResponseBodyError;
}

declare type EnrichedStreamPart<TOOLS extends ToolSet, PARTIAL_OUTPUT> = {
    part: TextStreamPart<TOOLS>;
    partialOutput: PARTIAL_OUTPUT | undefined;
};

export declare type ErrorHandler = (error: unknown) => void;

/**
 * A parsed EventSource message event
 *
 * @public
 */
declare interface EventSourceMessage {
    /**
     * The event type sent from the server. Note that this differs from the browser `EventSource`
     * implementation in that browsers will default this to `message`, whereas this parser will
     * leave this as `undefined` if not explicitly declared.
     */
    event?: string | undefined;
    /**
     * ID of the message, if any was provided by the server. Can be used by clients to keep the
     * last received message ID in sync when reconnecting.
     */
    id?: string | undefined;
    /**
     * The data received for this message
     */
    data: string;
}

/**
 * A TransformStream that ingests a stream of strings and produces a stream of `EventSourceMessage`.
 *
 * @example Basic usage
 * ```
 * const eventStream =
 *   response.body
 *     .pipeThrough(new TextDecoderStream())
 *     .pipeThrough(new EventSourceParserStream())
 * ```
 *
 * @example Terminate stream on parsing errors
 * ```
 * const eventStream =
 *  response.body
 *   .pipeThrough(new TextDecoderStream())
 *   .pipeThrough(new EventSourceParserStream({terminateOnError: true}))
 * ```
 *
 * @public
 */
declare class EventSourceParserStream extends TransformStream<
string,
EventSourceMessage
> {
    constructor({ onError, onRetry, onComment }?: StreamOptions);
}

declare type ExaSearchCategory = 'company' | 'people' | 'research paper' | 'news' | 'personal site' | 'financial report';

declare interface ExaSearchConfig {
    /**
     * Default search method. Exa defaults to auto when omitted.
     */
    type?: ExaSearchType;
    /**
     * Default maximum number of results to return (1-100, default: 10).
     */
    numResults?: number;
    /**
     * Default category filter for result types.
     */
    category?: ExaSearchCategory;
    /**
     * Default two-letter ISO country code for location-aware search.
     */
    userLocation?: string;
    /**
     * Default domains to include or exclude.
     */
    includeDomains?: string[];
    excludeDomains?: string[];
    /**
     * Default published date filters in ISO 8601 format.
     */
    startPublishedDate?: string;
    endPublishedDate?: string;
    /**
     * Default content extraction controls.
     */
    contents?: ExaSearchContentsConfig;
}

declare interface ExaSearchContentsConfig {
    text?: boolean | ExaSearchTextConfig;
    highlights?: boolean | ExaSearchHighlightsConfig;
    maxAgeHours?: number;
    livecrawlTimeout?: number;
    subpages?: number;
    subpageTarget?: string | string[];
    extras?: ExaSearchExtrasConfig;
}

declare interface ExaSearchError {
    error: 'api_error' | 'rate_limit' | 'timeout' | 'invalid_input' | 'configuration_error' | 'execution_error' | 'unknown';
    statusCode?: number;
    message: string;
}

declare interface ExaSearchExtrasConfig {
    links?: number;
    imageLinks?: number;
}

declare interface ExaSearchHighlightsConfig {
    query?: string;
    maxCharacters?: number;
}

declare interface ExaSearchInput {
    query: string;
    type?: ExaSearchType;
    num_results?: number;
    category?: ExaSearchCategory;
    user_location?: string;
    include_domains?: string[];
    exclude_domains?: string[];
    start_published_date?: string;
    end_published_date?: string;
    contents?: {
        text?: boolean | {
            max_characters?: number;
            include_html_tags?: boolean;
            verbosity?: 'compact' | 'standard' | 'full';
            include_sections?: ExaTextSection[];
            exclude_sections?: ExaTextSection[];
        };
        highlights?: boolean | {
            query?: string;
            max_characters?: number;
        };
        max_age_hours?: number;
        livecrawl_timeout?: number;
        subpages?: number;
        subpage_target?: string | string[];
        extras?: {
            links?: number;
            image_links?: number;
        };
    };
}

declare type ExaSearchOutput = ExaSearchResponse | ExaSearchError;

declare interface ExaSearchResponse {
    requestId: string;
    searchType?: string;
    resolvedSearchType?: string;
    results: ExaSearchResult[];
    costDollars?: {
        total?: number;
        search?: Record<string, number>;
    };
}

declare interface ExaSearchResult {
    title: string;
    url: string;
    id: string;
    publishedDate?: string | null;
    author?: string | null;
    image?: string | null;
    favicon?: string | null;
    text?: string;
    highlights?: string[];
    highlightScores?: number[];
    summary?: string;
    subpages?: ExaSearchResult[];
    extras?: {
        links?: string[];
        imageLinks?: string[];
    };
}

declare interface ExaSearchTextConfig {
    maxCharacters?: number;
    includeHtmlTags?: boolean;
    verbosity?: 'compact' | 'standard' | 'full';
    includeSections?: ExaTextSection[];
    excludeSections?: ExaTextSection[];
}

declare const exaSearchToolFactory: _ai_sdk_provider_utils.ProviderExecutedToolFactory<ExaSearchInput, ExaSearchOutput, ExaSearchConfig, {}>;

declare type ExaSearchType = 'auto' | 'fast' | 'instant';

declare type ExaTextSection = 'header' | 'navigation' | 'banner' | 'body' | 'sidebar' | 'footer' | 'metadata';

/**
 * A tool that is guaranteed to expose an execute function.
 */
declare type ExecutableTool<TOOL extends Tool_2 = Tool_2> = TOOL & {
    execute: NonNullable<TOOL['execute']>;
};

/**
 * A tool that is guaranteed to expose an execute function.
 */
declare type ExecutableTool_2<TOOL extends Tool = Tool> = TOOL & {
    execute: NonNullable<TOOL['execute']>;
};

/**
 * Executes a tool function and normalizes its results into a stream of outputs.
 *
 * - If the tool's `execute` function returns an `AsyncIterable`, each yielded value is emitted as
 *   `{ type: "preliminary", output }`. After iteration completes, the last yielded value is emitted
 *   again as `{ type: "final", output }`.
 * - If the tool returns a direct value or Promise, a single `{ type: "final", output }` is yielded.
 *
 * @param params.tool The tool whose `execute` function should be invoked.
 * @param params.input The input value to pass to the tool.
 * @param params.options Additional options for tool execution.
 * @yields A preliminary output for each streamed value, followed by a final output, or a single final
 * output for non-streaming tools.
 */
declare function executeTool<TOOL extends Tool_2>({ tool, input, options, }: {
    tool: ExecutableTool<TOOL>;
    input: InferToolInput_2<TOOL>;
    options: ToolExecutionOptions_2<InferToolContext<TOOL>>;
}): AsyncGenerator<{
    type: 'preliminary';
    output: InferToolOutput_2<TOOL>;
} | {
    type: 'final';
    output: InferToolOutput_2<TOOL>;
}>;

/**
 * Executes a tool function and normalizes its results into a stream of outputs.
 *
 * - If the tool's `execute` function returns an `AsyncIterable`, each yielded value is emitted as
 *   `{ type: "preliminary", output }`. After iteration completes, the last yielded value is emitted
 *   again as `{ type: "final", output }`.
 * - If the tool returns a direct value or Promise, a single `{ type: "final", output }` is yielded.
 *
 * @param params.tool The tool whose `execute` function should be invoked.
 * @param params.input The input value to pass to the tool.
 * @param params.options Additional options for tool execution.
 * @yields A preliminary output for each streamed value, followed by a final output, or a single final
 * output for non-streaming tools.
 */
declare function executeTool_2<TOOL extends Tool>({ tool, input, options, }: {
    tool: ExecutableTool_2<TOOL>;
    input: InferToolInput<TOOL>;
    options: ToolExecutionOptions<InferToolContext_2<TOOL>>;
}): AsyncGenerator<{
    type: 'preliminary';
    output: InferToolOutput<TOOL>;
} | {
    type: 'final';
    output: InferToolOutput<TOOL>;
}>;

export declare abstract class Experimental_AbstractRealtimeSession {
    protected state: Experimental_RealtimeState;
    protected maxEvents: number;
    onToolCall: Experimental_RealtimeSessionOptions['onToolCall'];
    onEvent: ((event: Experimental_RealtimeServerEvent) => void) | undefined;
    onError: ((error: Error) => void) | undefined;
    private readonly model;
    private readonly api;
    private readonly sessionConfig;
    private readonly reducer;
    private readonly transport;
    private readonly audio;
    private currentResponseItemId;
    private readonly toolCallsInResponse;
    private readonly submittedToolOutputs;
    private responseToolCallsClosed;
    protected abstract setState<K extends keyof Experimental_RealtimeState>(key: K, value: Experimental_RealtimeState[K]): void;
    constructor(options: Experimental_RealtimeSessionOptions);
    connect(): Promise<void>;
    disconnect(): void;
    sendEvent(event: Experimental_RealtimeClientEvent): void;
    sendTextMessage(text: string): void;
    sendAudio(base64Audio: string): void;
    commitAudio(): void;
    clearAudioBuffer(): void;
    requestResponse(options?: {
        modalities?: string[];
    }): void;
    cancelResponse(): void;
    addToolOutput(callId: string, result: unknown): void;
    /**
     * Requests a single response once the tool-bearing response has finished
     * delivering its tool calls and every one of them has an output. Requesting a
     * response after each individual output can cause the model to continue
     * without the full tool context on multi-tool turns.
     */
    private maybeRequestToolResponse;
    startAudioCapture(stream: MediaStream): void;
    stopAudioCapture(): void;
    stopPlayback(): void;
    dispose(): void;
    private applyState;
    private executeToolCall;
    private handleServerEvent;
    private handleReducerEffect;
}

/**
 * @deprecated Use `createProviderRegistry` instead.
 */
export declare const experimental_createProviderRegistry: typeof createProviderRegistry;

/**
 * Converts a base64-encoded PCM16 string (from a realtime model's
 * audio-delta event) back to Float32 audio samples.
 *
 * Input is expected to be 16-bit signed integer, little-endian, base64-encoded.
 * Output samples are in the range [-1.0, 1.0].
 */
export declare function experimental_decodeRealtimeAudio(base64Audio: string): Float32Array;

/**
 * Experimental. Can change in patch versions without warning.
 *
 * Download function. Called with the array of URLs and a boolean indicating
 * whether the URL is supported by the model.
 *
 * The download function can decide for each URL:
 * - to return null (which means that the URL should be passed to the model)
 * - to download the asset and return the data (incl. retries, authentication, etc.)
 *
 * Should throw DownloadError if the download fails.
 *
 * Should return an array of objects sorted by the order of the requested downloads.
 * For each object, the data should be a Uint8Array if the URL was downloaded.
 * For each object, the mediaType should be the media type of the downloaded asset.
 * For each object, the data should be null if the URL should be passed through as is.
 */
export declare type Experimental_DownloadFunction = (options: Array<{
    url: URL;
    isUrlSupportedByModel: boolean;
}>) => PromiseLike<Array<{
    data: Uint8Array;
    mediaType: string | undefined;
} | null>>;

/**
 * Converts Float32 audio samples to a base64-encoded PCM16 string
 * for sending to a realtime model via input_audio_buffer.append.
 *
 * Samples are expected to be in the range [-1.0, 1.0].
 * Output is 16-bit signed integer, little-endian, base64-encoded.
 */
export declare function experimental_encodeRealtimeAudio(float32Array: Float32Array): string;

/**
 * Filters the tools to only include the active tools.
 * When activeTools is provided, we only include the tools that are in the list.
 *
 * @param tools - The tools to filter.
 * @param activeTools - The active tools to include.
 * @returns The filtered tools.
 */
export declare function experimental_filterActiveTools<TOOLS extends ToolSet | undefined, ACTIVE_TOOL_NAMES extends ActiveTools<NonNullable<TOOLS>>>({ tools, activeTools, }: {
    tools: TOOLS;
    activeTools: ACTIVE_TOOL_NAMES;
}): ActiveToolSubset<TOOLS, ACTIVE_TOOL_NAMES>;

/**
 * @deprecated Use `GeneratedFile` instead. This alias will be removed in v8.
 */
export declare type Experimental_GeneratedImage = GeneratedFile;

/**
 * @deprecated Use `generateSpeech` instead.
 */
export declare const experimental_generateSpeech: typeof generateSpeech;

export declare function experimental_generateVideo({ model: modelArg, prompt: promptArg, n, maxVideosPerCall, aspectRatio, resolution, duration, fps, seed, frameImages, inputReferences, generateAudio, providerOptions, maxRetries: maxRetriesArg, abortSignal, headers, download: downloadFn, poll, webhook, }: {
    /**
     * The video model to use.
     */
    model: VideoModel;
    /**
     * The prompt that should be used to generate the video.
     */
    prompt: GenerateVideoPrompt;
    /**
     * Number of videos to generate.
     */
    n?: number;
    /**
     * Maximum number of videos per API call. If not provided, the model's default will be used.
     */
    maxVideosPerCall?: number;
    /**
     * Aspect ratio of the videos to generate. Must have the format `{width}:{height}`.
     */
    aspectRatio?: `${number}:${number}`;
    /**
     * Resolution of the videos to generate. Must have the format `{width}x${height}`.
     */
    resolution?: `${number}x${number}`;
    /**
     * Duration of the video in seconds.
     */
    duration?: number;
    /**
     * Frames per second for the video.
     */
    fps?: number;
    /**
     * Seed for the video generation.
     */
    seed?: number;
    /**
     * Role-tagged image inputs for image-to-video and first-last-frame generation.
     */
    frameImages?: Array<{
        /**
         * The image for this frame.
         */
        image: DataContent;
        /**
         * Which frame this image represents.
         */
        frameType: VideoModelV4FrameType_2;
    }>;
    /**
     * Reference inputs for reference-to-video generation.
     *
     * Each entry may be a plain image/video ({@link DataContent}), or an object
     * form that carries an explicit `mediaType`.
     */
    inputReferences?: Array<DataContent | {
        /**
         * The reference image or video.
         */
        data: DataContent;
        /**
         * The media type of the reference (e.g. 'image/png',
         * 'video/mp4').
         */
        mediaType?: string;
    }>;
    /**
     * Whether the model should generate audio alongside the video.
     */
    generateAudio?: boolean;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Maximum number of retries per video model call. Set to 0 to disable retries.
     *
     * @default 2
     */
    maxRetries?: number;
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional headers to include in the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string>;
    /**
     * Custom download function for fetching videos from URLs.
     * Use `createDownload()` from `ai` to create a download function with custom size limits.
     *
     * @default createDownload() (2 GiB limit)
     */
    download?: (options: {
        url: URL;
        abortSignal?: AbortSignal;
    }) => Promise<{
        data: Uint8Array;
        mediaType: string | undefined;
    }>;
    /**
     * Polling configuration for models that support the asynchronous
     * start/status flow. When provided and the model implements `doStart`
     * and `doStatus`, the SDK will orchestrate polling automatically.
     *
     * This option can be combined with `webhook`: `timeoutMs` limits the webhook
     * wait, and the polling settings apply if the model does not support
     * webhooks.
     */
    poll?: GenerateVideoPollOptions;
    /**
     * Webhook factory for models that support the asynchronous
     * start/status flow. When provided and the model implements `doStart`
     * and `doStatus`, the SDK will use webhooks instead of polling.
     *
     * The factory should return a URL for the provider to send notifications to,
     * and a `received` promise that resolves when the notification arrives.
     * `poll` can also be provided to configure the webhook timeout and polling
     * fallback.
     */
    webhook?: GenerateVideoWebhookFactory;
}): Promise<GenerateVideoResult>;

export declare function experimental_getRealtimeToolDefinitions<TOOLS extends ToolSet>({ tools, toolsContext, }: {
    tools: TOOLS;
    toolsContext?: InferToolSetContext_2<TOOLS>;
}): Promise<Experimental_RealtimeToolDefinition[]>;

export declare type Experimental_LanguageModelStreamPart<TOOLS extends ToolSet = ToolSet> = Exclude<TextStreamPart<TOOLS>, {
    type: 'finish' | 'stream-start' | 'tool-output-denied' | 'start-step' | 'finish-step' | 'start' | 'abort';
}> | TextStreamTextDeltaPart | TextStreamReasoningDeltaPart | TextStreamFilePart | TextStreamReasoningFilePart | TextStreamToolApprovalRequestPart<TOOLS> | TextStreamToolApprovalResponsePart<TOOLS> | TextStreamToolCallPart<TOOLS> | TextStreamToolResultPart<TOOLS> | TextStreamToolErrorPart<TOOLS> | {
    type: 'model-call-end';
    finishReason: FinishReason;
    rawFinishReason: string | undefined;
    usage: LanguageModelUsage;
    providerMetadata?: ProviderMetadata;
    performance: {
        responseTimeMs: number;
        effectiveOutputTokensPerSecond: number;
        outputTokensPerSecond: number | undefined;
        inputTokensPerSecond: number | undefined;
        effectiveTotalTokensPerSecond: number;
        timeToFirstOutputMs: number | undefined;
        timeBetweenOutputChunksMs?: OutputChunkTimingStats;
    };
} | {
    type: 'model-call-start';
    warnings: Array<CallWarning>;
} | {
    type: 'model-call-response-metadata';
    /**
     * ID for the generated response, if the provider sends one.
     */
    id?: string;
    /**
     * Timestamp for the start of the generated response, if the provider sends one.
     */
    timestamp?: Date;
    /**
     * The ID of the response model that was used to generate the response, if the provider sends one.
     */
    modelId?: string;
};

export declare type Experimental_RealtimeClientEvent = RealtimeModelV4ClientEvent_2;

export declare type Experimental_RealtimeFactory = RealtimeFactoryV4_2;

export declare type Experimental_RealtimeFactoryGetTokenOptions = RealtimeFactoryV4GetTokenOptions_2;

export declare type Experimental_RealtimeFactoryGetTokenResult = RealtimeFactoryV4GetTokenResult_2;

export declare type Experimental_RealtimeModel = RealtimeModelV4_2;

export declare type Experimental_RealtimeServerEvent = RealtimeModelV4ServerEvent_2;

export declare type Experimental_RealtimeSessionConfig = RealtimeModelV4SessionConfig_2;

export declare type Experimental_RealtimeSessionOptions = {
    model: Experimental_RealtimeModel;
    api: {
        token: string;
    };
    sessionConfig?: Partial<Experimental_RealtimeSessionConfig>;
    sampleRate?: number;
    maxEvents?: number;
    onToolCall?: (args: {
        toolCall: {
            toolCallId: string;
            toolName: string;
            args: unknown;
        };
    }) => Promise<unknown> | unknown | undefined;
    onEvent?: (event: Experimental_RealtimeServerEvent) => void;
    onError?: (error: Error) => void;
};

/**
 * Response shape for the realtime setup/token endpoint.
 * The client uses this to establish a WebSocket connection and
 * configure the session with tool definitions.
 */
export declare type Experimental_RealtimeSetupResponse = {
    token: string;
    url: string;
    expiresAt?: number;
    tools: Experimental_RealtimeToolDefinition[];
};

export declare interface Experimental_RealtimeState {
    status: Experimental_RealtimeStatus;
    messages: UIMessage[];
    events: Experimental_RealtimeServerEvent[];
    isCapturing: boolean;
    isPlaying: boolean;
}

export declare type Experimental_RealtimeStatus = 'disconnected' | 'connecting' | 'connected' | 'error';

export declare type Experimental_RealtimeToolDefinition = RealtimeModelV4ToolDefinition_2;

/**
 * Resamples audio from one sample rate to another using linear
 * interpolation. Suitable for voice audio.
 *
 * @param input - Float32 audio samples at the input sample rate.
 * @param inputRate - The sample rate of the input audio (e.g. 48000).
 * @param outputRate - The desired output sample rate (e.g. 24000).
 * @returns Float32 audio samples at the output sample rate.
 */
export declare function experimental_resampleAudio(input: Float32Array, inputRate: number, outputRate: number): Float32Array;

/**
 * Handle to a long-running process started via `SandboxSession.spawn`.
 */
export declare type Experimental_SandboxProcess = {
    /**
     * Process identifier, if the sandbox implementation exposes one.
     */
    readonly pid?: number;
    /**
     * Stream of bytes written by the process to standard output.
     */
    readonly stdout: ReadableStream<Uint8Array>;
    /**
     * Stream of bytes written by the process to standard error.
     */
    readonly stderr: ReadableStream<Uint8Array>;
    /**
     * Resolve when the process exits, yielding its exit code.
     */
    wait(): PromiseLike<{
        exitCode: number;
    }>;
    /**
     * Terminate the process. Idempotent.
     */
    kill(): PromiseLike<void>;
};

/**
 * Sandbox session that can execute commands and read/write files.
 */
export declare type Experimental_SandboxSession = {
    /**
     * Description of the sandbox environment that can be added to the agent's instructions
     * so that the agent knows about relevant details such as the root directory, exposed
     * ports, the public hostname, etc.
     */
    readonly description: string;
    /**
     * Read one file from the sandbox as a stream of bytes. Resolves to `null`
     * when the file does not exist.
     *
     * Relative path handling is implementation-defined. This is the lowest-level
     * read primitive; prefer `readBinaryFile` or `readTextFile` unless you need
     * to stream bytes.
     */
    readonly readFile: (options: ReadFileOptions_2) => PromiseLike<ReadableStream<Uint8Array> | null>;
    /**
     * Read one file from the sandbox as raw bytes. Resolves to `null` when the
     * file does not exist.
     */
    readonly readBinaryFile: (options: ReadFileOptions_2) => PromiseLike<Uint8Array | null>;
    /**
     * Read one text file from the sandbox, decoded using the requested encoding.
     * Resolves to `null` when the file does not exist.
     *
     * Line ranges are 1-based and inclusive. When `endLine` is past EOF the read
     * returns through EOF without error.
     */
    readonly readTextFile: (options: ReadFileOptions_2 & {
        /**
         * Text encoding used to decode the file bytes. Defaults to `"utf-8"`.
         */
        encoding?: string;
        /**
         * 1-based inclusive start line. Defaults to 1.
         */
        startLine?: number;
        /**
         * 1-based inclusive end line. When past the file's line count, the read
         * returns through EOF without error.
         */
        endLine?: number;
    }) => PromiseLike<string | null>;
    /**
     * Write one file to the sandbox from a stream of bytes. Creates parent
     * directories recursively and overwrites any existing file.
     *
     * This is the lowest-level write primitive; prefer `writeBinaryFile` or
     * `writeTextFile` when the full content is already materialized in memory.
     */
    readonly writeFile: (options: WriteFileOptions_2<ReadableStream<Uint8Array>>) => PromiseLike<void>;
    /**
     * Write one file to the sandbox from raw bytes. Creates parent directories
     * recursively and overwrites any existing file.
     */
    readonly writeBinaryFile: (options: WriteFileOptions_2<Uint8Array>) => PromiseLike<void>;
    /**
     * Write one file to the sandbox from a string, encoded using the requested
     * encoding. Creates parent directories recursively and overwrites any
     * existing file.
     */
    readonly writeTextFile: (options: WriteFileOptions_2<string> & {
        /**
         * Text encoding used to encode the string to bytes. Defaults to `"utf-8"`.
         */
        encoding?: string;
    }) => PromiseLike<void>;
    /**
     * Spawn a long-running process in the sandbox. Returns immediately with a
     * handle that streams stdout/stderr, can be waited on, and can be killed.
     *
     * `run` is conceptually a thin wrapper over this primitive: spawn,
     * collect both streams to strings, await `wait()`, return the result.
     */
    readonly spawn: (options: SandboxProcessOptions_2) => PromiseLike<Experimental_SandboxProcess>;
    /**
     * Run a command in the sandbox.
     */
    readonly run: (options: SandboxProcessOptions_2) => PromiseLike<{
        /**
         * Exit code returned by the command.
         */
        exitCode: number;
        /**
         * Standard output produced by the command.
         */
        stdout: string;
        /**
         * Standard error produced by the command.
         */
        stderr: string;
    }>;
};

/**
 * @deprecated Use `SpeechResult` instead.
 */
export declare type Experimental_SpeechResult = SpeechResult;

/**
 * Streams a single language model call after standardizing the prompt and tools.
 *
 * The returned stream emits model call parts together with request and response
 * metadata when available.
 *
 * @param model - The language model to use.
 * @param tools - Tools that are accessible to and can be called by the model. The model needs to support calling tools.
 * @param output - Output configuration that controls the response format requested from the model.
 * @param toolChoice - The tool choice strategy for the model call.
 *
 * @param system - A system message that will be part of the prompt.
 * @param prompt - A simple text prompt. You can either use `prompt` or `messages` but not both.
 * @param messages - A list of messages. You can either use `prompt` or `messages` but not both.
 * @param allowSystemInMessages - Whether system messages are allowed in the `prompt` or `messages` fields. Default: false.
 *
 * @param maxOutputTokens - Maximum number of tokens to generate.
 * @param temperature - Temperature setting.
 * The value is passed through to the provider. The range depends on the provider and model.
 * It is recommended to set either `temperature` or `topP`, but not both.
 * @param topP - Nucleus sampling.
 * The value is passed through to the provider. The range depends on the provider and model.
 * It is recommended to set either `temperature` or `topP`, but not both.
 * @param topK - Only sample from the top K options for each subsequent token.
 * Used to remove "long tail" low probability responses.
 * Recommended for advanced use cases only. You usually only need to use temperature.
 * @param presencePenalty - Presence penalty setting.
 * It affects the likelihood of the model to repeat information that is already in the prompt.
 * The value is passed through to the provider. The range depends on the provider and model.
 * @param frequencyPenalty - Frequency penalty setting.
 * It affects the likelihood of the model to repeatedly use the same words or phrases.
 * The value is passed through to the provider. The range depends on the provider and model.
 * @param stopSequences - Stop sequences.
 * If set, the model will stop generating text when one of the stop sequences is generated.
 * @param seed - The seed (integer) to use for random sampling.
 * If set and supported by the model, calls will generate deterministic results.
 * @param reasoning - Reasoning configuration for the model call.
 *
 * @param download - A function that downloads URLs as part of prompt conversion.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param headers - Additional HTTP headers to be sent with the request.
 * @param includeRawChunks - Whether to include raw provider stream chunks in the model stream.
 * @param providerOptions - Additional provider-specific options.
 * @param repairToolCall - A function that can repair invalid tool calls before they are emitted.
 * @param refineToolInput - Optional mapping of tool names to functions that refine parsed tool inputs before they are emitted, used for telemetry, or executed.
 * @param onStart - A callback that receives the fully converted prompt before the model call starts.
 *
 * @returns A stream of model call parts together with request and response metadata when available.
 */
export declare function experimental_streamLanguageModelCall<TOOLS extends ToolSet, OUTPUT extends Output_2 = Output_2>({ model, tools, toolOrder, output, toolChoice, prompt, system, instructions, messages, allowSystemInMessages, download, abortSignal, headers, includeRawChunks, providerOptions, repairToolCall, refineToolInput, executeLanguageModelCallInTelemetryContext, callId, toolsContext, experimental_sandbox: sandbox, _internal: { generateId, generateCallId, now, }, onStart, onLanguageModelCallStart, onLanguageModelCallEnd, ...callSettings }: {
    model: LanguageModel;
    tools?: TOOLS;
    toolOrder?: ToolOrder<TOOLS>;
    output?: OUTPUT;
    toolChoice?: ToolChoice<TOOLS>;
    download?: Experimental_DownloadFunction;
    abortSignal?: AbortSignal;
    headers?: Record<string, string | undefined>;
    includeRawChunks?: boolean;
    providerOptions?: ProviderOptions_2;
    repairToolCall?: ToolCallRepairFunction<TOOLS> | undefined;
    refineToolInput?: ToolInputRefinement<TOOLS> | undefined;
    executeLanguageModelCallInTelemetryContext?: Telemetry['executeLanguageModelCall'];
    callId?: string;
    /**
     * Tool context used to resolve per-call tool metadata such as function
     * descriptions before sending tools to the model.
     */
    toolsContext?: InferToolSetContext_2<TOOLS>;
    /**
     * Sandbox session passed through for resolving tool descriptions that depend on it.
     */
    experimental_sandbox?: Experimental_SandboxSession;
    _internal?: {
        generateId?: IdGenerator;
        generateCallId?: IdGenerator;
        now?: () => number;
    };
    onLanguageModelCallStart?: Arrayable_2<OnLanguageModelCallStartCallback>;
    onLanguageModelCallEnd?: Arrayable_2<OnLanguageModelCallEndCallback<TOOLS>>;
    onStart?: (args: {
        promptMessages: LanguageModelV4Prompt_2;
    }) => Promise<void> | void;
} & Prompt & LanguageModelCallOptions): Promise<{
    stream: AsyncIterableStream<Experimental_LanguageModelStreamPart<TOOLS>>;
    request?: {
        /**
         * Request HTTP body that was sent to the provider API.
         */
        body?: unknown;
    };
    response?: {
        /**
         * Response headers.
         */
        headers?: SharedV4Headers_2;
    };
}>;

/**
 * Streams transcripts using a transcription model.
 *
 * @param model - The transcription model to use.
 * @param audio - Raw audio chunks to transcribe.
 * @param inputAudioFormat - The input audio format for the raw audio chunks.
 * @param providerOptions - Additional provider-specific options.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param headers - Additional HTTP/WebSocket headers to send when supported by the provider.
 *
 * @returns A result object that contains the streaming transcript and final transcript metadata.
 */
export declare function experimental_streamTranscribe({ model, audio, inputAudioFormat, providerOptions, abortSignal, headers, includeRawChunks, _internal: { currentDate }, }: {
    /**
     * The transcription model to use.
     */
    model: TranscriptionModel;
    /**
     * Raw audio chunks to transcribe.
     */
    audio: ReadableStream<Uint8Array | string>;
    /**
     * The input audio format for the raw audio chunks.
     */
    inputAudioFormat: SharedV4AudioFormat_2;
    /**
     * Additional provider-specific options.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional headers to include in the request, if supported by the provider.
     */
    headers?: Record<string, string>;
    /**
     * When true, providers should include raw provider chunks in the stream.
     */
    includeRawChunks?: boolean;
    /**
     * Internal test hooks.
     */
    _internal?: {
        currentDate?: () => Date;
    };
}): StreamTranscriptionResult;

/**
 * Streams speech-to-speech translations using a speech translation model.
 *
 * @param model - The speech translation model to use.
 * @param audio - Raw audio chunks to translate.
 * @param inputAudioFormat - The input audio format for the raw audio chunks.
 * @param targetLanguage - The language to translate the audio into.
 * @param sourceLanguage - The language of the source audio. Auto-detected when absent.
 * @param outputAudioFormat - The desired audio format for translated audio chunks.
 * @param providerOptions - Additional provider-specific options.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param headers - Additional HTTP/WebSocket headers to send when supported by the provider.
 * @param includeRawChunks - When true, providers include raw provider chunks in the stream as `raw` parts.
 *
 * @returns A result object that contains the streaming translation and final translation metadata.
 */
export declare function experimental_streamTranslate({ model, audio, inputAudioFormat, targetLanguage, sourceLanguage, outputAudioFormat, providerOptions, abortSignal, headers, includeRawChunks, _internal: { currentDate }, }: {
    /**
     * The speech translation model to use.
     */
    model: SpeechTranslationModel;
    /**
     * Raw audio chunks to translate.
     */
    audio: ReadableStream<Uint8Array | string>;
    /**
     * The input audio format for the raw audio chunks.
     */
    inputAudioFormat: SharedV4AudioFormat_2;
    /**
     * The language to translate the audio into, as a BCP-47-style language
     * tag (e.g. `en`, `es`, `fr-CA`). Supported values are provider-specific
     * and validated by the provider.
     */
    targetLanguage: string;
    /**
     * The language of the source audio, as a BCP-47-style language tag.
     * When absent, providers auto-detect the source language.
     */
    sourceLanguage?: string;
    /**
     * The desired audio format for translated audio chunks.
     * When absent, the provider default output format is used.
     */
    outputAudioFormat?: SharedV4AudioFormat_2;
    /**
     * Additional provider-specific options.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional headers to include in the request, if supported by the provider.
     */
    headers?: Record<string, string>;
    /**
     * When true, providers should include raw provider chunks in the stream.
     */
    includeRawChunks?: boolean;
    /**
     * Internal test hooks.
     */
    _internal?: {
        currentDate?: () => Date;
    };
}): Experimental_StreamTranslationResult;

export declare interface Experimental_StreamTranslationResult {
    /**
     * The final source-language transcript of the input audio.
     */
    readonly sourceText: PromiseLike<string>;
    /**
     * The final translated text in the target language.
     *
     * May resolve to an empty string for providers that produce only audio
     * output.
     */
    readonly translationText: PromiseLike<string>;
    /**
     * The duration of the source audio in seconds, if available.
     */
    readonly durationInSeconds: PromiseLike<number | undefined>;
    /**
     * Usage information for the translation call, if reported by the provider.
     */
    readonly usage: PromiseLike<SpeechTranslationModelV4Usage | undefined>;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    readonly warnings: PromiseLike<Array<Warning>>;
    /**
     * Response metadata.
     */
    readonly response: PromiseLike<SpeechTranslationModelResponseMetadata>;
    /**
     * Additional provider-specific metadata.
     */
    readonly providerMetadata: PromiseLike<Record<string, JSONObject_2>>;
    /**
     * Full stream of translation parts.
     *
     * This is a single-consumer live stream and can only be accessed once.
     * Access it before any result promise when both stream parts and final
     * results are needed; accessing a result promise first consumes the stream
     * internally and makes `fullStream` unavailable.
     */
    readonly fullStream: AsyncIterableStream<Experimental_TranslationStreamPart>;
}

export declare function experimental_toolCaller<TOOL extends Tool>(tool: TOOL, definition: ToolCallerDefinition): Experimental_ToolCallerTool<TOOL>;

export declare type Experimental_ToolCallers<TOOLS extends ToolSet> = {
    [NAME in keyof TOOLS]?: ReadonlyArray<'AI_SDK_DIRECT_TOOL_CALL' | ToolCallerName<TOOLS>>;
};

export declare type Experimental_ToolCallerTool<TOOL extends Tool = Tool> = TOOL & {
    readonly [toolCallerSymbol]: ToolCallerDefinition;
};

/**
 * @deprecated Use `transcribe` instead.
 */
export declare const experimental_transcribe: typeof transcribe;

/**
 * @deprecated Use `TranscriptionResult` instead.
 */
export declare type Experimental_TranscriptionResult = TranscriptionResult;

/**
 * Stream parts emitted by `experimental_streamTranslate`.
 *
 * Speech translation model stream parts are passed through unchanged, except
 * for `stream-start`, `response-metadata`, and `finish`, which are consumed
 * internally and surfaced via the result promises.
 */
export declare type Experimental_TranslationStreamPart = {
    /**
     * Translated audio chunk in the target language.
     *
     * `Uint8Array` chunks contain raw audio bytes. `string` chunks contain
     * base64-encoded raw audio bytes.
     */
    type: 'audio';
    id?: string;
    audio: Uint8Array | string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Append-only translated text delta.
     */
    type: 'output-text-delta';
    id?: string;
    delta: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Final translated text for a provider-defined segment or utterance.
     */
    type: 'output-text-final';
    id?: string;
    text: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Append-only source transcript delta.
     */
    type: 'source-transcript-delta';
    id?: string;
    delta: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Non-final source transcript text. The text may be revised by later parts.
     */
    type: 'source-transcript-partial';
    id?: string;
    text: string;
    startSecond?: number;
    endSecond?: number;
    channelIndex?: number;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Final source transcript text for a provider-defined segment or utterance.
     */
    type: 'source-transcript-final';
    id?: string;
    text: string;
    startSecond?: number;
    endSecond?: number;
    channelIndex?: number;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Raw provider chunks if enabled via `includeRawChunks`.
     */
    type: 'raw';
    rawValue: unknown;
} | {
    /**
     * Error parts are streamed, allowing for multiple errors.
     */
    type: 'error';
    error: unknown;
};

/**
 * Middleware that extracts JSON from text content by stripping
 * markdown code fences and other formatting.
 *
 * This is useful when using Output.object() with models that wrap
 * JSON responses in markdown code blocks.
 *
 * @param options - Configuration options for the middleware.
 * @param options.transform - Custom transform function. If provided, this will be
 * used instead of the default markdown fence stripping.
 */
export declare function extractJsonMiddleware(options?: {
    /**
     * Custom transform function to apply to text content.
     * Receives the raw text and should return the transformed text.
     * If not provided, the default transform strips markdown code fences.
     */
    transform?: (text: string) => string;
}): LanguageModelMiddleware;

/**
 * Extracts a 1-based inclusive line range from `text`, auto-detecting the
 * file's line ending (`\r\n`, `\n`, or `\r`, in that priority).
 *
 * Mixed line endings are not supported: detection picks one and uses it for
 * both the split and the rejoin, so files that mix conventions will not slice
 * cleanly. When neither `startLine` nor `endLine` is provided, the input is
 * returned unchanged. `endLine` past EOF clamps to the last line.
 */
declare function extractLines({ text, startLine, endLine, }: {
    text: string;
    startLine?: number;
    endLine?: number;
}): string;

/**
 * Extracts a 1-based inclusive line range from `text`, auto-detecting the
 * file's line ending (`\r\n`, `\n`, or `\r`, in that priority).
 *
 * Mixed line endings are not supported: detection picks one and uses it for
 * both the split and the rejoin, so files that mix conventions will not slice
 * cleanly. When neither `startLine` nor `endLine` is provided, the input is
 * returned unchanged. `endLine` past EOF clamps to the last line.
 */
declare function extractLines_2({ text, startLine, endLine, }: {
    text: string;
    startLine?: number;
    endLine?: number;
}): string;

/**
 * If `text` is exactly the wide `string` type, there are no string literals to
 * preserve, so this resolves to `never`.
 *
 * If `text` is a string literal or a union of string literals, this resolves
 * to that literal union unchanged.
 *
 * This is used when building template-literal model identifiers (for example
 * `"provider:modelId"`) so that editors can suggest concrete `modelId` values
 * when the underlying method parameter is narrowed, while falling back to a
 * generic `"provider:${string}"` style overload when the parameter is only
 * typed as `string`.
 */
declare type ExtractLiteralUnion<text> = text extends string ? string extends text ? never : text : never;

declare type ExtractModelId<MODELS extends Record<string, unknown>> = Extract<keyof MODELS, string>;

/**
 * Extracts an XML-tagged reasoning section from the generated text and exposes it
 * as a `reasoning` property on the result.
 *
 * @param tagName - The name of the XML tag to extract reasoning from.
 * @param separator - The separator to use between reasoning and text sections.
 * @param startWithReasoning - Whether to start with reasoning tokens.
 */
export declare function extractReasoningMiddleware({ tagName, separator, startWithReasoning, }: {
    tagName: string;
    separator?: string;
    startWithReasoning?: boolean;
}): LanguageModelMiddleware;

/**
 * Extracts the headers from a response object and returns them as a key-value object.
 *
 * @param response - The response object to extract headers from.
 * @returns The headers as a key-value object.
 */
declare function extractResponseHeaders(response: Response): {
    [k: string]: string;
};

/**
 * Extracts the headers from a response object and returns them as a key-value object.
 *
 * @param response - The response object to extract headers from.
 * @returns The headers as a key-value object.
 */
declare function extractResponseHeaders_2(response: Response): {
    [k: string]: string;
};

/**
 * Fetch function type (standardizes the version of fetch used).
 */
declare type FetchFunction = typeof globalThis.fetch;

/**
 * Fetch function type (standardizes the version of fetch used).
 */
declare type FetchFunction_2 = typeof globalThis.fetch;

/**
 * Fetches a URL while enforcing the download guard on every hop.
 *
 * Redirects are followed manually (`redirect: 'manual'`) so each hop is
 * validated with {@link validateDownloadUrl} *before* it is requested. Relying
 * on the default `redirect: 'follow'` would issue the request to a redirect
 * target (e.g. an internal address) before we ever see its URL, defeating the
 * guard.
 *
 * Request headers are also protected: {@link sanitizeRequestHeaders} strips
 * proxy/metadata/cookie/hop-by-hop headers before the first request, and all
 * caller headers except `User-Agent` are dropped on a cross-origin redirect.
 * The fetch spec only strips `Authorization` on cross-origin redirects because
 * in a browser, CORS preflighting protects custom headers; there is no CORS on
 * the server, so provider API keys carried in custom headers (e.g. `x-key`)
 * must be dropped here as well.
 *
 * A `redirect: 'manual'` request yields an unreadable opaque response in the
 * browser (and in other spec-compliant fetch implementations), so the redirect
 * target cannot be validated here. In a real browser this is safe to follow
 * natively because reaching an internal network is not possible (fetch is
 * constrained by CORS and cannot reach a server's internal network or
 * cloud-metadata). On any other runtime we cannot validate the hop, so we fail
 * closed rather than follow it blindly and bypass the guard.
 *
 * A hop that is same-origin with `trustedOrigin` (the developer-configured
 * provider endpoint) skips target validation: that origin is exactly what an
 * unvalidated, config-derived request would fetch anyway, and validating it
 * would break legitimate self-hosted / localhost deployments whose response
 * URLs point back at the configured host. Hops on any other origin are always
 * validated.
 *
 * The returned response is the final (non-redirect) response. The caller is
 * responsible for checking `response.ok` and reading the body.
 *
 * Not solved here: this does string/literal checks only and does not resolve
 * DNS, so a hostname that *resolves* to a private address, and DNS rebinding
 * (the resolved IP flipping between validation and connect), are not blocked.
 * Server deployments fetching untrusted URLs should constrain egress at the
 * network layer or inject a Node `fetch` that pins the resolved IP at connect
 * time — those need DNS/socket APIs not available on all target runtimes
 * (edge, browser, Bun), so they are intentionally not built in.
 *
 * @throws DownloadError if a hop is unsafe, the redirect limit is exceeded, or
 * a redirect cannot be validated on a non-browser runtime.
 */
declare function fetchWithValidatedRedirects({ url, headers, abortSignal, maxRedirects, fetch, trustedOrigin, }: {
    url: string;
    headers?: HeadersInit;
    abortSignal?: AbortSignal;
    maxRedirects?: number;
    fetch?: FetchFunction;
    /**
     * A developer-configured origin (e.g. the provider's `baseURL`) whose hops
     * skip target validation. Must never be derived from response data.
     */
    trustedOrigin?: string;
}): Promise<Response>;

/**
 * Fetches a URL while enforcing the download guard on every hop.
 *
 * Redirects are followed manually (`redirect: 'manual'`) so each hop is
 * validated with {@link validateDownloadUrl} *before* it is requested. Relying
 * on the default `redirect: 'follow'` would issue the request to a redirect
 * target (e.g. an internal address) before we ever see its URL, defeating the
 * guard.
 *
 * Request headers are also protected: {@link sanitizeRequestHeaders} strips
 * proxy/metadata/cookie/hop-by-hop headers before the first request, and all
 * caller headers except `User-Agent` are dropped on a cross-origin redirect.
 * The fetch spec only strips `Authorization` on cross-origin redirects because
 * in a browser, CORS preflighting protects custom headers; there is no CORS on
 * the server, so provider API keys carried in custom headers (e.g. `x-key`)
 * must be dropped here as well.
 *
 * A `redirect: 'manual'` request yields an unreadable opaque response in the
 * browser (and in other spec-compliant fetch implementations), so the redirect
 * target cannot be validated here. In a real browser this is safe to follow
 * natively because reaching an internal network is not possible (fetch is
 * constrained by CORS and cannot reach a server's internal network or
 * cloud-metadata). On any other runtime we cannot validate the hop, so we fail
 * closed rather than follow it blindly and bypass the guard.
 *
 * A hop that is same-origin with `trustedOrigin` (the developer-configured
 * provider endpoint) skips target validation: that origin is exactly what an
 * unvalidated, config-derived request would fetch anyway, and validating it
 * would break legitimate self-hosted / localhost deployments whose response
 * URLs point back at the configured host. Hops on any other origin are always
 * validated.
 *
 * The returned response is the final (non-redirect) response. The caller is
 * responsible for checking `response.ok` and reading the body.
 *
 * On Node.js, the default fetch resolves every hostname through a validating
 * lookup hook and passes those exact addresses to the connector, preventing
 * hostname-to-private-IP and DNS-rebinding bypasses. An injected fetch is
 * responsible for equivalent connect-time validation. Other runtimes should
 * constrain egress at the network layer when handling untrusted URLs.
 *
 * @throws DownloadError if a hop is unsafe, the redirect limit is exceeded, or
 * a redirect cannot be validated on a non-browser runtime.
 */
declare function fetchWithValidatedRedirects_2({ url, headers, abortSignal, maxRedirects, fetch: customFetch, trustedOrigin, }: {
    url: string;
    headers?: HeadersInit;
    abortSignal?: AbortSignal;
    maxRedirects?: number;
    fetch?: FetchFunction_2;
    /**
     * A developer-configured origin (e.g. the provider's `baseURL`) whose hops
     * skip target validation. Must never be derived from response data.
     */
    trustedOrigin?: string;
}): Promise<Response>;

/**
 * File data as a tagged discriminated union:
 *
 * - `{ type: 'data', data }`: raw bytes (`Uint8Array`, `ArrayBuffer`, or
 *   `Buffer`) or a base64-encoded string.
 * - `{ type: 'url', url }`: a URL that points to the file.
 * - `{ type: 'reference', reference }`: a provider reference (`{ [provider]: id }`).
 * - `{ type: 'text', text }`: inline text content (e.g. an inline text document).
 */
declare type FileData = FileDataData | FileDataUrl | FileDataReference | FileDataText;

/**
 * File data as a tagged discriminated union:
 *
 * - `{ type: 'data', data }`: raw bytes (`Uint8Array`, `ArrayBuffer`, or
 *   `Buffer`) or a base64-encoded string.
 * - `{ type: 'url', url }`: a URL that points to the file.
 * - `{ type: 'reference', reference }`: a provider reference (`{ [provider]: id }`).
 * - `{ type: 'text', text }`: inline text content (e.g. an inline text document).
 */
declare type FileData_2 = FileDataData_2 | FileDataUrl_2 | FileDataReference_2 | FileDataText_2;

/**
 * File data variant containing raw bytes (`Uint8Array`, `ArrayBuffer`, or
 * `Buffer`) or a base64-encoded string.
 *
 * This is slightly more permissive than `SharedV4FileDataData`.
 */
declare interface FileDataData {
    type: 'data';
    data: DataContent_2;
}

/**
 * File data variant containing raw bytes (`Uint8Array`, `ArrayBuffer`, or
 * `Buffer`) or a base64-encoded string.
 *
 * This is slightly more permissive than `SharedV4FileDataData`.
 */
declare interface FileDataData_2 {
    type: 'data';
    data: DataContent;
}

/**
 * File data variant containing a provider reference (`{ [provider]: id }`).
 */
declare type FileDataReference = SharedV4FileDataReference;

/**
 * File data variant containing a provider reference (`{ [provider]: id }`).
 */
declare type FileDataReference_2 = SharedV4FileDataReference;

/**
 * File data variant containing inline text content (e.g. an inline text
 * document).
 */
declare type FileDataText = SharedV4FileDataText;

/**
 * File data variant containing inline text content (e.g. an inline text
 * document).
 */
declare type FileDataText_2 = SharedV4FileDataText;

/**
 * File data variant containing a URL that points to the file.
 */
declare type FileDataUrl = SharedV4FileDataUrl;

/**
 * File data variant containing a URL that points to the file.
 */
declare type FileDataUrl_2 = SharedV4FileDataUrl;

/**
 * File content part of a prompt. It contains a file.
 */
export declare interface FilePart {
    type: 'file';
    /**
     * File data. Either a tagged shape or a bare shorthand:
     *
     * - `{ type: 'data', data }` or bare `DataContent`: raw bytes
     *   (base64 string, Uint8Array, ArrayBuffer, Buffer)
     * - `{ type: 'url', url }` or bare `URL`: a URL that points to the file
     * - `{ type: 'reference', reference }` or bare `ProviderReference`:
     *   a provider reference from `uploadFile`
     * - `{ type: 'text', text }`: inline text content (tagged only)
     */
    data: FileData_2 | DataContent | URL | ProviderReference_3;
    /**
     * Optional filename of the file.
     */
    filename?: string;
    /**
     * Either a full IANA media type (`type/subtype`, e.g. `image/png`) or just
     * the top-level IANA segment (e.g. `image`, `audio`, `video`, `text`).
     *
     * `*`-subtype wildcards (e.g. `image/*`) are normalized as equivalent to the
     * top-level segment alone (e.g. `image`). Providers can use the helpers in
     * `@ai-sdk/provider-utils` (`isFullMediaType`, `getTopLevelMediaType`,
     * `detectMediaType`) to resolve the field according to their API
     * requirements.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
}

/**
 * File content part of a prompt. It contains a file.
 */
declare interface FilePart_2 {
    type: 'file';
    /**
     * File data. Either a tagged shape or a bare shorthand:
     *
     * - `{ type: 'data', data }` or bare `DataContent`: raw bytes
     *   (base64 string, Uint8Array, ArrayBuffer, Buffer)
     * - `{ type: 'url', url }` or bare `URL`: a URL that points to the file
     * - `{ type: 'reference', reference }` or bare `ProviderReference`:
     *   a provider reference from `uploadFile`
     * - `{ type: 'text', text }`: inline text content (tagged only)
     */
    data: FileData | DataContent_2 | URL | ProviderReference_2;
    /**
     * Optional filename of the file.
     */
    filename?: string;
    /**
     * Either a full IANA media type (`type/subtype`, e.g. `image/png`) or just
     * the top-level IANA segment (e.g. `image`, `audio`, `video`, `text`).
     *
     * `*`-subtype wildcards (e.g. `image/*`) are normalized as equivalent to the
     * top-level segment alone (e.g. `image`). Providers can use the helpers in
     * `@ai-sdk/provider-utils` (`isFullMediaType`, `getTopLevelMediaType`,
     * `detectMediaType`) to resolve the field according to their API
     * requirements.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
}

/**
 * Specification for a file management interface that implements the files interface version 4.
 */
declare type FilesV4 = {
    /**
     * The files interface must specify which files interface version it implements.
     */
    readonly specificationVersion: 'v4';
    /**
     * Provider ID.
     */
    readonly provider: string;
    /**
     * Uploads a file to the provider and returns a provider reference
     * that can be used in subsequent API calls.
     */
    uploadFile(options: FilesV4UploadFileCallOptions): PromiseLike<FilesV4UploadFileResult>;
};

/**
 * Specification for a file management interface that implements the files interface version 4.
 */
declare type FilesV4_2 = {
    /**
     * The files interface must specify which files interface version it implements.
     */
    readonly specificationVersion: 'v4';
    /**
     * Provider ID.
     */
    readonly provider: string;
    /**
     * Uploads a file to the provider and returns a provider reference
     * that can be used in subsequent API calls.
     */
    uploadFile(options: FilesV4UploadFileCallOptions_2): PromiseLike<FilesV4UploadFileResult_2>;
};

/**
 * Options for uploading a file via the files interface.
 */
declare type FilesV4UploadFileCallOptions = {
    /**
     * The file data.
     *
     * - `{ type: 'data', data }`: raw bytes (`Uint8Array`) or a base64-encoded string.
     * - `{ type: 'text', text }`: inline text (UTF-8).
     */
    data: SharedV4FileDataData | SharedV4FileDataText;
    /**
     * The IANA media type of the file (e.g. `'application/pdf'`).
     */
    mediaType: string;
    /**
     * The filename of the file.
     */
    filename?: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
};

/**
 * Options for uploading a file via the files interface.
 */
declare type FilesV4UploadFileCallOptions_2 = {
    /**
     * The file data.
     *
     * - `{ type: 'data', data }`: raw bytes (`Uint8Array`) or a base64-encoded string.
     * - `{ type: 'text', text }`: inline text (UTF-8).
     */
    data: SharedV4FileDataData_2 | SharedV4FileDataText_2;
    /**
     * The IANA media type of the file (e.g. `'application/pdf'`).
     */
    mediaType: string;
    /**
     * The filename of the file.
     */
    filename?: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
};

/**
 * Result of uploading a file via the files interface.
 */
declare type FilesV4UploadFileResult = {
    /**
     * A provider reference mapping provider names to provider-specific file identifiers.
     */
    providerReference: SharedV4ProviderReference_2;
    /**
     * The IANA media type of the uploaded file, if available from the provider.
     */
    mediaType?: string;
    /**
     * The filename of the uploaded file, if available from the provider.
     */
    filename?: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV4ProviderMetadata;
    /**
     * Warnings from the provider.
     */
    warnings: Array<SharedV4Warning>;
};

/**
 * Result of uploading a file via the files interface.
 */
declare type FilesV4UploadFileResult_2 = {
    /**
     * A provider reference mapping provider names to provider-specific file identifiers.
     */
    providerReference: SharedV4ProviderReference;
    /**
     * The IANA media type of the uploaded file, if available from the provider.
     */
    mediaType?: string;
    /**
     * The filename of the uploaded file, if available from the provider.
     */
    filename?: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
    /**
     * Warnings from the provider.
     */
    warnings: Array<SharedV4Warning_2>;
};

/**
 * A file part of a message.
 */
export declare type FileUIPart = {
    type: 'file';
    /**
     * Either a full IANA media type (`type/subtype`, e.g. `image/png`) or just
     * the top-level IANA segment (e.g. `image`, `audio`, `video`, `text`).
     *
     * `*`-subtype wildcards (e.g. `image/*`) are normalized as equivalent to the
     * top-level segment alone (e.g. `image`). Providers can use the helpers in
     * `@ai-sdk/provider-utils` (`isFullMediaType`, `getTopLevelMediaType`,
     * `detectMediaType`) to resolve the field according to their API
     * requirements.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Optional filename of the file.
     */
    filename?: string;
    /**
     * The URL of the file.
     * It can either be a URL to a hosted file or a [Data URL](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/Data_URLs).
     */
    url: string;
    /**
     * Provider reference for files uploaded via `uploadFile`.
     * Maps provider names to provider-specific file identifiers.
     * When present, takes precedence over `url` in model messages.
     */
    providerReference?: ProviderReference;
    /**
     * The provider metadata.
     */
    providerMetadata?: ProviderMetadata;
};

/**
 * Filters `null` and `undefined` values out of a list of values.
 *
 * @param values - The values to filter.
 * @returns A new array containing only non-nullish values.
 */
declare function filterNullable<T>(...values: Array<T | undefined | null>): Array<T>;

/**
 * Filters `null` and `undefined` values out of a list of values.
 *
 * @param values - The values to filter.
 * @returns A new array containing only non-nullish values.
 */
declare function filterNullable_2<T>(...values: Array<T | undefined | null>): Array<T>;

/**
 * Fingerprint the server-controlled, security-relevant fields of each tool in a
 * `ToolSet`: `description` (string form only), the resolved input JSON schema,
 * and `title`. Returns a map of tool name to a stable digest.
 *
 * Capture a baseline at trust time (first connect, human-reviewed) and compare
 * later fetches with {@link detectToolDrift} to catch MCP tool-definition drift
 * ("rug pull"). Baseline storage and the drift response are the app's concern.
 */
export declare function fingerprintTools(tools: ToolSet): Promise<Record<string, string>>;

/**
 * Reason why a language model finished generating a response.
 *
 * Can be one of the following:
 * - `stop`: model generated stop sequence
 * - `length`: model generated maximum number of tokens
 * - `content-filter`: content filter violation stopped the model
 * - `tool-calls`: model triggered tool calls
 * - `error`: model stopped because of an error
 * - `other`: model stopped for other reasons
 */
export declare type FinishReason = 'stop' | 'length' | 'content-filter' | 'tool-calls' | 'error' | 'other';

export declare type FlexibleSchema<SCHEMA = any> = Schema<SCHEMA> | LazySchema_2<SCHEMA> | ZodSchema_2<SCHEMA> | StandardSchema_2<SCHEMA>;

declare type FlexibleSchema_2<SCHEMA = any> = Schema_2<SCHEMA> | LazySchema<SCHEMA> | ZodSchema<SCHEMA> | StandardSchema<SCHEMA>;

/**
 * Tool with user-defined input and output schemas that is executed by the AI SDK.
 */
declare type FunctionTool<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context | unknown | never = any> = BaseFunctionTool<INPUT, OUTPUT, CONTEXT> & {
    type?: undefined | 'function';
};

/**
 * Tool with user-defined input and output schemas that is executed by the AI SDK.
 */
declare type FunctionTool_2<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context_2 | unknown | never = any> = BaseFunctionTool_2<INPUT, OUTPUT, CONTEXT> & {
    type?: undefined | 'function';
};

export declare const gateway: GatewayProvider;

declare interface GatewayCreditsResponse {
    /** The remaining gateway credit balance available for API usage */
    balance: string;
    /** The total amount of gateway credits that have been consumed */
    totalUsed: string;
}

declare type GatewayEmbeddingModelId = 'alibaba/qwen3-embedding-0.6b' | 'alibaba/qwen3-embedding-4b' | 'alibaba/qwen3-embedding-8b' | 'amazon/titan-embed-text-v2' | 'cohere/embed-v4.0' | 'google/gemini-embedding-001' | 'google/gemini-embedding-2' | 'google/text-embedding-005' | 'google/text-multilingual-embedding-002' | 'mistral/codestral-embed' | 'mistral/mistral-embed' | 'openai/text-embedding-3-large' | 'openai/text-embedding-3-small' | 'openai/text-embedding-ada-002' | 'voyage/voyage-3-large' | 'voyage/voyage-3.5' | 'voyage/voyage-3.5-lite' | 'voyage/voyage-4' | 'voyage/voyage-4-large' | 'voyage/voyage-4-lite' | 'voyage/voyage-code-2' | 'voyage/voyage-code-3' | 'voyage/voyage-finance-2' | 'voyage/voyage-law-2' | (string & {});

declare interface GatewayFetchMetadataResponse {
    models: GatewayLanguageModelEntry[];
}

declare interface GatewayGenerationInfo {
    /** The generation ID */
    id: string;
    /** Total cost in USD */
    totalCost: number;
    /** Upstream inference cost in USD (BYOK only) */
    upstreamInferenceCost: number;
    /** Usage cost in USD (same as totalCost) */
    usage: number;
    /** ISO 8601 timestamp when the generation was created */
    createdAt: string;
    /** Model identifier */
    model: string;
    /** Whether BYOK credentials were used */
    isByok: boolean;
    /** Provider that served this generation */
    providerName: string;
    /** Whether streaming was used */
    streamed: boolean;
    /** Finish reason (e.g. 'stop') */
    finishReason: string;
    /** Time to first token in milliseconds */
    latency: number;
    /** Total generation time in milliseconds */
    generationTime: number;
    /** Number of prompt tokens */
    promptTokens: number;
    /** Number of completion tokens */
    completionTokens: number;
    /** Reasoning tokens used */
    reasoningTokens: number;
    /** Cached tokens used */
    cachedTokens: number;
    /** Cache creation input tokens */
    cacheCreationTokens: number;
    /** Billable web search calls */
    billableWebSearchCalls: number;
}

declare interface GatewayGenerationInfoParams {
    /** The generation ID to look up (format: gen_<ulid>) */
    id: string;
}

declare type GatewayImageModelId = 'bfl/flux-2-flex' | 'bfl/flux-2-klein-4b' | 'bfl/flux-2-klein-9b' | 'bfl/flux-2-max' | 'bfl/flux-2-pro' | 'bfl/flux-kontext-max' | 'bfl/flux-kontext-pro' | 'bfl/flux-pro-1.0-fill' | 'bfl/flux-pro-1.1' | 'bfl/flux-pro-1.1-ultra' | 'bytedance/seedream-4.0' | 'bytedance/seedream-4.5' | 'bytedance/seedream-5.0-lite' | 'bytedance/seedream-5.0-pro' | 'google/imagen-4.0-fast-generate-001' | 'google/imagen-4.0-generate-001' | 'google/imagen-4.0-ultra-generate-001' | 'openai/gpt-image-1' | 'openai/gpt-image-1-mini' | 'openai/gpt-image-1.5' | 'openai/gpt-image-2' | 'prodia/flux-fast-schnell' | 'quiverai/arrow-1.1' | 'recraft/recraft-v2' | 'recraft/recraft-v3' | 'recraft/recraft-v4' | 'recraft/recraft-v4-pro' | 'recraft/recraft-v4.1' | 'recraft/recraft-v4.1-pro' | 'recraft/recraft-v4.1-utility' | 'recraft/recraft-v4.1-utility-pro' | 'xai/grok-imagine-image' | (string & {});

declare interface GatewayLanguageModelEntry {
    /**
     * The model id used by the remote provider in model settings and for specifying the
     * intended model for text generation.
     */
    id: string;
    /**
     * The display name of the model for presentation in user-facing contexts.
     */
    name: string;
    /**
     * Optional description of the model.
     */
    description?: string | null;
    /**
     * Optional pricing information for the model.
     */
    pricing?: {
        /**
         * Cost per input token in USD.
         */
        input: string;
        /**
         * Cost per output token in USD.
         */
        output: string;
        /**
         * Cost per cached input token in USD.
         * Only present for providers/models that support prompt caching.
         */
        cachedInputTokens?: string;
        /**
         * Cost per input token to create/write cache entries in USD.
         * Only present for providers/models that support prompt caching.
         */
        cacheCreationInputTokens?: string;
    } | null;
    /**
     * Additional AI SDK language model specifications for the model.
     */
    specification: GatewayLanguageModelSpecification;
    /**
     * Optional field to differentiate between model types.
     */
    modelType?: KnownModelType | null;
}

declare type GatewayLanguageModelSpecification = Pick<LanguageModelV4, 'specificationVersion' | 'provider' | 'modelId'>;

export declare type GatewayModelId = 'alibaba/qwen-3-14b' | 'alibaba/qwen-3-235b' | 'alibaba/qwen-3-30b' | 'alibaba/qwen-3-32b' | 'alibaba/qwen-3.6-max-preview' | 'alibaba/qwen3-235b-a22b-thinking' | 'alibaba/qwen3-coder' | 'alibaba/qwen3-coder-30b-a3b' | 'alibaba/qwen3-coder-next' | 'alibaba/qwen3-coder-plus' | 'alibaba/qwen3-max' | 'alibaba/qwen3-max-preview' | 'alibaba/qwen3-max-thinking' | 'alibaba/qwen3-next-80b-a3b-instruct' | 'alibaba/qwen3-next-80b-a3b-thinking' | 'alibaba/qwen3-vl-235b-a22b-instruct' | 'alibaba/qwen3-vl-instruct' | 'alibaba/qwen3-vl-thinking' | 'alibaba/qwen3.5-flash' | 'alibaba/qwen3.5-plus' | 'alibaba/qwen3.6-27b' | 'alibaba/qwen3.6-plus' | 'alibaba/qwen3.7-max' | 'alibaba/qwen3.7-plus' | 'amazon/nova-2-lite' | 'amazon/nova-lite' | 'amazon/nova-micro' | 'amazon/nova-pro' | 'anthropic/claude-3-haiku' | 'anthropic/claude-fable-5' | 'anthropic/claude-haiku-4.5' | 'anthropic/claude-opus-4' | 'anthropic/claude-opus-4.1' | 'anthropic/claude-opus-4.5' | 'anthropic/claude-opus-4.6' | 'anthropic/claude-opus-4.7' | 'anthropic/claude-opus-4.8' | 'anthropic/claude-opus-4.8-fast' | 'anthropic/claude-opus-5' | 'anthropic/claude-opus-5-fast' | 'anthropic/claude-sonnet-4' | 'anthropic/claude-sonnet-4.5' | 'anthropic/claude-sonnet-4.6' | 'anthropic/claude-sonnet-5' | 'arcee-ai/trinity-large-thinking' | 'arcee-ai/trinity-mini' | 'bytedance/seed-1.6' | 'bytedance/seed-1.8' | 'cohere/command-a' | 'deepseek/deepseek-r1' | 'deepseek/deepseek-v3' | 'deepseek/deepseek-v3.1' | 'deepseek/deepseek-v3.1-terminus' | 'deepseek/deepseek-v3.2' | 'deepseek/deepseek-v3.2-thinking' | 'deepseek/deepseek-v4-flash' | 'deepseek/deepseek-v4-pro' | 'google/gemini-2.5-flash' | 'google/gemini-2.5-flash-image' | 'google/gemini-2.5-flash-lite' | 'google/gemini-2.5-pro' | 'google/gemini-3-flash' | 'google/gemini-3-pro-image' | 'google/gemini-3.1-flash-image' | 'google/gemini-3.1-flash-image-preview' | 'google/gemini-3.1-flash-lite' | 'google/gemini-3.1-flash-lite-image' | 'google/gemini-3.1-pro-preview' | 'google/gemini-3.5-flash' | 'google/gemini-3.5-flash-lite' | 'google/gemini-3.6-flash' | 'google/gemini-omni-flash-preview' | 'google/gemma-4-26b-a4b-it' | 'google/gemma-4-31b-it' | 'inception/mercury-2' | 'inception/mercury-coder-small' | 'inclusionai/ling-3.0-flash-free' | 'interfaze/interfaze-beta' | 'kwaipilot/kat-coder-air-v2.5' | 'kwaipilot/kat-coder-pro-v1' | 'kwaipilot/kat-coder-pro-v2' | 'kwaipilot/kat-coder-pro-v2.5' | 'meta/llama-3.1-70b' | 'meta/llama-3.1-8b' | 'meta/llama-3.3-70b' | 'meta/llama-4-maverick' | 'meta/llama-4-scout' | 'meta/muse-spark-1.1' | 'minimax/minimax-m2' | 'minimax/minimax-m2.1' | 'minimax/minimax-m2.1-lightning' | 'minimax/minimax-m2.5' | 'minimax/minimax-m2.5-highspeed' | 'minimax/minimax-m2.7' | 'minimax/minimax-m2.7-highspeed' | 'minimax/minimax-m3' | 'mistral/codestral' | 'mistral/devstral-2' | 'mistral/devstral-small-2' | 'mistral/magistral-medium' | 'mistral/magistral-small' | 'mistral/ministral-14b' | 'mistral/ministral-3b' | 'mistral/ministral-8b' | 'mistral/mistral-large-3' | 'mistral/mistral-medium' | 'mistral/mistral-medium-3.5' | 'mistral/mistral-nemo' | 'mistral/mistral-small' | 'mistral/pixtral-12b' | 'moonshotai/kimi-k2' | 'moonshotai/kimi-k2-thinking' | 'moonshotai/kimi-k2.5' | 'moonshotai/kimi-k2.6' | 'moonshotai/kimi-k2.7-code' | 'moonshotai/kimi-k2.7-code-highspeed' | 'moonshotai/kimi-k3' | 'moonshotai/kimi-k3-fast' | 'morph/morph-v3-fast' | 'morph/morph-v3-large' | 'nvidia/nemotron-3-nano-30b-a3b' | 'nvidia/nemotron-3-super-120b-a12b' | 'nvidia/nemotron-3-ultra-550b-a55b' | 'nvidia/nemotron-nano-12b-v2-vl' | 'nvidia/nemotron-nano-9b-v2' | 'openai/gpt-3.5-turbo' | 'openai/gpt-4-turbo' | 'openai/gpt-4.1' | 'openai/gpt-4.1-mini' | 'openai/gpt-4.1-nano' | 'openai/gpt-4o' | 'openai/gpt-4o-mini' | 'openai/gpt-4o-mini-search-preview' | 'openai/gpt-5' | 'openai/gpt-5-codex' | 'openai/gpt-5-mini' | 'openai/gpt-5-nano' | 'openai/gpt-5-pro' | 'openai/gpt-5.1-codex' | 'openai/gpt-5.1-codex-max' | 'openai/gpt-5.1-codex-mini' | 'openai/gpt-5.1-instant' | 'openai/gpt-5.1-thinking' | 'openai/gpt-5.2' | 'openai/gpt-5.2-codex' | 'openai/gpt-5.2-pro' | 'openai/gpt-5.3-chat' | 'openai/gpt-5.3-codex' | 'openai/gpt-5.4' | 'openai/gpt-5.4-mini' | 'openai/gpt-5.4-nano' | 'openai/gpt-5.4-pro' | 'openai/gpt-5.5' | 'openai/gpt-5.5-pro' | 'openai/gpt-5.6-luna' | 'openai/gpt-5.6-sol' | 'openai/gpt-5.6-terra' | 'openai/gpt-oss-120b' | 'openai/gpt-oss-20b' | 'openai/gpt-oss-safeguard-20b' | 'openai/o1' | 'openai/o3' | 'openai/o3-deep-research' | 'openai/o3-mini' | 'openai/o3-pro' | 'openai/o4-mini' | 'perplexity/sonar' | 'perplexity/sonar-pro' | 'perplexity/sonar-reasoning-pro' | 'poolside/laguna-s-2.1' | 'poolside/laguna-s-2.1-free' | 'sakana/fugu-ultra' | 'stepfun/step-3.5-flash' | 'stepfun/step-3.7-flash' | 'tencent/hy3' | 'thinkingmachines/inkling' | 'xai/grok-4.1-fast-non-reasoning' | 'xai/grok-4.1-fast-reasoning' | 'xai/grok-4.20-multi-agent' | 'xai/grok-4.20-multi-agent-beta' | 'xai/grok-4.20-non-reasoning' | 'xai/grok-4.20-non-reasoning-beta' | 'xai/grok-4.20-reasoning' | 'xai/grok-4.20-reasoning-beta' | 'xai/grok-4.3' | 'xai/grok-4.5' | 'xai/grok-build-0.1' | 'xiaomi/mimo-v2.5' | 'xiaomi/mimo-v2.5-pro' | 'zai/glm-4.5' | 'zai/glm-4.5-air' | 'zai/glm-4.5v' | 'zai/glm-4.6' | 'zai/glm-4.6v' | 'zai/glm-4.6v-flash' | 'zai/glm-4.7' | 'zai/glm-4.7-flash' | 'zai/glm-4.7-flashx' | 'zai/glm-5' | 'zai/glm-5-turbo' | 'zai/glm-5.1' | 'zai/glm-5.2' | 'zai/glm-5.2-fast' | 'zai/glm-5v-turbo' | (string & {});

declare interface GatewayProvider extends ProviderV4 {
    (modelId: GatewayModelId): LanguageModelV4;
    /**
     * Creates a model for text generation.
     */
    chat(modelId: GatewayModelId): LanguageModelV4;
    /**
     * Creates a model for text generation.
     */
    languageModel(modelId: GatewayModelId): LanguageModelV4;
    /**
     * Returns available providers and models for use with the remote provider.
     */
    getAvailableModels(): Promise<GatewayFetchMetadataResponse>;
    /**
     * Returns credit information for the authenticated user.
     */
    getCredits(): Promise<GatewayCreditsResponse>;
    /**
     * Returns a spend report with cost, token, and request count data,
     * aggregated by the specified dimension.
     */
    getSpendReport(params: GatewaySpendReportParams): Promise<GatewaySpendReportResponse>;
    /**
     * Returns detailed information about a specific generation by its ID,
     * including cost, token usage, latency, and provider details.
     */
    getGenerationInfo(params: GatewayGenerationInfoParams): Promise<GatewayGenerationInfo>;
    /**
     * Creates a model for generating text embeddings.
     */
    embedding(modelId: GatewayEmbeddingModelId): EmbeddingModelV4;
    /**
     * Creates a model for generating text embeddings.
     */
    embeddingModel(modelId: GatewayEmbeddingModelId): EmbeddingModelV4;
    /**
     * @deprecated Use `embeddingModel` instead.
     */
    textEmbeddingModel(modelId: GatewayEmbeddingModelId): EmbeddingModelV4;
    /**
     * Creates a model for generating images.
     */
    image(modelId: GatewayImageModelId): ImageModelV4;
    /**
     * Creates a model for generating images.
     */
    imageModel(modelId: GatewayImageModelId): ImageModelV4;
    /**
     * Creates a model for generating videos.
     */
    video(modelId: GatewayVideoModelId): VideoModelV4;
    /**
     * Creates a model for generating videos.
     */
    videoModel(modelId: GatewayVideoModelId): VideoModelV4;
    /**
     * Creates a model for reranking documents.
     */
    reranking(modelId: GatewayRerankingModelId): RerankingModelV4;
    /**
     * Creates a model for reranking documents.
     */
    rerankingModel(modelId: GatewayRerankingModelId): RerankingModelV4;
    /**
     * Creates a model for text-to-speech generation.
     */
    speech(modelId: GatewaySpeechModelId): SpeechModelV4;
    /**
     * Creates a model for text-to-speech generation.
     */
    speechModel(modelId: GatewaySpeechModelId): SpeechModelV4;
    /**
     * Creates a model for audio transcription.
     */
    transcription(modelId: GatewayTranscriptionModelId): TranscriptionModelV4;
    /**
     * Creates a model for audio transcription.
     */
    transcriptionModel(modelId: GatewayTranscriptionModelId): TranscriptionModelV4;
    /**
     * Creates an experimental realtime model for bidirectional audio/text
     * communication over WebSocket, normalized through the AI Gateway.
     */
    experimental_realtime: RealtimeFactoryV4;
    /**
     * Experimental streaming-transcription entry point. Callable like
     * `transcription(modelId)`, plus `getToken` for minting a short-lived
     * client secret (`vcst_`) a browser can use to open the streaming
     * transcription WebSocket without holding the Gateway credential.
     */
    experimental_transcription: GatewayTranscriptionFactory;
    /**
     * Gateway-specific tools executed server-side.
     */
    tools: typeof gatewayTools;
}

declare interface GatewayProviderSettings {
    /**
     * The base URL prefix for API calls. Defaults to `https://ai-gateway.vercel.sh/v4/ai`.
     */
    baseURL?: string;
    /**
     * API key or Vercel access token that is being sent using the `Authorization`
     * header. It defaults to the `AI_GATEWAY_API_KEY` environment variable.
     */
    apiKey?: string;
    /**
     * Vercel team ID or slug to scope requests for access tokens that can access
     * multiple teams.
     */
    teamIdOrSlug?: string;
    /**
     * Custom headers to include in the requests.
     */
    headers?: Record<string, string>;
    /**
     * Custom fetch implementation. You can use it as a middleware to intercept requests,
     * or to provide a custom fetch implementation for e.g. testing.
     */
    fetch?: FetchFunction;
    /**
     * Custom WebSocket implementation used for streaming transcription. This is
     * useful for testing or for runtimes without a global WebSocket. A
     * header-capable implementation is not required — Gateway WebSocket auth is
     * carried in the subprotocols.
     */
    webSocket?: WebSocketConstructor;
    /**
     * How frequently to refresh the metadata cache in milliseconds.
     */
    metadataCacheRefreshMillis?: number;
}

declare type GatewayRerankingModelId = 'cohere/rerank-v3.5' | 'cohere/rerank-v4-fast' | 'cohere/rerank-v4-pro' | 'voyage/rerank-2.5' | 'voyage/rerank-2.5-lite' | (string & {});

declare type GatewaySpeechModelId = 'openai/tts-1' | 'openai/tts-1-hd' | 'xai/grok-tts' | (string & {});

declare interface GatewaySpendReportParams {
    /** Start date in YYYY-MM-DD format (inclusive) */
    startDate: string;
    /** End date in YYYY-MM-DD format (inclusive) */
    endDate: string;
    /** Primary aggregation dimension. Defaults to 'day'. */
    groupBy?: 'day' | 'user' | 'model' | 'tag' | 'provider' | 'credential_type';
    /** Time granularity when groupBy is 'day'. */
    datePart?: 'day' | 'hour';
    /** Filter to a specific user's spend. */
    userId?: string;
    /** Filter to a specific model (e.g. 'anthropic/claude-sonnet-4.5'). */
    model?: string;
    /** Filter to a specific provider (e.g. 'anthropic'). */
    provider?: string;
    /** Filter to BYOK or system credentials. */
    credentialType?: 'byok' | 'system';
    /** Filter to requests with these tags. */
    tags?: string[];
}

declare interface GatewaySpendReportResponse {
    results: GatewaySpendReportRow[];
}

declare interface GatewaySpendReportRow {
    /** Date string (present when groupBy is 'day') */
    day?: string;
    /** Hour timestamp (present when groupBy is 'day' and datePart is 'hour') */
    hour?: string;
    /** User identifier (present when groupBy is 'user') */
    user?: string;
    /** Model identifier (present when groupBy is 'model') */
    model?: string;
    /** Tag value (present when groupBy is 'tag') */
    tag?: string;
    /** Provider name (present when groupBy is 'provider') */
    provider?: string;
    /** Credential type (present when groupBy is 'credential_type') */
    credentialType?: 'byok' | 'system';
    /** Total cost in USD */
    totalCost: number;
    /** Market cost in USD */
    marketCost?: number;
    /** Number of input tokens */
    inputTokens?: number;
    /** Number of output tokens */
    outputTokens?: number;
    /** Number of cached input tokens */
    cachedInputTokens?: number;
    /** Number of cache creation input tokens */
    cacheCreationInputTokens?: number;
    /** Number of reasoning tokens */
    reasoningTokens?: number;
    /** Number of requests */
    requestCount?: number;
}

/**
 * Gateway-specific provider-defined tools.
 */
declare const gatewayTools: {
    /**
     * Search the web using Exa for current information and token-efficient
     * excerpts optimized for agent workflows.
     *
     * Supports search type, category, domain, date, location, and content
     * extraction controls.
     */
    exaSearch: (config?: ExaSearchConfig) => ReturnType<typeof exaSearchToolFactory>;
    /**
     * Search the web using Parallel AI's Search API for LLM-optimized excerpts.
     *
     * Takes a natural language objective and returns relevant excerpts,
     * replacing multiple keyword searches with a single call for broad
     * or complex queries. Supports different search types for depth vs
     * breadth tradeoffs.
     */
    parallelSearch: (config?: ParallelSearchConfig) => ReturnType<typeof parallelSearchToolFactory>;
    /**
     * Search the web using Perplexity's Search API for real-time information,
     * news, research papers, and articles.
     *
     * Provides ranked search results with advanced filtering options including
     * domain, language, date range, and recency filters.
     */
    perplexitySearch: (config?: PerplexitySearchConfig) => ReturnType<typeof perplexitySearchToolFactory>;
};

/**
 * Streaming-transcription factory: callable like `transcription(modelId)`,
 * plus a server-side `getToken` that mints a transcription-bound short-lived
 * client secret (`vcst_`). The browser connects with
 * `createGateway({ apiKey: token }).transcription(modelId)` — the token rides
 * the same auth subprotocol an API key does, without exposing the credential.
 */
declare interface GatewayTranscriptionFactory {
    (modelId: GatewayTranscriptionModelId): TranscriptionModelV4;
    getToken(options: GatewayTranscriptionFactoryGetTokenOptions): Promise<GatewayTranscriptionFactoryGetTokenResult>;
}

declare type GatewayTranscriptionFactoryGetTokenOptions = {
    model: GatewayTranscriptionModelId;
    /** Token lifetime in seconds. Gateway default is 60s (max 300s). */
    expiresAfterSeconds?: number;
};

declare type GatewayTranscriptionFactoryGetTokenResult = {
    /** The minted `vcst_` client secret. */
    token: string;
    /** WebSocket URL of the streaming transcription surface for this model. */
    url: string;
    /** Token expiry, epoch seconds. */
    expiresAt?: number;
};

declare type GatewayTranscriptionModelId = 'openai/gpt-4o-mini-transcribe' | 'openai/gpt-4o-transcribe' | 'openai/gpt-realtime-whisper' | 'openai/whisper-1' | 'xai/grok-stt' | (string & {});

declare type GatewayVideoModelId = 'alibaba/wan-v2.5-t2v-preview' | 'alibaba/wan-v2.6-i2v' | 'alibaba/wan-v2.6-i2v-flash' | 'alibaba/wan-v2.6-r2v' | 'alibaba/wan-v2.6-r2v-flash' | 'alibaba/wan-v2.6-t2v' | 'alibaba/wan-v2.7-r2v' | 'alibaba/wan-v2.7-t2v' | 'bytedance/seedance-2.0' | 'bytedance/seedance-2.0-fast' | 'bytedance/seedance-v1.0-pro' | 'bytedance/seedance-v1.0-pro-fast' | 'bytedance/seedance-v1.5-pro' | 'google/veo-3.0-fast-generate-001' | 'google/veo-3.0-generate-001' | 'google/veo-3.1-fast-generate-001' | 'google/veo-3.1-generate-001' | 'klingai/kling-v2.5-turbo-i2v' | 'klingai/kling-v2.5-turbo-t2v' | 'klingai/kling-v2.6-i2v' | 'klingai/kling-v2.6-motion-control' | 'klingai/kling-v2.6-t2v' | 'klingai/kling-v3.0-i2v' | 'klingai/kling-v3.0-motion-control' | 'klingai/kling-v3.0-t2v' | 'xai/grok-imagine-video' | 'xai/grok-imagine-video-1.5' | 'xai/grok-imagine-video-1.5-preview' | (string & {});

/**
 * A generated audio file.
 */
export declare interface GeneratedAudioFile extends GeneratedFile {
    /**
     * Audio format of the file (e.g., 'mp3', 'wav', etc.)
     */
    readonly format: string;
}

/**
 * A generated file.
 */
export declare interface GeneratedFile {
    /**
     * File as a base64 encoded string.
     */
    readonly base64: string;
    /**
     * File as a Uint8Array.
     */
    readonly uint8Array: Uint8Array;
    /**
     * The IANA media type of the file.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    readonly mediaType: string;
}

/**
 * Generates a 16-character random string to use for IDs.
 * Not cryptographically secure.
 */
export declare const generateId: IdGenerator;

/**
 * Generates a 16-character random string to use for IDs.
 * Not cryptographically secure.
 */
declare const generateId_2: IdGenerator_2;

/**
 * Generates images using an image model.
 *
 * @param model - The image model to use.
 * @param prompt - The prompt that should be used to generate the image.
 * @param n - Number of images to generate. Default: 1.
 * @param maxImagesPerCall - Maximum number of images to generate in a single API call.
 * @param size - Size of the images to generate. Must have the format `{width}x{height}`.
 * @param aspectRatio - Aspect ratio of the images to generate. Must have the format `{width}:{height}`.
 * @param seed - Seed for the image generation.
 * @param providerOptions - Additional provider-specific options that are passed through to the provider
 * as body parameters.
 * @param maxRetries - Maximum number of retries. Set to 0 to disable retries. Default: 2.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param headers - Additional HTTP headers to be sent with the request. Only applicable for HTTP-based providers.
 *
 * @returns A result object that contains the generated images.
 */
export declare function generateImage({ model: modelArg, prompt: promptArg, n, maxImagesPerCall, size, aspectRatio, seed, providerOptions, maxRetries: maxRetriesArg, abortSignal, headers, }: {
    /**
     * The image model to use.
     */
    model: ImageModel;
    /**
     * The prompt that should be used to generate the image.
     */
    prompt: GenerateImagePrompt;
    /**
     * Number of images to generate.
     */
    n?: number;
    /**
     * Maximum number of images to generate in a single API call. If not provided, the model's default will be used.
     */
    maxImagesPerCall?: number;
    /**
     * Size of the images to generate. Must have the format `{width}x{height}`. If not provided, the default size will be used.
     */
    size?: `${number}x${number}`;
    /**
     * Aspect ratio of the images to generate. Must have the format `{width}:{height}`. If not provided, the default aspect ratio will be used.
     */
    aspectRatio?: `${number}:${number}`;
    /**
     * Seed for the image generation. If not provided, the default seed will be used.
     */
    seed?: number;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     * "openai": {
     * "style": "vivid"
     * }
     * }
     * ```
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Maximum number of retries per image model call. Set to 0 to disable retries.
     *
     * @default 2
     */
    maxRetries?: number;
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional headers to include in the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string>;
}): Promise<GenerateImageResult>;

declare type GenerateImagePrompt = string | {
    images: Array<DataContent>;
    text?: string;
    mask?: DataContent;
};

/**
 * The result of a `generateImage` call.
 * It contains the images and additional information.
 */
export declare interface GenerateImageResult {
    /**
     * The first image that was generated.
     */
    readonly image: GeneratedFile;
    /**
     * The images that were generated.
     */
    readonly images: Array<GeneratedFile>;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    readonly warnings: Array<Warning>;
    /**
     * Response metadata from the provider. There may be multiple responses if we made multiple calls to the model.
     */
    readonly responses: Array<ImageModelResponseMetadata>;
    /**
     * Provider-specific metadata. They are passed through from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    readonly providerMetadata: ImageModelProviderMetadata;
    /**
     * Combined token usage across all underlying provider calls for this image generation.
     */
    readonly usage: ImageModelUsage;
}

/**
 * Generate a structured, typed object for a given prompt and schema using a language model.
 *
 * This function does not stream the output. If you want to stream the output, use `streamObject` instead.
 *
 * @param model - The language model to use.
 *
 * @param system - A system message that will be part of the prompt.
 * @param prompt - A simple text prompt. You can either use `prompt` or `messages` but not both.
 * @param messages - A list of messages. You can either use `prompt` or `messages` but not both.
 * @param allowSystemInMessages - Whether system messages are allowed in the `prompt` or `messages` fields. Default: false.
 *
 * @param maxOutputTokens - Maximum number of tokens to generate.
 * @param temperature - Temperature setting.
 * The value is passed through to the provider. The range depends on the provider and model.
 * It is recommended to set either `temperature` or `topP`, but not both.
 * @param topP - Nucleus sampling.
 * The value is passed through to the provider. The range depends on the provider and model.
 * It is recommended to set either `temperature` or `topP`, but not both.
 * @param topK - Only sample from the top K options for each subsequent token.
 * Used to remove "long tail" low probability responses.
 * Recommended for advanced use cases only. You usually only need to use temperature.
 * @param presencePenalty - Presence penalty setting.
 * It affects the likelihood of the model to repeat information that is already in the prompt.
 * The value is passed through to the provider. The range depends on the provider and model.
 * @param frequencyPenalty - Frequency penalty setting.
 * It affects the likelihood of the model to repeatedly use the same words or phrases.
 * The value is passed through to the provider. The range depends on the provider and model.
 * @param stopSequences - Stop sequences.
 * If set, the model will stop generating text when one of the stop sequences is generated.
 * @param seed - The seed (integer) to use for random sampling.
 * If set and supported by the model, calls will generate deterministic results.
 *
 * @param maxRetries - Maximum number of retries. Set to 0 to disable retries. Default: 2.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param headers - Additional HTTP headers to be sent with the request. Only applicable for HTTP-based providers.
 *
 * @param schema - The schema of the object that the model should generate.
 * @param schemaName - Optional name of the output that should be generated.
 * Used by some providers for additional LLM guidance, e.g.
 * via tool or schema name.
 * @param schemaDescription - Optional description of the output that should be generated.
 * Used by some providers for additional LLM guidance, e.g.
 * via tool or schema description.
 *
 * @param output - The type of the output.
 *
 * - 'object': The output is an object.
 * - 'array': The output is an array.
 * - 'enum': The output is an enum.
 * - 'no-schema': The output is not a schema.
 *
 * @param repairText - A function that attempts to repair the raw output of the model
 * to enable JSON parsing.
 * @param experimental_repairText - Deprecated alias for `repairText`.
 *
 * @param telemetry - Optional telemetry configuration.
 *
 * @param providerOptions - Additional provider-specific options. They are passed through
 * to the provider from the AI SDK and enable provider-specific
 * functionality that can be fully encapsulated in the provider.
 *
 * @param onStart - Callback invoked when generation begins, before the LLM call.
 * @param experimental_onStart - Deprecated alias for `onStart`.
 * @param onStepStart - Callback invoked when the model call begins.
 * @param experimental_onStepStart - Deprecated alias for `onStepStart`.
 * @param onStepEnd - Callback invoked when the model call completes with the raw result.
 * @param onStepFinish - Deprecated alias for `onStepEnd`.
 * @param onFinish - Callback invoked when the entire operation completes with the parsed object.
 *
 * @returns
 * A result object that contains the generated object, the finish reason, the token usage, and additional information.
 *
 * @deprecated Use `generateText` with an `output` setting instead.
 */
export declare function generateObject<SCHEMA extends FlexibleSchema<unknown> = FlexibleSchema<JSONValue_3>, OUTPUT extends 'object' | 'array' | 'enum' | 'no-schema' = InferSchema<SCHEMA> extends string ? 'enum' : 'object', RESULT = OUTPUT extends 'array' ? Array<InferSchema<SCHEMA>> : InferSchema<SCHEMA>>(options: Omit<LanguageModelCallOptions, 'stopSequences'> & Omit<RequestOptions, 'timeout'> & Prompt & (OUTPUT extends 'enum' ? {
    /**
     * The enum values that the model should use.
     */
    enum: Array<RESULT>;
    output: 'enum';
} : OUTPUT extends 'no-schema' ? {} : {
    /**
     * The schema of the object that the model should generate.
     */
    schema: SCHEMA;
    /**
     * Optional name of the output that should be generated.
     * Used by some providers for additional LLM guidance, e.g.
     * via tool or schema name.
     */
    schemaName?: string;
    /**
     * Optional description of the output that should be generated.
     * Used by some providers for additional LLM guidance, e.g.
     * via tool or schema description.
     */
    schemaDescription?: string;
}) & {
    output?: OUTPUT;
    /**
     * The language model to use.
     */
    model: LanguageModel;
    /**
     * A function that attempts to repair the raw output of the model
     * to enable JSON parsing.
     */
    repairText?: RepairTextFunction;
    /**
     * A function that attempts to repair the raw output of the model
     * to enable JSON parsing.
     *
     * @deprecated Use `repairText` instead.
     */
    experimental_repairText?: RepairTextFunction;
    /**
     * Optional telemetry configuration.
     */
    telemetry?: TelemetryOptions;
    /**
     * Optional telemetry configuration.
     *
     * @deprecated Use `telemetry` instead. This alias will be removed in a future major release.
     */
    experimental_telemetry?: TelemetryOptions;
    /**
     * Custom download function to use for URLs.
     *
     * By default, files are downloaded if the model does not support the URL for the given media type.
     */
    experimental_download?: Experimental_DownloadFunction | undefined;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Callback that is called when the generateObject operation begins,
     * before the LLM call is made.
     */
    onStart?: Callback<GenerateObjectStartEvent>;
    /**
     * Callback that is called when the generateObject operation begins,
     * before the LLM call is made.
     *
     * @deprecated Use `onStart` instead.
     */
    experimental_onStart?: Callback<GenerateObjectStartEvent>;
    /**
     * Callback that is called when the model call (step) begins,
     * before the provider is called.
     */
    onStepStart?: Callback<GenerateObjectStepStartEvent>;
    /**
     * Callback that is called when the model call (step) begins,
     * before the provider is called.
     *
     * @deprecated Use `onStepStart` instead.
     */
    experimental_onStepStart?: Callback<GenerateObjectStepStartEvent>;
    /**
     * Callback that is called when the model call (step) completes,
     * with the raw result before JSON parsing.
     */
    onStepEnd?: Callback<GenerateObjectStepEndEvent>;
    /**
     * Callback that is called when the model call (step) completes,
     * with the raw result before JSON parsing.
     *
     * @deprecated Use `onStepEnd` instead.
     */
    onStepFinish?: Callback<GenerateObjectStepEndEvent>;
    /**
     * Callback that is called when the entire operation completes
     * with the final parsed and validated object.
     */
    onFinish?: Callback<GenerateObjectEndEvent<RESULT>>;
    /**
     * Internal. For test use only. May change without notice.
     */
    _internal?: {
        generateId?: () => string;
        currentDate?: () => Date;
    };
}): Promise<GenerateObjectResult<RESULT>>;

/**
 * Event passed to the `onFinish` callback of
 * `generateObject` and `streamObject`.
 *
 * Called when the entire operation completes, including JSON parsing
 * and schema validation. For `streamObject`, the object may be undefined
 * if validation failed (the error is provided in that case).
 *
 * @deprecated
 */
export declare interface GenerateObjectEndEvent<RESULT> {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /**
     * The generated object (typed according to the schema).
     * Always defined for `generateObject`. May be `undefined` for `streamObject`
     * when parsing or validation fails.
     */
    readonly object: RESULT | undefined;
    /**
     * Error from parsing or schema validation, if any.
     * Always `undefined` for `generateObject` (which throws instead).
     */
    readonly error: unknown | undefined;
    /** The reasoning generated by the model, if any. */
    readonly reasoning: string | undefined;
    /** The unified reason why the generation finished. */
    readonly finishReason: FinishReason;
    /** The token usage of the generated response. */
    readonly usage: LanguageModelUsage;
    /** Warnings from the model provider (e.g. unsupported settings). */
    readonly warnings: CallWarning[] | undefined;
    /** Additional request information. */
    readonly request: Omit<LanguageModelRequestMetadata, 'messages'>;
    /** Additional response information. */
    readonly response: Omit<LanguageModelResponseMetadata, 'messages'>;
    /** Additional provider-specific metadata. */
    readonly providerMetadata: ProviderMetadata | undefined;
}

/**
 * The result of a `generateObject` call.
 */
export declare interface GenerateObjectResult<OBJECT> {
    /**
     * The generated object (typed according to the schema).
     */
    readonly object: OBJECT;
    /**
     * The reasoning that was used to generate the object.
     * Concatenated from all reasoning parts.
     */
    readonly reasoning: string | undefined;
    /**
     * The reason why the generation finished.
     */
    readonly finishReason: FinishReason;
    /**
     * The token usage of the generated response.
     */
    readonly usage: LanguageModelUsage;
    /**
     * Warnings from the model provider (e.g. unsupported settings).
     */
    readonly warnings: CallWarning[] | undefined;
    /**
     * Additional request information.
     */
    readonly request: Omit<LanguageModelRequestMetadata, 'messages'>;
    /**
     * Additional response information.
     */
    readonly response: Omit<LanguageModelResponseMetadata, 'messages'>;
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    readonly providerMetadata: ProviderMetadata | undefined;
    /**
     * Converts the object to a JSON response.
     * The response will have a status code of 200 and a content type of `application/json; charset=utf-8`.
     */
    toJsonResponse(init?: ResponseInit): Response;
}

/**
 * Event passed to the `onStart` callback of
 * `generateObject` and `streamObject`.
 *
 * Called when the operation begins, before any LLM call.
 *
 * @deprecated
 */
export declare interface GenerateObjectStartEvent {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /** Identifies the operation type (e.g. `'ai.generateObject'` or `'ai.streamObject'`). */
    readonly operationId: string;
    /** The provider identifier (e.g., 'openai', 'anthropic'). */
    readonly provider: string;
    /** The specific model identifier (e.g., 'gpt-4o'). */
    readonly modelId: string;
    /** The system message(s) provided to the model. */
    readonly system: Instructions | undefined;
    /** The prompt string or array of messages if using the prompt option. */
    readonly prompt: string | Array<ModelMessage> | undefined;
    /** The messages array if using the messages option. */
    readonly messages: Array<ModelMessage> | undefined;
    /** Maximum number of tokens to generate. */
    readonly maxOutputTokens: number | undefined;
    /** Sampling temperature for generation. */
    readonly temperature: number | undefined;
    /** Top-p (nucleus) sampling parameter. */
    readonly topP: number | undefined;
    /** Top-k sampling parameter. */
    readonly topK: number | undefined;
    /** Presence penalty for generation. */
    readonly presencePenalty: number | undefined;
    /** Frequency penalty for generation. */
    readonly frequencyPenalty: number | undefined;
    /** Random seed for reproducible generation. */
    readonly seed: number | undefined;
    /** Maximum number of retries for failed requests. */
    readonly maxRetries: number;
    /** Additional HTTP headers sent with the request. */
    readonly headers: Record<string, string | undefined> | undefined;
    /** Additional provider-specific options. */
    readonly providerOptions: ProviderOptions_2 | undefined;
    /** The output strategy type. */
    readonly output: 'object' | 'array' | 'enum' | 'no-schema';
    /** The JSON Schema used for object generation, if any. */
    readonly schema: Record<string, unknown> | undefined;
    /** Optional name of the schema. */
    readonly schemaName: string | undefined;
    /** Optional description of the schema. */
    readonly schemaDescription: string | undefined;
}

/**
 * Event passed to the `onStepEnd` callback of
 * `generateObject` and `streamObject`.
 *
 * Called when the model call (step) completes, with the raw result
 * before JSON parsing and schema validation.
 *
 * @deprecated
 */
export declare interface GenerateObjectStepEndEvent {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /** Zero-based index of the current step. Always `0` for object generation. */
    readonly stepNumber: 0;
    /** The provider identifier (e.g., 'openai', 'anthropic'). */
    readonly provider: string;
    /** The specific model identifier (e.g., 'gpt-4o'). */
    readonly modelId: string;
    /** The unified reason why the generation finished. */
    readonly finishReason: FinishReason;
    /** The token usage of the generated response. */
    readonly usage: LanguageModelUsage;
    /** The raw text output from the model (before JSON parsing). */
    readonly objectText: string;
    /** The reasoning generated by the model, if any. */
    readonly reasoning: string | undefined;
    /** Warnings from the model provider (e.g. unsupported settings). */
    readonly warnings: CallWarning[] | undefined;
    /** Additional request information. */
    readonly request: Omit<LanguageModelRequestMetadata, 'messages'>;
    /** Additional response information. */
    readonly response: Omit<LanguageModelResponseMetadata, 'messages'>;
    /** Additional provider-specific metadata. */
    readonly providerMetadata: ProviderMetadata | undefined;
    /** Milliseconds from the start of the stream to the first chunk (streaming only). */
    readonly msToFirstChunk: number | undefined;
}

/**
 * Event passed to the `onStepStart` callback of
 * `generateObject` and `streamObject`.
 *
 * Called when the model call (step) begins, before the provider is called.
 * For object generation, there is always exactly one step (step 0).
 *
 * @deprecated
 */
export declare interface GenerateObjectStepStartEvent {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /** Zero-based index of the current step. Always `0` for object generation. */
    readonly stepNumber: 0;
    /** The provider identifier (e.g., 'openai', 'anthropic'). */
    readonly provider: string;
    /** The specific model identifier (e.g., 'gpt-4o'). */
    readonly modelId: string;
    /** Additional provider-specific options. */
    readonly providerOptions: ProviderOptions_2 | undefined;
    /** Additional HTTP headers sent with the request. */
    readonly headers: Record<string, string | undefined> | undefined;
    /** The prompt messages in provider format (for telemetry). */
    readonly promptMessages?: LanguageModelV4Prompt_2;
}

/**
 * Generates speech audio using a speech model.
 *
 * @param model - The speech model to use.
 * @param text - The text to convert to speech.
 * @param voice - The voice to use for speech generation.
 * @param outputFormat - The output format to use for speech generation e.g. "mp3", "wav", etc.
 * @param instructions - Instructions for the speech generation e.g. "Speak in a slow and steady tone".
 * @param speed - The speed of the speech generation.
 * @param language - The language for speech generation (ISO 639-1 code e.g. "en", "es", "fr") or "auto" for automatic detection.
 * @param providerOptions - Additional provider-specific options that are passed through to the provider
 * as body parameters.
 * @param maxRetries - Maximum number of retries. Set to 0 to disable retries. Default: 2.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param headers - Additional HTTP headers to be sent with the request. Only applicable for HTTP-based providers.
 *
 * @returns A result object that contains the generated audio data.
 */
export declare function generateSpeech({ model, text, voice, outputFormat, instructions, speed, language, providerOptions, maxRetries: maxRetriesArg, abortSignal, headers, }: {
    /**
     * The speech model to use.
     */
    model: SpeechModel;
    /**
     * The text to convert to speech.
     */
    text: string;
    /**
     * The voice to use for speech generation.
     */
    voice?: string;
    /**
     * The desired output format for the audio e.g. "mp3", "wav", etc.
     */
    outputFormat?: 'mp3' | 'wav' | (string & {});
    /**
     * Instructions for the speech generation e.g. "Speak in a slow and steady tone".
     */
    instructions?: string;
    /**
     * The speed of the speech generation.
     */
    speed?: number;
    /**
     * The language for speech generation. This should be an ISO 639-1 language code (e.g. "en", "es", "fr")
     * or "auto" for automatic language detection. Provider support varies.
     */
    language?: string;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     * "openai": {}
     * }
     * ```
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Maximum number of retries per speech model call. Set to 0 to disable retries.
     *
     * @default 2
     */
    maxRetries?: number;
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional headers to include in the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string>;
}): Promise<SpeechResult>;

/**
 * Generate a text and call tools for a given prompt using a language model.
 *
 * This function does not stream the output. If you want to stream the output, use `streamText` instead.
 *
 * @param model - The language model to use.
 *
 * @param tools - Tools that are accessible to and can be called by the model. The model needs to support calling tools.
 * @param toolChoice - The tool choice strategy. Default: 'auto'.
 * @param toolOrder - Controls the order in which tools are sent to the provider. Tools not listed are appended alphabetically.
 *
 * @param system - A system message that will be part of the prompt.
 * @param prompt - A simple text prompt. You can either use `prompt` or `messages` but not both.
 * @param messages - A list of messages. You can either use `prompt` or `messages` but not both.
 * @param allowSystemInMessages - Whether system messages are allowed in the `prompt` or `messages` fields. Default: false.
 *
 * @param maxOutputTokens - Maximum number of tokens to generate.
 * @param temperature - Temperature setting.
 * The value is passed through to the provider. The range depends on the provider and model.
 * It is recommended to set either `temperature` or `topP`, but not both.
 * @param topP - Nucleus sampling.
 * The value is passed through to the provider. The range depends on the provider and model.
 * It is recommended to set either `temperature` or `topP`, but not both.
 * @param topK - Only sample from the top K options for each subsequent token.
 * Used to remove "long tail" low probability responses.
 * Recommended for advanced use cases only. You usually only need to use temperature.
 * @param presencePenalty - Presence penalty setting.
 * It affects the likelihood of the model to repeat information that is already in the prompt.
 * The value is passed through to the provider. The range depends on the provider and model.
 * @param frequencyPenalty - Frequency penalty setting.
 * It affects the likelihood of the model to repeatedly use the same words or phrases.
 * The value is passed through to the provider. The range depends on the provider and model.
 * @param stopSequences - Stop sequences.
 * If set, the model will stop generating text when one of the stop sequences is generated.
 * @param seed - The seed (integer) to use for random sampling.
 * If set and supported by the model, calls will generate deterministic results.
 *
 * @param maxRetries - Maximum number of retries. Set to 0 to disable retries. Default: 2.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param timeout - An optional timeout in milliseconds. The call will be aborted if it takes longer than the specified timeout.
 * @param headers - Additional HTTP headers to be sent with the request. Only applicable for HTTP-based providers.
 *
 * @param experimental_sandbox - The sandbox environment that is passed through to tool execution.
 * @param runtimeContext - User-defined runtime context that flows through the entire generation lifecycle.
 * @param experimental_refineToolInput - Optional mapping of tool names to functions that refine parsed tool inputs before tools are executed and before outputs, callbacks, and telemetry are recorded.
 * @param onStart - Callback invoked when generation begins, before any LLM calls.
 * @param experimental_onStart - Deprecated alias for `onStart`.
 * @param onStepStart - Callback invoked when each step begins, before the provider is called.
 * @param experimental_onStepStart - Deprecated alias for `onStepStart`.
 * Receives step number, messages (in ModelMessage format), tools, and runtimeContext.
 * @param onLanguageModelCallStart - Callback invoked immediately before each provider model call begins.
 * @param experimental_onLanguageModelCallStart - Deprecated alias for `onLanguageModelCallStart`.
 * @param onLanguageModelCallEnd - Callback invoked after each provider model call response is normalized and parsed.
 * @param experimental_onLanguageModelCallEnd - Deprecated alias for `onLanguageModelCallEnd`.
 * @param onToolExecutionStart - Callback invoked before each tool execution begins.
 * Receives tool name, call ID, input, and context.
 * @param experimental_onToolCallStart - Deprecated alias for `onToolExecutionStart`.
 * @param onToolExecutionEnd - Callback invoked after each tool execution completes.
 * Uses a discriminated union: check `success` to determine if `output` or `error` is present.
 * @param experimental_onToolCallFinish - Deprecated alias for `onToolExecutionEnd`.
 * @param onStepFinish - Callback that is called when each step (LLM call) is finished, including intermediate steps.
 * @param onEnd - Callback that is called when all steps are finished and the response is complete.
 * @param onFinish - Deprecated alias for `onEnd`.
 *
 * @returns
 * A result object that contains the generated text, the results of the tool calls, and additional information.
 */
export declare function generateText<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = Output_2<string, string>>({ model: modelArg, tools, toolChoice, instructions, system, prompt, messages, allowSystemInMessages, maxRetries: maxRetriesArg, abortSignal, timeout, headers, stopWhen, experimental_sandbox: sandbox, output, toolApproval, experimental_toolCallers, experimental_toolApprovalSecret, experimental_telemetry, telemetry, providerOptions, activeTools, toolOrder, prepareStep, experimental_repairToolCall, repairToolCall, experimental_refineToolInput: refineToolInput, experimental_download: download, runtimeContext, toolsContext, experimental_include, include, _internal: { generateId, generateCallId, now, }, onStart, experimental_onStart, onStepStart, experimental_onStepStart, onLanguageModelCallStart, experimental_onLanguageModelCallStart, onLanguageModelCallEnd, experimental_onLanguageModelCallEnd, onToolExecutionStart, onToolExecutionEnd, experimental_onToolCallStart, experimental_onToolCallFinish, onStepEnd, onStepFinish, onFinish, onEnd, ...settings }: LanguageModelCallOptions & RequestOptions<TOOLS> & Prompt & ToolsContextParameter<TOOLS> & {
    /**
     * The language model to use.
     */
    model: LanguageModel;
    /**
     * The tool choice strategy. Default: 'auto'.
     */
    toolChoice?: ToolChoice<NoInfer<TOOLS>>;
    /**
     * Condition for stopping the generation when there are tool results in the last step.
     * When the condition is an array, any of the conditions can be met to stop the generation.
     *
     * @default isStepCount(1)
     */
    stopWhen?: Arrayable_2<StopCondition<NoInfer<TOOLS>, RUNTIME_CONTEXT>>;
    /**
     * Optional telemetry configuration.
     */
    telemetry?: TelemetryOptions<RUNTIME_CONTEXT, NoInfer<TOOLS>>;
    /**
     * Optional telemetry configuration.
     *
     * @deprecated Use `telemetry` instead. This alias will be removed in a future major release.
     */
    experimental_telemetry?: TelemetryOptions<RUNTIME_CONTEXT, NoInfer<TOOLS>>;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * The sandbox environment that is passed through to tool execution.
     */
    experimental_sandbox?: Experimental_SandboxSession;
    /**
     * Runtime context. Treat runtime context as immutable.
     * If you need to mutate runtime context, update it in `prepareStep`.
     */
    runtimeContext?: RUNTIME_CONTEXT;
    /**
     * Limits the tools that are available for the model to call without
     * changing the tool call and result types in the result.
     */
    activeTools?: ActiveTools<NoInfer<TOOLS>>;
    /**
     * Controls the order in which tools are sent to the provider.
     *
     * The list can be partial. Tools not listed in `toolOrder` are sent after
     * the listed tools, sorted alphabetically. This can improve provider-side
     * caching by keeping tool definitions in a stable order.
     */
    toolOrder?: ToolOrder<NoInfer<TOOLS>>;
    /**
     * Optional specification for parsing structured outputs from the LLM response.
     */
    output?: OUTPUT;
    /**
     * Optional tool approval configuration.
     *
     * This configuration takes precedence over tool-defined approval settings.
     */
    toolApproval?: ToolApprovalConfiguration<TOOLS, RUNTIME_CONTEXT>;
    /**
     * Configures which caller tools may invoke each tool.
     */
    experimental_toolCallers?: Experimental_ToolCallers<NoInfer<TOOLS>>;
    /**
     * Secret for HMAC-signing tool approval requests. When set, the server
     * signs each approval request at issuance and verifies the signature when
     * the approval is replayed, preventing client-forged approvals.
     */
    experimental_toolApprovalSecret?: string | Uint8Array;
    /**
     * Custom download function to use for URLs.
     *
     * By default, files are downloaded if the model does not support the URL for the given media type.
     */
    experimental_download?: Experimental_DownloadFunction | undefined;
    /**
     * Optional function that you can use to provide different settings for a step.
     */
    prepareStep?: PrepareStepFunction<NoInfer<TOOLS>, RUNTIME_CONTEXT>;
    /**
     * A function that attempts to repair a tool call that failed to parse.
     */
    repairToolCall?: ToolCallRepairFunction<NoInfer<TOOLS>>;
    /**
     * A function that attempts to repair a tool call that failed to parse.
     *
     * @deprecated Use `repairToolCall` instead.
     */
    experimental_repairToolCall?: ToolCallRepairFunction<NoInfer<TOOLS>>;
    /**
     * Optional mapping of tool names to functions that refine parsed tool inputs.
     *
     * The refined input must have the same type shape as the tool input. Refined
     * inputs are used for tool execution, outputs, callbacks, and telemetry.
     */
    experimental_refineToolInput?: ToolInputRefinement<NoInfer<TOOLS>>;
    /**
     * Callback that is called when the generateText operation begins,
     * before any LLM calls are made.
     */
    onStart?: GenerateTextOnStartCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>, NoInfer<OUTPUT>>;
    /**
     * Callback that is called when the generateText operation begins,
     * before any LLM calls are made.
     *
     * @deprecated Use `onStart` instead.
     */
    experimental_onStart?: GenerateTextOnStartCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>, NoInfer<OUTPUT>>;
    /**
     * Callback that is called when a step (LLM call) begins,
     * before the provider is called.
     */
    onStepStart?: GenerateTextOnStepStartCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>, NoInfer<OUTPUT>>;
    /**
     * Callback that is called when a step (LLM call) begins,
     * before the provider is called.
     *
     * @deprecated Use `onStepStart` instead.
     */
    experimental_onStepStart?: GenerateTextOnStepStartCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>, NoInfer<OUTPUT>>;
    /**
     * Callback that is called immediately before the provider model call begins.
     */
    onLanguageModelCallStart?: OnLanguageModelCallStartCallback;
    /**
     * Callback that is called immediately before the provider model call begins.
     *
     * @deprecated Use `onLanguageModelCallStart` instead.
     */
    experimental_onLanguageModelCallStart?: OnLanguageModelCallStartCallback;
    /**
     * Callback that is called after the model response has been normalized and parsed,
     * but before any client-side tool execution begins.
     */
    onLanguageModelCallEnd?: OnLanguageModelCallEndCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called after the model response has been normalized and parsed,
     * but before any client-side tool execution begins.
     *
     * @deprecated Use `onLanguageModelCallEnd` instead.
     */
    experimental_onLanguageModelCallEnd?: OnLanguageModelCallEndCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called right before a tool's execute function runs.
     */
    onToolExecutionStart?: OnToolExecutionStartCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called right before a tool's execute function runs.
     *
     * @deprecated Use `onToolExecutionStart` instead.
     */
    experimental_onToolCallStart?: OnToolExecutionStartCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called right after a tool's execute function completes (or errors).
     */
    onToolExecutionEnd?: OnToolExecutionEndCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called right after a tool's execute function completes (or errors).
     *
     * @deprecated Use `onToolExecutionEnd` instead.
     */
    experimental_onToolCallFinish?: OnToolExecutionEndCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called when each step (LLM call) ends, including intermediate steps.
     */
    onStepEnd?: GenerateTextOnStepEndCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Callback that is called when each step (LLM call) ends, including intermediate steps.
     *
     * @deprecated Use `onStepEnd` instead.
     */
    onStepFinish?: GenerateTextOnStepFinishCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Callback that is called when all steps are finished and the response is complete.
     */
    onEnd?: GenerateTextOnEndCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Callback that is called when all steps are finished and the response is complete.
     *
     * @deprecated Use `onEnd` instead.
     */
    onFinish?: GenerateTextOnEndCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Settings for controlling what data is included in step results.
     * Disabling inclusion can help reduce memory usage when processing
     * large payloads like images.
     *
     * By default, request bodies, request messages, and response bodies are
     * excluded.
     */
    include?: GenerateTextInclude;
    /**
     * Settings for controlling what data is included in step results.
     *
     * @deprecated Use `include` instead.
     */
    experimental_include?: GenerateTextInclude;
    /**
     * Internal. For test use only. May change without notice.
     */
    _internal?: {
        generateId?: IdGenerator;
        generateCallId?: IdGenerator;
        now?: () => number;
    };
}): Promise<GenerateTextResult<TOOLS, RUNTIME_CONTEXT, OUTPUT>>;

/**
 * Event passed to the telemetry `onAbort` callback.
 *
 * Called when a streaming text generation operation is aborted before it
 * completes.
 */
export declare type GenerateTextAbortEvent<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /** Details for all previously finished steps. */
    readonly steps: StepResult<TOOLS, RUNTIME_CONTEXT>[];
    /** The abort reason from the AbortSignal, when one is available. */
    readonly reason?: unknown;
};

/**
 * Event passed to the `onEnd` callback.
 *
 * Called when the entire generation completes (all steps finished).
 * Includes the final step's result along with aggregated data from all steps.
 */
export declare type GenerateTextEndEvent<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /** Zero-based index of the final step. */
    readonly stepNumber: number;
    /** Information about the model that produced the final step. */
    readonly model: StepResult<TOOLS, RUNTIME_CONTEXT>['model'];
    /**
     * Tool context from the final step.
     *
     * @deprecated Use `finalStep.toolsContext` instead.
     */
    readonly toolsContext: InferToolSetContext_2<TOOLS>;
    /**
     * Runtime context from the final step.
     *
     * @deprecated Use `finalStep.runtimeContext` instead.
     */
    readonly runtimeContext: RUNTIME_CONTEXT;
    /** The content that was generated in all steps. */
    readonly content: StepResult<TOOLS, RUNTIME_CONTEXT>['content'];
    /** The text that was generated in the final step. */
    readonly text: StepResult<TOOLS, RUNTIME_CONTEXT>['text'];
    /**
     * The reasoning that was generated in the final step.
     *
     * @deprecated Use `finalStep.reasoning` instead.
     */
    readonly reasoning: StepResult<TOOLS, RUNTIME_CONTEXT>['reasoning'];
    /**
     * The reasoning text that was generated in the final step.
     *
     * @deprecated Use `finalStep.reasoningText` instead.
     */
    readonly reasoningText: StepResult<TOOLS, RUNTIME_CONTEXT>['reasoningText'];
    /** Files that were generated in all steps. */
    readonly files: StepResult<TOOLS, RUNTIME_CONTEXT>['files'];
    /** Sources that were used as references in all steps. */
    readonly sources: StepResult<TOOLS, RUNTIME_CONTEXT>['sources'];
    /** Tool calls that were made in all steps. */
    readonly toolCalls: StepResult<TOOLS, RUNTIME_CONTEXT>['toolCalls'];
    /** Static tool calls that were made in all steps. */
    readonly staticToolCalls: StepResult<TOOLS, RUNTIME_CONTEXT>['staticToolCalls'];
    /** Dynamic tool calls that were made in all steps. */
    readonly dynamicToolCalls: StepResult<TOOLS, RUNTIME_CONTEXT>['dynamicToolCalls'];
    /** Tool results that were generated in all steps. */
    readonly toolResults: StepResult<TOOLS, RUNTIME_CONTEXT>['toolResults'];
    /** Static tool results that were generated in all steps. */
    readonly staticToolResults: StepResult<TOOLS, RUNTIME_CONTEXT>['staticToolResults'];
    /** Dynamic tool results that were generated in all steps. */
    readonly dynamicToolResults: StepResult<TOOLS, RUNTIME_CONTEXT>['dynamicToolResults'];
    /** The unified reason why the generation finished. Taken from the final step. */
    readonly finishReason: StepResult<TOOLS, RUNTIME_CONTEXT>['finishReason'];
    /** The raw reason why the generation finished. Taken from the final step. */
    readonly rawFinishReason: StepResult<TOOLS, RUNTIME_CONTEXT>['rawFinishReason'];
    /** Aggregated token usage across all steps. */
    readonly usage: LanguageModelUsage;
    /**
     * Aggregated token usage across all steps.
     *
     * @deprecated Use `usage` instead.
     */
    readonly totalUsage: LanguageModelUsage;
    /** Warnings from the model provider in all steps. */
    readonly warnings: StepResult<TOOLS, RUNTIME_CONTEXT>['warnings'];
    /**
     * Additional request information from the final step.
     *
     * @deprecated Use `finalStep.request` instead.
     */
    readonly request: StepResult<TOOLS, RUNTIME_CONTEXT>['request'];
    /**
     * Additional response information from the final step.
     *
     * @deprecated Use `finalStep.response` instead.
     */
    readonly response: StepResult<TOOLS, RUNTIME_CONTEXT>['response'];
    /**
     * Additional provider-specific metadata from the final step.
     *
     * @deprecated Use `finalStep.providerMetadata` instead.
     */
    readonly providerMetadata: StepResult<TOOLS, RUNTIME_CONTEXT>['providerMetadata'];
    /** The response messages that were generated during the call. */
    readonly responseMessages: ResponseMessage[];
    /** Array containing results from all steps in the generation. */
    readonly steps: StepResult<TOOLS, RUNTIME_CONTEXT>[];
    /** The final step. This is a shortcut for `steps.at(-1)`. */
    readonly finalStep: StepResult<TOOLS, RUNTIME_CONTEXT>;
};

export declare type GenerateTextInclude = {
    /**
     * Whether to retain the request body in step results.
     * The request body can be large when sending images or files.
     *
     * @default false
     */
    requestBody?: boolean;
    /**
     * Whether to retain the request messages in step results.
     * The request messages can be large when sending images or files.
     *
     * @default false
     */
    requestMessages?: boolean;
    /**
     * Whether to retain the response body in step results.
     *
     * @default false
     */
    responseBody?: boolean;
};

/**
 * Callback that is set using the telemetry `onAbort` option.
 *
 * Called when a streaming text generation operation is aborted before it
 * completes.
 *
 * @param event - The abort event, including finished steps and abort reason.
 */
export declare type GenerateTextOnAbortCallback<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = Callback<GenerateTextAbortEvent<TOOLS, RUNTIME_CONTEXT>>;

/**
 * Callback that is set using the `onEnd` option.
 *
 * Called when the entire generation completes (all steps finished).
 * The event includes the final step's result properties along with
 * aggregated data from all steps.
 *
 * @param event - The final result along with aggregated step data.
 */
export declare type GenerateTextOnEndCallback<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = Callback<GenerateTextEndEvent<TOOLS, RUNTIME_CONTEXT>>;

/**
 * Callback that is set using the `onFinish` option.
 *
 * @deprecated Use `GenerateTextOnEndCallback` instead.
 */
export declare type GenerateTextOnFinishCallback<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = GenerateTextOnEndCallback<TOOLS, RUNTIME_CONTEXT>;

/**
 * Callback that is set using the `onStart` option.
 *
 * Called when the generateText operation begins, before any LLM calls.
 * Use this callback for logging, analytics, or initializing state at the
 * start of a generation.
 *
 * @param event - The event object containing generation configuration.
 */
export declare type GenerateTextOnStartCallback<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = Output_2> = Callback<GenerateTextStartEvent<TOOLS, RUNTIME_CONTEXT, OUTPUT>>;

/**
 * Callback that is set using the `onStepEnd` option.
 *
 * Called when a step (LLM call) completes. The event includes all step result
 * properties (text, tool calls, usage, etc.) along with additional metadata.
 *
 * @param stepResult - The result of the step.
 */
export declare type GenerateTextOnStepEndCallback<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = Callback<GenerateTextStepEndEvent<TOOLS, RUNTIME_CONTEXT>>;

/**
 * Callback that is set using the `onStepFinish` option.
 *
 * @deprecated Use `GenerateTextOnStepEndCallback` instead.
 */
export declare type GenerateTextOnStepFinishCallback<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = GenerateTextOnStepEndCallback<TOOLS, RUNTIME_CONTEXT>;

/**
 * Callback that is set using the `onStepStart` option.
 *
 * Called when a step (LLM call) begins, before the provider is called.
 * Each step represents a single LLM invocation. Multiple steps occur when
 * using tool calls (the model may be called multiple times in a loop).
 *
 * @param event - The event object containing step configuration.
 */
export declare type GenerateTextOnStepStartCallback<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = Output_2> = Callback<GenerateTextStepStartEvent<TOOLS, RUNTIME_CONTEXT, OUTPUT>>;

/**
 * The result of a `generateText` call.
 * It contains the generated text, the tool calls that were made during the generation, and the results of the tool calls.
 */
export declare interface GenerateTextResult<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2, OUTPUT extends Output_2> {
    /**
     * The content that was generated in all steps.
     */
    readonly content: Array<ContentPart<TOOLS>>;
    /**
     * The concatenation of all text parts generated in the final step.
     * It is an empty string if the final step contains no text parts.
     * Inspect `finalStep.content` to distinguish that case.
     */
    readonly text: string;
    /**
     * The full reasoning that the model has generated in the last step.
     *
     * @deprecated Use `finalStep.reasoning` instead.
     */
    readonly reasoning: Array<ReasoningOutput | ReasoningFileOutput>;
    /**
     * The reasoning text that the model has generated in the last step. Can be undefined if the model
     * has only generated text.
     *
     * @deprecated Use `finalStep.reasoningText` instead.
     */
    readonly reasoningText: string | undefined;
    /**
     * The files that were generated in all steps.
     * Empty array if no files were generated.
     */
    readonly files: Array<GeneratedFile>;
    /**
     * Sources that have been used as references in all steps.
     */
    readonly sources: Array<Source>;
    /**
     * The tool calls that were made in all steps.
     */
    readonly toolCalls: Array<TypedToolCall<TOOLS>>;
    /**
     * The static tool calls that were made in all steps.
     */
    readonly staticToolCalls: Array<StaticToolCall<TOOLS>>;
    /**
     * The dynamic tool calls that were made in all steps.
     */
    readonly dynamicToolCalls: Array<DynamicToolCall>;
    /**
     * The results of the tool calls from all steps.
     */
    readonly toolResults: Array<TypedToolResult<TOOLS>>;
    /**
     * The static tool results that were made in all steps.
     */
    readonly staticToolResults: Array<StaticToolResult<TOOLS>>;
    /**
     * The dynamic tool results that were made in all steps.
     */
    readonly dynamicToolResults: Array<DynamicToolResult>;
    /**
     * The unified reason why the generation finished.
     */
    readonly finishReason: FinishReason;
    /**
     * The raw reason why the generation finished (from the provider).
     */
    readonly rawFinishReason: string | undefined;
    /**
     * The total token usage of all steps.
     * When there are multiple steps, the usage is the sum of all step usages.
     */
    readonly usage: LanguageModelUsage;
    /**
     * The total token usage of all steps.
     * When there are multiple steps, the usage is the sum of all step usages.
     *
     * @deprecated Use `usage` instead.
     */
    readonly totalUsage: LanguageModelUsage;
    /**
     * Warnings from the model provider (e.g. unsupported settings) in all steps.
     */
    readonly warnings: CallWarning[] | undefined;
    /**
     * Additional request information from the last step.
     *
     * @deprecated Use `finalStep.request` instead.
     */
    readonly request: StepResult<TOOLS, RUNTIME_CONTEXT>['request'];
    /**
     * Additional response information from the last step.
     *
     * @deprecated Use `finalStep.response` instead.
     */
    readonly response: LanguageModelResponseMetadata;
    /**
     * The accumulated response messages of all steps that were generated during the call.
     */
    readonly responseMessages: Array<ResponseMessage>;
    /**
     * Additional provider-specific metadata from the final step. They are passed
     * through from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     *
     * @deprecated Use `finalStep.providerMetadata` instead.
     */
    readonly providerMetadata: ProviderMetadata | undefined;
    /**
     * Details for all steps.
     * You can use this to get information about intermediate steps,
     * such as the tool calls or the response headers.
     */
    readonly steps: Array<StepResult<TOOLS, RUNTIME_CONTEXT>>;
    /**
     * The final step. This is a shortcut for `steps.at(-1)`.
     */
    readonly finalStep: StepResult<TOOLS, RUNTIME_CONTEXT>;
    /**
     * The generated output according to the `output` specification.
     *
     * @throws {NoOutputGeneratedError} When no output is available, for example
     * when the final step does not finish with a `stop` reason.
     */
    readonly output: InferGenerateOutput<OUTPUT>;
}

/**
 * Event passed to the `onStart` callback.
 *
 * Called when the generation operation begins, before any LLM calls.
 */
export declare type GenerateTextStartEvent<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = Output_2> = {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /** Identifies the operation type (e.g. 'ai.generateText' or 'ai.streamText'). */
    readonly operationId: string;
    /** The provider identifier (e.g., 'openai', 'anthropic'). */
    readonly provider: string;
    /** The specific model identifier (e.g., 'gpt-4o'). */
    readonly modelId: string;
    /** The tools available for this generation. */
    readonly tools: TOOLS | undefined;
    /** The tool choice strategy for this generation. */
    readonly toolChoice: ToolChoice<NoInfer<TOOLS>> | undefined;
    /** Limits which tools are available for the model to call. */
    readonly activeTools: ActiveTools<TOOLS>;
    /** Controls the order in which tools are sent to the provider. */
    readonly toolOrder: ToolOrder<TOOLS>;
    /** Maximum number of retries for failed requests. */
    readonly maxRetries: number;
    /**
     * Timeout configuration for the generation.
     * Can be a number (milliseconds) or an object with totalMs, stepMs,
     * firstChunkMs (streaming only), chunkMs, toolMs, and per-tool overrides via tools.
     */
    readonly timeout: TimeoutConfiguration<TOOLS> | undefined;
    /** Additional HTTP headers sent with the request. */
    readonly headers: Record<string, string | undefined> | undefined;
    /** Additional provider-specific options. */
    readonly providerOptions: ProviderOptions_2 | undefined;
    /** The output specification for structured outputs, if configured. */
    readonly output: OUTPUT | undefined;
    /**
     * Tool context.
     */
    readonly toolsContext: InferToolSetContext_2<TOOLS>;
    /**
     * User-defined runtime context.
     */
    readonly runtimeContext: RUNTIME_CONTEXT;
} & LanguageModelCallOptions & StandardizedPrompt;

/**
 * Event passed to the `onStepEnd` callback.
 *
 * Called when a step (LLM call) completes.
 * Includes the StepResult for that step along with the call identifier.
 */
export declare type GenerateTextStepEndEvent<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = StepResult<TOOLS, RUNTIME_CONTEXT>;

/**
 * Event passed to the `onStepStart` callback.
 *
 * Called when a step (LLM call) begins, before the provider is called.
 * Each step represents a single LLM invocation.
 */
export declare type GenerateTextStepStartEvent<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = Output_2> = {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /** The provider identifier (e.g., 'openai', 'anthropic'). */
    readonly provider: string;
    /** The specific model identifier (e.g., 'gpt-4o'). */
    readonly modelId: string;
    /** Zero-based index of the current step. */
    readonly stepNumber: number;
    /** The tools available for this generation. */
    readonly tools: TOOLS | undefined;
    /** The tool choice configuration for this step. */
    readonly toolChoice: ToolChoice<NoInfer<TOOLS>> | undefined;
    /** Limits which tools are available for this step. */
    readonly activeTools: ActiveTools<TOOLS>;
    /** Controls the order in which tools are sent to the provider for this step. */
    readonly toolOrder: ToolOrder<TOOLS>;
    /** Array of results from previous steps (empty for first step). */
    readonly steps: ReadonlyArray<StepResult<TOOLS, RUNTIME_CONTEXT>>;
    /** Additional provider-specific options for this step. */
    readonly providerOptions: ProviderOptions_2 | undefined;
    /** The output specification for structured outputs, if configured. */
    readonly output: OUTPUT | undefined;
    /**
     * Runtime context. May be updated from `prepareStep` between steps.
     */
    readonly runtimeContext: RUNTIME_CONTEXT;
    /**
     * Tool context. May be updated from `prepareStep` between steps.
     */
    readonly toolsContext: InferToolSetContext_2<TOOLS>;
} & StandardizedPrompt;

/**
 * Polling configuration for models that support the asynchronous
 * start/status flow.
 *
 * When used with `webhook`, `timeoutMs` also limits how long the SDK waits for
 * the webhook notification. If the model does not support webhooks, these
 * options configure the automatic polling fallback.
 */
declare type GenerateVideoPollOptions = {
    /**
     * Interval between status checks in milliseconds.
     *
     * @default 5000
     */
    intervalMs?: number;
    /**
     * Maximum time to wait for completion in milliseconds.
     *
     * @default 600000 (10 minutes)
     */
    timeoutMs?: number;
    /**
     * Custom delay implementation for polling intervals and webhook timeouts.
     * This can be used with durable workflow sleep functions.
     *
     * @default the built-in timer-based delay
     */
    delay?: (delayInMs: number, options?: {
        abortSignal?: AbortSignal;
    }) => PromiseLike<void>;
};

export declare type GenerateVideoPrompt = string | {
    image: DataContent;
    text?: string;
};

/**
 * The result of an `experimental_generateVideo` call.
 * Contains the generated video and additional information.
 */
export declare interface GenerateVideoResult {
    /**
     * The first video that was generated.
     */
    readonly video: GeneratedFile;
    /**
     * All videos that were generated.
     */
    readonly videos: Array<GeneratedFile>;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    readonly warnings: Array<Warning>;
    /**
     * Response metadata from the provider.
     * May contain multiple responses if multiple calls were made.
     */
    readonly responses: Array<VideoModelResponseMetadata>;
    /**
     * Provider-specific metadata passed through from the provider.
     */
    readonly providerMetadata: VideoModelProviderMetadata;
}

/**
 * Webhook factory for models that support the asynchronous start/status flow.
 *
 * The factory should return a URL for the provider to send notifications to,
 * and a `received` promise that resolves when the notification arrives.
 */
declare type GenerateVideoWebhookFactory = () => PromiseLike<{
    url: string;
    received: PromiseLike<VideoModelV4OperationWebhook>;
}>;

/**
 * Function that is called to determine if a tool call needs approval before it can be executed.
 *
 * Return `undefined` for the same effect as the `not-applicable` status.
 */
export declare type GenericToolApprovalFunction<TOOLS extends ToolSet, TOOLS_CONTEXT extends InferToolSetContext_2<TOOLS>, RUNTIME_CONTEXT extends Context_2 | unknown | never> = (options: {
    /**
     * The tool call that needs approval.
     */
    toolCall: TypedToolCall<TOOLS>;
    /**
     * All tools that are available for the model to call.
     */
    tools: TOOLS | undefined;
    /**
     * Tool context for all tools that are available for the model to call.
     */
    toolsContext: TOOLS_CONTEXT;
    /**
     * Runtime context.
     */
    runtimeContext: RUNTIME_CONTEXT;
    /**
     * Messages that were sent to the language model to initiate the response that contained the tool call.
     * The messages **do not** include the system prompt nor the assistant response that contained the tool call.
     */
    messages: ModelMessage[];
}) => MaybePromiseLike_2<ToolApprovalStatus>;

/**
 * Extracts the chunk timeout value in milliseconds from a TimeoutConfiguration.
 * This timeout is for streaming only - it aborts if no new content chunk is received within the specified duration.
 *
 * @param timeout - The timeout configuration.
 * @returns The chunk timeout in milliseconds, or undefined if no chunk timeout is configured.
 */
export declare function getChunkTimeoutMs(timeout: TimeoutConfiguration<any> | undefined): number | undefined;

declare function getErrorMessage(error: unknown | undefined): string;

/**
 * Extracts the first chunk timeout value in milliseconds from a TimeoutConfiguration.
 * This timeout is for streaming only - it aborts if no content chunk is received within the specified duration.
 *
 * @param timeout - The timeout configuration.
 * @returns The first chunk timeout in milliseconds, or undefined if no first chunk timeout is configured.
 */
export declare function getFirstChunkTimeoutMs(timeout: TimeoutConfiguration<any> | undefined): number | undefined;

declare const getFromApi: <T>({ url, headers, successfulResponseHandler, failedResponseHandler, abortSignal, fetch, validateUrl, credentialedOrigin, trustedOrigin, }: {
    url: string;
    headers?: Record<string, string | undefined>;
    failedResponseHandler: ResponseHandler<Error>;
    successfulResponseHandler: ResponseHandler<T>;
    abortSignal?: AbortSignal;
    fetch?: FetchFunction;
    /**
     * Set `true` when `url` is untrusted (e.g. taken from a provider response
     * body): it is routed through {@link fetchWithValidatedRedirects}, which
     * rejects private/loopback/link-local targets and re-validates redirect
     * hops; blocked URLs throw `DownloadError`. Set `false` only for URLs built
     * from a developer-configured endpoint.
     *
     * Optional for backwards compatibility with existing callers; omitting it
     * behaves like `false` (no validation). Provider code in this repository
     * must always pass it explicitly so every call site makes a visible trust
     * decision — see `contributing/secure-url-handling.md`.
     */
    validateUrl?: boolean;
    /**
     * When set, `headers` are sent only if `url` is same-origin with this origin
     * (the user-agent suffix is always kept). Pass the provider's configured
     * base URL alongside `validateUrl: true` so credentials never ride a request
     * to a response-supplied host on a different origin (e.g. a CDN). Redirects
     * that later cross origin drop all caller headers regardless (see
     * {@link fetchWithValidatedRedirects}).
     */
    credentialedOrigin?: string;
    /**
     * A developer-configured origin (e.g. the provider's `baseURL`) that is
     * exempt from URL validation when `validateUrl` is `true`. A response URL
     * (or redirect hop) that is same-origin with it is fetched without target
     * validation — it points at exactly the host a config-derived
     * `validateUrl: false` request would fetch anyway, so blocking it would
     * only break legitimate self-hosted / localhost deployments whose response
     * URLs point back at the configured host. Hops on any other origin are
     * still validated. Must never be derived from response data.
     */
    trustedOrigin?: string;
}) => Promise<{
    value: T;
    rawValue?: unknown;
    responseHeaders?: Record<string, string>;
}>;

declare const getFromApi_2: <T>({ url, headers, successfulResponseHandler, failedResponseHandler, abortSignal, fetch, validateUrl, credentialedOrigin, trustedOrigin, }: {
    url: string;
    headers?: Record<string, string | undefined>;
    failedResponseHandler: ResponseHandler_2<Error>;
    successfulResponseHandler: ResponseHandler_2<T>;
    abortSignal?: AbortSignal;
    fetch?: FetchFunction_2;
    /**
     * Set `true` when `url` is untrusted (e.g. taken from a provider response
     * body): it is routed through {@link fetchWithValidatedRedirects}, which
     * rejects private/loopback/link-local targets and re-validates redirect
     * hops; blocked URLs throw `DownloadError`. Set `false` only for URLs built
     * from a developer-configured endpoint.
     *
     * Optional for backwards compatibility with existing callers; omitting it
     * behaves like `false` (no validation). Provider code in this repository
     * must always pass it explicitly so every call site makes a visible trust
     * decision — see `contributing/secure-url-handling.md`.
     */
    validateUrl?: boolean;
    /**
     * When set, `headers` are sent only if `url` is same-origin with this origin
     * (the user-agent suffix is always kept). Pass the provider's configured
     * base URL alongside `validateUrl: true` so credentials never ride a request
     * to a response-supplied host on a different origin (e.g. a CDN). Redirects
     * that later cross origin drop all caller headers regardless (see
     * {@link fetchWithValidatedRedirects}).
     */
    credentialedOrigin?: string;
    /**
     * A developer-configured origin (e.g. the provider's `baseURL`) that is
     * exempt from URL validation when `validateUrl` is `true`. A response URL
     * (or redirect hop) that is same-origin with it is fetched without target
     * validation — it points at exactly the host a config-derived
     * `validateUrl: false` request would fetch anyway, so blocking it would
     * only break legitimate self-hosted / localhost deployments whose response
     * URLs point back at the configured host. Hops on any other origin are
     * still validated. Must never be derived from response data.
     */
    trustedOrigin?: string;
}) => Promise<{
    value: T;
    rawValue?: unknown;
    responseHeaders?: Record<string, string>;
}>;

declare type GetMaxImagesPerCallFunction$1 = (options: {
    modelId: string;
}) => PromiseLike<number | undefined> | number | undefined;

declare type GetMaxImagesPerCallFunction$2 = (options: {
    modelId: string;
}) => PromiseLike<number | undefined> | number | undefined;

declare type GetMaxImagesPerCallFunction$2_2 = (options: {
    modelId: string;
}) => PromiseLike<number | undefined> | number | undefined;

declare type GetMaxImagesPerCallFunction = (options: {
    modelId: string;
}) => PromiseLike<number | undefined> | number | undefined;

declare type GetMaxVideosPerCallFunction$1 = (options: {
    modelId: string;
}) => PromiseLike<number | undefined> | number | undefined;

declare type GetMaxVideosPerCallFunction$1_2 = (options: {
    modelId: string;
}) => PromiseLike<number | undefined> | number | undefined;

declare type GetMaxVideosPerCallFunction = (options: {
    modelId: string;
}) => PromiseLike<number | undefined> | number | undefined;

declare const getOriginalFetch: () => typeof fetch;

declare function getRuntimeEnvironmentUserAgent(globalThisAny?: any): string;

declare function getRuntimeEnvironmentUserAgent_2(globalThisAny?: any): string;

/**
 * Returns the name of the static tool.
 *
 * The possible values are the keys of the tool set.
 */
export declare function getStaticToolName<TOOLS extends UITools>(part: ToolUIPart<TOOLS>): keyof TOOLS;

/**
 * Extracts the step timeout value in milliseconds from a TimeoutConfiguration.
 *
 * @param timeout - The timeout configuration.
 * @returns The step timeout in milliseconds, or undefined if no step timeout is configured.
 */
export declare function getStepTimeoutMs(timeout: TimeoutConfiguration<any> | undefined): number | undefined;

/**
 * Converts a data URL of type text/* to a text string.
 */
export declare function getTextFromDataUrl(dataUrl: string): string;

declare function getToolCaller(tool: Tool | undefined): ToolCallerDefinition | undefined;

/**
 * Returns the name of the tool (static or dynamic).
 *
 * This function will not restrict the name to the keys of the tool set.
 * If you need to restrict the name to the keys of the tool set, use `getStaticToolName` instead.
 */
export declare function getToolName(part: ToolUIPart<UITools> | DynamicToolUIPart): string;

/**
 * @deprecated Use getToolName instead.
 */
export declare const getToolOrDynamicToolName: typeof getToolName;

export declare function getToolTimeoutMs<TOOLS extends ToolSet>(timeout: TimeoutConfiguration<TOOLS> | undefined, toolName: keyof TOOLS & string): number | undefined;

/**
 * Returns the top-level segment of a media type (the portion before `/`).
 *
 * Examples:
 *   - `"image/png"` -> `"image"`
 *   - `"image/*"` -> `"image"`
 *   - `"image"` -> `"image"`
 *   - `"image/"` -> `"image"`
 *   - `""` -> `""`
 *   - `"/"` -> `""`
 */
declare function getTopLevelMediaType(mediaType: string): string;

/**
 * Returns the top-level segment of a media type (the portion before `/`).
 *
 * Examples:
 *   - `"image/png"` -> `"image"`
 *   - `"image/*"` -> `"image"`
 *   - `"image"` -> `"image"`
 *   - `"image/"` -> `"image"`
 *   - `""` -> `""`
 *   - `"/"` -> `""`
 */
declare function getTopLevelMediaType_2(mediaType: string): string;

/**
 * Extracts the total timeout value in milliseconds from a TimeoutConfiguration.
 *
 * @param timeout - The timeout configuration.
 * @returns The total timeout in milliseconds, or undefined if no timeout is configured.
 */
export declare function getTotalTimeoutMs(timeout: TimeoutConfiguration<any> | undefined): number | undefined;

declare function getWebSocketConstructor(webSocket: WebSocketConstructor | undefined): WebSocketConstructor;

declare function getWebSocketConstructor_2(webSocket: WebSocketConstructor_2 | undefined): WebSocketConstructor_2;

/**
 * Global provider model ID type that defaults to GatewayModelId but can be augmented
 * by third-party packages via declaration merging.
 */
declare type GlobalProviderModelId = [keyof RegisteredProviderModels] extends [
never
] ? GatewayModelId : keyof RegisteredProviderModels | RegisteredProviderModels[keyof RegisteredProviderModels];

/**
 * Checks if an object has required keys.
 * @param OBJECT - The object to check.
 * @returns True if the object has required keys, false otherwise.
 */
declare type HasRequiredKey<OBJECT> = {} extends OBJECT ? false : true;

/**
 * Checks if an object has required keys.
 * @param OBJECT - The object to check.
 * @returns True if the object has required keys, false otherwise.
 */
declare type HasRequiredKey_2<OBJECT> = {} extends OBJECT ? false : true;

/**
 * Creates a stop condition that returns `true` when the most recent step
 * contains a tool call with any of the specified names.
 *
 * @param toolName - The names of the tools that should stop the loop.
 */
export declare function hasToolCall<TOOLS extends ToolSet>(...toolName: Array<keyof TOOLS | (string & {})>): StopCondition<TOOLS, any>;

export declare abstract class HttpChatTransport<UI_MESSAGE extends UIMessage> implements ChatTransport<UI_MESSAGE> {
    protected api: string;
    protected credentials: HttpChatTransportInitOptions<UI_MESSAGE>['credentials'];
    protected headers: HttpChatTransportInitOptions<UI_MESSAGE>['headers'];
    protected body: HttpChatTransportInitOptions<UI_MESSAGE>['body'];
    protected fetch?: FetchFunction_2;
    protected prepareSendMessagesRequest?: PrepareSendMessagesRequest<UI_MESSAGE>;
    protected prepareReconnectToStreamRequest?: PrepareReconnectToStreamRequest;
    constructor({ api, credentials, headers, body, fetch, prepareSendMessagesRequest, prepareReconnectToStreamRequest, }: HttpChatTransportInitOptions<UI_MESSAGE>);
    sendMessages({ abortSignal, ...options }: Parameters<ChatTransport<UI_MESSAGE>['sendMessages']>[0]): Promise<ReadableStream<UIMessageChunk>>;
    reconnectToStream(options: Parameters<ChatTransport<UI_MESSAGE>['reconnectToStream']>[0]): Promise<ReadableStream<UIMessageChunk> | null>;
    protected abstract processResponseStream(stream: ReadableStream<Uint8Array<ArrayBufferLike>>): ReadableStream<UIMessageChunk>;
}

/**
 * Options for the `HttpChatTransport` class.
 *
 * @param UI_MESSAGE - The type of message to be used in the chat.
 */
export declare type HttpChatTransportInitOptions<UI_MESSAGE extends UIMessage> = {
    /**
     * The API URL to be used for the chat transport.
     * Defaults to '/api/chat'.
     */
    api?: string;
    /**
     * The credentials mode to be used for the fetch request.
     * Possible values are: 'omit', 'same-origin', 'include'.
     * Defaults to 'same-origin'.
     */
    credentials?: Resolvable_2<RequestCredentials>;
    /**
     * HTTP headers to be sent with the API request.
     */
    headers?: Resolvable_2<Record<string, string> | Headers>;
    /**
     * Extra body object to be sent with the API request.
     * @example
     * Send a `sessionId` to the API along with the messages.
     * ```js
     * useChat({
     *   body: {
     *     sessionId: '123',
     *   }
     * })
     * ```
     */
    body?: Resolvable_2<object>;
    /**
     * Custom fetch implementation. You can use it as a middleware to intercept requests,
     * or to provide a custom fetch implementation for e.g. testing.
     */
    fetch?: FetchFunction_2;
    /**
     * When a function is provided, it will be used
     * to prepare the request body for the chat API. This can be useful for
     * customizing the request body based on the messages and data in the chat.
     */
    prepareSendMessagesRequest?: PrepareSendMessagesRequest<UI_MESSAGE>;
    /**
     * When a function is provided, it will be used
     * to prepare the reconnect request for the chat API. This can be useful for
     * customizing the request based on the chat session.
     */
    prepareReconnectToStreamRequest?: PrepareReconnectToStreamRequest;
};

/**
 * A function that generates an ID.
 */
export declare type IdGenerator = () => string;

/**
 * A function that generates an ID.
 */
declare type IdGenerator_2 = () => string;

/**
 * Image model that is used by the AI SDK.
 */
export declare type ImageModel = string | ImageModelV4_2 | ImageModelV3 | ImageModelV2;

/**
 * Middleware for image models.
 * Accepts both V3 and V4 middleware types for backward compatibility.
 *
 * Uses ImageModelV4Middleware as the base but relaxes specificationVersion
 * to accept any string (including 'v3') and makes it optional.
 */
export declare type ImageModelMiddleware = Omit<ImageModelV4Middleware, 'specificationVersion'> & {
    readonly specificationVersion?: string;
};

/**
 * Metadata from the model provider for this call.
 */
export declare type ImageModelProviderMetadata = ImageModelV4ProviderMetadata_2 | ImageModelV2ProviderMetadata;

export declare type ImageModelResponseMetadata = {
    /**
     * Timestamp for the start of the generated response.
     */
    timestamp: Date;
    /**
     * The ID of the response model that was used to generate the response.
     */
    modelId: string;
    /**
     * Response headers.
     */
    headers?: Record<string, string>;
};

/**
 * Usage information for an image model call.
 */
export declare type ImageModelUsage = ImageModelV4Usage_2;

/**
 * Image generation model specification version 2.
 */
declare type ImageModelV2 = {
    /**
     * The image model must specify which image model interface
     * version it implements. This will allow us to evolve the image
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v2';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many images can be generated in a single API call.
     * Can be set to a number for a fixed limit, to undefined to use
     * the global limit, or a function that returns a number or undefined,
     * optionally as a promise.
     */
    readonly maxImagesPerCall: number | undefined | GetMaxImagesPerCallFunction;
    /**
     * Generates an array of images.
     */
    doGenerate(options: ImageModelV2CallOptions): PromiseLike<{
        /**
         * Generated images as base64 encoded strings or binary data.
         * The images should be returned without any unnecessary conversion.
         * If the API returns base64 encoded strings, the images should be returned
         * as base64 encoded strings. If the API returns binary data, the images should
         * be returned as binary data.
         */
        images: Array<string> | Array<Uint8Array>;
        /**
         * Warnings for the call, e.g. unsupported settings.
         */
        warnings: Array<ImageModelV2CallWarning>;
        /**
         * Additional provider-specific metadata. They are passed through
         * from the provider to the AI SDK and enable provider-specific
         * results that can be fully encapsulated in the provider.
         *
         * The outer record is keyed by the provider name, and the inner
         * record is provider-specific metadata. It always includes an
         * `images` key with image-specific metadata
         *
         * ```ts
         * {
         * "openai": {
         * "images": ["revisedPrompt": "Revised prompt here."]
         * }
         * }
         * ```
         */
        providerMetadata?: ImageModelV2ProviderMetadata;
        /**
         * Response information for telemetry and debugging purposes.
         */
        response: {
            /**
             * Timestamp for the start of the generated response.
             */
            timestamp: Date;
            /**
             * The ID of the response model that was used to generate the response.
             */
            modelId: string;
            /**
             * Response headers.
             */
            headers: Record<string, string> | undefined;
        };
    }>;
};

declare type ImageModelV2CallOptions = {
    /**
     * Prompt for the image generation.
     */
    prompt: string;
    /**
     * Number of images to generate.
     */
    n: number;
    /**
     * Size of the images to generate.
     * Must have the format `{width}x{height}`.
     * `undefined` will use the provider's default size.
     */
    size: `${number}x${number}` | undefined;
    /**
     * Aspect ratio of the images to generate.
     * Must have the format `{width}:{height}`.
     * `undefined` will use the provider's default aspect ratio.
     */
    aspectRatio: `${number}:${number}` | undefined;
    /**
     * Seed for the image generation.
     * `undefined` will use the provider's default seed.
     */
    seed: number | undefined;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     * "openai": {
     * "style": "vivid"
     * }
     * }
     * ```
     */
    providerOptions: SharedV2ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

/**
 * Warning from the model provider for this call. The call will proceed, but e.g.
 * some settings might not be supported, which can lead to suboptimal results.
 */
declare type ImageModelV2CallWarning = {
    type: 'unsupported-setting';
    setting: keyof ImageModelV2CallOptions;
    details?: string;
} | {
    type: 'other';
    message: string;
};

declare type ImageModelV2ProviderMetadata = Record<string, {
    images: JSONArray_2;
} & JSONValue_3>;

/**
 * Image generation model specification version 3.
 */
declare type ImageModelV3 = {
    /**
     * The image model must specify which image model interface
     * version it implements. This will allow us to evolve the image
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v3';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many images can be generated in a single API call.
     * Can be set to a number for a fixed limit, to undefined to use
     * the global limit, or a function that returns a number or undefined,
     * optionally as a promise.
     */
    readonly maxImagesPerCall: number | undefined | GetMaxImagesPerCallFunction$1;
    /**
     * Generates an array of images.
     */
    doGenerate(options: ImageModelV3CallOptions): PromiseLike<{
        /**
         * Generated images as base64 encoded strings or binary data.
         * The images should be returned without any unnecessary conversion.
         * If the API returns base64 encoded strings, the images should be returned
         * as base64 encoded strings. If the API returns binary data, the images should
         * be returned as binary data.
         */
        images: Array<string> | Array<Uint8Array>;
        /**
         * Warnings for the call, e.g. unsupported features.
         */
        warnings: Array<SharedV3Warning>;
        /**
         * Additional provider-specific metadata. They are passed through
         * from the provider to the AI SDK and enable provider-specific
         * results that can be fully encapsulated in the provider.
         *
         * The outer record is keyed by the provider name, and the inner
         * record is provider-specific metadata. It always includes an
         * `images` key with image-specific metadata
         *
         * ```ts
         * {
         * "openai": {
         * "images": ["revisedPrompt": "Revised prompt here."]
         * }
         * }
         * ```
         */
        providerMetadata?: ImageModelV3ProviderMetadata;
        /**
         * Response information for telemetry and debugging purposes.
         */
        response: {
            /**
             * Timestamp for the start of the generated response.
             */
            timestamp: Date;
            /**
             * The ID of the response model that was used to generate the response.
             */
            modelId: string;
            /**
             * Response headers.
             */
            headers: Record<string, string> | undefined;
        };
        /**
         * Optional token usage for the image generation call (if the provider reports it).
         */
        usage?: ImageModelV3Usage;
    }>;
};

declare type ImageModelV3CallOptions = {
    /**
     * Prompt for the image generation. Some operations, like upscaling, may not require a prompt.
     */
    prompt: string | undefined;
    /**
     * Number of images to generate.
     */
    n: number;
    /**
     * Size of the images to generate.
     * Must have the format `{width}x{height}`.
     * `undefined` will use the provider's default size.
     */
    size: `${number}x${number}` | undefined;
    /**
     * Aspect ratio of the images to generate.
     * Must have the format `{width}:{height}`.
     * `undefined` will use the provider's default aspect ratio.
     */
    aspectRatio: `${number}:${number}` | undefined;
    /**
     * Seed for the image generation.
     * `undefined` will use the provider's default seed.
     */
    seed: number | undefined;
    /**
     * Array of images for image editing or variation generation.
     * The images should be provided as base64 encoded strings or binary data.
     */
    files: ImageModelV3File[] | undefined;
    /**
     * Mask image for inpainting operations.
     * The mask should be provided as base64 encoded strings or binary data.
     */
    mask: ImageModelV3File | undefined;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     *
     * ```ts
     * {
     *   "openai": {
     *     "style": "vivid"
     *   }
     * }
     * ```
     */
    providerOptions: SharedV3ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

/**
 * An image file that can be used for image editing or variation generation.
 */
declare type ImageModelV3File = {
    type: 'file';
    /**
     * The IANA media type of the file, e.g. `image/png`. Any string is supported.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Generated file data as base64 encoded strings or binary data.
     *
     * The file data should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the file data should be returned
     * as base64 encoded strings. If the API returns binary data, the file data should
     * be returned as binary data.
     */
    data: string | Uint8Array;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV3ProviderMetadata;
} | {
    type: 'url';
    /**
     * The URL of the image file.
     */
    url: string;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV3ProviderMetadata;
};

declare type ImageModelV3ProviderMetadata = Record<string, {
    images: JSONArray_2;
} & JSONValue_3>;

/**
 * Usage information for an image model call.
 */
declare type ImageModelV3Usage = {
    /**
     * The number of input (prompt) tokens used.
     */
    inputTokens: number | undefined;
    /**
     * The number of output tokens used, if reported by the provider.
     */
    outputTokens: number | undefined;
    /**
     * The total number of tokens as reported by the provider.
     */
    totalTokens: number | undefined;
};

/**
 * Image generation model specification version 4.
 */
declare type ImageModelV4 = {
    /**
     * The image model must specify which image model interface
     * version it implements. This will allow us to evolve the image
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many images can be generated in a single API call.
     * Can be set to a number for a fixed limit, to undefined to use
     * the global limit, or a function that returns a number or undefined,
     * optionally as a promise.
     */
    readonly maxImagesPerCall: number | undefined | GetMaxImagesPerCallFunction$2;
    /**
     * Generates an array of images.
     */
    doGenerate(options: ImageModelV4CallOptions): PromiseLike<ImageModelV4Result>;
};

/**
 * Image generation model specification version 4.
 */
declare type ImageModelV4_2 = {
    /**
     * The image model must specify which image model interface
     * version it implements. This will allow us to evolve the image
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many images can be generated in a single API call.
     * Can be set to a number for a fixed limit, to undefined to use
     * the global limit, or a function that returns a number or undefined,
     * optionally as a promise.
     */
    readonly maxImagesPerCall: number | undefined | GetMaxImagesPerCallFunction$2_2;
    /**
     * Generates an array of images.
     */
    doGenerate(options: ImageModelV4CallOptions_2): PromiseLike<ImageModelV4Result_2>;
};

declare type ImageModelV4CallOptions = {
    /**
     * Prompt for the image generation. Some operations, like upscaling, may not require a prompt.
     */
    prompt: string | undefined;
    /**
     * Number of images to generate.
     */
    n: number;
    /**
     * Size of the images to generate.
     * Must have the format `{width}x{height}`.
     * `undefined` will use the provider's default size.
     */
    size: `${number}x${number}` | undefined;
    /**
     * Aspect ratio of the images to generate.
     * Must have the format `{width}:{height}`.
     * `undefined` will use the provider's default aspect ratio.
     */
    aspectRatio: `${number}:${number}` | undefined;
    /**
     * Seed for the image generation.
     * `undefined` will use the provider's default seed.
     */
    seed: number | undefined;
    /**
     * Array of images for image editing or variation generation.
     * The images should be provided as base64 encoded strings or binary data.
     */
    files: ImageModelV4File[] | undefined;
    /**
     * Mask image for inpainting operations.
     * The mask should be provided as base64 encoded strings or binary data.
     */
    mask: ImageModelV4File | undefined;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     *
     * ```ts
     * {
     *   "openai": {
     *     "style": "vivid"
     *   }
     * }
     * ```
     */
    providerOptions: SharedV4ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

declare type ImageModelV4CallOptions_2 = {
    /**
     * Prompt for the image generation. Some operations, like upscaling, may not require a prompt.
     */
    prompt: string | undefined;
    /**
     * Number of images to generate.
     */
    n: number;
    /**
     * Size of the images to generate.
     * Must have the format `{width}x{height}`.
     * `undefined` will use the provider's default size.
     */
    size: `${number}x${number}` | undefined;
    /**
     * Aspect ratio of the images to generate.
     * Must have the format `{width}:{height}`.
     * `undefined` will use the provider's default aspect ratio.
     */
    aspectRatio: `${number}:${number}` | undefined;
    /**
     * Seed for the image generation.
     * `undefined` will use the provider's default seed.
     */
    seed: number | undefined;
    /**
     * Array of images for image editing or variation generation.
     * The images should be provided as base64 encoded strings or binary data.
     */
    files: ImageModelV4File_2[] | undefined;
    /**
     * Mask image for inpainting operations.
     * The mask should be provided as base64 encoded strings or binary data.
     */
    mask: ImageModelV4File_2 | undefined;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     *
     * ```ts
     * {
     *   "openai": {
     *     "style": "vivid"
     *   }
     * }
     * ```
     */
    providerOptions: SharedV4ProviderOptions_2;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

/**
 * An image file that can be used for image editing or variation generation.
 */
declare type ImageModelV4File = {
    type: 'file';
    /**
     * The IANA media type of the file, e.g. `image/png`. Any string is supported.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Generated file data as base64 encoded strings or binary data.
     *
     * The file data should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the file data should be returned
     * as base64 encoded strings. If the API returns binary data, the file data should
     * be returned as binary data.
     */
    data: string | Uint8Array;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV4ProviderMetadata;
} | {
    type: 'url';
    /**
     * The URL of the image file.
     */
    url: string;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV4ProviderMetadata;
};

/**
 * An image file that can be used for image editing or variation generation.
 */
declare type ImageModelV4File_2 = {
    type: 'file';
    /**
     * The IANA media type of the file, e.g. `image/png`. Any string is supported.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Generated file data as base64 encoded strings or binary data.
     *
     * The file data should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the file data should be returned
     * as base64 encoded strings. If the API returns binary data, the file data should
     * be returned as binary data.
     */
    data: string | Uint8Array;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV4ProviderMetadata_2;
} | {
    type: 'url';
    /**
     * The URL of the image file.
     */
    url: string;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV4ProviderMetadata_2;
};

/**
 * Middleware for ImageModelV4.
 * This type defines the structure for middleware that can be used to modify
 * the behavior of ImageModelV4 operations.
 */
declare type ImageModelV4Middleware = {
    /**
     * Middleware specification version. Use `v4` for the current version.
     */
    readonly specificationVersion: 'v4';
    /**
     * Override the provider name if desired.
     * @param options.model - The image model instance.
     */
    overrideProvider?: (options: {
        model: ImageModelV4_2;
    }) => string;
    /**
     * Override the model ID if desired.
     * @param options.model - The image model instance.
     */
    overrideModelId?: (options: {
        model: ImageModelV4_2;
    }) => string;
    /**
     * Override the limit of how many images can be generated in a single API call if desired.
     * @param options.model - The image model instance.
     */
    overrideMaxImagesPerCall?: (options: {
        model: ImageModelV4_2;
    }) => ImageModelV4_2['maxImagesPerCall'];
    /**
     * Transforms the parameters before they are passed to the image model.
     * @param options - Object containing the parameters.
     * @param options.params - The original parameters for the image model call.
     * @returns A promise that resolves to the transformed parameters.
     */
    transformParams?: (options: {
        params: ImageModelV4CallOptions_2;
        model: ImageModelV4_2;
    }) => PromiseLike<ImageModelV4CallOptions_2>;
    /**
     * Wraps the generate operation of the image model.
     *
     * @param options - Object containing the generate function, parameters, and model.
     * @param options.doGenerate - The original generate function.
     * @param options.params - The parameters for the generate call. If the
     * `transformParams` middleware is used, this will be the transformed parameters.
     * @param options.model - The image model instance.
     * @returns A promise that resolves to the result of the generate operation.
     */
    wrapGenerate?: (options: {
        doGenerate: () => ReturnType<ImageModelV4_2['doGenerate']>;
        params: ImageModelV4CallOptions_2;
        model: ImageModelV4_2;
    }) => Promise<Awaited<ReturnType<ImageModelV4_2['doGenerate']>>>;
};

declare type ImageModelV4ProviderMetadata = Record<string, {
    images: JSONArray;
} & JSONValue_2>;

declare type ImageModelV4ProviderMetadata_2 = Record<string, {
    images: JSONArray_2;
} & JSONValue_3>;

/**
 * The result of an image model doGenerate call.
 */
declare type ImageModelV4Result = {
    /**
     * Generated images as base64 encoded strings or binary data.
     * The images should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the images should be returned
     * as base64 encoded strings. If the API returns binary data, the images should
     * be returned as binary data.
     */
    images: Array<string> | Array<Uint8Array>;
    /**
     * Warnings for the call, e.g. unsupported features.
     */
    warnings: Array<SharedV4Warning>;
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is provider-specific metadata. It always includes an
     * `images` key with image-specific metadata
     *
     * ```ts
     * {
     * "openai": {
     * "images": ["revisedPrompt": "Revised prompt here."]
     * }
     * }
     * ```
     */
    providerMetadata?: ImageModelV4ProviderMetadata;
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        /**
         * Timestamp for the start of the generated response.
         */
        timestamp: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId: string;
        /**
         * Response headers.
         */
        headers: Record<string, string> | undefined;
    };
    /**
     * Optional token usage for the image generation call (if the provider reports it).
     */
    usage?: ImageModelV4Usage;
};

/**
 * The result of an image model doGenerate call.
 */
declare type ImageModelV4Result_2 = {
    /**
     * Generated images as base64 encoded strings or binary data.
     * The images should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the images should be returned
     * as base64 encoded strings. If the API returns binary data, the images should
     * be returned as binary data.
     */
    images: Array<string> | Array<Uint8Array>;
    /**
     * Warnings for the call, e.g. unsupported features.
     */
    warnings: Array<SharedV4Warning_2>;
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is provider-specific metadata. It always includes an
     * `images` key with image-specific metadata
     *
     * ```ts
     * {
     * "openai": {
     * "images": ["revisedPrompt": "Revised prompt here."]
     * }
     * }
     * ```
     */
    providerMetadata?: ImageModelV4ProviderMetadata_2;
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        /**
         * Timestamp for the start of the generated response.
         */
        timestamp: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId: string;
        /**
         * Response headers.
         */
        headers: Record<string, string> | undefined;
    };
    /**
     * Optional token usage for the image generation call (if the provider reports it).
     */
    usage?: ImageModelV4Usage_2;
};

/**
 * Usage information for an image model call.
 */
declare type ImageModelV4Usage = {
    /**
     * The number of input (prompt) tokens used.
     */
    inputTokens: number | undefined;
    /**
     * The number of output tokens used, if reported by the provider.
     */
    outputTokens: number | undefined;
    /**
     * The total number of tokens as reported by the provider.
     */
    totalTokens: number | undefined;
};

/**
 * Usage information for an image model call.
 */
declare type ImageModelV4Usage_2 = {
    /**
     * The number of input (prompt) tokens used.
     */
    inputTokens: number | undefined;
    /**
     * The number of output tokens used, if reported by the provider.
     */
    outputTokens: number | undefined;
    /**
     * The total number of tokens as reported by the provider.
     */
    totalTokens: number | undefined;
};

/**
 * Image content part of a prompt. It contains an image.
 *
 * @deprecated Use `FilePart` with `mediaType: 'image'` instead:
 * `{ type: 'file', mediaType: 'image', data: { type: 'data', data } }`.
 */
export declare interface ImagePart {
    type: 'image';
    /**
     * Image data. Can either be:
     *
     * - data: a base64-encoded string, a Uint8Array, an ArrayBuffer, or a Buffer
     * - URL: a URL that points to the image
     * - ProviderReference: a provider reference from `uploadFile`
     */
    image: DataContent | URL | ProviderReference_3;
    /**
     * Optional IANA media type of the image.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType?: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
}

/**
 * Image content part of a prompt. It contains an image.
 *
 * @deprecated Use `FilePart` with `mediaType: 'image'` instead:
 * `{ type: 'file', mediaType: 'image', data: { type: 'data', data } }`.
 */
declare interface ImagePart_2 {
    type: 'image';
    /**
     * Image data. Can either be:
     *
     * - data: a base64-encoded string, a Uint8Array, an ArrayBuffer, or a Buffer
     * - URL: a URL that points to the image
     * - ProviderReference: a provider reference from `uploadFile`
     */
    image: DataContent_2 | URL | ProviderReference_2;
    /**
     * Optional IANA media type of the image.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType?: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
}

declare type IncludedContext<CONTEXT extends Context_2 | unknown | never> = {
    [KEY in keyof NoInfer<CONTEXT>]?: boolean;
} | undefined;

declare type IncludedToolsContext<TOOLS extends ToolSet> = {
    [TOOL_NAME in keyof NoInfer<InferToolSetContext_2<TOOLS>>]?: IncludedContext<NoInfer<InferToolSetContext_2<TOOLS>[TOOL_NAME]>>;
} | undefined;

/**
 * Infer the type of the tools of an agent.
 */
declare type InferAgentTools<AGENT> = AGENT extends Agent<any, infer TOOLS, any> ? TOOLS : never;

/**
 * Infer the UI message type of an agent.
 */
declare type InferAgentUIMessage<AGENT, MESSAGE_METADATA = unknown> = UIMessage<MESSAGE_METADATA, never, InferUITools<InferAgentTools<AGENT>>>;
export { InferAgentUIMessage as Experimental_InferAgentUIMessage }
export { InferAgentUIMessage }

/**
 * Infers the element type from an array output specification.
 */
declare type InferElementOutput<OUTPUT extends Output_2> = OUTPUT extends Output_2<any, any, infer ELEMENT> ? ELEMENT : never;

/**
 * Infers the complete output type from the output specification.
 */
export declare type InferGenerateOutput<OUTPUT extends Output_2> = OUTPUT extends Output_2<infer COMPLETE_OUTPUT, any, any> ? COMPLETE_OUTPUT : never;

export declare type InferSchema<SCHEMA> = SCHEMA extends ZodSchema_2<infer T> ? T : SCHEMA extends StandardSchema_2<infer T> ? T : SCHEMA extends LazySchema_2<infer T> ? T : SCHEMA extends Schema<infer T> ? T : never;

declare type InferSchema_2<SCHEMA> = SCHEMA extends ZodSchema<infer T> ? T : SCHEMA extends StandardSchema<infer T> ? T : SCHEMA extends LazySchema<infer T> ? T : SCHEMA extends Schema_2<infer T> ? T : never;

/**
 * Infers the partial output type from the output specification.
 */
export declare type InferStreamOutput<OUTPUT extends Output_2> = OUTPUT extends Output_2<any, infer PARTIAL_OUTPUT, any> ? PARTIAL_OUTPUT : never;

export declare type InferTelemetryEvent<EVENT> = EVENT & Omit<TelemetryOptions, 'integrations' | 'isEnabled' | 'includeRuntimeContext'>;

/**
 * Infer the context type of a tool.
 */
declare type InferToolContext<TOOL extends Tool_2> = TOOL extends Tool_2<any, any, infer CONTEXT> ? IsUntypedContext<CONTEXT> extends true ? never : CONTEXT : never;

/**
 * Infer the context type of a tool.
 */
declare type InferToolContext_2<TOOL extends Tool> = TOOL extends Tool<any, any, infer CONTEXT> ? IsUntypedContext_2<CONTEXT> extends true ? never : CONTEXT : never;

/**
 * Infer the input type of a tool.
 */
export declare type InferToolInput<TOOL extends Tool<any, any, any>> = TOOL extends Tool<infer INPUT, any, any> ? INPUT : never;

/**
 * Infer the input type of a tool.
 */
declare type InferToolInput_2<TOOL extends Tool_2<any, any, any>> = TOOL extends Tool_2<infer INPUT, any, any> ? INPUT : never;

/**
 * Infer the output type of a tool.
 */
export declare type InferToolOutput<TOOL extends Tool<any, any, any>> = TOOL extends Tool<any, infer OUTPUT, any> ? OUTPUT : never;

/**
 * Infer the output type of a tool.
 */
declare type InferToolOutput_2<TOOL extends Tool_2<any, any, any>> = TOOL extends Tool_2<any, infer OUTPUT, any> ? OUTPUT : never;

/**
 * Infer the context type for a tool set.
 *
 * The inferred type maps each contextual tool name to its context type.
 *
 * Tools without concrete context are omitted. Tool contexts that include
 * `undefined` are represented as optional properties.
 */
declare type InferToolSetContext<TOOLS extends ToolSet_2> = Normalize<RequiredToolSetContext<TOOLS> & OptionalToolSetContext<TOOLS>>;

/**
 * Infer the context type for a tool set.
 *
 * The inferred type maps each contextual tool name to its context type.
 *
 * Tools without concrete context are omitted. Tool contexts that include
 * `undefined` are represented as optional properties.
 */
declare type InferToolSetContext_2<TOOLS extends ToolSet> = Normalize_2<RequiredToolSetContext_2<TOOLS> & OptionalToolSetContext_2<TOOLS>>;

export declare type InferUIDataParts<T extends UIDataPartSchemas> = {
    [K in keyof T]: InferSchema<T[K]>;
};

export declare type InferUIMessageChunk<T extends UIMessage> = UIMessageChunk<InferUIMessageMetadata<T>, InferUIMessageData<T>>;

declare type InferUIMessageData<T extends UIMessage> = T extends UIMessage<unknown, infer DATA_TYPES> ? DATA_TYPES : UIDataTypes;

declare type InferUIMessageMetadata<T extends UIMessage> = T extends UIMessage<infer METADATA> ? METADATA : unknown;

declare type InferUIMessageToolCall<UI_MESSAGE extends UIMessage> = ValueOf<{
    [NAME in keyof InferUIMessageTools<UI_MESSAGE>]: ToolCall_2<NAME & string, InferUIMessageTools<UI_MESSAGE>[NAME] extends {
        input: infer INPUT;
    } ? INPUT : never> & {
        dynamic?: false;
    };
}> | (ToolCall_2<string, unknown> & {
    dynamic: true;
});

declare type InferUIMessageTools<T extends UIMessage> = T extends UIMessage<unknown, UIDataTypes, infer TOOLS> ? TOOLS : UITools;

/**
 * Infer the input and output types of a tool so it can be used as a UI tool.
 */
export declare type InferUITool<TOOL extends Tool> = {
    input: InferToolInput<TOOL>;
    output: InferToolOutput<TOOL>;
};

/**
 * Infer the input and output types of a tool set so it can be used as a UI tool set.
 */
export declare type InferUITools<TOOLS extends ToolSet> = {
    [NAME in keyof TOOLS & string]: InferUITool<TOOLS[NAME]>;
};

declare function injectJsonInstructionIntoMessages({ messages, schema, schemaPrefix, schemaSuffix, }: {
    messages: LanguageModelV4Prompt;
    schema?: JSONSchema7;
    schemaPrefix?: string;
    schemaSuffix?: string;
}): LanguageModelV4Prompt;

declare function injectJsonInstructionIntoMessages_2({ messages, schema, schemaPrefix, schemaSuffix, }: {
    messages: LanguageModelV4Prompt;
    schema?: JSONSchema7;
    schemaPrefix?: string;
    schemaSuffix?: string;
}): LanguageModelV4Prompt;

declare type InlineFileData = Extract<FilePart_2['data'], {
    type: 'data';
} | {
    type: 'text';
}>;

declare type InlineFileData_2 = Extract<FilePart['data'], {
    type: 'data';
} | {
    type: 'text';
}>;

/**
 * Instructions to include in the prompt. Can be used with `prompt` or `messages`.
 */
export declare type Instructions = string | SystemModelMessage | Array<SystemModelMessage>;

export declare class InvalidArgumentError extends AISDKError {
    private readonly [symbol$j];
    readonly parameter: string;
    readonly value: unknown;
    constructor({ parameter, value, message, }: {
        parameter: string;
        value: unknown;
        message: string;
    });
    static isInstance(error: unknown): error is InvalidArgumentError;
}

export declare class InvalidDataContentError extends AISDKError {
    private readonly [symbol$4_2];
    readonly content: unknown;
    constructor({ content, cause, message, }: {
        content: unknown;
        cause?: unknown;
        message?: string;
    });
    static isInstance(error: unknown): error is InvalidDataContentError;
}

export declare class InvalidMessageRoleError extends AISDKError {
    private readonly [symbol$3_2];
    readonly role: string;
    constructor({ role, message, }: {
        role: string;
        message?: string;
    });
    static isInstance(error: unknown): error is InvalidMessageRoleError;
}

/**
 * A prompt is invalid. This error should be thrown by providers when they cannot
 * process a prompt.
 */
export declare class InvalidPromptError extends AISDKError {
    private readonly [symbol$a];
    readonly prompt: unknown;
    constructor({ prompt, message, cause, }: {
        prompt: unknown;
        message: string;
        cause?: unknown;
    });
    static isInstance(error: unknown): error is InvalidPromptError;
}

/**
 * Server returned a response with invalid data content.
 * This should be thrown by providers when they cannot parse the response from the API.
 */
export declare class InvalidResponseDataError extends AISDKError {
    private readonly [symbol$9];
    readonly data: unknown;
    constructor({ data, message, }: {
        data: unknown;
        message?: string;
    });
    static isInstance(error: unknown): error is InvalidResponseDataError;
}

export declare class InvalidStreamPartError extends AISDKError {
    private readonly [symbol$i];
    readonly chunk: Experimental_LanguageModelStreamPart<any>;
    constructor({ chunk, message, }: {
        chunk: Experimental_LanguageModelStreamPart<any>;
        message: string;
    });
    static isInstance(error: unknown): error is InvalidStreamPartError;
}

export declare class InvalidToolApprovalError extends AISDKError {
    private readonly [symbol$h];
    readonly approvalId: string;
    constructor({ approvalId }: {
        approvalId: string;
    });
    static isInstance(error: unknown): error is InvalidToolApprovalError;
}

export declare class InvalidToolApprovalSignatureError extends AISDKError {
    private readonly [symbol$g];
    readonly approvalId: string;
    readonly toolCallId: string;
    constructor({ approvalId, toolCallId, reason, }: {
        approvalId: string;
        toolCallId: string;
        reason: string;
    });
    static isInstance(error: unknown): error is InvalidToolApprovalSignatureError;
}

export declare class InvalidToolInputError extends AISDKError {
    private readonly [symbol$l];
    readonly toolName: string;
    readonly toolInput: string;
    constructor({ toolInput, toolName, cause, message, }: {
        message?: string;
        toolInput: string;
        toolName: string;
        cause: unknown;
    });
    static isInstance(error: unknown): error is InvalidToolInputError;
}

declare function isAbortError(error: unknown): error is Error;

declare function isAbortError_2(error: unknown): error is Error;

/**
 * Detects the `any` type so untyped tools can be treated as having no explicit
 * context type.
 */
declare type IsAny<T> = 0 extends 1 & T ? true : false;

/**
 * Detects the `any` type so untyped tools can be treated as having no explicit
 * context type.
 */
declare type IsAny_2<T> = 0 extends 1 & T ? true : false;

/**
 * Returns `true` when running in a browser.
 *
 * Detection keys on the presence of a global `window`, matching the browser
 * check used elsewhere in this package (see `getRuntimeEnvironmentUserAgent`)
 * so the SDK has a single, consistent definition of "browser". Server runtimes
 * (Node.js, Deno, Bun, edge/workers) do not define `window`.
 */
declare function isBrowserRuntime(globalThisAny?: any): boolean;

/**
 * Returns `true` when running in a browser.
 *
 * Detection keys on the presence of a global `window`, matching the browser
 * check used elsewhere in this package (see `getRuntimeEnvironmentUserAgent`)
 * so the SDK has a single, consistent definition of "browser". Server runtimes
 * (Node.js, Deno, Bun, edge/workers) do not define `window`.
 */
declare function isBrowserRuntime_2(globalThisAny?: any): boolean;

/**
 * Type-guard for Node.js `Buffer` instances.
 *
 * Uses optional chaining on `globalThis.Buffer` so it returns `false` in
 * runtimes where `Buffer` is not available (e.g. CloudFlare Workers).
 */
declare function isBuffer(value: unknown): value is Buffer;

/**
 * Type-guard for Node.js `Buffer` instances.
 *
 * Uses optional chaining on `globalThis.Buffer` so it returns `false` in
 * runtimes where `Buffer` is not available (e.g. CloudFlare Workers).
 */
declare function isBuffer_2(value: unknown): value is Buffer;

/**
 * Type guard to check if a message part is a custom part.
 */
export declare function isCustomContentUIPart(part: UIMessagePart<UIDataTypes, UITools>): part is CustomContentUIPart;

declare function isCustomReasoning(reasoning: LanguageModelV4CallOptions['reasoning']): reasoning is Exclude<LanguageModelV4CallOptions['reasoning'], 'provider-default' | undefined>;

declare function isCustomReasoning_2(reasoning: LanguageModelV4CallOptions['reasoning']): reasoning is Exclude<LanguageModelV4CallOptions['reasoning'], 'provider-default' | undefined>;

/**
 * Check if a message part is a data part.
 */
export declare function isDataUIPart<DATA_TYPES extends UIDataTypes>(part: UIMessagePart<DATA_TYPES, UITools>): part is DataUIPart<DATA_TYPES>;

/**
 * Performs a deep-equal comparison of two parsed JSON objects.
 *
 * @param {any} obj1 - The first object to compare.
 * @param {any} obj2 - The second object to compare.
 * @returns {boolean} - Returns true if the two objects are deeply equal, false otherwise.
 */
export declare function isDeepEqualData(obj1: any, obj2: any): boolean;

/**
 * Check if a message part is a dynamic tool part.
 *
 * Dynamic tools are tools for which the input and output types are unknown.
 */
export declare function isDynamicToolUIPart(part: UIMessagePart<UIDataTypes, UITools>): part is DynamicToolUIPart;

/**
 * Detects exact empty object contexts, including `{}` combined with
 * `undefined`, which do not provide tool-specific context properties.
 */
declare type IsEmptyObject<T> = keyof NonNullable<T> extends never ? true : false;

/**
 * Detects exact empty object contexts, including `{}` combined with
 * `undefined`, which do not provide tool-specific context properties.
 */
declare type IsEmptyObject_2<T> = keyof NonNullable<T> extends never ? true : false;

/**
 * Checks whether a tool context map contains any contextual tool entries.
 */
declare type IsEmptyObject_3<OBJECT> = keyof OBJECT extends never ? true : false;

/**
 * Checks whether a tool exposes an execute function.
 */
declare function isExecutableTool<TOOL extends Tool_2>(tool: TOOL | undefined): tool is ExecutableTool<TOOL>;

/**
 * Checks whether a tool exposes an execute function.
 */
declare function isExecutableTool_2<TOOL extends Tool>(tool: TOOL | undefined): tool is ExecutableTool_2<TOOL>;

/**
 * Type guard to check if a message part is a file part.
 */
export declare function isFileUIPart(part: UIMessagePart<UIDataTypes, UITools>): part is FileUIPart;

/**
 * Returns `true` only when the given media type has a non-empty, non-wildcard
 * subtype (i.e. matches the form `type/subtype`, and `subtype` is not `*`).
 *
 * Examples:
 *   - `"image/png"` -> `true`
 *   - `"image/*"` -> `false`
 *   - `"image"` -> `false`
 *   - `"image/"` -> `false`
 *   - `""` -> `false`
 *   - `"/"` -> `false`
 */
declare function isFullMediaType(mediaType: string): boolean;

/**
 * Returns `true` only when the given media type has a non-empty, non-wildcard
 * subtype (i.e. matches the form `type/subtype`, and `subtype` is not `*`).
 *
 * Examples:
 *   - `"image/png"` -> `true`
 *   - `"image/*"` -> `false`
 *   - `"image"` -> `false`
 *   - `"image/"` -> `false`
 *   - `""` -> `false`
 *   - `"/"` -> `false`
 */
declare function isFullMediaType_2(mediaType: string): boolean;

/**
 * Creates a stop condition that never returns `true`.
 *
 * This lets the tool-calling loop continue until it reaches one of its
 * natural termination conditions.
 */
export declare function isLoopFinished(): StopCondition<any, any>;

/**
 * Type guard that checks whether a value is not `null` or `undefined`.
 *
 * @template T - The type of the value to check.
 * @param value - The value to check.
 * @returns `true` if the value is neither `null` nor `undefined`, otherwise `false`.
 */
declare function isNonNullable<T>(value: T | undefined | null): value is NonNullable<T>;

/**
 * Type guard that checks whether a value is not `null` or `undefined`.
 *
 * @template T - The type of the value to check.
 * @param value - The value to check.
 * @returns `true` if the value is neither `null` nor `undefined`, otherwise `false`.
 */
declare function isNonNullable_2<T>(value: T | undefined | null): value is NonNullable<T>;

declare function isParsableJson(input: string): boolean;

declare function isParsableJson_2(input: string): boolean;

/**
 * Checks whether a value is a provider reference (a mapping of provider names
 * to provider-specific identifiers) as opposed to raw bytes, a URL, or a
 * tagged `{ type: ... }` object.
 */
declare function isProviderReference(data: unknown): data is SharedV4ProviderReference_2;

/**
 * Checks whether a value is a provider reference (a mapping of provider names
 * to provider-specific identifiers) as opposed to raw bytes, a URL, or a
 * tagged `{ type: ... }` object.
 */
declare function isProviderReference_2(data: unknown): data is SharedV4ProviderReference_2;

/**
 * Type guard to check if a message part is a reasoning file part.
 */
export declare function isReasoningFileUIPart(part: UIMessagePart<UIDataTypes, UITools>): part is ReasoningFileUIPart;

/**
 * Type guard to check if a message part is a reasoning part.
 */
export declare function isReasoningUIPart(part: UIMessagePart<UIDataTypes, UITools>): part is ReasoningUIPart;

/**
 * Checks whether a value is a non-null, non-array object.
 */
declare function isRecord(value: unknown): value is Record<string, unknown>;

/**
 * Returns true when `url` has the same origin (scheme + host + port) as
 * `baseUrl`.
 *
 * Used to decide whether provider credentials may be attached to a request to a
 * URL taken from a provider response (e.g. a polling or media-download URL).
 * Credentials must only be sent to the provider's own origin; a response that
 * names a foreign host (a CDN, or an attacker-controlled host if the response
 * is tampered with) must not receive the API key.
 *
 * Returns false if either value is not a valid absolute URL (fail-closed).
 */
declare function isSameOrigin(url: string, baseUrl: string): boolean;

/**
 * Returns true when `url` has the same origin (scheme + host + port) as
 * `baseUrl`.
 *
 * Used to decide whether provider credentials may be attached to a request to a
 * URL taken from a provider response (e.g. a polling or media-download URL).
 * Credentials must only be sent to the provider's own origin; a response that
 * names a foreign host (a CDN, or an attacker-controlled host if the response
 * is tampered with) must not receive the API key.
 *
 * Returns false if either value is not a valid absolute URL (fail-closed).
 */
declare function isSameOrigin_2(url: string, baseUrl: string): boolean;

/**
 * Check if a message part is a static tool part.
 *
 * Static tools are tools for which the types are known at development time.
 */
export declare function isStaticToolUIPart<TOOLS extends UITools>(part: UIMessagePart<UIDataTypes, TOOLS>): part is ToolUIPart<TOOLS>;

/**
 * Creates a stop condition that returns `true` when the number of completed
 * steps equals `stepCount`.
 *
 * @param stepCount - The number of steps to allow before stopping.
 */
declare function isStepCount(stepCount: number): StopCondition<any, any>;
export { isStepCount }
export { isStepCount as stepCountIs }

/**
 * Type guard to check if a message part is a text part.
 */
export declare function isTextUIPart(part: UIMessagePart<UIDataTypes, UITools>): part is TextUIPart;

/**
 * Check if a message part is a tool part.
 *
 * Tool parts are either static or dynamic tools.
 *
 * Use `isStaticToolUIPart` or `isDynamicToolUIPart` to check the type of the tool.
 */
export declare function isToolUIPart<TOOLS extends UITools>(part: UIMessagePart<UIDataTypes, TOOLS>): part is ToolUIPart<TOOLS> | DynamicToolUIPart;

/**
 * Detects context types that come from omitted or broad context declarations
 * rather than a concrete tool context schema.
 */
declare type IsUntypedContext<CONTEXT> = IsAny<CONTEXT> extends true ? true : unknown extends CONTEXT ? true : IsEmptyObject<CONTEXT> extends true ? true : string extends keyof CONTEXT ? CONTEXT extends Context ? true : false : false;

/**
 * Detects context types that come from omitted or broad context declarations
 * rather than a concrete tool context schema.
 */
declare type IsUntypedContext_2<CONTEXT> = IsAny_2<CONTEXT> extends true ? true : unknown extends CONTEXT ? true : IsEmptyObject_2<CONTEXT> extends true ? true : string extends keyof CONTEXT ? CONTEXT extends Context_2 ? true : false : false;

/**
 * Checks if the given URL is supported natively by the model.
 *
 * @param mediaType - The media type of the URL. Case-sensitive. May be a full
 *                    `type/subtype`, a wildcard `type/*`, or just the
 *                    top-level segment (e.g. `image`).
 * @param url - The URL to check.
 * @param supportedUrls - A record where keys are case-sensitive media types (or '*')
 *                        and values are arrays of RegExp patterns for URLs.
 *
 * @returns `true` if the URL matches a pattern under the specific media type
 *          or the wildcard '*', `false` otherwise.
 */
declare function isUrlSupported({ mediaType, url, supportedUrls, }: {
    mediaType: string;
    url: string;
    supportedUrls: Record<string, RegExp[]>;
}): boolean;

/**
 * Checks if the given URL is supported natively by the model.
 *
 * @param mediaType - The media type of the URL. Case-sensitive. May be a full
 *                    `type/subtype`, a wildcard `type/*`, or just the
 *                    top-level segment (e.g. `image`).
 * @param url - The URL to check.
 * @param supportedUrls - A record where keys are case-sensitive media types (or '*')
 *                        and values are arrays of RegExp patterns for URLs.
 *
 * @returns `true` if the URL matches a pattern under the specific media type
 *          or the wildcard '*', `false` otherwise.
 */
declare function isUrlSupported_2({ mediaType, url, supportedUrls, }: {
    mediaType: string;
    url: string;
    supportedUrls: Record<string, RegExp[]>;
}): boolean;

declare type Job = () => Promise<void>;

declare type JSONArray = JSONValue_2[];

declare type JSONArray_2 = JSONValue_3[];

declare type JSONObject = {
    [key: string]: JSONValue_2 | undefined;
};

declare type JSONObject_2 = {
    [key: string]: JSONValue_3 | undefined;
};

export declare class JSONParseError extends AISDKError {
    private readonly [symbol$8];
    readonly text: string;
    constructor({ text, cause }: {
        text: string;
        cause: unknown;
    });
    static isInstance(error: unknown): error is JSONParseError;
}

declare class JSONParseError_2 extends AISDKError_2 {
    private readonly [symbol$8_2];
    readonly text: string;
    constructor({ text, cause }: {
        text: string;
        cause: unknown;
    });
    static isInstance(error: unknown): error is JSONParseError_2;
}

/**
 * Create a schema using a JSON Schema.
 *
 * @param jsonSchema The JSON Schema for the schema.
 * @param options.validate Optional. A validation function for the schema.
 */
export declare function jsonSchema<OBJECT = unknown>(jsonSchema: JSONSchema7 | PromiseLike<JSONSchema7> | (() => JSONSchema7 | PromiseLike<JSONSchema7>), { validate, }?: {
    validate?: (value: unknown) => ValidationResult_2<OBJECT> | PromiseLike<ValidationResult_2<OBJECT>>;
}): Schema<OBJECT>;

export { JSONSchema7 }

/**
 * Create a schema using a JSON Schema.
 *
 * @param jsonSchema The JSON Schema for the schema.
 * @param options.validate Optional. A validation function for the schema.
 */
declare function jsonSchema_2<OBJECT = unknown>(jsonSchema: JSONSchema7 | PromiseLike<JSONSchema7> | (() => JSONSchema7 | PromiseLike<JSONSchema7>), { validate, }?: {
    validate?: (value: unknown) => ValidationResult<OBJECT> | PromiseLike<ValidationResult<OBJECT>>;
}): Schema_2<OBJECT>;

/**
 * A TransformStream that converts JSON objects to Server-Sent Events (SSE) format.
 * Each object is serialized to JSON and wrapped in `data: ...\n\n` format.
 * When the stream ends, a `data: [DONE]\n\n` message is sent.
 */
export declare class JsonToSseTransformStream extends TransformStream<unknown, string> {
    constructor();
}

export declare type JSONValue = JSONValue_3;

/**
 * A JSON value can be a string, number, boolean, object, array, or null.
 * JSON values can be serialized and deserialized by the JSON.stringify and JSON.parse methods.
 */
declare type JSONValue_2 = null | string | number | boolean | JSONObject | JSONArray;

/**
 * A JSON value can be a string, number, boolean, object, array, or null.
 * JSON values can be serialized and deserialized by the JSON.stringify and JSON.parse methods.
 */
declare type JSONValue_3 = null | string | number | boolean | JSONObject_2 | JSONArray_2;

declare const KNOWN_MODEL_TYPES: readonly ["embedding", "image", "language", "realtime", "reranking", "speech", "transcription", "video"];

declare type KnownModelType = (typeof KNOWN_MODEL_TYPES)[number];

/**
 * Language model that is used by the AI SDK.
 */
export declare type LanguageModel = GlobalProviderModelId | LanguageModelV4_2 | LanguageModelV3 | LanguageModelV2;

/**
 * Event passed to the `onLanguageModelCallEnd` callback.
 *
 * Called after the model response has been normalized and parsed, but before
 * any client-side tool execution begins.
 */
export declare type LanguageModelCallEndEvent<TOOLS extends ToolSet = ToolSet> = ModelInfo & {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /** The unified reason why the model call finished. */
    readonly finishReason: FinishReason;
    /** The token usage reported by the model call. */
    readonly usage: LanguageModelUsage;
    /** The content parts produced by the model call. */
    readonly content: ReadonlyArray<ContentPart<TOOLS>>;
    /** The provider-returned response id for this model call. */
    readonly responseId: string;
    /** Performance metrics for the model call. */
    readonly performance: {
        /** Time spent waiting for the language model response in milliseconds. */
        readonly responseTimeMs: number;
        /**
         * Effective number of output tokens per second over the full language
         * model response.
         */
        readonly effectiveOutputTokensPerSecond: number;
        /**
         * Number of output tokens per second after the first generated output
         * chunk was received.
         *
         * Only available for streaming calls.
         */
        readonly outputTokensPerSecond: number | undefined;
        /**
         * Number of input tokens processed per second before the first generated
         * output chunk was received.
         *
         * Only available for streaming calls.
         */
        readonly inputTokensPerSecond: number | undefined;
        /**
         * Effective number of input and output tokens per second over the full
         * language model response.
         */
        readonly effectiveTotalTokensPerSecond: number;
        /**
         * Time until the first generated output chunk was received in
         * milliseconds.
         */
        readonly timeToFirstOutputMs: number | undefined;
        /**
         * Timing statistics for the gaps between generated output chunks in
         * milliseconds.
         *
         * Only available for streaming calls with at least two output chunks.
         */
        readonly timeBetweenOutputChunksMs?: OutputChunkTimingStats;
    };
};

/**
 * Model-facing generation controls. These settings influence how the model
 * generates its response (token limits, sampling, penalties, stop sequences,
 * seed, reasoning).
 */
export declare type LanguageModelCallOptions = {
    /**
     * Maximum number of tokens to generate.
     */
    maxOutputTokens?: number;
    /**
     * Temperature setting. The range depends on the provider and model.
     *
     * It is recommended to set either `temperature` or `topP`, but not both.
     */
    temperature?: number;
    /**
     * Nucleus sampling. This is a number between 0 and 1.
     *
     * E.g. 0.1 would mean that only tokens with the top 10% probability mass
     * are considered.
     *
     * It is recommended to set either `temperature` or `topP`, but not both.
     */
    topP?: number;
    /**
     * Only sample from the top K options for each subsequent token.
     *
     * Used to remove "long tail" low probability responses.
     * Recommended for advanced use cases only. You usually only need to use temperature.
     */
    topK?: number;
    /**
     * Presence penalty setting. It affects the likelihood of the model to
     * repeat information that is already in the prompt.
     *
     * The presence penalty is a number between -1 (increase repetition)
     * and 1 (maximum penalty, decrease repetition). 0 means no penalty.
     */
    presencePenalty?: number;
    /**
     * Frequency penalty setting. It affects the likelihood of the model
     * to repeatedly use the same words or phrases.
     *
     * The frequency penalty is a number between -1 (increase repetition)
     * and 1 (maximum penalty, decrease repetition). 0 means no penalty.
     */
    frequencyPenalty?: number;
    /**
     * Stop sequences.
     * If set, the model will stop generating text when one of the stop sequences is generated.
     * Providers may have limits on the number of stop sequences.
     */
    stopSequences?: string[];
    /**
     * The seed (integer) to use for random sampling. If set and supported
     * by the model, calls will generate deterministic results.
     */
    seed?: number;
    /**
     * Reasoning effort level for the model. Controls how much reasoning
     * the model performs before generating a response.
     *
     * Use `'provider-default'` to use the provider's default reasoning level.
     * Use `'none'` to disable reasoning (if supported by the provider).
     */
    reasoning?: LanguageModelV4CallOptions_2['reasoning'];
};

/**
 * Event passed to the `onLanguageModelCallStart` callback.
 *
 * Called immediately before the provider model call begins.
 * Unlike `onStepStart`, this only represents model invocation work.
 */
export declare type LanguageModelCallStartEvent = ModelInfo & {
    /** Unique identifier for this generation call, used to correlate events. */
    readonly callId: string;
    /** Prepared tool definitions for the model call, if any. */
    readonly tools: ReadonlyArray<Record<string, unknown>> | undefined;
} & StandardizedPrompt & LanguageModelCallOptions;

/**
 * Middleware for language models.
 * Accepts both V3 and V4 middleware types for backward compatibility.
 *
 * Uses LanguageModelV4Middleware as the base but relaxes specificationVersion
 * to accept any string (including 'v3') and makes it optional.
 */
export declare type LanguageModelMiddleware = Omit<LanguageModelV4Middleware, 'specificationVersion'> & {
    readonly specificationVersion?: string;
};

/**
 * Metadata for a language model request.
 */
export declare type LanguageModelRequestMetadata = {
    /**
     * The input messages that were sent to the model for this step.
     */
    readonly messages?: Array<ModelMessage>;
    /**
     * Request HTTP body that was sent to the provider API.
     */
    readonly body?: unknown;
};

/**
 * Metadata for a language model response.
 */
export declare type LanguageModelResponseMetadata = {
    /**
     * The response messages that were generated during the call.
     * Response messages can be either assistant messages or tool messages.
     * They contain a generated id.
     */
    readonly messages: Array<ResponseMessage>;
    /**
     * ID for the generated response.
     */
    readonly id: string;
    /**
     * Timestamp for the start of the generated response.
     */
    readonly timestamp: Date;
    /**
     * The ID of the response model that was used to generate the response.
     */
    readonly modelId: string;
    /**
     * Response headers (available only for providers that use HTTP requests).
     */
    readonly headers?: Record<string, string>;
    /**
     * Response body (available only for providers that use HTTP requests).
     */
    readonly body?: unknown;
};

/**
 * Represents the number of tokens used in a prompt and completion.
 */
export declare type LanguageModelUsage = {
    /**
     * The total number of input (prompt) tokens used.
     */
    inputTokens: number | undefined;
    /**
     * Detailed information about the input tokens.
     */
    inputTokenDetails: {
        /**
         * The number of non-cached input (prompt) tokens used.
         */
        noCacheTokens: number | undefined;
        /**
         * The number of cached input (prompt) tokens read.
         */
        cacheReadTokens: number | undefined;
        /**
         * The number of cached input (prompt) tokens written.
         */
        cacheWriteTokens: number | undefined;
    };
    /**
     * The number of total output (completion) tokens used.
     */
    outputTokens: number | undefined;
    /**
     * Detailed information about the output tokens.
     */
    outputTokenDetails: {
        /**
         * The number of text tokens used.
         */
        textTokens: number | undefined;
        /**
         * The number of reasoning tokens used.
         */
        reasoningTokens: number | undefined;
    };
    /**
     * The total number of tokens used.
     */
    totalTokens: number | undefined;
    /**
     * Raw usage information from the provider.
     *
     * This is the usage information in the shape that the provider returns.
     * It can include additional information that is not part of the standard usage information.
     */
    raw?: JSONObject_2;
};

/**
 * Specification for a language model that implements the language model interface version 2.
 */
declare type LanguageModelV2 = {
    /**
     * The language model must specify which language model interface version it implements.
     */
    readonly specificationVersion: 'v2';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Supported URL patterns by media type for the provider.
     *
     * The keys are media type patterns or full media types (e.g. `*\/*` for everything, `audio/*`, `video/*`, or `application/pdf`).
     * and the values are arrays of regular expressions that match the URL paths.
     *
     * The matching should be against lower-case URLs.
     *
     * Matched URLs are supported natively by the model and are not downloaded.
     *
     * @returns A map of supported URL patterns by media type (as a promise or a plain object).
     */
    supportedUrls: PromiseLike<Record<string, RegExp[]>> | Record<string, RegExp[]>;
    /**
     * Generates a language model output (non-streaming).
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     */
    doGenerate(options: LanguageModelV2CallOptions): PromiseLike<{
        /**
         * Ordered content that the model has generated.
         */
        content: Array<LanguageModelV2Content>;
        /**
         * Finish reason.
         */
        finishReason: LanguageModelV2FinishReason;
        /**
         * Usage information.
         */
        usage: LanguageModelV2Usage;
        /**
         * Additional provider-specific metadata. They are passed through
         * from the provider to the AI SDK and enable provider-specific
         * results that can be fully encapsulated in the provider.
         */
        providerMetadata?: SharedV2ProviderMetadata;
        /**
         * Optional request information for telemetry and debugging purposes.
         */
        request?: {
            /**
             * Request HTTP body that was sent to the provider API.
             */
            body?: unknown;
        };
        /**
         * Optional response information for telemetry and debugging purposes.
         */
        response?: LanguageModelV2ResponseMetadata & {
            /**
             * Response headers.
             */
            headers?: SharedV2Headers_2;
            /**
             * Response HTTP body.
             */
            body?: unknown;
        };
        /**
         * Warnings for the call, e.g. unsupported settings.
         */
        warnings: Array<LanguageModelV2CallWarning>;
    }>;
    /**
     * Generates a language model output (streaming).
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     *
     * @return A stream of higher-level language model output parts.
     */
    doStream(options: LanguageModelV2CallOptions): PromiseLike<{
        stream: ReadableStream<LanguageModelV2StreamPart>;
        /**
         * Optional request information for telemetry and debugging purposes.
         */
        request?: {
            /**
             * Request HTTP body that was sent to the provider API.
             */
            body?: unknown;
        };
        /**
         * Optional response data.
         */
        response?: {
            /**
             * Response headers.
             */
            headers?: SharedV2Headers_2;
        };
    }>;
};

declare type LanguageModelV2CallOptions = {
    /**
     * A language mode prompt is a standardized prompt type.
     *
     * Note: This is **not** the user-facing prompt. The AI SDK methods will map the
     * user-facing prompt types such as chat or instruction prompts to this format.
     * That approach allows us to evolve the user  facing prompts without breaking
     * the language model interface.
     */
    prompt: LanguageModelV2Prompt;
    /**
     * Maximum number of tokens to generate.
     */
    maxOutputTokens?: number;
    /**
     * Temperature setting. The range depends on the provider and model.
     */
    temperature?: number;
    /**
     * Stop sequences.
     * If set, the model will stop generating text when one of the stop sequences is generated.
     * Providers may have limits on the number of stop sequences.
     */
    stopSequences?: string[];
    /**
     * Nucleus sampling.
     */
    topP?: number;
    /**
     * Only sample from the top K options for each subsequent token.
     *
     * Used to remove "long tail" low probability responses.
     * Recommended for advanced use cases only. You usually only need to use temperature.
     */
    topK?: number;
    /**
     * Presence penalty setting. It affects the likelihood of the model to
     * repeat information that is already in the prompt.
     */
    presencePenalty?: number;
    /**
     * Frequency penalty setting. It affects the likelihood of the model
     * to repeatedly use the same words or phrases.
     */
    frequencyPenalty?: number;
    /**
     * Response format. The output can either be text or JSON. Default is text.
     *
     * If JSON is selected, a schema can optionally be provided to guide the LLM.
     */
    responseFormat?: {
        type: 'text';
    } | {
        type: 'json';
        /**
         * JSON schema that the generated output should conform to.
         */
        schema?: JSONSchema7;
        /**
         * Name of output that should be generated. Used by some providers for additional LLM guidance.
         */
        name?: string;
        /**
         * Description of the output that should be generated. Used by some providers for additional LLM guidance.
         */
        description?: string;
    };
    /**
     * The seed (integer) to use for random sampling. If set and supported
     * by the model, calls will generate deterministic results.
     */
    seed?: number;
    /**
     * The tools that are available for the model.
     */
    tools?: Array<LanguageModelV2FunctionTool | LanguageModelV2ProviderDefinedTool>;
    /**
     * Specifies how the tool should be selected. Defaults to 'auto'.
     */
    toolChoice?: LanguageModelV2ToolChoice;
    /**
     * Include raw chunks in the stream. Only applicable for streaming calls.
     */
    includeRawChunks?: boolean;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV2ProviderOptions;
};

/**
 * Warning from the model provider for this call. The call will proceed, but e.g.
 * some settings might not be supported, which can lead to suboptimal results.
 */
declare type LanguageModelV2CallWarning = {
    type: 'unsupported-setting';
    setting: Omit<keyof LanguageModelV2CallOptions, 'prompt'>;
    details?: string;
} | {
    type: 'unsupported-tool';
    tool: LanguageModelV2FunctionTool | LanguageModelV2ProviderDefinedTool;
    details?: string;
} | {
    type: 'other';
    message: string;
};

declare type LanguageModelV2Content = LanguageModelV2Text | LanguageModelV2Reasoning | LanguageModelV2File | LanguageModelV2Source | LanguageModelV2ToolCall | LanguageModelV2ToolResult;

/**
 * Data content. Can be a Uint8Array, base64 encoded data as a string or a URL.
 */
declare type LanguageModelV2DataContent = Uint8Array | string | URL;

/**
 * A file that has been generated by the model.
 * Generated files as base64 encoded strings or binary data.
 * The files should be returned without any unnecessary conversion.
 */
declare type LanguageModelV2File = {
    type: 'file';
    /**
     * The IANA media type of the file, e.g. `image/png` or `audio/mp3`.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Generated file data as base64 encoded strings or binary data.
     *
     * The file data should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the file data should be returned
     * as base64 encoded strings. If the API returns binary data, the file data should
     * be returned as binary data.
     */
    data: string | Uint8Array;
};

/**
 * File content part of a prompt. It contains a file.
 */
declare interface LanguageModelV2FilePart {
    type: 'file';
    /**
     * Optional filename of the file.
     */
    filename?: string;
    /**
     * File data. Can be a Uint8Array, base64 encoded data as a string or a URL.
     */
    data: LanguageModelV2DataContent;
    /**
     * IANA media type of the file.
     *
     * Can support wildcards, e.g. `image/*` (in which case the provider needs to take appropriate action).
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV2ProviderOptions;
}

/**
 * Reason why a language model finished generating a response.
 *
 * Can be one of the following:
 * - `stop`: model generated stop sequence
 * - `length`: model generated maximum number of tokens
 * - `content-filter`: content filter violation stopped the model
 * - `tool-calls`: model triggered tool calls
 * - `error`: model stopped because of an error
 * - `other`: model stopped for other reasons
 * - `unknown`: the model has not transmitted a finish reason
 */
declare type LanguageModelV2FinishReason = 'stop' | 'length' | 'content-filter' | 'tool-calls' | 'error' | 'other' | 'unknown';

/**
 * A tool has a name, a description, and a set of parameters.
 *
 * Note: this is **not** the user-facing tool definition. The AI SDK methods will
 * map the user-facing tool definitions to this format.
 */
declare type LanguageModelV2FunctionTool = {
    /**
     * The type of the tool (always 'function').
     */
    type: 'function';
    /**
     * The name of the tool. Unique within this model call.
     */
    name: string;
    /**
     * A description of the tool. The language model uses this to understand the
     * tool's purpose and to provide better completion suggestions.
     */
    description?: string;
    /**
     * The parameters that the tool expects. The language model uses this to
     * understand the tool's input requirements and to provide matching suggestions.
     */
    inputSchema: JSONSchema7;
    /**
     * The provider-specific options for the tool.
     */
    providerOptions?: SharedV2ProviderOptions;
};

declare type LanguageModelV2Message = ({
    role: 'system';
    content: string;
} | {
    role: 'user';
    content: Array<LanguageModelV2TextPart | LanguageModelV2FilePart>;
} | {
    role: 'assistant';
    content: Array<LanguageModelV2TextPart | LanguageModelV2FilePart | LanguageModelV2ReasoningPart | LanguageModelV2ToolCallPart | LanguageModelV2ToolResultPart>;
} | {
    role: 'tool';
    content: Array<LanguageModelV2ToolResultPart>;
}) & {
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV2ProviderOptions;
};

/**
 * A prompt is a list of messages.
 *
 * Note: Not all models and prompt formats support multi-modal inputs and
 * tool calls. The validation happens at runtime.
 *
 * Note: This is not a user-facing prompt. The AI SDK methods will map the
 * user-facing prompt types such as chat or instruction prompts to this format.
 */
declare type LanguageModelV2Prompt = Array<LanguageModelV2Message>;

/**
 * The configuration of a tool that is defined by the provider.
 */
declare type LanguageModelV2ProviderDefinedTool = {
    /**
     * The type of the tool (always 'provider-defined').
     */
    type: 'provider-defined';
    /**
     * The ID of the tool. Should follow the format `<provider-name>.<unique-tool-name>`.
     */
    id: `${string}.${string}`;
    /**
     * The name of the tool that the user must use in the tool set.
     */
    name: string;
    /**
     * The arguments for configuring the tool. Must match the expected arguments defined by the provider for this tool.
     */
    args: Record<string, unknown>;
};

/**
 * Reasoning that the model has generated.
 */
declare type LanguageModelV2Reasoning = {
    type: 'reasoning';
    text: string;
    /**
     * Optional provider-specific metadata for the reasoning part.
     */
    providerMetadata?: SharedV2ProviderMetadata;
};

/**
 * Reasoning content part of a prompt. It contains a string of reasoning text.
 */
declare interface LanguageModelV2ReasoningPart {
    type: 'reasoning';
    /**
     * The reasoning text.
     */
    text: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV2ProviderOptions;
}

declare interface LanguageModelV2ResponseMetadata {
    /**
     * ID for the generated response, if the provider sends one.
     */
    id?: string;
    /**
     * Timestamp for the start of the generated response, if the provider sends one.
     */
    timestamp?: Date;
    /**
     * The ID of the response model that was used to generate the response, if the provider sends one.
     */
    modelId?: string;
}

/**
 * A source that has been used as input to generate the response.
 */
declare type LanguageModelV2Source = {
    type: 'source';
    /**
     * The type of source - URL sources reference web content.
     */
    sourceType: 'url';
    /**
     * The ID of the source.
     */
    id: string;
    /**
     * The URL of the source.
     */
    url: string;
    /**
     * The title of the source.
     */
    title?: string;
    /**
     * Additional provider metadata for the source.
     */
    providerMetadata?: SharedV2ProviderMetadata;
} | {
    type: 'source';
    /**
     * The type of source - document sources reference files/documents.
     */
    sourceType: 'document';
    /**
     * The ID of the source.
     */
    id: string;
    /**
     * IANA media type of the document (e.g., 'application/pdf').
     */
    mediaType: string;
    /**
     * The title of the document.
     */
    title: string;
    /**
     * Optional filename of the document.
     */
    filename?: string;
    /**
     * Additional provider metadata for the source.
     */
    providerMetadata?: SharedV2ProviderMetadata;
};

declare type LanguageModelV2StreamPart = {
    type: 'text-start';
    providerMetadata?: SharedV2ProviderMetadata;
    id: string;
} | {
    type: 'text-delta';
    id: string;
    providerMetadata?: SharedV2ProviderMetadata;
    delta: string;
} | {
    type: 'text-end';
    providerMetadata?: SharedV2ProviderMetadata;
    id: string;
} | {
    type: 'reasoning-start';
    providerMetadata?: SharedV2ProviderMetadata;
    id: string;
} | {
    type: 'reasoning-delta';
    id: string;
    providerMetadata?: SharedV2ProviderMetadata;
    delta: string;
} | {
    type: 'reasoning-end';
    id: string;
    providerMetadata?: SharedV2ProviderMetadata;
} | {
    type: 'tool-input-start';
    id: string;
    toolName: string;
    providerMetadata?: SharedV2ProviderMetadata;
    providerExecuted?: boolean;
} | {
    type: 'tool-input-delta';
    id: string;
    delta: string;
    providerMetadata?: SharedV2ProviderMetadata;
} | {
    type: 'tool-input-end';
    id: string;
    providerMetadata?: SharedV2ProviderMetadata;
} | LanguageModelV2ToolCall | LanguageModelV2ToolResult | LanguageModelV2File | LanguageModelV2Source | {
    type: 'stream-start';
    warnings: Array<LanguageModelV2CallWarning>;
} | ({
    type: 'response-metadata';
} & LanguageModelV2ResponseMetadata) | {
    type: 'finish';
    usage: LanguageModelV2Usage;
    finishReason: LanguageModelV2FinishReason;
    providerMetadata?: SharedV2ProviderMetadata;
} | {
    type: 'raw';
    rawValue: unknown;
} | {
    type: 'error';
    error: unknown;
};

/**
 * Text that the model has generated.
 */
declare type LanguageModelV2Text = {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    providerMetadata?: SharedV2ProviderMetadata;
};

/**
 * Text content part of a prompt. It contains a string of text.
 */
declare interface LanguageModelV2TextPart {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV2ProviderOptions;
}

/**
 * Tool calls that the model has generated.
 */
declare type LanguageModelV2ToolCall = {
    type: 'tool-call';
    /**
     * The identifier of the tool call. It must be unique across all tool calls.
     */
    toolCallId: string;
    /**
     * The name of the tool that should be called.
     */
    toolName: string;
    /**
     * Stringified JSON object with the tool call arguments. Must match the
     * parameters schema of the tool.
     */
    input: string;
    /**
     * Whether the tool call will be executed by the provider.
     * If this flag is not set or is false, the tool call will be executed by the client.
     */
    providerExecuted?: boolean;
    /**
     * Additional provider-specific metadata for the tool call.
     */
    providerMetadata?: SharedV2ProviderMetadata;
};

/**
 * Tool call content part of a prompt. It contains a tool call (usually generated by the AI model).
 */
declare interface LanguageModelV2ToolCallPart {
    type: 'tool-call';
    /**
     * ID of the tool call. This ID is used to match the tool call with the tool result.
     */
    toolCallId: string;
    /**
     * Name of the tool that is being called.
     */
    toolName: string;
    /**
     * Arguments of the tool call. This is a JSON-serializable object that matches the tool's input schema.
     */
    input: unknown;
    /**
     * Whether the tool call will be executed by the provider.
     * If this flag is not set or is false, the tool call will be executed by the client.
     */
    providerExecuted?: boolean;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV2ProviderOptions;
}

declare type LanguageModelV2ToolChoice = {
    type: 'auto';
} | {
    type: 'none';
} | {
    type: 'required';
} | {
    type: 'tool';
    toolName: string;
};

/**
 * Result of a tool call that has been executed by the provider.
 */
declare type LanguageModelV2ToolResult = {
    type: 'tool-result';
    /**
     * The ID of the tool call that this result is associated with.
     */
    toolCallId: string;
    /**
     * Name of the tool that generated this result.
     */
    toolName: string;
    /**
     * Result of the tool call. This is a JSON-serializable object.
     */
    result: unknown;
    /**
     * Optional flag if the result is an error or an error message.
     */
    isError?: boolean;
    /**
     * Whether the tool result was generated by the provider.
     * If this flag is set to true, the tool result was generated by the provider.
     * If this flag is not set or is false, the tool result was generated by the client.
     */
    providerExecuted?: boolean;
    /**
     * Additional provider-specific metadata for the tool result.
     */
    providerMetadata?: SharedV2ProviderMetadata;
};

declare type LanguageModelV2ToolResultOutput = {
    type: 'text';
    value: string;
} | {
    type: 'json';
    value: JSONValue_3;
} | {
    type: 'error-text';
    value: string;
} | {
    type: 'error-json';
    value: JSONValue_3;
} | {
    type: 'content';
    value: Array<{
        type: 'text';
        /**
         * Text content.
         */
        text: string;
    } | {
        type: 'media';
        /**
         * Base-64 encoded media data.
         */
        data: string;
        /**
         * IANA media type.
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
    }>;
};

/**
 * Tool result content part of a prompt. It contains the result of the tool call with the matching ID.
 */
declare interface LanguageModelV2ToolResultPart {
    type: 'tool-result';
    /**
     * ID of the tool call that this result is associated with.
     */
    toolCallId: string;
    /**
     * Name of the tool that generated this result.
     */
    toolName: string;
    /**
     * Result of the tool call.
     */
    output: LanguageModelV2ToolResultOutput;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV2ProviderOptions;
}

/**
 * Usage information for a language model call.
 *
 * If your API return additional usage information, you can add it to the
 * provider metadata under your provider's key.
 */
declare type LanguageModelV2Usage = {
    /**
     * The number of input (prompt) tokens used.
     */
    inputTokens: number | undefined;
    /**
     * The number of output (completion) tokens used.
     */
    outputTokens: number | undefined;
    /**
     * The total number of tokens as reported by the provider.
     * This number might be different from the sum of `inputTokens` and `outputTokens`
     * and e.g. include reasoning tokens or other overhead.
     */
    totalTokens: number | undefined;
    /**
     * The number of reasoning tokens used.
     */
    reasoningTokens?: number | undefined;
    /**
     * The number of cached input tokens.
     */
    cachedInputTokens?: number | undefined;
};

/**
 * Specification for a language model that implements the language model interface version 3.
 */
declare type LanguageModelV3 = {
    /**
     * The language model must specify which language model interface version it implements.
     */
    readonly specificationVersion: 'v3';
    /**
     * Provider ID.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID.
     */
    readonly modelId: string;
    /**
     * Supported URL patterns by media type for the provider.
     *
     * The keys are media type patterns or full media types (e.g. `*\/*` for everything, `audio/*`, `video/*`, or `application/pdf`).
     * and the values are arrays of regular expressions that match the URL paths.
     *
     * The matching should be against lower-case URLs.
     *
     * Matched URLs are supported natively by the model and are not downloaded.
     *
     * @returns A map of supported URL patterns by media type (as a promise or a plain object).
     */
    supportedUrls: PromiseLike<Record<string, RegExp[]>> | Record<string, RegExp[]>;
    /**
     * Generates a language model output (non-streaming).

     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     */
    doGenerate(options: LanguageModelV3CallOptions): PromiseLike<LanguageModelV3GenerateResult>;
    /**
     * Generates a language model output (streaming).
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     *
     * @return A stream of higher-level language model output parts.
     */
    doStream(options: LanguageModelV3CallOptions): PromiseLike<LanguageModelV3StreamResult>;
};

declare type LanguageModelV3CallOptions = {
    /**
     * A language mode prompt is a standardized prompt type.
     *
     * Note: This is **not** the user-facing prompt. The AI SDK methods will map the
     * user-facing prompt types such as chat or instruction prompts to this format.
     * That approach allows us to evolve the user  facing prompts without breaking
     * the language model interface.
     */
    prompt: LanguageModelV3Prompt;
    /**
     * Maximum number of tokens to generate.
     */
    maxOutputTokens?: number;
    /**
     * Temperature setting. The range depends on the provider and model.
     */
    temperature?: number;
    /**
     * Stop sequences.
     * If set, the model will stop generating text when one of the stop sequences is generated.
     * Providers may have limits on the number of stop sequences.
     */
    stopSequences?: string[];
    /**
     * Nucleus sampling.
     */
    topP?: number;
    /**
     * Only sample from the top K options for each subsequent token.
     *
     * Used to remove "long tail" low probability responses.
     * Recommended for advanced use cases only. You usually only need to use temperature.
     */
    topK?: number;
    /**
     * Presence penalty setting. It affects the likelihood of the model to
     * repeat information that is already in the prompt.
     */
    presencePenalty?: number;
    /**
     * Frequency penalty setting. It affects the likelihood of the model
     * to repeatedly use the same words or phrases.
     */
    frequencyPenalty?: number;
    /**
     * Response format. The output can either be text or JSON. Default is text.
     *
     * If JSON is selected, a schema can optionally be provided to guide the LLM.
     */
    responseFormat?: {
        type: 'text';
    } | {
        type: 'json';
        /**
         * JSON schema that the generated output should conform to.
         */
        schema?: JSONSchema7;
        /**
         * Name of output that should be generated. Used by some providers for additional LLM guidance.
         */
        name?: string;
        /**
         * Description of the output that should be generated. Used by some providers for additional LLM guidance.
         */
        description?: string;
    };
    /**
     * The seed (integer) to use for random sampling. If set and supported
     * by the model, calls will generate deterministic results.
     */
    seed?: number;
    /**
     * The tools that are available for the model.
     */
    tools?: Array<LanguageModelV3FunctionTool | LanguageModelV3ProviderTool>;
    /**
     * Specifies how the tool should be selected. Defaults to 'auto'.
     */
    toolChoice?: LanguageModelV3ToolChoice;
    /**
     * Include raw chunks in the stream. Only applicable for streaming calls.
     */
    includeRawChunks?: boolean;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV3ProviderOptions;
};

declare type LanguageModelV3Content = LanguageModelV3Text | LanguageModelV3Reasoning | LanguageModelV3File | LanguageModelV3ToolApprovalRequest | LanguageModelV3Source | LanguageModelV3ToolCall | LanguageModelV3ToolResult;

/**
 * Data content. Can be a Uint8Array, base64 encoded data as a string or a URL.
 */
declare type LanguageModelV3DataContent = Uint8Array | string | URL;

/**
 * A file that has been generated by the model.
 * Generated files as base64 encoded strings or binary data.
 * The files should be returned without any unnecessary conversion.
 */
declare type LanguageModelV3File = {
    type: 'file';
    /**
     * The IANA media type of the file, e.g. `image/png` or `audio/mp3`.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Generated file data as base64 encoded strings or binary data.
     *
     * The file data should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the file data should be returned
     * as base64 encoded strings. If the API returns binary data, the file data should
     * be returned as binary data.
     */
    data: string | Uint8Array;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerMetadata?: SharedV3ProviderMetadata;
};

/**
 * File content part of a prompt. It contains a file.
 */
declare interface LanguageModelV3FilePart {
    type: 'file';
    /**
     * Optional filename of the file.
     */
    filename?: string;
    /**
     * File data. Can be a Uint8Array, base64 encoded data as a string or a URL.
     */
    data: LanguageModelV3DataContent;
    /**
     * IANA media type of the file.
     *
     * Can support wildcards, e.g. `image/*` (in which case the provider needs to take appropriate action).
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV3ProviderOptions;
}

/**
 * Reason why a language model finished generating a response.
 *
 * Contains both a unified finish reason and a raw finish reason from the provider.
 * The unified finish reason is used to provide a consistent finish reason across different providers.
 * The raw finish reason is used to provide the original finish reason from the provider.
 */
declare type LanguageModelV3FinishReason = {
    /**
     * Unified finish reason. This enables using the same finish reason across different providers.
     *
     * Can be one of the following:
     * - `stop`: model generated stop sequence
     * - `length`: model generated maximum number of tokens
     * - `content-filter`: content filter violation stopped the model
     * - `tool-calls`: model triggered tool calls
     * - `error`: model stopped because of an error
     * - `other`: model stopped for other reasons
     */
    unified: 'stop' | 'length' | 'content-filter' | 'tool-calls' | 'error' | 'other';
    /**
     * Raw finish reason from the provider.
     * This is the original finish reason from the provider.
     */
    raw: string | undefined;
};

/**
 * A tool has a name, a description, and a set of parameters.
 *
 * Note: this is **not** the user-facing tool definition. The AI SDK methods will
 * map the user-facing tool definitions to this format.
 */
declare type LanguageModelV3FunctionTool = {
    /**
     * The type of the tool (always 'function').
     */
    type: 'function';
    /**
     * The name of the tool. Unique within this model call.
     */
    name: string;
    /**
     * A description of the tool. The language model uses this to understand the
     * tool's purpose and to provide better completion suggestions.
     */
    description?: string;
    /**
     * The parameters that the tool expects. The language model uses this to
     * understand the tool's input requirements and to provide matching suggestions.
     */
    inputSchema: JSONSchema7;
    /**
     * An optional list of input examples that show the language
     * model what the input should look like.
     */
    inputExamples?: Array<{
        input: JSONObject_2;
    }>;
    /**
     * Strict mode setting for the tool.
     *
     * Providers that support strict mode will use this setting to determine
     * how the input should be generated. Strict mode will always produce
     * valid inputs, but it might limit what input schemas are supported.
     */
    strict?: boolean;
    /**
     * The provider-specific options for the tool.
     */
    providerOptions?: SharedV3ProviderOptions;
};

/**
 * The result of a language model doGenerate call.
 */
declare type LanguageModelV3GenerateResult = {
    /**
     * Ordered content that the model has generated.
     */
    content: Array<LanguageModelV3Content>;
    /**
     * The finish reason.
     */
    finishReason: LanguageModelV3FinishReason;
    /**
     * The usage information.
     */
    usage: LanguageModelV3Usage;
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV3ProviderMetadata;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Request HTTP body that was sent to the provider API.
         */
        body?: unknown;
    };
    /**
     * Optional response information for telemetry and debugging purposes.
     */
    response?: LanguageModelV3ResponseMetadata & {
        /**
         * Response headers.
         */
        headers?: SharedV3Headers;
        /**
         * Response HTTP body.
         */
        body?: unknown;
    };
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: Array<SharedV3Warning>;
};

declare type LanguageModelV3Message = ({
    role: 'system';
    content: string;
} | {
    role: 'user';
    content: Array<LanguageModelV3TextPart | LanguageModelV3FilePart>;
} | {
    role: 'assistant';
    content: Array<LanguageModelV3TextPart | LanguageModelV3FilePart | LanguageModelV3ReasoningPart | LanguageModelV3ToolCallPart | LanguageModelV3ToolResultPart>;
} | {
    role: 'tool';
    content: Array<LanguageModelV3ToolResultPart | LanguageModelV3ToolApprovalResponsePart>;
}) & {
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV3ProviderOptions;
};

/**
 * A prompt is a list of messages.
 *
 * Note: Not all models and prompt formats support multi-modal inputs and
 * tool calls. The validation happens at runtime.
 *
 * Note: This is not a user-facing prompt. The AI SDK methods will map the
 * user-facing prompt types such as chat or instruction prompts to this format.
 */
declare type LanguageModelV3Prompt = Array<LanguageModelV3Message>;

/**
 * The configuration of a provider tool.
 *
 * Provider tools are tools that are specific to a certain provider.
 * The input and output schemas are defined be the provider, and
 * some of the tools are also executed on the provider systems.
 */
declare type LanguageModelV3ProviderTool = {
    /**
     * The type of the tool (always 'provider').
     */
    type: 'provider';
    /**
     * The ID of the tool. Should follow the format `<provider-id>.<unique-tool-name>`.
     */
    id: `${string}.${string}`;
    /**
     * The name of the tool. Unique within this model call.
     */
    name: string;
    /**
     * The arguments for configuring the tool. Must match the expected arguments defined by the provider for this tool.
     */
    args: Record<string, unknown>;
};

/**
 * Reasoning that the model has generated.
 */
declare type LanguageModelV3Reasoning = {
    type: 'reasoning';
    text: string;
    /**
     * Optional provider-specific metadata for the reasoning part.
     */
    providerMetadata?: SharedV3ProviderMetadata;
};

/**
 * Reasoning content part of a prompt. It contains a string of reasoning text.
 */
declare interface LanguageModelV3ReasoningPart {
    type: 'reasoning';
    /**
     * The reasoning text.
     */
    text: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV3ProviderOptions;
}

declare interface LanguageModelV3ResponseMetadata {
    /**
     * ID for the generated response, if the provider sends one.
     */
    id?: string;
    /**
     * Timestamp for the start of the generated response, if the provider sends one.
     */
    timestamp?: Date;
    /**
     * The ID of the response model that was used to generate the response, if the provider sends one.
     */
    modelId?: string;
}

/**
 * A source that has been used as input to generate the response.
 */
declare type LanguageModelV3Source = {
    type: 'source';
    /**
     * The type of source - URL sources reference web content.
     */
    sourceType: 'url';
    /**
     * The ID of the source.
     */
    id: string;
    /**
     * The URL of the source.
     */
    url: string;
    /**
     * The title of the source.
     */
    title?: string;
    /**
     * Additional provider metadata for the source.
     */
    providerMetadata?: SharedV3ProviderMetadata;
} | {
    type: 'source';
    /**
     * The type of source - document sources reference files/documents.
     */
    sourceType: 'document';
    /**
     * The ID of the source.
     */
    id: string;
    /**
     * IANA media type of the document (e.g., 'application/pdf').
     */
    mediaType: string;
    /**
     * The title of the document.
     */
    title: string;
    /**
     * Optional filename of the document.
     */
    filename?: string;
    /**
     * Additional provider metadata for the source.
     */
    providerMetadata?: SharedV3ProviderMetadata;
};

declare type LanguageModelV3StreamPart = {
    type: 'text-start';
    providerMetadata?: SharedV3ProviderMetadata;
    id: string;
} | {
    type: 'text-delta';
    id: string;
    providerMetadata?: SharedV3ProviderMetadata;
    delta: string;
} | {
    type: 'text-end';
    providerMetadata?: SharedV3ProviderMetadata;
    id: string;
} | {
    type: 'reasoning-start';
    providerMetadata?: SharedV3ProviderMetadata;
    id: string;
} | {
    type: 'reasoning-delta';
    id: string;
    providerMetadata?: SharedV3ProviderMetadata;
    delta: string;
} | {
    type: 'reasoning-end';
    id: string;
    providerMetadata?: SharedV3ProviderMetadata;
} | {
    type: 'tool-input-start';
    id: string;
    toolName: string;
    providerMetadata?: SharedV3ProviderMetadata;
    providerExecuted?: boolean;
    dynamic?: boolean;
    title?: string;
} | {
    type: 'tool-input-delta';
    id: string;
    delta: string;
    providerMetadata?: SharedV3ProviderMetadata;
} | {
    type: 'tool-input-end';
    id: string;
    providerMetadata?: SharedV3ProviderMetadata;
} | LanguageModelV3ToolApprovalRequest | LanguageModelV3ToolCall | LanguageModelV3ToolResult | LanguageModelV3File | LanguageModelV3Source | {
    type: 'stream-start';
    warnings: Array<SharedV3Warning>;
} | ({
    type: 'response-metadata';
} & LanguageModelV3ResponseMetadata) | {
    type: 'finish';
    usage: LanguageModelV3Usage;
    finishReason: LanguageModelV3FinishReason;
    providerMetadata?: SharedV3ProviderMetadata;
} | {
    type: 'raw';
    rawValue: unknown;
} | {
    type: 'error';
    error: unknown;
};

/**
 * The result of a language model doStream call.
 */
declare type LanguageModelV3StreamResult = {
    /**
     * The stream.
     */
    stream: ReadableStream<LanguageModelV3StreamPart>;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Request HTTP body that was sent to the provider API.
         */
        body?: unknown;
    };
    /**
     * Optional response data.
     */
    response?: {
        /**
         * Response headers.
         */
        headers?: SharedV3Headers;
    };
};

/**
 * Text that the model has generated.
 */
declare type LanguageModelV3Text = {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    providerMetadata?: SharedV3ProviderMetadata;
};

/**
 * Text content part of a prompt. It contains a string of text.
 */
declare interface LanguageModelV3TextPart {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV3ProviderOptions;
}

/**
 * Tool approval request emitted by a provider for a provider-executed tool call.
 *
 * This is used for flows where the provider executes the tool (e.g. MCP tools)
 * but requires an explicit user approval before continuing.
 */
declare type LanguageModelV3ToolApprovalRequest = {
    type: 'tool-approval-request';
    /**
     * ID of the approval request. This ID is referenced by the subsequent
     * tool-approval-response (tool message) to approve or deny execution.
     */
    approvalId: string;
    /**
     * The tool call ID that this approval request is for.
     */
    toolCallId: string;
    /**
     * Additional provider-specific metadata for the approval request.
     */
    providerMetadata?: SharedV3ProviderMetadata;
};

/**
 * Tool approval response content part of a prompt. It contains the user's
 * decision to approve or deny a provider-executed tool call.
 */
declare interface LanguageModelV3ToolApprovalResponsePart {
    type: 'tool-approval-response';
    /**
     * ID of the approval request that this response refers to.
     */
    approvalId: string;
    /**
     * Whether the approval was granted (true) or denied (false).
     */
    approved: boolean;
    /**
     * Optional reason for approval or denial.
     */
    reason?: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV3ProviderOptions;
}

/**
 * Tool calls that the model has generated.
 */
declare type LanguageModelV3ToolCall = {
    type: 'tool-call';
    /**
     * The identifier of the tool call. It must be unique across all tool calls.
     */
    toolCallId: string;
    /**
     * The name of the tool that should be called.
     */
    toolName: string;
    /**
     * Stringified JSON object with the tool call arguments. Must match the
     * parameters schema of the tool.
     */
    input: string;
    /**
     * Whether the tool call will be executed by the provider.
     * If this flag is not set or is false, the tool call will be executed by the client.
     */
    providerExecuted?: boolean;
    /**
     * Whether the tool is dynamic, i.e. defined at runtime.
     * For example, MCP (Model Context Protocol) tools that are executed by the provider.
     */
    dynamic?: boolean;
    /**
     * Additional provider-specific metadata for the tool call.
     */
    providerMetadata?: SharedV3ProviderMetadata;
};

/**
 * Tool call content part of a prompt. It contains a tool call (usually generated by the AI model).
 */
declare interface LanguageModelV3ToolCallPart {
    type: 'tool-call';
    /**
     * ID of the tool call. This ID is used to match the tool call with the tool result.
     */
    toolCallId: string;
    /**
     * Name of the tool that is being called.
     */
    toolName: string;
    /**
     * Arguments of the tool call. This is a JSON-serializable object that matches the tool's input schema.
     */
    input: unknown;
    /**
     * Whether the tool call will be executed by the provider.
     * If this flag is not set or is false, the tool call will be executed by the client.
     */
    providerExecuted?: boolean;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV3ProviderOptions;
}

declare type LanguageModelV3ToolChoice = {
    type: 'auto';
} | {
    type: 'none';
} | {
    type: 'required';
} | {
    type: 'tool';
    toolName: string;
};

/**
 * Result of a tool call that has been executed by the provider.
 */
declare type LanguageModelV3ToolResult = {
    type: 'tool-result';
    /**
     * The ID of the tool call that this result is associated with.
     */
    toolCallId: string;
    /**
     * Name of the tool that generated this result.
     */
    toolName: string;
    /**
     * Result of the tool call. This is a JSON-serializable object.
     */
    result: NonNullable<JSONValue_3>;
    /**
     * Optional flag if the result is an error or an error message.
     */
    isError?: boolean;
    /**
     * Whether the tool result is preliminary.
     *
     * Preliminary tool results replace each other, e.g. image previews.
     * There always has to be a final, non-preliminary tool result.
     *
     * If this flag is set to true, the tool result is preliminary.
     * If this flag is not set or is false, the tool result is not preliminary.
     */
    preliminary?: boolean;
    /**
     * Whether the tool is dynamic, i.e. defined at runtime.
     * For example, MCP (Model Context Protocol) tools that are executed by the provider.
     */
    dynamic?: boolean;
    /**
     * Additional provider-specific metadata for the tool result.
     */
    providerMetadata?: SharedV3ProviderMetadata;
};

/**
 * Result of a tool call.
 */
declare type LanguageModelV3ToolResultOutput = {
    /**
     * Text tool output that should be directly sent to the API.
     */
    type: 'text';
    value: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV3ProviderOptions;
} | {
    type: 'json';
    value: JSONValue_3;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV3ProviderOptions;
} | {
    /**
     * Type when the user has denied the execution of the tool call.
     */
    type: 'execution-denied';
    /**
     * Optional reason for the execution denial.
     */
    reason?: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV3ProviderOptions;
} | {
    type: 'error-text';
    value: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV3ProviderOptions;
} | {
    type: 'error-json';
    value: JSONValue_3;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV3ProviderOptions;
} | {
    type: 'content';
    value: Array<{
        type: 'text';
        /**
         * Text content.
         */
        text: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV3ProviderOptions;
    } | {
        type: 'file-data';
        /**
         * Base-64 encoded media data.
         */
        data: string;
        /**
         * IANA media type.
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
        /**
         * Optional filename of the file.
         */
        filename?: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV3ProviderOptions;
    } | {
        type: 'file-url';
        /**
         * URL of the file.
         */
        url: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV3ProviderOptions;
    } | {
        type: 'file-id';
        /**
         * ID of the file.
         *
         * If you use multiple providers, you need to
         * specify the provider specific ids using
         * the Record option. The key is the provider
         * name, e.g. 'openai' or 'anthropic'.
         */
        fileId: string | Record<string, string>;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV3ProviderOptions;
    } | {
        /**
         * Images that are referenced using base64 encoded data.
         */
        type: 'image-data';
        /**
         * Base-64 encoded image data.
         */
        data: string;
        /**
         * IANA media type.
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV3ProviderOptions;
    } | {
        /**
         * Images that are referenced using a URL.
         */
        type: 'image-url';
        /**
         * URL of the image.
         */
        url: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV3ProviderOptions;
    } | {
        /**
         * Images that are referenced using a provider file id.
         */
        type: 'image-file-id';
        /**
         * Image that is referenced using a provider file id.
         *
         * If you use multiple providers, you need to
         * specify the provider specific ids using
         * the Record option. The key is the provider
         * name, e.g. 'openai' or 'anthropic'.
         */
        fileId: string | Record<string, string>;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV3ProviderOptions;
    } | {
        /**
         * Custom content part. This can be used to implement
         * provider-specific content parts.
         */
        type: 'custom';
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV3ProviderOptions;
    }>;
};

/**
 * Tool result content part of a prompt. It contains the result of the tool call with the matching ID.
 */
declare interface LanguageModelV3ToolResultPart {
    type: 'tool-result';
    /**
     * ID of the tool call that this result is associated with.
     */
    toolCallId: string;
    /**
     * Name of the tool that generated this result.
     */
    toolName: string;
    /**
     * Result of the tool call.
     */
    output: LanguageModelV3ToolResultOutput;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV3ProviderOptions;
}

/**
 * Usage information for a language model call.
 */
declare type LanguageModelV3Usage = {
    /**
     * Information about the input tokens.
     */
    inputTokens: {
        /**
         * The total number of input (prompt) tokens used.
         */
        total: number | undefined;
        /**
         * The number of non-cached input (prompt) tokens used.
         */
        noCache: number | undefined;
        /**
         * The number of cached input (prompt) tokens read.
         */
        cacheRead: number | undefined;
        /**
         * The number of cached input (prompt) tokens written.
         */
        cacheWrite: number | undefined;
    };
    /**
     * Information about the output tokens.
     */
    outputTokens: {
        /**
         * The total number of output (completion) tokens used.
         */
        total: number | undefined;
        /**
         * The number of text tokens used.
         */
        text: number | undefined;
        /**
         * The number of reasoning tokens used.
         */
        reasoning: number | undefined;
    };
    /**
     * Raw usage information from the provider.
     *
     * This is the usage information in the shape that the provider returns.
     * It can include additional information that is not part of the standard usage information.
     */
    raw?: JSONObject_2;
};

/**
 * Specification for a language model that implements the language model interface version 4.
 */
declare type LanguageModelV4 = {
    /**
     * The language model must specify which language model interface version it implements.
     */
    readonly specificationVersion: 'v4';
    /**
     * Provider ID.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID.
     */
    readonly modelId: string;
    /**
     * Supported URL patterns by media type for the provider.
     *
     * The keys are media type patterns or full media types (e.g. `*\/*` for everything, `audio/*`, `video/*`, or `application/pdf`).
     * and the values are arrays of regular expressions that match the URL paths.
     *
     * The matching should be against lower-case URLs.
     *
     * Matched URLs are supported natively by the model and are not downloaded.
     *
     * @returns A map of supported URL patterns by media type (as a promise or a plain object).
     */
    supportedUrls: PromiseLike<Record<string, RegExp[]>> | Record<string, RegExp[]>;
    /**
     * Generates a language model output (non-streaming).
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     */
    doGenerate(options: LanguageModelV4CallOptions): PromiseLike<LanguageModelV4GenerateResult>;
    /**
     * Generates a language model output (streaming).
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     *
     * @return A stream of higher-level language model output parts.
     */
    doStream(options: LanguageModelV4CallOptions): PromiseLike<LanguageModelV4StreamResult>;
};

/**
 * Specification for a language model that implements the language model interface version 4.
 */
declare type LanguageModelV4_2 = {
    /**
     * The language model must specify which language model interface version it implements.
     */
    readonly specificationVersion: 'v4';
    /**
     * Provider ID.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID.
     */
    readonly modelId: string;
    /**
     * Supported URL patterns by media type for the provider.
     *
     * The keys are media type patterns or full media types (e.g. `*\/*` for everything, `audio/*`, `video/*`, or `application/pdf`).
     * and the values are arrays of regular expressions that match the URL paths.
     *
     * The matching should be against lower-case URLs.
     *
     * Matched URLs are supported natively by the model and are not downloaded.
     *
     * @returns A map of supported URL patterns by media type (as a promise or a plain object).
     */
    supportedUrls: PromiseLike<Record<string, RegExp[]>> | Record<string, RegExp[]>;
    /**
     * Generates a language model output (non-streaming).
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     */
    doGenerate(options: LanguageModelV4CallOptions_2): PromiseLike<LanguageModelV4GenerateResult_2>;
    /**
     * Generates a language model output (streaming).
     *
     * Naming: "do" prefix to prevent accidental direct usage of the method
     * by the user.
     *
     * @return A stream of higher-level language model output parts.
     */
    doStream(options: LanguageModelV4CallOptions_2): PromiseLike<LanguageModelV4StreamResult_2>;
};

declare type LanguageModelV4CallOptions = {
    /**
     * A language mode prompt is a standardized prompt type.
     *
     * Note: This is **not** the user-facing prompt. The AI SDK methods will map the
     * user-facing prompt types such as chat or instruction prompts to this format.
     * That approach allows us to evolve the user  facing prompts without breaking
     * the language model interface.
     */
    prompt: LanguageModelV4Prompt;
    /**
     * Maximum number of tokens to generate.
     */
    maxOutputTokens?: number;
    /**
     * Temperature setting. The range depends on the provider and model.
     */
    temperature?: number;
    /**
     * Stop sequences.
     * If set, the model will stop generating text when one of the stop sequences is generated.
     * Providers may have limits on the number of stop sequences.
     */
    stopSequences?: string[];
    /**
     * Nucleus sampling.
     */
    topP?: number;
    /**
     * Only sample from the top K options for each subsequent token.
     *
     * Used to remove "long tail" low probability responses.
     * Recommended for advanced use cases only. You usually only need to use temperature.
     */
    topK?: number;
    /**
     * Presence penalty setting. It affects the likelihood of the model to
     * repeat information that is already in the prompt.
     */
    presencePenalty?: number;
    /**
     * Frequency penalty setting. It affects the likelihood of the model
     * to repeatedly use the same words or phrases.
     */
    frequencyPenalty?: number;
    /**
     * Response format. The output can either be text or JSON. Default is text.
     *
     * If JSON is selected, a schema can optionally be provided to guide the LLM.
     */
    responseFormat?: {
        type: 'text';
    } | {
        type: 'json';
        /**
         * JSON schema that the generated output should conform to.
         */
        schema?: JSONSchema7;
        /**
         * Name of output that should be generated. Used by some providers for additional LLM guidance.
         */
        name?: string;
        /**
         * Description of the output that should be generated. Used by some providers for additional LLM guidance.
         */
        description?: string;
    };
    /**
     * The seed (integer) to use for random sampling. If set and supported
     * by the model, calls will generate deterministic results.
     */
    seed?: number;
    /**
     * The tools that are available for the model.
     */
    tools?: Array<LanguageModelV4FunctionTool | LanguageModelV4ProviderTool>;
    /**
     * Specifies how the tool should be selected. Defaults to 'auto'.
     */
    toolChoice?: LanguageModelV4ToolChoice;
    /**
     * Include raw chunks in the stream. Only applicable for streaming calls.
     */
    includeRawChunks?: boolean;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
    /**
     * Reasoning effort level for the model. Controls how much reasoning
     * the model performs before generating a response. Defaults to 'provider-default'.
     */
    reasoning?: 'provider-default' | 'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh';
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
};

declare type LanguageModelV4CallOptions_2 = {
    /**
     * A language mode prompt is a standardized prompt type.
     *
     * Note: This is **not** the user-facing prompt. The AI SDK methods will map the
     * user-facing prompt types such as chat or instruction prompts to this format.
     * That approach allows us to evolve the user  facing prompts without breaking
     * the language model interface.
     */
    prompt: LanguageModelV4Prompt_2;
    /**
     * Maximum number of tokens to generate.
     */
    maxOutputTokens?: number;
    /**
     * Temperature setting. The range depends on the provider and model.
     */
    temperature?: number;
    /**
     * Stop sequences.
     * If set, the model will stop generating text when one of the stop sequences is generated.
     * Providers may have limits on the number of stop sequences.
     */
    stopSequences?: string[];
    /**
     * Nucleus sampling.
     */
    topP?: number;
    /**
     * Only sample from the top K options for each subsequent token.
     *
     * Used to remove "long tail" low probability responses.
     * Recommended for advanced use cases only. You usually only need to use temperature.
     */
    topK?: number;
    /**
     * Presence penalty setting. It affects the likelihood of the model to
     * repeat information that is already in the prompt.
     */
    presencePenalty?: number;
    /**
     * Frequency penalty setting. It affects the likelihood of the model
     * to repeatedly use the same words or phrases.
     */
    frequencyPenalty?: number;
    /**
     * Response format. The output can either be text or JSON. Default is text.
     *
     * If JSON is selected, a schema can optionally be provided to guide the LLM.
     */
    responseFormat?: {
        type: 'text';
    } | {
        type: 'json';
        /**
         * JSON schema that the generated output should conform to.
         */
        schema?: JSONSchema7;
        /**
         * Name of output that should be generated. Used by some providers for additional LLM guidance.
         */
        name?: string;
        /**
         * Description of the output that should be generated. Used by some providers for additional LLM guidance.
         */
        description?: string;
    };
    /**
     * The seed (integer) to use for random sampling. If set and supported
     * by the model, calls will generate deterministic results.
     */
    seed?: number;
    /**
     * The tools that are available for the model.
     */
    tools?: Array<LanguageModelV4FunctionTool_2 | LanguageModelV4ProviderTool_2>;
    /**
     * Specifies how the tool should be selected. Defaults to 'auto'.
     */
    toolChoice?: LanguageModelV4ToolChoice_2;
    /**
     * Include raw chunks in the stream. Only applicable for streaming calls.
     */
    includeRawChunks?: boolean;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
    /**
     * Reasoning effort level for the model. Controls how much reasoning
     * the model performs before generating a response. Defaults to 'provider-default'.
     */
    reasoning?: 'provider-default' | 'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh';
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
};

declare type LanguageModelV4Content = LanguageModelV4Text | LanguageModelV4Reasoning | LanguageModelV4CustomContent | LanguageModelV4ReasoningFile | LanguageModelV4File | LanguageModelV4ToolApprovalRequest | LanguageModelV4Source | LanguageModelV4ToolCall | LanguageModelV4ToolResult;

declare type LanguageModelV4Content_2 = LanguageModelV4Text_2 | LanguageModelV4Reasoning_2 | LanguageModelV4CustomContent_2 | LanguageModelV4ReasoningFile_2 | LanguageModelV4File_2 | LanguageModelV4ToolApprovalRequest_2 | LanguageModelV4Source_2 | LanguageModelV4ToolCall_2 | LanguageModelV4ToolResult_2;

/**
 * A provider-specific content block that does not map to another standardized
 * content part type.
 */
declare type LanguageModelV4CustomContent = {
    type: 'custom';
    /**
     * The kind of custom content, in the format `{provider}.{provider-type}`.
     */
    kind: `${string}.${string}`;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV4ProviderMetadata;
};

/**
 * A provider-specific content block that does not map to another standardized
 * content part type.
 */
declare type LanguageModelV4CustomContent_2 = {
    type: 'custom';
    /**
     * The kind of custom content, in the format `{provider}.{provider-type}`.
     */
    kind: `${string}.${string}`;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
};

/**
 * Provider-specific content part of a prompt. It contains no standardized
 * payload beyond provider-specific options.
 */
declare interface LanguageModelV4CustomPart {
    type: 'custom';
    /**
     * The kind of custom content, in the format `{provider}.{provider-type}`.
     */
    kind: `${string}.${string}`;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
}

/**
 * Provider-specific content part of a prompt. It contains no standardized
 * payload beyond provider-specific options.
 */
declare interface LanguageModelV4CustomPart_2 {
    type: 'custom';
    /**
     * The kind of custom content, in the format `{provider}.{provider-type}`.
     */
    kind: `${string}.${string}`;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
}

/**
 * A file that has been generated by the model.
 * Generated files as base64 encoded strings or binary data.
 * The files should be returned without any unnecessary conversion.
 */
declare type LanguageModelV4File = {
    type: 'file';
    /**
     * The IANA media type of the file, e.g. `image/png` or `audio/mp3`.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Generated file data as a tagged discriminated union:
     *
     * - `{ type: 'data', data }`: raw bytes (Uint8Array) or base64-encoded string.
     * - `{ type: 'url', url }`: a URL that points to the file.
     *
     * The file data should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the file data should be returned
     * as base64 encoded strings. If the API returns binary data, the file data should
     * be returned as binary data.
     */
    data: SharedV4FileDataData | SharedV4FileDataUrl;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerMetadata?: SharedV4ProviderMetadata;
};

/**
 * A file that has been generated by the model.
 * Generated files as base64 encoded strings or binary data.
 * The files should be returned without any unnecessary conversion.
 */
declare type LanguageModelV4File_2 = {
    type: 'file';
    /**
     * The IANA media type of the file, e.g. `image/png` or `audio/mp3`.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Generated file data as a tagged discriminated union:
     *
     * - `{ type: 'data', data }`: raw bytes (Uint8Array) or base64-encoded string.
     * - `{ type: 'url', url }`: a URL that points to the file.
     *
     * The file data should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the file data should be returned
     * as base64 encoded strings. If the API returns binary data, the file data should
     * be returned as binary data.
     */
    data: SharedV4FileDataData_2 | SharedV4FileDataUrl_2;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
};

/**
 * File content part of a prompt. It contains a file.
 */
declare interface LanguageModelV4FilePart {
    type: 'file';
    /**
     * Optional filename of the file.
     */
    filename?: string;
    /**
     * File data as a tagged discriminated union:
     *
     * - `{ type: 'data', data }`: raw bytes (Uint8Array) or base64-encoded string.
     * - `{ type: 'url', url }`: a URL that points to the file.
     * - `{ type: 'reference', reference }`: a provider reference (`{ [provider]: id }`).
     * - `{ type: 'text', text }`: inline text content (e.g. an inline text document).
     */
    data: SharedV4FileData;
    /**
     * Either a full IANA media type (`type/subtype`, e.g. `image/png`) or just
     * the top-level IANA segment (e.g. `image`, `audio`, `video`, `text`).
     *
     * `*`-subtype wildcards (e.g. `image/*`) are normalized as equivalent to the
     * top-level segment alone (e.g. `image`). Providers can use the helpers in
     * `@ai-sdk/provider-utils` (`isFullMediaType`, `getTopLevelMediaType`,
     * `detectMediaType`) to resolve the field according to their API
     * requirements.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
}

/**
 * File content part of a prompt. It contains a file.
 */
declare interface LanguageModelV4FilePart_2 {
    type: 'file';
    /**
     * Optional filename of the file.
     */
    filename?: string;
    /**
     * File data as a tagged discriminated union:
     *
     * - `{ type: 'data', data }`: raw bytes (Uint8Array) or base64-encoded string.
     * - `{ type: 'url', url }`: a URL that points to the file.
     * - `{ type: 'reference', reference }`: a provider reference (`{ [provider]: id }`).
     * - `{ type: 'text', text }`: inline text content (e.g. an inline text document).
     */
    data: SharedV4FileData_2;
    /**
     * Either a full IANA media type (`type/subtype`, e.g. `image/png`) or just
     * the top-level IANA segment (e.g. `image`, `audio`, `video`, `text`).
     *
     * `*`-subtype wildcards (e.g. `image/*`) are normalized as equivalent to the
     * top-level segment alone (e.g. `image`). Providers can use the helpers in
     * `@ai-sdk/provider-utils` (`isFullMediaType`, `getTopLevelMediaType`,
     * `detectMediaType`) to resolve the field according to their API
     * requirements.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
}

/**
 * Reason why a language model finished generating a response.
 *
 * Contains both a unified finish reason and a raw finish reason from the provider.
 * The unified finish reason is used to provide a consistent finish reason across different providers.
 * The raw finish reason is used to provide the original finish reason from the provider.
 */
declare type LanguageModelV4FinishReason = {
    /**
     * Unified finish reason. This enables using the same finish reason across different providers.
     *
     * Can be one of the following:
     * - `stop`: model generated stop sequence
     * - `length`: model generated maximum number of tokens
     * - `content-filter`: content filter violation stopped the model
     * - `tool-calls`: model triggered tool calls
     * - `error`: model stopped because of an error
     * - `other`: model stopped for other reasons
     */
    unified: 'stop' | 'length' | 'content-filter' | 'tool-calls' | 'error' | 'other';
    /**
     * Raw finish reason from the provider.
     * This is the original finish reason from the provider.
     */
    raw: string | undefined;
};

/**
 * Reason why a language model finished generating a response.
 *
 * Contains both a unified finish reason and a raw finish reason from the provider.
 * The unified finish reason is used to provide a consistent finish reason across different providers.
 * The raw finish reason is used to provide the original finish reason from the provider.
 */
declare type LanguageModelV4FinishReason_2 = {
    /**
     * Unified finish reason. This enables using the same finish reason across different providers.
     *
     * Can be one of the following:
     * - `stop`: model generated stop sequence
     * - `length`: model generated maximum number of tokens
     * - `content-filter`: content filter violation stopped the model
     * - `tool-calls`: model triggered tool calls
     * - `error`: model stopped because of an error
     * - `other`: model stopped for other reasons
     */
    unified: 'stop' | 'length' | 'content-filter' | 'tool-calls' | 'error' | 'other';
    /**
     * Raw finish reason from the provider.
     * This is the original finish reason from the provider.
     */
    raw: string | undefined;
};

/**
 * A tool has a name, a description, and a set of parameters.
 *
 * Note: this is **not** the user-facing tool definition. The AI SDK methods will
 * map the user-facing tool definitions to this format.
 */
declare type LanguageModelV4FunctionTool = {
    /**
     * The type of the tool (always 'function').
     */
    type: 'function';
    /**
     * The name of the tool. Unique within this model call.
     */
    name: string;
    /**
     * A description of the tool. The language model uses this to understand the
     * tool's purpose and to provide better completion suggestions.
     */
    description?: string;
    /**
     * The parameters that the tool expects. The language model uses this to
     * understand the tool's input requirements and to provide matching suggestions.
     */
    inputSchema: JSONSchema7;
    /**
     * An optional list of input examples that show the language
     * model what the input should look like.
     */
    inputExamples?: Array<{
        input: JSONObject;
    }>;
    /**
     * Strict mode setting for the tool.
     *
     * Providers that support strict mode will use this setting to determine
     * how the input should be generated. Strict mode will always produce
     * valid inputs, but it might limit what input schemas are supported.
     */
    strict?: boolean;
    /**
     * The provider-specific options for the tool.
     */
    providerOptions?: SharedV4ProviderOptions;
};

/**
 * A tool has a name, a description, and a set of parameters.
 *
 * Note: this is **not** the user-facing tool definition. The AI SDK methods will
 * map the user-facing tool definitions to this format.
 */
declare type LanguageModelV4FunctionTool_2 = {
    /**
     * The type of the tool (always 'function').
     */
    type: 'function';
    /**
     * The name of the tool. Unique within this model call.
     */
    name: string;
    /**
     * A description of the tool. The language model uses this to understand the
     * tool's purpose and to provide better completion suggestions.
     */
    description?: string;
    /**
     * The parameters that the tool expects. The language model uses this to
     * understand the tool's input requirements and to provide matching suggestions.
     */
    inputSchema: JSONSchema7;
    /**
     * An optional list of input examples that show the language
     * model what the input should look like.
     */
    inputExamples?: Array<{
        input: JSONObject_2;
    }>;
    /**
     * Strict mode setting for the tool.
     *
     * Providers that support strict mode will use this setting to determine
     * how the input should be generated. Strict mode will always produce
     * valid inputs, but it might limit what input schemas are supported.
     */
    strict?: boolean;
    /**
     * The provider-specific options for the tool.
     */
    providerOptions?: SharedV4ProviderOptions_2;
};

/**
 * The result of a language model doGenerate call.
 */
declare type LanguageModelV4GenerateResult = {
    /**
     * Ordered content that the model has generated.
     */
    content: Array<LanguageModelV4Content>;
    /**
     * The finish reason.
     */
    finishReason: LanguageModelV4FinishReason;
    /**
     * The usage information.
     */
    usage: LanguageModelV4Usage;
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV4ProviderMetadata;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Request HTTP body that was sent to the provider API.
         */
        body?: unknown;
    };
    /**
     * Optional response information for telemetry and debugging purposes.
     */
    response?: LanguageModelV4ResponseMetadata & {
        /**
         * Response headers.
         */
        headers?: SharedV4Headers;
        /**
         * Response HTTP body.
         */
        body?: unknown;
    };
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: Array<SharedV4Warning>;
};

/**
 * The result of a language model doGenerate call.
 */
declare type LanguageModelV4GenerateResult_2 = {
    /**
     * Ordered content that the model has generated.
     */
    content: Array<LanguageModelV4Content_2>;
    /**
     * The finish reason.
     */
    finishReason: LanguageModelV4FinishReason_2;
    /**
     * The usage information.
     */
    usage: LanguageModelV4Usage_2;
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Request HTTP body that was sent to the provider API.
         */
        body?: unknown;
    };
    /**
     * Optional response information for telemetry and debugging purposes.
     */
    response?: LanguageModelV4ResponseMetadata_2 & {
        /**
         * Response headers.
         */
        headers?: SharedV4Headers_2;
        /**
         * Response HTTP body.
         */
        body?: unknown;
    };
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: Array<SharedV4Warning_2>;
};

declare type LanguageModelV4Message = ({
    role: 'system';
    content: string;
} | {
    role: 'user';
    content: Array<LanguageModelV4TextPart | LanguageModelV4FilePart>;
} | {
    role: 'assistant';
    content: Array<LanguageModelV4TextPart | LanguageModelV4FilePart | LanguageModelV4CustomPart | LanguageModelV4ReasoningPart | LanguageModelV4ReasoningFilePart | LanguageModelV4ToolCallPart | LanguageModelV4ToolResultPart>;
} | {
    role: 'tool';
    content: Array<LanguageModelV4ToolResultPart | LanguageModelV4ToolApprovalResponsePart>;
}) & {
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
};

declare type LanguageModelV4Message_2 = ({
    role: 'system';
    content: string;
} | {
    role: 'user';
    content: Array<LanguageModelV4TextPart_2 | LanguageModelV4FilePart_2>;
} | {
    role: 'assistant';
    content: Array<LanguageModelV4TextPart_2 | LanguageModelV4FilePart_2 | LanguageModelV4CustomPart_2 | LanguageModelV4ReasoningPart_2 | LanguageModelV4ReasoningFilePart_2 | LanguageModelV4ToolCallPart_2 | LanguageModelV4ToolResultPart_2>;
} | {
    role: 'tool';
    content: Array<LanguageModelV4ToolResultPart_2 | LanguageModelV4ToolApprovalResponsePart_2>;
}) & {
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
};

/**
 * Experimental middleware for LanguageModelV4.
 * This type defines the structure for middleware that can be used to modify
 * the behavior of LanguageModelV4 operations.
 */
declare type LanguageModelV4Middleware = {
    /**
     * Middleware specification version. Use `v4` for the current version.
     */
    readonly specificationVersion: 'v4';
    /**
     * Override the provider name if desired.
     * @param options.model - The language model instance.
     */
    overrideProvider?: (options: {
        model: LanguageModelV4_2;
    }) => string;
    /**
     * Override the model ID if desired.
     * @param options.model - The language model instance.
     */
    overrideModelId?: (options: {
        model: LanguageModelV4_2;
    }) => string;
    /**
     * Override the supported URLs if desired.
     * @param options.model - The language model instance.
     */
    overrideSupportedUrls?: (options: {
        model: LanguageModelV4_2;
    }) => PromiseLike<Record<string, RegExp[]>> | Record<string, RegExp[]>;
    /**
     * Transforms the parameters before they are passed to the language model.
     * @param options - Object containing the type of operation and the parameters.
     * @param options.type - The type of operation ('generate' or 'stream').
     * @param options.params - The original parameters for the language model call.
     * @returns A promise that resolves to the transformed parameters.
     */
    transformParams?: (options: {
        type: 'generate' | 'stream';
        params: LanguageModelV4CallOptions_2;
        model: LanguageModelV4_2;
    }) => PromiseLike<LanguageModelV4CallOptions_2>;
    /**
     * Wraps the generate operation of the language model.
     * @param options - Object containing the generate function, parameters, and model.
     * @param options.doGenerate - The original generate function.
     * @param options.doStream - The original stream function.
     * @param options.params - The parameters for the generate call. If the
     * `transformParams` middleware is used, this will be the transformed parameters.
     * @param options.model - The language model instance.
     * @returns A promise that resolves to the result of the generate operation.
     */
    wrapGenerate?: (options: {
        doGenerate: () => PromiseLike<LanguageModelV4GenerateResult_2>;
        doStream: () => PromiseLike<LanguageModelV4StreamResult_2>;
        params: LanguageModelV4CallOptions_2;
        model: LanguageModelV4_2;
    }) => PromiseLike<LanguageModelV4GenerateResult_2>;
    /**
     * Wraps the stream operation of the language model.
     *
     * @param options - Object containing the stream function, parameters, and model.
     * @param options.doGenerate - The original generate function.
     * @param options.doStream - The original stream function.
     * @param options.params - The parameters for the stream call. If the
     * `transformParams` middleware is used, this will be the transformed parameters.
     * @param options.model - The language model instance.
     * @returns A promise that resolves to the result of the stream operation.
     */
    wrapStream?: (options: {
        doGenerate: () => PromiseLike<LanguageModelV4GenerateResult_2>;
        doStream: () => PromiseLike<LanguageModelV4StreamResult_2>;
        params: LanguageModelV4CallOptions_2;
        model: LanguageModelV4_2;
    }) => PromiseLike<LanguageModelV4StreamResult_2>;
};

/**
 * A prompt is a list of messages.
 *
 * Note: Not all models and prompt formats support multi-modal inputs and
 * tool calls. The validation happens at runtime.
 *
 * Note: This is not a user-facing prompt. The AI SDK methods will map the
 * user-facing prompt types such as chat or instruction prompts to this format.
 */
declare type LanguageModelV4Prompt = Array<LanguageModelV4Message>;

/**
 * A prompt is a list of messages.
 *
 * Note: Not all models and prompt formats support multi-modal inputs and
 * tool calls. The validation happens at runtime.
 *
 * Note: This is not a user-facing prompt. The AI SDK methods will map the
 * user-facing prompt types such as chat or instruction prompts to this format.
 */
declare type LanguageModelV4Prompt_2 = Array<LanguageModelV4Message_2>;

/**
 * The configuration of a provider tool.
 *
 * Provider tools are tools that are specific to a certain provider.
 * The input and output schemas are defined be the provider, and
 * some of the tools are also executed on the provider systems.
 */
declare type LanguageModelV4ProviderTool = {
    /**
     * The type of the tool (always 'provider').
     */
    type: 'provider';
    /**
     * The ID of the tool. Should follow the format `<provider-id>.<unique-tool-name>`.
     */
    id: `${string}.${string}`;
    /**
     * The name of the tool. Unique within this model call.
     */
    name: string;
    /**
     * The arguments for configuring the tool. Must match the expected arguments defined by the provider for this tool.
     */
    args: Record<string, unknown>;
};

/**
 * The configuration of a provider tool.
 *
 * Provider tools are tools that are specific to a certain provider.
 * The input and output schemas are defined be the provider, and
 * some of the tools are also executed on the provider systems.
 */
declare type LanguageModelV4ProviderTool_2 = {
    /**
     * The type of the tool (always 'provider').
     */
    type: 'provider';
    /**
     * The ID of the tool. Should follow the format `<provider-id>.<unique-tool-name>`.
     */
    id: `${string}.${string}`;
    /**
     * The name of the tool. Unique within this model call.
     */
    name: string;
    /**
     * The arguments for configuring the tool. Must match the expected arguments defined by the provider for this tool.
     */
    args: Record<string, unknown>;
};

/**
 * Reasoning that the model has generated.
 */
declare type LanguageModelV4Reasoning = {
    type: 'reasoning';
    text: string;
    /**
     * Optional provider-specific metadata for the reasoning part.
     */
    providerMetadata?: SharedV4ProviderMetadata;
};

/**
 * Reasoning that the model has generated.
 */
declare type LanguageModelV4Reasoning_2 = {
    type: 'reasoning';
    text: string;
    /**
     * Optional provider-specific metadata for the reasoning part.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
};

/**
 * A file that has been generated by the model as part of reasoning.
 * Generated files as base64 encoded strings or binary data.
 * The files should be returned without any unnecessary conversion.
 */
declare type LanguageModelV4ReasoningFile = {
    type: 'reasoning-file';
    /**
     * The IANA media type of the file, e.g. `image/png` or `audio/mp3`.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Generated file data as a tagged discriminated union:
     *
     * - `{ type: 'data', data }`: raw bytes (Uint8Array) or base64-encoded string.
     * - `{ type: 'url', url }`: a URL that points to the file.
     *
     * The file data should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the file data should be returned
     * as base64 encoded strings. If the API returns binary data, the file data should
     * be returned as binary data.
     */
    data: SharedV4FileDataData | SharedV4FileDataUrl;
    /**
     * Optional provider-specific metadata for the reasoning file part.
     */
    providerMetadata?: SharedV4ProviderMetadata;
};

/**
 * A file that has been generated by the model as part of reasoning.
 * Generated files as base64 encoded strings or binary data.
 * The files should be returned without any unnecessary conversion.
 */
declare type LanguageModelV4ReasoningFile_2 = {
    type: 'reasoning-file';
    /**
     * The IANA media type of the file, e.g. `image/png` or `audio/mp3`.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Generated file data as a tagged discriminated union:
     *
     * - `{ type: 'data', data }`: raw bytes (Uint8Array) or base64-encoded string.
     * - `{ type: 'url', url }`: a URL that points to the file.
     *
     * The file data should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the file data should be returned
     * as base64 encoded strings. If the API returns binary data, the file data should
     * be returned as binary data.
     */
    data: SharedV4FileDataData_2 | SharedV4FileDataUrl_2;
    /**
     * Optional provider-specific metadata for the reasoning file part.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
};

/**
 * Reasoning file content part of a prompt. It contains a file generated as part of reasoning.
 */
declare interface LanguageModelV4ReasoningFilePart {
    type: 'reasoning-file';
    /**
     * File data as a tagged discriminated union:
     *
     * - `{ type: 'data', data }`: raw bytes (Uint8Array) or base64-encoded string.
     * - `{ type: 'url', url }`: a URL that points to the file.
     */
    data: SharedV4FileDataData | SharedV4FileDataUrl;
    /**
     * IANA media type of the file.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
}

/**
 * Reasoning file content part of a prompt. It contains a file generated as part of reasoning.
 */
declare interface LanguageModelV4ReasoningFilePart_2 {
    type: 'reasoning-file';
    /**
     * File data as a tagged discriminated union:
     *
     * - `{ type: 'data', data }`: raw bytes (Uint8Array) or base64-encoded string.
     * - `{ type: 'url', url }`: a URL that points to the file.
     */
    data: SharedV4FileDataData_2 | SharedV4FileDataUrl_2;
    /**
     * IANA media type of the file.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
}

/**
 * Reasoning content part of a prompt. It contains a string of reasoning text.
 */
declare interface LanguageModelV4ReasoningPart {
    type: 'reasoning';
    /**
     * The reasoning text.
     */
    text: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
}

/**
 * Reasoning content part of a prompt. It contains a string of reasoning text.
 */
declare interface LanguageModelV4ReasoningPart_2 {
    type: 'reasoning';
    /**
     * The reasoning text.
     */
    text: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
}

declare interface LanguageModelV4ResponseMetadata {
    /**
     * ID for the generated response, if the provider sends one.
     */
    id?: string;
    /**
     * Timestamp for the start of the generated response, if the provider sends one.
     */
    timestamp?: Date;
    /**
     * The ID of the response model that was used to generate the response, if the provider sends one.
     */
    modelId?: string;
}

declare interface LanguageModelV4ResponseMetadata_2 {
    /**
     * ID for the generated response, if the provider sends one.
     */
    id?: string;
    /**
     * Timestamp for the start of the generated response, if the provider sends one.
     */
    timestamp?: Date;
    /**
     * The ID of the response model that was used to generate the response, if the provider sends one.
     */
    modelId?: string;
}

/**
 * A source that has been used as input to generate the response.
 */
declare type LanguageModelV4Source = {
    type: 'source';
    /**
     * The type of source - URL sources reference web content.
     */
    sourceType: 'url';
    /**
     * The ID of the source.
     */
    id: string;
    /**
     * The URL of the source.
     */
    url: string;
    /**
     * The title of the source.
     */
    title?: string;
    /**
     * Additional provider metadata for the source.
     */
    providerMetadata?: SharedV4ProviderMetadata;
} | {
    type: 'source';
    /**
     * The type of source - document sources reference files/documents.
     */
    sourceType: 'document';
    /**
     * The ID of the source.
     */
    id: string;
    /**
     * IANA media type of the document (e.g., 'application/pdf').
     */
    mediaType: string;
    /**
     * The title of the document.
     */
    title: string;
    /**
     * Optional filename of the document.
     */
    filename?: string;
    /**
     * Additional provider metadata for the source.
     */
    providerMetadata?: SharedV4ProviderMetadata;
};

/**
 * A source that has been used as input to generate the response.
 */
declare type LanguageModelV4Source_2 = {
    type: 'source';
    /**
     * The type of source - URL sources reference web content.
     */
    sourceType: 'url';
    /**
     * The ID of the source.
     */
    id: string;
    /**
     * The URL of the source.
     */
    url: string;
    /**
     * The title of the source.
     */
    title?: string;
    /**
     * Additional provider metadata for the source.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    type: 'source';
    /**
     * The type of source - document sources reference files/documents.
     */
    sourceType: 'document';
    /**
     * The ID of the source.
     */
    id: string;
    /**
     * IANA media type of the document (e.g., 'application/pdf').
     */
    mediaType: string;
    /**
     * The title of the document.
     */
    title: string;
    /**
     * Optional filename of the document.
     */
    filename?: string;
    /**
     * Additional provider metadata for the source.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
};

declare type LanguageModelV4StreamPart = {
    type: 'text-start';
    providerMetadata?: SharedV4ProviderMetadata;
    id: string;
} | {
    type: 'text-delta';
    id: string;
    providerMetadata?: SharedV4ProviderMetadata;
    delta: string;
} | {
    type: 'text-end';
    providerMetadata?: SharedV4ProviderMetadata;
    id: string;
} | {
    type: 'reasoning-start';
    providerMetadata?: SharedV4ProviderMetadata;
    id: string;
} | {
    type: 'reasoning-delta';
    id: string;
    providerMetadata?: SharedV4ProviderMetadata;
    delta: string;
} | {
    type: 'reasoning-end';
    id: string;
    providerMetadata?: SharedV4ProviderMetadata;
} | {
    type: 'tool-input-start';
    id: string;
    toolName: string;
    providerMetadata?: SharedV4ProviderMetadata;
    providerExecuted?: boolean;
    dynamic?: boolean;
    title?: string;
} | {
    type: 'tool-input-delta';
    id: string;
    delta: string;
    providerMetadata?: SharedV4ProviderMetadata;
} | {
    type: 'tool-input-end';
    id: string;
    providerMetadata?: SharedV4ProviderMetadata;
} | LanguageModelV4ToolApprovalRequest | LanguageModelV4ToolCall | LanguageModelV4ToolResult | LanguageModelV4CustomContent | LanguageModelV4File | LanguageModelV4ReasoningFile | LanguageModelV4Source | {
    type: 'stream-start';
    warnings: Array<SharedV4Warning>;
} | ({
    type: 'response-metadata';
} & LanguageModelV4ResponseMetadata) | {
    type: 'finish';
    usage: LanguageModelV4Usage;
    finishReason: LanguageModelV4FinishReason;
    providerMetadata?: SharedV4ProviderMetadata;
} | {
    type: 'raw';
    rawValue: unknown;
} | {
    type: 'error';
    error: unknown;
};

declare type LanguageModelV4StreamPart_2 = {
    type: 'text-start';
    providerMetadata?: SharedV4ProviderMetadata_2;
    id: string;
} | {
    type: 'text-delta';
    id: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
    delta: string;
} | {
    type: 'text-end';
    providerMetadata?: SharedV4ProviderMetadata_2;
    id: string;
} | {
    type: 'reasoning-start';
    providerMetadata?: SharedV4ProviderMetadata_2;
    id: string;
} | {
    type: 'reasoning-delta';
    id: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
    delta: string;
} | {
    type: 'reasoning-end';
    id: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    type: 'tool-input-start';
    id: string;
    toolName: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
    providerExecuted?: boolean;
    dynamic?: boolean;
    title?: string;
} | {
    type: 'tool-input-delta';
    id: string;
    delta: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    type: 'tool-input-end';
    id: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | LanguageModelV4ToolApprovalRequest_2 | LanguageModelV4ToolCall_2 | LanguageModelV4ToolResult_2 | LanguageModelV4CustomContent_2 | LanguageModelV4File_2 | LanguageModelV4ReasoningFile_2 | LanguageModelV4Source_2 | {
    type: 'stream-start';
    warnings: Array<SharedV4Warning_2>;
} | ({
    type: 'response-metadata';
} & LanguageModelV4ResponseMetadata_2) | {
    type: 'finish';
    usage: LanguageModelV4Usage_2;
    finishReason: LanguageModelV4FinishReason_2;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    type: 'raw';
    rawValue: unknown;
} | {
    type: 'error';
    error: unknown;
};

/**
 * The result of a language model doStream call.
 */
declare type LanguageModelV4StreamResult = {
    /**
     * The stream.
     */
    stream: ReadableStream<LanguageModelV4StreamPart>;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Request HTTP body that was sent to the provider API.
         */
        body?: unknown;
    };
    /**
     * Optional response data.
     */
    response?: {
        /**
         * Response headers.
         */
        headers?: SharedV4Headers;
    };
};

/**
 * The result of a language model doStream call.
 */
declare type LanguageModelV4StreamResult_2 = {
    /**
     * The stream.
     */
    stream: ReadableStream<LanguageModelV4StreamPart_2>;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Request HTTP body that was sent to the provider API.
         */
        body?: unknown;
    };
    /**
     * Optional response data.
     */
    response?: {
        /**
         * Response headers.
         */
        headers?: SharedV4Headers_2;
    };
};

/**
 * Text that the model has generated.
 */
declare type LanguageModelV4Text = {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    providerMetadata?: SharedV4ProviderMetadata;
};

/**
 * Text that the model has generated.
 */
declare type LanguageModelV4Text_2 = {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
};

/**
 * Text content part of a prompt. It contains a string of text.
 */
declare interface LanguageModelV4TextPart {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
}

/**
 * Text content part of a prompt. It contains a string of text.
 */
declare interface LanguageModelV4TextPart_2 {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
}

/**
 * Tool approval request emitted by a provider for a provider-executed tool call.
 *
 * This is used for flows where the provider executes the tool (e.g. MCP tools)
 * but requires an explicit user approval before continuing.
 */
declare type LanguageModelV4ToolApprovalRequest = {
    type: 'tool-approval-request';
    /**
     * ID of the approval request. This ID is referenced by the subsequent
     * tool-approval-response (tool message) to approve or deny execution.
     */
    approvalId: string;
    /**
     * The tool call ID that this approval request is for.
     */
    toolCallId: string;
    /**
     * Additional provider-specific metadata for the approval request.
     */
    providerMetadata?: SharedV4ProviderMetadata;
};

/**
 * Tool approval request emitted by a provider for a provider-executed tool call.
 *
 * This is used for flows where the provider executes the tool (e.g. MCP tools)
 * but requires an explicit user approval before continuing.
 */
declare type LanguageModelV4ToolApprovalRequest_2 = {
    type: 'tool-approval-request';
    /**
     * ID of the approval request. This ID is referenced by the subsequent
     * tool-approval-response (tool message) to approve or deny execution.
     */
    approvalId: string;
    /**
     * The tool call ID that this approval request is for.
     */
    toolCallId: string;
    /**
     * Additional provider-specific metadata for the approval request.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
};

/**
 * Tool approval response content part of a prompt. It contains the user's
 * decision to approve or deny a provider-executed tool call.
 */
declare interface LanguageModelV4ToolApprovalResponsePart {
    type: 'tool-approval-response';
    /**
     * ID of the approval request that this response refers to.
     */
    approvalId: string;
    /**
     * Whether the approval was granted (true) or denied (false).
     */
    approved: boolean;
    /**
     * Optional reason for approval or denial.
     */
    reason?: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
}

/**
 * Tool approval response content part of a prompt. It contains the user's
 * decision to approve or deny a provider-executed tool call.
 */
declare interface LanguageModelV4ToolApprovalResponsePart_2 {
    type: 'tool-approval-response';
    /**
     * ID of the approval request that this response refers to.
     */
    approvalId: string;
    /**
     * Whether the approval was granted (true) or denied (false).
     */
    approved: boolean;
    /**
     * Optional reason for approval or denial.
     */
    reason?: string;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
}

/**
 * Tool calls that the model has generated.
 */
declare type LanguageModelV4ToolCall = {
    type: 'tool-call';
    /**
     * The identifier of the tool call. It must be unique across all tool calls.
     */
    toolCallId: string;
    /**
     * The name of the tool that should be called.
     */
    toolName: string;
    /**
     * Stringified JSON object with the tool call arguments. Must match the
     * parameters schema of the tool.
     */
    input: string;
    /**
     * Whether the tool call will be executed by the provider.
     * If this flag is not set or is false, the tool call will be executed by the client.
     */
    providerExecuted?: boolean;
    /**
     * Whether the tool is dynamic, i.e. defined at runtime.
     * For example, MCP (Model Context Protocol) tools that are executed by the provider.
     */
    dynamic?: boolean;
    /**
     * Additional provider-specific metadata for the tool call.
     */
    providerMetadata?: SharedV4ProviderMetadata;
};

/**
 * Tool calls that the model has generated.
 */
declare type LanguageModelV4ToolCall_2 = {
    type: 'tool-call';
    /**
     * The identifier of the tool call. It must be unique across all tool calls.
     */
    toolCallId: string;
    /**
     * The name of the tool that should be called.
     */
    toolName: string;
    /**
     * Stringified JSON object with the tool call arguments. Must match the
     * parameters schema of the tool.
     */
    input: string;
    /**
     * Whether the tool call will be executed by the provider.
     * If this flag is not set or is false, the tool call will be executed by the client.
     */
    providerExecuted?: boolean;
    /**
     * Whether the tool is dynamic, i.e. defined at runtime.
     * For example, MCP (Model Context Protocol) tools that are executed by the provider.
     */
    dynamic?: boolean;
    /**
     * Additional provider-specific metadata for the tool call.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
};

/**
 * Tool call content part of a prompt. It contains a tool call (usually generated by the AI model).
 */
declare interface LanguageModelV4ToolCallPart {
    type: 'tool-call';
    /**
     * ID of the tool call. This ID is used to match the tool call with the tool result.
     */
    toolCallId: string;
    /**
     * Name of the tool that is being called.
     */
    toolName: string;
    /**
     * Arguments of the tool call. This is a JSON-serializable object that matches the tool's input schema.
     */
    input: unknown;
    /**
     * Whether the tool call will be executed by the provider.
     * If this flag is not set or is false, the tool call will be executed by the client.
     */
    providerExecuted?: boolean;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
}

/**
 * Tool call content part of a prompt. It contains a tool call (usually generated by the AI model).
 */
declare interface LanguageModelV4ToolCallPart_2 {
    type: 'tool-call';
    /**
     * ID of the tool call. This ID is used to match the tool call with the tool result.
     */
    toolCallId: string;
    /**
     * Name of the tool that is being called.
     */
    toolName: string;
    /**
     * Arguments of the tool call. This is a JSON-serializable object that matches the tool's input schema.
     */
    input: unknown;
    /**
     * Whether the tool call will be executed by the provider.
     * If this flag is not set or is false, the tool call will be executed by the client.
     */
    providerExecuted?: boolean;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
}

declare type LanguageModelV4ToolChoice = {
    type: 'auto';
} | {
    type: 'none';
} | {
    type: 'required';
} | {
    type: 'tool';
    toolName: string;
};

declare type LanguageModelV4ToolChoice_2 = {
    type: 'auto';
} | {
    type: 'none';
} | {
    type: 'required';
} | {
    type: 'tool';
    toolName: string;
};

/**
 * Result of a tool call that has been executed by the provider.
 */
declare type LanguageModelV4ToolResult = {
    type: 'tool-result';
    /**
     * The ID of the tool call that this result is associated with.
     */
    toolCallId: string;
    /**
     * Name of the tool that generated this result.
     */
    toolName: string;
    /**
     * Result of the tool call. This is a JSON-serializable object.
     */
    result: NonNullable<JSONValue_2>;
    /**
     * Optional flag if the result is an error or an error message.
     */
    isError?: boolean;
    /**
     * Whether the tool result is preliminary.
     *
     * Preliminary tool results replace each other, e.g. image previews.
     * There always has to be a final, non-preliminary tool result.
     *
     * If this flag is set to true, the tool result is preliminary.
     * If this flag is not set or is false, the tool result is not preliminary.
     */
    preliminary?: boolean;
    /**
     * Whether the tool is dynamic, i.e. defined at runtime.
     * For example, MCP (Model Context Protocol) tools that are executed by the provider.
     */
    dynamic?: boolean;
    /**
     * Additional provider-specific metadata for the tool result.
     */
    providerMetadata?: SharedV4ProviderMetadata;
};

/**
 * Result of a tool call that has been executed by the provider.
 */
declare type LanguageModelV4ToolResult_2 = {
    type: 'tool-result';
    /**
     * The ID of the tool call that this result is associated with.
     */
    toolCallId: string;
    /**
     * Name of the tool that generated this result.
     */
    toolName: string;
    /**
     * Result of the tool call. This is a JSON-serializable object.
     */
    result: NonNullable<JSONValue_3>;
    /**
     * Optional flag if the result is an error or an error message.
     */
    isError?: boolean;
    /**
     * Whether the tool result is preliminary.
     *
     * Preliminary tool results replace each other, e.g. image previews.
     * There always has to be a final, non-preliminary tool result.
     *
     * If this flag is set to true, the tool result is preliminary.
     * If this flag is not set or is false, the tool result is not preliminary.
     */
    preliminary?: boolean;
    /**
     * Whether the tool is dynamic, i.e. defined at runtime.
     * For example, MCP (Model Context Protocol) tools that are executed by the provider.
     */
    dynamic?: boolean;
    /**
     * Additional provider-specific metadata for the tool result.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
};

/**
 * Result of a tool call.
 */
declare type LanguageModelV4ToolResultOutput = {
    /**
     * Text tool output that should be directly sent to the API.
     */
    type: 'text';
    value: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions;
} | {
    type: 'json';
    value: JSONValue_2;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions;
} | {
    /**
     * Type when the user has denied the execution of the tool call.
     */
    type: 'execution-denied';
    /**
     * Optional reason for the execution denial.
     */
    reason?: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions;
} | {
    type: 'error-text';
    value: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions;
} | {
    type: 'error-json';
    value: JSONValue_2;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions;
} | {
    type: 'content';
    value: Array<{
        type: 'text';
        /**
         * Text content.
         */
        text: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV4ProviderOptions;
    } | {
        type: 'file';
        /**
         * File data as a tagged discriminated union:
         *
         * - `{ type: 'data', data }`: raw bytes (Uint8Array) or base64-encoded string.
         * - `{ type: 'url', url }`: a URL that points to the file.
         * - `{ type: 'reference', reference }`: a provider reference (`{ [provider]: id }`).
         * - `{ type: 'text', text }`: inline text content (e.g. an inline text document).
         */
        data: SharedV4FileData;
        /**
         * Either a full IANA media type (`type/subtype`, e.g. `image/png`) or just
         * the top-level IANA segment (e.g. `image`, `audio`, `video`, `text`).
         *
         * `*`-subtype wildcards (e.g. `image/*`) are normalized as equivalent to the
         * top-level segment alone (e.g. `image`). Providers can use the helpers in
         * `@ai-sdk/provider-utils` (`isFullMediaType`, `getTopLevelMediaType`,
         * `detectMediaType`) to resolve the field according to their API
         * requirements.
         *
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
        /**
         * Optional filename of the file.
         */
        filename?: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV4ProviderOptions;
    } | {
        /**
         * Custom content part. This can be used to implement
         * provider-specific content parts.
         */
        type: 'custom';
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV4ProviderOptions;
    }>;
};

/**
 * Result of a tool call.
 */
declare type LanguageModelV4ToolResultOutput_2 = {
    /**
     * Text tool output that should be directly sent to the API.
     */
    type: 'text';
    value: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions_2;
} | {
    type: 'json';
    value: JSONValue_3;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions_2;
} | {
    /**
     * Type when the user has denied the execution of the tool call.
     */
    type: 'execution-denied';
    /**
     * Optional reason for the execution denial.
     */
    reason?: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions_2;
} | {
    type: 'error-text';
    value: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions_2;
} | {
    type: 'error-json';
    value: JSONValue_3;
    /**
     * Provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions_2;
} | {
    type: 'content';
    value: Array<{
        type: 'text';
        /**
         * Text content.
         */
        text: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV4ProviderOptions_2;
    } | {
        type: 'file';
        /**
         * File data as a tagged discriminated union:
         *
         * - `{ type: 'data', data }`: raw bytes (Uint8Array) or base64-encoded string.
         * - `{ type: 'url', url }`: a URL that points to the file.
         * - `{ type: 'reference', reference }`: a provider reference (`{ [provider]: id }`).
         * - `{ type: 'text', text }`: inline text content (e.g. an inline text document).
         */
        data: SharedV4FileData_2;
        /**
         * Either a full IANA media type (`type/subtype`, e.g. `image/png`) or just
         * the top-level IANA segment (e.g. `image`, `audio`, `video`, `text`).
         *
         * `*`-subtype wildcards (e.g. `image/*`) are normalized as equivalent to the
         * top-level segment alone (e.g. `image`). Providers can use the helpers in
         * `@ai-sdk/provider-utils` (`isFullMediaType`, `getTopLevelMediaType`,
         * `detectMediaType`) to resolve the field according to their API
         * requirements.
         *
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
        /**
         * Optional filename of the file.
         */
        filename?: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV4ProviderOptions_2;
    } | {
        /**
         * Custom content part. This can be used to implement
         * provider-specific content parts.
         */
        type: 'custom';
        /**
         * Provider-specific options.
         */
        providerOptions?: SharedV4ProviderOptions_2;
    }>;
};

/**
 * Tool result content part of a prompt. It contains the result of the tool call with the matching ID.
 */
declare interface LanguageModelV4ToolResultPart {
    type: 'tool-result';
    /**
     * ID of the tool call that this result is associated with.
     */
    toolCallId: string;
    /**
     * Name of the tool that generated this result.
     */
    toolName: string;
    /**
     * Result of the tool call.
     */
    output: LanguageModelV4ToolResultOutput;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
}

/**
 * Tool result content part of a prompt. It contains the result of the tool call with the matching ID.
 */
declare interface LanguageModelV4ToolResultPart_2 {
    type: 'tool-result';
    /**
     * ID of the tool call that this result is associated with.
     */
    toolCallId: string;
    /**
     * Name of the tool that generated this result.
     */
    toolName: string;
    /**
     * Result of the tool call.
     */
    output: LanguageModelV4ToolResultOutput_2;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
}

/**
 * Usage information for a language model call.
 */
declare type LanguageModelV4Usage = {
    /**
     * Information about the input tokens.
     */
    inputTokens: {
        /**
         * The total number of input (prompt) tokens used.
         */
        total: number | undefined;
        /**
         * The number of non-cached input (prompt) tokens used.
         */
        noCache: number | undefined;
        /**
         * The number of cached input (prompt) tokens read.
         */
        cacheRead: number | undefined;
        /**
         * The number of cached input (prompt) tokens written.
         */
        cacheWrite: number | undefined;
    };
    /**
     * Information about the output tokens.
     */
    outputTokens: {
        /**
         * The total number of output (completion) tokens used.
         */
        total: number | undefined;
        /**
         * The number of text tokens used.
         */
        text: number | undefined;
        /**
         * The number of reasoning tokens used.
         */
        reasoning: number | undefined;
    };
    /**
     * Raw usage information from the provider.
     *
     * This is the usage information in the shape that the provider returns.
     * It can include additional information that is not part of the standard usage information.
     */
    raw?: JSONObject;
};

/**
 * Usage information for a language model call.
 */
declare type LanguageModelV4Usage_2 = {
    /**
     * Information about the input tokens.
     */
    inputTokens: {
        /**
         * The total number of input (prompt) tokens used.
         */
        total: number | undefined;
        /**
         * The number of non-cached input (prompt) tokens used.
         */
        noCache: number | undefined;
        /**
         * The number of cached input (prompt) tokens read.
         */
        cacheRead: number | undefined;
        /**
         * The number of cached input (prompt) tokens written.
         */
        cacheWrite: number | undefined;
    };
    /**
     * Information about the output tokens.
     */
    outputTokens: {
        /**
         * The total number of output (completion) tokens used.
         */
        total: number | undefined;
        /**
         * The number of text tokens used.
         */
        text: number | undefined;
        /**
         * The number of reasoning tokens used.
         */
        reasoning: number | undefined;
    };
    /**
     * Raw usage information from the provider.
     *
     * This is the usage information in the shape that the provider returns.
     * It can include additional information that is not part of the standard usage information.
     */
    raw?: JSONObject_2;
};

/**
 * Check if the last message is an assistant message with completed tool call approvals.
 * The last step of the message must have at least one tool approval response and
 * all tool approvals must have a response.
 */
export declare function lastAssistantMessageIsCompleteWithApprovalResponses({ messages, }: {
    messages: UIMessage[];
}): boolean;

/**
 * Check if the last message is an assistant message with completed tool calls.
 * The last step of the message must have at least one tool invocation and
 * all tool invocations must have a result.
 */
export declare function lastAssistantMessageIsCompleteWithToolCalls({ messages, }: {
    messages: UIMessage[];
}): boolean;

declare type LazySchema<SCHEMA> = () => Schema_2<SCHEMA>;

/**
 * Creates a schema with deferred creation.
 * This is important to reduce the startup time of the library
 * and to avoid initializing unused validators.
 *
 * @param createValidator A function that creates a schema.
 * @returns A function that returns a schema.
 */
declare function lazySchema<SCHEMA>(createSchema: () => Schema_2<SCHEMA>): LazySchema<SCHEMA>;

declare type LazySchema_2<SCHEMA> = () => Schema<SCHEMA>;

/**
 * Creates a schema with deferred creation.
 * This is important to reduce the startup time of the library
 * and to avoid initializing unused validators.
 *
 * @param createValidator A function that creates a schema.
 * @returns A function that returns a schema.
 */
declare function lazySchema_2<SCHEMA>(createSchema: () => Schema<SCHEMA>): LazySchema_2<SCHEMA>;

declare function loadApiKey({ apiKey, environmentVariableName, apiKeyParameterName, description, }: {
    apiKey: string | undefined;
    environmentVariableName: string;
    apiKeyParameterName?: string;
    description: string;
}): string;

declare function loadApiKey_2({ apiKey, environmentVariableName, apiKeyParameterName, description, }: {
    apiKey: string | undefined;
    environmentVariableName: string;
    apiKeyParameterName?: string;
    description: string;
}): string;

export declare class LoadAPIKeyError extends AISDKError {
    private readonly [symbol$7];
    constructor({ message }: {
        message: string;
    });
    static isInstance(error: unknown): error is LoadAPIKeyError;
}

/**
 * Loads an optional `string` setting from the environment or a parameter.
 *
 * @param settingValue - The setting value.
 * @param environmentVariableName - The environment variable name.
 * @returns The setting value.
 */
declare function loadOptionalSetting({ settingValue, environmentVariableName, }: {
    settingValue: string | undefined;
    environmentVariableName: string;
}): string | undefined;

/**
 * Loads an optional `string` setting from the environment or a parameter.
 *
 * @param settingValue - The setting value.
 * @param environmentVariableName - The environment variable name.
 * @returns The setting value.
 */
declare function loadOptionalSetting_2({ settingValue, environmentVariableName, }: {
    settingValue: string | undefined;
    environmentVariableName: string;
}): string | undefined;

/**
 * Loads a `string` setting from the environment or a parameter.
 *
 * @param settingValue - The setting value.
 * @param environmentVariableName - The environment variable name.
 * @param settingName - The setting name.
 * @param description - The description of the setting.
 * @returns The setting value.
 */
declare function loadSetting({ settingValue, environmentVariableName, settingName, description, }: {
    settingValue: string | undefined;
    environmentVariableName: string;
    settingName: string;
    description: string;
}): string;

/**
 * Loads a `string` setting from the environment or a parameter.
 *
 * @param settingValue - The setting value.
 * @param environmentVariableName - The environment variable name.
 * @param settingName - The setting name.
 * @param description - The description of the setting.
 * @returns The setting value.
 */
declare function loadSetting_2({ settingValue, environmentVariableName, settingName, description, }: {
    settingValue: string | undefined;
    environmentVariableName: string;
    settingName: string;
    description: string;
}): string;

export declare class LoadSettingError extends AISDKError {
    private readonly [symbol$6];
    constructor({ message }: {
        message: string;
    });
    static isInstance(error: unknown): error is LoadSettingError;
}

/**
 * A function for logging warnings.
 *
 * You can assign it to the `AI_SDK_LOG_WARNINGS` global variable to use it as the default warning logger.
 *
 * @example
 * ```ts
 * globalThis.AI_SDK_LOG_WARNINGS = (options) => {
 *   console.log('WARNINGS:', options.warnings, options.provider, options.model);
 * };
 * ```
 */
declare type LogWarningsFunction = (options: {
    /**
     * The warnings returned by the model provider.
     */
    warnings: Warning[];
    /**
     * The provider id used for the call, if scoped to a specific provider.
     */
    provider?: string;
    /**
     * The model id used for the call, if scoped to a specific provider.
     */
    model?: string;
}) => void;
export { LogWarningsFunction as Experimental_LogWarningsFunction }
export { LogWarningsFunction }

/**
 * Maps a top-level reasoning level to an absolute token budget by multiplying
 * the model's max output tokens by a percentage from the budget percentages
 * map. The result is clamped between `minReasoningBudget` (default 1024) and
 * `maxReasoningBudget`. Pushes an unsupported warning if the level is not
 * present in the budget percentages map.
 *
 * @returns The computed token budget, or `undefined` if the level is not
 *   supported.
 */
declare function mapReasoningToProviderBudget({ reasoning, maxOutputTokens, maxReasoningBudget, minReasoningBudget, budgetPercentages, warnings, }: {
    reasoning: ReasoningLevel;
    maxOutputTokens: number;
    maxReasoningBudget: number;
    minReasoningBudget?: number;
    budgetPercentages?: Partial<Record<ReasoningLevel, number>>;
    warnings: SharedV4Warning[];
}): number | undefined;

/**
 * Maps a top-level reasoning level to an absolute token budget by multiplying
 * the model's max output tokens by a percentage from the budget percentages
 * map. The result is clamped between `minReasoningBudget` (default 1024) and
 * `maxReasoningBudget`. Pushes an unsupported warning if the level is not
 * present in the budget percentages map.
 *
 * @returns The computed token budget, or `undefined` if the level is not
 *   supported.
 */
declare function mapReasoningToProviderBudget_2({ reasoning, maxOutputTokens, maxReasoningBudget, minReasoningBudget, budgetPercentages, warnings, }: {
    reasoning: ReasoningLevel_2;
    maxOutputTokens: number;
    maxReasoningBudget: number;
    minReasoningBudget?: number;
    budgetPercentages?: Partial<Record<ReasoningLevel_2, number>>;
    warnings: SharedV4Warning[];
}): number | undefined;

/**
 * Maps a top-level reasoning level to a provider-specific effort string using
 * the given effort map. Pushes a compatibility warning if the reasoning level
 * maps to a different string, or an unsupported warning if the level is not
 * present in the map.
 *
 * @returns The mapped effort string, or `undefined` if the level is not
 *   supported.
 */
declare function mapReasoningToProviderEffort<T extends string>({ reasoning, effortMap, warnings, }: {
    reasoning: ReasoningLevel;
    effortMap: Partial<Record<ReasoningLevel, T>>;
    warnings: SharedV4Warning[];
}): T | undefined;

/**
 * Maps a top-level reasoning level to a provider-specific effort string using
 * the given effort map. Pushes a compatibility warning if the reasoning level
 * maps to a different string, or an unsupported warning if the level is not
 * present in the map.
 *
 * @returns The mapped effort string, or `undefined` if the level is not
 *   supported.
 */
declare function mapReasoningToProviderEffort_2<T extends string>({ reasoning, effortMap, warnings, }: {
    reasoning: ReasoningLevel_2;
    effortMap: Partial<Record<ReasoningLevel_2, T>>;
    warnings: SharedV4Warning[];
}): T | undefined;

/**
 * A value that can be provided either synchronously or as a promise-like.
 */
declare type MaybePromiseLike<T> = T | PromiseLike<T>;

/**
 * A value that can be provided either synchronously or as a promise-like.
 */
declare type MaybePromiseLike_2<T> = T | PromiseLike<T>;

/**
 * Maps a media type to its corresponding file extension.
 * It was originally introduced to set a filename for audio file uploads
 * in https://github.com/vercel/ai/pull/8159.
 *
 * @param mediaType The media type to map.
 * @returns The corresponding file extension
 * @see https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/MIME_types/Common_types
 */
declare function mediaTypeToExtension(mediaType: string): string;

/**
 * Maps a media type to its corresponding file extension.
 * It was originally introduced to set a filename for audio file uploads
 * in https://github.com/vercel/ai/pull/8159.
 *
 * @param mediaType The media type to map.
 * @returns The corresponding file extension
 * @see https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/MIME_types/Common_types
 */
declare function mediaTypeToExtension_2(mediaType: string): string;

export declare class MessageConversionError extends AISDKError {
    private readonly [symbol$2_2];
    readonly originalMessage: Omit<UIMessage, 'id'>;
    constructor({ originalMessage, message, }: {
        originalMessage: Omit<UIMessage, 'id'>;
        message: string;
    });
    static isInstance(error: unknown): error is MessageConversionError;
}

export declare class MissingToolResultsError extends AISDKError {
    private readonly [symbol$e_3];
    readonly toolCallIds: string[];
    constructor({ toolCallIds }: {
        toolCallIds: string[];
    });
    static isInstance(error: unknown): error is MissingToolResultsError;
}

/**
 * Common model information used across callback events.
 */
export declare type ModelInfo = {
    /** The provider identifier (e.g., 'openai', 'anthropic'). */
    readonly provider: string;
    /** The specific model identifier (e.g., 'gpt-4o'). */
    readonly modelId: string;
};

/**
 * A message that can be used in the `messages` field of a prompt.
 * It can be a user message, an assistant message, or a tool message.
 */
export declare type ModelMessage = SystemModelMessage | UserModelMessage | AssistantModelMessage | ToolModelMessage;

/**
 * A message that can be used in the `messages` field of a prompt.
 * It can be a user message, an assistant message, or a tool message.
 */
declare type ModelMessage_2 = SystemModelMessage_2 | UserModelMessage_2 | AssistantModelMessage_2 | ToolModelMessage_2;

export declare const modelMessageSchema: z.ZodType<ModelMessage>;

declare type NeverOptional<N, T> = 0 extends 1 & N ? Partial<T> : [N] extends [never] ? Partial<Record<keyof T, undefined>> : T;

declare type NeverOptional_2<N, T> = 0 extends 1 & N ? Partial<T> : [N] extends [never] ? Partial<Record<keyof T, undefined>> : T;

/**
 * Thrown when the AI provider fails to generate any content.
 */
export declare class NoContentGeneratedError extends AISDKError {
    private readonly [symbol$5];
    constructor({ message, }?: {
        message?: string;
    });
    static isInstance(error: unknown): error is NoContentGeneratedError;
}

/**
 * Thrown when no image could be generated. This can have multiple causes:
 *
 * - The model failed to generate a response.
 * - The model generated a response that could not be parsed.
 */
export declare class NoImageGeneratedError extends AISDKError {
    private readonly [symbol$d_3];
    /**
     * The response metadata for each call.
     */
    readonly responses: Array<ImageModelResponseMetadata> | undefined;
    constructor({ message, cause, responses, }: {
        message?: string;
        cause?: Error;
        responses?: Array<ImageModelResponseMetadata>;
    });
    static isInstance(error: unknown): error is NoImageGeneratedError;
}

/**
 * Thrown when no object could be generated. This can have several causes:
 *
 * - The model failed to generate a response.
 * - The model generated a response that could not be parsed.
 * - The model generated a response that could not be validated against the schema.
 *
 * The error contains the following properties:
 *
 * - `text`: The text that was generated by the model. This can be the raw text or the tool call text, depending on the model.
 */
export declare class NoObjectGeneratedError extends AISDKError {
    private readonly [symbol$c_2];
    /**
     * The text that was generated by the model. This can be the raw text or the tool call text, depending on the model.
     */
    readonly text: string | undefined;
    /**
     * The response metadata.
     */
    readonly response: Omit<LanguageModelResponseMetadata, 'messages'> | undefined;
    /**
     * The usage of the model.
     */
    readonly usage: LanguageModelUsage | undefined;
    /**
     * Reason why the model finished generating a response.
     */
    readonly finishReason: FinishReason | undefined;
    constructor({ message, cause, text, response, usage, finishReason, }: {
        message?: string;
        cause?: Error;
        text?: string;
        response: Omit<LanguageModelResponseMetadata, 'messages'>;
        usage: LanguageModelUsage;
        finishReason: FinishReason;
    });
    static isInstance(error: unknown): error is NoObjectGeneratedError;
}

/**
 * Thrown when no LLM output was generated, e.g. because of errors.
 */
export declare class NoOutputGeneratedError extends AISDKError {
    private readonly [symbol$b];
    constructor({ message, cause, }?: {
        message?: string;
        cause?: Error;
    });
    static isInstance(error: unknown): error is NoOutputGeneratedError;
}

/**
 * Flattens intersected mapped types so type equality assertions and editor
 * hovers show the resulting object shape.
 */
declare type Normalize<OBJECT> = {
    [KEY in keyof OBJECT]: OBJECT[KEY];
};

/**
 * Flattens intersected mapped types so type equality assertions and editor
 * hovers show the resulting object shape.
 */
declare type Normalize_2<OBJECT> = {
    [KEY in keyof OBJECT]: OBJECT[KEY];
};

/**
 * Normalizes different header inputs into a plain record with lower-case keys.
 * Entries with `undefined` or `null` values are removed.
 *
 * @param headers - Input headers (`Headers`, tuples array, plain record) to normalize.
 * @returns A record containing the normalized header entries.
 */
declare function normalizeHeaders(headers: HeadersInit | Record<string, string | undefined> | Array<[string, string | undefined]> | undefined): Record<string, string>;

/**
 * Normalizes different header inputs into a plain record with lower-case keys.
 * Entries with `undefined` or `null` values are removed.
 *
 * @param headers - Input headers (`Headers`, tuples array, plain record) to normalize.
 * @returns A record containing the normalized header entries.
 */
declare function normalizeHeaders_2(headers: HeadersInit | Record<string, string | undefined> | Array<[string, string | undefined]> | undefined): Record<string, string>;

/**
 * Error that is thrown when no speech audio was generated.
 */
export declare class NoSpeechGeneratedError extends AISDKError {
    private readonly [symbol$a_2];
    readonly responses: Array<SpeechModelResponseMetadata>;
    constructor(options: {
        responses: Array<SpeechModelResponseMetadata>;
    });
    static isInstance(error: unknown): error is NoSpeechGeneratedError;
}

export declare class NoSuchModelError extends AISDKError {
    private readonly [symbol$4];
    readonly modelId: string;
    readonly modelType: 'languageModel' | 'embeddingModel' | 'imageModel' | 'transcriptionModel' | 'speechModel' | 'rerankingModel' | 'videoModel';
    constructor({ errorName, modelId, modelType, message, }: {
        errorName?: string;
        modelId: string;
        modelType: 'languageModel' | 'embeddingModel' | 'imageModel' | 'transcriptionModel' | 'speechModel' | 'rerankingModel' | 'videoModel';
        message?: string;
    });
    static isInstance(error: unknown): error is NoSuchModelError;
}

export declare class NoSuchProviderError extends NoSuchModelError {
    private readonly [symbol_3];
    readonly providerId: string;
    readonly availableProviders: string[];
    constructor({ modelId, modelType, providerId, availableProviders, message, }: {
        modelId: string;
        modelType: 'languageModel' | 'embeddingModel' | 'imageModel' | 'transcriptionModel' | 'speechModel' | 'rerankingModel' | 'videoModel';
        providerId: string;
        availableProviders: string[];
        message?: string;
    });
    static isInstance(error: unknown): error is NoSuchProviderError;
}

/**
 * Thrown when a provider reference cannot be resolved because the specified
 * provider is not found in the provider reference mapping.
 */
export declare class NoSuchProviderReferenceError extends AISDKError {
    private readonly [symbol$3];
    readonly provider: string;
    readonly reference: SharedV4ProviderReference;
    constructor({ provider, reference, message, }: {
        provider: string;
        reference: SharedV4ProviderReference;
        message?: string;
    });
    static isInstance(error: unknown): error is NoSuchProviderReferenceError;
}

export declare class NoSuchToolError extends AISDKError {
    private readonly [symbol$k];
    readonly toolName: string;
    readonly availableTools: string[] | undefined;
    constructor({ toolName, availableTools, message, }: {
        toolName: string;
        availableTools?: string[] | undefined;
        message?: string;
    });
    static isInstance(error: unknown): error is NoSuchToolError;
}

/**
 * Error that is thrown when no transcript was generated.
 */
export declare class NoTranscriptGeneratedError extends AISDKError {
    private readonly [symbol$9_2];
    readonly responses: Array<TranscriptionModelResponseMetadata>;
    constructor(options: {
        responses: Array<TranscriptionModelResponseMetadata>;
    });
    static isInstance(error: unknown): error is NoTranscriptGeneratedError;
}

/**
 * Error that is thrown when no translation was generated.
 */
export declare class NoTranslationGeneratedError extends AISDKError {
    private readonly [symbol$8_3];
    readonly response: SpeechTranslationModelResponseMetadata;
    constructor(options: {
        response: SpeechTranslationModelResponseMetadata;
    });
    static isInstance(error: unknown): error is NoTranslationGeneratedError;
}

export declare class NoVideoGeneratedError extends AISDKError {
    private readonly [symbol$7_2];
    readonly responses: Array<VideoModelResponseMetadata>;
    constructor({ message, cause, responses, }: {
        message?: string;
        cause?: unknown;
        responses: Array<VideoModelResponseMetadata>;
    });
    static isInstance(error: unknown): error is NoVideoGeneratedError;
    /**
     * @deprecated use `isInstance` instead
     */
    static isNoVideoGeneratedError(error: unknown): error is NoVideoGeneratedError;
    /**
     * @deprecated Do not use this method. It will be removed in the next major version.
     */
    toJSON(): {
        name: string;
        message: string;
        stack: string | undefined;
        cause: unknown;
        responses: VideoModelResponseMetadata[];
    };
}

export declare type ObjectStreamPart<PARTIAL> = {
    type: 'object';
    object: PARTIAL;
} | {
    type: 'text-delta';
    textDelta: string;
} | {
    type: 'error';
    error: unknown;
} | {
    type: 'finish';
    finishReason: FinishReason;
    usage: LanguageModelUsage;
    response: Omit<LanguageModelResponseMetadata, 'messages'>;
    providerMetadata?: ProviderMetadata;
};

/** @deprecated Use `GenerateTextEndEvent` instead. */
export declare type OnFinishEvent<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = GenerateTextEndEvent<TOOLS, RUNTIME_CONTEXT>;

/**
 * Callback that is set using the `onLanguageModelCallEnd` option.
 *
 * Called after the model response has been normalized and parsed, but before
 * any client-side tool execution begins.
 *
 * @param event - The event object containing model-call-specific outputs.
 */
export declare type OnLanguageModelCallEndCallback<TOOLS extends ToolSet = ToolSet> = Callback<LanguageModelCallEndEvent<TOOLS>>;

/**
 * Callback that is set using the `onLanguageModelCallStart` option.
 *
 * Called immediately before the provider model call begins.
 * Unlike step-start callbacks, this is scoped to model work only and
 * excludes any later client-side tool execution.
 *
 * @param event - The event object containing model-call-specific inputs.
 */
export declare type OnLanguageModelCallStartCallback = Callback<LanguageModelCallStartEvent>;

/** @deprecated Use `GenerateTextStartEvent` instead. */
export declare type OnStartEvent<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = Output_2> = GenerateTextStartEvent<TOOLS, RUNTIME_CONTEXT, OUTPUT>;

/** @deprecated Use `GenerateTextStepEndEvent` instead. */
export declare type OnStepFinishEvent<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = GenerateTextStepEndEvent<TOOLS, RUNTIME_CONTEXT>;

/** @deprecated Use `GenerateTextStepStartEvent` instead. */
export declare type OnStepStartEvent<TOOLS extends ToolSet = ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = Output_2> = GenerateTextStepStartEvent<TOOLS, RUNTIME_CONTEXT, OUTPUT>;

/** @deprecated Use `ToolExecutionEndEvent` instead. */
export declare type OnToolCallFinishEvent<TOOLS extends ToolSet = ToolSet> = ToolExecutionEndEvent<TOOLS>;

/** @deprecated Use `ToolExecutionStartEvent` instead. */
export declare type OnToolCallStartEvent<TOOLS extends ToolSet = ToolSet> = ToolExecutionStartEvent<TOOLS>;

/**
 * Callback that is set using the `onToolExecutionEnd` option.
 *
 * Called when a tool execution completes, either successfully or with an error.
 * Use this for logging tool results, tracking execution time, or error handling.
 *
 * The event uses a discriminated union on `toolOutput.type`:
 * - When `toolOutput.type === 'tool-result'`: `toolOutput.output` contains the tool result.
 * - When `toolOutput.type === 'tool-error'`: `toolOutput.error` contains the error.
 *
 * @param event - The event object containing tool call result information.
 */
export declare type OnToolExecutionEndCallback<TOOLS extends ToolSet = ToolSet> = Callback<ToolExecutionEndEvent<TOOLS>>;

/**
 * Callback that is set using the `onToolExecutionStart` option.
 *
 * Called when a tool execution begins, before the tool's `execute` function is invoked.
 * Use this for logging tool invocations, tracking tool usage, or pre-execution validation.
 *
 * @param event - The event object containing tool call information.
 */
export declare type OnToolExecutionStartCallback<TOOLS extends ToolSet = ToolSet> = Callback<ToolExecutionStartEvent<TOOLS>>;

declare type OperationEndEvent = GenerateTextEndEvent<ToolSet> | GenerateObjectEndEvent<unknown> | EmbedEndEvent | RerankEndEvent;

declare type OperationStartEvent = GenerateTextStartEvent | GenerateObjectStartEvent | EmbedStartEvent | RerankStartEvent;

/**
 * Builds the optional portion of the tool context map for tools whose context
 * object itself may be `undefined`.
 */
declare type OptionalToolSetContext<TOOLS extends ToolSet_2> = {
    [K in keyof TOOLS as InferToolContext<NoInfer<TOOLS[K]>> extends never ? never : undefined extends InferToolContext<NoInfer<TOOLS[K]>> ? K : never]?: InferToolContext<NoInfer<TOOLS[K]>>;
};

/**
 * Builds the optional portion of the tool context map for tools whose context
 * object itself may be `undefined`.
 */
declare type OptionalToolSetContext_2<TOOLS extends ToolSet> = {
    [K in keyof TOOLS as InferToolContext_2<NoInfer<TOOLS[K]>> extends never ? never : undefined extends InferToolContext_2<NoInfer<TOOLS[K]>> ? K : never]?: InferToolContext_2<NoInfer<TOOLS[K]>>;
};

export declare namespace Output {
    export {
        output_Output as Output,
        output_array as array,
        output_choice as choice,
        output_json as json,
        output_object as object,
        output_text as text,
    };
}

declare interface Output_2<OUTPUT = any, PARTIAL = any, ELEMENT = any> {
    /**
     * The name of the output mode.
     */
    name: string;
    /**
     * The response format to use for the model.
     */
    responseFormat: PromiseLike<LanguageModelV4CallOptions_2['responseFormat']>;
    /**
     * Parses the complete output of the model.
     */
    parseCompleteOutput(options: {
        text: string;
    }, context: {
        response: Omit<LanguageModelResponseMetadata, 'messages' | 'body'>;
        usage: LanguageModelUsage;
        finishReason: FinishReason;
    }): Promise<OUTPUT>;
    /**
     * Parses the partial output of the model.
     */
    parsePartialOutput(options: {
        text: string;
    }): Promise<{
        partial: PARTIAL;
    } | undefined>;
    /**
     * Creates a stream transform that emits individual elements as they complete.
     */
    createElementStreamTransform(): TransformStream<EnrichedStreamPart<any, PARTIAL>, ELEMENT> | undefined;
}

/**
 * Timing statistics for the gaps between generated output chunks.
 */
export declare type OutputChunkTimingStats = {
    /** Shortest observed time between output chunks in milliseconds. */
    readonly min: number;
    /** 10th percentile time between output chunks in milliseconds. */
    readonly p10: number;
    /** Median time between output chunks in milliseconds. */
    readonly median: number;
    /** Average time between output chunks in milliseconds. */
    readonly avg: number;
    /** 90th percentile time between output chunks in milliseconds. */
    readonly p90: number;
    /** Longest observed time between output chunks in milliseconds. */
    readonly max: number;
};

declare interface ParallelSearchConfig {
    /**
     * Mode preset for different use cases:
     * - "one-shot": Comprehensive results with longer excerpts for single-response answers (default)
     * - "agentic": Concise, token-efficient results for multi-step agentic workflows
     */
    mode?: 'one-shot' | 'agentic';
    /**
     * Default maximum number of results to return (1-20).
     * Defaults to 10 if not specified.
     */
    maxResults?: number;
    /**
     * Default source policy for controlling which domains to include/exclude.
     */
    sourcePolicy?: ParallelSearchSourcePolicy;
    /**
     * Default excerpt configuration for controlling result length.
     */
    excerpts?: ParallelSearchExcerpts;
    /**
     * Default fetch policy for controlling content freshness.
     */
    fetchPolicy?: ParallelSearchFetchPolicy;
}

declare interface ParallelSearchError {
    /** Error type */
    error: 'api_error' | 'rate_limit' | 'timeout' | 'invalid_input' | 'configuration_error' | 'unknown';
    /** HTTP status code if applicable */
    statusCode?: number;
    /** Human-readable error message */
    message: string;
}

declare interface ParallelSearchExcerpts {
    /**
     * Maximum characters per result.
     */
    maxCharsPerResult?: number;
    /**
     * Maximum total characters across all results.
     */
    maxCharsTotal?: number;
}

declare interface ParallelSearchFetchPolicy {
    /**
     * Maximum age in seconds for cached content.
     * Set to 0 to always fetch fresh content.
     */
    maxAgeSeconds?: number;
}

declare interface ParallelSearchInput {
    /**
     * Natural-language description of the web research goal.
     * Include source or freshness guidance and broader context from the task.
     * Maximum 5000 characters.
     */
    objective: string;
    /**
     * Optional search queries to supplement the objective.
     * Maximum 200 characters per query.
     */
    search_queries?: string[];
    /**
     * Mode preset for different use cases:
     * - "one-shot": Comprehensive results with longer excerpts
     * - "agentic": Concise, token-efficient results for multi-step workflows
     */
    mode?: 'one-shot' | 'agentic';
    /**
     * Maximum number of results to return (1-20).
     * Defaults to 10 if not specified.
     */
    max_results?: number;
    /**
     * Source policy for controlling which domains to include/exclude.
     */
    source_policy?: {
        include_domains?: string[];
        exclude_domains?: string[];
        after_date?: string;
    };
    /**
     * Excerpt configuration for controlling result length.
     */
    excerpts?: {
        max_chars_per_result?: number;
        max_chars_total?: number;
    };
    /**
     * Fetch policy for controlling content freshness.
     */
    fetch_policy?: {
        max_age_seconds?: number;
    };
}

declare type ParallelSearchOutput = ParallelSearchResponse | ParallelSearchError;

declare interface ParallelSearchResponse {
    /** Unique identifier for this search request */
    searchId: string;
    /** Array of search results */
    results: ParallelSearchResult[];
}

declare interface ParallelSearchResult {
    /** URL of the search result */
    url: string;
    /** Title of the search result */
    title: string;
    /** Extracted text excerpt/content from the page */
    excerpt: string;
    /** Publication date of the content (may be null) */
    publishDate?: string | null;
    /** Relevance score for the result */
    relevanceScore?: number;
}

declare interface ParallelSearchSourcePolicy {
    /**
     * List of domains to include in search results.
     * Example: ['wikipedia.org', 'nature.com']
     */
    includeDomains?: string[];
    /**
     * List of domains to exclude from search results.
     * Example: ['reddit.com', 'twitter.com']
     */
    excludeDomains?: string[];
    /**
     * Only include results published after this date (ISO 8601 format).
     * Example: '2024-01-01'
     */
    afterDate?: string;
}

declare const parallelSearchToolFactory: _ai_sdk_provider_utils.ProviderExecutedToolFactory<ParallelSearchInput, ParallelSearchOutput, ParallelSearchConfig, {}>;

/**
 * Parses a JSON string into an unknown object.
 *
 * @param text - The JSON string to parse.
 * @returns {JSONValue} - The parsed JSON object.
 */
declare function parseJSON(options: {
    text: string;
    schema?: undefined;
}): Promise<JSONValue_2>;

/**
 * Parses a JSON string into a strongly-typed object using the provided schema.
 *
 * @template T - The type of the object to parse the JSON into.
 * @param {string} text - The JSON string to parse.
 * @param {Validator<T>} schema - The schema to use for parsing the JSON.
 * @returns {Promise<T>} - The parsed object.
 */
declare function parseJSON<T>(options: {
    text: string;
    schema: FlexibleSchema_2<T>;
}): Promise<T>;

/**
 * Parses a JSON string into an unknown object.
 *
 * @param text - The JSON string to parse.
 * @returns {JSONValue} - The parsed JSON object.
 */
declare function parseJSON_2(options: {
    text: string;
    schema?: undefined;
}): Promise<JSONValue_2>;

/**
 * Parses a JSON string into a strongly-typed object using the provided schema.
 *
 * @template T - The type of the object to parse the JSON into.
 * @param {string} text - The JSON string to parse.
 * @param {Validator<T>} schema - The schema to use for parsing the JSON.
 * @returns {Promise<T>} - The parsed object.
 */
declare function parseJSON_2<T>(options: {
    text: string;
    schema: FlexibleSchema<T>;
}): Promise<T>;

/**
 * Parses a JSON event stream into a stream of parsed JSON objects.
 */
export declare function parseJsonEventStream<T>({ stream, schema, }: {
    stream: ReadableStream<Uint8Array>;
    schema: FlexibleSchema<T>;
}): ReadableStream<ParseResult_2<T>>;

/**
 * Parses a JSON event stream into a stream of parsed JSON objects.
 */
declare function parseJsonEventStream_2<T>({ stream, schema, }: {
    stream: ReadableStream<Uint8Array>;
    schema: FlexibleSchema_2<T>;
}): ReadableStream<ParseResult<T>>;

export declare function parsePartialJson(jsonText: string | undefined): Promise<{
    value: JSONValue_3 | undefined;
    state: 'undefined-input' | 'successful-parse' | 'repaired-parse' | 'failed-parse';
}>;

declare function parseProviderOptions<OPTIONS>({ provider, providerOptions, schema, }: {
    provider: string;
    providerOptions: Record<string, unknown> | undefined;
    schema: FlexibleSchema_2<OPTIONS>;
}): Promise<OPTIONS | undefined>;

declare function parseProviderOptions_2<OPTIONS>({ provider, providerOptions, schema, }: {
    provider: string;
    providerOptions: Record<string, unknown> | undefined;
    schema: FlexibleSchema<OPTIONS>;
}): Promise<OPTIONS | undefined>;

declare type ParseResult<T> = {
    success: true;
    value: T;
    rawValue: unknown;
} | {
    success: false;
    error: JSONParseError_2 | TypeValidationError_2;
    rawValue: unknown;
};

declare type ParseResult_2<T> = {
    success: true;
    value: T;
    rawValue: unknown;
} | {
    success: false;
    error: JSONParseError_2 | TypeValidationError_2;
    rawValue: unknown;
};

/**
 * Server-side: parse a client TEXT frame. Validates envelope shape only and
 * rejects prototype-pollution payloads (parsed with `secureJsonParse`). Never
 * throws.
 */
declare function parseTranscriptionStreamClientFrame(text: string): TranscriptionStreamClientFrame;

/**
 * Server-side: parse a client TEXT frame. Validates envelope shape only and
 * rejects prototype-pollution payloads (parsed with `secureJsonParse`). Never
 * throws.
 */
declare function parseTranscriptionStreamClientFrame_2(text: string): TranscriptionStreamClientFrame_2;

/**
 * Client-side: parse a server TEXT frame into a transcription stream part.
 * Returns `undefined` for malformed or unsafe (prototype-polluting) JSON
 * (parsed with `secureJsonParse`), unknown part types, and known part types
 * whose required or optional fields are missing or mistyped (including
 * warning and segment elements) — downstream SDK code dereferences those
 * fields, so a drifted server must not crash or pollute the stream.
 * Revives `response-metadata.timestamp` to a `Date`.
 */
declare function parseTranscriptionStreamPart(text: string): TranscriptionModelV4StreamPart | undefined;

/**
 * Client-side: parse a server TEXT frame into a transcription stream part.
 * Returns `undefined` for malformed or unsafe (prototype-polluting) JSON
 * (parsed with `secureJsonParse`), unknown part types, and known part types
 * whose required or optional fields are missing or mistyped (including
 * warning and segment elements) — downstream SDK code dereferences those
 * fields, so a drifted server must not crash or pollute the stream.
 * Revives `response-metadata.timestamp` to a `Date`.
 */
declare function parseTranscriptionStreamPart_2(text: string): TranscriptionModelV4StreamPart | undefined;

declare type PartialMap<KeyType, ValueType> = {} & Map<DeepPartialInternal<KeyType>, DeepPartialInternal<ValueType>>;

declare type PartialObject<ObjectType extends object> = {
    [KeyType in keyof ObjectType]?: DeepPartialInternal<ObjectType[KeyType]>;
};

declare type PartialReadonlyMap<KeyType, ValueType> = {} & ReadonlyMap<DeepPartialInternal<KeyType>, DeepPartialInternal<ValueType>>;

declare type PartialReadonlySet<T> = {} & ReadonlySet<DeepPartialInternal<T>>;

declare type PartialSet<T> = {} & Set<DeepPartialInternal<T>>;

declare interface PerplexitySearchConfig {
    /**
     * Default maximum number of search results to return (1-20, default: 10).
     */
    maxResults?: number;
    /**
     * Default maximum tokens to extract per search result page (256-2048, default: 2048).
     */
    maxTokensPerPage?: number;
    /**
     * Default maximum total tokens across all search results (default: 25000, max: 1000000).
     */
    maxTokens?: number;
    /**
     * Default two-letter ISO 3166-1 alpha-2 country code for regional search results.
     * Examples: 'US', 'GB', 'FR'
     */
    country?: string;
    /**
     * Default list of domains to include or exclude from search results (max 20).
     * To include: ['nature.com', 'science.org']
     * To exclude: ['-example.com', '-spam.net']
     */
    searchDomainFilter?: string[];
    /**
     * Default list of ISO 639-1 language codes to filter results (max 10, lowercase).
     * Examples: ['en', 'fr', 'de']
     */
    searchLanguageFilter?: string[];
    /**
     * Default recency filter for results.
     * Cannot be combined with searchAfterDate/searchBeforeDate at runtime.
     */
    searchRecencyFilter?: 'day' | 'week' | 'month' | 'year';
}

declare interface PerplexitySearchError {
    /** Error type */
    error: 'api_error' | 'rate_limit' | 'timeout' | 'invalid_input' | 'unknown';
    /** HTTP status code if applicable */
    statusCode?: number;
    /** Human-readable error message */
    message: string;
}

declare interface PerplexitySearchInput {
    /**
     * Search query (string) or multiple queries (array of up to 5 strings).
     * Multi-query searches return combined results from all queries.
     */
    query: string | string[];
    /**
     * Maximum number of search results to return (1-20, default: 10).
     */
    max_results?: number;
    /**
     * Maximum number of tokens to extract per search result page (256-2048, default: 2048).
     */
    max_tokens_per_page?: number;
    /**
     * Maximum total tokens across all search results (default: 25000, max: 1000000).
     */
    max_tokens?: number;
    /**
     * Two-letter ISO 3166-1 alpha-2 country code for regional search results.
     * Examples: 'US', 'GB', 'FR'
     */
    country?: string;
    /**
     * List of domains to include or exclude from search results (max 20).
     * To include: ['nature.com', 'science.org']
     * To exclude: ['-example.com', '-spam.net']
     */
    search_domain_filter?: string[];
    /**
     * List of ISO 639-1 language codes to filter results (max 10, lowercase).
     * Examples: ['en', 'fr', 'de']
     */
    search_language_filter?: string[];
    /**
     * Include only results published after this date.
     * Format: 'MM/DD/YYYY' (e.g., '3/1/2025')
     * Cannot be used with search_recency_filter.
     */
    search_after_date?: string;
    /**
     * Include only results published before this date.
     * Format: 'MM/DD/YYYY' (e.g., '3/15/2025')
     * Cannot be used with search_recency_filter.
     */
    search_before_date?: string;
    /**
     * Include only results last updated after this date.
     * Format: 'MM/DD/YYYY' (e.g., '3/1/2025')
     * Cannot be used with search_recency_filter.
     */
    last_updated_after_filter?: string;
    /**
     * Include only results last updated before this date.
     * Format: 'MM/DD/YYYY' (e.g., '3/15/2025')
     * Cannot be used with search_recency_filter.
     */
    last_updated_before_filter?: string;
    /**
     * Filter results by relative time period.
     * Cannot be used with search_after_date or search_before_date.
     */
    search_recency_filter?: 'day' | 'week' | 'month' | 'year';
}

declare type PerplexitySearchOutput = PerplexitySearchResponse | PerplexitySearchError;

declare interface PerplexitySearchResponse {
    /** Array of search results */
    results: PerplexitySearchResult[];
    /** Unique identifier for this search request */
    id: string;
}

declare interface PerplexitySearchResult {
    /** Title of the search result */
    title: string;
    /** URL of the search result */
    url: string;
    /** Text snippet/preview of the content */
    snippet: string;
    /** Publication date of the content */
    date?: string;
    /** Last updated date of the content */
    lastUpdated?: string;
}

declare const perplexitySearchToolFactory: _ai_sdk_provider_utils.ProviderExecutedToolFactory<PerplexitySearchInput, PerplexitySearchOutput, PerplexitySearchConfig, {}>;

/**
 * Pipes the agent UI message stream to a Node.js ServerResponse object.
 *
 * @param response - The Node.js ServerResponse object to pipe to.
 * @param agent - The agent to run.
 * @param uiMessages - The input UI messages.
 * @param abortSignal - Abort signal. Optional.
 * @param timeout - Timeout in milliseconds. Optional.
 * @param experimental_sandbox - The sandbox environment that is passed through to tool execution. Optional.
 * @param options - The options for the agent. Optional.
 * @param experimental_transform - Stream transformations. Optional.
 * @param onStepEnd - Callback that is called when each step ends. Optional.
 * @param onStepFinish - Deprecated alias for `onStepEnd`. Optional.
 * @param headers - Additional headers for the response. Optional.
 * @param status - The status code for the response. Optional.
 * @param statusText - The status text for the response. Optional.
 * @param consumeSseStream - Whether to consume the SSE stream. Optional.
 */
export declare function pipeAgentUIStreamToResponse<CALL_OPTIONS = never, TOOLS extends ToolSet = {}, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = never, MESSAGE_METADATA = unknown>({ response, headers, status, statusText, consumeSseStream, ...options }: {
    response: ServerResponse;
    agent: Agent<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT, OUTPUT>;
    uiMessages: unknown[];
    abortSignal?: AbortSignal;
    timeout?: TimeoutConfiguration<TOOLS>;
    experimental_sandbox?: Experimental_SandboxSession;
    options?: CALL_OPTIONS;
    experimental_transform?: Arrayable_2<StreamTextTransform<TOOLS>>;
    onStepEnd?: GenerateTextOnStepEndCallback<TOOLS>;
    /** @deprecated Use `onStepEnd` instead. */
    onStepFinish?: GenerateTextOnStepFinishCallback<TOOLS>;
} & UIMessageStreamResponseInit & UIMessageStreamOptions<UIMessage<MESSAGE_METADATA, never, InferUITools<TOOLS>>>): Promise<void>;

/**
 * Writes a text stream to a Node.js ServerResponse object.
 * Each text chunk is encoded as UTF-8 and written as a separate chunk.
 * Sets a `Content-Type` header to `text/plain; charset=utf-8`.
 *
 * @param options - The options for piping the stream.
 * @param options.response - The Node.js ServerResponse to write to.
 * @param options.status - Optional HTTP status code.
 * @param options.statusText - Optional HTTP status text.
 * @param options.headers - Optional response headers.
 * @param options.stream - The text stream to pipe.
 * @returns A promise that resolves when the stream has been written.
 */
export declare function pipeTextStreamToResponse({ response, status, statusText, headers, stream, }: {
    response: ServerResponse;
    stream: ReadableStream<string>;
} & ResponseInit): Promise<void>;

/**
 * Pipes a UI message stream to a Node.js ServerResponse object.
 * The stream is transformed to Server-Sent Events (SSE) format.
 *
 * @param options.response - The Node.js ServerResponse object to write to.
 * @param options.status - The HTTP status code for the response.
 * @param options.statusText - The HTTP status text for the response.
 * @param options.headers - Additional HTTP headers to include in the response.
 * @param options.stream - The UI message chunk stream to send.
 * @param options.consumeSseStream - Optional callback to consume a copy of the SSE stream independently.
 * @returns A promise that resolves when the stream has been written.
 */
export declare function pipeUIMessageStreamToResponse({ response, status, statusText, headers, stream, consumeSseStream, }: {
    response: ServerResponse;
    stream: ReadableStream<UIMessageChunk>;
} & UIMessageStreamResponseInit): Promise<void>;

declare const postFormDataToApi: <T>({ url, headers, formData, failedResponseHandler, successfulResponseHandler, abortSignal, fetch, }: {
    url: string;
    headers?: Record<string, string | undefined>;
    formData: FormData;
    failedResponseHandler: ResponseHandler<APICallError_2>;
    successfulResponseHandler: ResponseHandler<T>;
    abortSignal?: AbortSignal;
    fetch?: FetchFunction;
}) => Promise<{
    value: T;
    rawValue?: unknown;
    responseHeaders?: Record<string, string>;
}>;

declare const postFormDataToApi_2: <T>({ url, headers, formData, failedResponseHandler, successfulResponseHandler, abortSignal, fetch, }: {
    url: string;
    headers?: Record<string, string | undefined>;
    formData: FormData;
    failedResponseHandler: ResponseHandler_2<APICallError_2>;
    successfulResponseHandler: ResponseHandler_2<T>;
    abortSignal?: AbortSignal;
    fetch?: FetchFunction_2;
}) => Promise<{
    value: T;
    rawValue?: unknown;
    responseHeaders?: Record<string, string>;
}>;

declare const postJsonToApi: <T>({ url, headers, body, failedResponseHandler, successfulResponseHandler, abortSignal, fetch, }: {
    url: string;
    headers?: Record<string, string | undefined>;
    body: unknown;
    failedResponseHandler: ResponseHandler<APICallError_2>;
    successfulResponseHandler: ResponseHandler<T>;
    abortSignal?: AbortSignal;
    fetch?: FetchFunction;
}) => Promise<{
    value: T;
    rawValue?: unknown;
    responseHeaders?: Record<string, string>;
}>;

declare const postJsonToApi_2: <T>({ url, headers, body, failedResponseHandler, successfulResponseHandler, abortSignal, fetch, }: {
    url: string;
    headers?: Record<string, string | undefined>;
    body: unknown;
    failedResponseHandler: ResponseHandler_2<APICallError_2>;
    successfulResponseHandler: ResponseHandler_2<T>;
    abortSignal?: AbortSignal;
    fetch?: FetchFunction_2;
}) => Promise<{
    value: T;
    rawValue?: unknown;
    responseHeaders?: Record<string, string>;
}>;

declare const postToApi: <T>({ url, headers, body, successfulResponseHandler, failedResponseHandler, abortSignal, fetch, }: {
    url: string;
    headers?: Record<string, string | undefined>;
    body: {
        content: string | FormData | Uint8Array;
        values: unknown;
    };
    failedResponseHandler: ResponseHandler<Error>;
    successfulResponseHandler: ResponseHandler<T>;
    abortSignal?: AbortSignal;
    fetch?: FetchFunction;
}) => Promise<{
    value: T;
    rawValue?: unknown;
    responseHeaders?: Record<string, string>;
}>;

declare const postToApi_2: <T>({ url, headers, body, successfulResponseHandler, failedResponseHandler, abortSignal, fetch, }: {
    url: string;
    headers?: Record<string, string | undefined>;
    body: {
        content: string | FormData | Uint8Array;
        values: unknown;
    };
    failedResponseHandler: ResponseHandler_2<Error>;
    successfulResponseHandler: ResponseHandler_2<T>;
    abortSignal?: AbortSignal;
    fetch?: FetchFunction_2;
}) => Promise<{
    value: T;
    rawValue?: unknown;
    responseHeaders?: Record<string, string>;
}>;

export declare type PrepareReconnectToStreamRequest = (options: {
    id: string;
    requestMetadata: unknown;
    body: Record<string, any> | undefined;
    credentials: RequestCredentials | undefined;
    headers: HeadersInit | undefined;
    api: string;
}) => {
    headers?: HeadersInit;
    credentials?: RequestCredentials;
    api?: string;
} | PromiseLike<{
    headers?: HeadersInit;
    credentials?: RequestCredentials;
    api?: string;
}>;

export declare type PrepareSendMessagesRequest<UI_MESSAGE extends UIMessage> = (options: {
    id: string;
    messages: UI_MESSAGE[];
    requestMetadata: unknown;
    body: Record<string, any> | undefined;
    credentials: RequestCredentials | undefined;
    headers: HeadersInit | undefined;
    api: string;
} & {
    trigger: 'submit-message' | 'regenerate-message';
    messageId: string | undefined;
}) => {
    body: object;
    headers?: HeadersInit;
    credentials?: RequestCredentials;
    api?: string;
} | PromiseLike<{
    body: object;
    headers?: HeadersInit;
    credentials?: RequestCredentials;
    api?: string;
}>;

/**
 * Function that you can use to provide different settings for a step.
 *
 * @param options - The options for the step.
 * @param options.steps - The steps that have been executed so far.
 * @param options.stepNumber - The number of the step that is being executed.
 * @param options.model - The model that is being used.
 * @param options.instructions - The instructions that will be sent to the model for the current step.
 * @param options.initialInstructions - The initial instructions that were passed into generateText or streamText.
 * @param options.messages - The messages that will be sent to the model for the current step. If you return a `messages` override, those messages carry forward to later steps.
 * @param options.initialMessages - The initial messages that were passed into generateText or streamText.
 * @param options.responseMessages - The response messages that have been accumulated from previous steps.
 * @param options.runtimeContext - The user-defined runtime context.
 *
 * @returns An object that contains the settings for the step.
 * If you return undefined (or for undefined settings), the settings from the outer level will be used.
 */
export declare type PrepareStepFunction<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = (options: {
    /**
     * The steps that have been executed so far.
     */
    steps: Array<StepResult<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>>;
    /**
     * The number of the step that is being executed.
     */
    stepNumber: number;
    /**
     * The model instance that is being used for this step.
     */
    model: LanguageModel;
    /**
     * The instructions that will be sent to the model for the current step.
     */
    instructions: Instructions | undefined;
    /**
     * The initial instructions that were passed into generateText or streamText.
     */
    initialInstructions: Instructions | undefined;
    /**
     * The messages that will be sent to the model for the current step.
     * If you return a `messages` override, those messages carry forward to later steps.
     */
    messages: Array<ModelMessage>;
    /**
     * The initial messages that were passed into generateText or streamText.
     */
    initialMessages: Array<ModelMessage>;
    /**
     * The response messages that have been accumulated from all previous steps.
     */
    responseMessages: Array<ResponseMessage>;
    /**
     * Tool context.
     */
    toolsContext: InferToolSetContext_2<TOOLS>;
    /**
     * User-defined runtime context.
     */
    runtimeContext: RUNTIME_CONTEXT;
    /**
     * The sandbox environment that the step is operating in.
     */
    experimental_sandbox?: Experimental_SandboxSession;
}) => PromiseLike<PrepareStepResult<TOOLS, RUNTIME_CONTEXT>> | PrepareStepResult<TOOLS, RUNTIME_CONTEXT>;

/**
 * The result type returned by a {@link PrepareStepFunction},
 * allowing per-step overrides of model call settings, model, tools,
 * instructions, or messages.
 *
 * Model call setting overrides apply only to the current step. Undefined
 * settings fall back to the outer call settings.
 */
export declare type PrepareStepResult<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = ({
    /**
     * Optionally override which LanguageModel instance is used for this step.
     */
    model?: LanguageModel;
    /**
     * Optionally set which tool the model must call, or provide tool call configuration
     * for this step.
     */
    toolChoice?: ToolChoice<NoInfer<TOOLS>>;
    /**
     * If provided, only these tools are enabled/available for this step.
     */
    activeTools?: ActiveTools<NoInfer<TOOLS>>;
    /**
     * Optionally override the order in which tools are sent to the provider
     * for this step.
     */
    toolOrder?: ToolOrder<NoInfer<TOOLS>>;
    /**
     * Optionally override the instructions sent to the model for this step.
     * The override carries forward to later steps.
     */
    instructions?: Instructions;
    /**
     * Optionally override the instructions sent to the model for this step.
     *
     * @deprecated Use `instructions` instead.
     */
    system?: Instructions;
    /**
     * Optionally override the full set of messages sent to the model
     * for this step. The override carries forward to later steps.
     */
    messages?: Array<ModelMessage>;
    /**
     * Tool context.
     *
     * Changing the toolsContext will affect the toolsContext in this step
     * and all subsequent steps.
     *
     * The toolsContext is passed into tool execution.
     */
    toolsContext?: InferToolSetContext_2<TOOLS>;
    /**
     * Runtime context.
     *
     * Changing the runtimeContext will affect the runtimeContext in this step
     * and all subsequent steps.
     */
    runtimeContext?: RUNTIME_CONTEXT;
    /**
     * The sandbox environment that the step is operating in.
     *
     * Changing the sandbox will affect tool execution in this step only.
     */
    experimental_sandbox?: Experimental_SandboxSession;
    /**
     * Additional provider-specific options for this step.
     *
     * Can be used to pass provider-specific configuration such as
     * container IDs for Anthropic's code execution.
     */
    providerOptions?: ProviderOptions_2;
} & LanguageModelCallOptions) | undefined;

/**
 * Prompt part of the AI function options.
 * It contains instructions, a simple text prompt, or a list of messages.
 */
export declare type Prompt = {
    /**
     * Instructions to include in the prompt. Can be used with `prompt` or `messages`.
     */
    instructions?: Instructions;
    /**
     * Instructions to include in the prompt. Can be used with `prompt` or `messages`.
     *
     * @deprecated Use `instructions` instead.
     */
    system?: Instructions;
    /**
     * Whether system messages are allowed in the `prompt` or `messages` fields.
     *
     * When disabled, system messages must be provided through the `instructions`
     * option.
     *
     * @default false
     */
    allowSystemInMessages?: boolean;
} & ({
    /**
     * A prompt. It can be either a text prompt or a list of messages.
     *
     * You can either use `prompt` or `messages` but not both.
     */
    prompt: string | Array<ModelMessage>;
    /**
     * A list of messages.
     *
     * You can either use `prompt` or `messages` but not both.
     */
    messages?: never;
} | {
    /**
     * A list of messages.
     *
     * You can either use `prompt` or `messages` but not both.
     */
    messages: Array<ModelMessage>;
    /**
     * A prompt. It can be either a text prompt or a list of messages.
     *
     * You can either use `prompt` or `messages` but not both.
     */
    prompt?: never;
});

/**
 * Provider for language, text embedding, and image models.
 */
export declare type Provider = {
    /**
     * Returns the language model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {LanguageModel} The language model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    languageModel(modelId: string): LanguageModel;
    /**
     * Returns the text embedding model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {EmbeddingModel} The embedding model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    embeddingModel(modelId: string): EmbeddingModel;
    /**
     * Returns the image model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {ImageModel} The image model associated with the id
     */
    imageModel(modelId: string): ImageModel;
    /**
     * Returns the reranking model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {RerankingModel} The reranking model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    rerankingModel(modelId: string): RerankingModel;
};

/**
 * Tool with provider-defined input and output schemas that is executed by the
 * user.
 *
 * For example, shell tools that are executed in a local shell, but have provider-defined input and output schemas.
 */
declare type ProviderDefinedTool<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context | unknown | never = any> = BaseProviderTool<INPUT, OUTPUT, CONTEXT> & {
    /**
     * Flag that indicates whether the tool is executed by the provider.
     */
    isProviderExecuted: false;
    supportsDeferredResults?: never;
};

/**
 * Tool with provider-defined input and output schemas that is executed by the
 * user.
 *
 * For example, shell tools that are executed in a local shell, but have provider-defined input and output schemas.
 */
declare type ProviderDefinedTool_2<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context_2 | unknown | never = any> = BaseProviderTool_2<INPUT, OUTPUT, CONTEXT> & {
    /**
     * Flag that indicates whether the tool is executed by the provider.
     */
    isProviderExecuted: false;
    supportsDeferredResults?: never;
};

/**
 * A provider-defined tool is a tool for which the provider defines the input
 * and output schemas, but does not execute the tool.
 */
declare type ProviderDefinedToolFactory<INPUT, ARGS extends object, CONTEXT extends Context = {}> = <OUTPUT>(options: ARGS & {
    execute?: ToolExecuteFunction_2<INPUT, OUTPUT, CONTEXT>;
    needsApproval?: Tool_2<INPUT, OUTPUT, CONTEXT>['needsApproval'];
    toModelOutput?: Tool_2<INPUT, OUTPUT, CONTEXT>['toModelOutput'];
    onInputStart?: Tool_2<INPUT, OUTPUT, CONTEXT>['onInputStart'];
    onInputDelta?: Tool_2<INPUT, OUTPUT, CONTEXT>['onInputDelta'];
    onInputAvailable?: Tool_2<INPUT, OUTPUT, CONTEXT>['onInputAvailable'];
}) => ProviderDefinedTool<INPUT, OUTPUT, CONTEXT>;

/**
 * A provider-defined tool is a tool for which the provider defines the input
 * and output schemas, but does not execute the tool.
 */
declare type ProviderDefinedToolFactory_2<INPUT, ARGS extends object, CONTEXT extends Context_2 = {}> = <OUTPUT>(options: ARGS & {
    execute?: ToolExecuteFunction<INPUT, OUTPUT, CONTEXT>;
    needsApproval?: Tool<INPUT, OUTPUT, CONTEXT>['needsApproval'];
    toModelOutput?: Tool<INPUT, OUTPUT, CONTEXT>['toModelOutput'];
    onInputStart?: Tool<INPUT, OUTPUT, CONTEXT>['onInputStart'];
    onInputDelta?: Tool<INPUT, OUTPUT, CONTEXT>['onInputDelta'];
    onInputAvailable?: Tool<INPUT, OUTPUT, CONTEXT>['onInputAvailable'];
}) => ProviderDefinedTool_2<INPUT, OUTPUT, CONTEXT>;

declare type ProviderDefinedToolFactoryWithOutputSchema<INPUT, OUTPUT, ARGS extends object, CONTEXT extends Context = {}> = (options: ARGS & {
    execute?: ToolExecuteFunction_2<INPUT, OUTPUT, CONTEXT>;
    needsApproval?: Tool_2<INPUT, OUTPUT, CONTEXT>['needsApproval'];
    toModelOutput?: Tool_2<INPUT, OUTPUT, CONTEXT>['toModelOutput'];
    onInputStart?: Tool_2<INPUT, OUTPUT, CONTEXT>['onInputStart'];
    onInputDelta?: Tool_2<INPUT, OUTPUT, CONTEXT>['onInputDelta'];
    onInputAvailable?: Tool_2<INPUT, OUTPUT, CONTEXT>['onInputAvailable'];
}) => ProviderDefinedTool<INPUT, OUTPUT, CONTEXT>;

declare type ProviderDefinedToolFactoryWithOutputSchema_2<INPUT, OUTPUT, ARGS extends object, CONTEXT extends Context_2 = {}> = (options: ARGS & {
    execute?: ToolExecuteFunction<INPUT, OUTPUT, CONTEXT>;
    needsApproval?: Tool<INPUT, OUTPUT, CONTEXT>['needsApproval'];
    toModelOutput?: Tool<INPUT, OUTPUT, CONTEXT>['toModelOutput'];
    onInputStart?: Tool<INPUT, OUTPUT, CONTEXT>['onInputStart'];
    onInputDelta?: Tool<INPUT, OUTPUT, CONTEXT>['onInputDelta'];
    onInputAvailable?: Tool<INPUT, OUTPUT, CONTEXT>['onInputAvailable'];
}) => ProviderDefinedTool_2<INPUT, OUTPUT, CONTEXT>;

/**
 * Tool with provider-defined input and output schemas that is executed by the
 * provider.
 *
 * For example, web search tools and code execution tools that are executed by the provider itself.
 */
declare type ProviderExecutedTool<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context | unknown | never = any> = BaseProviderTool<INPUT, OUTPUT, CONTEXT> & {
    /**
     * Flag that indicates whether the tool is executed by the provider.
     */
    isProviderExecuted: true;
    /**
     * Whether this provider-executed tool supports deferred results.
     *
     * When true, the tool result may not be returned in the same turn as the
     * tool call (e.g., when using programmatic tool calling where a server tool
     * triggers a client-executed tool, and the server tool's result is deferred
     * until the client tool is resolved).
     *
     * This flag allows the AI SDK to handle tool results that arrive without
     * a matching tool call in the current response.
     *
     * @default false
     */
    supportsDeferredResults?: boolean;
};

/**
 * Tool with provider-defined input and output schemas that is executed by the
 * provider.
 *
 * For example, web search tools and code execution tools that are executed by the provider itself.
 */
declare type ProviderExecutedTool_2<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context_2 | unknown | never = any> = BaseProviderTool_2<INPUT, OUTPUT, CONTEXT> & {
    /**
     * Flag that indicates whether the tool is executed by the provider.
     */
    isProviderExecuted: true;
    /**
     * Whether this provider-executed tool supports deferred results.
     *
     * When true, the tool result may not be returned in the same turn as the
     * tool call (e.g., when using programmatic tool calling where a server tool
     * triggers a client-executed tool, and the server tool's result is deferred
     * until the client tool is resolved).
     *
     * This flag allows the AI SDK to handle tool results that arrive without
     * a matching tool call in the current response.
     *
     * @default false
     */
    supportsDeferredResults?: boolean;
};

/**
 * A provider-executed tool is a tool for which the provider executes the tool.
 */
declare type ProviderExecutedToolFactory<INPUT, OUTPUT, ARGS extends object, CONTEXT extends Context = {}> = (options: ARGS & {
    onInputStart?: Tool_2<INPUT, OUTPUT, CONTEXT>['onInputStart'];
    onInputDelta?: Tool_2<INPUT, OUTPUT, CONTEXT>['onInputDelta'];
    onInputAvailable?: Tool_2<INPUT, OUTPUT, CONTEXT>['onInputAvailable'];
}) => ProviderExecutedTool<INPUT, OUTPUT, CONTEXT>;

/**
 * A provider-executed tool is a tool for which the provider executes the tool.
 */
declare type ProviderExecutedToolFactory_2<INPUT, OUTPUT, ARGS extends object, CONTEXT extends Context_2 = {}> = (options: ARGS & {
    onInputStart?: Tool<INPUT, OUTPUT, CONTEXT>['onInputStart'];
    onInputDelta?: Tool<INPUT, OUTPUT, CONTEXT>['onInputDelta'];
    onInputAvailable?: Tool<INPUT, OUTPUT, CONTEXT>['onInputAvailable'];
}) => ProviderExecutedTool_2<INPUT, OUTPUT, CONTEXT>;

/**
 * Additional provider-specific metadata that is returned from the provider.
 *
 * This is needed to enable provider-specific functionality that can be
 * fully encapsulated in the provider.
 */
export declare type ProviderMetadata = SharedV4ProviderMetadata_2;

/**
 * Additional provider-specific options.
 *
 * They are passed through to the provider from the AI SDK and enable
 * provider-specific functionality that can be fully encapsulated in the provider.
 */
declare type ProviderOptions = SharedV4ProviderOptions;

/**
 * Additional provider-specific options.
 *
 * They are passed through to the provider from the AI SDK and enable
 * provider-specific functionality that can be fully encapsulated in the provider.
 */
declare type ProviderOptions_2 = SharedV4ProviderOptions;

export declare type ProviderReference = SharedV4ProviderReference;

/**
 * A mapping of provider names to provider-specific file identifiers.
 *
 * Provider references allow files to be identified across different
 * providers without re-uploading, by storing each provider's own
 * identifier for the same logical file.
 */
declare type ProviderReference_2 = SharedV4ProviderReference_2;

/**
 * A mapping of provider names to provider-specific file identifiers.
 *
 * Provider references allow files to be identified across different
 * providers without re-uploading, by storing each provider's own
 * identifier for the same logical file.
 */
declare type ProviderReference_3 = SharedV4ProviderReference_2;

export declare interface ProviderRegistryProvider<PROVIDERS extends Record<string, ProviderV4_2 | ProviderV3> = Record<string, ProviderV4_2 | ProviderV3>, SEPARATOR extends string = ':'> {
    languageModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${ExtractLiteralUnion<Parameters<NonNullable<PROVIDERS[KEY]['languageModel']>>[0]>}` : never): LanguageModelV4_2;
    languageModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${string}` : never): LanguageModelV4_2;
    embeddingModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${ExtractLiteralUnion<Parameters<NonNullable<PROVIDERS[KEY]['embeddingModel']>>[0]>}` : never): EmbeddingModelV4_2;
    embeddingModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${string}` : never): EmbeddingModelV4_2;
    imageModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${ExtractLiteralUnion<Parameters<NonNullable<PROVIDERS[KEY]['imageModel']>>[0]>}` : never): ImageModelV4_2;
    imageModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${string}` : never): ImageModelV4_2;
    transcriptionModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${ExtractLiteralUnion<Parameters<NonNullable<PROVIDERS[KEY]['transcriptionModel']>>[0]>}` : never): TranscriptionModelV4_2;
    transcriptionModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${string}` : never): TranscriptionModelV4_2;
    speechModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${ExtractLiteralUnion<Parameters<NonNullable<PROVIDERS[KEY]['speechModel']>>[0]>}` : never): SpeechModelV4_2;
    speechModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${string}` : never): SpeechModelV4_2;
    rerankingModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${ExtractLiteralUnion<Parameters<NonNullable<PROVIDERS[KEY]['rerankingModel']>>[0]>}` : never): RerankingModelV4_2;
    rerankingModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${string}` : never): RerankingModelV4_2;
    videoModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${ProviderVideoModelIdentifier<PROVIDERS[KEY]>}` : never): VideoModelV4_2;
    videoModel<KEY extends keyof PROVIDERS>(id: KEY extends string ? `${KEY & string}${SEPARATOR}${string}` : never): VideoModelV4_2;
    files<KEY extends keyof PROVIDERS>(id: KEY extends string ? KEY & string : never): FilesV4_2;
    skills<KEY extends keyof PROVIDERS>(id: KEY extends string ? KEY & string : never): SkillsV4_2;
}

/**
 * Provider for language, text embedding, and image generation models.
 */
declare interface ProviderV2 {
    /**
     * Returns the language model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {LanguageModel} The language model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    languageModel(modelId: string): LanguageModelV2;
    /**
     * Returns the text embedding model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {LanguageModel} The language model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    textEmbeddingModel(modelId: string): EmbeddingModelV2<string>;
    /**
     * Returns the image model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {ImageModel} The image model associated with the id
     */
    imageModel(modelId: string): ImageModelV2;
    /**
     * Returns the transcription model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {TranscriptionModel} The transcription model associated with the id
     */
    transcriptionModel?(modelId: string): TranscriptionModelV2;
    /**
     * Returns the speech model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {SpeechModel} The speech model associated with the id
     */
    speechModel?(modelId: string): SpeechModelV2;
}

/**
 * Provider for language, text embedding, and image generation models.
 */
declare interface ProviderV3 {
    readonly specificationVersion: 'v3';
    /**
     * Returns the language model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {LanguageModel} The language model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    languageModel(modelId: string): LanguageModelV3;
    /**
     * Returns the text embedding model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {LanguageModel} The language model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    embeddingModel(modelId: string): EmbeddingModelV3;
    /**
     * Returns the text embedding model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {EmbeddingModel} The embedding model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     *
     * @deprecated Use `embeddingModel` instead.
     */
    textEmbeddingModel?(modelId: string): EmbeddingModelV3;
    /**
     * Returns the image model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {ImageModel} The image model associated with the id
     */
    imageModel(modelId: string): ImageModelV3;
    /**
     * Returns the transcription model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {TranscriptionModel} The transcription model associated with the id
     */
    transcriptionModel?(modelId: string): TranscriptionModelV3;
    /**
     * Returns the speech model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {SpeechModel} The speech model associated with the id
     */
    speechModel?(modelId: string): SpeechModelV3;
    /**
     * Returns the reranking model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {RerankingModel} The reranking model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    rerankingModel?(modelId: string): RerankingModelV3;
}

/**
 * Provider for language, text embedding, and image generation models.
 */
declare interface ProviderV4 {
    readonly specificationVersion: 'v4';
    /**
     * Returns the language model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {LanguageModelV4} The language model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    languageModel(modelId: string): LanguageModelV4;
    /**
     * Returns the text embedding model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {EmbeddingModelV4} The embedding model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    embeddingModel(modelId: string): EmbeddingModelV4;
    /**
     * Returns the image model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {ImageModelV4} The image model associated with the id
     */
    imageModel(modelId: string): ImageModelV4;
    /**
     * Returns the transcription model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {TranscriptionModelV4} The transcription model associated with the id
     */
    transcriptionModel?(modelId: string): TranscriptionModelV4;
    /**
     * Returns the speech model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {SpeechModelV4} The speech model associated with the id
     */
    speechModel?(modelId: string): SpeechModelV4;
    /**
     * Returns the reranking model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {RerankingModelV4} The reranking model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    rerankingModel?(modelId: string): RerankingModelV4;
    /**
     * Returns the files interface for uploading files to the provider.
     * The returned interface can be passed to the `uploadFile` function.
     *
     * @returns {FilesV4} The files interface for this provider.
     */
    files?(): FilesV4;
    /**
     * Returns the skills interface for uploading skills to the provider.
     * The returned interface can be passed to the `uploadSkill` function.
     */
    skills?(): SkillsV4;
}

/**
 * Provider for language, text embedding, and image generation models.
 */
declare interface ProviderV4_2 {
    readonly specificationVersion: 'v4';
    /**
     * Returns the language model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {LanguageModelV4} The language model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    languageModel(modelId: string): LanguageModelV4_2;
    /**
     * Returns the text embedding model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {EmbeddingModelV4} The embedding model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    embeddingModel(modelId: string): EmbeddingModelV4_2;
    /**
     * Returns the image model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {ImageModelV4} The image model associated with the id
     */
    imageModel(modelId: string): ImageModelV4_2;
    /**
     * Returns the transcription model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {TranscriptionModelV4} The transcription model associated with the id
     */
    transcriptionModel?(modelId: string): TranscriptionModelV4_2;
    /**
     * Returns the speech model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {SpeechModelV4} The speech model associated with the id
     */
    speechModel?(modelId: string): SpeechModelV4_2;
    /**
     * Returns the reranking model with the given id.
     * The model id is then passed to the provider function to get the model.
     *
     * @param {string} modelId - The id of the model to return.
     *
     * @returns {RerankingModelV4} The reranking model associated with the id
     *
     * @throws {NoSuchModelError} If no such model exists.
     */
    rerankingModel?(modelId: string): RerankingModelV4_2;
    /**
     * Returns the files interface for uploading files to the provider.
     * The returned interface can be passed to the `uploadFile` function.
     *
     * @returns {FilesV4} The files interface for this provider.
     */
    files?(): FilesV4_2;
    /**
     * Returns the skills interface for uploading skills to the provider.
     * The returned interface can be passed to the `uploadSkill` function.
     */
    skills?(): SkillsV4_2;
}

declare type ProviderVideoModelIdentifier<PROVIDER> = PROVIDER extends {
    videoModel: (...args: infer ARGS) => unknown;
} ? ExtractLiteralUnion<ARGS[0]> : never;

/**
 * Prunes model messages from a list of model messages.
 *
 * @param messages - The list of model messages to prune.
 * @param reasoning - How to remove reasoning content from assistant messages. Default is `'none'`.
 * @param toolCalls - How to prune tool call/results/approval content. Default is `[]`.
 * @param emptyMessages - Whether to keep or remove messages whose content is empty after pruning. Default is `'remove'`.
 *
 * @returns The pruned list of model messages.
 */
export declare function pruneMessages({ messages, reasoning, toolCalls, emptyMessages, }: {
    messages: ModelMessage[];
    reasoning?: 'all' | 'before-last-message' | 'none';
    toolCalls?: 'all' | 'before-last-message' | `before-last-${number}-messages` | 'none' | Array<{
        type: 'all' | 'before-last-message' | `before-last-${number}-messages`;
        tools?: string[];
    }>;
    emptyMessages?: 'keep' | 'remove';
}): ModelMessage[];

/**
 * Options for reading a file from the sandbox.
 */
declare type ReadFileOptions = {
    /**
     * Path of the file to read.
     */
    path: string;
    /**
     * Signal that can be used to abort the read.
     */
    abortSignal?: AbortSignal;
};

/**
 * Options for reading a file from the sandbox.
 */
declare type ReadFileOptions_2 = {
    /**
     * Path of the file to read.
     */
    path: string;
    /**
     * Signal that can be used to abort the read.
     */
    abortSignal?: AbortSignal;
};

/**
 * Reads a fetch Response body with a size limit to prevent memory exhaustion.
 *
 * Checks the Content-Length header for early rejection, then reads the body
 * incrementally via ReadableStream and aborts with a DownloadError when the
 * limit is exceeded.
 *
 * @param response - The fetch Response to read.
 * @param url - The URL being downloaded (used in error messages).
 * @param maxBytes - Maximum allowed bytes. Defaults to DEFAULT_MAX_DOWNLOAD_SIZE.
 * @returns A Uint8Array containing the response body.
 * @throws DownloadError if the response exceeds maxBytes.
 */
declare function readResponseWithSizeLimit({ response, url, maxBytes, }: {
    response: Response;
    url: string;
    maxBytes?: number;
}): Promise<Uint8Array>;

/**
 * Reads a fetch Response body with a size limit to prevent memory exhaustion.
 *
 * Checks the Content-Length header for early rejection, then reads the body
 * incrementally via ReadableStream and aborts with a DownloadError when the
 * limit is exceeded.
 *
 * @param response - The fetch Response to read.
 * @param url - The URL being downloaded (used in error messages).
 * @param maxBytes - Maximum allowed bytes. Defaults to DEFAULT_MAX_DOWNLOAD_SIZE.
 * @returns A Uint8Array containing the response body.
 * @throws DownloadError if the response exceeds maxBytes.
 */
declare function readResponseWithSizeLimit_2({ response, url, maxBytes, }: {
    response: Response;
    url: string;
    maxBytes?: number;
}): Promise<Uint8Array>;

/**
 * Transforms a stream of `UIMessageChunk`s into an `AsyncIterableStream` of `UIMessage`s.
 *
 * @param options.message - The last assistant message to use as a starting point when the conversation is resumed. Otherwise undefined.
 * @param options.stream - The stream of `UIMessageChunk`s to read.
 * @param options.terminateOnError - Whether to terminate the stream if an error occurs.
 * @param options.onError - A function that is called when an error occurs.
 *
 * @returns An `AsyncIterableStream` of `UIMessage`s. Each stream part is a different state of the same message
 * as it is being completed.
 */
export declare function readUIMessageStream<UI_MESSAGE extends UIMessage>({ message, stream, onError, terminateOnError, }: {
    message?: UI_MESSAGE;
    stream: ReadableStream<UIMessageChunk>;
    onError?: (error: unknown) => void;
    terminateOnError?: boolean;
}): AsyncIterableStream<UI_MESSAGE>;

/**
 * Reads WebSocket message data as text, handling string, binary,
 * and Blob payloads.
 */
declare function readWebSocketMessageText(data: unknown): Promise<string>;

/**
 * Reads WebSocket message data as text, handling string, binary,
 * and Blob payloads.
 */
declare function readWebSocketMessageText_2(data: unknown): Promise<string>;

declare interface RealtimeFactoryV4 {
    (modelId: string): RealtimeModelV4;
    getToken(options: RealtimeFactoryV4GetTokenOptions): Promise<RealtimeFactoryV4GetTokenResult>;
}

declare interface RealtimeFactoryV4_2 {
    (modelId: string): RealtimeModelV4_2;
    getToken(options: RealtimeFactoryV4GetTokenOptions_2): Promise<RealtimeFactoryV4GetTokenResult_2>;
}

declare type RealtimeFactoryV4GetTokenOptions = {
    model: string;
} & RealtimeModelV4ClientSecretOptions;

declare type RealtimeFactoryV4GetTokenOptions_2 = {
    model: string;
} & RealtimeModelV4ClientSecretOptions_2;

declare type RealtimeFactoryV4GetTokenResult = {
    token: string;
    url: string;
    expiresAt?: number;
};

declare type RealtimeFactoryV4GetTokenResult_2 = {
    token: string;
    url: string;
    expiresAt?: number;
};

/**
 * Specification for a realtime model that supports bidirectional
 * audio/text communication over WebSocket.
 *
 * Providers implement this interface to enable realtime voice
 * conversations through the AI SDK.
 */
declare type RealtimeModelV4 = {
    /**
     * The realtime model must specify which interface version it implements.
     */
    readonly specificationVersion: 'v4';
    /**
     * Provider ID (e.g. 'openai', 'xai').
     */
    readonly provider: string;
    /**
     * Provider-specific model ID (e.g. 'gpt-4o-realtime', 'grok-3').
     */
    readonly modelId: string;
    /**
     * Server-side: Creates an ephemeral client secret for authenticating
     * browser-side WebSocket connections. The secret is short-lived and
     * safe to expose to client code.
     *
     * Naming: "do" prefix to prevent accidental direct usage by the user.
     */
    doCreateClientSecret(options: RealtimeModelV4ClientSecretOptions): PromiseLike<RealtimeModelV4ClientSecretResult>;
    /**
     * Browser-side: Returns the WebSocket URL and subprotocols to use
     * when connecting. Each provider has its own authentication mechanism
     * (e.g. OpenAI uses subprotocol headers, xAI may use query params).
     */
    getWebSocketConfig(options: {
        token: string;
        url: string;
    }): {
        url: string;
        protocols?: string[];
    };
    /**
     * Browser-side: Parses a raw JSON event received over the WebSocket
     * and returns one or more normalized events. Providers map their native
     * event format to the common RealtimeModelV4ServerEvent union.
     *
     * Returns an array when a single provider message maps to multiple
     * normalized events (e.g. Google's serverContent can contain audio,
     * text, and turn-complete data in one message).
     */
    parseServerEvent(raw: unknown): RealtimeModelV4ServerEvent | RealtimeModelV4ServerEvent[];
    /**
     * Browser-side: Serializes a normalized client event into the
     * provider's native JSON format for sending over the WebSocket.
     */
    serializeClientEvent(event: RealtimeModelV4ClientEvent): unknown | PromiseLike<unknown>;
    /**
     * Browser-side: Builds the provider-specific session configuration
     * payload from a normalized session config. Used to construct the
     * session.update event sent after WebSocket connection.
     */
    buildSessionConfig(config: RealtimeModelV4SessionConfig): unknown;
    /**
     * Browser-side: Returns a message to auto-send back over the WebSocket
     * in response to a raw incoming message, or null if no response is needed.
     *
     * Used for provider-specific keepalive protocols (e.g. ping/pong).
     * Called by the session layer before parseServerEvent.
     */
    getHealthCheckResponse?(raw: unknown): unknown | null;
};

/**
 * Specification for a realtime model that supports bidirectional
 * audio/text communication over WebSocket.
 *
 * Providers implement this interface to enable realtime voice
 * conversations through the AI SDK.
 */
declare type RealtimeModelV4_2 = {
    /**
     * The realtime model must specify which interface version it implements.
     */
    readonly specificationVersion: 'v4';
    /**
     * Provider ID (e.g. 'openai', 'xai').
     */
    readonly provider: string;
    /**
     * Provider-specific model ID (e.g. 'gpt-4o-realtime', 'grok-3').
     */
    readonly modelId: string;
    /**
     * Server-side: Creates an ephemeral client secret for authenticating
     * browser-side WebSocket connections. The secret is short-lived and
     * safe to expose to client code.
     *
     * Naming: "do" prefix to prevent accidental direct usage by the user.
     */
    doCreateClientSecret(options: RealtimeModelV4ClientSecretOptions_2): PromiseLike<RealtimeModelV4ClientSecretResult_2>;
    /**
     * Browser-side: Returns the WebSocket URL and subprotocols to use
     * when connecting. Each provider has its own authentication mechanism
     * (e.g. OpenAI uses subprotocol headers, xAI may use query params).
     */
    getWebSocketConfig(options: {
        token: string;
        url: string;
    }): {
        url: string;
        protocols?: string[];
    };
    /**
     * Browser-side: Parses a raw JSON event received over the WebSocket
     * and returns one or more normalized events. Providers map their native
     * event format to the common RealtimeModelV4ServerEvent union.
     *
     * Returns an array when a single provider message maps to multiple
     * normalized events (e.g. Google's serverContent can contain audio,
     * text, and turn-complete data in one message).
     */
    parseServerEvent(raw: unknown): RealtimeModelV4ServerEvent_2 | RealtimeModelV4ServerEvent_2[];
    /**
     * Browser-side: Serializes a normalized client event into the
     * provider's native JSON format for sending over the WebSocket.
     */
    serializeClientEvent(event: RealtimeModelV4ClientEvent_2): unknown | PromiseLike<unknown>;
    /**
     * Browser-side: Builds the provider-specific session configuration
     * payload from a normalized session config. Used to construct the
     * session.update event sent after WebSocket connection.
     */
    buildSessionConfig(config: RealtimeModelV4SessionConfig_2): unknown;
    /**
     * Browser-side: Returns a message to auto-send back over the WebSocket
     * in response to a raw incoming message, or null if no response is needed.
     *
     * Used for provider-specific keepalive protocols (e.g. ping/pong).
     * Called by the session layer before parseServerEvent.
     */
    getHealthCheckResponse?(raw: unknown): unknown | null;
};

/**
 * An audio message from the user (complete audio, not streamed).
 */
declare type RealtimeModelV4AudioMessage = {
    type: 'audio-message';
    role: 'user';
    /**
     * Base64-encoded audio data.
     */
    audio: string;
};

/**
 * An audio message from the user (complete audio, not streamed).
 */
declare type RealtimeModelV4AudioMessage_2 = {
    type: 'audio-message';
    role: 'user';
    /**
     * Base64-encoded audio data.
     */
    audio: string;
};

/**
 * Normalized events sent from the browser to the realtime model.
 * Each provider maps this to its native event format before sending
 * over the WebSocket.
 */
declare type RealtimeModelV4ClientEvent = {
    type: 'session-update';
    config: RealtimeModelV4SessionConfig;
} | {
    type: 'input-audio-append';
    /**
     * Base64-encoded audio chunk to append to the input buffer.
     */
    audio: string;
} | {
    type: 'input-audio-commit';
} | {
    type: 'input-audio-clear';
} | {
    type: 'conversation-item-create';
    item: RealtimeModelV4ConversationItem;
} | {
    type: 'conversation-item-truncate';
    /**
     * The ID of the assistant message item to truncate.
     */
    itemId: string;
    /**
     * The index of the content part to truncate.
     */
    contentIndex: number;
    /**
     * Truncate audio after this many milliseconds.
     */
    audioEndMs: number;
} | {
    type: 'response-create';
    options?: {
        modalities?: string[];
        instructions?: string;
        metadata?: Record<string, unknown>;
    };
} | {
    type: 'response-cancel';
};

/**
 * Normalized events sent from the browser to the realtime model.
 * Each provider maps this to its native event format before sending
 * over the WebSocket.
 */
declare type RealtimeModelV4ClientEvent_2 = {
    type: 'session-update';
    config: RealtimeModelV4SessionConfig_2;
} | {
    type: 'input-audio-append';
    /**
     * Base64-encoded audio chunk to append to the input buffer.
     */
    audio: string;
} | {
    type: 'input-audio-commit';
} | {
    type: 'input-audio-clear';
} | {
    type: 'conversation-item-create';
    item: RealtimeModelV4ConversationItem_2;
} | {
    type: 'conversation-item-truncate';
    /**
     * The ID of the assistant message item to truncate.
     */
    itemId: string;
    /**
     * The index of the content part to truncate.
     */
    contentIndex: number;
    /**
     * Truncate audio after this many milliseconds.
     */
    audioEndMs: number;
} | {
    type: 'response-create';
    options?: {
        modalities?: string[];
        instructions?: string;
        metadata?: Record<string, unknown>;
    };
} | {
    type: 'response-cancel';
};

/**
 * Options for creating an ephemeral client secret for browser-side
 * WebSocket connections to a realtime model.
 */
declare type RealtimeModelV4ClientSecretOptions = {
    /**
     * Number of seconds until the client secret expires.
     */
    expiresAfterSeconds?: number;
    /**
     * Optional session configuration to embed in the token request.
     * Some providers (e.g. Google) require the full session config at token creation time.
     */
    sessionConfig?: RealtimeModelV4SessionConfig;
};

/**
 * Options for creating an ephemeral client secret for browser-side
 * WebSocket connections to a realtime model.
 */
declare type RealtimeModelV4ClientSecretOptions_2 = {
    /**
     * Number of seconds until the client secret expires.
     */
    expiresAfterSeconds?: number;
    /**
     * Optional session configuration to embed in the token request.
     * Some providers (e.g. Google) require the full session config at token creation time.
     */
    sessionConfig?: RealtimeModelV4SessionConfig_2;
};

/**
 * Result of creating an ephemeral client secret.
 */
declare type RealtimeModelV4ClientSecretResult = {
    /**
     * The ephemeral token value. Used as a Bearer token or in the
     * WebSocket subprotocol header for authentication.
     */
    token: string;
    /**
     * The WebSocket URL to connect to. Includes any provider-specific
     * query parameters (e.g. model ID).
     */
    url: string;
    /**
     * Unix timestamp (seconds) when this client secret expires.
     */
    expiresAt?: number;
};

/**
 * Result of creating an ephemeral client secret.
 */
declare type RealtimeModelV4ClientSecretResult_2 = {
    /**
     * The ephemeral token value. Used as a Bearer token or in the
     * WebSocket subprotocol header for authentication.
     */
    token: string;
    /**
     * The WebSocket URL to connect to. Includes any provider-specific
     * query parameters (e.g. model ID).
     */
    url: string;
    /**
     * Unix timestamp (seconds) when this client secret expires.
     */
    expiresAt?: number;
};

/**
 * A conversation item that can be created by the client and sent to
 * the model via the conversation.item.create event.
 */
declare type RealtimeModelV4ConversationItem = RealtimeModelV4TextMessage | RealtimeModelV4AudioMessage | RealtimeModelV4FunctionCallOutput;

/**
 * A conversation item that can be created by the client and sent to
 * the model via the conversation.item.create event.
 */
declare type RealtimeModelV4ConversationItem_2 = RealtimeModelV4TextMessage_2 | RealtimeModelV4AudioMessage_2 | RealtimeModelV4FunctionCallOutput_2;

/**
 * The output of a function call, sent back to the model so it can
 * continue generating a response using the tool result.
 */
declare type RealtimeModelV4FunctionCallOutput = {
    type: 'function-call-output';
    /**
     * The call ID from the function-call-arguments-done event.
     * Must match so the model knows which function call this result is for.
     */
    callId: string;
    /**
     * The name of the function that was called.
     * Required by some providers (e.g. Google) in the tool response routing.
     */
    name?: string;
    /**
     * JSON string containing the function call result.
     */
    output: string;
};

/**
 * The output of a function call, sent back to the model so it can
 * continue generating a response using the tool result.
 */
declare type RealtimeModelV4FunctionCallOutput_2 = {
    type: 'function-call-output';
    /**
     * The call ID from the function-call-arguments-done event.
     * Must match so the model knows which function call this result is for.
     */
    callId: string;
    /**
     * The name of the function that was called.
     * Required by some providers (e.g. Google) in the tool response routing.
     */
    name?: string;
    /**
     * JSON string containing the function call result.
     */
    output: string;
};

/**
 * Normalized events emitted by the realtime model (model → browser).
 * Each provider maps its native event format to this discriminated union.
 *
 * Every event includes a `raw` field with the original provider-specific
 * event data for debugging and provider-specific access.
 */
declare type RealtimeModelV4ServerEvent = {
    type: 'session-created';
    sessionId?: string;
    raw: unknown;
} | {
    type: 'session-updated';
    raw: unknown;
} | {
    type: 'speech-started';
    itemId?: string;
    raw: unknown;
} | {
    type: 'speech-stopped';
    itemId?: string;
    raw: unknown;
} | {
    type: 'audio-committed';
    itemId?: string;
    previousItemId?: string;
    raw: unknown;
} | {
    type: 'conversation-item-added';
    itemId: string;
    item: unknown;
    raw: unknown;
} | {
    type: 'input-transcription-completed';
    itemId: string;
    transcript: string;
    raw: unknown;
} | {
    type: 'response-created';
    responseId: string;
    raw: unknown;
} | {
    type: 'response-done';
    responseId: string;
    status: string;
    raw: unknown;
} | {
    type: 'output-item-added';
    responseId: string;
    itemId: string;
    raw: unknown;
} | {
    type: 'output-item-done';
    responseId: string;
    itemId: string;
    raw: unknown;
} | {
    type: 'content-part-added';
    responseId: string;
    itemId: string;
    raw: unknown;
} | {
    type: 'content-part-done';
    responseId: string;
    itemId: string;
    raw: unknown;
} | {
    type: 'audio-delta';
    responseId: string;
    itemId: string;
    /**
     * Base64-encoded audio chunk.
     */
    delta: string;
    raw: unknown;
} | {
    type: 'audio-done';
    responseId: string;
    itemId: string;
    raw: unknown;
} | {
    type: 'audio-transcript-delta';
    responseId: string;
    itemId: string;
    /**
     * Text chunk of the audio transcript.
     */
    delta: string;
    raw: unknown;
} | {
    type: 'audio-transcript-done';
    responseId: string;
    itemId: string;
    transcript?: string;
    raw: unknown;
} | {
    type: 'text-delta';
    responseId: string;
    itemId: string;
    /**
     * Text chunk of the model's text response.
     */
    delta: string;
    raw: unknown;
} | {
    type: 'text-done';
    responseId: string;
    itemId: string;
    text?: string;
    raw: unknown;
} | {
    type: 'function-call-arguments-delta';
    responseId: string;
    itemId: string;
    callId: string;
    /**
     * Partial JSON string of function call arguments.
     */
    delta: string;
    raw: unknown;
} | {
    type: 'function-call-arguments-done';
    responseId: string;
    itemId: string;
    callId: string;
    /**
     * The name of the function to call.
     */
    name: string;
    /**
     * Complete JSON string of function call arguments.
     */
    arguments: string;
    raw: unknown;
} | {
    type: 'error';
    message: string;
    code?: string;
    raw: unknown;
} | {
    type: 'custom';
    /**
     * The original event type string from the provider.
     */
    rawType: string;
    raw: unknown;
};

/**
 * Normalized events emitted by the realtime model (model → browser).
 * Each provider maps its native event format to this discriminated union.
 *
 * Every event includes a `raw` field with the original provider-specific
 * event data for debugging and provider-specific access.
 */
declare type RealtimeModelV4ServerEvent_2 = {
    type: 'session-created';
    sessionId?: string;
    raw: unknown;
} | {
    type: 'session-updated';
    raw: unknown;
} | {
    type: 'speech-started';
    itemId?: string;
    raw: unknown;
} | {
    type: 'speech-stopped';
    itemId?: string;
    raw: unknown;
} | {
    type: 'audio-committed';
    itemId?: string;
    previousItemId?: string;
    raw: unknown;
} | {
    type: 'conversation-item-added';
    itemId: string;
    item: unknown;
    raw: unknown;
} | {
    type: 'input-transcription-completed';
    itemId: string;
    transcript: string;
    raw: unknown;
} | {
    type: 'response-created';
    responseId: string;
    raw: unknown;
} | {
    type: 'response-done';
    responseId: string;
    status: string;
    raw: unknown;
} | {
    type: 'output-item-added';
    responseId: string;
    itemId: string;
    raw: unknown;
} | {
    type: 'output-item-done';
    responseId: string;
    itemId: string;
    raw: unknown;
} | {
    type: 'content-part-added';
    responseId: string;
    itemId: string;
    raw: unknown;
} | {
    type: 'content-part-done';
    responseId: string;
    itemId: string;
    raw: unknown;
} | {
    type: 'audio-delta';
    responseId: string;
    itemId: string;
    /**
     * Base64-encoded audio chunk.
     */
    delta: string;
    raw: unknown;
} | {
    type: 'audio-done';
    responseId: string;
    itemId: string;
    raw: unknown;
} | {
    type: 'audio-transcript-delta';
    responseId: string;
    itemId: string;
    /**
     * Text chunk of the audio transcript.
     */
    delta: string;
    raw: unknown;
} | {
    type: 'audio-transcript-done';
    responseId: string;
    itemId: string;
    transcript?: string;
    raw: unknown;
} | {
    type: 'text-delta';
    responseId: string;
    itemId: string;
    /**
     * Text chunk of the model's text response.
     */
    delta: string;
    raw: unknown;
} | {
    type: 'text-done';
    responseId: string;
    itemId: string;
    text?: string;
    raw: unknown;
} | {
    type: 'function-call-arguments-delta';
    responseId: string;
    itemId: string;
    callId: string;
    /**
     * Partial JSON string of function call arguments.
     */
    delta: string;
    raw: unknown;
} | {
    type: 'function-call-arguments-done';
    responseId: string;
    itemId: string;
    callId: string;
    /**
     * The name of the function to call.
     */
    name: string;
    /**
     * Complete JSON string of function call arguments.
     */
    arguments: string;
    raw: unknown;
} | {
    type: 'error';
    message: string;
    code?: string;
    raw: unknown;
} | {
    type: 'custom';
    /**
     * The original event type string from the provider.
     */
    rawType: string;
    raw: unknown;
};

/**
 * Provider-neutral configuration for a realtime session.
 * Each provider maps this to their specific session.update payload.
 */
declare type RealtimeModelV4SessionConfig = {
    /**
     * System instructions for the model.
     */
    instructions?: string;
    /**
     * Voice to use for audio output.
     */
    voice?: string;
    /**
     * Which output modalities the model should produce.
     */
    outputModalities?: Array<'text' | 'audio'>;
    /**
     * Audio format configuration for input audio.
     */
    inputAudioFormat?: SharedV4AudioFormat;
    /**
     * Input audio transcription configuration.
     *
     * When enabled, providers that support input transcription emit normalized
     * `input-transcription-completed` events that can be rendered as user
     * messages.
     */
    inputAudioTranscription?: {
        /**
         * Provider-specific transcription model.
         */
        model?: string;
        /**
         * Optional language hint for the input audio.
         */
        language?: string;
        /**
         * Optional prompt to guide transcription.
         */
        prompt?: string;
    };
    /**
     * Output audio transcription configuration.
     *
     * When enabled, providers that support output transcription emit normalized
     * `audio-transcript-delta` / `audio-transcript-done` events for the model's
     * spoken response. Some providers transcribe output by default; setting this
     * makes the behavior explicit rather than relying on that default.
     */
    outputAudioTranscription?: {
        /**
         * Provider-specific transcription model.
         */
        model?: string;
        /**
         * Optional language hint for the output audio.
         */
        language?: string;
        /**
         * Optional prompt to guide transcription.
         */
        prompt?: string;
    };
    /**
     * Audio format configuration for output audio.
     */
    outputAudioFormat?: SharedV4AudioFormat;
    /**
     * Voice activity detection configuration.
     * Set to null or type 'disabled' to turn off VAD (push-to-talk mode).
     */
    turnDetection?: {
        /**
         * VAD mode. 'server-vad' for automatic detection,
         * 'semantic-vad' for OpenAI's semantic detection,
         * 'disabled' to turn off VAD.
         */
        type: 'server-vad' | 'semantic-vad' | 'disabled';
        /**
         * VAD activation threshold (0.0-1.0).
         * Higher values require louder audio to trigger.
         */
        threshold?: number;
        /**
         * How long the user must be silent (in ms) before
         * the server ends the turn.
         */
        silenceDurationMs?: number;
        /**
         * Amount of audio (in ms) to include before the
         * detected start of speech.
         */
        prefixPaddingMs?: number;
    } | null;
    /**
     * Tool definitions available to the model in this session.
     */
    tools?: RealtimeModelV4ToolDefinition[];
    /**
     * Provider-specific options that are passed through to the provider.
     */
    providerOptions?: Record<string, unknown>;
};

/**
 * Provider-neutral configuration for a realtime session.
 * Each provider maps this to their specific session.update payload.
 */
declare type RealtimeModelV4SessionConfig_2 = {
    /**
     * System instructions for the model.
     */
    instructions?: string;
    /**
     * Voice to use for audio output.
     */
    voice?: string;
    /**
     * Which output modalities the model should produce.
     */
    outputModalities?: Array<'text' | 'audio'>;
    /**
     * Audio format configuration for input audio.
     */
    inputAudioFormat?: SharedV4AudioFormat_2;
    /**
     * Input audio transcription configuration.
     *
     * When enabled, providers that support input transcription emit normalized
     * `input-transcription-completed` events that can be rendered as user
     * messages.
     */
    inputAudioTranscription?: {
        /**
         * Provider-specific transcription model.
         */
        model?: string;
        /**
         * Optional language hint for the input audio.
         */
        language?: string;
        /**
         * Optional prompt to guide transcription.
         */
        prompt?: string;
    };
    /**
     * Output audio transcription configuration.
     *
     * When enabled, providers that support output transcription emit normalized
     * `audio-transcript-delta` / `audio-transcript-done` events for the model's
     * spoken response. Some providers transcribe output by default; setting this
     * makes the behavior explicit rather than relying on that default.
     */
    outputAudioTranscription?: {
        /**
         * Provider-specific transcription model.
         */
        model?: string;
        /**
         * Optional language hint for the output audio.
         */
        language?: string;
        /**
         * Optional prompt to guide transcription.
         */
        prompt?: string;
    };
    /**
     * Audio format configuration for output audio.
     */
    outputAudioFormat?: SharedV4AudioFormat_2;
    /**
     * Voice activity detection configuration.
     * Set to null or type 'disabled' to turn off VAD (push-to-talk mode).
     */
    turnDetection?: {
        /**
         * VAD mode. 'server-vad' for automatic detection,
         * 'semantic-vad' for OpenAI's semantic detection,
         * 'disabled' to turn off VAD.
         */
        type: 'server-vad' | 'semantic-vad' | 'disabled';
        /**
         * VAD activation threshold (0.0-1.0).
         * Higher values require louder audio to trigger.
         */
        threshold?: number;
        /**
         * How long the user must be silent (in ms) before
         * the server ends the turn.
         */
        silenceDurationMs?: number;
        /**
         * Amount of audio (in ms) to include before the
         * detected start of speech.
         */
        prefixPaddingMs?: number;
    } | null;
    /**
     * Tool definitions available to the model in this session.
     */
    tools?: RealtimeModelV4ToolDefinition_2[];
    /**
     * Provider-specific options that are passed through to the provider.
     */
    providerOptions?: Record<string, unknown>;
};

/**
 * A text message from the user.
 */
declare type RealtimeModelV4TextMessage = {
    type: 'text-message';
    role: 'user';
    text: string;
};

/**
 * A text message from the user.
 */
declare type RealtimeModelV4TextMessage_2 = {
    type: 'text-message';
    role: 'user';
    text: string;
};

/**
 * A tool definition for realtime models. Sent as part of the session
 * configuration so the model knows which functions it can call.
 */
declare type RealtimeModelV4ToolDefinition = {
    /**
     * The type of the tool (always 'function').
     */
    type: 'function';
    /**
     * The name of the tool. Unique within the session.
     */
    name: string;
    /**
     * A description of what the tool does. The model uses this to decide
     * whether to call the tool.
     */
    description?: string;
    /**
     * JSON Schema describing the parameters the tool expects.
     */
    parameters: JSONSchema7;
};

/**
 * A tool definition for realtime models. Sent as part of the session
 * configuration so the model knows which functions it can call.
 */
declare type RealtimeModelV4ToolDefinition_2 = {
    /**
     * The type of the tool (always 'function').
     */
    type: 'function';
    /**
     * The name of the tool. Unique within the session.
     */
    name: string;
    /**
     * A description of what the tool does. The model uses this to decide
     * whether to call the tool.
     */
    description?: string;
    /**
     * JSON Schema describing the parameters the tool expects.
     */
    parameters: JSONSchema7;
};

/**
 * Reasoning file output of a text generation.
 * It contains a file generated as part of reasoning.
 */
export declare interface ReasoningFileOutput {
    type: 'reasoning-file';
    /**
     * The generated file.
     */
    file: GeneratedFile;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerMetadata?: ProviderMetadata;
}

/**
 * Reasoning file content part of a prompt. It contains a file generated as part of reasoning.
 */
declare interface ReasoningFilePart {
    type: 'reasoning-file';
    /**
     * Reasoning file data.
     *
     * Reasoning files originate from a model's reasoning output and are always
     * raw bytes or a fetchable URL. Unlike `FilePart.data`, the `reference` and
     * `text` shapes are not supported here: provider references describe files
     * uploaded by the user (not produced as model output), and reasoning text is
     * carried by `ReasoningPart` rather than as a file.
     *
     * Either a tagged shape or a bare shorthand:
     *
     * - `{ type: 'data', data }` or bare `DataContent`: raw bytes
     *   (base64 string, Uint8Array, ArrayBuffer, Buffer)
     * - `{ type: 'url', url }` or bare `URL`: a URL that points to the file
     */
    data: FileDataData | FileDataUrl | DataContent_2 | URL;
    /**
     * IANA media type of the file.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
}

/**
 * Reasoning file content part of a prompt. It contains a file generated as part of reasoning.
 */
declare interface ReasoningFilePart_2 {
    type: 'reasoning-file';
    /**
     * Reasoning file data.
     *
     * Reasoning files originate from a model's reasoning output and are always
     * raw bytes or a fetchable URL. Unlike `FilePart.data`, the `reference` and
     * `text` shapes are not supported here: provider references describe files
     * uploaded by the user (not produced as model output), and reasoning text is
     * carried by `ReasoningPart` rather than as a file.
     *
     * Either a tagged shape or a bare shorthand:
     *
     * - `{ type: 'data', data }` or bare `DataContent`: raw bytes
     *   (base64 string, Uint8Array, ArrayBuffer, Buffer)
     * - `{ type: 'url', url }` or bare `URL`: a URL that points to the file
     */
    data: FileDataData_2 | FileDataUrl_2 | DataContent | URL;
    /**
     * IANA media type of the file.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
}

/**
 * A reasoning file part of a message.
 */
export declare type ReasoningFileUIPart = {
    type: 'reasoning-file';
    /**
     * IANA media type of the file.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * The URL of the file.
     * It can either be a URL to a hosted file or a [Data URL](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/Data_URLs).
     */
    url: string;
    /**
     * The provider metadata.
     */
    providerMetadata?: ProviderMetadata;
};

declare type ReasoningLevel = Exclude<LanguageModelV4CallOptions['reasoning'], 'none' | 'provider-default' | undefined>;

declare type ReasoningLevel_2 = Exclude<LanguageModelV4CallOptions['reasoning'], 'none' | 'provider-default' | undefined>;

/**
 * Reasoning output of a text generation. It contains a reasoning.
 */
export declare interface ReasoningOutput {
    type: 'reasoning';
    /**
     * The reasoning text.
     */
    text: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerMetadata?: ProviderMetadata;
}

/**
 * Reasoning content part of a prompt. It contains a reasoning.
 */
declare interface ReasoningPart {
    type: 'reasoning';
    /**
     * The reasoning text.
     */
    text: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
}

/**
 * Reasoning content part of a prompt. It contains a reasoning.
 */
declare interface ReasoningPart_2 {
    type: 'reasoning';
    /**
     * The reasoning text.
     */
    text: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
}

/**
 * A reasoning part of a message.
 */
export declare type ReasoningUIPart = {
    type: 'reasoning';
    /**
     * The reasoning text.
     */
    text: string;
    /**
     * The state of the reasoning part.
     */
    state?: 'streaming' | 'done';
    /**
     * The provider metadata.
     */
    providerMetadata?: ProviderMetadata;
};

/**
 * Registers one or more telemetry integrations globally.
 */
export declare function registerTelemetry(...integrations: Telemetry[]): void;

/**
 * Removes entries from a record where the value is null or undefined.
 * @param record - The input object whose entries may be null or undefined.
 * @returns A new object containing only entries with non-null and non-undefined values.
 */
declare function removeUndefinedEntries<T>(record: Record<string, T | undefined>): Record<string, T>;

/**
 * Removes entries from a record where the value is null or undefined.
 * @param record - The input object whose entries may be null or undefined.
 * @returns A new object containing only entries with non-null and non-undefined values.
 */
declare function removeUndefinedEntries_2<T>(record: Record<string, T | undefined>): Record<string, T>;

/**
 * A function that attempts to repair the raw output of the model
 * to enable JSON parsing.
 *
 * Should return the repaired text or null if the text cannot be repaired.
 */
export declare type RepairTextFunction = (options: {
    text: string;
    error: JSONParseError | TypeValidationError;
}) => Promise<string | null>;

/**
 * Request-facing controls. These settings affect transport, retries,
 * cancellation, headers, and timeout – not model generation behavior.
 */
export declare type RequestOptions<TOOLS extends ToolSet = ToolSet> = {
    /**
     * Maximum number of retries. Set to 0 to disable retries.
     *
     * @default 2
     */
    maxRetries?: number;
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
    /**
     * Timeout configuration for the request.
     */
    timeout?: TimeoutConfiguration<TOOLS>;
};

/**
 * Builds the required portion of the tool context map for tools whose context
 * type does not include `undefined`.
 */
declare type RequiredToolSetContext<TOOLS extends ToolSet_2> = {
    [K in keyof TOOLS as InferToolContext<NoInfer<TOOLS[K]>> extends never ? never : undefined extends InferToolContext<NoInfer<TOOLS[K]>> ? never : K]: InferToolContext<NoInfer<TOOLS[K]>>;
};

/**
 * Builds the required portion of the tool context map for tools whose context
 * type does not include `undefined`.
 */
declare type RequiredToolSetContext_2<TOOLS extends ToolSet> = {
    [K in keyof TOOLS as InferToolContext_2<NoInfer<TOOLS[K]>> extends never ? never : undefined extends InferToolContext_2<NoInfer<TOOLS[K]>> ? never : K]: InferToolContext_2<NoInfer<TOOLS[K]>>;
};

/**
 * Rerank documents using a reranking model. The type of the value is defined by the reranking model.
 *
 * @param model - The reranking model to use.
 * @param documents - The documents that should be reranked.
 * @param query - The query to rerank the documents against.
 * @param topN - Number of top documents to return.
 *
 * @param maxRetries - Maximum number of retries. Set to 0 to disable retries. Default: 2.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param headers - Additional HTTP headers to be sent with the request. Only applicable for HTTP-based providers.
 * @param providerOptions - Additional provider-specific options.
 * @param telemetry - Optional telemetry configuration.
 *
 * @returns A result object that contains the reranked documents, the reranked indices, and additional information.
 */
export declare function rerank<VALUE extends JSONObject_2 | string>({ model: modelArg, documents, query, topN, maxRetries: maxRetriesArg, abortSignal, headers, providerOptions, experimental_telemetry, telemetry, onStart, experimental_onStart, onEnd, experimental_onEnd, _internal: { generateCallId }, }: {
    /**
     * The reranking model to use.
     */
    model: RerankingModel;
    /**
     * The documents that should be reranked.
     */
    documents: Array<VALUE>;
    /**
     * The query to rerank the documents against.
     */
    query: string;
    /**
     * Number of top documents to return.
     */
    topN?: number;
    /**
     * Maximum number of retries per reranking model call. Set to 0 to disable retries.
     *
     * @default 2
     */
    maxRetries?: number;
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional headers to include in the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string>;
    /**
     * Optional telemetry configuration.
     */
    telemetry?: TelemetryOptions;
    /**
     * Optional telemetry configuration.
     *
     * @deprecated Use `telemetry` instead. This alias will be removed in a future major release.
     */
    experimental_telemetry?: TelemetryOptions;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Callback that is called when the rerank operation begins,
     * before the reranking model is called.
     */
    onStart?: Callback<RerankStartEvent>;
    /**
     * Callback that is called when the rerank operation begins,
     * before the reranking model is called.
     *
     * @deprecated Use `onStart` instead.
     */
    experimental_onStart?: Callback<RerankStartEvent>;
    /**
     * Callback that is called when the rerank operation completes,
     * after the reranking model returns.
     */
    onEnd?: Callback<RerankEndEvent>;
    /**
     * Callback that is called when the rerank operation completes,
     * after the reranking model returns.
     *
     * @deprecated Use `onEnd` instead.
     */
    experimental_onEnd?: Callback<RerankEndEvent>;
    /**
     * Internal. For test use only. May change without notice.
     */
    _internal?: {
        generateCallId?: () => string;
    };
}): Promise<RerankResult<VALUE>>;

/**
 * Event passed to the `onEnd` callback for rerank operations.
 *
 * Called when the operation completes, after the reranking model returns.
 */
export declare type RerankEndEvent = {
    /** Unique identifier for this rerank call, used to correlate events. */
    readonly callId: string;
    /** Identifies the operation type ('ai.rerank'). */
    readonly operationId: string;
    readonly provider: string;
    /** The specific model identifier (e.g., 'gpt-4o'). */
    readonly modelId: string;
    /** The documents that were reranked. */
    readonly documents: Array<JSONObject_2 | string>;
    /** The query that documents were reranked against. */
    readonly query: string;
    /** The reranked results sorted by relevance score in descending order. */
    readonly ranking: Array<{
        originalIndex: number;
        score: number;
        document: JSONObject_2 | string;
    }>;
    /** Warnings from the reranking model. */
    readonly warnings: Array<Warning>;
    /** Optional provider-specific metadata. */
    readonly providerMetadata: ProviderMetadata | undefined;
    /** Response data including headers and body. */
    readonly response: {
        id?: string;
        timestamp: Date;
        modelId: string;
        headers?: Record<string, string>;
        body?: unknown;
    };
};

/**
 * Reranking model that is used by the AI SDK.
 */
export declare type RerankingModel = string | RerankingModelV4_2 | RerankingModelV3;

/**
 * Event fired when an individual reranking model call (doRerank) completes.
 *
 * Contains the ranking results from the model response.
 */
export declare type RerankingModelCallEndEvent = {
    /** Unique identifier for this rerank call, used to correlate events. */
    readonly callId: string;
    /** Identifies the inner operation ('ai.rerank.doRerank'). */
    readonly operationId: string;
    /** The provider identifier. */
    readonly provider: string;
    /** The specific model identifier. */
    readonly modelId: string;
    /** The type of documents ('text' or 'object'). */
    readonly documentsType: string;
    /** The ranking results from the model. */
    readonly ranking: Array<{
        index: number;
        relevanceScore: number;
    }>;
};

/**
 * Event fired when an individual reranking model call (inner doRerank) begins.
 */
export declare type RerankingModelCallStartEvent = {
    /** Unique identifier for this rerank call, used to correlate events. */
    readonly callId: string;
    /** Identifies the inner operation ('ai.rerank.doRerank'). */
    readonly operationId: string;
    /** The provider identifier. */
    readonly provider: string;
    /** The specific model identifier. */
    readonly modelId: string;
    /** The documents being reranked. */
    readonly documents: Array<JSONObject_2 | string>;
    /** The type of documents ('text' or 'object'). */
    readonly documentsType: string;
    /** The query to rerank against. */
    readonly query: string;
    /** Number of top documents to return. */
    readonly topN: number | undefined;
};

/**
 * Specification for a reranking model that implements the reranking model interface version 3.
 */
declare type RerankingModelV3 = {
    /**
     * The reranking model must specify which reranking model interface version it implements.
     */
    readonly specificationVersion: 'v3';
    /**
     * Provider ID.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID.
     */
    readonly modelId: string;
    /**
     * Reranking a list of documents using the query.
     */
    doRerank(options: RerankingModelV3CallOptions): PromiseLike<{
        /**
         * Ordered list of reranked documents (via index before reranking).
         * The documents are sorted by the descending order of relevance scores.
         */
        ranking: Array<{
            /**
             * The index of the document in the original list of documents before reranking.
             */
            index: number;
            /**
             * The relevance score of the document after reranking.
             */
            relevanceScore: number;
        }>;
        /**
         * Additional provider-specific metadata. They are passed through
         * to the provider from the AI SDK and enable provider-specific
         * functionality that can be fully encapsulated in the provider.
         */
        providerMetadata?: SharedV3ProviderMetadata;
        /**
         * Warnings for the call, e.g. unsupported settings.
         */
        warnings?: Array<SharedV3Warning>;
        /**
         * Optional response information for debugging purposes.
         */
        response?: {
            /**
             * ID for the generated response, if the provider sends one.
             */
            id?: string;
            /**
             * Timestamp for the start of the generated response, if the provider sends one.
             */
            timestamp?: Date;
            /**
             * The ID of the response model that was used to generate the response, if the provider sends one.
             */
            modelId?: string;
            /**
             * Response headers.
             */
            headers?: SharedV3Headers;
            /**
             * Response body.
             */
            body?: unknown;
        };
    }>;
};

declare type RerankingModelV3CallOptions = {
    /**
     * Documents to rerank.
     * Either a list of texts or a list of JSON objects.
     */
    documents: {
        type: 'text';
        values: string[];
    } | {
        type: 'object';
        values: JSONObject_2[];
    };
    /**
     * The query is a string that represents the query to rerank the documents against.
     */
    query: string;
    /**
     * Optional limit returned documents to the top n documents.
     */
    topN?: number;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV3ProviderOptions;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: SharedV3Headers;
};

/**
 * Specification for a reranking model that implements the reranking model interface version 4.
 */
declare type RerankingModelV4 = {
    /**
     * The reranking model must specify which reranking model interface version it implements.
     */
    readonly specificationVersion: 'v4';
    /**
     * Provider ID.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID.
     */
    readonly modelId: string;
    /**
     * Reranking a list of documents using the query.
     */
    doRerank(options: RerankingModelV4CallOptions): PromiseLike<RerankingModelV4Result>;
};

/**
 * Specification for a reranking model that implements the reranking model interface version 4.
 */
declare type RerankingModelV4_2 = {
    /**
     * The reranking model must specify which reranking model interface version it implements.
     */
    readonly specificationVersion: 'v4';
    /**
     * Provider ID.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID.
     */
    readonly modelId: string;
    /**
     * Reranking a list of documents using the query.
     */
    doRerank(options: RerankingModelV4CallOptions_2): PromiseLike<RerankingModelV4Result_2>;
};

declare type RerankingModelV4CallOptions = {
    /**
     * Documents to rerank.
     * Either a list of texts or a list of JSON objects.
     */
    documents: {
        type: 'text';
        values: string[];
    } | {
        type: 'object';
        values: JSONObject[];
    };
    /**
     * The query is a string that represents the query to rerank the documents against.
     */
    query: string;
    /**
     * Optional limit returned documents to the top n documents.
     */
    topN?: number;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: SharedV4Headers;
};

declare type RerankingModelV4CallOptions_2 = {
    /**
     * Documents to rerank.
     * Either a list of texts or a list of JSON objects.
     */
    documents: {
        type: 'text';
        values: string[];
    } | {
        type: 'object';
        values: JSONObject_2[];
    };
    /**
     * The query is a string that represents the query to rerank the documents against.
     */
    query: string;
    /**
     * Optional limit returned documents to the top n documents.
     */
    topN?: number;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: SharedV4ProviderOptions_2;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: SharedV4Headers_2;
};

/**
 * The result of a reranking model doRerank call.
 */
declare type RerankingModelV4Result = {
    /**
     * Ordered list of reranked documents (via index before reranking).
     * The documents are sorted by the descending order of relevance scores.
     */
    ranking: Array<{
        /**
         * The index of the document in the original list of documents before reranking.
         */
        index: number;
        /**
         * The relevance score of the document after reranking.
         */
        relevanceScore: number;
    }>;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV4ProviderMetadata;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings?: Array<SharedV4Warning>;
    /**
     * Optional response information for debugging purposes.
     */
    response?: {
        /**
         * ID for the generated response, if the provider sends one.
         */
        id?: string;
        /**
         * Timestamp for the start of the generated response, if the provider sends one.
         */
        timestamp?: Date;
        /**
         * The ID of the response model that was used to generate the response, if the provider sends one.
         */
        modelId?: string;
        /**
         * Response headers.
         */
        headers?: SharedV4Headers;
        /**
         * Response body.
         */
        body?: unknown;
    };
};

/**
 * The result of a reranking model doRerank call.
 */
declare type RerankingModelV4Result_2 = {
    /**
     * Ordered list of reranked documents (via index before reranking).
     * The documents are sorted by the descending order of relevance scores.
     */
    ranking: Array<{
        /**
         * The index of the document in the original list of documents before reranking.
         */
        index: number;
        /**
         * The relevance score of the document after reranking.
         */
        relevanceScore: number;
    }>;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings?: Array<SharedV4Warning_2>;
    /**
     * Optional response information for debugging purposes.
     */
    response?: {
        /**
         * ID for the generated response, if the provider sends one.
         */
        id?: string;
        /**
         * Timestamp for the start of the generated response, if the provider sends one.
         */
        timestamp?: Date;
        /**
         * The ID of the response model that was used to generate the response, if the provider sends one.
         */
        modelId?: string;
        /**
         * Response headers.
         */
        headers?: SharedV4Headers_2;
        /**
         * Response body.
         */
        body?: unknown;
    };
};

/**
 * The result of a `rerank` call.
 * It contains the original documents, the reranked documents, and additional information.
 */
export declare interface RerankResult<VALUE> {
    /**
     * The original documents that were reranked.
     */
    readonly originalDocuments: Array<VALUE>;
    /**
     * Reranked documents.
     *
     * Sorted by relevance score in descending order.
     *
     * Can be less than the original documents if there was a topN limit.
     */
    readonly rerankedDocuments: Array<VALUE>;
    /**
     * The ranking is a list of objects with the original index,
     * relevance score, and the reranked document.
     *
     * Sorted by relevance score in descending order.
     *
     * Can be less than the original documents if there was a topN limit.
     */
    readonly ranking: Array<{
        originalIndex: number;
        score: number;
        document: VALUE;
    }>;
    /**
     * Optional provider-specific metadata.
     */
    readonly providerMetadata?: ProviderMetadata;
    /**
     * Optional raw response data.
     */
    readonly response: {
        /**
         * ID for the generated response if the provider sends one.
         */
        id?: string;
        /**
         * Timestamp of the generated response.
         */
        timestamp: Date;
        /**
         * The ID of the model that was used to generate the response.
         */
        modelId: string;
        /**
         * Response headers.
         */
        headers?: Record<string, string>;
        /**
         * The response body.
         */
        body?: unknown;
    };
}

/**
 * Event passed to the `onStart` callback for rerank operations.
 *
 * Called when the operation begins, before the reranking model is called.
 */
export declare type RerankStartEvent = {
    /** Unique identifier for this rerank call, used to correlate events. */
    readonly callId: string;
    /** Identifies the operation type ('ai.rerank'). */
    readonly operationId: string;
    readonly provider: string;
    /** The specific model identifier (e.g., 'gpt-4o'). */
    readonly modelId: string;
    /** The documents being reranked. */
    readonly documents: Array<JSONObject_2 | string>;
    /** The query to rerank the documents against. */
    readonly query: string;
    /** Number of top documents to return. */
    readonly topN: number | undefined;
    /** Maximum number of retries for failed requests. */
    readonly maxRetries: number;
    /** Additional HTTP headers sent with the request. */
    readonly headers: Record<string, string | undefined> | undefined;
    /** Additional provider-specific options. */
    readonly providerOptions: ProviderOptions_2 | undefined;
};

/**
 * A value or a lazy provider of a value, each of which may be synchronous or asynchronous.
 *
 * @template T The resolved type after {@link resolve} runs.
 *
 * One of:
 * - A plain value of type {@link T}
 * - A {@link PromiseLike} of {@link T} (e.g. a `Promise<T>`)
 * - A zero-argument function that returns a plain {@link T}
 * - A zero-argument function that returns a {@link PromiseLike} of {@link T}
 *
 * The function form is only invoked when passed to {@link resolve}; it is not distinguished from
 * a {@link T} that happens to be a function—callers should wrap function values if disambiguation
 * is required.
 */
declare type Resolvable<T> = MaybePromiseLike<T> | (() => MaybePromiseLike<T>);

/**
 * A value or a lazy provider of a value, each of which may be synchronous or asynchronous.
 *
 * @template T The resolved type after {@link resolve} runs.
 *
 * One of:
 * - A plain value of type {@link T}
 * - A {@link PromiseLike} of {@link T} (e.g. a `Promise<T>`)
 * - A zero-argument function that returns a plain {@link T}
 * - A zero-argument function that returns a {@link PromiseLike} of {@link T}
 *
 * The function form is only invoked when passed to {@link resolve}; it is not distinguished from
 * a {@link T} that happens to be a function—callers should wrap function values if disambiguation
 * is required.
 */
declare type Resolvable_2<T> = MaybePromiseLike_2<T> | (() => MaybePromiseLike_2<T>);

/**
 * Resolves a value that could be a raw value, a Promise, a function returning a value,
 * or a function returning a Promise.
 */
declare function resolve<T>(value: Resolvable<T>): Promise<T>;

/**
 * Resolves a value that could be a raw value, a Promise, a function returning a value,
 * or a function returning a Promise.
 */
declare function resolve_2<T>(value: Resolvable_2<T>): Promise<T>;

/**
 * Resolves a file part's media type to a full `type/subtype` form required by
 * providers whose API demands the full IANA media type.
 *
 * - If `part.mediaType` is already a full media type (e.g. `image/png`), it is
 *   returned as-is.
 * - Otherwise, when inline bytes are available (`part.data.type === 'data'`),
 *   the subtype is sniffed from the bytes using the signature table that
 *   corresponds to the top-level segment.
 * - When neither applies (e.g. top-level-only with a URL source, or bytes that
 *   cannot be detected), an `UnsupportedFunctionalityError` is thrown.
 */
declare function resolveFullMediaType({ part, }: {
    part: LanguageModelV4FilePart;
}): string;

/**
 * Resolves a file part's media type to a full `type/subtype` form required by
 * providers whose API demands the full IANA media type.
 *
 * - If `part.mediaType` is already a full media type (e.g. `image/png`), it is
 *   returned as-is.
 * - Otherwise, when inline bytes are available (`part.data.type === 'data'`),
 *   the subtype is sniffed from the bytes using the signature table that
 *   corresponds to the top-level segment.
 * - When neither applies (e.g. top-level-only with a URL source, or bytes that
 *   cannot be detected), an `UnsupportedFunctionalityError` is thrown.
 */
declare function resolveFullMediaType_2({ part, }: {
    part: LanguageModelV4FilePart;
}): string;

/**
 * Resolves a provider reference to the provider-specific identifier for the
 * given provider. Throws `NoSuchProviderReferenceError` if the provider is not
 * found in the reference mapping.
 */
declare function resolveProviderReference({ reference, provider, }: {
    reference: SharedV4ProviderReference_2;
    provider: string;
}): string;

/**
 * Resolves a provider reference to the provider-specific identifier for the
 * given provider. Throws `NoSuchProviderReferenceError` if the provider is not
 * found in the reference mapping.
 */
declare function resolveProviderReference_2({ reference, provider, }: {
    reference: SharedV4ProviderReference_2;
    provider: string;
}): string;

declare type ResponseHandler<RETURN_TYPE> = (options: {
    url: string;
    requestBodyValues: unknown;
    response: Response;
}) => PromiseLike<{
    value: RETURN_TYPE;
    rawValue?: unknown;
    responseHeaders?: Record<string, string>;
}>;

declare type ResponseHandler_2<RETURN_TYPE> = (options: {
    url: string;
    requestBodyValues: unknown;
    response: Response;
}) => PromiseLike<{
    value: RETURN_TYPE;
    rawValue?: unknown;
    responseHeaders?: Record<string, string>;
}>;

/**
 * A message that was generated during the generation process.
 * It can be either an assistant message or a tool message.
 */
declare type ResponseMessage = AssistantModelMessage | ToolModelMessage;

declare type RetryDelayProvider = ({ error, exponentialBackoffDelay, }: {
    error: unknown;
    exponentialBackoffDelay: number;
}) => number;

declare type RetryDelayProvider_2 = ({ error, exponentialBackoffDelay, }: {
    error: unknown;
    exponentialBackoffDelay: number;
}) => number;

export declare class RetryError extends AISDKError {
    private readonly [symbol$1_5];
    readonly reason: RetryErrorReason_2;
    readonly lastError: unknown;
    readonly errors: Array<unknown>;
    constructor({ message, reason, errors, }: {
        message: string;
        reason: RetryErrorReason_2;
        errors: Array<unknown>;
    });
    static isInstance(error: unknown): error is RetryError;
}

declare type RetryErrorFactory = ({ message, reason, errors, }: {
    message: string;
    reason: RetryErrorReason;
    errors: Array<unknown>;
}) => unknown;

declare type RetryErrorFactory_2 = ({ message, reason, errors, }: {
    message: string;
    reason: RetryErrorReason_3;
    errors: Array<unknown>;
}) => unknown;

declare type RetryErrorReason = 'maxRetriesExceeded' | 'errorNotRetryable';

declare type RetryErrorReason_2 = 'maxRetriesExceeded' | 'errorNotRetryable' | 'abort';

declare type RetryErrorReason_3 = 'maxRetriesExceeded' | 'errorNotRetryable';

declare type RetryFunction = <OUTPUT>(fn: () => PromiseLike<OUTPUT>) => PromiseLike<OUTPUT>;

declare type RetryFunction_2 = <OUTPUT>(fn: () => PromiseLike<OUTPUT>) => PromiseLike<OUTPUT>;

/**
 * Retries a failed operation with exponential backoff.
 */
declare const retryWithExponentialBackoff: ({ maxRetries, initialDelayInMs, backoffFactor, abortSignal, shouldRetry, getDelayInMs, createRetryError, }: {
    maxRetries?: number;
    initialDelayInMs?: number;
    backoffFactor?: number;
    abortSignal?: AbortSignal;
    shouldRetry: ShouldRetryFunction;
    getDelayInMs?: RetryDelayProvider;
    createRetryError?: RetryErrorFactory;
}) => RetryFunction;

/**
 * Retries a failed operation with exponential backoff.
 */
declare const retryWithExponentialBackoff_2: ({ maxRetries, initialDelayInMs, backoffFactor, abortSignal, shouldRetry, getDelayInMs, createRetryError, }: {
    maxRetries?: number;
    initialDelayInMs?: number;
    backoffFactor?: number;
    abortSignal?: AbortSignal;
    shouldRetry: ShouldRetryFunction_2;
    getDelayInMs?: RetryDelayProvider_2;
    createRetryError?: RetryErrorFactory_2;
}) => RetryFunction_2;

/**
 * Safely parses a JSON string and returns the result as an object of type `unknown`.
 *
 * @param text - The JSON string to parse.
 * @returns {Promise<object>} Either an object with `success: true` and the parsed data, or an object with `success: false` and the error that occurred.
 */
declare function safeParseJSON(options: {
    text: string;
    schema?: undefined;
}): Promise<ParseResult<JSONValue_2>>;

/**
 * Safely parses a JSON string into a strongly-typed object, using a provided schema to validate the object.
 *
 * @template T - The type of the object to parse the JSON into.
 * @param {string} text - The JSON string to parse.
 * @param {Validator<T>} schema - The schema to use for parsing the JSON.
 * @returns An object with either a `success` flag and the parsed and typed data, or a `success` flag and an error object.
 */
declare function safeParseJSON<T>(options: {
    text: string;
    schema: FlexibleSchema_2<T>;
}): Promise<ParseResult<T>>;

/**
 * Safely parses a JSON string and returns the result as an object of type `unknown`.
 *
 * @param text - The JSON string to parse.
 * @returns {Promise<object>} Either an object with `success: true` and the parsed data, or an object with `success: false` and the error that occurred.
 */
declare function safeParseJSON_2(options: {
    text: string;
    schema?: undefined;
}): Promise<ParseResult_2<JSONValue_2>>;

/**
 * Safely parses a JSON string into a strongly-typed object, using a provided schema to validate the object.
 *
 * @template T - The type of the object to parse the JSON into.
 * @param {string} text - The JSON string to parse.
 * @param {Validator<T>} schema - The schema to use for parsing the JSON.
 * @returns An object with either a `success` flag and the parsed and typed data, or a `success` flag and an error object.
 */
declare function safeParseJSON_2<T>(options: {
    text: string;
    schema: FlexibleSchema<T>;
}): Promise<ParseResult_2<T>>;

/**
 * Safely validates the types of an unknown object using a schema and
 * return a strongly-typed object.
 *
 * @template T - The type of the object to validate.
 * @param {string} options.value - The JSON object to validate.
 * @param {Validator<T>} options.schema - The schema to use for validating the JSON.
 * @param {TypeValidationContext} options.context - Optional context about what is being validated.
 * @returns An object with either a `success` flag and the parsed and typed data, or a `success` flag and an error object.
 */
declare function safeValidateTypes<OBJECT>({ value, schema, context, }: {
    value: unknown;
    schema: FlexibleSchema_2<OBJECT>;
    context?: TypeValidationContext_2;
}): Promise<{
    success: true;
    value: OBJECT;
    rawValue: unknown;
} | {
    success: false;
    error: TypeValidationError_2;
    rawValue: unknown;
}>;

/**
 * Safely validates the types of an unknown object using a schema and
 * return a strongly-typed object.
 *
 * @template T - The type of the object to validate.
 * @param {string} options.value - The JSON object to validate.
 * @param {Validator<T>} options.schema - The schema to use for validating the JSON.
 * @param {TypeValidationContext} options.context - Optional context about what is being validated.
 * @returns An object with either a `success` flag and the parsed and typed data, or a `success` flag and an error object.
 */
declare function safeValidateTypes_2<OBJECT>({ value, schema, context, }: {
    value: unknown;
    schema: FlexibleSchema<OBJECT>;
    context?: TypeValidationContext_2;
}): Promise<{
    success: true;
    value: OBJECT;
    rawValue: unknown;
} | {
    success: false;
    error: TypeValidationError_2;
    rawValue: unknown;
}>;

/**
 * Validates a list of UI messages like `validateUIMessages`,
 * but instead of throwing it returns `{ success: true, data }`
 * or `{ success: false, error }`.
 */
export declare function safeValidateUIMessages<UI_MESSAGE extends UIMessage>({ messages, metadataSchema, dataSchemas, tools, }: {
    messages: unknown;
    metadataSchema?: FlexibleSchema<UIMessage['metadata']>;
    dataSchemas?: {
        [NAME in keyof InferUIMessageData<UI_MESSAGE> & string]?: FlexibleSchema<InferUIMessageData<UI_MESSAGE>[NAME]>;
    };
    tools?: {
        [NAME in keyof InferUIMessageTools<UI_MESSAGE> & string]?: Tool<InferUIMessageTools<UI_MESSAGE>[NAME]['input'], InferUIMessageTools<UI_MESSAGE>[NAME]['output']>;
    };
}): Promise<SafeValidateUIMessagesResult<UI_MESSAGE>>;

export declare type SafeValidateUIMessagesResult<UI_MESSAGE extends UIMessage> = {
    success: true;
    data: Array<UI_MESSAGE>;
} | {
    success: false;
    error: Error;
};

/**
 * Handle to a long-running process started via `SandboxSession.spawn`.
 */
declare type SandboxProcess = {
    /**
     * Process identifier, if the sandbox implementation exposes one.
     */
    readonly pid?: number;
    /**
     * Stream of bytes written by the process to standard output.
     */
    readonly stdout: ReadableStream<Uint8Array>;
    /**
     * Stream of bytes written by the process to standard error.
     */
    readonly stderr: ReadableStream<Uint8Array>;
    /**
     * Resolve when the process exits, yielding its exit code.
     */
    wait(): PromiseLike<{
        exitCode: number;
    }>;
    /**
     * Terminate the process. Idempotent.
     */
    kill(): PromiseLike<void>;
};

/**
 * Options for executing a command in the sandbox via `run` or `spawn`.
 */
declare type SandboxProcessOptions = {
    /**
     * Command to execute in the sandbox.
     */
    command: string;
    /**
     * Working directory to execute the command in.
     */
    workingDirectory?: string;
    /**
     * Environment variables to set for this command. Merged with the
     * sandbox's default environment; values here take precedence.
     * Supporting environment variables as an option is preferable from a
     * security perspective, e.g. to avoid them leaking in logs.
     */
    env?: Record<string, string>;
    /**
     * Signal that can be used to abort the command. When aborted, the running
     * process is killed; for `spawn`, `wait()` rejects with the abort reason.
     */
    abortSignal?: AbortSignal;
};

/**
 * Options for executing a command in the sandbox via `run` or `spawn`.
 */
declare type SandboxProcessOptions_2 = {
    /**
     * Command to execute in the sandbox.
     */
    command: string;
    /**
     * Working directory to execute the command in.
     */
    workingDirectory?: string;
    /**
     * Environment variables to set for this command. Merged with the
     * sandbox's default environment; values here take precedence.
     * Supporting environment variables as an option is preferable from a
     * security perspective, e.g. to avoid them leaking in logs.
     */
    env?: Record<string, string>;
    /**
     * Signal that can be used to abort the command. When aborted, the running
     * process is killed; for `spawn`, `wait()` rejects with the abort reason.
     */
    abortSignal?: AbortSignal;
};

/**
 * Sandbox session that can execute commands and read/write files.
 */
declare type SandboxSession = {
    /**
     * Description of the sandbox environment that can be added to the agent's instructions
     * so that the agent knows about relevant details such as the root directory, exposed
     * ports, the public hostname, etc.
     */
    readonly description: string;
    /**
     * Read one file from the sandbox as a stream of bytes. Resolves to `null`
     * when the file does not exist.
     *
     * Relative path handling is implementation-defined. This is the lowest-level
     * read primitive; prefer `readBinaryFile` or `readTextFile` unless you need
     * to stream bytes.
     */
    readonly readFile: (options: ReadFileOptions) => PromiseLike<ReadableStream<Uint8Array> | null>;
    /**
     * Read one file from the sandbox as raw bytes. Resolves to `null` when the
     * file does not exist.
     */
    readonly readBinaryFile: (options: ReadFileOptions) => PromiseLike<Uint8Array | null>;
    /**
     * Read one text file from the sandbox, decoded using the requested encoding.
     * Resolves to `null` when the file does not exist.
     *
     * Line ranges are 1-based and inclusive. When `endLine` is past EOF the read
     * returns through EOF without error.
     */
    readonly readTextFile: (options: ReadFileOptions & {
        /**
         * Text encoding used to decode the file bytes. Defaults to `"utf-8"`.
         */
        encoding?: string;
        /**
         * 1-based inclusive start line. Defaults to 1.
         */
        startLine?: number;
        /**
         * 1-based inclusive end line. When past the file's line count, the read
         * returns through EOF without error.
         */
        endLine?: number;
    }) => PromiseLike<string | null>;
    /**
     * Write one file to the sandbox from a stream of bytes. Creates parent
     * directories recursively and overwrites any existing file.
     *
     * This is the lowest-level write primitive; prefer `writeBinaryFile` or
     * `writeTextFile` when the full content is already materialized in memory.
     */
    readonly writeFile: (options: WriteFileOptions<ReadableStream<Uint8Array>>) => PromiseLike<void>;
    /**
     * Write one file to the sandbox from raw bytes. Creates parent directories
     * recursively and overwrites any existing file.
     */
    readonly writeBinaryFile: (options: WriteFileOptions<Uint8Array>) => PromiseLike<void>;
    /**
     * Write one file to the sandbox from a string, encoded using the requested
     * encoding. Creates parent directories recursively and overwrites any
     * existing file.
     */
    readonly writeTextFile: (options: WriteFileOptions<string> & {
        /**
         * Text encoding used to encode the string to bytes. Defaults to `"utf-8"`.
         */
        encoding?: string;
    }) => PromiseLike<void>;
    /**
     * Spawn a long-running process in the sandbox. Returns immediately with a
     * handle that streams stdout/stderr, can be waited on, and can be killed.
     *
     * `run` is conceptually a thin wrapper over this primitive: spawn,
     * collect both streams to strings, await `wait()`, return the result.
     */
    readonly spawn: (options: SandboxProcessOptions) => PromiseLike<SandboxProcess>;
    /**
     * Run a command in the sandbox.
     */
    readonly run: (options: SandboxProcessOptions) => PromiseLike<{
        /**
         * Exit code returned by the command.
         */
        exitCode: number;
        /**
         * Standard output produced by the command.
         */
        stdout: string;
        /**
         * Standard error produced by the command.
         */
        stderr: string;
    }>;
};

export declare type Schema<OBJECT = unknown> = {
    /**
     * Used to mark schemas so we can support both Zod and custom schemas.
     */
    [schemaSymbol_2]: true;
    /**
     * Schema type for inference.
     */
    _type: OBJECT;
    /**
     * Optional. Validates that the structure of a value matches this schema,
     * and returns a typed version of the value if it does.
     */
    readonly validate?: (value: unknown) => ValidationResult_2<OBJECT> | PromiseLike<ValidationResult_2<OBJECT>>;
    /**
     * The JSON Schema for the schema. It is passed to the providers.
     */
    readonly jsonSchema: JSONSchema7 | PromiseLike<JSONSchema7>;
};

declare type Schema_2<OBJECT = unknown> = {
    /**
     * Used to mark schemas so we can support both Zod and custom schemas.
     */
    [schemaSymbol]: true;
    /**
     * Schema type for inference.
     */
    _type: OBJECT;
    /**
     * Optional. Validates that the structure of a value matches this schema,
     * and returns a typed version of the value if it does.
     */
    readonly validate?: (value: unknown) => ValidationResult<OBJECT> | PromiseLike<ValidationResult<OBJECT>>;
    /**
     * The JSON Schema for the schema. It is passed to the providers.
     */
    readonly jsonSchema: JSONSchema7 | PromiseLike<JSONSchema7>;
};

/**
 * Used to mark schemas so we can support both Zod and custom schemas.
 */
declare const schemaSymbol: unique symbol;

/**
 * Used to mark schemas so we can support both Zod and custom schemas.
 */
declare const schemaSymbol_2: unique symbol;

declare function secureJsonParse(text: string): any;

declare function secureJsonParse_2(text: string): any;

declare class SerializationError extends AISDKError_2 {
    private readonly [symbol_2];
    constructor({ message, cause, }?: {
        message?: string;
        cause?: unknown;
    });
    static isInstance(error: unknown): error is SerializationError;
}

declare class SerializationError_2 extends AISDKError_2 {
    private readonly [symbol_4];
    constructor({ message, cause, }?: {
        message?: string;
        cause?: unknown;
    });
    static isInstance(error: unknown): error is SerializationError_2;
}

/**
 * Serializes a model instance for workflow step boundaries.
 * Returns the `modelId` plus the JSON-serializable config properties.
 *
 * Non-serializable values are omitted. As a special case, a
 * function-valued `headers` property is resolved during serialization
 * and included if the returned value is JSON-serializable.
 *
 * Used as the body of `static [WORKFLOW_SERIALIZE]` in provider models.
 *
 * @example
 * ```ts
 * static [WORKFLOW_SERIALIZE](model: MyLanguageModel) {
 *   return serializeModelOptions({
 *     modelId: model.modelId,
 *     config: model.config,
 *   });
 * }
 * ```
 */
declare function serializeModelOptions<CONFIG extends {
    headers?: Resolvable<Record<string, string | undefined>>;
}>(options: {
    modelId: string;
    config: CONFIG;
}): {
    modelId: string;
    config: JSONObject;
};

/**
 * Serializes a model instance for workflow step boundaries.
 * Returns the `modelId` plus the JSON-serializable config properties.
 *
 * Non-serializable values are omitted. As a special case, a
 * function-valued `headers` property is resolved during serialization
 * and included if the returned value is JSON-serializable.
 *
 * Used as the body of `static [WORKFLOW_SERIALIZE]` in provider models.
 *
 * @example
 * ```ts
 * static [WORKFLOW_SERIALIZE](model: MyLanguageModel) {
 *   return serializeModelOptions({
 *     modelId: model.modelId,
 *     config: model.config,
 *   });
 * }
 * ```
 */
declare function serializeModelOptions_2<CONFIG extends {
    headers?: Resolvable_2<Record<string, string | undefined>>;
}>(options: {
    modelId: string;
    config: CONFIG;
}): {
    modelId: string;
    config: JSONObject;
};

/**
 * Server-side: serialize a transcription stream part as one TEXT frame.
 * `Error` payloads in `error` parts serialize as `{ name, message }` —
 * `Error` properties are non-enumerable, so a plain `JSON.stringify` would
 * serialize them to `{}` and lose the message end-to-end. Returns
 * `undefined` for payloads that are not JSON-serializable (envelope rule 4,
 * e.g. bigint or cyclic values); callers drop the frame.
 */
declare function serializeTranscriptionStreamPart(part: TranscriptionModelV4StreamPart): string | undefined;

/**
 * Server-side: serialize a transcription stream part as one TEXT frame.
 * `Error` payloads in `error` parts serialize as `{ name, message }` —
 * `Error` properties are non-enumerable, so a plain `JSON.stringify` would
 * serialize them to `{}` and lose the message end-to-end. Returns
 * `undefined` for payloads that are not JSON-serializable (envelope rule 4,
 * e.g. bigint or cyclic values); callers drop the frame.
 */
declare function serializeTranscriptionStreamPart_2(part: TranscriptionModelV4StreamPart): string | undefined;

export declare class SerialJobExecutor {
    private queue;
    private isProcessing;
    private processQueue;
    run(job: Job): Promise<void>;
}

declare type SharedV2Headers = Record<string, string>;

declare type SharedV2Headers_2 = Record<string, string>;

/**
 * Additional provider-specific metadata.
 * Metadata are additional outputs from the provider.
 * They are passed through to the provider from the AI SDK
 * and enable provider-specific functionality
 * that can be fully encapsulated in the provider.
 *
 * This enables us to quickly ship provider-specific functionality
 * without affecting the core AI SDK.
 *
 * The outer record is keyed by the provider name, and the inner
 * record is keyed by the provider-specific metadata key.
 *
 * ```ts
 * {
 *   "anthropic": {
 *     "cacheControl": { "type": "ephemeral" }
 *   }
 * }
 * ```
 */
declare type SharedV2ProviderMetadata = Record<string, Record<string, JSONValue_3>>;

/**
 * Additional provider-specific options.
 * Options are additional input to the provider.
 * They are passed through to the provider from the AI SDK
 * and enable provider-specific functionality
 * that can be fully encapsulated in the provider.
 *
 * This enables us to quickly ship provider-specific functionality
 * without affecting the core AI SDK.
 *
 * The outer record is keyed by the provider name, and the inner
 * record is keyed by the provider-specific metadata key.
 *
 * ```ts
 * {
 *   "anthropic": {
 *     "cacheControl": { "type": "ephemeral" }
 *   }
 * }
 * ```
 */
declare type SharedV2ProviderOptions = Record<string, Record<string, JSONValue_3>>;

declare type SharedV3Headers = Record<string, string>;

/**
 * Additional provider-specific metadata.
 * Metadata are additional outputs from the provider.
 * They are passed through to the provider from the AI SDK
 * and enable provider-specific functionality
 * that can be fully encapsulated in the provider.
 *
 * This enables us to quickly ship provider-specific functionality
 * without affecting the core AI SDK.
 *
 * The outer record is keyed by the provider name, and the inner
 * record is keyed by the provider-specific metadata key.
 *
 * ```ts
 * {
 *   "anthropic": {
 *     "cacheControl": { "type": "ephemeral" }
 *   }
 * }
 * ```
 */
declare type SharedV3ProviderMetadata = Record<string, JSONObject_2>;

/**
 * Additional provider-specific options.
 * Options are additional input to the provider.
 * They are passed through to the provider from the AI SDK
 * and enable provider-specific functionality
 * that can be fully encapsulated in the provider.
 *
 * This enables us to quickly ship provider-specific functionality
 * without affecting the core AI SDK.
 *
 * The outer record is keyed by the provider name, and the inner
 * record is keyed by the provider-specific metadata key.
 *
 * ```ts
 * {
 *   "anthropic": {
 *     "cacheControl": { "type": "ephemeral" }
 *   }
 * }
 * ```
 */
declare type SharedV3ProviderOptions = Record<string, JSONObject_2>;

/**
 * Warning from the model.
 *
 * For example, that certain features are unsupported or compatibility
 * functionality is used (which might lead to suboptimal results).
 */
declare type SharedV3Warning = {
    /**
     * A feature is not supported by the model.
     */
    type: 'unsupported';
    /**
     * The feature that is not supported.
     */
    feature: string;
    /**
     * Additional details about the warning.
     */
    details?: string;
} | {
    /**
     * A compatibility feature is used that might lead to suboptimal results.
     */
    type: 'compatibility';
    /**
     * The feature that is used in a compatibility mode.
     */
    feature: string;
    /**
     * Additional details about the warning.
     */
    details?: string;
} | {
    /**
     * Other warning.
     */
    type: 'other';
    /**
     * The message of the warning.
     */
    message: string;
};

/**
 * Audio format configuration shared by provider V4 model specifications.
 */
declare type SharedV4AudioFormat = {
    /**
     * Audio format type, e.g. `audio/pcm`, `audio/pcmu`, or `audio/pcma`.
     */
    type: string;
    /**
     * Sample rate in Hz. Only applicable for formats that require a rate.
     */
    rate?: number;
};

/**
 * Audio format configuration shared by provider V4 model specifications.
 */
declare type SharedV4AudioFormat_2 = {
    /**
     * Audio format type, e.g. `audio/pcm`, `audio/pcmu`, or `audio/pcma`.
     */
    type: string;
    /**
     * Sample rate in Hz. Only applicable for formats that require a rate.
     */
    rate?: number;
};

/**
 * File data as a tagged discriminated union:
 *
 * - `{ type: 'data', data }`: raw bytes (`Uint8Array`) or base64-encoded string.
 * - `{ type: 'url', url }`: a URL that points to the file.
 * - `{ type: 'reference', reference }`: a provider reference (`{ [provider]: id }`).
 * - `{ type: 'text', text }`: inline text content (e.g. an inline text document).
 */
declare type SharedV4FileData = SharedV4FileDataData | SharedV4FileDataUrl | SharedV4FileDataReference | SharedV4FileDataText;

/**
 * File data as a tagged discriminated union:
 *
 * - `{ type: 'data', data }`: raw bytes (`Uint8Array`) or base64-encoded string.
 * - `{ type: 'url', url }`: a URL that points to the file.
 * - `{ type: 'reference', reference }`: a provider reference (`{ [provider]: id }`).
 * - `{ type: 'text', text }`: inline text content (e.g. an inline text document).
 */
declare type SharedV4FileData_2 = SharedV4FileDataData_2 | SharedV4FileDataUrl_2 | SharedV4FileDataReference_2 | SharedV4FileDataText_2;

/**
 * File data variant containing raw bytes (`Uint8Array`) or a base64-encoded
 * string.
 */
declare interface SharedV4FileDataData {
    type: 'data';
    data: Uint8Array | string;
}

/**
 * File data variant containing raw bytes (`Uint8Array`) or a base64-encoded
 * string.
 */
declare interface SharedV4FileDataData_2 {
    type: 'data';
    data: Uint8Array | string;
}

/**
 * File data variant containing a provider reference (`{ [provider]: id }`).
 */
declare interface SharedV4FileDataReference {
    type: 'reference';
    reference: SharedV4ProviderReference_2;
}

/**
 * File data variant containing a provider reference (`{ [provider]: id }`).
 */
declare interface SharedV4FileDataReference_2 {
    type: 'reference';
    reference: SharedV4ProviderReference;
}

/**
 * File data variant containing inline text content (e.g. an inline text
 * document).
 */
declare interface SharedV4FileDataText {
    type: 'text';
    text: string;
}

/**
 * File data variant containing inline text content (e.g. an inline text
 * document).
 */
declare interface SharedV4FileDataText_2 {
    type: 'text';
    text: string;
}

/**
 * File data variant containing a URL that points to the file.
 */
declare interface SharedV4FileDataUrl {
    type: 'url';
    url: URL;
}

/**
 * File data variant containing a URL that points to the file.
 */
declare interface SharedV4FileDataUrl_2 {
    type: 'url';
    url: URL;
}

declare type SharedV4Headers = Record<string, string>;

declare type SharedV4Headers_2 = Record<string, string>;

/**
 * Additional provider-specific metadata.
 * Metadata are additional outputs from the provider.
 * They are passed through to the provider from the AI SDK
 * and enable provider-specific functionality
 * that can be fully encapsulated in the provider.
 *
 * This enables us to quickly ship provider-specific functionality
 * without affecting the core AI SDK.
 *
 * The outer record is keyed by the provider name, and the inner
 * record is keyed by the provider-specific metadata key.
 *
 * ```ts
 * {
 *   "anthropic": {
 *     "cacheControl": { "type": "ephemeral" }
 *   }
 * }
 * ```
 */
declare type SharedV4ProviderMetadata = Record<string, JSONObject>;

/**
 * Additional provider-specific metadata.
 * Metadata are additional outputs from the provider.
 * They are passed through to the provider from the AI SDK
 * and enable provider-specific functionality
 * that can be fully encapsulated in the provider.
 *
 * This enables us to quickly ship provider-specific functionality
 * without affecting the core AI SDK.
 *
 * The outer record is keyed by the provider name, and the inner
 * record is keyed by the provider-specific metadata key.
 *
 * ```ts
 * {
 *   "anthropic": {
 *     "cacheControl": { "type": "ephemeral" }
 *   }
 * }
 * ```
 */
declare type SharedV4ProviderMetadata_2 = Record<string, JSONObject_2>;

/**
 * Additional provider-specific options.
 * Options are additional input to the provider.
 * They are passed through to the provider from the AI SDK
 * and enable provider-specific functionality
 * that can be fully encapsulated in the provider.
 *
 * This enables us to quickly ship provider-specific functionality
 * without affecting the core AI SDK.
 *
 * The outer record is keyed by the provider name, and the inner
 * record is keyed by the provider-specific metadata key.
 *
 * ```ts
 * {
 *   "anthropic": {
 *     "cacheControl": { "type": "ephemeral" }
 *   }
 * }
 * ```
 */
declare type SharedV4ProviderOptions = Record<string, JSONObject>;

/**
 * Additional provider-specific options.
 * Options are additional input to the provider.
 * They are passed through to the provider from the AI SDK
 * and enable provider-specific functionality
 * that can be fully encapsulated in the provider.
 *
 * This enables us to quickly ship provider-specific functionality
 * without affecting the core AI SDK.
 *
 * The outer record is keyed by the provider name, and the inner
 * record is keyed by the provider-specific metadata key.
 *
 * ```ts
 * {
 *   "anthropic": {
 *     "cacheControl": { "type": "ephemeral" }
 *   }
 * }
 * ```
 */
declare type SharedV4ProviderOptions_2 = Record<string, JSONObject_2>;

/**
 * A mapping of provider names to provider-specific file identifiers.
 *
 * Provider references allow files to be identified across different
 * providers without re-uploading, by storing each provider's own
 * identifier for the same logical file.
 *
 * ```ts
 * {
 *   "openai": "file-abc123",
 *   "anthropic": "file-xyz789"
 * }
 * ```
 *
 * The `type?: never` constraint excludes any object that has a `type`
 * property, so a `SharedV4ProviderReference` cannot be confused with a
 * tagged file-data shape (e.g. `{ type: 'data', data }` or
 * `{ type: 'reference', reference }`) when both appear in the same union.
 */
declare type SharedV4ProviderReference = Record<string, string> & {
    type?: never;
};

/**
 * A mapping of provider names to provider-specific file identifiers.
 *
 * Provider references allow files to be identified across different
 * providers without re-uploading, by storing each provider's own
 * identifier for the same logical file.
 *
 * ```ts
 * {
 *   "openai": "file-abc123",
 *   "anthropic": "file-xyz789"
 * }
 * ```
 *
 * The `type?: never` constraint excludes any object that has a `type`
 * property, so a `SharedV4ProviderReference` cannot be confused with a
 * tagged file-data shape (e.g. `{ type: 'data', data }` or
 * `{ type: 'reference', reference }`) when both appear in the same union.
 */
declare type SharedV4ProviderReference_2 = Record<string, string> & {
    type?: never;
};

/**
 * Warning from the model.
 *
 * For example, that certain features are unsupported or compatibility
 * functionality is used (which might lead to suboptimal results).
 */
declare type SharedV4Warning = {
    /**
     * A feature is not supported by the model.
     */
    type: 'unsupported';
    /**
     * The feature that is not supported.
     */
    feature: string;
    /**
     * Additional details about the warning.
     */
    details?: string;
} | {
    /**
     * A compatibility feature is used that might lead to suboptimal results.
     */
    type: 'compatibility';
    /**
     * The feature that is used in a compatibility mode.
     */
    feature: string;
    /**
     * Additional details about the warning.
     */
    details?: string;
} | {
    /**
     * A deprecated feature or option is being used.
     */
    type: 'deprecated';
    /**
     * The deprecated setting or feature name.
     */
    setting: string;
    /**
     * A human-readable message explaining what to use instead.
     */
    message: string;
} | {
    /**
     * Other warning.
     */
    type: 'other';
    /**
     * The message of the warning.
     */
    message: string;
};

/**
 * Warning from the model.
 *
 * For example, that certain features are unsupported or compatibility
 * functionality is used (which might lead to suboptimal results).
 */
declare type SharedV4Warning_2 = {
    /**
     * A feature is not supported by the model.
     */
    type: 'unsupported';
    /**
     * The feature that is not supported.
     */
    feature: string;
    /**
     * Additional details about the warning.
     */
    details?: string;
} | {
    /**
     * A compatibility feature is used that might lead to suboptimal results.
     */
    type: 'compatibility';
    /**
     * The feature that is used in a compatibility mode.
     */
    feature: string;
    /**
     * Additional details about the warning.
     */
    details?: string;
} | {
    /**
     * A deprecated feature or option is being used.
     */
    type: 'deprecated';
    /**
     * The deprecated setting or feature name.
     */
    setting: string;
    /**
     * A human-readable message explaining what to use instead.
     */
    message: string;
} | {
    /**
     * Other warning.
     */
    type: 'other';
    /**
     * The message of the warning.
     */
    message: string;
};

declare type ShouldRetryFunction = (error: unknown) => boolean | Promise<boolean>;

declare type ShouldRetryFunction_2 = (error: unknown) => boolean | Promise<boolean>;

/**
 * Creates a ReadableStream that emits the provided values with an optional delay between each value.
 *
 * @param options - The configuration options
 * @param options.chunks - Array of values to be emitted by the stream
 * @param options.initialDelayInMs - Optional initial delay in milliseconds before emitting the first value (default: 0). Can be set to `null` to skip the initial delay. The difference between `initialDelayInMs: null` and `initialDelayInMs: 0` is that `initialDelayInMs: null` will emit the values without any delay, while `initialDelayInMs: 0` will emit the values with a delay of 0 milliseconds.
 * @param options.chunkDelayInMs - Optional delay in milliseconds between emitting each value (default: 0). Can be set to `null` to skip the delay. The difference between `chunkDelayInMs: null` and `chunkDelayInMs: 0` is that `chunkDelayInMs: null` will emit the values without any delay, while `chunkDelayInMs: 0` will emit the values with a delay of 0 milliseconds.
 * @returns A ReadableStream that emits the provided values
 */
export declare function simulateReadableStream<T>({ chunks, initialDelayInMs, chunkDelayInMs, _internal, }: {
    chunks: T[];
    initialDelayInMs?: number | null;
    chunkDelayInMs?: number | null;
    _internal?: {
        delay?: (ms: number | null) => Promise<void>;
    };
}): ReadableStream<T>;

/**
 * Simulates streaming chunks with the response from a generate call.
 */
export declare function simulateStreamingMiddleware(): LanguageModelMiddleware;

/**
 * Function that is called to determine if the tool needs approval before it can be executed.
 *
 * Return `undefined` for the same effect as the `not-applicable` status.
 */
export declare type SingleToolApprovalFunction<INPUT, TOOL_CONTEXT extends Context_2 | unknown | never, RUNTIME_CONTEXT extends Context_2 | unknown | never> = (input: INPUT, options: Omit<ToolExecutionOptions<TOOL_CONTEXT>, 'abortSignal' | 'context'> & {
    toolContext: TOOL_CONTEXT;
    runtimeContext: RUNTIME_CONTEXT;
}) => MaybePromiseLike_2<ToolApprovalStatus>;

/**
 * Skills specification version 4.
 */
declare interface SkillsV4 {
    /**
     * The skills implementation must specify which skills interface
     * version it implements. This will allow us to evolve the skills
     * interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Uploads a new skill from the given files.
     */
    uploadSkill(params: SkillsV4UploadSkillCallOptions): PromiseLike<SkillsV4UploadSkillResult>;
}

/**
 * Skills specification version 4.
 */
declare interface SkillsV4_2 {
    /**
     * The skills implementation must specify which skills interface
     * version it implements. This will allow us to evolve the skills
     * interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Uploads a new skill from the given files.
     */
    uploadSkill(params: SkillsV4UploadSkillCallOptions_2): PromiseLike<SkillsV4UploadSkillResult_2>;
}

declare interface SkillsV4File {
    /**
     * The path of the file relative to the skill root.
     */
    path: string;
    /**
     * The file data.
     *
     * - `{ type: 'data', data }`: raw bytes (`Uint8Array`) or a base64-encoded string.
     * - `{ type: 'text', text }`: inline text (UTF-8).
     */
    data: SharedV4FileDataData | SharedV4FileDataText;
}

declare interface SkillsV4File_2 {
    /**
     * The path of the file relative to the skill root.
     */
    path: string;
    /**
     * The file data.
     *
     * - `{ type: 'data', data }`: raw bytes (`Uint8Array`) or a base64-encoded string.
     * - `{ type: 'text', text }`: inline text (UTF-8).
     */
    data: SharedV4FileDataData_2 | SharedV4FileDataText_2;
}

declare interface SkillsV4UploadSkillCallOptions {
    /**
     * The files that make up the skill.
     */
    files: SkillsV4File[];
    /**
     * Optional human-readable title for the skill.
     */
    displayTitle?: string;
    /**
     * Additional provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions;
}

declare interface SkillsV4UploadSkillCallOptions_2 {
    /**
     * The files that make up the skill.
     */
    files: SkillsV4File_2[];
    /**
     * Optional human-readable title for the skill.
     */
    displayTitle?: string;
    /**
     * Additional provider-specific options.
     */
    providerOptions?: SharedV4ProviderOptions_2;
}

declare interface SkillsV4UploadSkillResult {
    /**
     * A provider reference mapping provider names to provider-specific skill identifiers.
     */
    providerReference: SharedV4ProviderReference_2;
    /**
     * Optional human-readable title for the uploaded skill.
     */
    displayTitle?: string;
    /**
     * Optional name of the uploaded skill.
     */
    name?: string;
    /**
     * Optional description of what the uploaded skill does.
     */
    description?: string;
    /**
     * Optional latest version identifier of the uploaded skill.
     */
    latestVersion?: string;
    /**
     * Additional provider-specific metadata.
     */
    providerMetadata?: SharedV4ProviderMetadata;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: SharedV4Warning[];
}

declare interface SkillsV4UploadSkillResult_2 {
    /**
     * A provider reference mapping provider names to provider-specific skill identifiers.
     */
    providerReference: SharedV4ProviderReference;
    /**
     * Optional human-readable title for the uploaded skill.
     */
    displayTitle?: string;
    /**
     * Optional name of the uploaded skill.
     */
    name?: string;
    /**
     * Optional description of what the uploaded skill does.
     */
    description?: string;
    /**
     * Optional latest version identifier of the uploaded skill.
     */
    latestVersion?: string;
    /**
     * Additional provider-specific metadata.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: SharedV4Warning_2[];
}

/**
 * Smooths text and reasoning streaming output.
 *
 * @param delayInMs - The delay in milliseconds between each chunk. Defaults to 10ms. Can be set to `null` to skip the delay.
 * @param chunking - Controls how the text is chunked for streaming. Use "word" to stream word by word (default), "line" to stream line by line, provide a custom RegExp pattern for custom chunking, provide an Intl.Segmenter for locale-aware word segmentation (recommended for CJK languages), or provide a custom ChunkDetector function.
 *
 * @returns A transform stream that smooths text streaming output.
 */
export declare function smoothStream<TOOLS extends ToolSet>({ delayInMs, chunking, _internal: { delay }, }?: {
    delayInMs?: number | null;
    chunking?: 'word' | 'line' | RegExp | ChunkDetector | Intl.Segmenter;
    /**
     * Internal. For test use only. May change without notice.
     */
    _internal?: {
        delay?: (delayInMs: number | null) => Promise<void>;
    };
}): (options: {
    tools: TOOLS;
}) => TransformStream<TextStreamPart<TOOLS>, TextStreamPart<TOOLS>>;

/**
 * A source that has been used as input to generate the response.
 */
declare type Source = LanguageModelV4Source_2;

/**
 * A document source part of a message.
 */
export declare type SourceDocumentUIPart = {
    type: 'source-document';
    sourceId: string;
    mediaType: string;
    title: string;
    filename?: string;
    providerMetadata?: ProviderMetadata;
};

/**
 * A source part of a message.
 */
export declare type SourceUrlUIPart = {
    type: 'source-url';
    sourceId: string;
    url: string;
    title?: string;
    providerMetadata?: ProviderMetadata;
};

/**
 * Speech model that is used by the AI SDK.
 */
export declare type SpeechModel = string | SpeechModelV4_2 | SpeechModelV3 | SpeechModelV2;

export declare type SpeechModelResponseMetadata = {
    /**
     * Timestamp for the start of the generated response.
     */
    timestamp: Date;
    /**
     * The ID of the response model that was used to generate the response.
     */
    modelId: string;
    /**
     * Response headers.
     */
    headers?: Record<string, string>;
    /**
     * Response body.
     */
    body?: unknown;
};

/**
 * Speech model specification version 2.
 */
declare type SpeechModelV2 = {
    /**
     * The speech model must specify which speech model interface
     * version it implements. This will allow us to evolve the speech
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v2';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Generates speech audio from text.
     */
    doGenerate(options: SpeechModelV2CallOptions): PromiseLike<{
        /**
         * Generated audio as an ArrayBuffer.
         * The audio should be returned without any unnecessary conversion.
         * If the API returns base64 encoded strings, the audio should be returned
         * as base64 encoded strings. If the API returns binary data, the audio
         * should be returned as binary data.
         */
        audio: string | Uint8Array;
        /**
         * Warnings for the call, e.g. unsupported settings.
         */
        warnings: Array<SpeechModelV2CallWarning>;
        /**
         * Optional request information for telemetry and debugging purposes.
         */
        request?: {
            /**
             * Response body (available only for providers that use HTTP requests).
             */
            body?: unknown;
        };
        /**
         * Response information for telemetry and debugging purposes.
         */
        response: {
            /**
             * Timestamp for the start of the generated response.
             */
            timestamp: Date;
            /**
             * The ID of the response model that was used to generate the response.
             */
            modelId: string;
            /**
             * Response headers.
             */
            headers?: SharedV2Headers_2;
            /**
             * Response body.
             */
            body?: unknown;
        };
        /**
         * Additional provider-specific metadata. They are passed through
         * from the provider to the AI SDK and enable provider-specific
         * results that can be fully encapsulated in the provider.
         */
        providerMetadata?: Record<string, Record<string, JSONValue_3>>;
    }>;
};

declare type SpeechModelV2CallOptions = {
    /**
     * Text to convert to speech.
     */
    text: string;
    /**
     * The voice to use for speech synthesis.
     * This is provider-specific and may be a voice ID, name, or other identifier.
     */
    voice?: string;
    /**
     * The desired output format for the audio e.g. "mp3", "wav", etc.
     */
    outputFormat?: string;
    /**
     * Instructions for the speech generation e.g. "Speak in a slow and steady tone".
     */
    instructions?: string;
    /**
     * The speed of the speech generation.
     */
    speed?: number;
    /**
     * The language for speech generation. This should be an ISO 639-1 language code (e.g. "en", "es", "fr")
     * or "auto" for automatic language detection. Provider support varies.
     */
    language?: string;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     *   "openai": {}
     * }
     * ```
     */
    providerOptions?: SpeechModelV2ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

/**
 * Warning from the model provider for this call. The call will proceed, but e.g.
 * some settings might not be supported, which can lead to suboptimal results.
 */
declare type SpeechModelV2CallWarning = {
    type: 'unsupported-setting';
    setting: keyof SpeechModelV2CallOptions;
    details?: string;
} | {
    type: 'other';
    message: string;
};

declare type SpeechModelV2ProviderOptions = Record<string, Record<string, JSONValue_3>>;

/**
 * Speech model specification version 3.
 */
declare type SpeechModelV3 = {
    /**
     * The speech model must specify which speech model interface
     * version it implements. This will allow us to evolve the speech
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v3';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Generates speech audio from text.
     */
    doGenerate(options: SpeechModelV3CallOptions): PromiseLike<{
        /**
         * Generated audio as an ArrayBuffer.
         * The audio should be returned without any unnecessary conversion.
         * If the API returns base64 encoded strings, the audio should be returned
         * as base64 encoded strings. If the API returns binary data, the audio
         * should be returned as binary data.
         */
        audio: string | Uint8Array;
        /**
         * Warnings for the call, e.g. unsupported settings.
         */
        warnings: Array<SharedV3Warning>;
        /**
         * Optional request information for telemetry and debugging purposes.
         */
        request?: {
            /**
             * Response body (available only for providers that use HTTP requests).
             */
            body?: unknown;
        };
        /**
         * Response information for telemetry and debugging purposes.
         */
        response: {
            /**
             * Timestamp for the start of the generated response.
             */
            timestamp: Date;
            /**
             * The ID of the response model that was used to generate the response.
             */
            modelId: string;
            /**
             * Response headers.
             */
            headers?: SharedV2Headers_2;
            /**
             * Response body.
             */
            body?: unknown;
        };
        /**
         * Additional provider-specific metadata. They are passed through
         * from the provider to the AI SDK and enable provider-specific
         * results that can be fully encapsulated in the provider.
         */
        providerMetadata?: Record<string, JSONObject_2>;
    }>;
};

declare type SpeechModelV3CallOptions = {
    /**
     * Text to convert to speech.
     */
    text: string;
    /**
     * The voice to use for speech synthesis.
     * This is provider-specific and may be a voice ID, name, or other identifier.
     */
    voice?: string;
    /**
     * The desired output format for the audio e.g. "mp3", "wav", etc.
     */
    outputFormat?: string;
    /**
     * Instructions for the speech generation e.g. "Speak in a slow and steady tone".
     */
    instructions?: string;
    /**
     * The speed of the speech generation.
     */
    speed?: number;
    /**
     * The language for speech generation. This should be an ISO 639-1 language code (e.g. "en", "es", "fr")
     * or "auto" for automatic language detection. Provider support varies.
     */
    language?: string;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     *   "openai": {}
     * }
     * ```
     */
    providerOptions?: SpeechModelV3ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

declare type SpeechModelV3ProviderOptions = Record<string, JSONObject_2>;

/**
 * Speech model specification version 4.
 */
declare type SpeechModelV4 = {
    /**
     * The speech model must specify which speech model interface
     * version it implements. This will allow us to evolve the speech
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Generates speech audio from text.
     */
    doGenerate(options: SpeechModelV4CallOptions): PromiseLike<SpeechModelV4Result>;
};

/**
 * Speech model specification version 4.
 */
declare type SpeechModelV4_2 = {
    /**
     * The speech model must specify which speech model interface
     * version it implements. This will allow us to evolve the speech
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Generates speech audio from text.
     */
    doGenerate(options: SpeechModelV4CallOptions_2): PromiseLike<SpeechModelV4Result_2>;
};

declare type SpeechModelV4CallOptions = {
    /**
     * Text to convert to speech.
     */
    text: string;
    /**
     * The voice to use for speech synthesis.
     * This is provider-specific and may be a voice ID, name, or other identifier.
     */
    voice?: string;
    /**
     * The desired output format for the audio e.g. "mp3", "wav", etc.
     */
    outputFormat?: string;
    /**
     * Instructions for the speech generation e.g. "Speak in a slow and steady tone".
     */
    instructions?: string;
    /**
     * The speed of the speech generation.
     */
    speed?: number;
    /**
     * The language for speech generation. This should be an ISO 639-1 language code (e.g. "en", "es", "fr")
     * or "auto" for automatic language detection. Provider support varies.
     */
    language?: string;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     *   "openai": {}
     * }
     * ```
     */
    providerOptions?: SpeechModelV4ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

declare type SpeechModelV4CallOptions_2 = {
    /**
     * Text to convert to speech.
     */
    text: string;
    /**
     * The voice to use for speech synthesis.
     * This is provider-specific and may be a voice ID, name, or other identifier.
     */
    voice?: string;
    /**
     * The desired output format for the audio e.g. "mp3", "wav", etc.
     */
    outputFormat?: string;
    /**
     * Instructions for the speech generation e.g. "Speak in a slow and steady tone".
     */
    instructions?: string;
    /**
     * The speed of the speech generation.
     */
    speed?: number;
    /**
     * The language for speech generation. This should be an ISO 639-1 language code (e.g. "en", "es", "fr")
     * or "auto" for automatic language detection. Provider support varies.
     */
    language?: string;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     *   "openai": {}
     * }
     * ```
     */
    providerOptions?: SpeechModelV4ProviderOptions_2;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

declare type SpeechModelV4ProviderOptions = Record<string, JSONObject>;

declare type SpeechModelV4ProviderOptions_2 = Record<string, JSONObject_2>;

/**
 * The result of a speech model doGenerate call.
 */
declare type SpeechModelV4Result = {
    /**
     * Generated audio as an ArrayBuffer.
     * The audio should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the audio should be returned
     * as base64 encoded strings. If the API returns binary data, the audio
     * should be returned as binary data.
     */
    audio: string | Uint8Array;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: Array<SharedV4Warning>;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Response body (available only for providers that use HTTP requests).
         */
        body?: unknown;
    };
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        /**
         * Timestamp for the start of the generated response.
         */
        timestamp: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId: string;
        /**
         * Response headers.
         */
        headers?: SharedV2Headers;
        /**
         * Response body.
         */
        body?: unknown;
    };
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    providerMetadata?: Record<string, JSONObject>;
};

/**
 * The result of a speech model doGenerate call.
 */
declare type SpeechModelV4Result_2 = {
    /**
     * Generated audio as an ArrayBuffer.
     * The audio should be returned without any unnecessary conversion.
     * If the API returns base64 encoded strings, the audio should be returned
     * as base64 encoded strings. If the API returns binary data, the audio
     * should be returned as binary data.
     */
    audio: string | Uint8Array;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: Array<SharedV4Warning_2>;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Response body (available only for providers that use HTTP requests).
         */
        body?: unknown;
    };
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        /**
         * Timestamp for the start of the generated response.
         */
        timestamp: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId: string;
        /**
         * Response headers.
         */
        headers?: SharedV2Headers_2;
        /**
         * Response body.
         */
        body?: unknown;
    };
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    providerMetadata?: Record<string, JSONObject_2>;
};

/**
 * The result of a `generateSpeech` call.
 * It contains the audio data and additional information.
 */
export declare interface SpeechResult {
    /**
     * The generated audio file with the audio data.
     */
    readonly audio: GeneratedAudioFile;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    readonly warnings: Array<Warning>;
    /**
     * Response metadata from the provider. There may be multiple responses if we made multiple calls to the model.
     */
    readonly responses: Array<SpeechModelResponseMetadata>;
    /**
     * Provider metadata from the provider.
     */
    readonly providerMetadata: Record<string, JSONObject_2>;
}

/**
 * Speech translation model that is used by the AI SDK.
 *
 * Experimental: part of the experimental speech translation modality and may
 * change in patch releases.
 */
declare type SpeechTranslationModel = string | SpeechTranslationModelV4;

/**
 * Response metadata for speech translation model calls.
 *
 * Experimental: part of the experimental speech translation modality and may
 * change in patch releases.
 */
declare type SpeechTranslationModelResponseMetadata = {
    /**
     * Timestamp for the start of the generated response.
     */
    timestamp: Date;
    /**
     * The ID of the response model that was used to generate the response.
     */
    modelId: string;
    /**
     * Response headers.
     */
    headers?: Record<string, string>;
};

/**
 * Speech translation model specification version 4.
 *
 * Speech translation is a streaming-only modality: models translate live
 * source audio into target-language audio and text.
 *
 * Experimental: the speech translation model contract may change in patch
 * releases while the functions built on it are experimental. All types of
 * this modality are exported with `Experimental_` prefixes for this reason.
 */
declare type SpeechTranslationModelV4 = {
    /**
     * The speech translation model must specify which speech translation model
     * interface version it implements. This will allow us to evolve the
     * speech translation model interface and retain backwards compatibility.
     * The different implementation versions can be handled as a discriminated
     * union on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Streams a speech translation for live audio.
     */
    doStream(options: SpeechTranslationModelV4StreamOptions): PromiseLike<SpeechTranslationModelV4StreamResult>;
};

declare type SpeechTranslationModelV4ProviderOptions = Record<string, JSONObject_2>;

/**
 * Options for a speech translation model stream call.
 */
declare type SpeechTranslationModelV4StreamOptions = {
    /**
     * Source audio chunks to transform.
     *
     * `Uint8Array` chunks contain raw audio bytes. `string` chunks contain
     * base64-encoded raw audio bytes.
     */
    audio: ReadableStream<Uint8Array | string>;
    /**
     * The input audio format for the raw audio chunks.
     */
    inputAudioFormat: SharedV4AudioFormat_2;
    /**
     * The language to produce output audio and text in, as a BCP-47-style
     * language tag (e.g. `en`, `es`, `fr-CA`). Supported values are
     * provider-specific and validated by the provider.
     */
    targetLanguage: string;
    /**
     * The language of the source audio, as a BCP-47-style language tag.
     * When absent, providers should auto-detect the source language.
     */
    sourceLanguage?: string;
    /**
     * The desired audio format for output audio chunks.
     * When absent, the provider default output format is used.
     */
    outputAudioFormat?: SharedV4AudioFormat_2;
    /**
     * Additional provider-specific options that are passed through to the provider.
     *
     * The outer record is keyed by the provider name, and the inner record is keyed
     * by provider-specific option names.
     */
    providerOptions?: SpeechTranslationModelV4ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP/WebSocket-based providers that support headers.
     */
    headers?: Record<string, string | undefined>;
    /**
     * When true, providers should include raw provider chunks in the stream.
     */
    includeRawChunks?: boolean;
};

declare type SpeechTranslationModelV4StreamPart = {
    /**
     * Stream start event with warnings for the call, e.g. unsupported settings.
     */
    type: 'stream-start';
    warnings: Array<SharedV4Warning_2>;
} | {
    /**
     * Output audio chunk.
     *
     * `Uint8Array` chunks contain raw audio bytes. `string` chunks contain
     * base64-encoded raw audio bytes.
     */
    type: 'audio';
    id?: string;
    audio: Uint8Array | string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Append-only output text delta.
     *
     * Output text is append-only: providers stream `output-text-delta`
     * parts and finalize per-utterance with `output-text-final`. There is
     * no partial/revision part for output text by design for now.
     */
    type: 'output-text-delta';
    id?: string;
    delta: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Final output text for a provider-defined segment or utterance.
     *
     * Output text is append-only: providers stream `output-text-delta`
     * parts and finalize per-utterance with `output-text-final`. There is
     * no partial/revision part for output text by design for now.
     */
    type: 'output-text-final';
    id?: string;
    text: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Append-only source transcript delta.
     */
    type: 'source-transcript-delta';
    id?: string;
    delta: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Non-final source transcript text. The text may be revised by later parts.
     */
    type: 'source-transcript-partial';
    id?: string;
    text: string;
    startSecond?: number;
    endSecond?: number;
    channelIndex?: number;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Final source transcript text for a provider-defined segment or utterance.
     */
    type: 'source-transcript-final';
    id?: string;
    text: string;
    startSecond?: number;
    endSecond?: number;
    channelIndex?: number;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Metadata for the response, emitted once available.
     */
    type: 'response-metadata';
    timestamp?: Date;
    modelId?: string;
    headers?: SharedV4Headers_2;
    body?: unknown;
} | {
    /**
     * Metadata that is available after the stream is finished.
     */
    type: 'finish';
    /**
     * The final source-language transcript of the input audio.
     */
    sourceText: string;
    /**
     * The final output text. May be an empty string for providers that
     * produce only audio output.
     */
    outputText: string;
    /**
     * The duration of the source audio in seconds, if available.
     */
    durationInSeconds?: number;
    /**
     * Usage information for the call, if reported by the provider.
     */
    usage?: SpeechTranslationModelV4Usage;
    /**
     * Additional provider-specific metadata.
     */
    providerMetadata?: Record<string, JSONObject_2>;
} | {
    /**
     * Raw provider chunks if enabled.
     */
    type: 'raw';
    rawValue: unknown;
} | {
    /**
     * Error parts are streamed, allowing for multiple errors.
     */
    type: 'error';
    error: unknown;
};

/**
 * The result of a speech translation model doStream call.
 */
declare type SpeechTranslationModelV4StreamResult = {
    /**
     * The stream.
     */
    stream: ReadableStream<SpeechTranslationModelV4StreamPart>;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Request body or setup payload that was sent to the provider API.
         */
        body?: unknown;
    };
    /**
     * Optional response data.
     */
    response?: {
        /**
         * Timestamp for the start of the streamed response.
         */
        timestamp?: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId?: string;
        /**
         * Response headers.
         */
        headers?: SharedV4Headers_2;
        /**
         * Response body.
         */
        body?: unknown;
    };
};

/**
 * Usage information for a speech translation call.
 *
 * All fields are optional because providers report usage with different
 * granularity (seconds of audio, audio tokens, and/or text tokens).
 */
declare type SpeechTranslationModelV4Usage = {
    /**
     * Seconds of input audio that were processed, if reported.
     */
    inputAudioSeconds?: number;
    /**
     * Number of input audio tokens, if reported.
     */
    inputAudioTokens?: number;
    /**
     * Number of output audio tokens, if reported.
     */
    outputAudioTokens?: number;
    /**
     * Number of input text tokens, if reported.
     */
    inputTextTokens?: number;
    /**
     * Number of output text tokens, if reported.
     */
    outputTextTokens?: number;
};

declare type StandardizedPrompt = {
    /**
     * Instructions.
     */
    instructions: Instructions | undefined;
    /**
     * Messages.
     */
    messages: ModelMessage[];
};

/** The Standard JSON Schema interface. */
declare interface StandardJSONSchemaV1<Input = unknown, Output = Input> {
    /** The Standard JSON Schema properties. */
    readonly "~standard": StandardJSONSchemaV1.Props<Input, Output>;
}

declare namespace StandardJSONSchemaV1 {
    /** The Standard JSON Schema properties interface. */
    interface Props<Input = unknown, Output = Input> extends StandardTypedV1.Props<Input, Output> {
        /** Methods for generating the input/output JSON Schema. */
        readonly jsonSchema: StandardJSONSchemaV1.Converter;
    }
    /** The Standard JSON Schema converter interface. */
    interface Converter {
        /** Converts the input type to JSON Schema. May throw if conversion is not supported. */
        readonly input: (options: StandardJSONSchemaV1.Options) => Record<string, unknown>;
        /** Converts the output type to JSON Schema. May throw if conversion is not supported. */
        readonly output: (options: StandardJSONSchemaV1.Options) => Record<string, unknown>;
    }
    /**
     * The target version of the generated JSON Schema.
     *
     * It is *strongly recommended* that implementers support `"draft-2020-12"` and `"draft-07"`, as they are both in wide use. All other targets can be implemented on a best-effort basis. Libraries should throw if they don't support a specified target.
     *
     * The `"openapi-3.0"` target is intended as a standardized specifier for OpenAPI 3.0 which is a superset of JSON Schema `"draft-04"`.
     */
    type Target = "draft-2020-12" | "draft-07" | "openapi-3.0" | ({} & string);
    /** The options for the input/output methods. */
    interface Options {
        /** Specifies the target version of the generated JSON Schema. Support for all versions is on a best-effort basis. If a given version is not supported, the library should throw. */
        readonly target: Target;
        /** Explicit support for additional vendor-specific parameters, if needed. */
        readonly libraryOptions?: Record<string, unknown> | undefined;
    }
    /** The Standard types interface. */
    interface Types<Input = unknown, Output = Input> extends StandardTypedV1.Types<Input, Output> {
    }
    /** Infers the input type of a Standard. */
    type InferInput<Schema extends StandardTypedV1> = StandardTypedV1.InferInput<Schema>;
    /** Infers the output type of a Standard. */
    type InferOutput<Schema extends StandardTypedV1> = StandardTypedV1.InferOutput<Schema>;
}

declare type StandardSchema<SCHEMA = any> = StandardSchemaV1<unknown, SCHEMA> & {
    readonly '~standard': StandardSchemaV1.Props<unknown, SCHEMA> & {
        readonly jsonSchema?: StandardJSONSchemaV1.Converter;
    };
};

declare type StandardSchema_2<SCHEMA = any> = StandardSchemaV1<unknown, SCHEMA> & {
    readonly '~standard': StandardSchemaV1.Props<unknown, SCHEMA> & {
        readonly jsonSchema?: StandardJSONSchemaV1.Converter;
    };
};

/** The Standard Schema interface. */
declare interface StandardSchemaV1<Input = unknown, Output = Input> {
    /** The Standard Schema properties. */
    readonly "~standard": StandardSchemaV1.Props<Input, Output>;
}

declare namespace StandardSchemaV1 {
    /** The Standard Schema properties interface. */
    interface Props<Input = unknown, Output = Input> extends StandardTypedV1.Props<Input, Output> {
        /** Validates unknown input values. */
        readonly validate: (value: unknown, options?: StandardSchemaV1.Options | undefined) => Result<Output> | Promise<Result<Output>>;
    }
    /** The result interface of the validate function. */
    type Result<Output> = SuccessResult<Output> | FailureResult;
    /** The result interface if validation succeeds. */
    interface SuccessResult<Output> {
        /** The typed output value. */
        readonly value: Output;
        /** A falsy value for `issues` indicates success. */
        readonly issues?: undefined;
    }
    interface Options {
        /** Explicit support for additional vendor-specific parameters, if needed. */
        readonly libraryOptions?: Record<string, unknown> | undefined;
    }
    /** The result interface if validation fails. */
    interface FailureResult {
        /** The issues of failed validation. */
        readonly issues: ReadonlyArray<Issue>;
    }
    /** The issue interface of the failure output. */
    interface Issue {
        /** The error message of the issue. */
        readonly message: string;
        /** The path of the issue, if any. */
        readonly path?: ReadonlyArray<PropertyKey | PathSegment> | undefined;
    }
    /** The path segment interface of the issue. */
    interface PathSegment {
        /** The key representing a path segment. */
        readonly key: PropertyKey;
    }
    /** The Standard types interface. */
    interface Types<Input = unknown, Output = Input> extends StandardTypedV1.Types<Input, Output> {
    }
    /** Infers the input type of a Standard. */
    type InferInput<Schema extends StandardTypedV1> = StandardTypedV1.InferInput<Schema>;
    /** Infers the output type of a Standard. */
    type InferOutput<Schema extends StandardTypedV1> = StandardTypedV1.InferOutput<Schema>;
}

/** The Standard Typed interface. This is a base type extended by other specs. */
declare interface StandardTypedV1<Input = unknown, Output = Input> {
    /** The Standard properties. */
    readonly "~standard": StandardTypedV1.Props<Input, Output>;
}

declare namespace StandardTypedV1 {
    /** The Standard Typed properties interface. */
    interface Props<Input = unknown, Output = Input> {
        /** The version number of the standard. */
        readonly version: 1;
        /** The vendor name of the schema library. */
        readonly vendor: string;
        /** Inferred types associated with the schema. */
        readonly types?: Types<Input, Output> | undefined;
    }
    /** The Standard Typed types interface. */
    interface Types<Input = unknown, Output = Input> {
        /** The input type of the schema. */
        readonly input: Input;
        /** The output type of the schema. */
        readonly output: Output;
    }
    /** Infers the input type of a Standard Typed. */
    type InferInput<Schema extends StandardTypedV1> = NonNullable<Schema["~standard"]["types"]>["input"];
    /** Infers the output type of a Standard Typed. */
    type InferOutput<Schema extends StandardTypedV1> = NonNullable<Schema["~standard"]["types"]>["output"];
}

/**
 * A tool call whose `toolName` maps to a tool in the declared tool set,
 * with an `input` type inferred from that tool's input schema.
 */
export declare type StaticToolCall<TOOLS extends ToolSet> = ValueOf<{
    [NAME in keyof TOOLS]: BaseToolCall & {
        toolName: NAME & string;
        input: InferToolInput<TOOLS[NAME]>;
        dynamic?: false | undefined;
        invalid?: false | undefined;
        error?: never;
        title?: string;
    };
}>;

export declare type StaticToolError<TOOLS extends ToolSet> = ValueOf<{
    [NAME in keyof TOOLS]: {
        type: 'tool-error';
        toolCallId: string;
        toolName: NAME & string;
        input: InferToolInput<TOOLS[NAME]>;
        error: unknown;
        providerExecuted?: boolean;
        providerMetadata?: ProviderMetadata;
        toolMetadata?: JSONObject_2;
        dynamic?: false | undefined;
        title?: string;
    };
}>;

/**
 * Precise end event union for statically known tools.
 *
 * Each union member preserves the link between `toolCall.toolName`, the
 * corresponding validated `toolContext`, and the tool execution result.
 */
declare type StaticToolExecutionEndEvent<TOOLS extends ToolSet> = ValueOf<{
    [NAME in keyof TOOLS]: BaseToolExecutionEndFields & {
        readonly toolCall: Extract<StaticToolCall<TOOLS>, {
            toolName: NAME;
        }>;
        readonly toolContext: ToolContextFor<TOOLS[NAME]>;
        readonly toolOutput: ToolOutput<TOOLS>;
    };
}>;

/**
 * Precise start event union for statically known tools.
 *
 * Each union member ties a specific `toolCall.toolName` to that tool's
 * validated `toolContext` type.
 */
declare type StaticToolExecutionStartEvent<TOOLS extends ToolSet> = ValueOf<{
    [NAME in keyof TOOLS]: BaseToolExecutionStartFields & {
        readonly toolCall: Extract<StaticToolCall<TOOLS>, {
            toolName: NAME;
        }>;
        readonly toolContext: ToolContextFor<TOOLS[NAME]>;
    };
}>;

/**
 * Tool output when the tool execution has been denied (for static tools).
 */
export declare type StaticToolOutputDenied<TOOLS extends ToolSet> = ValueOf<{
    [NAME in keyof TOOLS]: {
        type: 'tool-output-denied';
        toolCallId: string;
        toolName: NAME & string;
        providerExecuted?: boolean;
        dynamic?: false | undefined;
    };
}>;

export declare type StaticToolResult<TOOLS extends ToolSet> = ValueOf<{
    [NAME in keyof TOOLS]: {
        type: 'tool-result';
        toolCallId: string;
        toolName: NAME & string;
        input: InferToolInput<TOOLS[NAME]>;
        output: InferToolOutput<TOOLS[NAME]>;
        providerExecuted?: boolean;
        providerMetadata?: ProviderMetadata;
        toolMetadata?: JSONObject_2;
        dynamic?: false | undefined;
        preliminary?: boolean;
        title?: string;
    };
}>;

/**
 * The result of a single step in the generation process.
 */
export declare type StepResult<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = {
    /**
     * Unique identifier for the generation call this step belongs to.
     */
    readonly callId: string;
    /**
     * Zero-based index of this step.
     */
    readonly stepNumber: number;
    /**
     * Information about the model that produced this step.
     */
    readonly model: {
        /** The provider of the model. */
        readonly provider: string;
        /** The ID of the model. */
        readonly modelId: string;
    };
    /**
     * Tool context.
     */
    readonly toolsContext: InferToolSetContext_2<TOOLS>;
    /**
     * The runtime context that was used as input for the step.
     */
    readonly runtimeContext: RUNTIME_CONTEXT;
    /**
     * The content that was generated in the last step.
     */
    readonly content: Array<ContentPart<TOOLS>>;
    /**
     * The concatenation of all text parts generated in this step.
     * It is an empty string if the step contains no text parts.
     */
    readonly text: string;
    /**
     * The reasoning that was generated during the generation.
     */
    readonly reasoning: Array<ReasoningPart_2 | ReasoningFilePart_2>;
    /**
     * The reasoning text that was generated during the generation.
     *
     * It is a concatenation of all reasoning parts (but excluding reasoning file parts).
     * Can be undefined if the model has only generated text.
     */
    readonly reasoningText: string | undefined;
    /**
     * The files that were generated during the generation.
     */
    readonly files: Array<GeneratedFile>;
    /**
     * The sources that were used to generate the text.
     */
    readonly sources: Array<Source>;
    /**
     * The tool calls that were made during the generation.
     */
    readonly toolCalls: Array<TypedToolCall<TOOLS>>;
    /**
     * The static tool calls that were made in the last step.
     */
    readonly staticToolCalls: Array<StaticToolCall<TOOLS>>;
    /**
     * The dynamic tool calls that were made in the last step.
     */
    readonly dynamicToolCalls: Array<DynamicToolCall>;
    /**
     * The results of the tool calls.
     */
    readonly toolResults: Array<TypedToolResult<TOOLS>>;
    /**
     * The static tool results that were made in the last step.
     */
    readonly staticToolResults: Array<StaticToolResult<TOOLS>>;
    /**
     * The dynamic tool results that were made in the last step.
     */
    readonly dynamicToolResults: Array<DynamicToolResult>;
    /**
     * The unified reason why the generation finished.
     */
    readonly finishReason: FinishReason;
    /**
     * The raw reason why the generation finished (from the provider).
     */
    readonly rawFinishReason: string | undefined;
    /**
     * The token usage of the generated text.
     */
    readonly usage: LanguageModelUsage;
    /**
     * Performance metrics for the step.
     */
    readonly performance: StepResultPerformance;
    /**
     * Warnings from the model provider (e.g. unsupported settings).
     */
    readonly warnings: CallWarning[] | undefined;
    /**
     * Additional request information.
     */
    readonly request: LanguageModelRequestMetadata;
    /**
     * Additional response information.
     */
    readonly response: LanguageModelResponseMetadata;
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    readonly providerMetadata: ProviderMetadata | undefined;
};

/**
 * Performance metrics for a single step in the generation process.
 */
export declare type StepResultPerformance = {
    /**
     * Effective number of output tokens per second over the full language model
     * response.
     *
     * Calculated as `outputTokens / requestSeconds`.
     */
    readonly effectiveOutputTokensPerSecond: number;
    /**
     * Number of output tokens per second after the first generated output chunk
     * was received.
     *
     * Only available for streaming steps.
     *
     * Calculated as `outputTokens / outputStreamSeconds`.
     */
    readonly outputTokensPerSecond: number | undefined;
    /**
     * Number of input tokens processed per second before the first generated
     * output chunk was received.
     *
     * Only available for streaming steps.
     *
     * Calculated as `inputTokens / ttftSeconds`.
     */
    readonly inputTokensPerSecond: number | undefined;
    /**
     * Effective number of input and output tokens per second over the full
     * language model response.
     *
     * Calculated as `(inputTokens + outputTokens) / requestSeconds`.
     */
    readonly effectiveTotalTokensPerSecond: number;
    /**
     * Total time spent on the step in milliseconds.
     */
    readonly stepTimeMs: number;
    /**
     * Time spent waiting for the language model response in milliseconds.
     */
    readonly responseTimeMs: number;
    /**
     * Time spent executing each client-side tool call in milliseconds, keyed by
     * tool call ID.
     */
    readonly toolExecutionMs: Readonly<Record<string, number>>;
    /**
     * Time until the first generated output chunk was received in milliseconds.
     *
     * This includes text deltas, reasoning deltas, generated files, reasoning
     * files, tool input deltas, and tool calls.
     *
     * Only available for streaming steps.
     */
    readonly timeToFirstOutputMs: number | undefined;
    /**
     * Timing statistics for the gaps between generated output chunks in
     * milliseconds.
     *
     * Only available for streaming steps with at least two generated output
     * chunks.
     */
    readonly timeBetweenOutputChunksMs?: OutputChunkTimingStats;
};

/**
 * A step boundary part of a message.
 */
export declare type StepStartUIPart = {
    type: 'step-start';
};

/**
 * A predicate that decides whether a tool-calling loop should stop after the
 * current step.
 *
 * A tool calling loop continues until one of the following conditions is met:
 * - The model returns a finish reason other than `tool-calls`
 * - A tool without an execute function is called
 * - A tool call needs approval
 * - One of the provided stop conditions returns `true`
 */
export declare type StopCondition<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2> = (options: {
    steps: Array<StepResult<TOOLS, RUNTIME_CONTEXT>>;
}) => PromiseLike<boolean> | boolean;

/**
 * Minimal interface for a streaming tool call delta from an OpenAI-compatible API.
 */
declare interface StreamingToolCallDelta {
    index?: number | null;
    id?: string | null;
    type?: string | null;
    function?: {
        name?: string | null;
        arguments?: string | null;
    } | null;
}

/**
 * Minimal interface for a streaming tool call delta from an OpenAI-compatible API.
 */
declare interface StreamingToolCallDelta_2 {
    index?: number | null;
    id?: string | null;
    type?: string | null;
    function?: {
        name?: string | null;
        arguments?: string | null;
    } | null;
}

/**
 * Tracks streaming tool call state across multiple deltas from an
 * OpenAI-compatible chat completion stream. Handles argument accumulation,
 * emits tool-input-start/delta/end and tool-call events, and finalizes
 * unfinished tool calls on flush.
 *
 * Used by openai, openai-compatible, groq, deepseek, and alibaba providers.
 */
declare class StreamingToolCallTracker<DELTA extends StreamingToolCallDelta = StreamingToolCallDelta> {
    private toolCalls;
    private readonly controller;
    private readonly _generateId;
    private readonly typeValidation;
    private readonly extractMetadata?;
    private readonly buildToolCallProviderMetadata?;
    constructor(controller: StreamingToolCallTrackerController, options?: StreamingToolCallTrackerOptions<DELTA>);
    /**
     * Process a tool call delta from a streaming response chunk.
     * Emits tool-input-start, tool-input-delta, tool-input-end, and tool-call
     * events as appropriate.
     */
    processDelta(toolCallDelta: DELTA): void;
    /**
     * Finalize any unfinished tool calls. Should be called during the stream's
     * flush handler to ensure all tool calls are properly completed.
     */
    flush(): void;
    private processNewToolCall;
    private processExistingToolCall;
    private finishToolCall;
}

/**
 * Tracks streaming tool call state across multiple deltas from an
 * OpenAI-compatible chat completion stream. Handles argument accumulation,
 * emits tool-input-start/delta/end and tool-call events, and finalizes
 * unfinished tool calls on flush.
 *
 * Used by openai, openai-compatible, groq, deepseek, and alibaba providers.
 */
declare class StreamingToolCallTracker_2<DELTA extends StreamingToolCallDelta_2 = StreamingToolCallDelta_2> {
    private toolCalls;
    private readonly controller;
    private readonly _generateId;
    private readonly typeValidation;
    private readonly extractMetadata?;
    private readonly buildToolCallProviderMetadata?;
    constructor(controller: StreamingToolCallTrackerController_2, options?: StreamingToolCallTrackerOptions_2<DELTA>);
    /**
     * Process a tool call delta from a streaming response chunk.
     * Emits tool-input-start, tool-input-delta, tool-input-end, and tool-call
     * events as appropriate.
     */
    processDelta(toolCallDelta: DELTA): void;
    /**
     * Finalize any unfinished tool calls. Should be called during the stream's
     * flush handler to ensure all tool calls are properly completed.
     */
    flush(): void;
    private processNewToolCall;
    private processExistingToolCall;
    private finishToolCall;
}

declare type StreamingToolCallTrackerController = Pick<TransformStreamDefaultController<LanguageModelV4StreamPart>, 'enqueue'>;

declare type StreamingToolCallTrackerController_2 = Pick<TransformStreamDefaultController<LanguageModelV4StreamPart>, 'enqueue'>;

declare interface StreamingToolCallTrackerOptions<DELTA extends StreamingToolCallDelta = StreamingToolCallDelta> {
    /**
     * ID generator function for tool call IDs.
     * Defaults to the standard generateId.
     */
    generateId?: () => string;
    /**
     * How to validate the `type` field on new tool call deltas.
     * - `'none'`: no validation (default)
     * - `'if-present'`: throw if type is present and not `'function'`
     * - `'required'`: throw if type is not exactly `'function'`
     */
    typeValidation?: 'none' | 'if-present' | 'required';
    /**
     * Extract provider-specific metadata from a tool call delta.
     * Called once when a new tool call is detected.
     * The returned metadata is stored on the tool call and passed to
     * `buildToolCallProviderMetadata` when the tool call is finalized.
     */
    extractMetadata?: (delta: DELTA) => SharedV4ProviderMetadata | undefined;
    /**
     * Build the `providerMetadata` object for a `tool-call` event.
     * Receives the metadata previously extracted via `extractMetadata`.
     * If `undefined` is returned, no `providerMetadata` is included in the event.
     */
    buildToolCallProviderMetadata?: (metadata: SharedV4ProviderMetadata | undefined) => SharedV4ProviderMetadata | undefined;
}

declare interface StreamingToolCallTrackerOptions_2<DELTA extends StreamingToolCallDelta_2 = StreamingToolCallDelta_2> {
    /**
     * ID generator function for tool call IDs.
     * Defaults to the standard generateId.
     */
    generateId?: () => string;
    /**
     * How to validate the `type` field on new tool call deltas.
     * - `'none'`: no validation (default)
     * - `'if-present'`: throw if type is present and not `'function'`
     * - `'required'`: throw if type is not exactly `'function'`
     */
    typeValidation?: 'none' | 'if-present' | 'required';
    /**
     * Extract provider-specific metadata from a tool call delta.
     * Called once when a new tool call is detected.
     * The returned metadata is stored on the tool call and passed to
     * `buildToolCallProviderMetadata` when the tool call is finalized.
     */
    extractMetadata?: (delta: DELTA) => SharedV4ProviderMetadata | undefined;
    /**
     * Build the `providerMetadata` object for a `tool-call` event.
     * Receives the metadata previously extracted via `extractMetadata`.
     * If `undefined` is returned, no `providerMetadata` is included in the event.
     */
    buildToolCallProviderMetadata?: (metadata: SharedV4ProviderMetadata | undefined) => SharedV4ProviderMetadata | undefined;
}

/**
 * Generate a structured, typed object for a given prompt and schema using a language model.
 *
 * This function streams the output. If you do not want to stream the output, use `generateObject` instead.
 *
 * @param model - The language model to use.
 *
 * @param system - A system message that will be part of the prompt.
 * @param prompt - A simple text prompt. You can either use `prompt` or `messages` but not both.
 * @param messages - A list of messages. You can either use `prompt` or `messages` but not both.
 * @param allowSystemInMessages - Whether system messages are allowed in the `prompt` or `messages` fields. Default: false.
 *
 * @param maxOutputTokens - Maximum number of tokens to generate.
 * @param temperature - Temperature setting.
 * The value is passed through to the provider. The range depends on the provider and model.
 * It is recommended to set either `temperature` or `topP`, but not both.
 * @param topP - Nucleus sampling.
 * The value is passed through to the provider. The range depends on the provider and model.
 * It is recommended to set either `temperature` or `topP`, but not both.
 * @param topK - Only sample from the top K options for each subsequent token.
 * Used to remove "long tail" low probability responses.
 * Recommended for advanced use cases only. You usually only need to use temperature.
 * @param presencePenalty - Presence penalty setting.
 * It affects the likelihood of the model to repeat information that is already in the prompt.
 * The value is passed through to the provider. The range depends on the provider and model.
 * @param frequencyPenalty - Frequency penalty setting.
 * It affects the likelihood of the model to repeatedly use the same words or phrases.
 * The value is passed through to the provider. The range depends on the provider and model.
 * @param stopSequences - Stop sequences.
 * If set, the model will stop generating text when one of the stop sequences is generated.
 * @param seed - The seed (integer) to use for random sampling.
 * If set and supported by the model, calls will generate deterministic results.
 *
 * @param maxRetries - Maximum number of retries. Set to 0 to disable retries. Default: 2.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param headers - Additional HTTP headers to be sent with the request. Only applicable for HTTP-based providers.
 *
 * @param schema - The schema of the object that the model should generate.
 * @param schemaName - Optional name of the output that should be generated.
 * Used by some providers for additional LLM guidance, e.g.
 * via tool or schema name.
 * @param schemaDescription - Optional description of the output that should be generated.
 * Used by some providers for additional LLM guidance, e.g.
 * via tool or schema description.
 *
 * @param output - The type of the output.
 *
 * - 'object': The output is an object.
 * - 'array': The output is an array.
 * - 'enum': The output is an enum.
 * - 'no-schema': The output is not a schema.
 *
 * @param repairText - A function that attempts to repair the raw output of the model
 * to enable JSON parsing.
 * @param experimental_repairText - Deprecated alias for `repairText`.
 *
 * @param telemetry - Optional telemetry configuration.
 *
 * @param providerOptions - Additional provider-specific options. They are passed through
 * to the provider from the AI SDK and enable provider-specific
 * functionality that can be fully encapsulated in the provider.
 *
 * @returns
 * A result object for accessing the partial object stream and additional information.
 *
 * @deprecated Use `streamText` with an `output` setting instead.
 */
export declare function streamObject<SCHEMA extends FlexibleSchema<unknown> = FlexibleSchema<JSONValue_3>, OUTPUT extends 'object' | 'array' | 'enum' | 'no-schema' = InferSchema<SCHEMA> extends string ? 'enum' : 'object', RESULT = OUTPUT extends 'array' ? Array<InferSchema<SCHEMA>> : InferSchema<SCHEMA>>(options: Omit<LanguageModelCallOptions, 'stopSequences'> & Omit<RequestOptions, 'timeout'> & Prompt & (OUTPUT extends 'enum' ? {
    /**
     * The enum values that the model should use.
     */
    enum: Array<RESULT>;
    output: 'enum';
} : OUTPUT extends 'no-schema' ? {} : {
    /**
     * The schema of the object that the model should generate.
     */
    schema: SCHEMA;
    /**
     * Optional name of the output that should be generated.
     * Used by some providers for additional LLM guidance, e.g.
     * via tool or schema name.
     */
    schemaName?: string;
    /**
     * Optional description of the output that should be generated.
     * Used by some providers for additional LLM guidance, e.g.
     * via tool or schema description.
     */
    schemaDescription?: string;
}) & {
    output?: OUTPUT;
    /**
     * The language model to use.
     */
    model: LanguageModel;
    /**
     * A function that attempts to repair the raw output of the model
     * to enable JSON parsing.
     */
    repairText?: RepairTextFunction;
    /**
     * A function that attempts to repair the raw output of the model
     * to enable JSON parsing.
     *
     * @deprecated Use `repairText` instead.
     */
    experimental_repairText?: RepairTextFunction;
    /**
     * Optional telemetry configuration.
     */
    telemetry?: TelemetryOptions;
    /**
     * Optional telemetry configuration.
     *
     * @deprecated Use `telemetry` instead. This alias will be removed in a future major release.
     */
    experimental_telemetry?: TelemetryOptions;
    /**
     * Custom download function to use for URLs.
     *
     * By default, files are downloaded if the model does not support the URL for the given media type.
     */
    experimental_download?: Experimental_DownloadFunction | undefined;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Callback that is called when the streamObject operation begins,
     * before the LLM call is made.
     */
    onStart?: Callback<GenerateObjectStartEvent>;
    /**
     * Callback that is called when the streamObject operation begins,
     * before the LLM call is made.
     *
     * @deprecated Use `onStart` instead.
     */
    experimental_onStart?: Callback<GenerateObjectStartEvent>;
    /**
     * Callback that is called when the model call (step) begins,
     * before the provider is called.
     */
    onStepStart?: Callback<GenerateObjectStepStartEvent>;
    /**
     * Callback that is called when the model call (step) begins,
     * before the provider is called.
     *
     * @deprecated Use `onStepStart` instead.
     */
    experimental_onStepStart?: Callback<GenerateObjectStepStartEvent>;
    /**
     * Callback that is called when the model streaming step completes,
     * with the raw accumulated text before final schema validation.
     */
    onStepEnd?: Callback<GenerateObjectStepEndEvent>;
    /**
     * Callback that is called when the model streaming step completes,
     * with the raw accumulated text before final schema validation.
     *
     * @deprecated Use `onStepEnd` instead.
     */
    onStepFinish?: Callback<GenerateObjectStepEndEvent>;
    /**
     * Callback that is invoked when an error occurs during streaming.
     * You can use it to log errors.
     * The stream processing will pause until the callback promise is resolved.
     */
    onError?: StreamObjectOnErrorCallback;
    /**
     * Callback that is called when the LLM response and the final object validation are finished.
     */
    onFinish?: Callback<GenerateObjectEndEvent<RESULT>>;
    /**
     * Internal. For test use only. May change without notice.
     */
    _internal?: {
        generateId?: () => string;
        currentDate?: () => Date;
        now?: () => number;
    };
}): StreamObjectResult<OUTPUT extends 'enum' ? string : OUTPUT extends 'array' ? RESULT : DeepPartial<RESULT>, OUTPUT extends 'array' ? RESULT : RESULT, OUTPUT extends 'array' ? RESULT extends Array<infer U> ? AsyncIterableStream<U> : never : never>;

/**
 * Callback that is set using the `onError` option.
 *
 * @param event - The event that is passed to the callback.
 */
declare type StreamObjectOnErrorCallback = (event: {
    error: unknown;
}) => Promise<void> | void;

/**
 * Callback that is set using the `onFinish` option.
 *
 * @param event - The event that is passed to the callback.
 */
export declare type StreamObjectOnFinishCallback<RESULT> = (event: {
    /**
     * The token usage of the generated response.
     */
    usage: LanguageModelUsage;
    /**
     * The generated object. Can be undefined if the final object does not match the schema.
     */
    object: RESULT | undefined;
    /**
     * Optional error object. This is e.g. a TypeValidationError when the final object does not match the schema.
     */
    error: unknown | undefined;
    /**
     * Response metadata.
     */
    response: LanguageModelResponseMetadata;
    /**
     * Warnings from the model provider (e.g. unsupported settings).
     */
    warnings?: CallWarning[];
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerMetadata: ProviderMetadata | undefined;
}) => Promise<void> | void;

/**
 * The result of a `streamObject` call that contains the partial object stream and additional information.
 */
export declare interface StreamObjectResult<PARTIAL, RESULT, ELEMENT_STREAM> {
    /**
     * Warnings from the model provider (e.g. unsupported settings)
     */
    readonly warnings: Promise<CallWarning[] | undefined>;
    /**
     * The token usage of the generated response. Resolved when the response is finished.
     */
    readonly usage: Promise<LanguageModelUsage>;
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    readonly providerMetadata: Promise<ProviderMetadata | undefined>;
    /**
     * Additional request information from the last step.
     */
    readonly request: Promise<Omit<LanguageModelRequestMetadata, 'messages'>>;
    /**
     * Additional response information.
     */
    readonly response: Promise<Omit<LanguageModelResponseMetadata, 'messages'>>;
    /**
     * The reason why the generation finished. Taken from the last step.
     *
     * Resolved when the response is finished.
     */
    readonly finishReason: Promise<FinishReason>;
    /**
     * The generated object (typed according to the schema). Resolved when the response is finished.
     */
    readonly object: Promise<RESULT>;
    /**
     * Stream of partial objects. It gets more complete as the stream progresses.
     *
     * Note that the partial object is not validated.
     * If you want to be certain that the actual content matches your schema, you need to implement your own validation for partial results.
     */
    readonly partialObjectStream: AsyncIterableStream<PARTIAL>;
    /**
     * Stream over complete array elements. Only available if the output strategy is set to `array`.
     */
    readonly elementStream: ELEMENT_STREAM;
    /**
     * Text stream of the JSON representation of the generated object. It contains text chunks.
     * When the stream is finished, the object is valid JSON that can be parsed.
     */
    readonly textStream: AsyncIterableStream<string>;
    /**
     * Stream of different types of events, including partial objects, errors, and finish events.
     * Only errors that stop the stream, such as network errors, are thrown.
     */
    readonly fullStream: AsyncIterableStream<ObjectStreamPart<PARTIAL>>;
    /**
     * Writes text delta output to a Node.js response-like object.
     * It sets a `Content-Type` header to `text/plain; charset=utf-8` and
     * writes each text delta as a separate chunk.
     *
     * @param response A Node.js response-like object (ServerResponse).
     * @param init Optional headers, status code, and status text.
     */
    pipeTextStreamToResponse(response: ServerResponse_2, init?: ResponseInit): Promise<void>;
    /**
     * Creates a simple text stream response.
     * The response has a `Content-Type` header set to `text/plain; charset=utf-8`.
     * Each text delta is encoded as UTF-8 and sent as a separate chunk.
     * Non-text-delta events are ignored.
     *
     * @param init Optional headers, status code, and status text.
     */
    toTextStreamResponse(init?: ResponseInit): Response;
}

/**
 * Options for the EventSourceParserStream.
 *
 * @public
 */
declare interface StreamOptions {
    /**
     * Behavior when a parsing error occurs.
     *
     * - A custom function can be provided to handle the error.
     * - `'terminate'` will error the stream and stop parsing.
     * - Any other value will ignore the error and continue parsing.
     *
     * @defaultValue `undefined`
     */
    onError?: ("terminate" | ((error: Error) => void)) | undefined;
    /**
     * Callback for when a reconnection interval is sent from the server.
     *
     * @param retry - The number of milliseconds to wait before reconnecting.
     */
    onRetry?: ((retry: number) => void) | undefined;
    /**
     * Callback for when a comment is encountered in the stream.
     *
     * @param comment - The comment encountered in the stream.
     */
    onComment?: ((comment: string) => void) | undefined;
}

/**
 * Generate a text and call tools for a given prompt using a language model.
 *
 * This function streams the output. If you do not want to stream the output, use `generateText` instead.
 *
 * @param model - The language model to use.
 * @param tools - Tools that are accessible to and can be called by the model. The model needs to support calling tools.
 * @param toolOrder - Controls the order in which tools are sent to the provider. Tools not listed are appended alphabetically.
 *
 * @param system - A system message that will be part of the prompt.
 * @param prompt - A simple text prompt. You can either use `prompt` or `messages` but not both.
 * @param messages - A list of messages. You can either use `prompt` or `messages` but not both.
 * @param allowSystemInMessages - Whether system messages are allowed in the `prompt` or `messages` fields. Default: false.
 *
 * @param maxOutputTokens - Maximum number of tokens to generate.
 * @param temperature - Temperature setting.
 * The value is passed through to the provider. The range depends on the provider and model.
 * It is recommended to set either `temperature` or `topP`, but not both.
 * @param topP - Nucleus sampling.
 * The value is passed through to the provider. The range depends on the provider and model.
 * It is recommended to set either `temperature` or `topP`, but not both.
 * @param topK - Only sample from the top K options for each subsequent token.
 * Used to remove "long tail" low probability responses.
 * Recommended for advanced use cases only. You usually only need to use temperature.
 * @param presencePenalty - Presence penalty setting.
 * It affects the likelihood of the model to repeat information that is already in the prompt.
 * The value is passed through to the provider. The range depends on the provider and model.
 * @param frequencyPenalty - Frequency penalty setting.
 * It affects the likelihood of the model to repeatedly use the same words or phrases.
 * The value is passed through to the provider. The range depends on the provider and model.
 * @param stopSequences - Stop sequences.
 * If set, the model will stop generating text when one of the stop sequences is generated.
 * @param seed - The seed (integer) to use for random sampling.
 * If set and supported by the model, calls will generate deterministic results.
 *
 * @param maxRetries - Maximum number of retries. Set to 0 to disable retries. Default: 2.
 * @param abortSignal - An optional abort signal that can be used to cancel the call.
 * @param timeout - An optional timeout in milliseconds. The call will be aborted if it takes longer than the specified timeout.
 * @param headers - Additional HTTP headers to be sent with the request. Only applicable for HTTP-based providers.
 *
 * @param experimental_sandbox - The sandbox environment that is passed through to tool execution.
 * @param runtimeContext - User-defined runtime context that flows through the entire generation lifecycle.
 * @param experimental_refineToolInput - Optional mapping of tool names to functions that refine parsed tool inputs before tools are executed and before outputs, callbacks, and telemetry are recorded.
 *
 * @param onChunk - Callback that is called for each chunk of the stream. The stream processing will pause until the callback promise is resolved.
 * @param onError - Callback that is called when an error occurs during streaming. You can use it to log errors.
 * @param onStart - Callback invoked when generation begins, before any LLM calls.
 * @param experimental_onStart - Deprecated alias for `onStart`.
 * @param onStepStart - Callback invoked when each step begins, before the provider is called.
 * @param experimental_onStepStart - Deprecated alias for `onStepStart`.
 * @param onLanguageModelCallStart - Callback invoked immediately before each provider model call begins.
 * @param experimental_onLanguageModelCallStart - Deprecated alias for `onLanguageModelCallStart`.
 * @param onLanguageModelCallEnd - Callback invoked after each provider model call response is normalized and parsed.
 * @param experimental_onLanguageModelCallEnd - Deprecated alias for `onLanguageModelCallEnd`.
 * @param onToolExecutionStart - Callback invoked before each tool execution begins.
 * @param experimental_onToolCallStart - Deprecated alias for `onToolExecutionStart`.
 * @param onToolExecutionEnd - Callback invoked after each tool execution completes.
 * @param experimental_onToolCallFinish - Deprecated alias for `onToolExecutionEnd`.
 * @param onStepEnd - Callback that is called when each step (LLM call) ends, including intermediate steps.
 * @param onStepFinish - Deprecated alias for `onStepEnd`.
 * @param onEnd - Callback that is called when all steps are finished and the response is complete.
 * @param onFinish - Deprecated alias for `onEnd`.
 *
 * @returns
 * A result object for accessing different stream types and additional information.
 */
export declare function streamText<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = Output_2<string, string, never>>({ model, tools, toolChoice, instructions, system, prompt, messages, allowSystemInMessages, maxRetries, abortSignal, timeout, headers, stopWhen, experimental_sandbox: sandbox, output, toolApproval, experimental_toolCallers, experimental_toolApprovalSecret, experimental_telemetry, telemetry, prepareStep, providerOptions, activeTools, toolOrder, experimental_repairToolCall, repairToolCall, experimental_refineToolInput: refineToolInput, experimental_transform: transform, experimental_download: download, includeRawChunks, onChunk, onError, onFinish, onEnd, onAbort, onStepEnd, onStepFinish, onStart, experimental_onStart, onStepStart, experimental_onStepStart, onLanguageModelCallStart, experimental_onLanguageModelCallStart, onLanguageModelCallEnd, experimental_onLanguageModelCallEnd, onToolExecutionStart, onToolExecutionEnd, experimental_onToolCallStart, experimental_onToolCallFinish, runtimeContext, toolsContext, experimental_include, include, _internal: { now, generateId, generateCallId, }, ...settings }: LanguageModelCallOptions & RequestOptions<TOOLS> & Prompt & ToolsContextParameter<TOOLS> & {
    /**
     * The language model to use.
     */
    model: LanguageModel;
    /**
     * The tool choice strategy. Default: 'auto'.
     */
    toolChoice?: ToolChoice<TOOLS>;
    /**
     * Condition for stopping the generation when there are tool results in the last step.
     * When the condition is an array, any of the conditions can be met to stop the generation.
     *
     * @default isStepCount(1)
     */
    stopWhen?: Arrayable_2<StopCondition<NoInfer<TOOLS>, RUNTIME_CONTEXT>>;
    /**
     * Optional telemetry configuration.
     */
    telemetry?: TelemetryOptions<RUNTIME_CONTEXT, NoInfer<TOOLS>>;
    /**
     * Optional telemetry configuration.
     *
     * @deprecated Use `telemetry` instead. This alias will be removed in a future major release.
     */
    experimental_telemetry?: TelemetryOptions<RUNTIME_CONTEXT, NoInfer<TOOLS>>;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * The sandbox environment that is passed through to tool execution.
     */
    experimental_sandbox?: Experimental_SandboxSession;
    /**
     * Runtime context. Treat runtime context as immutable.
     * If you need to mutate runtime context, update it in `prepareStep`.
     */
    runtimeContext?: RUNTIME_CONTEXT;
    /**
     * Limits the tools that are available for the model to call without
     * changing the tool call and result types in the result.
     */
    activeTools?: ActiveTools<NoInfer<TOOLS>>;
    /**
     * Controls the order in which tools are sent to the provider.
     *
     * The list can be partial. Tools not listed in `toolOrder` are sent after
     * the listed tools, sorted alphabetically. This can improve provider-side
     * caching by keeping tool definitions in a stable order.
     */
    toolOrder?: ToolOrder<NoInfer<TOOLS>>;
    /**
     * Optional specification for parsing structured outputs from the LLM response.
     */
    output?: OUTPUT;
    /**
     * Optional tool approval configuration.
     *
     * This configuration takes precedence over tool-defined approval settings.
     */
    toolApproval?: ToolApprovalConfiguration<TOOLS, RUNTIME_CONTEXT>;
    /**
     * Configures which caller tools may invoke each tool.
     */
    experimental_toolCallers?: Experimental_ToolCallers<NoInfer<TOOLS>>;
    /**
     * Secret for HMAC-signing tool approval requests. When set, the server
     * signs each approval request at issuance and verifies the signature when
     * the approval is replayed, preventing client-forged approvals.
     */
    experimental_toolApprovalSecret?: string | Uint8Array;
    /**
     * Optional function that you can use to provide different settings for a step.
     *
     * @param options - The options for the step.
     * @param options.steps - The steps that have been executed so far.
     * @param options.stepNumber - The number of the step that is being executed.
     * @param options.model - The model that is being used.
     *
     * @returns An object that contains the settings for the step.
     * If you return undefined (or for undefined settings), the settings from the outer level will be used.
     */
    prepareStep?: PrepareStepFunction<NoInfer<TOOLS>, RUNTIME_CONTEXT>;
    /**
     * A function that attempts to repair a tool call that failed to parse.
     */
    repairToolCall?: ToolCallRepairFunction<TOOLS>;
    /**
     * A function that attempts to repair a tool call that failed to parse.
     *
     * @deprecated Use `repairToolCall` instead.
     */
    experimental_repairToolCall?: ToolCallRepairFunction<TOOLS>;
    /**
     * Optional mapping of tool names to functions that refine parsed tool inputs.
     *
     * The refined input must have the same type shape as the tool input. Refined
     * inputs are used for tool execution, stream parts, callbacks, and telemetry.
     */
    experimental_refineToolInput?: ToolInputRefinement<NoInfer<TOOLS>>;
    /**
     * Optional stream transformations.
     * They are applied in the order they are provided.
     * The stream transformations must maintain the stream structure for streamText to work correctly.
     */
    experimental_transform?: Arrayable_2<StreamTextTransform<TOOLS>>;
    /**
     * Custom download function to use for URLs.
     *
     * By default, files are downloaded if the model does not support the URL for the given media type.
     */
    experimental_download?: Experimental_DownloadFunction | undefined;
    /**
     * Whether to include raw chunks from the provider in the stream.
     * When enabled, you will receive raw chunks with type 'raw' that contain the unprocessed data from the provider.
     * This allows access to cutting-edge provider features not yet wrapped by the AI SDK.
     * Defaults to false.
     *
     * @deprecated Use `include.rawChunks` instead.
     */
    includeRawChunks?: boolean;
    /**
     * Callback that is called for each chunk of the stream.
     * The stream processing will pause until the callback promise is resolved.
     */
    onChunk?: StreamTextOnChunkCallback<TOOLS>;
    /**
     * Callback that is invoked when an error occurs during streaming.
     * You can use it to log errors.
     * The stream processing will pause until the callback promise is resolved.
     */
    onError?: StreamTextOnErrorCallback;
    /**
     * Callback that is called when the LLM response and all request tool executions
     * (for tools that have an `execute` function) are finished.
     *
     * The usage is the combined usage of all steps.
     */
    onEnd?: GenerateTextOnEndCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Callback that is called when the LLM response and all request tool executions
     * (for tools that have an `execute` function) are finished.
     *
     * The usage is the combined usage of all steps.
     *
     * @deprecated Use `onEnd` instead.
     */
    onFinish?: GenerateTextOnEndCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    onAbort?: StreamTextOnAbortCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Callback that is called when each step (LLM call) ends, including intermediate steps.
     */
    onStepEnd?: GenerateTextOnStepEndCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Callback that is called when each step (LLM call) ends, including intermediate steps.
     *
     * @deprecated Use `onStepEnd` instead.
     */
    onStepFinish?: GenerateTextOnStepFinishCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Callback that is called when the streamText operation begins,
     * before any LLM calls are made.
     */
    onStart?: GenerateTextOnStartCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>, NoInfer<OUTPUT>>;
    /**
     * Callback that is called when the streamText operation begins,
     * before any LLM calls are made.
     *
     * @deprecated Use `onStart` instead.
     */
    experimental_onStart?: GenerateTextOnStartCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>, NoInfer<OUTPUT>>;
    /**
     * Callback that is called when a step (LLM call) begins,
     * before the provider is called.
     */
    onStepStart?: GenerateTextOnStepStartCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>, NoInfer<OUTPUT>>;
    /**
     * Callback that is called when a step (LLM call) begins,
     * before the provider is called.
     *
     * @deprecated Use `onStepStart` instead.
     */
    experimental_onStepStart?: GenerateTextOnStepStartCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>, NoInfer<OUTPUT>>;
    /**
     * Callback that is called immediately before the provider model call begins.
     */
    onLanguageModelCallStart?: OnLanguageModelCallStartCallback;
    /**
     * Callback that is called immediately before the provider model call begins.
     *
     * @deprecated Use `onLanguageModelCallStart` instead.
     */
    experimental_onLanguageModelCallStart?: OnLanguageModelCallStartCallback;
    /**
     * Callback that is called after the model response has been normalized and parsed,
     * but before any client-side tool execution begins.
     */
    onLanguageModelCallEnd?: OnLanguageModelCallEndCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called after the model response has been normalized and parsed,
     * but before any client-side tool execution begins.
     *
     * @deprecated Use `onLanguageModelCallEnd` instead.
     */
    experimental_onLanguageModelCallEnd?: OnLanguageModelCallEndCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called right before a tool's execute function runs.
     */
    onToolExecutionStart?: OnToolExecutionStartCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called right before a tool's execute function runs.
     *
     * @deprecated Use `onToolExecutionStart` instead.
     */
    experimental_onToolCallStart?: OnToolExecutionStartCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called right after a tool's execute function completes (or errors).
     */
    onToolExecutionEnd?: OnToolExecutionEndCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called right after a tool's execute function completes (or errors).
     *
     * @deprecated Use `onToolExecutionEnd` instead.
     */
    experimental_onToolCallFinish?: OnToolExecutionEndCallback<NoInfer<TOOLS>>;
    /**
     * Settings for controlling what data is included in step results.
     * Disabling inclusion can help reduce memory usage when processing
     * large payloads like images.
     *
     * By default, request bodies and request messages are excluded.
     */
    include?: StreamTextInclude;
    /**
     * Settings for controlling what data is included in step results.
     *
     * @deprecated Use `include` instead.
     */
    experimental_include?: StreamTextInclude;
    /**
     * Internal. For test use only. May change without notice.
     */
    _internal?: {
        now?: () => number;
        generateId?: IdGenerator;
        generateCallId?: IdGenerator;
    };
}): StreamTextResult<TOOLS, RUNTIME_CONTEXT, OUTPUT>;

export declare type StreamTextInclude = {
    /**
     * Whether to retain the request body in step results.
     * The request body can be large when sending images or files.
     *
     * @default false
     */
    requestBody?: boolean;
    /**
     * Whether to retain the request messages in step results.
     * The request messages can be large when sending images or files.
     *
     * @default false
     */
    requestMessages?: boolean;
    /**
     * Whether to include raw chunks from the provider in the stream.
     *
     * When enabled, you will receive raw chunks with type 'raw' that contain
     * the unprocessed data from the provider.
     *
     * This allows access to cutting-edge provider features not yet wrapped by
     * the AI SDK.
     *
     * @default false
     */
    rawChunks?: boolean;
};

/**
 * Callback that is set using the `onAbort` option.
 *
 * @param event - The event that is passed to the callback.
 */
declare type StreamTextOnAbortCallback<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2> = Callback<{
    /**
     * Details for all previously finished steps.
     */
    readonly steps: StepResult<TOOLS, RUNTIME_CONTEXT>[];
}>;

/**
 * Callback that is set using the `onChunk` option.
 *
 * @param event - The event that is passed to the callback.
 */
export declare type StreamTextOnChunkCallback<TOOLS extends ToolSet> = (event: {
    chunk: TextStreamPart<TOOLS>;
}) => PromiseLike<void> | void;

/**
 * Callback that is set using the `onError` option.
 *
 * @param event - The event that is passed to the callback.
 */
export declare type StreamTextOnErrorCallback = Callback<{
    error: unknown;
}>;

/**
 * A result object for accessing different stream types and additional information.
 */
export declare interface StreamTextResult<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2, OUTPUT extends Output_2> {
    /**
     * The content that was generated in all steps.
     *
     * Automatically consumes the stream.
     */
    readonly content: PromiseLike<Array<ContentPart<TOOLS>>>;
    /**
     * The full text that has been generated by the final step.
     *
     * Automatically consumes the stream.
     */
    readonly text: PromiseLike<string>;
    /**
     * The full reasoning that the model has generated.
     *
     * Automatically consumes the stream.
     *
     * @deprecated Use `finalStep.reasoning` instead.
     */
    readonly reasoning: PromiseLike<Array<ReasoningOutput | ReasoningFileOutput>>;
    /**
     * The reasoning that has been generated by the last step.
     *
     * Automatically consumes the stream.
     *
     * @deprecated Use `finalStep.reasoningText` instead.
     */
    readonly reasoningText: PromiseLike<string | undefined>;
    /**
     * Files that have been generated by the model in all steps.
     *
     * Automatically consumes the stream.
     */
    readonly files: PromiseLike<GeneratedFile[]>;
    /**
     * Sources that have been used as references in all steps.
     *
     * Automatically consumes the stream.
     */
    readonly sources: PromiseLike<Source[]>;
    /**
     * The tool calls that have been executed in all steps.
     *
     * Automatically consumes the stream.
     */
    readonly toolCalls: PromiseLike<TypedToolCall<TOOLS>[]>;
    /**
     * The static tool calls that have been executed in all steps.
     *
     * Automatically consumes the stream.
     */
    readonly staticToolCalls: PromiseLike<StaticToolCall<TOOLS>[]>;
    /**
     * The dynamic tool calls that have been executed in all steps.
     *
     * Automatically consumes the stream.
     */
    readonly dynamicToolCalls: PromiseLike<DynamicToolCall[]>;
    /**
     * The static tool results that have been generated in all steps.
     *
     * Automatically consumes the stream.
     */
    readonly staticToolResults: PromiseLike<StaticToolResult<TOOLS>[]>;
    /**
     * The dynamic tool results that have been generated in all steps.
     *
     * Automatically consumes the stream.
     */
    readonly dynamicToolResults: PromiseLike<DynamicToolResult[]>;
    /**
     * The tool results that have been generated in all steps.
     *
     * Automatically consumes the stream.
     */
    readonly toolResults: PromiseLike<TypedToolResult<TOOLS>[]>;
    /**
     * The unified finish reason why the generation finished. Taken from the last step.
     *
     * Automatically consumes the stream.
     */
    readonly finishReason: PromiseLike<FinishReason>;
    /**
     * The raw reason why the generation finished (from the provider). Taken from the last step.
     *
     * Automatically consumes the stream.
     */
    readonly rawFinishReason: PromiseLike<string | undefined>;
    /**
     * The total token usage of the generated response.
     * When there are multiple steps, the usage is the sum of all step usages.
     *
     * Automatically consumes the stream.
     */
    readonly usage: PromiseLike<LanguageModelUsage>;
    /**
     * The total token usage of the generated response.
     * When there are multiple steps, the usage is the sum of all step usages.
     *
     * Automatically consumes the stream.
     *
     * @deprecated Use `usage` instead.
     */
    readonly totalUsage: PromiseLike<LanguageModelUsage>;
    /**
     * Warnings from the model provider (e.g. unsupported settings) in all steps.
     *
     * Automatically consumes the stream.
     */
    readonly warnings: PromiseLike<CallWarning[] | undefined>;
    /**
     * Details for all steps.
     * You can use this to get information about intermediate steps,
     * such as the tool calls or the response headers.
     *
     * Automatically consumes the stream.
     */
    readonly steps: PromiseLike<Array<StepResult<TOOLS, RUNTIME_CONTEXT>>>;
    /**
     * The final step. This is a shortcut for `steps.at(-1)`.
     *
     * Automatically consumes the stream.
     */
    readonly finalStep: PromiseLike<StepResult<TOOLS, RUNTIME_CONTEXT>>;
    /**
     * Additional request information from the last step.
     *
     * Automatically consumes the stream.
     *
     * @deprecated Use `finalStep.request` instead.
     */
    readonly request: PromiseLike<LanguageModelRequestMetadata>;
    /**
     * Additional response information from the last step.
     *
     * Automatically consumes the stream.
     *
     * @deprecated Use `finalStep.response` instead.
     */
    readonly response: PromiseLike<LanguageModelResponseMetadata>;
    /**
     * The accumulated response messages of all steps that were generated during the call.
     *
     * Automatically consumes the stream.
     */
    readonly responseMessages: PromiseLike<Array<ResponseMessage>>;
    /**
     * Additional provider-specific metadata from the last step.
     * Metadata is passed through from the provider to the AI SDK and
     * enables provider-specific results that can be fully encapsulated in the provider.
     *
     * @deprecated Use `finalStep.providerMetadata` instead.
     */
    readonly providerMetadata: PromiseLike<ProviderMetadata | undefined>;
    /**
     * A text stream that returns only the generated text deltas. You can use it
     * as either an AsyncIterable or a ReadableStream. Error parts are not
     * surfaced in this stream. Use the `onError` callback or `stream` to observe
     * them.
     */
    readonly textStream: AsyncIterableStream<string>;
    /**
     * A stream with all events, including text deltas, tool calls, tool results, and
     * errors.
     * You can use it as either an AsyncIterable or a ReadableStream.
     * Only errors that stop the stream, such as network errors, are thrown.
     */
    readonly stream: AsyncIterableStream<TextStreamPart<TOOLS>>;
    /**
     * A stream with all events, including text deltas, tool calls, tool results, and
     * errors.
     * You can use it as either an AsyncIterable or a ReadableStream.
     * Only errors that stop the stream, such as network errors, are thrown.
     *
     * @deprecated Use `stream` instead.
     */
    readonly fullStream: AsyncIterableStream<TextStreamPart<TOOLS>>;
    /**
     * A stream of partial outputs. It uses the `output` specification.
     *
     * @deprecated Use `partialOutputStream` instead.
     */
    readonly experimental_partialOutputStream: AsyncIterableStream<InferStreamOutput<OUTPUT>>;
    /**
     * A stream of partial parsed outputs. It uses the `output` specification.
     */
    readonly partialOutputStream: AsyncIterableStream<InferStreamOutput<OUTPUT>>;
    /**
     * A stream of individual array elements as they complete.
     * Only available when using `output: Output.array()`.
     */
    readonly elementStream: AsyncIterableStream<InferElementOutput<OUTPUT>>;
    /**
     * The complete parsed output. It uses the `output` specification.
     */
    readonly output: PromiseLike<InferGenerateOutput<OUTPUT>>;
    /**
     * Consumes the stream without processing the parts.
     * This is useful to force the stream to finish.
     * It effectively removes the backpressure and allows the stream to finish,
     * triggering the `onEnd` callback and the promise resolution.
     *
     * If an error occurs, it is passed to the optional `onError` callback.
     */
    consumeStream(options?: ConsumeStreamOptions): PromiseLike<void>;
    /**
     * Converts the result to a UI message stream.
     *
     * @returns A UI message stream.
     *
     * @deprecated Use the standalone `toUIMessageStream` helper from
     *   `'ai'` with `result.stream` instead. This method will be removed
     *   in the next major release.
     */
    toUIMessageStream<UI_MESSAGE extends UIMessage>(options?: UIMessageStreamOptions<UI_MESSAGE>): AsyncIterableStream<InferUIMessageChunk<UI_MESSAGE>>;
    /**
     * Writes UI message stream output to a Node.js response-like object.
     *
     * @deprecated Use the standalone `toUIMessageStream` and
     *   `pipeUIMessageStreamToResponse` helpers from `'ai'` with `result.stream`
     *   instead. This method will be removed in the next major release.
     */
    pipeUIMessageStreamToResponse<UI_MESSAGE extends UIMessage>(response: ServerResponse, options?: UIMessageStreamResponseInit & UIMessageStreamOptions<UI_MESSAGE>): Promise<void>;
    /**
     * Writes text delta output to a Node.js response-like object.
     * It sets a `Content-Type` header to `text/plain; charset=utf-8` and
     * writes each text delta as a separate chunk.
     *
     * @param response A Node.js response-like object (ServerResponse).
     * @param init Optional headers, status code, and status text.
     *
     * @deprecated Use the standalone `toTextStream` and
     *   `pipeTextStreamToResponse` helpers from `'ai'` with `result.stream`
     *   instead. This method will be removed in the next major release.
     */
    pipeTextStreamToResponse(response: ServerResponse, init?: ResponseInit): Promise<void>;
    /**
     * Converts the result to a streamed response object with a stream data part stream.
     *
     * @returns A response object.
     *
     * @deprecated Use the standalone `toUIMessageStream` and
     *   `createUIMessageStreamResponse` helpers from `'ai'` with `result.stream`
     *   instead. This method will be removed in the next major release.
     */
    toUIMessageStreamResponse<UI_MESSAGE extends UIMessage>(options?: UIMessageStreamResponseInit & UIMessageStreamOptions<UI_MESSAGE>): Response;
    /**
     * Creates a simple text stream response.
     * Each text delta is encoded as UTF-8 and sent as a separate chunk.
     * Non-text-delta events are ignored.
     * @param init Optional headers, status code, and status text.
     *
     * @deprecated Use the standalone `toTextStream` and `createTextStreamResponse`
     *   helpers from `'ai'` with `result.stream` instead. This method will be
     *   removed in the next major release.
     */
    toTextStreamResponse(init?: ResponseInit): Response;
}

/**
 * A transformation that is applied to the stream.
 *
 * @param stopStream - A function that stops the source stream.
 * @param tools - The tools that are accessible to and can be called by the model. The model needs to support calling tools.
 */
export declare type StreamTextTransform<TOOLS extends ToolSet> = (options: {
    tools: TOOLS;
    stopStream: () => void;
}) => TransformStream<TextStreamPart<TOOLS>, TextStreamPart<TOOLS>>;

export declare interface StreamTranscriptionResult {
    /**
     * The final transcribed text.
     */
    readonly text: PromiseLike<string>;
    /**
     * Final transcript segments with timing information, if available.
     */
    readonly segments: PromiseLike<Array<{
        text: string;
        startSecond: number;
        endSecond: number;
    }>>;
    /**
     * The language of the transcript, if available.
     */
    readonly language: PromiseLike<string | undefined>;
    /**
     * The duration of the transcript in seconds, if available.
     */
    readonly durationInSeconds: PromiseLike<number | undefined>;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    readonly warnings: PromiseLike<Array<Warning>>;
    /**
     * Response metadata.
     */
    readonly responses: PromiseLike<Array<TranscriptionModelResponseMetadata>>;
    /**
     * Additional provider-specific metadata.
     */
    readonly providerMetadata: PromiseLike<Record<string, JSONObject_2>>;
    /**
     * Full stream of transcription parts.
     *
     * This is a single-consumer live stream and can only be accessed once.
     * Access it before any result promise when both stream parts and final
     * results are needed; accessing a result promise first consumes the stream
     * internally and makes `fullStream` unavailable.
     */
    readonly fullStream: AsyncIterableStream<TranscriptionStreamPart>;
}

/**
 * Strips file extension segments from a filename.
 *
 * Examples:
 * - "report.pdf" -> "report"
 * - "archive.tar.gz" -> "archive"
 * - "filename" -> "filename"
 */
declare function stripFileExtension(filename: string): string;

/**
 * Strips file extension segments from a filename.
 *
 * Examples:
 * - "report.pdf" -> "report"
 * - "archive.tar.gz" -> "archive"
 * - "filename" -> "filename"
 */
declare function stripFileExtension_2(filename: string): string;

declare const symbol$1: unique symbol;

declare const symbol$1_2: unique symbol;

declare const symbol$1_3: unique symbol;

declare const symbol$1_4: unique symbol;

declare const symbol$1_5: unique symbol;

declare const symbol$2: unique symbol;

declare const symbol$2_2: unique symbol;

declare const symbol$3: unique symbol;

declare const symbol$3_2: unique symbol;

declare const symbol$4: unique symbol;

declare const symbol$4_2: unique symbol;

declare const symbol$5: unique symbol;

declare const symbol$5_2: unique symbol;

declare const symbol$6: unique symbol;

declare const symbol$6_2: unique symbol;

declare const symbol$7: unique symbol;

declare const symbol$7_2: unique symbol;

declare const symbol$8: unique symbol;

declare const symbol$8_2: unique symbol;

declare const symbol$8_3: unique symbol;

declare const symbol$9: unique symbol;

declare const symbol$9_2: unique symbol;

declare const symbol$a: unique symbol;

declare const symbol$a_2: unique symbol;

declare const symbol$b: unique symbol;

declare const symbol$c: unique symbol;

declare const symbol$c_2: unique symbol;

declare const symbol$d: unique symbol;

declare const symbol$d_2: unique symbol;

declare const symbol$d_3: unique symbol;

declare const symbol$e: unique symbol;

declare const symbol$e_2: unique symbol;

declare const symbol$e_3: unique symbol;

declare const symbol$f: unique symbol;

declare const symbol$g: unique symbol;

declare const symbol$h: unique symbol;

declare const symbol$i: unique symbol;

declare const symbol$j: unique symbol;

declare const symbol$k: unique symbol;

declare const symbol$l: unique symbol;

declare const symbol: unique symbol;

declare const symbol_2: unique symbol;

declare const symbol_3: unique symbol;

declare const symbol_4: unique symbol;

/**
 * A system message. It can contain system information.
 *
 * Note: using the "system" part of the prompt is strongly preferred
 * to increase the resilience against prompt injection attacks,
 * and because not all providers support several system messages.
 */
export declare type SystemModelMessage = {
    role: 'system';
    content: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
};

/**
 * A system message. It can contain system information.
 *
 * Note: using the "system" part of the prompt is strongly preferred
 * to increase the resilience against prompt injection attacks,
 * and because not all providers support several system messages.
 */
declare type SystemModelMessage_2 = {
    role: 'system';
    content: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
};

export declare const systemModelMessageSchema: z.ZodType<SystemModelMessage>;

/**
 * Implement this interface to create custom telemetry integrations.
 * Methods can be sync or return a PromiseLike.
 */
export declare interface Telemetry {
    /**
     * Called when an operation begins. Fired for text generation
     * (generateText/streamText), object generation (generateObject/streamObject),
     * embedding (embed/embedMany), and reranking operations.
     *
     * Use the `operationId` field to distinguish between operation types.
     */
    onStart?: Callback<InferTelemetryEvent<OperationStartEvent>>;
    /**
     * Called when an individual step (single LLM invocation) begins.
     * A generation may consist of multiple steps (e.g. when tool calls trigger
     * follow-up LLM calls). Use this to create per-step spans or record
     * step-level inputs.
     *
     * The event includes the step number, accumulated previous step results,
     * and the messages that will be sent to the model.
     */
    onStepStart?: Callback<InferTelemetryEvent<GenerateTextStepStartEvent>>;
    /**
     * Called immediately before the provider model call begins.
     * Unlike `onStepStart`, this callback is scoped to model work only and
     * excludes any later client-side tool execution.
     */
    onLanguageModelCallStart?: Callback<InferTelemetryEvent<LanguageModelCallStartEvent>>;
    /**
     * Called after the model response has been normalized and parsed, but before
     * any client-side tool execution begins.
     */
    onLanguageModelCallEnd?: Callback<InferTelemetryEvent<LanguageModelCallEndEvent>>;
    /**
     * Called when a tool execution begins, before the tool's `execute` function
     * is invoked. Use this to create tool-level spans or log tool invocations.
     */
    onToolExecutionStart?: Callback<InferTelemetryEvent<ToolExecutionStartEvent>>;
    /**
     * Called when a tool execution completes, either successfully or with an error.
     * The event uses a discriminated union on the `success` field — check
     * `event.success` to determine whether `output` or `error` is available.
     *
     * The event includes execution time (`toolExecutionMs`) for performance tracking.
     */
    onToolExecutionEnd?: Callback<InferTelemetryEvent<ToolExecutionEndEvent>>;
    /**
     * Called when an individual step (single LLM invocation) completes.
     * The event is a `StepResult` containing the model's response, tool calls
     * and results, usage statistics, finish reason, and optional request/response
     * bodies.
     */
    onStepEnd?: Callback<InferTelemetryEvent<GenerateTextStepEndEvent>>;
    /**
     * Called when an individual step (single LLM invocation) completes.
     *
     * @deprecated Use `onStepEnd` instead.
     */
    onStepFinish?: Callback<InferTelemetryEvent<GenerateTextStepEndEvent>>;
    /**
     * Called when an object generation step (single LLM invocation) begins.
     * For generateObject/streamObject there is always exactly one step.
     *
     * @deprecated
     */
    onObjectStepStart?: Callback<InferTelemetryEvent<GenerateObjectStepStartEvent>>;
    /**
     * Called when an object generation step (single LLM invocation) completes,
     * with the raw result before JSON parsing and schema validation.
     *
     * @deprecated
     */
    onObjectStepEnd?: Callback<InferTelemetryEvent<GenerateObjectStepEndEvent>>;
    /**
     * Called when an individual embedding model call (doEmbed) begins.
     * For `embed`, there is one call. For `embedMany`, there may be multiple
     * calls when values are chunked.
     */
    onEmbedStart?: Callback<InferTelemetryEvent<EmbeddingModelCallStartEvent>>;
    /**
     * Called when an individual embedding model call (doEmbed) completes.
     * Contains the embeddings, usage, and any warnings from the model response.
     */
    onEmbedEnd?: Callback<InferTelemetryEvent<EmbeddingModelCallEndEvent>>;
    /**
     * Called when an individual reranking model call (doRerank) begins.
     * There is one call per `rerank` invocation.
     */
    onRerankStart?: Callback<InferTelemetryEvent<RerankingModelCallStartEvent>>;
    /**
     * Called when an individual reranking model call (doRerank) completes.
     * Contains the ranking results from the model response.
     */
    onRerankEnd?: Callback<InferTelemetryEvent<RerankingModelCallEndEvent>>;
    /**
     * Called when an operation completes. Fired for text generation
     * (generateText/streamText), object generation (generateObject/streamObject),
     * embedding (embed/embedMany), and reranking operations.
     *
     * Use the event shape or `operationId` to distinguish between operation types.
     */
    onEnd?: Callback<InferTelemetryEvent<OperationEndEvent>>;
    /**
     * Called when a streaming text generation operation is aborted before it
     * completes.
     */
    onAbort?: Callback<InferTelemetryEvent<GenerateTextAbortEvent<ToolSet>>>;
    /**
     * Called when an unrecoverable error occurs during the generation lifecycle.
     * The error value is untyped — it may be an `Error` instance, an `AISDKError`,
     * or any thrown value.
     *
     * Use this to record error details on telemetry spans and set error status.
     */
    onError?: Callback<unknown>;
    /**
     * Optionally runs the language model call in a telemetry-integration-specific context. This enables
     * auto-instrumented model provider requests to become children of the current
     * model-call span.
     *
     * The options carry the model-call start-event content as context (the event
     * fields are optional), alongside the always-present `callId` and the
     * `execute` function that performs the model call.
     */
    executeLanguageModelCall?: <T>(options: Partial<InferTelemetryEvent<LanguageModelCallStartEvent>> & {
        callId: string;
        execute: () => PromiseLike<T>;
    }) => PromiseLike<T>;
    /**
     * Optionally runs the tool execute function in a telemetry-integration-specific context. This enables
     * nested traces — e.g. when a tool's `execute` function calls `generateText`,
     * the inner call's spans become children of the tool span.
     *
     * The options carry the tool-execution start-event content as context (the
     * event fields are optional), alongside the always-present `callId`,
     * `toolCallId`, and the `execute` function to run.
     */
    executeTool?: <T>(options: Partial<InferTelemetryEvent<ToolExecutionStartEvent>> & {
        callId: string;
        toolCallId: string;
        execute: () => PromiseLike<T>;
    }) => PromiseLike<T>;
}

/**
 * Telemetry configuration.
 */
export declare type TelemetryOptions<RUNTIME_CONTEXT extends Context_2 = Context_2, TOOLS extends ToolSet = ToolSet> = {
    /**
     * Enable or disable telemetry. Enabled by default when a telemetry
     * integration is registered. Set to `false` to opt out.
     */
    isEnabled?: boolean;
    /**
     * Enable or disable input recording. Enabled by default.
     *
     * You might want to disable input recording to avoid recording sensitive
     * information, to reduce data transfers, or to increase performance.
     */
    recordInputs?: boolean;
    /**
     * Enable or disable output recording. Enabled by default.
     *
     * You might want to disable output recording to avoid recording sensitive
     * information, to reduce data transfers, or to increase performance.
     */
    recordOutputs?: boolean;
    /**
     * Identifier for this function. Used to group telemetry data by function.
     */
    functionId?: string;
    /**
     * Top-level runtime context properties that should be included in telemetry.
     * Runtime context properties are excluded unless they are explicitly set to `true`.
     */
    includeRuntimeContext?: IncludedContext<RUNTIME_CONTEXT>;
    /**
     * Top-level tool context properties that should be included in telemetry,
     * configured per tool.
     *
     * Tool context properties are excluded unless they are explicitly set to `true`.
     */
    includeToolsContext?: IncludedToolsContext<TOOLS>;
    /**
     * Per-call telemetry integrations that receive lifecycle events during generation.
     *
     * When provided, these integrations will take precedence over the globally registered
     * integrations for this call.
     */
    integrations?: Arrayable_2<Telemetry>;
};

export declare type TelemetryTracingChannelMessage<EVENT = unknown> = {
    readonly type: TelemetryTracingEventType;
    readonly event: EVENT;
};

export declare type TelemetryTracingEventType = 'generateText' | 'streamText' | 'step' | 'languageModelCall' | 'executeTool' | 'embed' | 'embedMany' | 'rerank';

/**
 * Text content part of a prompt. It contains a string of text.
 */
export declare interface TextPart {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
}

/**
 * Text content part of a prompt. It contains a string of text.
 */
declare interface TextPart_2 {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
}

declare type TextStreamAbortPart = {
    type: 'abort';
    reason?: string;
};

export declare class TextStreamChatTransport<UI_MESSAGE extends UIMessage> extends HttpChatTransport<UI_MESSAGE> {
    constructor(options?: HttpChatTransportInitOptions<UI_MESSAGE>);
    protected processResponseStream(stream: ReadableStream<Uint8Array<ArrayBufferLike>>): ReadableStream<UIMessageChunk>;
}

declare type TextStreamCustomPart = {
    type: 'custom';
    kind: `${string}.${string}`;
    providerMetadata?: ProviderMetadata;
};

declare type TextStreamErrorPart = {
    type: 'error';
    error: unknown;
};

declare type TextStreamFilePart = {
    type: 'file';
    file: GeneratedFile;
    providerMetadata?: ProviderMetadata;
};

declare type TextStreamFinishPart = {
    type: 'finish';
    finishReason: FinishReason;
    rawFinishReason: string | undefined;
    totalUsage: LanguageModelUsage;
};

declare type TextStreamFinishStepPart = {
    type: 'finish-step';
    response: Omit<LanguageModelResponseMetadata, 'messages' | 'body'>;
    usage: LanguageModelUsage;
    performance: StepResultPerformance;
    finishReason: FinishReason;
    rawFinishReason: string | undefined;
    providerMetadata: ProviderMetadata | undefined;
};

export declare type TextStreamPart<TOOLS extends ToolSet> = TextStreamTextStartPart | TextStreamTextEndPart | TextStreamTextDeltaPart | TextStreamReasoningStartPart | TextStreamReasoningEndPart | TextStreamReasoningDeltaPart | TextStreamCustomPart | TextStreamToolInputStartPart | TextStreamToolInputEndPart | TextStreamToolInputDeltaPart | TextStreamSourcePart | TextStreamFilePart | TextStreamReasoningFilePart | TextStreamToolCallPart<TOOLS> | TextStreamToolResultPart<TOOLS> | TextStreamToolErrorPart<TOOLS> | TextStreamToolOutputDeniedPart<TOOLS> | TextStreamToolApprovalRequestPart<TOOLS> | TextStreamToolApprovalResponsePart<TOOLS> | TextStreamStartStepPart | TextStreamFinishStepPart | TextStreamStartPart | TextStreamFinishPart | TextStreamAbortPart | TextStreamErrorPart | TextStreamRawPart;

declare type TextStreamRawPart = {
    type: 'raw';
    rawValue: unknown;
};

declare type TextStreamReasoningDeltaPart = {
    type: 'reasoning-delta';
    providerMetadata?: ProviderMetadata;
    id: string;
    text: string;
};

declare type TextStreamReasoningEndPart = {
    type: 'reasoning-end';
    id: string;
    providerMetadata?: ProviderMetadata;
};

declare type TextStreamReasoningFilePart = {
    type: 'reasoning-file';
    file: GeneratedFile;
    providerMetadata?: ProviderMetadata;
};

declare type TextStreamReasoningStartPart = {
    type: 'reasoning-start';
    id: string;
    providerMetadata?: ProviderMetadata;
};

declare type TextStreamSourcePart = {
    type: 'source';
} & Source;

declare type TextStreamStartPart = {
    type: 'start';
};

declare type TextStreamStartStepPart = {
    type: 'start-step';
    request: LanguageModelRequestMetadata;
    warnings: CallWarning[];
};

declare type TextStreamTextDeltaPart = {
    type: 'text-delta';
    id: string;
    providerMetadata?: ProviderMetadata;
    text: string;
};

declare type TextStreamTextEndPart = {
    type: 'text-end';
    id: string;
    providerMetadata?: ProviderMetadata;
};

declare type TextStreamTextStartPart = {
    type: 'text-start';
    id: string;
    providerMetadata?: ProviderMetadata;
};

declare type TextStreamToolApprovalRequestPart<TOOLS extends ToolSet> = ToolApprovalRequestOutput<TOOLS>;

declare type TextStreamToolApprovalResponsePart<TOOLS extends ToolSet> = ToolApprovalResponseOutput<TOOLS>;

declare type TextStreamToolCallPart<TOOLS extends ToolSet> = {
    type: 'tool-call';
} & TypedToolCall<TOOLS>;

declare type TextStreamToolErrorPart<TOOLS extends ToolSet> = {
    type: 'tool-error';
} & TypedToolError<TOOLS>;

declare type TextStreamToolInputDeltaPart = {
    type: 'tool-input-delta';
    id: string;
    delta: string;
    providerMetadata?: ProviderMetadata;
};

declare type TextStreamToolInputEndPart = {
    type: 'tool-input-end';
    id: string;
    providerMetadata?: ProviderMetadata;
};

declare type TextStreamToolInputStartPart = {
    type: 'tool-input-start';
    id: string;
    toolName: string;
    providerMetadata?: ProviderMetadata;
    toolMetadata?: JSONObject_2;
    providerExecuted?: boolean;
    dynamic?: boolean;
    title?: string;
};

declare type TextStreamToolOutputDeniedPart<TOOLS extends ToolSet> = {
    type: 'tool-output-denied';
} & StaticToolOutputDenied<TOOLS>;

declare type TextStreamToolResultPart<TOOLS extends ToolSet> = {
    type: 'tool-result';
} & TypedToolResult<TOOLS>;

/**
 * A text part of a message.
 */
export declare type TextUIPart = {
    type: 'text';
    /**
     * The text content.
     */
    text: string;
    /**
     * The state of the text part.
     */
    state?: 'streaming' | 'done';
    /**
     * The provider metadata.
     */
    providerMetadata?: ProviderMetadata;
};

/**
 * Timeout configuration for API calls. Can be specified as:
 * - A number representing milliseconds
 * - An object with `totalMs` property for the total timeout in milliseconds
 * - An object with `stepMs` property for the timeout of each step in milliseconds
 * - An object with `firstChunkMs` property for the timeout until the first content chunk of each step (streaming only)
 * - An object with `chunkMs` property for the timeout between content chunks (streaming only)
 * - An object with `toolMs` property for the default timeout for all tool executions
 * - An object with `tools` property for per-tool timeout overrides using `{toolName}Ms` keys
 */
export declare type TimeoutConfiguration<TOOLS extends ToolSet> = number | {
    totalMs?: number;
    stepMs?: number;
    firstChunkMs?: number;
    chunkMs?: number;
    toolMs?: number;
    tools?: Partial<Record<`${keyof TOOLS & string}Ms`, number>>;
};

/**
 * A tool can either be user-defined or provider-defined.
 *
 * It contains the schemas and metadata needed for the language model to call
 * the tool and can include an execute function for tools that are executed by
 * the AI SDK.
 */
export declare type Tool<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context_2 | unknown | never = any> = FunctionTool_2<INPUT, OUTPUT, CONTEXT> | DynamicTool_2<INPUT, OUTPUT, CONTEXT> | ProviderDefinedTool_2<INPUT, OUTPUT, CONTEXT> | ProviderExecutedTool_2<INPUT, OUTPUT, CONTEXT>;

/**
 * Infer the tool type from a tool object.
 *
 * This is useful for type inference when working with tool objects.
 *
 * When the input has an `execute` function, the return type narrows to
 * `ExecutableTool<Tool<...>>` so that `.execute` is non-nullable without
 * needing `isExecutableTool` or a `!` assertion at the call site.
 */
export declare function tool<INPUT, OUTPUT, CONTEXT extends Context_2>(tool: Tool<INPUT, OUTPUT, CONTEXT> & {
    execute: ToolExecuteFunction<INPUT, OUTPUT, CONTEXT>;
}): ExecutableTool_2<Tool<INPUT, OUTPUT, CONTEXT>>;

export declare function tool<INPUT, OUTPUT, CONTEXT extends Context_2>(tool: Tool<INPUT, OUTPUT, CONTEXT>): Tool<INPUT, OUTPUT, CONTEXT>;

export declare function tool<INPUT, CONTEXT extends Context_2>(tool: Tool<INPUT, never, CONTEXT>): Tool<INPUT, never, CONTEXT>;

export declare function tool<OUTPUT, CONTEXT extends Context_2>(tool: Tool<never, OUTPUT, CONTEXT>): Tool<never, OUTPUT, CONTEXT>;

export declare function tool<CONTEXT extends Context_2>(tool: Tool<never, never, CONTEXT>): Tool<never, never, CONTEXT>;

/**
 * A tool can either be user-defined or provider-defined.
 *
 * It contains the schemas and metadata needed for the language model to call
 * the tool and can include an execute function for tools that are executed by
 * the AI SDK.
 */
declare type Tool_2<INPUT extends JSONValue_2 | unknown | never = any, OUTPUT extends JSONValue_2 | unknown | never = any, CONTEXT extends Context | unknown | never = any> = FunctionTool<INPUT, OUTPUT, CONTEXT> | DynamicTool<INPUT, OUTPUT, CONTEXT> | ProviderDefinedTool<INPUT, OUTPUT, CONTEXT> | ProviderExecutedTool<INPUT, OUTPUT, CONTEXT>;

/**
 * Infer the tool type from a tool object.
 *
 * This is useful for type inference when working with tool objects.
 *
 * When the input has an `execute` function, the return type narrows to
 * `ExecutableTool<Tool<...>>` so that `.execute` is non-nullable without
 * needing `isExecutableTool` or a `!` assertion at the call site.
 */
declare function tool_2<INPUT, OUTPUT, CONTEXT extends Context>(tool: Tool_2<INPUT, OUTPUT, CONTEXT> & {
    execute: ToolExecuteFunction_2<INPUT, OUTPUT, CONTEXT>;
}): ExecutableTool<Tool_2<INPUT, OUTPUT, CONTEXT>>;

declare function tool_2<INPUT, OUTPUT, CONTEXT extends Context>(tool: Tool_2<INPUT, OUTPUT, CONTEXT>): Tool_2<INPUT, OUTPUT, CONTEXT>;

declare function tool_2<INPUT, CONTEXT extends Context>(tool: Tool_2<INPUT, never, CONTEXT>): Tool_2<INPUT, never, CONTEXT>;

declare function tool_2<OUTPUT, CONTEXT extends Context>(tool: Tool_2<never, OUTPUT, CONTEXT>): Tool_2<never, OUTPUT, CONTEXT>;

declare function tool_2<CONTEXT extends Context>(tool: Tool_2<never, never, CONTEXT>): Tool_2<never, never, CONTEXT>;

/**
 * Configure whether individual tools require approval before they can run.
 *
 * You can either use a generic function that is called for all tool calls,
 * or you can use a per-tool function.
 *
 * For the per-tool functions, each tool can be assigned either an approval status
 * or a function that produces an approval status at runtime.
 *
 * The approval status can be one of the following:
 * - 'not-applicable': The tool does not require approval.
 * - 'approved': The tool is automatically approved.
 * - 'denied': The tool is automatically denied.
 * - 'user-approval': The tool requires user approval.
 *
 * In addition to the string statuses, you can also use object statuses with a reason property.
 */
export declare type ToolApprovalConfiguration<TOOLS extends ToolSet, RUNTIME_CONTEXT extends Context_2 | unknown | never> = GenericToolApprovalFunction<TOOLS, InferToolSetContext_2<TOOLS>, RUNTIME_CONTEXT> | {
    [key in keyof TOOLS]?: ToolApprovalStatus | SingleToolApprovalFunction<InferToolInput<TOOLS[key]>, InferToolContext_2<TOOLS[key]>, RUNTIME_CONTEXT>;
};

/**
 * Tool approval request prompt part.
 */
export declare type ToolApprovalRequest = {
    type: 'tool-approval-request';
    /**
     * ID of the tool approval.
     */
    approvalId: string;
    /**
     * ID of the tool call that the approval request is for.
     */
    toolCallId: string;
    /**
     * Flag indicating whether the tool was automatically approved or denied.
     *
     * @default false
     */
    isAutomatic?: boolean;
    /**
     * HMAC-SHA256 signature binding this approval to its tool call.
     * Present only when `experimental_toolApprovalSecret` is configured.
     */
    signature?: string;
};

/**
 * Tool approval request prompt part.
 */
declare type ToolApprovalRequest_2 = {
    type: 'tool-approval-request';
    /**
     * ID of the tool approval.
     */
    approvalId: string;
    /**
     * ID of the tool call that the approval request is for.
     */
    toolCallId: string;
    /**
     * Flag indicating whether the tool was automatically approved or denied.
     *
     * @default false
     */
    isAutomatic?: boolean;
    /**
     * HMAC-SHA256 signature binding this approval to its tool call.
     * Present only when `experimental_toolApprovalSecret` is configured.
     */
    signature?: string;
};

/**
 * Output part that indicates that a tool approval request has been made.
 *
 * The tool approval request can be approved or denied in the next tool message.
 */
export declare type ToolApprovalRequestOutput<TOOLS extends ToolSet> = {
    type: 'tool-approval-request';
    /**
     * ID of the tool approval request.
     */
    approvalId: string;
    /**
     * Tool call that the approval request is for.
     */
    toolCall: TypedToolCall<TOOLS>;
    /**
     * Flag indicating whether the tool was automatically approved or denied.
     *
     * @default false
     */
    isAutomatic?: boolean;
    /**
     * HMAC-SHA256 signature binding this approval request to its tool call.
     */
    signature?: string;
};

/**
 * Tool approval response prompt part.
 */
export declare type ToolApprovalResponse = {
    type: 'tool-approval-response';
    /**
     * ID of the tool approval.
     */
    approvalId: string;
    /**
     * Flag indicating whether the approval was granted or denied.
     */
    approved: boolean;
    /**
     * Optional reason for the approval or denial.
     */
    reason?: string;
    /**
     * Flag indicating whether the tool call is provider-executed.
     * Only provider-executed tool approval responses should be sent to the model.
     */
    providerExecuted?: boolean;
};

/**
 * Tool approval response prompt part.
 */
declare type ToolApprovalResponse_2 = {
    type: 'tool-approval-response';
    /**
     * ID of the tool approval.
     */
    approvalId: string;
    /**
     * Flag indicating whether the approval was granted or denied.
     */
    approved: boolean;
    /**
     * Optional reason for the approval or denial.
     */
    reason?: string;
    /**
     * Flag indicating whether the tool call is provider-executed.
     * Only provider-executed tool approval responses should be sent to the model.
     */
    providerExecuted?: boolean;
};

/**
 * Output part that indicates that a tool approval response is available.
 */
export declare type ToolApprovalResponseOutput<TOOLS extends ToolSet> = {
    type: 'tool-approval-response';
    /**
     * ID of the tool approval.
     */
    approvalId: string;
    /**
     * Tool call that the approval response is for.
     */
    toolCall: TypedToolCall<TOOLS>;
    /**
     * Flag indicating whether the approval was granted or denied.
     */
    approved: boolean;
    /**
     * Optional reason for the approval or denial.
     */
    reason?: string;
    /**
     * Flag indicating whether the tool call is provider-executed.
     * Only provider-executed tool approval responses should be sent to the model.
     */
    providerExecuted?: boolean;
};

/**
 * The approval status of a tool configuration. This can be one of the following:
 *
 * - 'not-applicable': The tool does not require approval.
 * - 'approved': The tool is automatically approved.
 * - 'denied': The tool is automatically denied.
 * - 'user-approval': The tool requires user approval.
 *
 * In addition to the string statuses, you can also use object statuses with a reason property.
 *
 * `undefined` is treated as the `not-applicable` status.
 */
export declare type ToolApprovalStatus = undefined | 'not-applicable' | 'approved' | 'denied' | 'user-approval' | {
    type: 'not-applicable';
    reason?: never;
} | {
    type: 'approved';
    reason?: string;
} | {
    type: 'denied';
    reason?: string;
} | {
    type: 'user-approval';
    reason?: never;
};

/**
 * Typed tool call that is returned by generateText and streamText.
 * It contains the tool call ID, the tool name, and the tool arguments.
 */
declare interface ToolCall<NAME extends string, INPUT> {
    /**
     * ID of the tool call. This ID is used to match the tool call with the tool result.
     */
    toolCallId: string;
    /**
     * Name of the tool that is being called.
     */
    toolName: NAME;
    /**
     * Arguments of the tool call. This is a JSON-serializable object that matches the tool's input schema.
     */
    input: INPUT;
    /**
     * Whether the tool call will be executed by the provider.
     * If this flag is not set or is false, the tool call will be executed by the client.
     */
    providerExecuted?: boolean;
    /**
     * Whether the tool is dynamic.
     */
    dynamic?: boolean;
}

/**
 * Typed tool call that is returned by generateText and streamText.
 * It contains the tool call ID, the tool name, and the tool arguments.
 */
declare interface ToolCall_2<NAME extends string, INPUT> {
    /**
     * ID of the tool call. This ID is used to match the tool call with the tool result.
     */
    toolCallId: string;
    /**
     * Name of the tool that is being called.
     */
    toolName: NAME;
    /**
     * Arguments of the tool call. This is a JSON-serializable object that matches the tool's input schema.
     */
    input: INPUT;
    /**
     * Whether the tool call will be executed by the provider.
     * If this flag is not set or is false, the tool call will be executed by the client.
     */
    providerExecuted?: boolean;
    /**
     * Whether the tool is dynamic.
     */
    dynamic?: boolean;
}

declare type ToolCallerDefinition = {
    type: 'local';
    bind: (tools: ToolSet) => Tool;
} | {
    type: 'provider';
    prepareProviderOptions: (providerOptions: ProviderOptions_2 | undefined) => ProviderOptions_2;
};

declare type ToolCallerName<TOOLS extends ToolSet> = {
    [NAME in keyof TOOLS]: TOOLS[NAME] extends Experimental_ToolCallerTool ? NAME : never;
}[keyof TOOLS] & string;

declare const toolCallerSymbol: unique symbol;

export declare class ToolCallNotFoundForApprovalError extends AISDKError {
    private readonly [symbol$f];
    readonly toolCallId: string;
    readonly approvalId: string;
    constructor({ toolCallId, approvalId, }: {
        toolCallId: string;
        approvalId: string;
    });
    static isInstance(error: unknown): error is ToolCallNotFoundForApprovalError;
}

/**
 * Tool call content part of a prompt. It contains a tool call (usually generated by the AI model).
 */
export declare interface ToolCallPart {
    type: 'tool-call';
    /**
     * ID of the tool call. This ID is used to match the tool call with the tool result.
     */
    toolCallId: string;
    /**
     * Name of the tool that is being called.
     */
    toolName: string;
    /**
     * Arguments of the tool call. This is a JSON-serializable object that matches the tool's input schema.
     */
    input: unknown;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Whether the tool call was executed by the provider.
     */
    providerExecuted?: boolean;
}

/**
 * Tool call content part of a prompt. It contains a tool call (usually generated by the AI model).
 */
declare interface ToolCallPart_2 {
    type: 'tool-call';
    /**
     * ID of the tool call. This ID is used to match the tool call with the tool result.
     */
    toolCallId: string;
    /**
     * Name of the tool that is being called.
     */
    toolName: string;
    /**
     * Arguments of the tool call. This is a JSON-serializable object that matches the tool's input schema.
     */
    input: unknown;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
    /**
     * Whether the tool call was executed by the provider.
     */
    providerExecuted?: boolean;
}

export declare class ToolCallRepairError extends AISDKError {
    private readonly [symbol$6_2];
    readonly originalError: NoSuchToolError | InvalidToolInputError;
    constructor({ cause, originalError, message, }: {
        message?: string;
        cause: unknown;
        originalError: NoSuchToolError | InvalidToolInputError;
    });
    static isInstance(error: unknown): error is ToolCallRepairError;
}

/**
 * A function that attempts to repair a tool call that failed to parse.
 *
 * It receives the error and the context as arguments and returns the repair
 * tool call JSON as text.
 *
 * @param options.instructions - The instructions provided to the model.
 * @param options.system - The instructions provided to the model.
 * @param options.messages - The messages in the current generation step.
 * @param options.toolCall - The tool call that failed to parse.
 * @param options.tools - The tools that are available.
 * @param options.inputSchema - A function that returns the JSON Schema for a tool.
 * @param options.error - The error that occurred while parsing the tool call.
 */
export declare type ToolCallRepairFunction<TOOLS extends ToolSet> = (options: {
    instructions: Instructions | undefined;
    /**
     * @deprecated Use `instructions` instead.
     */
    system: Instructions | undefined;
    messages: ModelMessage[];
    toolCall: LanguageModelV4ToolCall_2;
    tools: TOOLS;
    inputSchema: (options: {
        toolName: string;
    }) => PromiseLike<JSONSchema7>;
    error: NoSuchToolError | InvalidToolInputError;
}) => Promise<LanguageModelV4ToolCall_2 | null>;

/**
 * Tool choice for the generation. It supports the following settings:
 *
 * - `auto` (default): the model can choose whether and which tools to call.
 * - `required`: the model must call a tool. It can choose which tool to call.
 * - `none`: the model must not call tools
 * - `{ type: 'tool', toolName: string (typed) }`: the model must call the specified tool
 */
export declare type ToolChoice<TOOLS extends Record<string, unknown>> = 'auto' | 'none' | 'required' | {
    type: 'tool';
    toolName: Extract<keyof TOOLS, string>;
};

/**
 * Content of a tool message. It is an array of tool result parts.
 */
export declare type ToolContent = Array<ToolResultPart | ToolApprovalResponse>;

/**
 * Content of a tool message. It is an array of tool result parts.
 */
declare type ToolContent_2 = Array<ToolResultPart_2 | ToolApprovalResponse_2>;

/**
 * Resolves a single tool's context type, falling back to `undefined` when the
 * tool does not declare a `contextSchema`.
 */
declare type ToolContextFor<TOOL extends ToolSet[keyof ToolSet]> = [
InferToolContext_2<TOOL>
] extends [never] ? undefined : InferToolContext_2<TOOL>;

/**
 * Function that executes the tool and returns either a single result or a stream of results.
 */
export declare type ToolExecuteFunction<INPUT, OUTPUT, CONTEXT extends Context_2 | unknown | never> = (input: INPUT, options: ToolExecutionOptions<CONTEXT>) => AsyncIterable<OUTPUT> | PromiseLike<OUTPUT> | OUTPUT;

/**
 * Function that executes the tool and returns either a single result or a stream of results.
 */
declare type ToolExecuteFunction_2<INPUT, OUTPUT, CONTEXT extends Context | unknown | never> = (input: INPUT, options: ToolExecutionOptions_2<CONTEXT>) => AsyncIterable<OUTPUT> | PromiseLike<OUTPUT> | OUTPUT;

/**
 * Event passed to the `onToolExecutionEnd` callback.
 *
 * Called when a tool execution completes, either successfully or with an error.
 * Uses the `toolOutput.type` discriminator to distinguish success and error.
 */
export declare type ToolExecutionEndEvent<TOOLS extends ToolSet = ToolSet> = [
ToolSet
] extends [TOOLS] ? WidenedToolExecutionEndEvent : StaticToolExecutionEndEvent<TOOLS> | DynamicToolExecutionEndEvent<TOOLS>;

/**
 * Additional options that are sent into each tool execution.
 */
export declare interface ToolExecutionOptions<CONTEXT extends Context_2 | unknown | never> {
    /**
     * The ID of the tool call. You can use it e.g. when sending tool-call related information with stream data.
     */
    toolCallId: string;
    /**
     * Messages that were sent to the language model to initiate the response that contained the tool call.
     * The messages **do not** include the system prompt nor the assistant response that contained the tool call.
     */
    messages: ModelMessage[];
    /**
     * An optional abort signal that indicates that the overall operation should be aborted.
     */
    abortSignal?: AbortSignal;
    /**
     * Tool context as defined by the tool's context schema.
     * The tool context is specific to the tool and is passed to the tool execution.
     *
     * Treat the context object as immutable inside tools.
     * Mutating the context object can lead to race conditions and unexpected results
     * when tools are called in parallel.
     *
     * If you need to mutate the context, analyze the tool calls and results
     * in `prepareStep` and update it there.
     */
    context: CONTEXT;
    /**
     * The sandbox environment that the tool is operating in.
     */
    experimental_sandbox?: Experimental_SandboxSession;
}

/**
 * Additional options that are sent into each tool execution.
 */
declare interface ToolExecutionOptions_2<CONTEXT extends Context | unknown | never> {
    /**
     * The ID of the tool call. You can use it e.g. when sending tool-call related information with stream data.
     */
    toolCallId: string;
    /**
     * Messages that were sent to the language model to initiate the response that contained the tool call.
     * The messages **do not** include the system prompt nor the assistant response that contained the tool call.
     */
    messages: ModelMessage_2[];
    /**
     * An optional abort signal that indicates that the overall operation should be aborted.
     */
    abortSignal?: AbortSignal;
    /**
     * Tool context as defined by the tool's context schema.
     * The tool context is specific to the tool and is passed to the tool execution.
     *
     * Treat the context object as immutable inside tools.
     * Mutating the context object can lead to race conditions and unexpected results
     * when tools are called in parallel.
     *
     * If you need to mutate the context, analyze the tool calls and results
     * in `prepareStep` and update it there.
     */
    context: CONTEXT;
    /**
     * The sandbox environment that the tool is operating in.
     */
    experimental_sandbox?: SandboxSession;
}

/**
 * Event passed to the `onToolExecutionStart` callback.
 *
 * Called when a tool execution begins, before the tool's `execute` function is invoked.
 */
export declare type ToolExecutionStartEvent<TOOLS extends ToolSet = ToolSet> = [
ToolSet
] extends [TOOLS] ? WidenedToolExecutionStartEvent : StaticToolExecutionStartEvent<TOOLS> | DynamicToolExecutionStartEvent;

/**
 * Mapping of tool names to functions that refine parsed tool inputs.
 *
 * Each refinement function receives the typed input for its tool and must return
 * an input with the same type shape. Refined inputs are used for tool execution,
 * output parts, lifecycle callbacks, and telemetry.
 */
export declare type ToolInputRefinement<TOOLS extends ToolSet> = {
    [NAME in keyof TOOLS]?: (input: InferToolInput<TOOLS[NAME]>) => MaybePromiseLike_2<InferToolInput<TOOLS[NAME]>>;
};

/**
 * A tool loop agent is an agent that runs tools in a loop. In each step,
 * it calls the LLM, and if there are tool calls, it executes the tools
 * and calls the LLM again in a new step with the tool results.
 *
 * The loop continues until:
 * - A finish reasoning other than tool-calls is returned, or
 * - A tool that is invoked does not have an execute function, or
 * - A tool call needs approval via `toolApproval` or tool-level `needsApproval`, or
 * - A stop condition is met (default stop condition is isStepCount(20))
 */
declare class ToolLoopAgent<CALL_OPTIONS = never, TOOLS extends ToolSet = {}, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = never> implements Agent<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT, OUTPUT> {
    readonly version = "agent-v1";
    private readonly settings;
    constructor(settings: ToolLoopAgentSettings<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT, OUTPUT>);
    /**
     * The id of the agent.
     */
    get id(): string | undefined;
    /**
     * The tools that the agent can use.
     */
    get tools(): TOOLS;
    private prepareCall;
    /**
     * Tags outgoing requests so usage can be attributed to ToolLoopAgent. Chains
     * with the `ai/<version>` and `ai-sdk/<provider>/<version>` suffixes added
     * downstream by generateText/streamText and the provider.
     */
    private agentHeaders;
    /**
     * Generates an output from the agent (non-streaming).
     */
    generate({ abortSignal, timeout, experimental_sandbox: sandbox, onStart, experimental_onStart, onStepStart, experimental_onStepStart, onToolExecutionStart, onToolExecutionEnd, onStepEnd, onStepFinish, onFinish, onEnd, ...options }: AgentCallParameters<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT>): Promise<GenerateTextResult<TOOLS, RUNTIME_CONTEXT, OUTPUT>>;
    /**
     * Streams an output from the agent (streaming).
     */
    stream({ abortSignal, timeout, experimental_sandbox: sandbox, experimental_transform, onStart, experimental_onStart, onStepStart, experimental_onStepStart, onToolExecutionStart, onToolExecutionEnd, onStepEnd, onStepFinish, onFinish, onEnd, ...options }: AgentStreamParameters<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT>): Promise<StreamTextResult<TOOLS, RUNTIME_CONTEXT, OUTPUT>>;
}
export { ToolLoopAgent as Experimental_Agent }
export { ToolLoopAgent }

/**
 * Configuration options for an agent.
 */
declare type ToolLoopAgentSettings<CALL_OPTIONS = never, TOOLS extends ToolSet = {}, RUNTIME_CONTEXT extends Context_2 = Context_2, OUTPUT extends Output_2 = never> = LanguageModelCallOptions & Omit<RequestOptions<TOOLS>, 'abortSignal'> & ToolsContextParameter<TOOLS> & {
    /**
     * The id of the agent.
     */
    id?: string;
    /**
     * The instructions for the agent.
     *
     * It can be a string, or, if you need to pass additional provider options (e.g. for caching), a `SystemModelMessage`.
     */
    instructions?: Instructions;
    /**
     * Whether system messages are allowed in the `prompt` or `messages` fields.
     *
     * When disabled, system messages must be provided through the `instructions`
     * option.
     *
     * @default false
     */
    allowSystemInMessages?: boolean;
    /**
     * The language model to use.
     */
    model: LanguageModel;
    /**
     * The tool choice strategy. Default: 'auto'.
     */
    toolChoice?: ToolChoice<NoInfer<TOOLS>>;
    /**
     * Condition for stopping the generation when there are tool results in the last step.
     * When the condition is an array, any of the conditions can be met to stop the generation.
     *
     * @default isStepCount(20)
     */
    stopWhen?: Arrayable_2<StopCondition<NoInfer<TOOLS>, RUNTIME_CONTEXT>>;
    /**
     * Optional telemetry configuration.
     */
    telemetry?: TelemetryOptions<RUNTIME_CONTEXT, NoInfer<TOOLS>>;
    /**
     * Optional telemetry configuration.
     *
     * @deprecated Use `telemetry` instead. This alias will be removed in a future major release.
     */
    experimental_telemetry?: TelemetryOptions<RUNTIME_CONTEXT, NoInfer<TOOLS>>;
    /**
     * Limits the tools that are available for the model to call without
     * changing the tool call and result types in the result.
     */
    activeTools?: ActiveTools<NoInfer<TOOLS>>;
    /**
     * Controls the order in which tools are sent to the provider.
     *
     * The list can be partial. Tools not listed in `toolOrder` are sent after
     * the listed tools, sorted alphabetically. This can improve provider-side
     * caching by keeping tool definitions in a stable order.
     */
    toolOrder?: ToolOrder<NoInfer<TOOLS>>;
    /**
     * Optional specification for generating structured outputs.
     */
    output?: OUTPUT;
    /**
     * Runtime context. Treat runtime context as immutable.
     * If you need to mutate runtime context, update it in `prepareStep`.
     */
    runtimeContext?: RUNTIME_CONTEXT;
    /**
     * Optional tool approval configuration.
     *
     * This configuration takes precedence over tool-defined approval settings.
     */
    toolApproval?: ToolApprovalConfiguration<NoInfer<TOOLS>, RUNTIME_CONTEXT>;
    /**
     * Configures which caller tools may invoke each tool.
     */
    experimental_toolCallers?: Experimental_ToolCallers<NoInfer<TOOLS>>;
    /**
     * Optional function that you can use to provide different settings for a step.
     */
    prepareStep?: PrepareStepFunction<NoInfer<TOOLS>, RUNTIME_CONTEXT>;
    /**
     * A function that attempts to repair a tool call that failed to parse.
     */
    repairToolCall?: ToolCallRepairFunction<NoInfer<TOOLS>>;
    /**
     * A function that attempts to repair a tool call that failed to parse.
     *
     * @deprecated Use `repairToolCall` instead.
     */
    experimental_repairToolCall?: ToolCallRepairFunction<NoInfer<TOOLS>>;
    /**
     * Optional mapping of tool names to functions that refine parsed tool inputs.
     *
     * The refined input must have the same type shape as the tool input. Refined
     * inputs are used for tool execution, outputs, callbacks, and telemetry.
     */
    experimental_refineToolInput?: ToolInputRefinement<NoInfer<TOOLS>>;
    /**
     * Callback that is called when the agent operation begins, before any LLM calls.
     */
    onStart?: GenerateTextOnStartCallback<NoInfer<TOOLS>, RUNTIME_CONTEXT, NoInfer<OUTPUT>>;
    /**
     * Callback that is called when the agent operation begins, before any LLM calls.
     *
     * @deprecated Use `onStart` instead.
     */
    experimental_onStart?: GenerateTextOnStartCallback<NoInfer<TOOLS>, RUNTIME_CONTEXT, NoInfer<OUTPUT>>;
    /**
     * Callback that is called when a step (LLM call) begins, before the provider is called.
     */
    onStepStart?: GenerateTextOnStepStartCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>, NoInfer<OUTPUT>>;
    /**
     * Callback that is called when a step (LLM call) begins, before the provider is called.
     *
     * @deprecated Use `onStepStart` instead.
     */
    experimental_onStepStart?: GenerateTextOnStepStartCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>, NoInfer<OUTPUT>>;
    /**
     * Callback that is called before each tool execution begins.
     */
    onToolExecutionStart?: OnToolExecutionStartCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called after each tool execution completes.
     */
    onToolExecutionEnd?: OnToolExecutionEndCallback<NoInfer<TOOLS>>;
    /**
     * Callback that is called when each step (LLM call) ends, including intermediate steps.
     */
    onStepEnd?: GenerateTextOnStepEndCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Callback that is called when each step (LLM call) ends, including intermediate steps.
     *
     * @deprecated Use `onStepEnd` instead.
     */
    onStepFinish?: GenerateTextOnStepFinishCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Callback that is called when all steps are finished and the response is complete.
     */
    onEnd?: GenerateTextOnEndCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Callback that is called when all steps are finished and the response is complete.
     *
     * @deprecated Use `onEnd` instead.
     */
    onFinish?: GenerateTextOnEndCallback<NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>;
    /**
     * Additional provider-specific options. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Custom download function to use for URLs.
     *
     * By default, files are downloaded if the model does not support the URL for the given media type.
     */
    experimental_download?: Experimental_DownloadFunction | undefined;
    /**
     * Settings for controlling what data is included in step results.
     * Disabling inclusion can help reduce memory usage when processing
     * large payloads like images.
     *
     * By default, request and response bodies are included, and request
     * messages are excluded.
     */
    include?: GenerateTextInclude & StreamTextInclude;
    /**
     * Internal. For test use only. May change without notice.
     */
    _internal?: {
        generateId?: IdGenerator;
        generateCallId?: IdGenerator;
    };
    /**
     * The schema for the call options.
     */
    callOptionsSchema?: FlexibleSchema<CALL_OPTIONS>;
    /**
     * Prepare the parameters for the generateText or streamText call.
     *
     * You can use this to have templates based on call options.
     *
     * The design requires you to pass call parameters as follows to
     * allow for the removal of parameters from the original settings
     * by setting them to `undefined`:
     *
     * ```
     *   prepareCall: ({ options, ...rest }) => ({
     *     ...rest,
     *   }),
     * ```
     */
    prepareCall?: (options: Omit<AgentCallParameters<CALL_OPTIONS, NoInfer<TOOLS>, NoInfer<RUNTIME_CONTEXT>>, 'onStepEnd' | 'onStepFinish'> & Pick<ToolLoopAgentSettings<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT, NoInfer<OUTPUT>>, 'model' | 'tools' | 'maxOutputTokens' | 'temperature' | 'topP' | 'topK' | 'presencePenalty' | 'frequencyPenalty' | 'stopSequences' | 'seed' | 'headers' | 'instructions' | 'allowSystemInMessages' | 'stopWhen' | 'telemetry' | 'experimental_telemetry' | 'activeTools' | 'toolOrder' | 'toolApproval' | 'experimental_toolCallers' | 'providerOptions' | 'experimental_download' | 'experimental_refineToolInput' | 'include' | 'runtimeContext' | '_internal'> & {
        toolsContext: InferToolSetContext_2<TOOLS>;
    }) => MaybePromiseLike_2<Pick<ToolLoopAgentSettings<CALL_OPTIONS, TOOLS, RUNTIME_CONTEXT, NoInfer<OUTPUT>>, 'model' | 'tools' | 'maxOutputTokens' | 'temperature' | 'topP' | 'topK' | 'presencePenalty' | 'frequencyPenalty' | 'stopSequences' | 'seed' | 'headers' | 'instructions' | 'allowSystemInMessages' | 'stopWhen' | 'telemetry' | 'experimental_telemetry' | 'activeTools' | 'toolOrder' | 'toolApproval' | 'experimental_toolCallers' | 'providerOptions' | 'experimental_download' | 'experimental_refineToolInput' | 'include' | 'runtimeContext' | '_internal'> & Omit<Prompt, 'system'> & {
        toolsContext: InferToolSetContext_2<TOOLS>;
    }>;
};
export { ToolLoopAgentSettings as Experimental_AgentSettings }
export { ToolLoopAgentSettings }

/**
 * A tool message. It contains the result of one or more tool calls.
 */
export declare type ToolModelMessage = {
    role: 'tool';
    content: ToolContent;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
};

/**
 * A tool message. It contains the result of one or more tool calls.
 */
declare type ToolModelMessage_2 = {
    role: 'tool';
    content: ToolContent_2;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
};

export declare const toolModelMessageSchema: z.ZodType<ToolModelMessage>;

/**
 * Interface for mapping between custom tool names and provider tool names.
 */
declare interface ToolNameMapping {
    /**
     * Maps a custom tool name (used by the client) to the provider's tool name.
     * If the custom tool name does not have a mapping, returns the input name.
     *
     * @param customToolName - The custom name of the tool defined by the client.
     * @returns The corresponding provider tool name, or the input name if not mapped.
     */
    toProviderToolName: (customToolName: string) => string;
    /**
     * Maps a provider tool name to the custom tool name used by the client.
     * If the provider tool name does not have a mapping, returns the input name.
     *
     * @param providerToolName - The name of the tool as understood by the provider.
     * @returns The corresponding custom tool name, or the input name if not mapped.
     */
    toCustomToolName: (providerToolName: string) => string;
}

/**
 * Interface for mapping between custom tool names and provider tool names.
 */
declare interface ToolNameMapping_2 {
    /**
     * Maps a custom tool name (used by the client) to the provider's tool name.
     * If the custom tool name does not have a mapping, returns the input name.
     *
     * @param customToolName - The custom name of the tool defined by the client.
     * @returns The corresponding provider tool name, or the input name if not mapped.
     */
    toProviderToolName: (customToolName: string) => string;
    /**
     * Maps a provider tool name to the custom tool name used by the client.
     * If the provider tool name does not have a mapping, returns the input name.
     *
     * @param providerToolName - The name of the tool as understood by the provider.
     * @returns The corresponding custom tool name, or the input name if not mapped.
     */
    toCustomToolName: (providerToolName: string) => string;
}

/**
 * Function that is called to determine if the tool needs approval before it can be executed.
 *
 * @deprecated Tool approval is handled on a `generateText` / `streamText` level now.
 */
declare type ToolNeedsApprovalFunction<INPUT, CONTEXT extends Context | unknown | never> = (input: INPUT, options: {
    /**
     * The ID of the tool call. You can use it e.g. when sending tool-call related information with stream data.
     */
    toolCallId: string;
    /**
     * Messages that were sent to the language model to initiate the response that contained the tool call.
     * The messages **do not** include the system prompt nor the assistant response that contained the tool call.
     */
    messages: ModelMessage_2[];
    /**
     * Tool context as defined by the tool's context schema.
     * The tool context is specific to the tool and is passed to the tool execution.
     *
     * Treat the context object as immutable inside tools.
     * Mutating the context object can lead to race conditions and unexpected results
     * when tools are called in parallel.
     *
     * If you need to mutate the context, analyze the tool calls and results
     * in `prepareStep` and update it there.
     */
    context: CONTEXT;
}) => boolean | PromiseLike<boolean>;

/**
 * Function that is called to determine if the tool needs approval before it can be executed.
 *
 * @deprecated Tool approval is handled on a `generateText` / `streamText` level now.
 */
declare type ToolNeedsApprovalFunction_2<INPUT, CONTEXT extends Context_2 | unknown | never> = (input: INPUT, options: {
    /**
     * The ID of the tool call. You can use it e.g. when sending tool-call related information with stream data.
     */
    toolCallId: string;
    /**
     * Messages that were sent to the language model to initiate the response that contained the tool call.
     * The messages **do not** include the system prompt nor the assistant response that contained the tool call.
     */
    messages: ModelMessage[];
    /**
     * Tool context as defined by the tool's context schema.
     * The tool context is specific to the tool and is passed to the tool execution.
     *
     * Treat the context object as immutable inside tools.
     * Mutating the context object can lead to race conditions and unexpected results
     * when tools are called in parallel.
     *
     * If you need to mutate the context, analyze the tool calls and results
     * in `prepareStep` and update it there.
     */
    context: CONTEXT;
}) => boolean | PromiseLike<boolean>;

/**
 * Tool names that define the order in which tools are sent to the provider.
 *
 * Tool names are object keys at runtime, so the type is restricted to the
 * string keys of the configured tool set. The list can be partial; tools not
 * listed in `toolOrder` are sent after the listed tools, sorted alphabetically.
 */
export declare type ToolOrder<TOOLS extends ToolSet> = ReadonlyArray<keyof TOOLS & string> | undefined;

declare type ToolOutput<TOOLS extends ToolSet> = TypedToolResult<TOOLS> | TypedToolError<TOOLS>;

/**
 * Helper type to determine the outputSchema and execute function properties of a tool.
 */
declare type ToolOutputProperties<INPUT, OUTPUT, CONTEXT extends Context | unknown | never> = NeverOptional<OUTPUT, {
    /**
     * The optional schema of the output that the tool produces.
     *
     * If not provided, the output shape will be inferred from the execute function.
     */
    outputSchema?: FlexibleSchema_2<OUTPUT>;
    /**
     * An async function that is called with the arguments from the tool call and produces a result.
     * If not provided, the tool will not be executed automatically.
     *
     * @args is the input of the tool call.
     * @options.abortSignal is a signal that can be used to abort the tool call.
     */
    execute: ToolExecuteFunction_2<INPUT, OUTPUT, CONTEXT>;
} | {
    /**
     * The schema of the output that the tool produces.
     *
     * Required when no execute function is provided.
     */
    outputSchema: FlexibleSchema_2<OUTPUT>;
    execute?: never;
}>;

/**
 * Helper type to determine the outputSchema and execute function properties of a tool.
 */
declare type ToolOutputProperties_2<INPUT, OUTPUT, CONTEXT extends Context_2 | unknown | never> = NeverOptional_2<OUTPUT, {
    /**
     * The optional schema of the output that the tool produces.
     *
     * If not provided, the output shape will be inferred from the execute function.
     */
    outputSchema?: FlexibleSchema<OUTPUT>;
    /**
     * An async function that is called with the arguments from the tool call and produces a result.
     * If not provided, the tool will not be executed automatically.
     *
     * @args is the input of the tool call.
     * @options.abortSignal is a signal that can be used to abort the tool call.
     */
    execute: ToolExecuteFunction<INPUT, OUTPUT, CONTEXT>;
} | {
    /**
     * The schema of the output that the tool produces.
     *
     * Required when no execute function is provided.
     */
    outputSchema: FlexibleSchema<OUTPUT>;
    execute?: never;
}>;

/**
 * Typed tool result that is returned by `generateText` and `streamText`.
 * It contains the tool call ID, the tool name, the tool arguments, and the tool result.
 */
declare interface ToolResult<NAME extends string, INPUT, OUTPUT> {
    /**
     * ID of the tool call. This ID is used to match the tool call with the tool result.
     */
    toolCallId: string;
    /**
     * Name of the tool that was called.
     */
    toolName: NAME;
    /**
     * Arguments of the tool call. This is a JSON-serializable object that matches the tool's input schema.
     */
    input: INPUT;
    /**
     * Result of the tool call. This is the result of the tool's execution.
     */
    output: OUTPUT;
    /**
     * Whether the tool result has been executed by the provider.
     */
    providerExecuted?: boolean;
    /**
     * Whether the tool is dynamic.
     */
    dynamic?: boolean;
}

/**
 * Typed tool result that is returned by `generateText` and `streamText`.
 * It contains the tool call ID, the tool name, the tool arguments, and the tool result.
 */
declare interface ToolResult_2<NAME extends string, INPUT, OUTPUT> {
    /**
     * ID of the tool call. This ID is used to match the tool call with the tool result.
     */
    toolCallId: string;
    /**
     * Name of the tool that was called.
     */
    toolName: NAME;
    /**
     * Arguments of the tool call. This is a JSON-serializable object that matches the tool's input schema.
     */
    input: INPUT;
    /**
     * Result of the tool call. This is the result of the tool's execution.
     */
    output: OUTPUT;
    /**
     * Whether the tool result has been executed by the provider.
     */
    providerExecuted?: boolean;
    /**
     * Whether the tool is dynamic.
     */
    dynamic?: boolean;
}

/**
 * Output of a tool result.
 */
declare type ToolResultOutput = {
    /**
     * Text tool output that should be directly sent to the API.
     */
    type: 'text';
    value: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: ProviderOptions;
} | {
    type: 'json';
    value: JSONValue_2;
    /**
     * Provider-specific options.
     */
    providerOptions?: ProviderOptions;
} | {
    /**
     * Type when the user has denied the execution of the tool call.
     */
    type: 'execution-denied';
    /**
     * Optional reason for the execution denial.
     */
    reason?: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: ProviderOptions;
} | {
    type: 'error-text';
    value: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: ProviderOptions;
} | {
    type: 'error-json';
    value: JSONValue_2;
    /**
     * Provider-specific options.
     */
    providerOptions?: ProviderOptions;
} | {
    type: 'content';
    value: Array<{
        type: 'text';
        /**
         * Text content.
         */
        text: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    } | {
        type: 'file';
        /**
         * File data as a tagged discriminated union:
         *
         * - `{ type: 'data', data }`: raw bytes
         *   (base64 string, Uint8Array, ArrayBuffer, Buffer)
         * - `{ type: 'url', url }`: a URL that points to the file
         * - `{ type: 'reference', reference }`: a provider reference
         *   from `uploadFile`
         * - `{ type: 'text', text }`: inline text content (e.g. an inline
         *   text document)
         */
        data: FileData;
        /**
         * Either a full IANA media type (`type/subtype`, e.g. `image/png`) or just
         * the top-level IANA segment (e.g. `image`, `audio`, `video`, `text`).
         *
         * `*`-subtype wildcards (e.g. `image/*`) are normalized as equivalent to the
         * top-level segment alone (e.g. `image`). Providers can use the helpers in
         * `@ai-sdk/provider-utils` (`isFullMediaType`, `getTopLevelMediaType`,
         * `detectMediaType`) to resolve the field according to their API
         * requirements.
         *
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
        /**
         * Optional filename of the file.
         */
        filename?: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    } | {
        /**
         * @deprecated Use 'file' with mediaType + tagged data instead:
         * `{ type: 'file', mediaType, data: { type: 'data', data } }`.
         */
        type: 'file-data';
        /**
         * Base-64 encoded media data.
         */
        data: string;
        /**
         * IANA media type.
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
        /**
         * Optional filename of the file.
         */
        filename?: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    } | {
        /**
         * @deprecated Use 'file' with mediaType and tagged data instead:
         * `{ type: 'file', mediaType, data: { type: 'url', url: new URL(url) } }`.
         */
        type: 'file-url';
        /**
         * URL of the file.
         */
        url: string;
        /**
         * IANA media type.
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType?: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    } | {
        /**
         * @deprecated Use 'file' with tagged data instead:
         * `{ type: 'file', mediaType, data: { type: 'reference', reference } }`.
         */
        type: 'file-id';
        /**
         * ID of the file.
         *
         * If you use multiple providers, you need to
         * specify the provider specific ids using
         * the Record option. The key is the provider
         * name, e.g. 'openai' or 'anthropic'.
         */
        fileId: string | Record<string, string>;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    } | {
        /**
         * @deprecated Use 'file' with tagged data instead:
         * `{ type: 'file', mediaType, data: { type: 'reference', reference } }`.
         */
        type: 'file-reference';
        /**
         * Provider-specific references for the file.
         * The key is the provider name, e.g. 'openai' or 'anthropic'.
         */
        providerReference: ProviderReference_2;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    } | {
        /**
         * @deprecated Use 'file' with mediaType (e.g. 'image' or a specific
         * `image/*` subtype) and tagged data instead:
         * `{ type: 'file', mediaType: 'image', data: { type: 'data', data } }`.
         */
        type: 'image-data';
        /**
         * Base-64 encoded image data.
         */
        data: string;
        /**
         * IANA media type.
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    } | {
        /**
         * @deprecated Use 'file' with `mediaType: 'image'` (or a specific
         * `image/*` subtype) and tagged data instead:
         * `{ type: 'file', mediaType: 'image', data: { type: 'url', url: new URL(url) } }`.
         */
        type: 'image-url';
        /**
         * URL of the image.
         */
        url: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    } | {
        /**
         * @deprecated Use 'file' with `mediaType: 'image'` (or a specific
         * `image/*` subtype) and tagged data instead:
         * `{ type: 'file', mediaType: 'image', data: { type: 'reference', reference } }`.
         */
        type: 'image-file-id';
        /**
         * Image that is referenced using a provider file id.
         *
         * If you use multiple providers, you need to
         * specify the provider specific ids using
         * the Record option. The key is the provider
         * name, e.g. 'openai' or 'anthropic'.
         */
        fileId: string | Record<string, string>;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    } | {
        /**
         * @deprecated Use 'file' with `mediaType: 'image'` (or a specific
         * `image/*` subtype) and tagged data instead:
         * `{ type: 'file', mediaType: 'image', data: { type: 'reference', reference } }`.
         */
        type: 'image-file-reference';
        /**
         * Provider-specific references for the image file.
         * The key is the provider name, e.g. 'openai' or 'anthropic'.
         */
        providerReference: ProviderReference_2;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    } | {
        /**
         * Custom content part. This can be used to implement
         * provider-specific content parts.
         */
        type: 'custom';
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions;
    }>;
};

/**
 * Output of a tool result.
 */
declare type ToolResultOutput_2 = {
    /**
     * Text tool output that should be directly sent to the API.
     */
    type: 'text';
    value: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: ProviderOptions_2;
} | {
    type: 'json';
    value: JSONValue_2;
    /**
     * Provider-specific options.
     */
    providerOptions?: ProviderOptions_2;
} | {
    /**
     * Type when the user has denied the execution of the tool call.
     */
    type: 'execution-denied';
    /**
     * Optional reason for the execution denial.
     */
    reason?: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: ProviderOptions_2;
} | {
    type: 'error-text';
    value: string;
    /**
     * Provider-specific options.
     */
    providerOptions?: ProviderOptions_2;
} | {
    type: 'error-json';
    value: JSONValue_2;
    /**
     * Provider-specific options.
     */
    providerOptions?: ProviderOptions_2;
} | {
    type: 'content';
    value: Array<{
        type: 'text';
        /**
         * Text content.
         */
        text: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    } | {
        type: 'file';
        /**
         * File data as a tagged discriminated union:
         *
         * - `{ type: 'data', data }`: raw bytes
         *   (base64 string, Uint8Array, ArrayBuffer, Buffer)
         * - `{ type: 'url', url }`: a URL that points to the file
         * - `{ type: 'reference', reference }`: a provider reference
         *   from `uploadFile`
         * - `{ type: 'text', text }`: inline text content (e.g. an inline
         *   text document)
         */
        data: FileData_2;
        /**
         * Either a full IANA media type (`type/subtype`, e.g. `image/png`) or just
         * the top-level IANA segment (e.g. `image`, `audio`, `video`, `text`).
         *
         * `*`-subtype wildcards (e.g. `image/*`) are normalized as equivalent to the
         * top-level segment alone (e.g. `image`). Providers can use the helpers in
         * `@ai-sdk/provider-utils` (`isFullMediaType`, `getTopLevelMediaType`,
         * `detectMediaType`) to resolve the field according to their API
         * requirements.
         *
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
        /**
         * Optional filename of the file.
         */
        filename?: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    } | {
        /**
         * @deprecated Use 'file' with mediaType + tagged data instead:
         * `{ type: 'file', mediaType, data: { type: 'data', data } }`.
         */
        type: 'file-data';
        /**
         * Base-64 encoded media data.
         */
        data: string;
        /**
         * IANA media type.
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
        /**
         * Optional filename of the file.
         */
        filename?: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    } | {
        /**
         * @deprecated Use 'file' with mediaType and tagged data instead:
         * `{ type: 'file', mediaType, data: { type: 'url', url: new URL(url) } }`.
         */
        type: 'file-url';
        /**
         * URL of the file.
         */
        url: string;
        /**
         * IANA media type.
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType?: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    } | {
        /**
         * @deprecated Use 'file' with tagged data instead:
         * `{ type: 'file', mediaType, data: { type: 'reference', reference } }`.
         */
        type: 'file-id';
        /**
         * ID of the file.
         *
         * If you use multiple providers, you need to
         * specify the provider specific ids using
         * the Record option. The key is the provider
         * name, e.g. 'openai' or 'anthropic'.
         */
        fileId: string | Record<string, string>;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    } | {
        /**
         * @deprecated Use 'file' with tagged data instead:
         * `{ type: 'file', mediaType, data: { type: 'reference', reference } }`.
         */
        type: 'file-reference';
        /**
         * Provider-specific references for the file.
         * The key is the provider name, e.g. 'openai' or 'anthropic'.
         */
        providerReference: ProviderReference_3;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    } | {
        /**
         * @deprecated Use 'file' with mediaType (e.g. 'image' or a specific
         * `image/*` subtype) and tagged data instead:
         * `{ type: 'file', mediaType: 'image', data: { type: 'data', data } }`.
         */
        type: 'image-data';
        /**
         * Base-64 encoded image data.
         */
        data: string;
        /**
         * IANA media type.
         * @see https://www.iana.org/assignments/media-types/media-types.xhtml
         */
        mediaType: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    } | {
        /**
         * @deprecated Use 'file' with `mediaType: 'image'` (or a specific
         * `image/*` subtype) and tagged data instead:
         * `{ type: 'file', mediaType: 'image', data: { type: 'url', url: new URL(url) } }`.
         */
        type: 'image-url';
        /**
         * URL of the image.
         */
        url: string;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    } | {
        /**
         * @deprecated Use 'file' with `mediaType: 'image'` (or a specific
         * `image/*` subtype) and tagged data instead:
         * `{ type: 'file', mediaType: 'image', data: { type: 'reference', reference } }`.
         */
        type: 'image-file-id';
        /**
         * Image that is referenced using a provider file id.
         *
         * If you use multiple providers, you need to
         * specify the provider specific ids using
         * the Record option. The key is the provider
         * name, e.g. 'openai' or 'anthropic'.
         */
        fileId: string | Record<string, string>;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    } | {
        /**
         * @deprecated Use 'file' with `mediaType: 'image'` (or a specific
         * `image/*` subtype) and tagged data instead:
         * `{ type: 'file', mediaType: 'image', data: { type: 'reference', reference } }`.
         */
        type: 'image-file-reference';
        /**
         * Provider-specific references for the image file.
         * The key is the provider name, e.g. 'openai' or 'anthropic'.
         */
        providerReference: ProviderReference_3;
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    } | {
        /**
         * Custom content part. This can be used to implement
         * provider-specific content parts.
         */
        type: 'custom';
        /**
         * Provider-specific options.
         */
        providerOptions?: ProviderOptions_2;
    }>;
};

/**
 * Tool result content part of a prompt. It contains the result of the tool call with the matching ID.
 */
export declare interface ToolResultPart {
    type: 'tool-result';
    /**
     * ID of the tool call that this result is associated with.
     */
    toolCallId: string;
    /**
     * Name of the tool that generated this result.
     */
    toolName: string;
    /**
     * Result of the tool call. This is a JSON-serializable object.
     */
    output: ToolResultOutput_2;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
}

/**
 * Tool result content part of a prompt. It contains the result of the tool call with the matching ID.
 */
declare interface ToolResultPart_2 {
    type: 'tool-result';
    /**
     * ID of the tool call that this result is associated with.
     */
    toolCallId: string;
    /**
     * Name of the tool that generated this result.
     */
    toolName: string;
    /**
     * Result of the tool call. This is a JSON-serializable object.
     */
    output: ToolResultOutput;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
}

/**
 * Helper type to make the toolsContext parameter optional, required, or
 * unavailable based on the tool set.
 */
declare type ToolsContextParameter<TOOLS extends ToolSet> = {
    tools?: TOOLS;
} & (IsEmptyObject_3<InferToolSetContext_2<TOOLS>> extends true ? {
    toolsContext?: never;
} : HasRequiredKey_2<InferToolSetContext_2<TOOLS>> extends true ? {
    toolsContext: InferToolSetContext_2<TOOLS>;
} : {
    toolsContext?: InferToolSetContext_2<TOOLS>;
});

/**
 * A mapping of tool names to tool definitions.
 */
export declare type ToolSet = Record<string, (Tool<never, never, any> | Tool<any, any, any> | Tool<any, never, any> | Tool<never, any, any>) & Pick<Tool<any, any, any>, 'execute' | 'onInputAvailable' | 'onInputStart' | 'onInputDelta' | 'needsApproval'>>;

/**
 * A mapping of tool names to tool definitions.
 */
declare type ToolSet_2 = Record<string, (Tool_2<never, never, any> | Tool_2<any, any, any> | Tool_2<any, never, any> | Tool_2<never, any, any>) & Pick<Tool_2<any, any, any>, 'execute' | 'onInputAvailable' | 'onInputStart' | 'onInputDelta' | 'needsApproval'>>;

export declare type ToolUIPart<TOOLS extends UITools = UITools> = ValueOf<{
    [NAME in keyof TOOLS & string]: {
        type: `tool-${NAME}`;
    } & UIToolInvocation<TOOLS[NAME]>;
}>;

export declare class TooManyEmbeddingValuesForCallError extends AISDKError {
    private readonly [symbol$2];
    readonly provider: string;
    readonly modelId: string;
    readonly maxEmbeddingsPerCall: number;
    readonly values: Array<unknown>;
    constructor(options: {
        provider: string;
        modelId: string;
        maxEmbeddingsPerCall: number;
        values: Array<unknown>;
    });
    static isInstance(error: unknown): error is TooManyEmbeddingValuesForCallError;
}

/**
 * Converts a stream of `TextStreamPart` chunks into a stream of text deltas.
 */
export declare function toTextStream<TOOLS extends ToolSet = ToolSet>({ stream, }: {
    stream: ReadableStream<TextStreamPart<TOOLS>>;
}): ReadableStream<string>;

/**
 * Converts a single `TextStreamPart` (as emitted by `streamText`'s
 * `stream`) into a `UIMessageChunk`.
 *
 * Returns `undefined` for stream parts that do not produce UI message chunks.
 */
export declare function toUIMessageChunk<TOOLS extends ToolSet = ToolSet, UI_MESSAGE extends UIMessage = UIMessage>(part: TextStreamPart<TOOLS>, { tools, sendReasoning, sendSources, sendStart, sendFinish, onError, // prevent leaking server error details to the client by default
    messageMetadata, responseMessageId, }?: ToUIMessageChunkOptions<TOOLS, UI_MESSAGE>): InferUIMessageChunk<UI_MESSAGE> | undefined;

export declare type ToUIMessageChunkOptions<TOOLS extends ToolSet = ToolSet, UI_MESSAGE extends UIMessage = UIMessage> = {
    tools?: TOOLS;
    sendReasoning?: boolean;
    sendSources?: boolean;
    sendStart?: boolean;
    sendFinish?: boolean;
    onError?: (error: unknown) => string;
    messageMetadata?: InferUIMessageMetadata<UI_MESSAGE>;
    responseMessageId?: string;
};

/**
 * Converts a stream of `TextStreamPart<TOOLS>` chunks (as emitted by
 * `streamText`'s `stream`) into a stream of `UIMessageChunk`s suitable for
 * UI message streaming, including response message ID injection and
 * `onEnd` handling.
 */
export declare function toUIMessageStream<TOOLS extends ToolSet = ToolSet, UI_MESSAGE extends UIMessage = UIMessage>({ stream, tools, sendReasoning, sendSources, sendStart, sendFinish, onError, // prevent leaking server error details to the client by default
    messageMetadata, originalMessages, generateMessageId, onEnd, onFinish, }: {
    stream: ReadableStream<TextStreamPart<TOOLS>>;
    tools?: TOOLS;
} & UIMessageStreamOptions<UI_MESSAGE>): ReadableStream<InferUIMessageChunk<UI_MESSAGE>>;

/**
 * Converts an http(s) URL to the corresponding ws(s) URL.
 */
declare function toWebSocketUrl(url: string | URL): URL;

/**
 * Converts an http(s) URL to the corresponding ws(s) URL.
 */
declare function toWebSocketUrl_2(url: string | URL): URL;

export declare function transcribe({ model, audio, providerOptions, maxRetries: maxRetriesArg, abortSignal, headers, download: downloadFn, }: {
    /**
     * The transcription model to use.
     */
    model: TranscriptionModel;
    /**
     * The audio data to transcribe.
     */
    audio: DataContent | URL;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     *   "openai": {
     *     "temperature": 0
     *   }
     * }
     * ```
     */
    providerOptions?: ProviderOptions_2;
    /**
     * Maximum number of retries per transcript model call. Set to 0 to disable retries.
     *
     * @default 2
     */
    maxRetries?: number;
    /**
     * Abort signal.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional headers to include in the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string>;
    /**
     * Custom download function for fetching audio from URLs.
     * Use `createDownload()` from `ai` to create a download function with custom size limits.
     *
     * @default createDownload() (2 GiB limit)
     */
    download?: (options: {
        url: URL;
        abortSignal?: AbortSignal;
    }) => Promise<{
        data: Uint8Array;
        mediaType: string | undefined;
    }>;
}): Promise<TranscriptionResult>;

/** Type of the client TEXT frame that signals the end of the audio input. */
declare const TRANSCRIPTION_STREAM_AUDIO_DONE_FRAME_TYPE = "transcription-stream.audio-done";

/** Type of the client TEXT frame that signals the end of the audio input. */
declare const TRANSCRIPTION_STREAM_AUDIO_DONE_FRAME_TYPE_2 = "transcription-stream.audio-done";

/** Type of the first client TEXT frame. */
declare const TRANSCRIPTION_STREAM_START_FRAME_TYPE = "transcription-stream.start";

/** Type of the first client TEXT frame. */
declare const TRANSCRIPTION_STREAM_START_FRAME_TYPE_2 = "transcription-stream.start";

/**
 * Transcription model that is used by the AI SDK.
 */
export declare type TranscriptionModel = string | TranscriptionModelV4_2 | TranscriptionModelV3 | TranscriptionModelV2;

export declare type TranscriptionModelResponseMetadata = {
    /**
     * Timestamp for the start of the generated response.
     */
    timestamp: Date;
    /**
     * The ID of the response model that was used to generate the response.
     */
    modelId: string;
    /**
     * Response headers.
     */
    headers?: Record<string, string>;
};

/**
 * Transcription model specification version 2.
 */
declare type TranscriptionModelV2 = {
    /**
     * The transcription model must specify which transcription model interface
     * version it implements. This will allow us to evolve the transcription
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v2';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Generates a transcript.
     */
    doGenerate(options: TranscriptionModelV2CallOptions): PromiseLike<{
        /**
         * The complete transcribed text from the audio.
         */
        text: string;
        /**
         * Array of transcript segments with timing information.
         * Each segment represents a portion of the transcribed text with start and end times.
         */
        segments: Array<{
            /**
             * The text content of this segment.
             */
            text: string;
            /**
             * The start time of this segment in seconds.
             */
            startSecond: number;
            /**
             * The end time of this segment in seconds.
             */
            endSecond: number;
        }>;
        /**
         * The detected language of the audio content, as an ISO-639-1 code (e.g., 'en' for English).
         * May be undefined if the language couldn't be detected.
         */
        language: string | undefined;
        /**
         * The total duration of the audio file in seconds.
         * May be undefined if the duration couldn't be determined.
         */
        durationInSeconds: number | undefined;
        /**
         * Warnings for the call, e.g. unsupported settings.
         */
        warnings: Array<TranscriptionModelV2CallWarning>;
        /**
         * Optional request information for telemetry and debugging purposes.
         */
        request?: {
            /**
             * Raw request HTTP body that was sent to the provider API as a string (JSON should be stringified).
             * Non-HTTP(s) providers should not set this.
             */
            body?: string;
        };
        /**
         * Response information for telemetry and debugging purposes.
         */
        response: {
            /**
             * Timestamp for the start of the generated response.
             */
            timestamp: Date;
            /**
             * The ID of the response model that was used to generate the response.
             */
            modelId: string;
            /**
             * Response headers.
             */
            headers?: SharedV2Headers_2;
            /**
             * Response body.
             */
            body?: unknown;
        };
        /**
         * Additional provider-specific metadata. They are passed through
         * from the provider to the AI SDK and enable provider-specific
         * results that can be fully encapsulated in the provider.
         */
        providerMetadata?: Record<string, Record<string, JSONValue_3>>;
    }>;
};

declare type TranscriptionModelV2CallOptions = {
    /**
     * Audio data to transcribe.
     * Accepts a `Uint8Array` or `string`, where `string` is a base64 encoded audio file.
     */
    audio: Uint8Array | string;
    /**
     * The IANA media type of the audio data.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     * "openai": {
     * "timestampGranularities": ["word"]
     * }
     * }
     * ```
     */
    providerOptions?: TranscriptionModelV2ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

/**
 * Warning from the model provider for this call. The call will proceed, but e.g.
 * some settings might not be supported, which can lead to suboptimal results.
 */
declare type TranscriptionModelV2CallWarning = {
    type: 'unsupported-setting';
    setting: keyof TranscriptionModelV2CallOptions;
    details?: string;
} | {
    type: 'other';
    message: string;
};

declare type TranscriptionModelV2ProviderOptions = Record<string, Record<string, JSONValue_3>>;

/**
 * Transcription model specification version 3.
 */
declare type TranscriptionModelV3 = {
    /**
     * The transcription model must specify which transcription model interface
     * version it implements. This will allow us to evolve the transcription
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v3';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Generates a transcript.
     */
    doGenerate(options: TranscriptionModelV3CallOptions): PromiseLike<{
        /**
         * The complete transcribed text from the audio.
         */
        text: string;
        /**
         * Array of transcript segments with timing information.
         * Each segment represents a portion of the transcribed text with start and end times.
         */
        segments: Array<{
            /**
             * The text content of this segment.
             */
            text: string;
            /**
             * The start time of this segment in seconds.
             */
            startSecond: number;
            /**
             * The end time of this segment in seconds.
             */
            endSecond: number;
        }>;
        /**
         * The detected language of the audio content, as an ISO-639-1 code (e.g., 'en' for English).
         * May be undefined if the language couldn't be detected.
         */
        language: string | undefined;
        /**
         * The total duration of the audio file in seconds.
         * May be undefined if the duration couldn't be determined.
         */
        durationInSeconds: number | undefined;
        /**
         * Warnings for the call, e.g. unsupported settings.
         */
        warnings: Array<SharedV3Warning>;
        /**
         * Optional request information for telemetry and debugging purposes.
         */
        request?: {
            /**
             * Raw request HTTP body that was sent to the provider API as a string (JSON should be stringified).
             * Non-HTTP(s) providers should not set this.
             */
            body?: string;
        };
        /**
         * Response information for telemetry and debugging purposes.
         */
        response: {
            /**
             * Timestamp for the start of the generated response.
             */
            timestamp: Date;
            /**
             * The ID of the response model that was used to generate the response.
             */
            modelId: string;
            /**
             * Response headers.
             */
            headers?: SharedV3Headers;
            /**
             * Response body.
             */
            body?: unknown;
        };
        /**
         * Additional provider-specific metadata. They are passed through
         * from the provider to the AI SDK and enable provider-specific
         * results that can be fully encapsulated in the provider.
         */
        providerMetadata?: Record<string, JSONObject_2>;
    }>;
};

declare type TranscriptionModelV3CallOptions = {
    /**
     * Audio data to transcribe.
     * Accepts a `Uint8Array` or `string`, where `string` is a base64 encoded audio file.
     */
    audio: Uint8Array | string;
    /**
     * The IANA media type of the audio data.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     * "openai": {
     * "timestampGranularities": ["word"]
     * }
     * }
     * ```
     */
    providerOptions?: TranscriptionModelV3ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

declare type TranscriptionModelV3ProviderOptions = Record<string, JSONObject_2>;

/**
 * Transcription model specification version 4.
 */
declare type TranscriptionModelV4 = {
    /**
     * The transcription model must specify which transcription model interface
     * version it implements. This will allow us to evolve the transcription
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Generates a transcript.
     */
    doGenerate(options: TranscriptionModelV4CallOptions): PromiseLike<TranscriptionModelV4Result>;
    /**
     * Streams a transcript for live audio.
     *
     * Experimental: the streaming transcription contract may change in patch
     * releases while `experimental_streamTranscribe` is experimental. The
     * stream option/part/result types are exported with `Experimental_`
     * prefixes for this reason.
     */
    doStream?(options: TranscriptionModelV4StreamOptions): PromiseLike<TranscriptionModelV4StreamResult>;
};

/**
 * Transcription model specification version 4.
 */
declare type TranscriptionModelV4_2 = {
    /**
     * The transcription model must specify which transcription model interface
     * version it implements. This will allow us to evolve the transcription
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Generates a transcript.
     */
    doGenerate(options: TranscriptionModelV4CallOptions_2): PromiseLike<TranscriptionModelV4Result_2>;
    /**
     * Streams a transcript for live audio.
     *
     * Experimental: the streaming transcription contract may change in patch
     * releases while `experimental_streamTranscribe` is experimental. The
     * stream option/part/result types are exported with `Experimental_`
     * prefixes for this reason.
     */
    doStream?(options: TranscriptionModelV4StreamOptions_2): PromiseLike<TranscriptionModelV4StreamResult_2>;
};

declare type TranscriptionModelV4CallOptions = {
    /**
     * Audio data to transcribe.
     * Accepts a `Uint8Array` or `string`, where `string` is a base64 encoded audio file.
     */
    audio: Uint8Array | string;
    /**
     * The IANA media type of the audio data.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     * "openai": {
     * "timestampGranularities": ["word"]
     * }
     * }
     * ```
     */
    providerOptions?: TranscriptionModelV4ProviderOptions$1;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

declare type TranscriptionModelV4CallOptions_2 = {
    /**
     * Audio data to transcribe.
     * Accepts a `Uint8Array` or `string`, where `string` is a base64 encoded audio file.
     */
    audio: Uint8Array | string;
    /**
     * The IANA media type of the audio data.
     *
     * @see https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is keyed by the provider-specific metadata key.
     * ```ts
     * {
     * "openai": {
     * "timestampGranularities": ["word"]
     * }
     * }
     * ```
     */
    providerOptions?: TranscriptionModelV4ProviderOptions$1_2;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

declare type TranscriptionModelV4ProviderOptions$1 = Record<string, JSONObject>;

declare type TranscriptionModelV4ProviderOptions$1_2 = Record<string, JSONObject_2>;

declare type TranscriptionModelV4ProviderOptions = Record<string, JSONObject>;

declare type TranscriptionModelV4ProviderOptions_2 = Record<string, JSONObject_2>;

/**
 * The result of a transcription model doGenerate call.
 */
declare type TranscriptionModelV4Result = {
    /**
     * The complete transcribed text from the audio.
     */
    text: string;
    /**
     * Array of transcript segments with timing information.
     * Each segment represents a portion of the transcribed text with start and end times.
     */
    segments: Array<{
        /**
         * The text content of this segment.
         */
        text: string;
        /**
         * The start time of this segment in seconds.
         */
        startSecond: number;
        /**
         * The end time of this segment in seconds.
         */
        endSecond: number;
    }>;
    /**
     * The detected language of the audio content, as an ISO-639-1 code (e.g., 'en' for English).
     * May be undefined if the language couldn't be detected.
     */
    language: string | undefined;
    /**
     * The total duration of the audio file in seconds.
     * May be undefined if the duration couldn't be determined.
     */
    durationInSeconds: number | undefined;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: Array<SharedV4Warning>;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Raw request HTTP body that was sent to the provider API as a string (JSON should be stringified).
         * Non-HTTP(s) providers should not set this.
         */
        body?: string;
    };
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        /**
         * Timestamp for the start of the generated response.
         */
        timestamp: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId: string;
        /**
         * Response headers.
         */
        headers?: SharedV4Headers;
        /**
         * Response body.
         */
        body?: unknown;
    };
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    providerMetadata?: Record<string, JSONObject>;
};

/**
 * The result of a transcription model doGenerate call.
 */
declare type TranscriptionModelV4Result_2 = {
    /**
     * The complete transcribed text from the audio.
     */
    text: string;
    /**
     * Array of transcript segments with timing information.
     * Each segment represents a portion of the transcribed text with start and end times.
     */
    segments: Array<{
        /**
         * The text content of this segment.
         */
        text: string;
        /**
         * The start time of this segment in seconds.
         */
        startSecond: number;
        /**
         * The end time of this segment in seconds.
         */
        endSecond: number;
    }>;
    /**
     * The detected language of the audio content, as an ISO-639-1 code (e.g., 'en' for English).
     * May be undefined if the language couldn't be detected.
     */
    language: string | undefined;
    /**
     * The total duration of the audio file in seconds.
     * May be undefined if the duration couldn't be determined.
     */
    durationInSeconds: number | undefined;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    warnings: Array<SharedV4Warning_2>;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Raw request HTTP body that was sent to the provider API as a string (JSON should be stringified).
         * Non-HTTP(s) providers should not set this.
         */
        body?: string;
    };
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        /**
         * Timestamp for the start of the generated response.
         */
        timestamp: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId: string;
        /**
         * Response headers.
         */
        headers?: SharedV4Headers_2;
        /**
         * Response body.
         */
        body?: unknown;
    };
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     */
    providerMetadata?: Record<string, JSONObject_2>;
};

declare type TranscriptionModelV4StreamOptions = {
    /**
     * Audio chunks to transcribe.
     *
     * `Uint8Array` chunks contain raw audio bytes. `string` chunks contain
     * base64-encoded raw audio bytes.
     */
    audio: ReadableStream<Uint8Array | string>;
    /**
     * The input audio format for the raw audio chunks.
     */
    inputAudioFormat: SharedV4AudioFormat;
    /**
     * Additional provider-specific options that are passed through to the provider.
     *
     * The outer record is keyed by the provider name, and the inner record is keyed
     * by provider-specific option names.
     */
    providerOptions?: TranscriptionModelV4ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP/WebSocket-based providers that support headers.
     */
    headers?: Record<string, string | undefined>;
    /**
     * When true, providers should include raw provider chunks in the stream.
     */
    includeRawChunks?: boolean;
};

declare type TranscriptionModelV4StreamOptions_2 = {
    /**
     * Audio chunks to transcribe.
     *
     * `Uint8Array` chunks contain raw audio bytes. `string` chunks contain
     * base64-encoded raw audio bytes.
     */
    audio: ReadableStream<Uint8Array | string>;
    /**
     * The input audio format for the raw audio chunks.
     */
    inputAudioFormat: SharedV4AudioFormat_2;
    /**
     * Additional provider-specific options that are passed through to the provider.
     *
     * The outer record is keyed by the provider name, and the inner record is keyed
     * by provider-specific option names.
     */
    providerOptions?: TranscriptionModelV4ProviderOptions_2;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP/WebSocket-based providers that support headers.
     */
    headers?: Record<string, string | undefined>;
    /**
     * When true, providers should include raw provider chunks in the stream.
     */
    includeRawChunks?: boolean;
};

declare type TranscriptionModelV4StreamPart = {
    /**
     * Stream start event with warnings for the call, e.g. unsupported settings.
     */
    type: 'stream-start';
    warnings: Array<SharedV4Warning>;
} | {
    /**
     * Append-only transcript delta.
     */
    type: 'transcript-delta';
    id?: string;
    delta: string;
    providerMetadata?: SharedV4ProviderMetadata;
} | {
    /**
     * Non-final transcript text. The text may be revised by later parts.
     */
    type: 'transcript-partial';
    id?: string;
    text: string;
    startSecond?: number;
    durationInSeconds?: number;
    channelIndex?: number;
    providerMetadata?: SharedV4ProviderMetadata;
} | {
    /**
     * Final transcript text for a provider-defined segment or utterance.
     */
    type: 'transcript-final';
    id?: string;
    text: string;
    startSecond?: number;
    endSecond?: number;
    channelIndex?: number;
    providerMetadata?: SharedV4ProviderMetadata;
} | {
    /**
     * Metadata for the response, emitted once available.
     */
    type: 'response-metadata';
    timestamp?: Date;
    modelId?: string;
    headers?: SharedV4Headers;
    body?: unknown;
} | {
    /**
     * Metadata that is available after the stream is finished.
     */
    type: 'finish';
    text: string;
    segments: Array<{
        text: string;
        startSecond: number;
        endSecond: number;
    }>;
    language?: string;
    durationInSeconds?: number;
    providerMetadata?: Record<string, JSONObject>;
} | {
    /**
     * Raw provider chunks if enabled.
     */
    type: 'raw';
    rawValue: unknown;
} | {
    /**
     * Error parts are streamed, allowing for multiple errors.
     */
    type: 'error';
    error: unknown;
};

declare type TranscriptionModelV4StreamPart_2 = {
    /**
     * Stream start event with warnings for the call, e.g. unsupported settings.
     */
    type: 'stream-start';
    warnings: Array<SharedV4Warning_2>;
} | {
    /**
     * Append-only transcript delta.
     */
    type: 'transcript-delta';
    id?: string;
    delta: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Non-final transcript text. The text may be revised by later parts.
     */
    type: 'transcript-partial';
    id?: string;
    text: string;
    startSecond?: number;
    durationInSeconds?: number;
    channelIndex?: number;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Final transcript text for a provider-defined segment or utterance.
     */
    type: 'transcript-final';
    id?: string;
    text: string;
    startSecond?: number;
    endSecond?: number;
    channelIndex?: number;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    /**
     * Metadata for the response, emitted once available.
     */
    type: 'response-metadata';
    timestamp?: Date;
    modelId?: string;
    headers?: SharedV4Headers_2;
    body?: unknown;
} | {
    /**
     * Metadata that is available after the stream is finished.
     */
    type: 'finish';
    text: string;
    segments: Array<{
        text: string;
        startSecond: number;
        endSecond: number;
    }>;
    language?: string;
    durationInSeconds?: number;
    providerMetadata?: Record<string, JSONObject_2>;
} | {
    /**
     * Raw provider chunks if enabled.
     */
    type: 'raw';
    rawValue: unknown;
} | {
    /**
     * Error parts are streamed, allowing for multiple errors.
     */
    type: 'error';
    error: unknown;
};

/**
 * The result of a transcription model doStream call.
 */
declare type TranscriptionModelV4StreamResult = {
    /**
     * The stream.
     */
    stream: ReadableStream<TranscriptionModelV4StreamPart>;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Request body or setup payload that was sent to the provider API.
         */
        body?: unknown;
    };
    /**
     * Optional response data.
     */
    response?: {
        /**
         * Timestamp for the start of the streamed response.
         */
        timestamp?: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId?: string;
        /**
         * Response headers.
         */
        headers?: SharedV4Headers;
        /**
         * Response body.
         */
        body?: unknown;
    };
};

/**
 * The result of a transcription model doStream call.
 */
declare type TranscriptionModelV4StreamResult_2 = {
    /**
     * The stream.
     */
    stream: ReadableStream<TranscriptionModelV4StreamPart_2>;
    /**
     * Optional request information for telemetry and debugging purposes.
     */
    request?: {
        /**
         * Request body or setup payload that was sent to the provider API.
         */
        body?: unknown;
    };
    /**
     * Optional response data.
     */
    response?: {
        /**
         * Timestamp for the start of the streamed response.
         */
        timestamp?: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId?: string;
        /**
         * Response headers.
         */
        headers?: SharedV4Headers_2;
        /**
         * Response body.
         */
        body?: unknown;
    };
};

/**
 * The result of a `transcribe` call.
 * It contains the transcript and additional information.
 */
export declare interface TranscriptionResult {
    /**
     * The complete transcribed text from the audio.
     */
    readonly text: string;
    /**
     * Array of transcript segments with timing information.
     * Each segment represents a portion of the transcribed text with start and end times.
     */
    readonly segments: Array<{
        /**
         * The text content of this segment.
         */
        readonly text: string;
        /**
         * The start time of this segment in seconds.
         */
        readonly startSecond: number;
        /**
         * The end time of this segment in seconds.
         */
        readonly endSecond: number;
    }>;
    /**
     * The detected language of the audio content, as an ISO-639-1 code (e.g., 'en' for English).
     * May be undefined if the language couldn't be detected.
     */
    readonly language: string | undefined;
    /**
     * The total duration of the audio file in seconds.
     * May be undefined if the duration couldn't be determined.
     */
    readonly durationInSeconds: number | undefined;
    /**
     * Warnings for the call, e.g. unsupported settings.
     */
    readonly warnings: Array<Warning>;
    /**
     * Response metadata from the provider. There may be multiple responses if we made multiple calls to the model.
     */
    readonly responses: Array<TranscriptionModelResponseMetadata>;
    /**
     * Provider metadata from the provider.
     */
    readonly providerMetadata: Record<string, JSONObject_2>;
}

/** Server-side classification of a client TEXT frame. */
declare type TranscriptionStreamClientFrame = {
    type: 'start';
    frame: TranscriptionStreamStartFrame;
} | {
    type: 'audio-done';
} | {
    /** Malformed JSON or a recognized frame with an invalid shape. */
    type: 'invalid';
    message: string;
} | {
    /** Unrecognized frame type; ignore for forward compatibility. */
    type: 'unknown';
};

/** Server-side classification of a client TEXT frame. */
declare type TranscriptionStreamClientFrame_2 = {
    type: 'start';
    frame: TranscriptionStreamStartFrame_2;
} | {
    type: 'audio-done';
} | {
    /** Malformed JSON or a recognized frame with an invalid shape. */
    type: 'invalid';
    message: string;
} | {
    /** Unrecognized frame type; ignore for forward compatibility. */
    type: 'unknown';
};

export declare type TranscriptionStreamPart = {
    type: 'transcript-delta';
    id?: string;
    delta: string;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    type: 'transcript-partial';
    id?: string;
    text: string;
    startSecond?: number;
    durationInSeconds?: number;
    channelIndex?: number;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    type: 'transcript-final';
    id?: string;
    text: string;
    startSecond?: number;
    endSecond?: number;
    channelIndex?: number;
    providerMetadata?: SharedV4ProviderMetadata_2;
} | {
    type: 'raw';
    rawValue: unknown;
} | {
    type: 'error';
    error: unknown;
};

/**
 * The client's session start frame. Optional keys are omitted when undefined.
 */
declare type TranscriptionStreamStartFrame = {
    type: typeof TRANSCRIPTION_STREAM_START_FRAME_TYPE;
    /** Audio format of the binary audio frames, e.g. `{ type: 'audio/pcm', rate: 16000 }`. */
    inputAudioFormat: {
        type: string;
        rate?: number;
    };
    /** Provider-specific options, passed through verbatim. */
    providerOptions?: Record<string, JSONObject>;
    /** When true, the server should include `raw` parts in the stream. */
    includeRawChunks?: boolean;
};

/**
 * The client's session start frame. Optional keys are omitted when undefined.
 */
declare type TranscriptionStreamStartFrame_2 = {
    type: typeof TRANSCRIPTION_STREAM_START_FRAME_TYPE_2;
    /** Audio format of the binary audio frames, e.g. `{ type: 'audio/pcm', rate: 16000 }`. */
    inputAudioFormat: {
        type: string;
        rate?: number;
    };
    /** Provider-specific options, passed through verbatim. */
    providerOptions?: Record<string, JSONObject>;
    /** When true, the server should include `raw` parts in the stream. */
    includeRawChunks?: boolean;
};

/**
 * A tool call returned by text generation, either statically typed from the
 * declared tool set or dynamically typed when the tool cannot be inferred.
 */
export declare type TypedToolCall<TOOLS extends ToolSet> = StaticToolCall<TOOLS> | DynamicToolCall;

export declare type TypedToolError<TOOLS extends ToolSet> = StaticToolError<TOOLS> | DynamicToolError;

/**
 * Tool output when the tool execution has been denied.
 */
export declare type TypedToolOutputDenied<TOOLS extends ToolSet> = StaticToolOutputDenied<TOOLS>;

export declare type TypedToolResult<TOOLS extends ToolSet> = StaticToolResult<TOOLS> | DynamicToolResult;

declare interface TypeValidationContext {
    /**
     * Field path in dot notation (e.g., "message.metadata", "message.parts[3].data")
     */
    field?: string;
    /**
     * Entity name (e.g., tool name, data type name)
     */
    entityName?: string;
    /**
     * Entity identifier (e.g., message ID, tool call ID)
     */
    entityId?: string;
}

declare interface TypeValidationContext_2 {
    /**
     * Field path in dot notation (e.g., "message.metadata", "message.parts[3].data")
     */
    field?: string;
    /**
     * Entity name (e.g., tool name, data type name)
     */
    entityName?: string;
    /**
     * Entity identifier (e.g., message ID, tool call ID)
     */
    entityId?: string;
}

export declare class TypeValidationError extends AISDKError {
    private readonly [symbol$1];
    readonly value: unknown;
    readonly context?: TypeValidationContext;
    constructor({ value, cause, context, }: {
        value: unknown;
        cause: unknown;
        context?: TypeValidationContext;
    });
    static isInstance(error: unknown): error is TypeValidationError;
    /**
     * Wraps an error into a TypeValidationError.
     * If the cause is already a TypeValidationError with the same value and context, it returns the cause.
     * Otherwise, it creates a new TypeValidationError.
     *
     * @param {Object} params - The parameters for wrapping the error.
     * @param {unknown} params.value - The value that failed validation.
     * @param {unknown} params.cause - The original error or cause of the validation failure.
     * @param {TypeValidationContext} params.context - Optional context about what is being validated.
     * @returns {TypeValidationError} A TypeValidationError instance.
     */
    static wrap({ value, cause, context, }: {
        value: unknown;
        cause: unknown;
        context?: TypeValidationContext;
    }): TypeValidationError;
}

declare class TypeValidationError_2 extends AISDKError_2 {
    private readonly [symbol$1_3];
    readonly value: unknown;
    readonly context?: TypeValidationContext_2;
    constructor({ value, cause, context, }: {
        value: unknown;
        cause: unknown;
        context?: TypeValidationContext_2;
    });
    static isInstance(error: unknown): error is TypeValidationError_2;
    /**
     * Wraps an error into a TypeValidationError.
     * If the cause is already a TypeValidationError with the same value and context, it returns the cause.
     * Otherwise, it creates a new TypeValidationError.
     *
     * @param {Object} params - The parameters for wrapping the error.
     * @param {unknown} params.value - The value that failed validation.
     * @param {unknown} params.cause - The original error or cause of the validation failure.
     * @param {TypeValidationContext} params.context - Optional context about what is being validated.
     * @returns {TypeValidationError} A TypeValidationError instance.
     */
    static wrap({ value, cause, context, }: {
        value: unknown;
        cause: unknown;
        context?: TypeValidationContext_2;
    }): TypeValidationError_2;
}

export declare const UI_MESSAGE_STREAM_HEADERS: {
    'content-type': string;
    'cache-control': string;
    connection: string;
    'x-vercel-ai-ui-message-stream': string;
    'x-accel-buffering': string;
};

export declare type UIDataPartSchemas = Record<string, FlexibleSchema>;

/**
 * The data types that can be used in the UI message for the UI message data parts.
 */
export declare type UIDataTypes = Record<string, unknown>;

declare type UIDataTypesToSchemas<T extends UIDataTypes> = {
    [K in keyof T]: FlexibleSchema<T[K]>;
};

/**
 * AI SDK UI Messages. They are used in the client and to communicate between the frontend and the API routes.
 */
export declare interface UIMessage<METADATA = unknown, DATA_PARTS extends UIDataTypes = UIDataTypes, TOOLS extends UITools = UITools> {
    /**
     * A unique identifier for the message.
     */
    id: string;
    /**
     * The role of the message.
     */
    role: 'system' | 'user' | 'assistant';
    /**
     * The metadata of the message.
     */
    metadata?: METADATA;
    /**
     * The parts of the message. Use this for rendering the message in the UI.
     *
     * System messages should be avoided (set the system prompt on the server instead).
     * They can have text parts.
     *
     * User messages can have text parts and file parts.
     *
     * Assistant messages can have text, reasoning, tool invocation, and file parts.
     */
    parts: Array<UIMessagePart<DATA_PARTS, TOOLS>>;
}

export declare type UIMessageChunk<METADATA = unknown, DATA_TYPES extends UIDataTypes = UIDataTypes> = {
    type: 'text-start';
    id: string;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'text-delta';
    delta: string;
    id: string;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'text-end';
    id: string;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'reasoning-start';
    id: string;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'reasoning-delta';
    id: string;
    delta: string;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'reasoning-end';
    id: string;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'custom';
    kind: `${string}.${string}`;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'error';
    errorText: string;
} | {
    type: 'tool-input-available';
    toolCallId: string;
    toolName: string;
    input: unknown;
    providerExecuted?: boolean;
    providerMetadata?: ProviderMetadata;
    toolMetadata?: JSONObject_2;
    dynamic?: boolean;
    title?: string;
} | {
    type: 'tool-input-error';
    toolCallId: string;
    toolName: string;
    input: unknown;
    providerExecuted?: boolean;
    providerMetadata?: ProviderMetadata;
    toolMetadata?: JSONObject_2;
    dynamic?: boolean;
    errorText: string;
    title?: string;
} | {
    type: 'tool-approval-request';
    approvalId: string;
    toolCallId: string;
    isAutomatic?: boolean;
    signature?: string;
} | {
    type: 'tool-approval-response';
    approvalId: string;
    approved: boolean;
    reason?: string;
    providerExecuted?: boolean;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'tool-output-available';
    toolCallId: string;
    output: unknown;
    providerExecuted?: boolean;
    providerMetadata?: ProviderMetadata;
    toolMetadata?: JSONObject_2;
    dynamic?: boolean;
    preliminary?: boolean;
} | {
    type: 'tool-output-error';
    toolCallId: string;
    errorText: string;
    providerExecuted?: boolean;
    providerMetadata?: ProviderMetadata;
    toolMetadata?: JSONObject_2;
    dynamic?: boolean;
} | {
    type: 'tool-output-denied';
    toolCallId: string;
} | {
    type: 'tool-input-start';
    toolCallId: string;
    toolName: string;
    providerExecuted?: boolean;
    providerMetadata?: ProviderMetadata;
    toolMetadata?: JSONObject_2;
    dynamic?: boolean;
    title?: string;
} | {
    type: 'tool-input-delta';
    toolCallId: string;
    inputTextDelta: string;
} | {
    type: 'source-url';
    sourceId: string;
    url: string;
    title?: string;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'source-document';
    sourceId: string;
    mediaType: string;
    title: string;
    filename?: string;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'file';
    url: string;
    mediaType: string;
    providerMetadata?: ProviderMetadata;
} | {
    type: 'reasoning-file';
    url: string;
    mediaType: string;
    providerMetadata?: ProviderMetadata;
} | DataUIMessageChunk<DATA_TYPES> | {
    type: 'start-step';
} | {
    type: 'finish-step';
} | {
    type: 'start';
    messageId?: string;
    messageMetadata?: METADATA;
} | {
    type: 'finish';
    finishReason?: FinishReason;
    messageMetadata?: METADATA;
} | {
    type: 'abort';
    reason?: string;
} | {
    type: 'message-metadata';
    messageMetadata: METADATA;
};

export declare const uiMessageChunkSchema: _ai_sdk_provider_utils_2.LazySchema<UIMessageChunk<unknown, UIDataTypes>>;

export declare type UIMessagePart<DATA_TYPES extends UIDataTypes, TOOLS extends UITools> = TextUIPart | CustomContentUIPart | ReasoningUIPart | ToolUIPart<TOOLS> | DynamicToolUIPart | SourceUrlUIPart | SourceDocumentUIPart | FileUIPart | ReasoningFileUIPart | DataUIPart<DATA_TYPES> | StepStartUIPart;

/**
 * Error thrown when a UI message stream contains invalid or out-of-sequence chunks.
 *
 * This typically occurs when:
 * - A delta chunk is received without a corresponding start chunk
 * - An end chunk is received without a corresponding start chunk
 * - A tool invocation is not found for the given toolCallId
 *
 * @see https://ai-sdk.dev/docs/reference/ai-sdk-errors/ai-ui-message-stream-error
 */
export declare class UIMessageStreamError extends AISDKError {
    private readonly [symbol$5_2];
    /**
     * The type of chunk that caused the error (e.g., 'text-delta', 'reasoning-end').
     */
    readonly chunkType: string;
    /**
     * The ID associated with the failing chunk (part ID or toolCallId).
     */
    readonly chunkId: string;
    constructor({ chunkType, chunkId, message, }: {
        chunkType: string;
        chunkId: string;
        message: string;
    });
    static isInstance(error: unknown): error is UIMessageStreamError;
}

export declare type UIMessageStreamOnEndCallback<UI_MESSAGE extends UIMessage> = (event: {
    /**
     * The updated list of UI messages.
     */
    messages: UI_MESSAGE[];
    /**
     * Indicates whether the response message is a continuation of the last original message,
     * or if a new message was created.
     */
    isContinuation: boolean;
    /**
     * Indicates whether the stream was aborted.
     */
    isAborted: boolean;
    /**
     * The message that was sent to the client as a response
     * (including the original message if it was extended).
     */
    responseMessage: UI_MESSAGE;
    /**
     * The reason why the generation finished.
     */
    finishReason?: FinishReason;
}) => PromiseLike<void> | void;

/**
 * @deprecated Use `UIMessageStreamOnEndCallback` instead.
 */
export declare type UIMessageStreamOnFinishCallback<UI_MESSAGE extends UIMessage> = UIMessageStreamOnEndCallback<UI_MESSAGE>;

/**
 * Callback that is called when a step ends during streaming.
 * This is useful for persisting intermediate UI messages during multi-step agent runs.
 */
export declare type UIMessageStreamOnStepEndCallback<UI_MESSAGE extends UIMessage> = (event: {
    /**
     * The updated list of UI messages at the end of this step.
     */
    messages: UI_MESSAGE[];
    /**
     * Indicates whether the response message is a continuation of the last original message,
     * or if a new message was created.
     */
    isContinuation: boolean;
    /**
     * The message that was sent to the client as a response
     * (including the original message if it was extended).
     */
    responseMessage: UI_MESSAGE;
}) => PromiseLike<void> | void;

/**
 * Callback that is called when a step finishes during streaming.
 * This is useful for persisting intermediate UI messages during multi-step agent runs.
 *
 * @deprecated Use `UIMessageStreamOnStepEndCallback` instead.
 */
export declare type UIMessageStreamOnStepFinishCallback<UI_MESSAGE extends UIMessage> = UIMessageStreamOnStepEndCallback<UI_MESSAGE>;

export declare type UIMessageStreamOptions<UI_MESSAGE extends UIMessage> = {
    /**
     * The original messages. If they are provided, persistence mode is assumed,
     * and a message ID is provided for the response message.
     */
    originalMessages?: UI_MESSAGE[];
    /**
     * Generate a message ID for the response message.
     *
     * If not provided, no message ID will be set for the response message (unless
     * the original messages are provided and the last message is an assistant message).
     */
    generateMessageId?: IdGenerator;
    onEnd?: UIMessageStreamOnEndCallback<UI_MESSAGE>;
    /**
     * @deprecated Use `onEnd` instead.
     */
    onFinish?: UIMessageStreamOnEndCallback<UI_MESSAGE>;
    /**
     * Extracts message metadata that will be sent to the client.
     *
     * Called on `start` and `finish` events.
     */
    messageMetadata?: (options: {
        part: TextStreamPart<ToolSet>;
    }) => InferUIMessageMetadata<UI_MESSAGE> | undefined;
    /**
     * Send reasoning parts to the client.
     * Default to true.
     */
    sendReasoning?: boolean;
    /**
     * Send source parts to the client.
     * Default to false.
     */
    sendSources?: boolean;
    /**
     * Send the finish event to the client.
     * Set to false if you are using additional streamText calls
     * that send additional data.
     * Default to true.
     */
    sendFinish?: boolean;
    /**
     * Send the message start event to the client.
     * Set to false if you are using additional streamText calls
     * and the message start event has already been sent.
     * Default to true.
     */
    sendStart?: boolean;
    /**
     * Process an error, e.g. to log it. Default to `() => 'An error occurred.'`.
     *
     * @returns error message to include in the data stream.
     */
    onError?: (error: unknown) => string;
};

/**
 * Options for creating a UI message stream response.
 * Extends the standard `ResponseInit` with additional streaming options.
 */
declare type UIMessageStreamResponseInit = ResponseInit & {
    /**
     * Optional callback to consume a copy of the SSE stream independently.
     * This is useful for logging, debugging, or processing the stream in parallel.
     * The callback receives a tee'd copy of the stream and does not block the response.
     */
    consumeSseStream?: (options: {
        stream: ReadableStream<string>;
    }) => PromiseLike<void> | void;
};

export declare interface UIMessageStreamWriter<UI_MESSAGE extends UIMessage = UIMessage> {
    /**
     * Appends a data stream part to the stream.
     */
    write(part: InferUIMessageChunk<UI_MESSAGE>): void;
    /**
     * Merges the contents of another stream to this stream.
     */
    merge(stream: ReadableStream<InferUIMessageChunk<UI_MESSAGE>>): void;
    /**
     * Error handler that is used by the data stream writer.
     * This is intended for forwarding when merging streams
     * to prevent duplicated error masking.
     */
    onError: ErrorHandler | undefined;
}

export declare type UITool = {
    input: unknown;
    output: unknown | undefined;
};

/**
 * A UI tool invocation contains all the information needed to render a tool invocation in the UI.
 * It can be derived from a tool without knowing the tool name, and can be used to define
 * UI components for the tool.
 */
export declare type UIToolInvocation<TOOL extends UITool | Tool> = {
    /**
     * ID of the tool call.
     */
    toolCallId: string;
    title?: string;
    toolMetadata?: JSONObject_2;
    /**
     * Whether the tool call was executed by the provider.
     */
    providerExecuted?: boolean;
} & ({
    state: 'input-streaming';
    input?: DeepPartial<asUITool<TOOL>['input']> | undefined;
    output?: never;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    approval?: never;
} | {
    state: 'input-available';
    input: asUITool<TOOL>['input'];
    output?: never;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    approval?: never;
} | {
    state: 'approval-requested';
    input: asUITool<TOOL>['input'];
    output?: never;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    approval: {
        id: string;
        approved?: never;
        reason?: never;
        isAutomatic?: boolean;
        signature?: string;
    };
} | {
    state: 'approval-responded';
    input: asUITool<TOOL>['input'];
    output?: never;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    approval: {
        id: string;
        approved: boolean;
        reason?: string;
        isAutomatic?: boolean;
        signature?: string;
    };
} | {
    state: 'output-available';
    input: asUITool<TOOL>['input'];
    output: asUITool<TOOL>['output'];
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    resultProviderMetadata?: ProviderMetadata;
    preliminary?: boolean;
    approval?: {
        id: string;
        approved: true;
        reason?: string;
        isAutomatic?: boolean;
        signature?: string;
    };
} | {
    state: 'output-error';
    input: asUITool<TOOL>['input'] | undefined;
    rawInput?: unknown;
    output?: never;
    errorText: string;
    callProviderMetadata?: ProviderMetadata;
    resultProviderMetadata?: ProviderMetadata;
    approval?: {
        id: string;
        approved: true;
        reason?: string;
        isAutomatic?: boolean;
        signature?: string;
    };
} | {
    state: 'output-denied';
    input: asUITool<TOOL>['input'];
    output?: never;
    errorText?: never;
    callProviderMetadata?: ProviderMetadata;
    approval: {
        id: string;
        approved: false;
        reason?: string;
        isAutomatic?: boolean;
        signature?: string;
    };
});

export declare type UITools = Record<string, UITool>;

export declare class UnsupportedFunctionalityError extends AISDKError {
    private readonly [symbol];
    readonly functionality: string;
    constructor({ functionality, message, }: {
        functionality: string;
        message?: string;
    });
    static isInstance(error: unknown): error is UnsupportedFunctionalityError;
}

/**
 * Error that is thrown when a model with an unsupported version is used.
 */
export declare class UnsupportedModelVersionError extends AISDKError {
    readonly version: string;
    readonly provider: string;
    readonly modelId: string;
    constructor(options: {
        version: string;
        provider: string;
        modelId: string;
    });
}

/**
 * Uploads a file using a files API interface.
 *
 * @param api - The Files API interface to use for uploading.
 * @param data - The file data to upload (tagged `{ type: 'data' | 'text' }`).
 * @param mediaType - Optional IANA media type. Auto-detected from file bytes
 * when omitted (falls back to `text/plain` for the `text` variant).
 * @param filename - Optional filename for the uploaded file.
 * @param providerOptions - Additional provider-specific options.
 *
 * @returns A result object containing the provider reference and optional metadata.
 */
export declare function uploadFile({ api, data: dataArg, mediaType: mediaTypeArg, filename, providerOptions, }: {
    /**
     * The files API interface to use for uploading.
     * Can be a `FilesV4` instance or a `ProviderV4` instance with a `files()` method.
     */
    api: FilesV4_2 | ProviderV4_2;
} & Omit<FilesV4UploadFileCallOptions_2, 'mediaType' | 'data'> & {
    /**
     * The file data. Accepts the tagged `{ type: 'data' | 'text' }` shapes, or
     * the shorthand `Uint8Array | string` (treated as `{ type: 'data', data }`).
     */
    data: FilesV4UploadFileCallOptions_2['data'] | Uint8Array | string;
    /**
     * Optional IANA media type of the file. Auto-detected from file bytes when
     * omitted; falls back to `text/plain` for the `text` variant.
     */
    mediaType?: string;
}): Promise<UploadFileResult>;

export declare interface UploadFileResult {
    readonly providerReference: ProviderReference;
    readonly mediaType?: string;
    readonly filename?: string;
    readonly providerMetadata?: ProviderMetadata;
    readonly warnings: Array<Warning>;
}

export declare function uploadSkill({ api, files, displayTitle, providerOptions, }: {
    api: SkillsV4_2 | ProviderV4_2;
} & Omit<SkillsV4UploadSkillCallOptions_2, 'files'> & {
    files: UploadSkillFile[];
}): Promise<UploadSkillResult>;

declare type UploadSkillFile = Omit<SkillsV4File_2, 'data'> & {
    /**
     * The file data. Accepts the tagged `{ type: 'data' | 'text' }` shapes, or
     * the shorthand `Uint8Array | string` (treated as `{ type: 'data', data }`).
     */
    data: SkillsV4File_2['data'] | Uint8Array | string;
};

export declare type UploadSkillResult = Omit<SkillsV4UploadSkillResult_2, 'providerReference' | 'warnings'> & {
    readonly providerReference: ProviderReference;
    readonly warnings: Warning[];
};

export declare type UseCompletionOptions = {
    /**
     * The API endpoint that accepts a `{ prompt: string }` object and returns
     * a stream of tokens of the AI completion response. Defaults to `/api/completion`.
     */
    api?: string;
    /**
     * A unique identifier for the completion. If not provided, a random one will be
     * generated. When provided, the `useCompletion` hook with the same `id` will
     * have shared states across components.
     */
    id?: string;
    /**
     * Initial prompt input of the completion.
     */
    initialInput?: string;
    /**
     * Initial completion result. Useful to load an existing history.
     */
    initialCompletion?: string;
    /**
     * Callback function to be called when the completion is finished streaming.
     */
    onFinish?: (prompt: string, completion: string) => void;
    /**
     * Callback function to be called when an error is encountered.
     */
    onError?: (error: Error) => void;
    /**
     * The credentials mode to be used for the fetch request.
     * Possible values are: 'omit', 'same-origin', 'include'.
     * Defaults to 'same-origin'.
     */
    credentials?: RequestCredentials;
    /**
     * HTTP headers to be sent with the API request.
     */
    headers?: Record<string, string> | Headers;
    /**
     * Extra body object to be sent with the API request.
     * @example
     * Send a `sessionId` to the API along with the prompt.
     * ```js
     * useCompletion({
     *   body: {
     *     sessionId: '123',
     *   }
     * })
     * ```
     */
    body?: object;
    /**
     * Streaming protocol that is used. Defaults to `data`.
     */
    streamProtocol?: 'data' | 'text';
    /**
     * Custom fetch implementation. You can use it as a middleware to intercept requests,
     * or to provide a custom fetch implementation for e.g. testing.
     */
    fetch?: FetchFunction_2;
};

/**
 * Content of a user message. It can be a string or an array of text and image parts.
 */
export declare type UserContent = string | Array<TextPart | ImagePart | FilePart>;

/**
 * Content of a user message. It can be a string or an array of text and image parts.
 */
declare type UserContent_2 = string | Array<TextPart_2 | ImagePart_2 | FilePart_2>;

/**
 * A user message. It can contain text or a combination of text and images.
 */
export declare type UserModelMessage = {
    role: 'user';
    content: UserContent;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions_2;
};

/**
 * A user message. It can contain text or a combination of text and images.
 */
declare type UserModelMessage_2 = {
    role: 'user';
    content: UserContent_2;
    /**
     * Additional provider-specific metadata. They are passed through
     * to the provider from the AI SDK and enable provider-specific
     * functionality that can be fully encapsulated in the provider.
     */
    providerOptions?: ProviderOptions;
};

export declare const userModelMessageSchema: z.ZodType<UserModelMessage>;

declare function validateBaseURL(baseURL: string | undefined): string | undefined;

declare function validateBaseURL_2(baseURL: string | undefined): string | undefined;

/**
 * Validates that a URL is safe to download from, blocking private/internal addresses
 * to prevent SSRF attacks.
 *
 * Note: this performs string/literal-IP checks only. It does not resolve DNS, so a
 * hostname that resolves to a private address is not blocked here (see callers, which
 * should additionally constrain egress at the network layer when handling untrusted URLs).
 *
 * @param url - The URL string to validate.
 * @throws DownloadError if the URL is unsafe.
 */
declare function validateDownloadUrl(url: string): void;

/**
 * Validates that a URL is safe to download from, blocking private/internal addresses
 * to prevent SSRF attacks.
 *
 * Note: this function performs string/literal-IP checks only. The Node.js
 * download fetch additionally validates and pins DNS results at connect time.
 *
 * @param url - The URL string to validate.
 * @throws DownloadError if the URL is unsafe.
 */
declare function validateDownloadUrl_2(url: string): void;

/**
 * Validates the types of an unknown object using a schema and
 * return a strongly-typed object.
 *
 * @template T - The type of the object to validate.
 * @param {string} options.value - The object to validate.
 * @param {Validator<T>} options.schema - The schema to use for validating the JSON.
 * @param {TypeValidationContext} options.context - Optional context about what is being validated.
 * @returns {Promise<T>} - The typed object.
 */
declare function validateTypes<OBJECT>({ value, schema, context, }: {
    value: unknown;
    schema: FlexibleSchema_2<OBJECT>;
    context?: TypeValidationContext_2;
}): Promise<OBJECT>;

/**
 * Validates the types of an unknown object using a schema and
 * return a strongly-typed object.
 *
 * @template T - The type of the object to validate.
 * @param {string} options.value - The object to validate.
 * @param {Validator<T>} options.schema - The schema to use for validating the JSON.
 * @param {TypeValidationContext} options.context - Optional context about what is being validated.
 * @returns {Promise<T>} - The typed object.
 */
declare function validateTypes_2<OBJECT>({ value, schema, context, }: {
    value: unknown;
    schema: FlexibleSchema<OBJECT>;
    context?: TypeValidationContext_2;
}): Promise<OBJECT>;

/**
 * Validates a list of UI messages.
 *
 * Metadata, data parts, and generic tool call structures are only validated if
 * the corresponding schemas are provided. Otherwise, they are assumed to be
 * valid.
 */
export declare function validateUIMessages<UI_MESSAGE extends UIMessage>({ messages, metadataSchema, dataSchemas, tools, }: {
    messages: unknown;
    metadataSchema?: FlexibleSchema<UIMessage['metadata']>;
    dataSchemas?: {
        [NAME in keyof InferUIMessageData<UI_MESSAGE> & string]?: FlexibleSchema<InferUIMessageData<UI_MESSAGE>[NAME]>;
    };
    tools?: {
        [NAME in keyof InferUIMessageTools<UI_MESSAGE> & string]?: Tool<InferUIMessageTools<UI_MESSAGE>[NAME]['input'], InferUIMessageTools<UI_MESSAGE>[NAME]['output']>;
    };
}): Promise<Array<UI_MESSAGE>>;

declare type ValidationResult<OBJECT> = {
    success: true;
    value: OBJECT;
} | {
    success: false;
    error: Error;
};

declare type ValidationResult_2<OBJECT> = {
    success: true;
    value: OBJECT;
} | {
    success: false;
    error: Error;
};

/**
 * Create a union of the given object's values, and optionally specify which keys to get the values from.
 *
 * Please upvote [this issue](https://github.com/microsoft/TypeScript/issues/31438) if you want to have this type as a built-in in TypeScript.
 *
 * @example
 * ```
 * // data.json
 * {
 * 	'foo': 1,
 * 	'bar': 2,
 * 	'biz': 3
 * }
 *
 * // main.ts
 * import type {ValueOf} from 'type-fest';
 * import data = require('./data.json');
 *
 * export function getData(name: string): ValueOf<typeof data> {
 * 	return data[name];
 * }
 *
 * export function onlyBar(name: string): ValueOf<typeof data, 'bar'> {
 * 	return data[name];
 * }
 *
 * // file.ts
 * import {getData, onlyBar} from './main';
 *
 * getData('foo');
 * //=> 1
 *
 * onlyBar('foo');
 * //=> TypeError ...
 *
 * onlyBar('bar');
 * //=> 2
 * ```
 * @see https://github.com/sindresorhus/type-fest/blob/main/source/value-of.d.ts
 */
declare type ValueOf<ObjectType, ValueType extends keyof ObjectType = keyof ObjectType> = ObjectType[ValueType];

declare const VERSION: string;

declare const VERSION_2: string;

/**
 * A video model can be a string (model ID) or a video model object.
 */
declare type VideoModel = string | VideoModelV4_2 | VideoModelV3;

declare type VideoModelProviderMetadata = SharedV4ProviderMetadata_2;

/**
 * Response metadata for a video model call.
 */
declare type VideoModelResponseMetadata = {
    /**
     * Timestamp for the start of the generated response.
     */
    timestamp: Date;
    /**
     * The ID of the response model that was used to generate the response.
     */
    modelId: string;
    /**
     * Response headers.
     */
    headers?: Record<string, string>;
    /**
     * Provider-specific metadata for this call.
     * When multiple calls are made (n > maxVideosPerCall), each response
     * contains its own providerMetadata, allowing lossless per-call access.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
};

/**
 * Video generation model specification version 3.
 */
declare type VideoModelV3 = {
    /**
     * The video model must specify which video model interface
     * version it implements. This will allow us to evolve the video
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v3';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many videos can be generated in a single API call.
     * Can be set to a number for a fixed limit, to undefined to use
     * the global limit, or a function that returns a number or undefined,
     * optionally as a promise.
     *
     * Most video models only support generating 1 video at a time due to
     * computational cost. Default is typically 1.
     */
    readonly maxVideosPerCall: number | undefined | GetMaxVideosPerCallFunction;
    /**
     * Generates an array of videos.
     */
    doGenerate(options: VideoModelV3CallOptions): PromiseLike<{
        /**
         * Generated videos as URLs, base64 strings, or binary data.
         *
         * Most providers return URLs to video files (MP4, WebM) due to large file sizes.
         * Use the discriminated union to indicate the type of video data being returned.
         */
        videos: Array<VideoModelV3VideoData>;
        /**
         * Warnings for the call, e.g. unsupported features.
         */
        warnings: Array<SharedV3Warning>;
        /**
         * Additional provider-specific metadata. They are passed through
         * from the provider to the AI SDK and enable provider-specific
         * results that can be fully encapsulated in the provider.
         *
         * The outer record is keyed by the provider name, and the inner
         * record is provider-specific metadata.
         *
         * ```ts
         * {
         *   "fal": {
         *     "videos": [{
         *       "duration": 5.0,
         *       "fps": 24,
         *       "width": 1280,
         *       "height": 720
         *     }]
         *   }
         * }
         * ```
         */
        providerMetadata?: SharedV3ProviderMetadata;
        /**
         * Response information for telemetry and debugging purposes.
         */
        response: {
            /**
             * Timestamp for the start of the generated response.
             */
            timestamp: Date;
            /**
             * The ID of the response model that was used to generate the response.
             */
            modelId: string;
            /**
             * Response headers.
             */
            headers: Record<string, string> | undefined;
        };
    }>;
};

declare type VideoModelV3CallOptions = {
    /**
     * Text prompt for the video generation.
     */
    prompt: string | undefined;
    /**
     * Number of videos to generate. Default: 1.
     * Most video models only support n=1 due to computational cost.
     */
    n: number;
    /**
     * Aspect ratio of the videos to generate.
     * Must have the format `{width}:{height}`.
     * `undefined` will use the provider's default aspect ratio.
     * Common values: '16:9', '9:16', '1:1', '21:9', '4:3'
     */
    aspectRatio: `${number}:${number}` | undefined;
    /**
     * Resolution of the video to generate.
     * Format: `{width}x{height}` (e.g., '1280x720', '1920x1080')
     * `undefined` will use the provider's default resolution.
     */
    resolution: `${number}x${number}` | undefined;
    /**
     * Duration of the video in seconds.
     * `undefined` will use the provider's default duration.
     * Typically 3-10 seconds for most models.
     */
    duration: number | undefined;
    /**
     * Frames per second (FPS) for the video.
     * `undefined` will use the provider's default FPS.
     * Common values: 24, 30, 60
     */
    fps: number | undefined;
    /**
     * Seed for deterministic video generation.
     * `undefined` will use a random seed.
     */
    seed: number | undefined;
    /**
     * Input image for image-to-video generation.
     * The image serves as the starting frame that the model will animate.
     */
    image: VideoModelV3File | undefined;
    /**
     * Role-tagged image inputs for first-last-frame generation.
     * Each entry declares whether it is the `first_frame` or the
     * `last_frame` of the generated video.
     */
    frameImages: Array<VideoModelV3FrameImage> | undefined;
    /**
     * Reference inputs for reference-to-video generation.
     *
     * Each entry is an image or video file. Providers route each reference by
     * its media type (image vs. video) and warn when a reference kind is
     * unsupported.
     */
    inputReferences: Array<VideoModelV3File> | undefined;
    /**
     * Whether the model should generate audio alongside the video.
     */
    generateAudio: boolean | undefined;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * Example:
     * {
     *   "fal": {
     *     "loop": true,
     *     "motionStrength": 0.8
     *   }
     * }
     */
    providerOptions: SharedV3ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

/**
 * A video or image file that can be used for video editing or image-to-video generation.
 * Supports both image inputs (for image-to-video) and video inputs (for editing).
 */
declare type VideoModelV3File = {
    type: 'file';
    /**
     * The IANA media type of the file.
     * Video types: 'video/mp4', 'video/webm', 'video/quicktime'
     * Image types: 'image/png', 'image/jpeg', 'image/webp'
     */
    mediaType: string;
    /**
     * File data as base64 encoded string or binary data.
     */
    data: string | Uint8Array;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV3ProviderMetadata;
} | {
    type: 'url';
    /**
     * The URL of the video or image file.
     */
    url: string;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV3ProviderMetadata;
};

/**
 * A role-tagged image input for image-to-video and first-last-frame generation.
 */
declare type VideoModelV3FrameImage = {
    /**
     * The image file used for this frame.
     */
    image: VideoModelV3File;
    /**
     * Which frame this image represents.
     */
    frameType: VideoModelV3FrameType;
};

/**
 * The role a frame image plays in video generation.
 *
 * - `first_frame`: the starting frame the model animates from
 * - `last_frame`: the ending frame the model animates towards
 */
declare type VideoModelV3FrameType = 'first_frame' | 'last_frame';

/**
 * Generated video data. Can be a URL, base64-encoded string, or binary data.
 */
declare type VideoModelV3VideoData = {
    /**
     * Video available as a URL (most common for video providers).
     */
    type: 'url';
    url: string;
    mediaType: string;
} | {
    /**
     * Video as base64-encoded string.
     */
    type: 'base64';
    data: string;
    mediaType: string;
} | {
    /**
     * Video as binary data.
     */
    type: 'binary';
    data: Uint8Array;
    mediaType: string;
};

/**
 * Video generation model specification version 4.
 */
declare type VideoModelV4 = {
    /**
     * The video model must specify which video model interface
     * version it implements. This will allow us to evolve the video
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many videos can be generated in a single API call.
     * Can be set to a number for a fixed limit, to undefined to use
     * the global limit, or a function that returns a number or undefined,
     * optionally as a promise.
     *
     * Most video models only support generating 1 video at a time due to
     * computational cost. Default is typically 1.
     */
    readonly maxVideosPerCall: number | undefined | GetMaxVideosPerCallFunction$1;
    /**
     * Generates an array of videos.
     */
    doGenerate(options: VideoModelV4CallOptions): PromiseLike<VideoModelV4Result>;
};

/**
 * Video generation model specification version 4.
 */
declare type VideoModelV4_2 = {
    /**
     * The video model must specify which video model interface
     * version it implements. This will allow us to evolve the video
     * model interface and retain backwards compatibility. The different
     * implementation versions can be handled as a discriminated union
     * on our side.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Limit of how many videos can be generated in a single API call.
     * Can be set to a number for a fixed limit, to undefined to use
     * the global limit, or a function that returns a number or undefined,
     * optionally as a promise.
     *
     * Most video models only support generating 1 video at a time due to
     * computational cost. Default is typically 1.
     */
    readonly maxVideosPerCall: number | undefined | GetMaxVideosPerCallFunction$1_2;
    /**
     * Generates an array of videos.
     *
     * Optional when `doStart` and `doStatus` are provided to support
     * the asynchronous start/status flow.
     */
    doGenerate?(options: VideoModelV4CallOptions_2): PromiseLike<VideoModelV4Result_2>;
    /**
     * Optional method that handles the user's `webhook` option for the
     * asynchronous start/status flow.
     *
     * Its presence on the model signals that the provider's API natively
     * supports webhooks. The SDK checks for this method before invoking the
     * user-provided `webhook` factory:
     *
     * - **Present**: The SDK calls this method with the user's webhook factory.
     *   The implementation should invoke the factory to obtain a webhook URL
     *   and a `received` promise. The URL is then forwarded to `doStart` via
     *   `webhookUrl`, and the SDK awaits `received` instead of polling.
     *
     * - **Absent**: The SDK never calls the user's `webhook` factory and falls
     *   back to polling via `doStatus`. This avoids unnecessary webhook
     *   endpoint creation for providers whose APIs have no native webhook
     *   mechanism.
     *
     * This method exists because the SDK must decide whether to invoke the
     * user's webhook factory — which may create real HTTP endpoints or
     * external resources — *before* calling `doStart`. Without an explicit
     * capability signal on the model, the SDK would eagerly create a webhook
     * endpoint for every provider, even those that silently ignore the URL.
     */
    handleWebhookOption?: (options: {
        webhook: () => PromiseLike<{
            url: string;
            received: PromiseLike<VideoModelV4OperationWebhook>;
        }>;
    }) => PromiseLike<{
        webhookUrl: string;
        received: PromiseLike<VideoModelV4OperationWebhook>;
    }>;
    /**
     * Starts an asynchronous video generation and returns an opaque operation
     * reference that can be passed to `doStatus` to poll for completion.
     *
     * When both `doStart` and `doStatus` are implemented, the SDK core can
     * orchestrate polling or webhook-based completion instead of requiring
     * the provider to implement its own polling loop in `doGenerate`.
     */
    doStart?(options: VideoModelV4CallOptions_2 & {
        /**
         * When provided, the provider should register this URL to receive
         * a webhook notification when the video generation completes.
         */
        webhookUrl?: string;
    }): PromiseLike<VideoModelV4OperationStartResult>;
    /**
     * Checks the status of an asynchronous video generation that was
     * started with `doStart`.
     *
     * Returns either a `pending` status or a `completed` status with the
     * generated videos.
     */
    doStatus?(options: {
        /**
         * The JSON-serializable opaque operation reference returned by `doStart`.
         */
        operation: JSONValue_3;
        /**
         * Abort signal for cancelling the operation.
         */
        abortSignal?: AbortSignal;
        /**
         * Additional HTTP headers.
         */
        headers?: Record<string, string | undefined>;
    }): PromiseLike<VideoModelV4OperationStatusResult>;
};

declare type VideoModelV4CallOptions = {
    /**
     * Text prompt for the video generation.
     */
    prompt: string | undefined;
    /**
     * Number of videos to generate. Default: 1.
     * Most video models only support n=1 due to computational cost.
     */
    n: number;
    /**
     * Aspect ratio of the videos to generate.
     * Must have the format `{width}:{height}`.
     * `undefined` will use the provider's default aspect ratio.
     * Common values: '16:9', '9:16', '1:1', '21:9', '4:3'
     */
    aspectRatio: `${number}:${number}` | undefined;
    /**
     * Resolution of the video to generate.
     * Format: `{width}x{height}` (e.g., '1280x720', '1920x1080')
     * `undefined` will use the provider's default resolution.
     */
    resolution: `${number}x${number}` | undefined;
    /**
     * Duration of the video in seconds.
     * `undefined` will use the provider's default duration.
     * Typically 3-10 seconds for most models.
     */
    duration: number | undefined;
    /**
     * Frames per second (FPS) for the video.
     * `undefined` will use the provider's default FPS.
     * Common values: 24, 30, 60
     */
    fps: number | undefined;
    /**
     * Seed for deterministic video generation.
     * `undefined` will use a random seed.
     */
    seed: number | undefined;
    /**
     * Input image for image-to-video generation.
     * The image serves as the starting frame that the model will animate.
     */
    image: VideoModelV4File | undefined;
    /**
     * Role-tagged image inputs for first-last-frame generation.
     * Each entry declares whether it is the `first_frame` or the
     * `last_frame` of the generated video.
     */
    frameImages: Array<VideoModelV4FrameImage> | undefined;
    /**
     * Reference inputs for reference-to-video generation.
     *
     * Each entry is an image or video file. Providers route each reference by
     * its media type (image vs. video) and warn when a reference kind is
     * unsupported.
     */
    inputReferences: Array<VideoModelV4File> | undefined;
    /**
     * Whether the model should generate audio alongside the video.
     */
    generateAudio: boolean | undefined;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * Example:
     * {
     *   "fal": {
     *     "loop": true,
     *     "motionStrength": 0.8
     *   }
     * }
     */
    providerOptions: SharedV4ProviderOptions;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

declare type VideoModelV4CallOptions_2 = {
    /**
     * Text prompt for the video generation.
     */
    prompt: string | undefined;
    /**
     * Number of videos to generate. Default: 1.
     * Most video models only support n=1 due to computational cost.
     */
    n: number;
    /**
     * Aspect ratio of the videos to generate.
     * Must have the format `{width}:{height}`.
     * `undefined` will use the provider's default aspect ratio.
     * Common values: '16:9', '9:16', '1:1', '21:9', '4:3'
     */
    aspectRatio: `${number}:${number}` | undefined;
    /**
     * Resolution of the video to generate.
     * Format: `{width}x{height}` (e.g., '1280x720', '1920x1080')
     * `undefined` will use the provider's default resolution.
     */
    resolution: `${number}x${number}` | undefined;
    /**
     * Duration of the video in seconds.
     * `undefined` will use the provider's default duration.
     * Typically 3-10 seconds for most models.
     */
    duration: number | undefined;
    /**
     * Frames per second (FPS) for the video.
     * `undefined` will use the provider's default FPS.
     * Common values: 24, 30, 60
     */
    fps: number | undefined;
    /**
     * Seed for deterministic video generation.
     * `undefined` will use a random seed.
     */
    seed: number | undefined;
    /**
     * Input image for image-to-video generation.
     * The image serves as the starting frame that the model will animate.
     */
    image: VideoModelV4File_2 | undefined;
    /**
     * Role-tagged image inputs for first-last-frame generation.
     * Each entry declares whether it is the `first_frame` or the
     * `last_frame` of the generated video.
     */
    frameImages: Array<VideoModelV4FrameImage_2> | undefined;
    /**
     * Reference inputs for reference-to-video generation.
     *
     * Each entry is an image or video file. Providers route each reference by
     * its media type (image vs. video) and warn when a reference kind is
     * unsupported.
     */
    inputReferences: Array<VideoModelV4File_2> | undefined;
    /**
     * Whether the model should generate audio alongside the video.
     */
    generateAudio: boolean | undefined;
    /**
     * Additional provider-specific options that are passed through to the provider
     * as body parameters.
     *
     * Example:
     * {
     *   "fal": {
     *     "loop": true,
     *     "motionStrength": 0.8
     *   }
     * }
     */
    providerOptions: SharedV4ProviderOptions_2;
    /**
     * Abort signal for cancelling the operation.
     */
    abortSignal?: AbortSignal;
    /**
     * Additional HTTP headers to be sent with the request.
     * Only applicable for HTTP-based providers.
     */
    headers?: Record<string, string | undefined>;
};

/**
 * A video or image file that can be used for video editing or image-to-video generation.
 * Supports both image inputs (for image-to-video) and video inputs (for editing).
 */
declare type VideoModelV4File = {
    type: 'file';
    /**
     * The IANA media type of the file.
     * Video types: 'video/mp4', 'video/webm', 'video/quicktime'
     * Image types: 'image/png', 'image/jpeg', 'image/webp'
     */
    mediaType: string;
    /**
     * File data as base64 encoded string or binary data.
     */
    data: string | Uint8Array;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV4ProviderMetadata;
} | {
    type: 'url';
    /**
     * The URL of the video or image file.
     */
    url: string;
    /**
     * The media type of the referenced file, when known.
     * Video types: 'video/mp4', 'video/webm', 'video/quicktime'
     * Image types: 'image/png', 'image/jpeg', 'image/webp'
     */
    mediaType?: string;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV4ProviderMetadata;
};

/**
 * A video or image file that can be used for video editing or image-to-video generation.
 * Supports both image inputs (for image-to-video) and video inputs (for editing).
 */
declare type VideoModelV4File_2 = {
    type: 'file';
    /**
     * The IANA media type of the file.
     * Video types: 'video/mp4', 'video/webm', 'video/quicktime'
     * Image types: 'image/png', 'image/jpeg', 'image/webp'
     */
    mediaType: string;
    /**
     * File data as base64 encoded string or binary data.
     */
    data: string | Uint8Array;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV4ProviderMetadata_2;
} | {
    type: 'url';
    /**
     * The URL of the video or image file.
     */
    url: string;
    /**
     * The media type of the referenced file, when known.
     * Video types: 'video/mp4', 'video/webm', 'video/quicktime'
     * Image types: 'image/png', 'image/jpeg', 'image/webp'
     */
    mediaType?: string;
    /**
     * Optional provider-specific metadata for the file part.
     */
    providerOptions?: SharedV4ProviderMetadata_2;
};

/**
 * A role-tagged image input for image-to-video and first-last-frame generation.
 */
declare type VideoModelV4FrameImage = {
    /**
     * The image file used for this frame.
     */
    image: VideoModelV4File;
    /**
     * Which frame this image represents.
     */
    frameType: VideoModelV4FrameType;
};

/**
 * A role-tagged image input for image-to-video and first-last-frame generation.
 */
declare type VideoModelV4FrameImage_2 = {
    /**
     * The image file used for this frame.
     */
    image: VideoModelV4File_2;
    /**
     * Which frame this image represents.
     */
    frameType: VideoModelV4FrameType_2;
};

/**
 * The role a frame image plays in video generation.
 *
 * - `first_frame`: the starting frame the model animates from
 * - `last_frame`: the ending frame the model animates towards
 */
declare type VideoModelV4FrameType = 'first_frame' | 'last_frame';

/**
 * The role a frame image plays in video generation.
 *
 * - `first_frame`: the starting frame the model animates from
 * - `last_frame`: the ending frame the model animates towards
 */
declare type VideoModelV4FrameType_2 = 'first_frame' | 'last_frame';

/**
 * Result returned by `doStart` when initiating an asynchronous video generation.
 */
declare type VideoModelV4OperationStartResult = {
    /**
     * JSON-serializable opaque reference passed to `doStatus` to check the
     * status of the generation (e.g., a task ID or prediction URL).
     */
    operation: JSONValue_3;
    /**
     * Warnings for the call, e.g. unsupported features.
     */
    warnings: Array<SharedV4Warning_2>;
    /**
     * Additional provider-specific metadata.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        /**
         * Timestamp for the start of the response.
         */
        timestamp: Date;
        /**
         * The ID of the response model that was used.
         */
        modelId: string;
        /**
         * Response headers.
         */
        headers: Record<string, string> | undefined;
    };
};

/**
 * Result returned by `doStatus` when checking the status of an
 * asynchronous video generation.
 */
declare type VideoModelV4OperationStatusResult = {
    /**
     * The video generation is still in progress.
     */
    status: 'pending';
    /**
     * Warnings for the call.
     */
    warnings?: Array<SharedV4Warning_2>;
    /**
     * Additional provider-specific metadata.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        timestamp: Date;
        modelId: string;
        headers: Record<string, string> | undefined;
    };
} | {
    /**
     * The video generation is complete.
     */
    status: 'completed';
    /**
     * Generated videos.
     */
    videos: Array<VideoModelV4VideoData_2>;
    /**
     * Warnings for the call.
     */
    warnings: Array<SharedV4Warning_2>;
    /**
     * Additional provider-specific metadata.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        timestamp: Date;
        modelId: string;
        headers: Record<string, string> | undefined;
    };
} | {
    /**
     * The video generation failed.
     */
    status: 'error';
    /**
     * A human-readable error message describing why the generation failed.
     */
    error: string;
    /**
     * Additional provider-specific metadata.
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        timestamp: Date;
        modelId: string;
        headers: Record<string, string> | undefined;
    };
};

/**
 * Data received from a webhook notification during asynchronous video
 * generation. Generic over the body type so providers/consumers can
 * narrow it to a specific shape.
 */
declare type VideoModelV4OperationWebhook<TBody = JSONValue_3> = {
    headers: Record<string, string>;
    body: TBody;
};

/**
 * The result of a video model doGenerate call.
 */
declare type VideoModelV4Result = {
    /**
     * Generated videos as URLs, base64 strings, or binary data.
     *
     * Most providers return URLs to video files (MP4, WebM) due to large file sizes.
     * Use the discriminated union to indicate the type of video data being returned.
     */
    videos: Array<VideoModelV4VideoData>;
    /**
     * Warnings for the call, e.g. unsupported features.
     */
    warnings: Array<SharedV4Warning>;
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is provider-specific metadata.
     *
     * ```ts
     * {
     *   "fal": {
     *     "videos": [{
     *       "duration": 5.0,
     *       "fps": 24,
     *       "width": 1280,
     *       "height": 720
     *     }]
     *   }
     * }
     * ```
     */
    providerMetadata?: SharedV4ProviderMetadata;
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        /**
         * Timestamp for the start of the generated response.
         */
        timestamp: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId: string;
        /**
         * Response headers.
         */
        headers: Record<string, string> | undefined;
    };
};

/**
 * The result of a video model doGenerate call.
 */
declare type VideoModelV4Result_2 = {
    /**
     * Generated videos as URLs, base64 strings, or binary data.
     *
     * Most providers return URLs to video files (MP4, WebM) due to large file sizes.
     * Use the discriminated union to indicate the type of video data being returned.
     */
    videos: Array<VideoModelV4VideoData_2>;
    /**
     * Warnings for the call, e.g. unsupported features.
     */
    warnings: Array<SharedV4Warning_2>;
    /**
     * Additional provider-specific metadata. They are passed through
     * from the provider to the AI SDK and enable provider-specific
     * results that can be fully encapsulated in the provider.
     *
     * The outer record is keyed by the provider name, and the inner
     * record is provider-specific metadata.
     *
     * ```ts
     * {
     *   "fal": {
     *     "videos": [{
     *       "duration": 5.0,
     *       "fps": 24,
     *       "width": 1280,
     *       "height": 720
     *     }]
     *   }
     * }
     * ```
     */
    providerMetadata?: SharedV4ProviderMetadata_2;
    /**
     * Response information for telemetry and debugging purposes.
     */
    response: {
        /**
         * Timestamp for the start of the generated response.
         */
        timestamp: Date;
        /**
         * The ID of the response model that was used to generate the response.
         */
        modelId: string;
        /**
         * Response headers.
         */
        headers: Record<string, string> | undefined;
    };
};

/**
 * Generated video data. Can be a URL, base64-encoded string, or binary data.
 */
declare type VideoModelV4VideoData = {
    /**
     * Video available as a URL (most common for video providers).
     */
    type: 'url';
    url: string;
    mediaType: string;
} | {
    /**
     * Video as base64-encoded string.
     */
    type: 'base64';
    data: string;
    mediaType: string;
} | {
    /**
     * Video as binary data.
     */
    type: 'binary';
    data: Uint8Array;
    mediaType: string;
};

/**
 * Generated video data. Can be a URL, base64-encoded string, or binary data.
 */
declare type VideoModelV4VideoData_2 = {
    /**
     * Video available as a URL (most common for video providers).
     */
    type: 'url';
    url: string;
    mediaType: string;
} | {
    /**
     * Video as base64-encoded string.
     */
    type: 'base64';
    data: string;
    mediaType: string;
} | {
    /**
     * Video as binary data.
     */
    type: 'binary';
    data: Uint8Array;
    mediaType: string;
};

/**
 * Waits until the socket's send buffer drains below `highWaterMark` bytes.
 * No-op for implementations that do not expose `bufferedAmount`. There is no
 * portable drain event, so this polls. Returns as soon as the socket is no
 * longer open or the signal aborts — `bufferedAmount` never drains on a
 * closed socket, so waiting on would poll forever.
 */
declare function waitForWebSocketBufferDrain(socket: WebSocketLike, { highWaterMark, pollIntervalMs, abortSignal, }?: {
    highWaterMark?: number;
    pollIntervalMs?: number;
    abortSignal?: AbortSignal;
}): Promise<void>;

/**
 * Waits until the socket's send buffer drains below `highWaterMark` bytes.
 * No-op for implementations that do not expose `bufferedAmount`. There is no
 * portable drain event, so this polls. Returns as soon as the socket is no
 * longer open or the signal aborts — `bufferedAmount` never drains on a
 * closed socket, so waiting on would poll forever.
 */
declare function waitForWebSocketBufferDrain_2(socket: WebSocketLike_2, { highWaterMark, pollIntervalMs, abortSignal, }?: {
    highWaterMark?: number;
    pollIntervalMs?: number;
    abortSignal?: AbortSignal;
}): Promise<void>;

/**
 * Warning from the model provider for this call. The call will proceed, but e.g.
 * some settings might not be supported, which can lead to suboptimal results.
 */
export declare type Warning = SharedV4Warning_2;

declare interface WebSocketConnection {
    /** Undefined when the constructor threw or the signal was already aborted. */
    socket: WebSocketLike | undefined;
    /**
     * Unregisters the abort listener and closes the socket. Never throws.
     * Handlers stay attached; callers guard their own terminal state.
     */
    close: (code?: number) => void;
}

declare interface WebSocketConnection_2 {
    /** Undefined when the constructor threw or the signal was already aborted. */
    socket: WebSocketLike_2 | undefined;
    /**
     * Unregisters the abort listener and closes the socket. Never throws.
     * Handlers stay attached; callers guard their own terminal state.
     */
    close: (code?: number) => void;
}

declare type WebSocketConstructor = new (url: string | URL, protocols?: string | string[], options?: {
    headers?: Record<string, string | undefined>;
}) => WebSocketLike;

declare type WebSocketConstructor_2 = new (url: string | URL, protocols?: string | string[], options?: {
    headers?: Record<string, string | undefined>;
}) => WebSocketLike_2;

declare type WebSocketLike = {
    readyState: number;
    /** Bytes queued by `send` but not yet transmitted (native + `ws`). */
    readonly bufferedAmount?: number;
    send(data: string | Uint8Array | ArrayBuffer): void;
    close(code?: number, reason?: string): void;
    onopen: ((event: unknown) => void) | null;
    onmessage: ((event: {
        data: unknown;
    }) => void) | null;
    onerror: ((event: unknown) => void) | null;
    onclose: ((event: unknown) => void) | null;
};

declare type WebSocketLike_2 = {
    readyState: number;
    /** Bytes queued by `send` but not yet transmitted (native + `ws`). */
    readonly bufferedAmount?: number;
    send(data: string | Uint8Array | ArrayBuffer): void;
    close(code?: number, reason?: string): void;
    onopen: ((event: unknown) => void) | null;
    onmessage: ((event: {
        data: unknown;
    }) => void) | null;
    onerror: ((event: unknown) => void) | null;
    onclose: ((event: unknown) => void) | null;
};

/**
 * Broad end event shape used for the default `ToolSet` specialization.
 *
 * This provides an assignable catch-all event type for generic consumers while
 * the concrete-tool specialization retains full per-tool narrowing.
 */
declare type WidenedToolExecutionEndEvent = BaseToolExecutionEndFields & {
    readonly toolCall: StaticToolCall<ToolSet> | DynamicToolCall;
    readonly toolContext: unknown;
    readonly toolOutput: ToolOutput<ToolSet>;
};

/**
 * Broad start event shape used for the default `ToolSet` specialization.
 *
 * This keeps generic collectors ergonomic when the caller is not working with
 * a concrete tool set and therefore cannot benefit from per-tool narrowing.
 */
declare type WidenedToolExecutionStartEvent = BaseToolExecutionStartFields & {
    readonly toolCall: StaticToolCall<ToolSet> | DynamicToolCall;
    readonly toolContext: unknown;
};

declare function withoutTrailingSlash(url: string | undefined): string | undefined;

declare function withoutTrailingSlash_2(url: string | undefined): string | undefined;

/**
 * Appends suffix parts to the `user-agent` header.
 * If a `user-agent` header already exists, the suffix parts are appended to it.
 * If no `user-agent` header exists, a new one is created with the suffix parts.
 * Automatically removes undefined entries from the headers.
 *
 * @param headers - The original headers.
 * @param userAgentSuffixParts - The parts to append to the `user-agent` header.
 * @returns The new headers with the `user-agent` header set or updated.
 */
declare function withUserAgentSuffix(headers: HeadersInit | Record<string, string | undefined> | undefined, ...userAgentSuffixParts: string[]): Record<string, string>;

/**
 * Appends suffix parts to the `user-agent` header.
 * If a `user-agent` header already exists, the suffix parts are appended to it.
 * If no `user-agent` header exists, a new one is created with the suffix parts.
 * Automatically removes undefined entries from the headers.
 *
 * @param headers - The original headers.
 * @param userAgentSuffixParts - The parts to append to the `user-agent` header.
 * @returns The new headers with the `user-agent` header set or updated.
 */
declare function withUserAgentSuffix_2(headers: HeadersInit | Record<string, string | undefined> | undefined, ...userAgentSuffixParts: string[]): Record<string, string>;

/**
 * Wraps an EmbeddingModelV4 instance with middleware functionality.
 * This function allows you to apply middleware to transform parameters,
 * wrap embed operations of an embedding model.
 *
 * @param options - Configuration options for wrapping the embedding model.
 * @param options.model - The original EmbeddingModelV4 instance to be wrapped.
 * @param options.middleware - The middleware to be applied to the embedding model. When multiple middlewares are provided, the first middleware will transform the input first, and the last middleware will be wrapped directly around the model.
 * @param options.modelId - Optional custom model ID to override the original model's ID.
 * @param options.providerId - Optional custom provider ID to override the original model's provider ID.
 * @returns A new EmbeddingModelV4 instance with middleware applied.
 */
export declare const wrapEmbeddingModel: ({ model: inputModel, middleware: middlewareArg, modelId, providerId, }: {
    model: EmbeddingModelV3 | EmbeddingModelV4_2;
    middleware: EmbeddingModelMiddleware | EmbeddingModelMiddleware[];
    modelId?: string;
    providerId?: string;
}) => EmbeddingModelV4_2;

/**
 * Wraps an ImageModelV4 instance with middleware functionality.
 * This function allows you to apply middleware to transform parameters
 * and wrap generate operations of an image model.
 *
 * @param options - Configuration options for wrapping the image model.
 * @param options.model - The original ImageModelV4 instance to be wrapped.
 * @param options.middleware - The middleware to be applied to the image model. When multiple middlewares are provided, the first middleware will transform the input first, and the last middleware will be wrapped directly around the model.
 * @param options.modelId - Optional custom model ID to override the original model's ID.
 * @param options.providerId - Optional custom provider ID to override the original model's provider ID.
 * @returns A new ImageModelV4 instance with middleware applied.
 */
export declare const wrapImageModel: ({ model: inputModel, middleware: middlewareArg, modelId, providerId, }: {
    model: ImageModelV2 | ImageModelV3 | ImageModelV4_2;
    middleware: ImageModelMiddleware | ImageModelMiddleware[];
    modelId?: string;
    providerId?: string;
}) => ImageModelV4_2;

/**
 * Wraps a LanguageModelV4 instance with middleware functionality.
 * This function allows you to apply middleware to transform parameters,
 * wrap generate operations, and wrap stream operations of a language model.
 *
 * @param options - Configuration options for wrapping the language model.
 * @param options.model - The original LanguageModelV4 instance to be wrapped.
 * @param options.middleware - The middleware to be applied to the language model. When multiple middlewares are provided, the first middleware will transform the input first, and the last middleware will be wrapped directly around the model.
 * @param options.modelId - Optional custom model ID to override the original model's ID.
 * @param options.providerId - Optional custom provider ID to override the original model's provider ID.
 * @returns A new LanguageModelV4 instance with middleware applied.
 */
export declare const wrapLanguageModel: ({ model: inputModel, middleware: middlewareArg, modelId, providerId, }: {
    model: LanguageModelV2 | LanguageModelV3 | LanguageModelV4_2;
    middleware: LanguageModelMiddleware | LanguageModelMiddleware[];
    modelId?: string;
    providerId?: string;
}) => LanguageModelV4_2;

/**
 * Wraps a ProviderV4 instance with middleware functionality.
 * This function allows you to apply middleware to all language models
 * from the provider, enabling you to transform parameters, wrap generate
 * operations, and wrap stream operations for every language model.
 *
 * @param options - Configuration options for wrapping the provider.
 * @param options.provider - The original ProviderV4 instance to be wrapped.
 * @param options.languageModelMiddleware - The middleware to be applied to all language models from the provider. When multiple middlewares are provided, the first middleware will transform the input first, and the last middleware will be wrapped directly around the model.
 * @param options.imageModelMiddleware - Optional middleware to be applied to all image models from the provider. When multiple middlewares are provided, the first middleware will transform the input first, and the last middleware will be wrapped directly around the model.
 * @returns A new ProviderV4 instance with middleware applied to all language models.
 */
export declare function wrapProvider({ provider, languageModelMiddleware, imageModelMiddleware, }: {
    provider: ProviderV4_2 | ProviderV3 | ProviderV2;
    languageModelMiddleware: LanguageModelMiddleware | LanguageModelMiddleware[];
    imageModelMiddleware?: ImageModelMiddleware | ImageModelMiddleware[];
}): ProviderV4_2;

/**
 * Options for writing a file to the sandbox. `CONTENT` is the payload written
 * to the file: a byte stream, raw bytes, or a string.
 */
declare type WriteFileOptions<CONTENT> = {
    /**
     * Path of the file to write.
     */
    path: string;
    /**
     * Content to write to the file.
     */
    content: CONTENT;
    /**
     * Signal that can be used to abort the write.
     */
    abortSignal?: AbortSignal;
};

/**
 * Options for writing a file to the sandbox. `CONTENT` is the payload written
 * to the file: a byte stream, raw bytes, or a string.
 */
declare type WriteFileOptions_2<CONTENT> = {
    /**
     * Path of the file to write.
     */
    path: string;
    /**
     * Content to write to the file.
     */
    content: CONTENT;
    /**
     * Signal that can be used to abort the write.
     */
    abortSignal?: AbortSignal;
};

declare type ZodSchema<SCHEMA = any> = z3.Schema<SCHEMA, z3.ZodTypeDef, any> | z4.core.$ZodType<SCHEMA, any>;

export declare function zodSchema<OBJECT>(zodSchema: z4.core.$ZodType<OBJECT, any> | z3.Schema<OBJECT, z3.ZodTypeDef, any>, options?: {
    /**
     * Enables support for references in the schema.
     * This is required for recursive schemas, e.g. with `z.lazy`.
     * However, not all language models and providers support such references.
     * Defaults to `false`.
     */
    useReferences?: boolean;
}): Schema<OBJECT>;

declare type ZodSchema_2<SCHEMA = any> = z3.Schema<SCHEMA, z3.ZodTypeDef, any> | z4.core.$ZodType<SCHEMA, any>;

declare function zodSchema_2<OBJECT>(zodSchema: z4.core.$ZodType<OBJECT, any> | z3.Schema<OBJECT, z3.ZodTypeDef, any>, options?: {
    /**
     * Enables support for references in the schema.
     * This is required for recursive schemas, e.g. with `z.lazy`.
     * However, not all language models and providers support such references.
     * Defaults to `false`.
     */
    useReferences?: boolean;
}): Schema_2<OBJECT>;

export { }
export { GatewayProviderSettings as GatewayProviderSettings, GatewayProvider as GatewayProvider, FileData_2 as FileData_2, ProviderReference_3 as ProviderReference_3, ToolResultOutput_2 as ToolResultOutput_2, AISDKError_2 as AISDKError_2, LazySchema_2 as LazySchema_2, ZodSchema_2 as ZodSchema_2, StandardSchema_2 as StandardSchema_2, ValidationResult_2 as ValidationResult_2, ParseResult_2 as ParseResult_2, ProviderOptions_2 as ProviderOptions_2, CustomPart_2 as CustomPart_2, ReasoningPart_2 as ReasoningPart_2, ReasoningFilePart_2 as ReasoningFilePart_2, ReadFileOptions_2 as ReadFileOptions_2, WriteFileOptions_2 as WriteFileOptions_2, SandboxProcessOptions_2 as SandboxProcessOptions_2, JSONValue_2 as JSONValue_2, FunctionTool_2 as FunctionTool_2, ProviderDefinedTool_2 as ProviderDefinedTool_2, ProviderExecutedTool_2 as ProviderExecutedTool_2, ExecutableTool_2 as ExecutableTool_2, DynamicTool_2 as DynamicTool_2, ToolCallerDefinition as ToolCallerDefinition, SharedV4ProviderReference as SharedV4ProviderReference, TypeValidationContext as TypeValidationContext, EmbeddingModelV3 as EmbeddingModelV3, EmbeddingModelV2 as EmbeddingModelV2, EmbeddingModelV4Embedding_2 as EmbeddingModelV4Embedding_2, EmbeddingModelV4Middleware as EmbeddingModelV4Middleware, ImageModelV3 as ImageModelV3, ImageModelV2 as ImageModelV2, ImageModelV4ProviderMetadata_2 as ImageModelV4ProviderMetadata_2, ImageModelV2ProviderMetadata as ImageModelV2ProviderMetadata, ImageModelV4Middleware as ImageModelV4Middleware, GlobalProviderModelId as GlobalProviderModelId, LanguageModelV3 as LanguageModelV3, LanguageModelV2 as LanguageModelV2, SharedV4Warning_2 as SharedV4Warning_2, LanguageModelV4Middleware as LanguageModelV4Middleware, RerankingModelV3 as RerankingModelV3, SpeechModelV3 as SpeechModelV3, SpeechModelV2 as SpeechModelV2, TranscriptionModelV3 as TranscriptionModelV3, TranscriptionModelV2 as TranscriptionModelV2, ImageModelV4Usage_2 as ImageModelV4Usage_2, DeepPartialInternal as DeepPartialInternal, IncludedContext as IncludedContext, IncludedToolsContext as IncludedToolsContext, BaseToolCall as BaseToolCall, Source as Source, ValueOf as ValueOf, asUITool as asUITool, _ai_sdk_provider_utils_2 as _ai_sdk_provider_utils_2, DataUIMessageChunk as DataUIMessageChunk, InferElementOutput as InferElementOutput, ConsumeStreamOptions as ConsumeStreamOptions, TextStreamTextStartPart as TextStreamTextStartPart, TextStreamTextEndPart as TextStreamTextEndPart, TextStreamReasoningStartPart as TextStreamReasoningStartPart, TextStreamReasoningEndPart as TextStreamReasoningEndPart, TextStreamCustomPart as TextStreamCustomPart, TextStreamToolInputStartPart as TextStreamToolInputStartPart, TextStreamToolInputEndPart as TextStreamToolInputEndPart, TextStreamToolInputDeltaPart as TextStreamToolInputDeltaPart, TextStreamSourcePart as TextStreamSourcePart, TextStreamToolOutputDeniedPart as TextStreamToolOutputDeniedPart, TextStreamStartStepPart as TextStreamStartStepPart, TextStreamFinishStepPart as TextStreamFinishStepPart, TextStreamStartPart as TextStreamStartPart, TextStreamFinishPart as TextStreamFinishPart, TextStreamAbortPart as TextStreamAbortPart, TextStreamErrorPart as TextStreamErrorPart, TextStreamRawPart as TextStreamRawPart, MaybePromiseLike_2 as MaybePromiseLike_2, InferToolContext_2 as InferToolContext_2, ToolCallerName as ToolCallerName, LanguageModelV4ToolCall_2 as LanguageModelV4ToolCall_2, WidenedToolExecutionStartEvent as WidenedToolExecutionStartEvent, StaticToolExecutionStartEvent as StaticToolExecutionStartEvent, DynamicToolExecutionStartEvent as DynamicToolExecutionStartEvent, WidenedToolExecutionEndEvent as WidenedToolExecutionEndEvent, StaticToolExecutionEndEvent as StaticToolExecutionEndEvent, DynamicToolExecutionEndEvent as DynamicToolExecutionEndEvent, StreamTextOnAbortCallback as StreamTextOnAbortCallback, StandardizedPrompt as StandardizedPrompt, ResponseMessage as ResponseMessage, OperationStartEvent as OperationStartEvent, OperationEndEvent as OperationEndEvent, Arrayable_2 as Arrayable_2, Context_2 as Context_2, Output_2 as Output_2, ToolsContextParameter as ToolsContextParameter, InferAgentTools as InferAgentTools, UIMessageStreamResponseInit as UIMessageStreamResponseInit, getOriginalFetch as getOriginalFetch, InferUIMessageTools as InferUIMessageTools, InferUIMessageToolCall as InferUIMessageToolCall, UIDataTypesToSchemas as UIDataTypesToSchemas, InferUIMessageData as InferUIMessageData, InferUIMessageMetadata as InferUIMessageMetadata, Resolvable_2 as Resolvable_2, FetchFunction_2 as FetchFunction_2, Callback as Callback, TextStreamTextDeltaPart as TextStreamTextDeltaPart, TextStreamReasoningDeltaPart as TextStreamReasoningDeltaPart, TextStreamFilePart as TextStreamFilePart, TextStreamReasoningFilePart as TextStreamReasoningFilePart, TextStreamToolApprovalRequestPart as TextStreamToolApprovalRequestPart, TextStreamToolApprovalResponsePart as TextStreamToolApprovalResponsePart, TextStreamToolCallPart as TextStreamToolCallPart, TextStreamToolResultPart as TextStreamToolResultPart, TextStreamToolErrorPart as TextStreamToolErrorPart, LanguageModelV4Prompt_2 as LanguageModelV4Prompt_2, SharedV4Headers_2 as SharedV4Headers_2, RetryErrorReason_2 as RetryErrorReason_2, ActiveToolSubset as ActiveToolSubset, GenerateImagePrompt as GenerateImagePrompt, JSONValue_3 as JSONValue_3, Job as Job, StreamObjectOnErrorCallback as StreamObjectOnErrorCallback, VideoModelResponseMetadata as VideoModelResponseMetadata, VideoModelProviderMetadata as VideoModelProviderMetadata, VideoModelV4FrameType_2 as VideoModelV4FrameType_2, GenerateVideoPollOptions as GenerateVideoPollOptions, GenerateVideoWebhookFactory as GenerateVideoWebhookFactory, EmbeddingModelV4CallOptions_2 as EmbeddingModelV4CallOptions_2, LanguageModelV4CallOptions_2 as LanguageModelV4CallOptions_2, JSONObject_2 as JSONObject_2, RealtimeFactoryV4_2 as RealtimeFactoryV4_2, RealtimeFactoryV4GetTokenOptions_2 as RealtimeFactoryV4GetTokenOptions_2, RealtimeFactoryV4GetTokenResult_2 as RealtimeFactoryV4GetTokenResult_2, RealtimeModelV4_2 as RealtimeModelV4_2, RealtimeModelV4ClientEvent_2 as RealtimeModelV4ClientEvent_2, RealtimeModelV4ServerEvent_2 as RealtimeModelV4ServerEvent_2, RealtimeModelV4SessionConfig_2 as RealtimeModelV4SessionConfig_2, RealtimeModelV4ToolDefinition_2 as RealtimeModelV4ToolDefinition_2, InferToolSetContext_2 as InferToolSetContext_2, VideoModel as VideoModel, FilesV4_2 as FilesV4_2, SkillsV4_2 as SkillsV4_2, ProviderV2 as ProviderV2, ExtractModelId as ExtractModelId, LanguageModelV4_2 as LanguageModelV4_2, EmbeddingModelV4_2 as EmbeddingModelV4_2, ImageModelV4_2 as ImageModelV4_2, TranscriptionModelV4_2 as TranscriptionModelV4_2, RerankingModelV4_2 as RerankingModelV4_2, SpeechModelV4_2 as SpeechModelV4_2, VideoModelV4_2 as VideoModelV4_2, ExtractLiteralUnion as ExtractLiteralUnion, ProviderVideoModelIdentifier as ProviderVideoModelIdentifier, ProviderV4_2 as ProviderV4_2, ProviderV3 as ProviderV3, SharedV4AudioFormat_2 as SharedV4AudioFormat_2, SharedV4ProviderMetadata_2 as SharedV4ProviderMetadata_2, SpeechTranslationModelV4Usage as SpeechTranslationModelV4Usage, SpeechTranslationModelResponseMetadata as SpeechTranslationModelResponseMetadata, SpeechTranslationModel as SpeechTranslationModel, FilesV4UploadFileCallOptions_2 as FilesV4UploadFileCallOptions_2, SkillsV4UploadSkillResult_2 as SkillsV4UploadSkillResult_2, SkillsV4UploadSkillCallOptions_2 as SkillsV4UploadSkillCallOptions_2, UploadSkillFile as UploadSkillFile };
export { schemaSymbol, schemaSymbol_2, symbol$1, symbol$1_2, symbol$1_3, symbol$1_4, symbol$1_5, symbol$2, symbol$2_2, symbol$3, symbol$3_2, symbol$4, symbol$4_2, symbol$5, symbol$5_2, symbol$6, symbol$6_2, symbol$7, symbol$7_2, symbol$8, symbol$8_2, symbol$8_3, symbol$9, symbol$9_2, symbol$a, symbol$a_2, symbol$b, symbol$c, symbol$c_2, symbol$d, symbol$d_2, symbol$d_3, symbol$e, symbol$e_2, symbol$e_3, symbol$f, symbol$g, symbol$h, symbol$i, symbol$j, symbol$k, symbol$l, symbol, symbol_2, symbol_3, symbol_4, toolCallerSymbol };
