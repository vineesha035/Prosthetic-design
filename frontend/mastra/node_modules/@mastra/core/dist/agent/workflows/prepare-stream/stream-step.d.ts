import type { BackgroundTaskManager } from '../../../background-tasks/index.js';
import type { AgentBackgroundConfig } from '../../../background-tasks/types.js';
import type { MastraMemory } from '../../../memory/memory.js';
import type { MemoryConfigInternal } from '../../../memory/types.js';
import { MastraModelOutput } from '../../../stream/index.js';
import type { RequireToolApproval, ToolPayloadTransformPolicy } from '../../../tools/index.js';
import type { Workspace } from '../../../workspace/workspace.js';
import type { SaveQueueManager } from '../../save-queue/index.js';
import type { CreatedAgentSignal } from '../../signals.js';
import type { AgentMethodType } from '../../types.js';
import type { PrepareStreamRunScope } from './run-scope.js';
import type { AgentCapabilities } from './schema.js';
interface StreamStepOptions<OUTPUT = undefined> {
    capabilities: AgentCapabilities;
    runId: string;
    returnScorerData?: boolean;
    requireToolApproval?: RequireToolApproval;
    toolCallConcurrency?: number;
    resumeContext?: {
        resumeData: any;
        snapshot: any;
    };
    agentId: string;
    agentName?: string;
    toolCallId?: string;
    methodType: AgentMethodType;
    saveQueueManager?: SaveQueueManager;
    memoryConfig?: MemoryConfigInternal;
    memory?: MastraMemory;
    resourceId?: string;
    autoResumeSuspendedTools?: boolean;
    workspace?: Workspace;
    backgroundTaskManager?: BackgroundTaskManager;
    agentBackgroundConfig?: AgentBackgroundConfig;
    toolPayloadTransform?: ToolPayloadTransformPolicy;
    /**
     * When true, the in-loop `backgroundTaskCheckStep` skips its wait for
     * running tasks. Used when an outer caller (e.g. `agent.streamUntilIdle`)
     * drives continuation from outside the loop.
     */
    skipBgTaskWait?: boolean;
    drainPendingSignals?: (runId: string, scope?: 'pending' | 'pre-run') => CreatedAgentSignal[];
    runScope: PrepareStreamRunScope<OUTPUT>;
}
export declare function createStreamStep<OUTPUT = undefined>({ capabilities, runId: _runId, returnScorerData, requireToolApproval, toolCallConcurrency, resumeContext, agentId, agentName, toolCallId, methodType, saveQueueManager, memoryConfig, memory, resourceId, autoResumeSuspendedTools, workspace, backgroundTaskManager, agentBackgroundConfig, toolPayloadTransform, skipBgTaskWait, drainPendingSignals, runScope, }: StreamStepOptions<OUTPUT>): import("../../../workflows").Step<"stream-text-step", unknown, any, MastraModelOutput<OUTPUT>, unknown, unknown, import("../../../workflows").DefaultEngineType, unknown>;
export {};
//# sourceMappingURL=stream-step.d.ts.map