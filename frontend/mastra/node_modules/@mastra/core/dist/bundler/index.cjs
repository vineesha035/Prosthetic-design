Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_base = require("../base-B6soWsYg.cjs");
let fs_promises = require("fs/promises");
let dotenv = require("dotenv");
//#region src/bundler/index.ts
var MastraBundler = class extends require_base.MastraBase {
	constructor({ name, component = "BUNDLER" }) {
		super({
			component,
			name
		});
	}
	async loadEnvVars() {
		const envVars = /* @__PURE__ */ new Map();
		for (const file of await this.getEnvFiles()) {
			const config = (0, dotenv.parse)(await (0, fs_promises.readFile)(file, "utf-8"));
			Object.entries(config).forEach(([key, value]) => {
				envVars.set(key, value);
			});
		}
		return envVars;
	}
};
//#endregion
exports.MastraBundler = MastraBundler;

//# sourceMappingURL=index.cjs.map