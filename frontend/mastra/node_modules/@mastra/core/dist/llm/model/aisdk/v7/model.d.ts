import type { LanguageModelV4, LanguageModelV4CallOptions, LanguageModelV4StreamPart } from '../../../../_types/@ai-sdk_provider-v7/dist/index.d.ts';
import type { MastraLanguageModelV4 } from '../../shared.types.js';
type StreamResult = Awaited<ReturnType<LanguageModelV4['doStream']>>;
/**
 * Wrapper class for AI SDK V7 (LanguageModelV4) that converts doGenerate to return
 * a stream format for consistency with Mastra's streaming architecture.
 */
export declare class AISDKV7LanguageModel implements MastraLanguageModelV4 {
    #private;
    /**
     * The language model must specify which language model interface version it implements.
     */
    readonly specificationVersion: 'v4';
    /**
     * Name of the provider for logging purposes.
     */
    readonly provider: string;
    /**
     * Provider-specific model ID for logging purposes.
     */
    readonly modelId: string;
    /**
     * Supported URL patterns by media type for the provider.
     *
     * The keys are media type patterns or full media types (e.g. `*\/*` for everything, `audio/*`, `video/*`, or `application/pdf`).
     * and the values are arrays of regular expressions that match the URL paths.
     * The matching should be against lower-case URLs.
     * Matched URLs are supported natively by the model and are not downloaded.
     * @returns A map of supported URL patterns by media type (as a promise or a plain object).
     */
    supportedUrls: PromiseLike<Record<string, RegExp[]>> | Record<string, RegExp[]>;
    constructor(config: LanguageModelV4);
    doGenerate(options: LanguageModelV4CallOptions): Promise<{
        request: {
            body?: unknown;
        };
        response: StreamResult["response"];
        stream: ReadableStream<any>;
        content: Array<import("../../../../_types/@ai-sdk_provider-v7/dist/index.d.ts").LanguageModelV4Content>;
        finishReason: import("../../../../_types/@ai-sdk_provider-v7/dist/index.d.ts").LanguageModelV4FinishReason;
        usage: import("../../../../_types/@ai-sdk_provider-v7/dist/index.d.ts").LanguageModelV4Usage;
        providerMetadata?: import("../../../../_types/@ai-sdk_provider-v7/dist/index.d.ts").SharedV4ProviderMetadata;
        warnings: Array<import("../../../../_types/@ai-sdk_provider-v7/dist/index.d.ts").SharedV4Warning>;
    }>;
    doStream(options: LanguageModelV4CallOptions): Promise<{
        stream: ReadableStream<LanguageModelV4StreamPart>;
        request?: {
            body?: unknown;
        };
        response?: {
            headers?: import("../../../../_types/@ai-sdk_provider-v7/dist/index.d.ts").SharedV4Headers;
        };
    }>;
    /**
     * Custom serialization for tracing/observability spans.
     * `#model` is already a true JS private field and not enumerable, so
     * the wrapped provider SDK client can't leak. This method makes the
     * safe shape explicit and avoids walking `supportedUrls` (a
     * PromiseLike / regex map that isn't useful in spans).
     */
    serializeForSpan(): {
        specificationVersion: 'v4';
        modelId: string;
        provider: string;
    };
}
export {};
//# sourceMappingURL=model.d.ts.map