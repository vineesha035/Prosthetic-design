import { HarnessStorage } from './base.js';
import type { SessionRecord, SessionRecordUpdate } from './types.js';
export declare class InMemoryHarness extends HarnessStorage {
    #private;
    dangerouslyClearAll(): Promise<void>;
    loadSession(sessionId: string): Promise<SessionRecord | null>;
    saveSession(record: SessionRecord): Promise<void>;
    listSessions(): Promise<SessionRecord[]>;
    updateSession(sessionId: string, updates: SessionRecordUpdate): Promise<SessionRecord>;
}
//# sourceMappingURL=inmemory.d.ts.map