const require_rolldown_runtime = require("./rolldown-runtime-uwYp4b74.cjs");
const require_logger = require("./logger-BPclhj7J.cjs");
const require_base = require("./base-B6soWsYg.cjs");
const require_error = require("./error-B-e62x-A.cjs");
const require_observability = require("./observability-BzV5axz0.cjs");
const require_utils = require("./utils-CNiGU0Uf.cjs");
require("./tracing-BUrUJwCM.cjs");
const require_request_context = require("./request-context-ByoZMp-j.cjs");
const require_types = require("./types-CepZ83u8.cjs");
const require_utils$1 = require("./utils-Bw7FoAI3.cjs");
const require_trip_wire = require("./trip-wire-dxd_uCHj.cjs");
const require_workflows_constants = require("./workflows/constants.cjs");
let crypto = require("crypto");
let events = require("events");
events = require_rolldown_runtime.__toESM(events, 1);
let zod = require("zod");
let _mastra_schema_compat_schema = require("@mastra/schema-compat/schema");
//#region src/workflows/stream-utils.ts
async function forwardAgentStreamChunk({ writer, chunk }) {
	if (!writer) return;
	await writer.write(chunk);
}
//#endregion
//#region src/workflows/step.ts
const getStepResult = (stepResults, step) => {
	let result;
	if (typeof step === "string") result = stepResults[step];
	else {
		if (!step?.id) return null;
		result = stepResults[step.id];
	}
	return result?.status === "success" ? result.output : null;
};
//#endregion
//#region src/workflows/step-entry.ts
/**
* Accessors for the {@link SingleStepEntry} union.
*
* This module is the single place allowed to pattern-match the union's shape.
* Everything else (both engines, handlers, utils) should go through these
* helpers so that adding a new variant means changing exactly one file.
*
* Union *shape* questions (id, retries, schemas, …) live here; how each
* declarative kind is *interpreted* at run time lives in `./entry-executors`.
*/
/**
* The id of a single step-like entry. Plain `step` entries key off the wrapped
* step's id; declarative variants (agent / tool / mapping) carry their own `id`.
*/
function getEntryId(entry) {
	return entry.type === "step" ? entry.step.id : entry.id;
}
/**
* The effective retry count for an entry, falling back to the provided
* workflow-level default when the entry doesn't declare its own.
*
* - `step` — the step's own `retries`
* - `agent` / `tool` — the declarative `options.retries`
* - `mapping` — never declares retries; always the fallback
*/
function getEntryRetries(entry, fallback) {
	switch (entry.type) {
		case "step": return entry.step.retries ?? fallback;
		case "agent":
		case "tool": return entry.options?.retries ?? fallback;
		case "mapping": return fallback;
	}
}
/**
* The `component` discriminator of the entry, if any. Only plain `step`
* entries can carry one (notably `'WORKFLOW'` for nested workflows);
* declarative variants have none.
*/
function getEntryComponent(entry) {
	return entry.type === "step" ? entry.step.component : void 0;
}
/**
* Probes an entry for a nested workflow. Only the `type: 'step'` variant can
* wrap a live `Workflow` (identified by its `component === 'WORKFLOW'`
* discriminator from MastraBase); declarative variants never nest one.
*/
function getEntryWorkflow(entry) {
	if (entry.type !== "step") return null;
	const step = entry.step;
	if (step && typeof step === "object" && step.component === "WORKFLOW") return entry.step;
	return null;
}
/**
* The validation schemas of an entry, used by the engines to validate step
* input / suspend / resume data without materializing a live Step.
*
* - `step` — the step's own schemas
* - `agent` — the fixed `{ prompt: string }` input contract (mirrors `createStepFromAgent`)
* - `tool` — the resolved tool's schemas
* - `mapping` — none (mappings accept and return anything)
*
* Never throws: when a tool can't be resolved the schemas are simply empty and
* the run path surfaces the actionable not-found error.
*/
function getEntrySchemas(entry, mastra) {
	switch (entry.type) {
		case "step": return {
			inputSchema: entry.step.inputSchema,
			resumeSchema: entry.step.resumeSchema,
			suspendSchema: entry.step.suspendSchema
		};
		case "agent": return { inputSchema: (0, _mastra_schema_compat_schema.toStandardSchema)(zod.z.object({ prompt: zod.z.string() })) };
		case "tool": {
			let tool;
			try {
				tool = entry.tool ?? mastra?.getTool(entry.toolId);
			} catch {
				tool = void 0;
			}
			return tool ? {
				inputSchema: tool.inputSchema,
				resumeSchema: tool.resumeSchema,
				suspendSchema: tool.suspendSchema
			} : {};
		}
		case "mapping": return {};
	}
}
//#endregion
//#region src/workflows/utils.ts
/**
* Validates data against a StandardSchema and returns the result.
* Works with both sync and async schemas.
*/
async function validateWithStandardSchema(schema, data) {
	const result = schema["~standard"].validate(data);
	const resolvedResult = result instanceof Promise ? await result : result;
	if ("issues" in resolvedResult && resolvedResult.issues) return {
		success: false,
		issues: resolvedResult.issues.map((issue) => ({
			path: issue.path?.map((p) => typeof p === "object" && "key" in p ? p.key : p),
			message: issue.message
		}))
	};
	return {
		success: true,
		data: resolvedResult.value
	};
}
async function validateStepInput({ prevOutput, step, validateInputs }) {
	let inputData = prevOutput;
	let validationError;
	const inputSchema = step.inputSchema;
	if (validateInputs && inputSchema) {
		const validatedInput = await validateWithStandardSchema(inputSchema, prevOutput);
		if (!validatedInput.success) {
			const errorMessages = validatedInput.issues.map((e) => `- ${e.path?.join(".")}: ${e.message}`).join("\n");
			validationError = new require_error.MastraError({
				id: "WORKFLOW_STEP_INPUT_VALIDATION_FAILED",
				domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
				category: require_error.ErrorCategory.USER,
				text: "Step input validation failed: \n" + errorMessages
			}, { issues: validatedInput.issues });
		} else inputData = validatedInput.data !== null && typeof validatedInput.data === "object" && !Array.isArray(validatedInput.data) && Object.keys(validatedInput.data).length === 0 ? prevOutput : validatedInput.data;
	}
	return {
		inputData,
		validationError
	};
}
async function validateStepResumeData({ resumeData, step }) {
	if (!resumeData) return {
		resumeData: void 0,
		validationError: void 0
	};
	let validationError;
	const resumeSchema = step.resumeSchema;
	if (resumeSchema) {
		const validatedResumeData = await validateWithStandardSchema(resumeSchema, resumeData);
		if (!validatedResumeData.success) {
			const errorMessages = validatedResumeData.issues.map((e) => `- ${e.path?.join(".")}: ${e.message}`).join("\n");
			validationError = new require_error.MastraError({
				id: "WORKFLOW_STEP_RESUME_DATA_VALIDATION_FAILED",
				domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
				category: require_error.ErrorCategory.USER,
				text: "Step resume data validation failed: \n" + errorMessages
			});
		} else resumeData = validatedResumeData.data;
	}
	return {
		resumeData,
		validationError
	};
}
async function validateStepSuspendData({ suspendData, step, validateInputs }) {
	if (!suspendData) return {
		suspendData: void 0,
		validationError: void 0
	};
	let validationError;
	const suspendSchema = step.suspendSchema;
	if (suspendSchema && validateInputs) {
		const validatedSuspendData = await validateWithStandardSchema(suspendSchema, suspendData);
		if (!validatedSuspendData.success) {
			const errorMessages = validatedSuspendData.issues.map((e) => `- ${e.path?.join(".")}: ${e.message}`).join("\n");
			validationError = new require_error.MastraError({
				id: "WORKFLOW_STEP_SUSPEND_DATA_VALIDATION_FAILED",
				domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
				category: require_error.ErrorCategory.USER,
				text: "Step suspend data validation failed: \n" + errorMessages
			});
		} else suspendData = validatedSuspendData.data;
	}
	return {
		suspendData,
		validationError
	};
}
async function validateStepStateData({ stateData, step, validateInputs }) {
	if (!stateData) return {
		stateData: void 0,
		validationError: void 0
	};
	let validationError;
	const stateSchema = step.stateSchema;
	if (stateSchema && validateInputs) {
		const validatedStateData = await validateWithStandardSchema(stateSchema, stateData);
		if (!validatedStateData.success) {
			const errorMessages = validatedStateData.issues.map((e) => `- ${e.path?.join(".")}: ${e.message}`).join("\n");
			validationError = /* @__PURE__ */ new Error("Step state data validation failed: \n" + errorMessages);
		} else stateData = validatedStateData.data;
	}
	return {
		stateData,
		validationError
	};
}
async function validateStepRequestContext({ requestContext, step, validateInputs }) {
	let validationError;
	const requestContextSchema = step.requestContextSchema;
	if (requestContextSchema && validateInputs) {
		const validatedRequestContext = await validateWithStandardSchema(requestContextSchema, requestContext?.all ?? {});
		if (!validatedRequestContext.success) {
			const errorMessages = validatedRequestContext.issues.map((e) => `- ${e.path?.join(".")}: ${e.message}`).join("\n");
			validationError = new require_error.MastraError({
				id: "WORKFLOW_STEP_REQUEST_CONTEXT_VALIDATION_FAILED",
				domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
				category: require_error.ErrorCategory.USER,
				text: `Step request context validation failed for step '${step.id}': \n` + errorMessages
			});
		}
	}
	return { validationError };
}
function getResumeLabelsByStepId(resumeLabels, stepId) {
	return Object.entries(resumeLabels).filter(([_, value]) => value.stepId === stepId).reduce((acc, [key, value]) => {
		acc[key] = value;
		return acc;
	}, {});
}
const runCountDeprecationMessage = "Warning: 'runCount' is deprecated and will be removed on November 4th, 2025. Please use 'retryCount' instead.";
/**
* Track which deprecation warnings have been shown globally to avoid spam
*/
const shownWarnings = /* @__PURE__ */ new Set();
/**
* Creates a Proxy that wraps execute function parameters to show deprecation warnings
* when accessing deprecated properties.
*
* Currently handles:
* - `runCount`: Deprecated in favor of `retryCount`, will be removed on November 4th, 2025
*/
function createDeprecationProxy(params, { paramName, deprecationMessage, logger }) {
	return new Proxy(params, { get(target, prop, receiver) {
		if (prop === paramName && !shownWarnings.has(paramName)) {
			shownWarnings.add(paramName);
			if (logger) logger.warn("\x1B[33m%s\x1B[0m", deprecationMessage);
			else console.warn("\x1B[33m%s\x1B[0m", deprecationMessage);
		}
		return Reflect.get(target, prop, receiver);
	} });
}
const SINGLE_STEP_TYPES = [
	"step",
	"agent",
	"tool",
	"mapping"
];
/**
* Whether an entry is a "single step-like" entry: a plain user step or one of the
* declarative variants (agent / tool / mapping) that resolve to exactly one step.
*/
function isSingleStepEntry(entry) {
	return SINGLE_STEP_TYPES.includes(entry.type);
}
/**
* The id of a single step-like entry. Plain `step` entries key off the wrapped
* step's id; declarative variants (agent / tool / mapping) carry their own `id`.
*
* Public alias of {@link getEntryId} from `./step-entry`.
*/
const getSingleStepEntryId = getEntryId;
const getStepIds = (entry) => {
	if (isSingleStepEntry(entry)) return [getSingleStepEntryId(entry)];
	if (entry.type === "foreach" || entry.type === "loop") return [getSingleStepEntryId(entry.step)];
	if (entry.type === "parallel" || entry.type === "conditional") return entry.steps.map((s) => getSingleStepEntryId(s));
	if (entry.type === "sleep" || entry.type === "sleepUntil") return [entry.id];
	return [];
};
const createTimeTravelExecutionParams = (params) => {
	const { steps, inputData, resumeData, context, nestedStepsContext, snapshot, initialState, graph, perStep } = params;
	const firstStepId = steps[0];
	let executionPath = [];
	const stepResults = {};
	const snapshotContext = snapshot.context;
	for (const [index, entry] of graph.steps.entries()) {
		const currentExecPathLength = executionPath.length;
		if (currentExecPathLength > 0 && !resumeData) break;
		const stepIds = getStepIds(entry);
		const isTargetEntry = stepIds.includes(firstStepId);
		if (isTargetEntry) executionPath = [index, ...stepIds?.length > 1 ? [stepIds?.findIndex((s) => s === firstStepId)] : []];
		const prevStep = graph.steps[index - 1];
		let stepPayload = void 0;
		if (prevStep) {
			const prevStepIds = getStepIds(prevStep);
			if (prevStepIds.length > 0) if (prevStepIds.length === 1) stepPayload = (stepResults?.[prevStepIds[0]])?.output ?? {};
			else stepPayload = prevStepIds.reduce((acc, stepId) => {
				acc[stepId] = (stepResults?.[stepId])?.output ?? {};
				return acc;
			}, {});
		}
		if (index === 0 && stepIds.includes(firstStepId)) stepResults.input = context?.[firstStepId]?.payload ?? inputData ?? snapshotContext?.input;
		else if (index === 0) stepResults.input = stepIds?.reduce((acc, stepId) => {
			if (acc) return acc;
			return context?.[stepId]?.payload ?? snapshotContext?.[stepId]?.payload;
		}, null) ?? snapshotContext?.input ?? {};
		let stepOutput = void 0;
		const nextStep = graph.steps[index + 1];
		if (nextStep) {
			const nextStepIds = getStepIds(nextStep);
			if (nextStepIds.length > 0 && inputData && nextStepIds.includes(firstStepId) && steps.length === 1) stepOutput = inputData;
		}
		stepIds.forEach((stepId) => {
			let result;
			const stepContext = context?.[stepId] ?? snapshotContext[stepId];
			const isUnselectedConditionalSibling = isTargetEntry && entry.type === "conditional" && !steps?.includes(stepId);
			const defaultStepStatus = steps?.includes(stepId) ? "running" : isUnselectedConditionalSibling ? "skipped" : "success";
			const status = ["failed", "canceled"].includes(stepContext?.status) ? defaultStepStatus : stepContext?.status ?? defaultStepStatus;
			const isCompleteStatus = [
				"success",
				"failed",
				"canceled"
			].includes(status);
			result = {
				status,
				payload: context?.[stepId]?.payload ?? stepPayload ?? snapshotContext[stepId]?.payload ?? {},
				output: isCompleteStatus ? context?.[stepId]?.output ?? stepOutput ?? snapshotContext[stepId]?.output ?? {} : void 0,
				resumePayload: stepContext?.resumePayload,
				suspendPayload: stepContext?.suspendPayload,
				suspendOutput: stepContext?.suspendOutput,
				startedAt: stepContext?.startedAt ?? Date.now(),
				endedAt: isCompleteStatus ? stepContext?.endedAt ?? Date.now() : void 0,
				suspendedAt: stepContext?.suspendedAt,
				resumedAt: stepContext?.resumedAt
			};
			if ((perStep ? executionPath.length : currentExecPathLength) > 0 && !steps?.includes(stepId) && !context?.[stepId] && (!snapshotContext[stepId] || snapshotContext[stepId] && snapshotContext[stepId].status !== "suspended")) result = void 0;
			if (result) {
				const formattedResult = require_utils$1.removeUndefinedValues(result);
				stepResults[stepId] = formattedResult;
			}
		});
	}
	if (!executionPath.length) throw new Error(`Time travel target step not found in execution graph: '${steps?.join(".")}'. Verify the step id/path.`);
	return {
		inputData,
		executionPath,
		steps,
		stepResults,
		nestedStepResults: nestedStepsContext,
		state: initialState ?? snapshot.value ?? {},
		resumeData,
		stepExecutionPath: snapshot?.stepExecutionPath
	};
};
const createRestartExecutionParams = ({ snapshot, graph }) => {
	let nestedWorkflowPending = false;
	if (snapshot.status !== "running" && snapshot.status !== "waiting") if (snapshot.status === "pending" && snapshot.context && Object.prototype.hasOwnProperty.call(snapshot.context, "input")) nestedWorkflowPending = true;
	else throw new Error("This workflow run was not active");
	let nestedWorkflowActiveStepsPath = {};
	const firstEntry = graph.steps[0];
	if (isSingleStepEntry(firstEntry)) nestedWorkflowActiveStepsPath = { [getSingleStepEntryId(firstEntry)]: [0] };
	else if (firstEntry.type === "foreach" || firstEntry.type === "loop") nestedWorkflowActiveStepsPath = { [getSingleStepEntryId(firstEntry.step)]: [0] };
	else if (firstEntry.type === "sleep" || firstEntry.type === "sleepUntil") nestedWorkflowActiveStepsPath = { [firstEntry.id]: [0] };
	else if (firstEntry.type === "conditional" || firstEntry.type === "parallel") nestedWorkflowActiveStepsPath = firstEntry.steps.reduce((acc, step) => {
		acc[getSingleStepEntryId(step)] = [0];
		return acc;
	}, {});
	return {
		activePaths: nestedWorkflowPending ? [0] : snapshot.activePaths,
		activeStepsPath: nestedWorkflowPending ? nestedWorkflowActiveStepsPath : snapshot.activeStepsPath,
		stepResults: snapshot.context,
		state: snapshot.value,
		stepExecutionPath: snapshot?.stepExecutionPath
	};
};
/**
* Re-hydrates serialized errors in step results back into proper Error instances.
* This is useful when errors have been serialized through an event system (e.g., evented engine, Inngest)
* and need to be converted back to Error instances with their custom properties preserved.
*
* @param steps - The workflow step results (context) that may contain serialized errors
* @returns The same steps object with errors hydrated as Error instances
*/
function hydrateSerializedStepErrors(steps) {
	if (steps) {
		for (const step of Object.values(steps)) if (step.status === "failed" && "error" in step && step.error) step.error = require_error.getErrorFromUnknown(step.error, { serializeStack: false });
	}
	return steps;
}
/**
* Cleans a single step result object by removing internal properties.
* This is a helper for cleanStepResult that handles one level of cleaning.
*/
function cleanSingleResult(result) {
	const { __state: _state, metadata, ...rest } = result;
	if (metadata && typeof metadata === "object" && !Array.isArray(metadata)) {
		const { nestedRunId: _nestedRunId, ...userMetadata } = metadata;
		if (Object.keys(userMetadata).length > 0) return {
			...rest,
			metadata: userMetadata
		};
	}
	return rest;
}
/**
* Cleans step result data by removing internal properties at known structural levels.
*
* Removes:
* - `__state` properties (internal workflow state for state propagation)
* - `nestedRunId` from `metadata` objects (internal tracking for nested workflow retrieval)
*
* ## Why targeted cleaning instead of recursive?
*
* Internal properties only appear at specific, known locations:
*
* 1. **`__state`** - Added by step-executor.ts to every step result. For forEach,
*    suspended iterations store the full result (including __state) while completed
*    iterations only store the output value. See workflow-event-processor/index.ts:1227-1230.
*
* 2. **`metadata.nestedRunId`** - Added when nested workflows complete, stored at the
*    step result level. For forEach with nested workflows, each iteration result can
*    have this. See workflow-event-processor/index.ts:1449-1453.
*
* By only cleaning at the step result level and forEach iteration level, we avoid
* accidentally stripping user data that happens to use `__state` as a property name
* in their actual output values.
*
* @param stepResult - A step result object, or an array of iteration results (forEach)
* @returns The cleaned step result with internal properties removed
*/
function cleanStepResult(stepResult) {
	if (stepResult === null || stepResult === void 0) return stepResult;
	if (typeof stepResult !== "object") return stepResult;
	if (Array.isArray(stepResult)) return stepResult.map((item) => {
		if (item && typeof item === "object" && !Array.isArray(item)) return cleanSingleResult(item);
		return item;
	});
	const cleaned = cleanSingleResult(stepResult);
	if (Array.isArray(cleaned.output)) cleaned.output = cleaned.output.map((item) => {
		if (item && typeof item === "object" && !Array.isArray(item)) return cleanSingleResult(item);
		return item;
	});
	return cleaned;
}
/**
* Resolves the effective concurrency for a foreach entry at execution time.
*
* Supports both a static number and a {@link ForeachConcurrencyResolver}
* function that derives concurrency from the run's input. Invalid or
* non-positive values fall back to 1 (sequential).
*/
function resolveForeachConcurrency(opts, context) {
	const configured = opts?.concurrency ?? 1;
	const resolved = typeof configured === "function" ? configured(context) : configured;
	if (typeof resolved !== "number" || !Number.isFinite(resolved) || resolved < 1) return 1;
	return Math.floor(resolved);
}
const RESUME_SNAPSHOT_POLL_INTERVAL_MS = 25;
const RESUME_SNAPSHOT_POLL_TIMEOUT_MS = 2e3;
async function waitForSuspendedSnapshot(workflowsStore, workflowName, runId) {
	if (!workflowsStore) return null;
	const deadline = Date.now() + RESUME_SNAPSHOT_POLL_TIMEOUT_MS;
	let snapshot = await workflowsStore.loadWorkflowSnapshot({
		workflowName,
		runId
	}) ?? null;
	while ((!snapshot || snapshot.status !== "suspended") && Date.now() < deadline) {
		await new Promise((resolve) => setTimeout(resolve, RESUME_SNAPSHOT_POLL_INTERVAL_MS));
		snapshot = await workflowsStore.loadWorkflowSnapshot({
			workflowName,
			runId
		}) ?? null;
	}
	return snapshot;
}
//#endregion
//#region src/workflows/entry-executors/run-agent-entry.ts
/**
* Runs a declarative `agent` entry: resolves the agent (inline handle, else the
* Mastra registry), streams the prompt through it, forwards stream chunks, and
* returns either the structured output or `{ text }`.
*
* `ctx` is the step execute context (the same object a plain step's `execute`
* receives). `mastra` defaults to `ctx.mastra` when omitted.
*/
async function runAgentEntry(entry, ctx, mastra) {
	const registry = mastra ?? ctx?.mastra;
	const agent = entry.agent ?? registry?.getAgentById(entry.agentId);
	if (!agent) throw new Error(`Agent '${entry.agentId}' not found for workflow step '${entry.id}'. Register the agent on the Mastra instance or pass the agent instance directly.`);
	const { retries: _retries, scorers: _scorers, metadata: _metadata, ...agentOptions } = entry.options ?? {};
	const { inputData, runId, [require_workflows_constants.PUBSUB_SYMBOL]: pubsub, [require_workflows_constants.STREAM_FORMAT_SYMBOL]: streamFormat, requestContext, abortSignal, abort, writer, ...rest } = ctx;
	const observabilityContext = require_observability.resolveObservabilityContext(rest);
	let streamPromise = {};
	streamPromise.promise = new Promise((resolve, reject) => {
		streamPromise.resolve = resolve;
		streamPromise.reject = reject;
	});
	streamPromise.promise.catch(() => {});
	let structuredResult = null;
	const toolData = {
		name: agent.name,
		args: inputData
	};
	let stream;
	const handleFinish = (result) => {
		const resultWithObject = result;
		if (agentOptions?.structuredOutput?.schema && resultWithObject.object) structuredResult = resultWithObject.object;
		streamPromise.resolve(result.text);
		agentOptions?.onFinish?.(result);
	};
	if ((await agent.getModel({ requestContext })).specificationVersion === "v1" && typeof agent.streamLegacy === "function") {
		const { fullStream } = await agent.streamLegacy(inputData.prompt, {
			...agentOptions,
			requestContext,
			...observabilityContext,
			onFinish: handleFinish,
			abortSignal
		});
		stream = fullStream;
	} else {
		const modelOutput = await agent.stream(inputData.prompt, {
			...agentOptions,
			requestContext,
			...observabilityContext,
			onFinish: handleFinish,
			abortSignal
		});
		modelOutput.text.then(() => {}, (err) => streamPromise.reject(err));
		stream = modelOutput.fullStream;
	}
	const tripwireChunk = streamFormat === "legacy" ? await bridgeLegacyWatchEvents({
		stream,
		pubsub,
		runId,
		toolData
	}) : await consumeStreamForTripwire(stream, writer);
	if (tripwireChunk) throw new require_trip_wire.TripWire(tripwireChunk.payload?.reason || "Agent tripwire triggered", {
		retry: tripwireChunk.payload?.retry,
		metadata: tripwireChunk.payload?.metadata
	}, tripwireChunk.payload?.processorId);
	if (abortSignal.aborted) return abort();
	if (structuredResult !== null) return structuredResult;
	return { text: await streamPromise.promise };
}
/**
* Legacy-format watch-event bridge: instead of forwarding chunks to the step
* writer, mirrors the agent stream onto the run's pubsub watch channel as
* `tool-call-streaming-*` / `tool-call-delta` events (the shape v1 watchers
* expect). Returns the tripwire chunk if one was seen, else `null`.
*/
async function bridgeLegacyWatchEvents({ stream, pubsub, runId, toolData }) {
	let tripwireChunk = null;
	await pubsub.publish(`workflow.events.v2.${runId}`, {
		type: "watch",
		runId,
		data: {
			type: "tool-call-streaming-start",
			...toolData ?? {}
		}
	});
	try {
		for await (const chunk of stream) {
			if (chunk.type === "tripwire") {
				tripwireChunk = chunk;
				break;
			}
			if (chunk.type === "text-delta") await pubsub.publish(`workflow.events.v2.${runId}`, {
				type: "watch",
				runId,
				data: {
					type: "tool-call-delta",
					...toolData ?? {},
					argsTextDelta: chunk.textDelta
				}
			});
		}
	} finally {
		await pubsub.publish(`workflow.events.v2.${runId}`, {
			type: "watch",
			runId,
			data: {
				type: "tool-call-streaming-finish",
				...toolData ?? {}
			}
		}).catch(() => {});
	}
	return tripwireChunk;
}
/**
* Forwards every chunk to the step writer, stopping early when a tripwire
* chunk appears. Returns the tripwire chunk if one was seen, else `null`.
*/
async function consumeStreamForTripwire(stream, writer) {
	for await (const chunk of stream) {
		await forwardAgentStreamChunk({
			writer,
			chunk
		});
		if (chunk.type === "tripwire") return chunk;
	}
	return null;
}
//#endregion
//#region src/workflows/entry-executors/run-tool-entry.ts
/**
* Runs a declarative `tool` entry: resolves the tool (inline handle, else the
* Mastra registry) and executes it with the step context mapped into the tool
* execution context.
*/
async function runToolEntry(entry, ctx, mastra) {
	const registry = mastra ?? ctx?.mastra;
	const tool = entry.tool ?? registry?.getTool(entry.toolId);
	if (!tool) throw new Error(`Tool '${entry.toolId}' not found for workflow step '${entry.id}'. Pass the tool instance directly.`);
	const { inputData, mastra: ctxMastra, requestContext, suspend, resumeData, runId, workflowId, state, setState, abortSignal, ...rest } = ctx;
	const toolContext = {
		mastra: ctxMastra,
		requestContext,
		...require_observability.resolveObservabilityContext(rest),
		abortSignal,
		resumeData,
		workflow: {
			runId,
			suspend,
			resumeData,
			workflowId,
			state,
			setState
		}
	};
	return tool.execute(inputData, toolContext);
}
//#endregion
//#region src/workflows/mapping-template.ts
/**
* The `${scope.path}` mapping-template DSL used by `.map()` template sources.
*
* Definition-time syntax checks live in {@link validateTemplate}; run-time
* resolution (path lookup + value coercion) lives in {@link resolveTemplate}.
* This module has no knowledge of the step-entry union — it is a pure
* string-DSL interpreter over a step's execute context.
*/
/** Walks a dotted path on an object. `''` or `'.'` returns the root unchanged. */
function traverseMappingPath(root, path, errorLabel) {
	if (path === "" || path === ".") return root;
	const parts = path.split(".");
	let value = root;
	for (const part of parts) if (typeof value === "object" && value !== null) value = value[part];
	else throw new Error(`Invalid path ${path} in ${errorLabel}`);
	return value;
}
const TEMPLATE_PLACEHOLDER = /\$\{([^}]*)\}/g;
const TEMPLATE_NAMESPACES = [
	"inputData",
	"initData",
	"state",
	"requestContext",
	"stepResults"
];
/** Common error-message prefix so every template diagnostic points at the exact placeholder. */
function describeBadPlaceholder(template, idx, rawExpr) {
	return `Template placeholder #${idx} (\${${rawExpr}}) in '${template}'`;
}
/** Split a placeholder body `scope.path.with.dots` into its leading scope and the dotted remainder. */
function parseTemplatePlaceholder(rawExpr) {
	const dot = rawExpr.indexOf(".");
	return {
		scope: dot === -1 ? rawExpr : rawExpr.slice(0, dot),
		rest: dot === -1 ? "" : rawExpr.slice(dot + 1)
	};
}
/**
* Validates a `{ template }` source's syntax at workflow-definition time.
* Throws if any placeholder is empty, whitespace-padded, references an unknown
* namespace, or is a malformed `stepResults.<stepId>` / `stepResults.<stepId>.<path>` shape.
*
* Run-time concerns (does the step actually exist, does the path resolve, is
* the value a primitive) stay in {@link resolveTemplate}.
*/
function validateTemplate(template) {
	let idx = 0;
	for (const match of template.matchAll(TEMPLATE_PLACEHOLDER)) {
		idx++;
		const rawExpr = match[1] ?? "";
		if (rawExpr.length === 0 || rawExpr !== rawExpr.trim()) throw new Error(`${describeBadPlaceholder(template, idx, rawExpr)} has empty or whitespace-padded contents. Use \${<scope>.<path>} with no surrounding whitespace.`);
		const { scope, rest } = parseTemplatePlaceholder(rawExpr);
		if (scope === "stepResults") {
			const innerDot = rest.indexOf(".");
			if (!(innerDot === -1 ? rest : rest.slice(0, innerDot))) throw new Error(`${describeBadPlaceholder(template, idx, rawExpr)} must be of the form \${stepResults.<stepId>} or \${stepResults.<stepId>.<path>}.`);
			continue;
		}
		if (scope === "requestContext") {
			if (!rest) throw new Error(`${describeBadPlaceholder(template, idx, rawExpr)} requires a request-context key — use \${requestContext.<key>}.`);
			continue;
		}
		if (TEMPLATE_NAMESPACES.includes(scope)) continue;
		throw new Error(`${describeBadPlaceholder(template, idx, rawExpr)} references unknown namespace "${scope}". Use one of: ${TEMPLATE_NAMESPACES.join(", ")}.`);
	}
}
/**
* Collects the step ids referenced by `${stepResults.<stepId>}` /
* `${stepResults.<stepId>.<path>}` placeholders in a template. Assumes the
* template already passed {@link validateTemplate}; malformed placeholders are
* skipped. Used by validation to scope-check template references against the
* preceding workflow-local steps.
*/
function collectTemplateStepIds(template) {
	const ids = [];
	for (const match of template.matchAll(TEMPLATE_PLACEHOLDER)) {
		const { scope, rest } = parseTemplatePlaceholder(match[1] ?? "");
		if (scope !== "stepResults") continue;
		const innerDot = rest.indexOf(".");
		const stepId = innerDot === -1 ? rest : rest.slice(0, innerDot);
		if (stepId) ids.push(stepId);
	}
	return ids;
}
/**
* Coerces a resolved placeholder value to a string. Primitives are stringified
* the normal way; objects and arrays are JSON-encoded so downstream agents can
* consume complex step outputs (e.g. `foreach(agent)` returns `{ text }[]`)
* directly in a template. `null`/`undefined` render as empty. If JSON encoding
* fails (circular references, BigInt, etc.), throws with a hint pointing at
* the offending placeholder.
*/
function stringifyTemplateValue(v, template, idx, rawExpr) {
	if (v === null || v === void 0) return "";
	if (typeof v === "object") try {
		return JSON.stringify(v);
	} catch (err) {
		throw new Error(`${describeBadPlaceholder(template, idx, rawExpr)} resolved to a value that could not be JSON-stringified (${err.message}). Drill into a primitive path (e.g. \${${rawExpr}.someField}) or reshape the value in a preceding step.`);
	}
	return String(v);
}
/**
* Resolves `${<scope>.<path>}` placeholders against the implicit namespaces
* available in a step's execute context. See the `.map()` overload signature
* for the full list of accepted scopes (`inputData`, `initData`, `state`,
* `requestContext`, `stepResults.<stepId>`).
*/
function resolveTemplate(template, ctx) {
	let idx = 0;
	return template.replace(TEMPLATE_PLACEHOLDER, (_match, rawExpr) => {
		idx++;
		return resolveTemplatePlaceholder(rawExpr, template, idx, ctx);
	});
}
function resolveTemplatePlaceholder(rawExpr, template, idx, ctx) {
	const { scope, rest } = parseTemplatePlaceholder(rawExpr);
	const label = describeBadPlaceholder(template, idx, rawExpr);
	switch (scope) {
		case "inputData": return stringifyTemplateValue(traverseMappingPath(ctx.inputData, rest, label), template, idx, rawExpr);
		case "initData": return stringifyTemplateValue(traverseMappingPath(ctx.getInitData(), rest, label), template, idx, rawExpr);
		case "state": return stringifyTemplateValue(traverseMappingPath(ctx.state, rest, label), template, idx, rawExpr);
		case "requestContext": return stringifyTemplateValue(ctx.requestContext.get(rest), template, idx, rawExpr);
		case "stepResults": {
			const innerDot = rest.indexOf(".");
			const stepId = innerDot === -1 ? rest : rest.slice(0, innerDot);
			const subPath = innerDot === -1 ? "" : rest.slice(innerDot + 1);
			const stepResult = ctx.getStepResult(stepId);
			if (stepResult == null) throw new Error(`${label} references stepResults.${stepId} but step "${stepId}" has no successful output (not run yet, not registered, failed, or produced no output).`);
			return stringifyTemplateValue(traverseMappingPath(stepResult, subPath, label), template, idx, rawExpr);
		}
		default: throw new Error(`${label} references unknown namespace "${scope}". Use one of: ${TEMPLATE_NAMESPACES.join(", ")}.`);
	}
}
//#endregion
//#region src/workflows/entry-executors/run-mapping-entry.ts
/**
* Runs a declarative `mapping` entry. Function configs are invoked directly;
* object configs are interpreted key-by-key (`value` / `fn` / `template` /
* `requestContextPath` / `step`+`path` / `initData`+`path`).
*/
async function runMappingEntry(entry, ctx) {
	const { mapConfig } = entry;
	if (typeof mapConfig === "function") return mapConfig(ctx);
	const { getStepResult, getInitData, requestContext } = ctx;
	const result = {};
	for (const [key, mapping] of Object.entries(mapConfig)) {
		const m = mapping;
		if (m.value !== void 0) {
			result[key] = m.value;
			continue;
		}
		if (m.fn !== void 0) {
			result[key] = await m.fn(ctx);
			continue;
		}
		if (typeof m.template === "string") {
			result[key] = resolveTemplate(m.template, ctx);
			continue;
		}
		if (m.requestContextPath) {
			result[key] = requestContext.get(m.requestContextPath);
			continue;
		}
		result[key] = traverseMappingPath(m.initData ? getInitData() : getStepResult(Array.isArray(m.step) ? m.step.find((s) => {
			const stepRes = getStepResult(s);
			if (typeof stepRes === "object" && stepRes !== null) return Object.keys(stepRes).length > 0;
			return stepRes;
		}) : m.step), m.path, describeMappingSource(m));
	}
	return result;
}
/** Human-readable source label for path-traversal errors. */
function describeMappingSource(m) {
	if (m.initData) return "initData";
	const stepLabel = (s) => typeof s === "string" ? s : s?.id ?? "unknown";
	if (Array.isArray(m.step)) return `step ${m.step.map(stepLabel).join("|")}`;
	return `step ${stepLabel(m.step)}`;
}
//#endregion
//#region src/workflows/evented/workflow-event-processor/utils.ts
function getNestedWorkflow(mastra, { workflowId, executionPath, parentWorkflow, runId }) {
	let workflow = null;
	if (parentWorkflow) {
		const nestedWorkflow = getNestedWorkflow(mastra, parentWorkflow);
		if (!nestedWorkflow) return null;
		workflow = nestedWorkflow;
	}
	workflow = workflow ?? (mastra.__hasInternalWorkflow(workflowId, runId) ? mastra.__getInternalWorkflow(workflowId, runId) : mastra.getWorkflow(workflowId));
	let parentStep = workflow.stepGraph[executionPath[0]];
	if (parentStep?.type === "parallel" || parentStep?.type === "conditional") parentStep = parentStep.steps[executionPath[1]];
	if (parentStep?.type === "loop" || parentStep?.type === "foreach") return getEntryWorkflow(parentStep.step);
	if (parentStep && isSingleStepEntry(parentStep)) return getEntryWorkflow(parentStep);
	return null;
}
/**
* Resolves the single-step entry addressed by an execution path, or null when
* the path doesn't land on a single-step-like entry. For `loop` / `foreach`
* the body entry is returned.
*/
function getStepEntry(workflow, executionPath) {
	let parentStep = workflow.stepGraph[executionPath[0]];
	if (parentStep?.type === "parallel" || parentStep?.type === "conditional") parentStep = parentStep.steps[executionPath[1]];
	if (parentStep?.type === "loop" || parentStep?.type === "foreach") return parentStep.step;
	if (parentStep && isSingleStepEntry(parentStep)) return parentStep;
	return null;
}
/**
* Resolves the id of the entry addressed by an execution path, or null when the
* path doesn't land on a single-step-like entry. For `loop` / `foreach` the id
* of the body entry is returned.
*/
function getStepId(workflow, executionPath) {
	const entry = getStepEntry(workflow, executionPath);
	return entry ? getEntryId(entry) : null;
}
function isExecutableStep(step) {
	return isSingleStepEntry(step) || step.type === "loop" || step.type === "foreach";
}
//#endregion
//#region src/workflows/evented/helpers.ts
/**
* Helper functions for evented workflow execution.
*/
/**
* Type guard to check if a chunk is a tripwire chunk.
* @param chunk - The chunk to check
* @returns True if the chunk is a TripwireChunk
*/
function isTripwireChunk(chunk) {
	return chunk !== null && typeof chunk === "object" && "type" in chunk && chunk.type === "tripwire" && "payload" in chunk;
}
/**
* Creates a TripWire error from a tripwire chunk.
* @param chunk - The tripwire chunk from the stream
* @returns A TripWire error instance
*/
function createTripWireFromChunk(chunk) {
	const { payload } = chunk;
	return new require_trip_wire.TripWire(payload.reason || "Agent tripwire triggered", {
		retry: payload.retry,
		metadata: payload.metadata
	}, payload.processorId);
}
/**
* Extracts text delta from a stream chunk, handling V1 vs V2 differences.
*
* V1 (AI SDK v4): Uses `chunk.textDelta` for raw text
* V2 (AI SDK v5): Uses `chunk.payload.text` for normalized text
*
* @param chunk - The stream chunk
* @param isV2Model - Whether this is a V2 model (uses normalized payload)
* @returns The text delta string, or undefined if not a text-delta chunk
*/
function getTextDeltaFromChunk(chunk, isV2Model) {
	if (chunk.type !== "text-delta") return;
	return isV2Model ? chunk.payload?.text : chunk.textDelta;
}
/**
* Resolves the current workflow state from multiple potential sources.
* Priority order: stepResult.__state > stepResults.__state > state > empty object
*
* @param params - The state sources to check
* @returns The resolved state object
*/
function resolveCurrentState(params) {
	const { stepResult, stepResults, state } = params;
	return stepResult?.__state ?? stepResults?.__state ?? state ?? {};
}
//#endregion
//#region src/events/processor.ts
var EventProcessor = class {
	mastra;
	__registerMastra(mastra) {
		this.mastra = mastra;
	}
	constructor({ mastra }) {
		this.mastra = mastra;
	}
};
//#endregion
//#region src/workflows/evented/step-executor.ts
var StepExecutor = class extends require_base.MastraBase {
	mastra;
	constructor({ mastra }) {
		super({
			name: "StepExecutor",
			component: require_logger.RegisteredLogger.WORKFLOW
		});
		this.mastra = mastra;
	}
	__registerMastra(mastra) {
		this.mastra = mastra;
		const logger = mastra?.getLogger();
		if (logger) this.__setLogger(logger);
	}
	/**
	* Creates an output writer function that publishes chunks to the workflow event stream.
	* @param runId - The workflow run ID
	* @returns An async function that writes chunks to the pubsub
	*/
	createOutputWriter(runId) {
		return async (chunk) => {
			try {
				if (this.mastra?.pubsub) await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {
					type: "watch",
					runId,
					data: chunk
				});
			} catch (err) {
				this.logger.debug("Failed to publish workflow watch event", {
					runId,
					error: err
				});
			}
		};
	}
	async execute(params) {
		const { entry, stepResults, runId, requestContext, retryCount = 0, perStep } = params;
		const stepId = getEntryId(entry);
		const schemas = getEntrySchemas(entry, this.mastra);
		const abortController = params.abortController ?? new AbortController();
		let suspended;
		let bailed;
		const startedAt = Date.now();
		const { inputData, validationError } = await validateStepInput({
			prevOutput: typeof params.foreachIdx === "number" ? params.input?.[params.foreachIdx] : params.input,
			step: schemas,
			validateInputs: params.validateInputs ?? true
		});
		let stepInfo = {
			...stepResults[stepId],
			startedAt,
			payload: (typeof params.foreachIdx === "number" ? params.input : inputData) ?? {}
		};
		if (params.resumeData) {
			stepInfo.resumePayload = params.resumeData;
			stepInfo.resumedAt = Date.now();
			if (stepInfo.suspendPayload && "__workflow_meta" in stepInfo.suspendPayload) {
				const { __workflow_meta, ...userSuspendPayload } = stepInfo.suspendPayload;
				stepInfo.suspendPayload = userSuspendPayload;
			}
		}
		let suspendDataToUse = params.stepResults[stepId]?.status === "suspended" ? params.stepResults[stepId]?.suspendPayload : void 0;
		if (suspendDataToUse && typeof params.foreachIdx === "number") {
			const iterationResult = suspendDataToUse.__workflow_meta?.foreachOutput?.[params.foreachIdx];
			if (iterationResult?.status === "suspended" && iterationResult.suspendPayload) suspendDataToUse = iterationResult.suspendPayload;
		}
		if (suspendDataToUse && "__workflow_meta" in suspendDataToUse) {
			const { __workflow_meta, ...userSuspendData } = suspendDataToUse;
			suspendDataToUse = userSuspendData;
		}
		let stateUpdate;
		const workflowStepSpan = params.tracingContext?.currentSpan?.createChildSpan({
			type: "workflow_step",
			name: `workflow step: '${stepId}'`,
			entityType: require_utils.EntityType.WORKFLOW_STEP,
			entityId: stepId,
			input: inputData,
			tracingPolicy: params.tracingPolicy,
			requestContext
		});
		const stepTracingContext = workflowStepSpan ? { currentSpan: workflowStepSpan } : params.tracingContext ?? {};
		try {
			if (validationError) throw validationError;
			const callId = (0, crypto.randomUUID)();
			const outputWriter = this.createOutputWriter(runId);
			const stepOutput = await require_utils.executeWithContext({
				span: stepTracingContext.currentSpan,
				fn: () => {
					const executionContext = createDeprecationProxy({
						workflowId: params.workflowId,
						runId,
						mastra: this.mastra,
						requestContext,
						inputData,
						state: params.state,
						setState: async (newState) => {
							stateUpdate = {
								...stateUpdate ?? params.state,
								...newState
							};
						},
						retryCount,
						resumeData: params.resumeData,
						suspendData: suspendDataToUse,
						getInitData: () => stepResults?.input,
						getStepResult: getStepResult.bind(this, stepResults),
						suspend: async (suspendPayload, suspendOptions) => {
							const { suspendData, validationError } = await validateStepSuspendData({
								suspendData: suspendPayload,
								step: schemas,
								validateInputs: params.validateInputs ?? true
							});
							if (validationError) throw validationError;
							const resumeLabels = {};
							if (suspendOptions?.resumeLabel) {
								const labels = Array.isArray(suspendOptions.resumeLabel) ? suspendOptions.resumeLabel : [suspendOptions.resumeLabel];
								for (const label of labels) resumeLabels[label] = {
									stepId,
									foreachIndex: params.foreachIdx
								};
							}
							suspended = { payload: {
								...suspendData,
								__workflow_meta: {
									runId,
									path: [stepId],
									foreachIndex: params.foreachIdx,
									resumeLabels: Object.keys(resumeLabels).length > 0 ? resumeLabels : void 0
								}
							} };
						},
						bail: (result) => {
							bailed = { payload: result };
						},
						writer: new require_types.ToolStream({
							prefix: "workflow-step",
							callId,
							name: stepId,
							runId
						}, outputWriter),
						abort: () => {
							abortController?.abort();
						},
						[require_workflows_constants.PUBSUB_SYMBOL]: this.mastra.pubsub,
						[require_workflows_constants.STREAM_FORMAT_SYMBOL]: params.format,
						engine: {},
						abortSignal: abortController?.signal,
						...require_observability.createObservabilityContext(stepTracingContext)
					}, {
						paramName: "runCount",
						deprecationMessage: runCountDeprecationMessage,
						logger: this.logger
					});
					switch (entry.type) {
						case "step": return entry.step.execute(executionContext);
						case "agent": return runAgentEntry(entry, executionContext, this.mastra);
						case "tool": return runToolEntry(entry, executionContext, this.mastra);
						case "mapping": return runMappingEntry(entry, executionContext);
					}
				}
			});
			const nestedWflowStepPaused = getEntryComponent(entry) === "WORKFLOW" && perStep;
			const endedAt = Date.now();
			const finalState = stateUpdate ?? params.state;
			let finalResult;
			if (suspended) {
				finalResult = {
					...stepInfo,
					status: "suspended",
					suspendedAt: endedAt,
					...stepOutput ? { suspendOutput: stepOutput } : {},
					__state: finalState
				};
				if (suspended.payload) finalResult.suspendPayload = suspended.payload;
			} else if (bailed) finalResult = {
				...stepInfo,
				status: "bailed",
				endedAt,
				output: bailed.payload,
				__state: finalState
			};
			else if (nestedWflowStepPaused) finalResult = {
				...stepInfo,
				status: "paused",
				__state: finalState
			};
			else finalResult = {
				...stepInfo,
				status: "success",
				endedAt,
				output: stepOutput,
				__state: finalState
			};
			if (finalResult.status === "success") workflowStepSpan?.end({
				output: stepOutput,
				attributes: { status: "success" }
			});
			else workflowStepSpan?.end({ attributes: { status: finalResult.status } });
			return finalResult;
		} catch (error) {
			const endedAt = Date.now();
			const errorInstance = require_error.getErrorFromUnknown(error, {
				serializeStack: false,
				fallbackMessage: "Unknown step execution error"
			});
			workflowStepSpan?.error({ error: errorInstance });
			const mastraError = new require_error.MastraError({
				id: "WORKFLOW_STEP_INVOKE_FAILED",
				domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
				category: require_error.ErrorCategory.USER,
				details: {
					workflowId: params.workflowId,
					runId: params.runId,
					stepId
				}
			}, errorInstance);
			this.logger?.trackException(mastraError);
			this.logger?.error(`Error executing step ${stepId}: ` + errorInstance?.stack);
			return {
				...stepInfo,
				status: "failed",
				endedAt,
				error: errorInstance,
				...error instanceof require_error.MastraNonRetryableError && { nonRetryable: true },
				tripwire: error instanceof require_trip_wire.TripWire ? {
					reason: error.message,
					retry: error.options?.retry,
					metadata: error.options?.metadata,
					processorId: error.processorId
				} : void 0
			};
		}
	}
	async evaluateConditions(params) {
		const { step, stepResults, runId, requestContext, retryCount = 0 } = params;
		const abortController = params.abortController ?? new AbortController();
		return (await Promise.all(step.conditions.map((condition) => {
			try {
				return this.evaluateCondition({
					workflowId: params.workflowId,
					condition,
					runId,
					requestContext,
					inputData: params.input,
					state: params.state,
					retryCount,
					resumeData: params.resumeData,
					abortController,
					stepResults,
					iterationCount: 0
				});
			} catch (e) {
				this.mastra?.getLogger()?.error("error evaluating condition", e);
				return false;
			}
		}))).reduce((acc, result, idx) => {
			if (result) acc.push(idx);
			return acc;
		}, []);
	}
	async evaluateCondition({ workflowId, condition, runId, inputData, resumeData, stepResults, state, requestContext, abortController, retryCount = 0, iterationCount }) {
		const callId = (0, crypto.randomUUID)();
		const outputWriter = this.createOutputWriter(runId);
		return condition(createDeprecationProxy({
			workflowId,
			runId,
			mastra: this.mastra,
			requestContext,
			inputData,
			state,
			retryCount,
			resumeData,
			getInitData: () => stepResults?.input,
			getStepResult: getStepResult.bind(this, stepResults),
			bail: (_result) => {
				throw new Error("Not implemented");
			},
			writer: new require_types.ToolStream({
				prefix: "workflow-step",
				callId,
				name: "condition",
				runId
			}, outputWriter),
			abort: () => {
				abortController?.abort();
			},
			[require_workflows_constants.PUBSUB_SYMBOL]: this.mastra.pubsub,
			[require_workflows_constants.STREAM_FORMAT_SYMBOL]: void 0,
			engine: {},
			abortSignal: abortController?.signal,
			...require_observability.createObservabilityContext(),
			iterationCount
		}, {
			paramName: "runCount",
			deprecationMessage: runCountDeprecationMessage,
			logger: this.logger
		}));
	}
	async resolveSleep(params) {
		const { step, stepResults, runId, requestContext, retryCount = 0 } = params;
		const currentState = params.state ?? stepResults?.__state ?? {};
		const abortController = params.abortController ?? new AbortController();
		if (step.duration) return step.duration;
		if (!step.fn) return 0;
		try {
			const callId = (0, crypto.randomUUID)();
			const outputWriter = this.createOutputWriter(runId);
			return await step.fn(createDeprecationProxy({
				workflowId: params.workflowId,
				runId,
				mastra: this.mastra,
				requestContext,
				inputData: params.input,
				state: currentState,
				setState: async (newState) => {
					Object.assign(currentState, newState);
				},
				retryCount,
				resumeData: params.resumeData,
				getInitData: () => stepResults?.input,
				getStepResult: getStepResult.bind(this, stepResults),
				suspend: async (_suspendPayload) => {
					throw new Error("Not implemented");
				},
				bail: (_result) => {
					throw new Error("Not implemented");
				},
				abort: () => {
					abortController?.abort();
				},
				writer: new require_types.ToolStream({
					prefix: "workflow-step",
					callId,
					name: step.id,
					runId
				}, outputWriter),
				[require_workflows_constants.PUBSUB_SYMBOL]: this.mastra.pubsub,
				[require_workflows_constants.STREAM_FORMAT_SYMBOL]: void 0,
				engine: {},
				abortSignal: abortController?.signal,
				...require_observability.createObservabilityContext()
			}, {
				paramName: "runCount",
				deprecationMessage: runCountDeprecationMessage,
				logger: this.logger
			}));
		} catch (e) {
			this.mastra?.getLogger()?.error("error evaluating condition", e);
			return 0;
		}
	}
	async resolveSleepUntil(params) {
		const { step, stepResults, runId, requestContext, retryCount = 0 } = params;
		const currentState = params.state ?? stepResults?.__state ?? {};
		const abortController = params.abortController ?? new AbortController();
		if (step.date) return step.date.getTime() - Date.now();
		if (!step.fn) return 0;
		try {
			const callId = (0, crypto.randomUUID)();
			const outputWriter = this.createOutputWriter(runId);
			return (await step.fn(createDeprecationProxy({
				workflowId: params.workflowId,
				runId,
				mastra: this.mastra,
				requestContext,
				inputData: params.input,
				state: currentState,
				setState: async (newState) => {
					Object.assign(currentState, newState);
				},
				retryCount,
				resumeData: params.resumeData,
				getInitData: () => stepResults?.input,
				getStepResult: getStepResult.bind(this, stepResults),
				suspend: async (_suspendPayload) => {
					throw new Error("Not implemented");
				},
				bail: (_result) => {
					throw new Error("Not implemented");
				},
				abort: () => {
					abortController?.abort();
				},
				writer: new require_types.ToolStream({
					prefix: "workflow-step",
					callId,
					name: step.id,
					runId
				}, outputWriter),
				[require_workflows_constants.PUBSUB_SYMBOL]: this.mastra.pubsub,
				[require_workflows_constants.STREAM_FORMAT_SYMBOL]: void 0,
				engine: {},
				abortSignal: abortController?.signal,
				...require_observability.createObservabilityContext()
			}, {
				paramName: "runCount",
				deprecationMessage: runCountDeprecationMessage,
				logger: this.logger
			}))).getTime() - Date.now();
		} catch (e) {
			this.mastra?.getLogger()?.error("error evaluating condition", e);
			return 0;
		}
	}
};
//#endregion
//#region src/workflows/evented/types.ts
/**
* Types and utilities for evented workflow execution.
*/
/**
* String key used to mark pending forEach iterations.
* Using a string key (not Symbol) ensures the marker survives JSON serialization
* which is critical for distributed execution where state is persisted to storage
* and loaded by different engine instances.
*/
const PENDING_MARKER_KEY = "__mastra_pending__";
/**
* Creates a new pending marker object.
* Used to mark forEach iterations that are about to be resumed.
*/
function createPendingMarker() {
	return { [PENDING_MARKER_KEY]: true };
}
//#endregion
//#region src/workflows/evented/workflow-event-processor/loop.ts
async function processWorkflowLoop({ workflowId, prevResult, runId, executionPath, stepResults, activeStepsPath, resumeSteps, resumeData, parentWorkflow, requestContext, retryCount = 0, perStep, state, outputOptions }, { pubsub, stepExecutor, step, stepResult }) {
	const currentState = resolveCurrentState({
		stepResult,
		stepResults,
		state
	});
	const reqContext = new require_request_context.RequestContext(Object.entries(requestContext ?? {}));
	const iterationCount = (stepResults[getEntryId(step.step)]?.metadata?.iterationCount ?? 0) + 1;
	const loopCondition = await stepExecutor.evaluateCondition({
		workflowId,
		condition: step.condition,
		runId,
		stepResults,
		state: currentState,
		requestContext: reqContext,
		inputData: prevResult?.status === "success" ? prevResult.output : void 0,
		resumeData,
		abortController: new AbortController(),
		retryCount,
		iterationCount
	});
	const loopAgainData = {
		parentWorkflow,
		workflowId,
		runId,
		executionPath,
		resumeSteps: [],
		stepResults: {
			...stepResults,
			[getEntryId(step.step)]: {
				...stepResults[getEntryId(step.step)],
				metadata: {
					...stepResults[getEntryId(step.step)]?.metadata,
					iterationCount
				}
			}
		},
		prevResult: stepResult,
		resumeData: void 0,
		activeStepsPath,
		requestContext,
		retryCount,
		perStep,
		state: currentState,
		outputOptions
	};
	const loopEndData = {
		parentWorkflow,
		workflowId,
		runId,
		executionPath,
		resumeSteps,
		stepResults,
		prevResult: stepResult,
		resumeData,
		activeStepsPath,
		requestContext,
		perStep,
		state: currentState,
		outputOptions
	};
	if (step.loopType === "dountil") if (loopCondition) await pubsub.publish("workflows", {
		type: "workflow.step.end",
		runId,
		data: loopEndData
	});
	else await pubsub.publish("workflows", {
		type: "workflow.step.run",
		runId,
		data: loopAgainData
	});
	else if (loopCondition) await pubsub.publish("workflows", {
		type: "workflow.step.run",
		runId,
		data: loopAgainData
	});
	else await pubsub.publish("workflows", {
		type: "workflow.step.end",
		runId,
		data: loopEndData
	});
}
async function processWorkflowForEach({ workflowId, prevResult, runId, executionPath, stepResults, activeStepsPath, resumeSteps, timeTravel, restart, resumeData, parentWorkflow, requestContext, perStep, state, outputOptions, forEachIndex }, { pubsub, mastra, step }) {
	const currentState = resolveCurrentState({
		stepResults,
		state
	});
	const currentResult = stepResults[getEntryId(step.step)];
	const idx = currentResult?.output?.length ?? 0;
	const targetLen = prevResult?.output?.length ?? 0;
	if (forEachIndex !== void 0 && resumeSteps?.length > 0 && idx > 0) {
		const outputArray = currentResult?.output;
		const outputLength = Array.isArray(outputArray) ? outputArray.length : 0;
		if (!Array.isArray(outputArray) || forEachIndex < 0 || forEachIndex >= outputLength) {
			const error = /* @__PURE__ */ new Error(`Invalid forEachIndex ${forEachIndex} for forEach resume: expected index in range [0, ${outputLength - 1}] but output array has length ${outputLength}`);
			await pubsub.publish("workflows", {
				type: "workflow.fail",
				runId,
				data: {
					parentWorkflow,
					workflowId,
					runId,
					executionPath,
					resumeSteps,
					stepResults,
					prevResult: {
						status: "failed",
						error
					},
					activeStepsPath,
					requestContext,
					state: currentState,
					outputOptions
				}
			});
			return;
		}
		const iterationResult = currentResult?.output?.[forEachIndex];
		if (iterationResult?.status === "suspended" || iterationResult === null) {
			const isNestedWorkflow = getEntryWorkflow(step.step) !== null;
			const targetArray = prevResult?.output;
			const iterationPrevResult = isNestedWorkflow && prevResult.status === "success" && Array.isArray(targetArray) ? {
				status: "success",
				output: targetArray[forEachIndex]
			} : prevResult;
			await pubsub.publish("workflows", {
				type: "workflow.step.run",
				runId,
				data: {
					parentWorkflow,
					workflowId,
					runId,
					executionPath: [executionPath[0], forEachIndex],
					resumeSteps,
					timeTravel,
					restart,
					stepResults,
					prevResult: iterationPrevResult,
					resumeData,
					activeStepsPath,
					requestContext,
					perStep,
					state: currentState,
					outputOptions
				}
			});
			return;
		}
		if (currentResult.output.filter((r) => r === null || r?.status === "suspended").length > 0) {
			const collectedResumeLabels = {};
			let firstSuspendedIterationPayload;
			for (let i = 0; i < currentResult.output.length; i++) {
				const iterResult = currentResult.output[i];
				if (iterResult?.status === "suspended") {
					if (iterResult.suspendPayload?.__workflow_meta?.resumeLabels) Object.assign(collectedResumeLabels, iterResult.suspendPayload.__workflow_meta.resumeLabels);
					if (firstSuspendedIterationPayload === void 0) firstSuspendedIterationPayload = iterResult.suspendPayload;
				}
			}
			const suspendMeta = { foreachIndex: forEachIndex };
			if (Object.keys(collectedResumeLabels).length > 0) suspendMeta.resumeLabels = collectedResumeLabels;
			const aggregatedSuspendPayload = {
				...firstSuspendedIterationPayload,
				__workflow_meta: suspendMeta
			};
			await pubsub.publish("workflows", {
				type: "workflow.step.end",
				runId,
				data: {
					parentWorkflow,
					workflowId,
					runId,
					executionPath,
					resumeSteps,
					stepResults: {
						...stepResults,
						[getEntryId(step.step)]: {
							...currentResult,
							status: "suspended",
							suspendedAt: Date.now(),
							suspendPayload: aggregatedSuspendPayload
						}
					},
					prevResult: {
						status: "suspended",
						output: currentResult.output,
						suspendPayload: aggregatedSuspendPayload,
						payload: currentResult.payload,
						startedAt: currentResult.startedAt,
						suspendedAt: Date.now()
					},
					activeStepsPath,
					requestContext,
					state: currentState,
					outputOptions
				}
			});
			return;
		}
		return;
	}
	if (resumeData !== void 0 && forEachIndex === void 0 && currentResult?.output?.length > 0) {
		const suspendedIndices = [];
		for (let i = 0; i < currentResult.output.length; i++) {
			const iterResult = currentResult.output[i];
			if (iterResult && typeof iterResult === "object" && iterResult.status === "suspended") suspendedIndices.push(i);
		}
		if (suspendedIndices.length > 0) {
			const concurrency = resolveForeachConcurrency(step.opts, {
				inputData: prevResult?.output,
				getInitData: () => stepResults?.input
			});
			const indicesToResume = suspendedIndices.slice(0, concurrency);
			const workflowsStore = await mastra.getStorage()?.getStore("workflows");
			const updatedOutput = [...currentResult.output];
			for (const suspIdx of indicesToResume) updatedOutput[suspIdx] = createPendingMarker();
			await workflowsStore?.updateWorkflowResults({
				workflowName: workflowId,
				runId,
				stepId: getEntryId(step.step),
				result: {
					...currentResult,
					output: updatedOutput
				},
				requestContext
			});
			const isNestedWorkflow = getEntryWorkflow(step.step) !== null;
			for (const suspIdx of indicesToResume) {
				const targetArray = prevResult?.output;
				const iterationPrevResult = isNestedWorkflow && prevResult.status === "success" && Array.isArray(targetArray) ? {
					status: "success",
					output: targetArray[suspIdx]
				} : prevResult;
				try {
					await pubsub.publish("workflows", {
						type: "workflow.step.run",
						runId,
						data: {
							parentWorkflow,
							workflowId,
							runId,
							executionPath: [executionPath[0], suspIdx],
							resumeSteps,
							timeTravel,
							restart,
							stepResults,
							prevResult: iterationPrevResult,
							resumeData,
							activeStepsPath,
							requestContext,
							perStep,
							state: currentState,
							outputOptions
						}
					});
				} catch {}
			}
			return;
		}
	}
	const workflowsStore = await mastra.getStorage()?.getStore("workflows");
	if (idx >= targetLen && currentResult?.output?.filter((r) => r !== null)?.length >= targetLen || prevResult?.output?.length === 0) {
		let result = currentResult;
		if (prevResult?.output?.length === 0) {
			result = {
				status: "success",
				output: [],
				startedAt: Date.now(),
				endedAt: Date.now(),
				payload: prevResult?.output
			};
			await workflowsStore?.updateWorkflowResults({
				workflowName: workflowId,
				runId,
				stepId: getEntryId(step.step),
				result,
				requestContext
			});
			stepResults[getEntryId(step.step)] = result;
		}
		await pubsub.publish("workflows", {
			type: "workflow.step.run",
			runId,
			data: {
				parentWorkflow,
				workflowId,
				runId,
				executionPath: executionPath.slice(0, -1).concat([executionPath[executionPath.length - 1] + 1]),
				resumeSteps,
				stepResults,
				timeTravel,
				restart,
				prevResult: result,
				resumeData: void 0,
				activeStepsPath,
				requestContext,
				perStep,
				state: currentState,
				outputOptions
			}
		});
		return;
	} else if (idx >= targetLen) return;
	if (executionPath.length === 1 && idx === 0) {
		const resolvedConcurrency = resolveForeachConcurrency(step.opts, {
			inputData: prevResult?.output,
			getInitData: () => stepResults?.input
		});
		const concurrency = Math.min(resolvedConcurrency, targetLen);
		const dummyResult = Array.from({ length: concurrency }, () => null);
		await workflowsStore?.updateWorkflowResults({
			workflowName: workflowId,
			runId,
			stepId: getEntryId(step.step),
			result: {
				status: "success",
				output: dummyResult,
				startedAt: Date.now(),
				payload: prevResult?.output
			},
			requestContext
		});
		const isNestedWorkflow = getEntryWorkflow(step.step) !== null;
		for (let i = 0; i < concurrency; i++) {
			const targetArray = prevResult?.output;
			const iterationPrevResult = isNestedWorkflow && prevResult.status === "success" && Array.isArray(targetArray) ? {
				status: "success",
				output: targetArray[i]
			} : prevResult;
			await pubsub.publish("workflows", {
				type: "workflow.step.run",
				runId,
				data: {
					parentWorkflow,
					workflowId,
					runId,
					executionPath: [executionPath[0], i],
					resumeSteps,
					stepResults,
					timeTravel,
					restart,
					prevResult: iterationPrevResult,
					resumeData,
					activeStepsPath,
					requestContext,
					perStep,
					state: currentState,
					outputOptions
				}
			});
		}
		return;
	}
	currentResult.output.push(null);
	await workflowsStore?.updateWorkflowResults({
		workflowName: workflowId,
		runId,
		stepId: getEntryId(step.step),
		result: {
			status: "success",
			output: currentResult.output,
			startedAt: Date.now(),
			payload: prevResult?.output
		},
		requestContext
	});
	const isNestedWorkflow = getEntryWorkflow(step.step) !== null;
	const targetArray = prevResult?.output;
	const iterationPrevResult = isNestedWorkflow && prevResult.status === "success" && Array.isArray(targetArray) ? {
		status: "success",
		output: targetArray[idx]
	} : prevResult;
	await pubsub.publish("workflows", {
		type: "workflow.step.run",
		runId,
		data: {
			parentWorkflow,
			workflowId,
			runId,
			executionPath: [executionPath[0], idx],
			resumeSteps,
			timeTravel,
			restart,
			stepResults,
			prevResult: iterationPrevResult,
			resumeData,
			activeStepsPath,
			requestContext,
			perStep,
			state: currentState,
			outputOptions
		}
	});
}
//#endregion
//#region src/workflows/evented/workflow-event-processor/parallel.ts
async function processWorkflowParallel({ workflowId, runId, executionPath, stepResults, activeStepsPath, resumeSteps, timeTravel, restart, prevResult, resumeData, parentWorkflow, requestContext, perStep, state, outputOptions }, { pubsub, step }) {
	const pathsToRun = {};
	const currentState = resolveCurrentState({
		stepResults,
		state
	});
	for (let i = 0; i < step.steps.length; i++) {
		const nestedStep = step.steps[i];
		if (nestedStep) {
			const nestedStepId = getSingleStepEntryId(nestedStep);
			if (restart) pathsToRun[nestedStepId] = !!restart.activeStepsPath[nestedStepId];
			else pathsToRun[nestedStepId] = true;
			if (perStep) break;
		}
	}
	await Promise.all(step.steps?.map(async (child, idx) => {
		if (!pathsToRun[getSingleStepEntryId(child)]) return;
		return pubsub.publish("workflows", {
			type: "workflow.step.run",
			runId,
			data: {
				workflowId,
				runId,
				executionPath: restart ? executionPath.slice(0, -1).concat([idx]) : executionPath.concat([idx]),
				resumeSteps,
				stepResults,
				prevResult,
				resumeData,
				timeTravel,
				restart: restart ? {
					...restart,
					isParallelOrConditionalRestarted: true
				} : void 0,
				parentWorkflow,
				activeStepsPath,
				requestContext,
				perStep,
				state: currentState,
				outputOptions
			}
		});
	}));
}
async function processWorkflowConditional({ workflowId, runId, executionPath, stepResults, activeStepsPath, resumeSteps, timeTravel, restart, prevResult, resumeData, parentWorkflow, requestContext, perStep, state, outputOptions }, { pubsub, stepExecutor, step }) {
	const currentState = resolveCurrentState({
		stepResults,
		state
	});
	const reqContext = new require_request_context.RequestContext(Object.entries(requestContext ?? {}));
	const idxs = await stepExecutor.evaluateConditions({
		workflowId,
		step,
		runId,
		stepResults,
		state: currentState,
		requestContext: reqContext,
		input: prevResult?.status === "success" ? prevResult.output : void 0,
		resumeData
	});
	const truthyIdxs = {};
	for (let i = 0; i < idxs.length; i++) truthyIdxs[idxs[i]] = true;
	let onlyStepToRun;
	if (perStep) onlyStepToRun = step.steps.filter((_, idx) => truthyIdxs[idx])[0];
	if (onlyStepToRun) {
		const onlyStepToRunId = getSingleStepEntryId(onlyStepToRun);
		const stepIndex = step.steps.findIndex((child) => getSingleStepEntryId(child) === onlyStepToRunId);
		activeStepsPath[onlyStepToRunId] = executionPath.concat([stepIndex]);
		await pubsub.publish("workflows", {
			type: "workflow.step.run",
			runId,
			data: {
				workflowId,
				runId,
				executionPath: executionPath.concat([stepIndex]),
				resumeSteps,
				stepResults,
				timeTravel,
				restart,
				prevResult,
				resumeData,
				parentWorkflow,
				activeStepsPath,
				requestContext,
				perStep,
				state: currentState,
				outputOptions
			}
		});
	} else await Promise.all(step.steps.map(async (child, idx) => {
		if (truthyIdxs[idx]) {
			if (child) activeStepsPath[getSingleStepEntryId(child)] = executionPath.concat([idx]);
			return pubsub.publish("workflows", {
				type: "workflow.step.run",
				runId,
				data: {
					workflowId,
					runId,
					executionPath: executionPath.concat([idx]),
					resumeSteps,
					stepResults,
					timeTravel,
					restart: restart ? {
						...restart,
						isParallelOrConditionalRestarted: true
					} : void 0,
					prevResult,
					resumeData,
					parentWorkflow,
					activeStepsPath,
					requestContext,
					perStep,
					state: currentState,
					outputOptions
				}
			});
		} else return pubsub.publish("workflows", {
			type: "workflow.step.end",
			runId,
			data: {
				workflowId,
				runId,
				executionPath: executionPath.concat([idx]),
				resumeSteps,
				stepResults,
				prevResult: { status: "skipped" },
				resumeData,
				parentWorkflow,
				activeStepsPath,
				requestContext,
				perStep,
				state: currentState,
				outputOptions
			}
		});
	}));
}
//#endregion
//#region src/workflows/evented/workflow-event-processor/sleep.ts
async function processWorkflowWaitForEvent(workflowData, { pubsub, eventName, currentState }) {
	const executionPath = currentState?.waitingPaths[eventName];
	if (!executionPath) return;
	const currentStepId = getStepId(workflowData.workflow, executionPath);
	const prevResult = {
		status: "success",
		output: currentState?.context[currentStepId ?? "input"]?.payload
	};
	await pubsub.publish("workflows", {
		type: "workflow.step.run",
		runId: workflowData.runId,
		data: {
			workflowId: workflowData.workflowId,
			runId: workflowData.runId,
			executionPath,
			resumeSteps: [],
			resumeData: workflowData.resumeData,
			parentWorkflow: workflowData.parentWorkflow,
			stepResults: currentState?.context,
			prevResult,
			activeStepsPath: {},
			requestContext: currentState?.requestContext,
			perStep: workflowData.perStep
		}
	});
}
async function processWorkflowSleep({ workflowId, runId, executionPath, stepResults, activeStepsPath, resumeSteps, timeTravel, restart, prevResult, resumeData, parentWorkflow, requestContext, perStep }, { pubsub, stepExecutor, step }) {
	const startedAt = Date.now();
	await pubsub.publish(`workflow.events.v2.${runId}`, {
		type: "watch",
		runId,
		data: {
			type: "workflow-step-waiting",
			payload: {
				id: step.id,
				status: "waiting",
				payload: prevResult.status === "success" ? prevResult.output : void 0,
				startedAt
			}
		}
	});
	const reqContext = new require_request_context.RequestContext(Object.entries(requestContext ?? {}));
	const duration = await stepExecutor.resolveSleep({
		workflowId,
		step,
		runId,
		stepResults,
		requestContext: reqContext,
		input: prevResult?.status === "success" ? prevResult.output : void 0,
		resumeData
	});
	setTimeout(async () => {
		await pubsub.publish(`workflow.events.v2.${runId}`, {
			type: "watch",
			runId,
			data: {
				type: "workflow-step-result",
				payload: {
					id: step.id,
					status: "success",
					payload: prevResult.status === "success" ? prevResult.output : void 0,
					output: prevResult.status === "success" ? prevResult.output : void 0,
					startedAt,
					endedAt: Date.now()
				}
			}
		});
		await pubsub.publish(`workflow.events.v2.${runId}`, {
			type: "watch",
			runId,
			data: {
				type: "workflow-step-finish",
				payload: {
					id: step.id,
					metadata: {}
				}
			}
		});
		await pubsub.publish("workflows", {
			type: "workflow.step.run",
			runId,
			data: {
				workflowId,
				runId,
				executionPath: executionPath.slice(0, -1).concat([executionPath[executionPath.length - 1] + 1]),
				resumeSteps,
				timeTravel,
				restart,
				stepResults,
				prevResult,
				resumeData,
				parentWorkflow,
				activeStepsPath,
				requestContext,
				perStep
			}
		});
	}, duration < 0 ? 0 : duration);
}
async function processWorkflowSleepUntil({ workflowId, runId, executionPath, stepResults, activeStepsPath, resumeSteps, timeTravel, restart, prevResult, resumeData, parentWorkflow, requestContext, perStep }, { pubsub, stepExecutor, step }) {
	const startedAt = Date.now();
	const reqContext = new require_request_context.RequestContext(Object.entries(requestContext ?? {}));
	const duration = await stepExecutor.resolveSleepUntil({
		workflowId,
		step,
		runId,
		stepResults,
		requestContext: reqContext,
		input: prevResult?.status === "success" ? prevResult.output : void 0,
		resumeData
	});
	await pubsub.publish(`workflow.events.v2.${runId}`, {
		type: "watch",
		runId,
		data: {
			type: "workflow-step-waiting",
			payload: {
				id: step.id,
				status: "waiting",
				payload: prevResult.status === "success" ? prevResult.output : void 0,
				startedAt
			}
		}
	});
	setTimeout(async () => {
		await pubsub.publish(`workflow.events.v2.${runId}`, {
			type: "watch",
			runId,
			data: {
				type: "workflow-step-result",
				payload: {
					id: step.id,
					status: "success",
					payload: prevResult.status === "success" ? prevResult.output : void 0,
					output: prevResult.status === "success" ? prevResult.output : void 0,
					startedAt,
					endedAt: Date.now()
				}
			}
		});
		await pubsub.publish(`workflow.events.v2.${runId}`, {
			type: "watch",
			runId,
			data: {
				type: "workflow-step-finish",
				payload: {
					id: step.id,
					metadata: {}
				}
			}
		});
		await pubsub.publish("workflows", {
			type: "workflow.step.run",
			runId,
			data: {
				workflowId,
				runId,
				executionPath: executionPath.slice(0, -1).concat([executionPath[executionPath.length - 1] + 1]),
				resumeSteps,
				timeTravel,
				restart,
				stepResults,
				prevResult,
				resumeData,
				parentWorkflow,
				activeStepsPath,
				requestContext,
				perStep
			}
		});
	}, duration < 0 ? 0 : duration);
}
//#endregion
//#region src/workflows/evented/workflow-event-processor/index.ts
function readForeachResult(stepResults, id) {
	return stepResults[id];
}
var WorkflowEventProcessor = class WorkflowEventProcessor extends EventProcessor {
	stepExecutor;
	stepExecutionStrategy;
	abortControllers = /* @__PURE__ */ new Map();
	parentChildRelationships = /* @__PURE__ */ new Map();
	runFormats = /* @__PURE__ */ new Map();
	deliveryAttempts = /* @__PURE__ */ new Map();
	static MAX_DELIVERY_ATTEMPTS = 3;
	static TERMINAL_SENTINEL = Number.POSITIVE_INFINITY;
	static DELIVERY_ATTEMPTS_MAX_ENTRIES = 1024;
	topicCleanupDelayMs;
	static DEFAULT_TOPIC_CLEANUP_DELAY_MS = 3e4;
	pendingTopicCleanups = /* @__PURE__ */ new Map();
	static ACTIVE_RUN_STATUSES = /* @__PURE__ */ new Set([
		"running",
		"pending",
		"waiting",
		"suspended",
		"paused"
	]);
	constructor({ mastra, stepExecutionStrategy, topicCleanupDelayMs }) {
		super({ mastra });
		this.stepExecutor = new StepExecutor({ mastra });
		this.stepExecutionStrategy = stepExecutionStrategy;
		this.topicCleanupDelayMs = topicCleanupDelayMs ?? WorkflowEventProcessor.DEFAULT_TOPIC_CLEANUP_DELAY_MS;
	}
	/**
	* Schedule deletion of a finished run's `workflow.events.v2.<runId>` topic.
	*
	* Per-run watch topics are written by every step of a run; on transports
	* that retain messages (e.g. Redis Streams) they would otherwise live
	* forever once the run ends. Deletion is delayed so subscribers still
	* draining the terminal `workflow-finish` event aren't cut off, and
	* fire-and-forget because topic cleanup must never affect run completion.
	*
	* Best-effort by design: if the process exits before the timer fires, the
	* transport-level idle TTL (e.g. `streamIdleTtlMs`) is the backstop.
	*
	* A finished run can be re-executed under the same runId (`timeTravel`,
	* `restart`), so deletion is double-guarded: a restart processed by this
	* process cancels the pending timer directly, and when the timer fires we
	* re-check the run's persisted status — a restart may have been picked up
	* by a different worker process — and skip deletion while the run is
	* active again.
	*/
	scheduleRunTopicCleanup(workflowId, runId) {
		if (this.topicCleanupDelayMs <= 0) return;
		this.cancelRunTopicCleanup(runId);
		const timer = setTimeout(() => {
			this.pendingTopicCleanups.delete(runId);
			this.clearRunTopicUnlessActive(workflowId, runId);
		}, this.topicCleanupDelayMs);
		timer.unref?.();
		this.pendingTopicCleanups.set(runId, timer);
	}
	cancelRunTopicCleanup(runId) {
		const timer = this.pendingTopicCleanups.get(runId);
		if (timer !== void 0) {
			clearTimeout(timer);
			this.pendingTopicCleanups.delete(runId);
		}
	}
	async clearRunTopicUnlessActive(workflowId, runId) {
		try {
			const workflowsStore = await this.mastra.getStorage()?.getStore("workflows");
			if (workflowsStore) {
				const snapshot = await workflowsStore.loadWorkflowSnapshot({
					workflowName: workflowId,
					runId
				});
				const status = typeof snapshot === "string" ? void 0 : snapshot?.status;
				if (status && WorkflowEventProcessor.ACTIVE_RUN_STATUSES.has(status)) return;
			}
			await this.mastra.pubsub.clearTopic(`workflow.events.v2.${runId}`);
		} catch (err) {
			this.mastra.getLogger()?.warn("Failed to clear workflow events topic", {
				workflowId,
				runId,
				error: err
			});
		}
	}
	/**
	* Get or create an AbortController for a workflow run
	*/
	getOrCreateAbortController(runId) {
		let controller = this.abortControllers.get(runId);
		if (!controller) {
			controller = new AbortController();
			this.abortControllers.set(runId, controller);
		}
		return controller;
	}
	/**
	* Cancel a workflow run and all its nested child workflows
	*/
	cancelRunAndChildren(runId) {
		const controller = this.abortControllers.get(runId);
		if (controller) controller.abort();
		for (const [childRunId, parentRunId] of this.parentChildRelationships.entries()) if (parentRunId === runId) this.cancelRunAndChildren(childRunId);
	}
	/**
	* Clean up abort controller and relationships when a workflow completes.
	* Also cleans up any orphaned child entries that reference this run as parent.
	*/
	cleanupRun(runId) {
		this.abortControllers.delete(runId);
		this.parentChildRelationships.delete(runId);
		this.runFormats.delete(runId);
		for (const [childRunId, parentRunId] of this.parentChildRelationships.entries()) if (parentRunId === runId) this.parentChildRelationships.delete(childRunId);
	}
	/**
	* Resolves the tracing context for a run, walking up the parent chain so a
	* nested workflow run (e.g. `agentic-execution` inside `agentic-loop`)
	* inherits its parent's parent span. `EventedRun.start` records the context
	* on Mastra keyed by runId; nested runs are only registered against their
	* parent.
	*/
	resolveRunTracingContext(runId) {
		const seen = /* @__PURE__ */ new Set();
		let current = runId;
		while (current && !seen.has(current)) {
			seen.add(current);
			const ctx = this.mastra.__getRunTracingContext(current);
			if (ctx) return ctx;
			current = this.parentChildRelationships.get(current);
		}
	}
	/**
	* Snapshot of the run's current span as the {traceId, spanId, parentSpanId} shape that
	* `UpdateWorkflowStateOptions.tracingContext` expects, so a suspend's persisted snapshot
	* can stitch the resumed AGENT_RUN/WORKFLOW_RUN span back to the original trace. Mirrors
	* `default.ts`'s `persistTracingContext`; the evented engine holds the live span on
	* Mastra (since it can't ride pubsub events), so we resolve it via runId here.
	*/
	resolveSuspendTracingContext(runId) {
		const span = this.resolveRunTracingContext(runId)?.currentSpan;
		if (!span) return void 0;
		return {
			traceId: span.traceId,
			spanId: span.id,
			parentSpanId: span.getParentSpanId?.()
		};
	}
	/**
	* Applies the workflow's `pruneSnapshot` option to an already-persisted snapshot.
	*
	* The evented engine persists suspensions via merge operations
	* (`updateWorkflowResults` + `updateWorkflowState`) rather than writing a full
	* snapshot object, so the prune hook can't intercept the write itself. Instead,
	* after the merge completes, we load the merged snapshot, prune it, and
	* re-persist the full row. No-op when the workflow has no `pruneSnapshot` option.
	*/
	async pruneAndRepersistSnapshot({ workflow, workflowId, runId }) {
		const pruneSnapshot = workflow?.options?.pruneSnapshot;
		if (!pruneSnapshot) return;
		try {
			const workflowsStore = await this.mastra.getStorage()?.getStore("workflows");
			if (!workflowsStore) return;
			const run = await workflowsStore.getWorkflowRunById({
				runId,
				workflowName: workflowId
			});
			const snapshot = run?.snapshot;
			if (!snapshot || typeof snapshot === "string") return;
			const pruned = pruneSnapshot({
				snapshot,
				workflowStatus: snapshot.status
			});
			await workflowsStore.persistWorkflowSnapshot({
				workflowName: workflowId,
				runId,
				resourceId: run?.resourceId,
				snapshot: pruned
			});
		} catch (error) {
			this.mastra.getLogger()?.warn?.(`Failed to prune workflow snapshot for run ${runId}: ${error}`);
		}
	}
	__registerMastra(mastra) {
		super.__registerMastra(mastra);
		this.stepExecutor.__registerMastra(mastra);
	}
	/**
	* Resolves a workflow by id without throwing. Searches first by the
	* workflow's `.id` (the value that ends up on event payloads) and then
	* falls back to the registration key in `Mastra.workflows`. Returns
	* `undefined` if neither lookup succeeds — callers decide how to handle
	* the missing case (e.g. terminal failure vs. cleanup pass-through) so
	* we don't throw inside `#dispatch` and trigger infinite event retries.
	*/
	#tryResolveWorkflow(workflowId) {
		try {
			return this.mastra.getWorkflowById(workflowId);
		} catch {
			return;
		}
	}
	async errorWorkflow({ parentWorkflow, workflowId, runId, resumeSteps, stepResults, resumeData, requestContext }, e) {
		await this.mastra.pubsub.publish("workflows", {
			type: "workflow.fail",
			runId,
			data: {
				workflowId,
				runId,
				executionPath: [],
				resumeSteps,
				stepResults,
				prevResult: {
					status: "failed",
					error: require_error.getErrorFromUnknown(e).toJSON()
				},
				requestContext,
				resumeData,
				activeStepsPath: {},
				parentWorkflow
			}
		});
	}
	async processWorkflowCancel({ workflowId, runId, prevResult, ...args }) {
		this.cancelRunAndChildren(runId);
		if (!await (await this.mastra.getStorage()?.getStore("workflows"))?.loadWorkflowSnapshot({
			workflowName: workflowId,
			runId
		})) this.mastra.getLogger()?.warn("Canceling workflow without loaded state", {
			workflowId,
			runId
		});
		await this.endWorkflow({
			workflowId,
			runId,
			prevResult,
			...args
		}, "canceled");
	}
	async processWorkflowStart({ workflow, parentWorkflow, workflowId, runId, resumeSteps, prevResult, resumeData, timeTravel, restart, executionPath, stepResults, requestContext, perStep, format, state, outputOptions, forEachIndex }) {
		const initialState = arguments[0].initialState ?? state ?? {};
		const resolvedFormat = format ?? this.runFormats.get(runId);
		this.runFormats.set(runId, resolvedFormat);
		this.cancelRunTopicCleanup(runId);
		this.getOrCreateAbortController(runId);
		if (parentWorkflow?.runId) this.parentChildRelationships.set(runId, parentWorkflow.runId);
		const workflowsStore = await this.mastra.getStorage()?.getStore("workflows");
		const resourceId = (await workflowsStore?.getWorkflowRunById({
			runId,
			workflowName: workflow.id
		}))?.resourceId;
		if (workflow?.options?.shouldPersistSnapshot?.({
			stepResults: stepResults ?? {},
			workflowStatus: "running"
		}) ?? true) {
			const runningSnapshot = {
				activePaths: [],
				suspendedPaths: {},
				resumeLabels: {},
				waitingPaths: {},
				activeStepsPath: {},
				serializedStepGraph: workflow.serializedStepGraph,
				timestamp: Date.now(),
				runId,
				context: {
					...stepResults ?? { input: prevResult?.status === "success" ? prevResult.output : void 0 },
					__state: initialState
				},
				status: "running",
				value: initialState
			};
			await workflowsStore?.persistWorkflowSnapshot({
				workflowName: workflow.id,
				runId,
				resourceId,
				snapshot: workflow?.options?.pruneSnapshot ? workflow.options.pruneSnapshot({
					snapshot: runningSnapshot,
					workflowStatus: "running"
				}) : runningSnapshot
			});
			if (parentWorkflow) {
				const existing = (await workflowsStore?.loadWorkflowSnapshot({
					workflowName: parentWorkflow.workflowId,
					runId: parentWorkflow.runId
				}))?.context?.[workflowId];
				await workflowsStore?.updateWorkflowResults({
					workflowName: parentWorkflow.workflowId,
					runId: parentWorkflow.runId,
					stepId: workflowId,
					result: {
						startedAt: existing?.startedAt ?? Date.now(),
						status: "running",
						payload: existing?.payload ?? parentWorkflow.input?.output ?? {},
						...existing ?? {},
						metadata: {
							...existing?.metadata ?? {},
							nestedRunId: runId
						}
					},
					requestContext
				});
			}
		}
		const startExecutionPath = executionPath ?? [0];
		await this.mastra.pubsub.publish("workflows", {
			type: "workflow.step.run",
			runId,
			data: {
				parentWorkflow,
				workflowId,
				runId,
				executionPath: startExecutionPath,
				resumeSteps,
				stepResults: {
					...stepResults ?? { input: prevResult?.status === "success" ? prevResult.output : void 0 },
					__state: initialState
				},
				prevResult,
				timeTravel,
				restart,
				requestContext,
				resumeData,
				activeStepsPath: {},
				perStep,
				state: initialState,
				outputOptions,
				forEachIndex
			}
		});
	}
	async endWorkflow(args, status = "success") {
		const { workflowId, runId, prevResult, perStep, workflow, stepResults, activeStepsPath, executionPath, parentWorkflow } = args;
		const workflowsStore = await this.mastra.getStorage()?.getStore("workflows");
		const normalizedPrevResult = prevResult ?? { status };
		const finalStatus = perStep && status === "success" ? "paused" : status;
		if (workflow?.options?.shouldPersistSnapshot?.({
			stepResults: stepResults ?? {},
			workflowStatus: finalStatus
		}) ?? true) await workflowsStore?.updateWorkflowState({
			workflowName: workflowId,
			runId,
			opts: {
				status: finalStatus,
				result: normalizedPrevResult,
				activePaths: executionPath,
				activeStepsPath
			}
		});
		else if (parentWorkflow && finalStatus !== "paused") try {
			await workflowsStore?.deleteWorkflowRunById({
				runId,
				workflowName: workflowId
			});
		} catch (e) {
			this.mastra.getLogger()?.warn("Failed to clean up nested workflow snapshot", {
				workflowId,
				runId,
				error: e
			});
		}
		if (perStep) await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {
			type: "watch",
			runId,
			data: {
				type: "workflow-paused",
				payload: {}
			}
		});
		await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {
			type: "watch",
			runId,
			data: {
				type: "workflow-finish",
				payload: {
					runId,
					workflowStatus: normalizedPrevResult.status,
					...normalizedPrevResult.status === "success" ? { finalWorkflowResult: normalizedPrevResult.output } : {}
				}
			}
		});
		await this.mastra.pubsub.publish("workflows", {
			type: "workflow.end",
			runId,
			data: {
				...args,
				prevResult: normalizedPrevResult,
				workflow: void 0
			}
		});
	}
	async processWorkflowEnd(args) {
		const { resumeSteps, prevResult, resumeData, parentWorkflow, activeStepsPath, requestContext, runId, timeTravel, perStep, stepResults, state, workflowId } = args;
		const finalState = resolveCurrentState({
			stepResults,
			state
		});
		this.cleanupRun(runId);
		if (!perStep) this.scheduleRunTopicCleanup(workflowId, runId);
		if (parentWorkflow) {
			const step = parentWorkflow.stepGraph[parentWorkflow.executionPath[0]];
			if (step?.type === "loop") await processWorkflowLoop({
				workflow: parentWorkflow,
				workflowId: parentWorkflow.workflowId,
				prevResult,
				runId: parentWorkflow.runId,
				executionPath: parentWorkflow.executionPath,
				stepResults: parentWorkflow.stepResults,
				activeStepsPath: parentWorkflow.activeStepsPath,
				resumeSteps: parentWorkflow.resumeSteps,
				resumeData: parentWorkflow.resumeData,
				parentWorkflow: parentWorkflow.parentWorkflow,
				requestContext,
				retryCount: 0
			}, {
				pubsub: this.mastra.pubsub,
				stepExecutor: this.stepExecutor,
				step,
				stepResult: prevResult
			});
			else await this.mastra.pubsub.publish("workflows", {
				type: "workflow.step.end",
				runId: parentWorkflow.runId,
				data: {
					workflowId: parentWorkflow.workflowId,
					runId: parentWorkflow.runId,
					executionPath: parentWorkflow.executionPath,
					resumeSteps,
					stepResults: parentWorkflow.stepResults,
					prevResult,
					resumeData,
					activeStepsPath,
					parentWorkflow: parentWorkflow.parentWorkflow,
					parentContext: parentWorkflow,
					requestContext,
					timeTravel,
					perStep,
					state: finalState,
					nestedRunId: runId
				}
			});
		}
		await this.mastra.pubsub.publish("workflows-finish", {
			type: "workflow.end",
			runId,
			data: {
				...args,
				workflow: void 0,
				state: finalState
			}
		});
		if (this.mastra.__hasInternalWorkflow(args.workflowId, runId)) this.mastra.__unregisterInternalWorkflow(args.workflowId, runId);
	}
	async processWorkflowSuspend(args) {
		const { workflow, executionPath, resumeSteps, prevResult, resumeData, parentWorkflow, activeStepsPath, runId, requestContext, timeTravel, restart, stepResults, state, outputOptions } = args;
		const finalState = resolveCurrentState({
			stepResults,
			state
		});
		if (parentWorkflow) {
			const existingPath = prevResult.suspendPayload?.__workflow_meta?.path ?? [];
			const suspendedStepId = workflow && executionPath ? getStepId(workflow, executionPath) ?? void 0 : void 0;
			const propagatedPath = suspendedStepId && existingPath[0] !== suspendedStepId ? [suspendedStepId, ...existingPath] : existingPath;
			const resumeLabels = {};
			const nestedResumeLabels = prevResult.suspendPayload?.__workflow_meta?.resumeLabels ?? {};
			for (const label of Object.keys(nestedResumeLabels)) resumeLabels[label] = {
				stepId: parentWorkflow.stepId,
				foreachIndex: nestedResumeLabels[label].foreachIndex
			};
			await this.mastra.pubsub.publish("workflows", {
				type: "workflow.step.end",
				runId: parentWorkflow.runId,
				data: {
					workflowId: parentWorkflow.workflowId,
					runId: parentWorkflow.runId,
					executionPath: parentWorkflow.executionPath,
					resumeSteps,
					stepResults: parentWorkflow.stepResults,
					prevResult: {
						...prevResult,
						suspendPayload: {
							...prevResult.suspendPayload,
							__workflow_meta: {
								...prevResult.suspendPayload?.__workflow_meta ?? {},
								resumeLabels: Object.keys(resumeLabels).length > 0 ? resumeLabels : void 0,
								runId,
								path: propagatedPath
							}
						}
					},
					timeTravel,
					restart,
					resumeData,
					activeStepsPath,
					requestContext,
					parentWorkflow: parentWorkflow.parentWorkflow,
					parentContext: parentWorkflow,
					state: finalState,
					outputOptions,
					nestedRunId: runId
				}
			});
		}
		await this.mastra.pubsub.publish("workflows-finish", {
			type: "workflow.suspend",
			runId,
			data: {
				...args,
				workflow: void 0,
				state: finalState
			}
		});
		if (this.mastra.__hasInternalWorkflow(args.workflowId, runId)) this.mastra.__unregisterInternalWorkflow(args.workflowId, runId);
	}
	async processWorkflowFail(args) {
		const { workflowId, runId, resumeSteps, prevResult, resumeData, parentWorkflow, activeStepsPath, requestContext, timeTravel, restart, stepResults, state, outputOptions, workflow, executionPath } = args;
		const finalState = resolveCurrentState({
			stepResults,
			state
		});
		this.cleanupRun(runId);
		this.scheduleRunTopicCleanup(workflowId, runId);
		const workflowsStore = await this.mastra.getStorage()?.getStore("workflows");
		if (workflow?.options?.shouldPersistSnapshot?.({
			stepResults: stepResults ?? {},
			workflowStatus: "failed"
		}) ?? true) await workflowsStore?.updateWorkflowState({
			workflowName: workflowId,
			runId,
			opts: {
				status: "failed",
				error: prevResult.error,
				activePaths: executionPath,
				activeStepsPath
			}
		});
		else if (parentWorkflow) try {
			await workflowsStore?.deleteWorkflowRunById({
				runId,
				workflowName: workflowId
			});
		} catch (e) {
			this.mastra.getLogger()?.warn("Failed to clean up nested workflow snapshot", {
				workflowId,
				runId,
				error: e
			});
		}
		if (parentWorkflow) await this.mastra.pubsub.publish("workflows", {
			type: "workflow.step.end",
			runId: parentWorkflow.runId,
			data: {
				workflowId: parentWorkflow.workflowId,
				runId: parentWorkflow.runId,
				executionPath: parentWorkflow.executionPath,
				resumeSteps,
				stepResults: parentWorkflow.stepResults,
				prevResult,
				timeTravel,
				restart,
				resumeData,
				activeStepsPath,
				requestContext,
				parentWorkflow: parentWorkflow.parentWorkflow,
				parentContext: parentWorkflow,
				state: finalState,
				outputOptions,
				nestedRunId: runId
			}
		});
		await this.mastra.pubsub.publish("workflows-finish", {
			type: "workflow.fail",
			runId,
			data: {
				...args,
				workflow: void 0,
				state: finalState
			}
		});
		if (this.mastra.__hasInternalWorkflow(args.workflowId, runId)) this.mastra.__unregisterInternalWorkflow(args.workflowId, runId);
	}
	async processWorkflowStepRun(args) {
		const { workflow, workflowId, runId, executionPath, stepResults, activeStepsPath, resumeSteps, timeTravel, restart, prevResult, resumeData, parentWorkflow, requestContext, perStep, state, outputOptions, forEachIndex } = args;
		const currentState = resolveCurrentState({
			stepResults,
			state
		});
		const stepGraph = workflow.stepGraph;
		if (!executionPath?.length) return this.errorWorkflow({
			workflowId,
			runId,
			executionPath,
			stepResults,
			activeStepsPath,
			resumeSteps,
			prevResult,
			resumeData,
			parentWorkflow,
			requestContext
		}, new require_error.MastraError({
			id: "MASTRA_WORKFLOW",
			text: `Execution path is empty: ${JSON.stringify(executionPath)}`,
			domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
			category: require_error.ErrorCategory.SYSTEM
		}));
		const rawStep = stepGraph[executionPath[0]];
		if (!rawStep) {
			if (executionPath[0] >= stepGraph.length) return this.endWorkflow({
				workflow,
				parentWorkflow,
				workflowId,
				runId,
				executionPath,
				resumeSteps,
				stepResults,
				prevResult,
				activeStepsPath,
				requestContext,
				state: currentState,
				outputOptions
			});
			return this.errorWorkflow({
				workflowId,
				runId,
				executionPath,
				stepResults,
				activeStepsPath,
				resumeSteps,
				prevResult,
				resumeData,
				parentWorkflow,
				requestContext
			}, new require_error.MastraError({
				id: "MASTRA_WORKFLOW",
				text: `Step not found in step graph: ${JSON.stringify(executionPath)}`,
				domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
				category: require_error.ErrorCategory.SYSTEM
			}));
		}
		let step = rawStep;
		if ((step.type === "parallel" || step.type === "conditional") && executionPath.length > 1 && (!restart || restart && restart.isParallelOrConditionalRestarted)) step = step.steps[executionPath[1]];
		else if (step.type === "parallel") return processWorkflowParallel({
			workflow,
			workflowId,
			runId,
			executionPath,
			stepResults,
			activeStepsPath,
			resumeSteps,
			restart,
			timeTravel,
			prevResult,
			resumeData,
			parentWorkflow,
			requestContext,
			perStep,
			state: currentState,
			outputOptions
		}, {
			pubsub: this.mastra.pubsub,
			step
		});
		else if (step?.type === "conditional") return processWorkflowConditional({
			workflow,
			workflowId,
			runId,
			executionPath,
			stepResults,
			activeStepsPath,
			resumeSteps,
			restart,
			timeTravel,
			prevResult,
			resumeData,
			parentWorkflow,
			requestContext,
			perStep,
			state: currentState,
			outputOptions
		}, {
			pubsub: this.mastra.pubsub,
			stepExecutor: this.stepExecutor,
			step
		});
		else if (step?.type === "sleep") return processWorkflowSleep({
			workflow,
			workflowId,
			runId,
			executionPath,
			stepResults,
			activeStepsPath,
			resumeSteps,
			timeTravel,
			restart,
			prevResult,
			resumeData,
			parentWorkflow,
			requestContext,
			perStep,
			state: currentState,
			outputOptions
		}, {
			pubsub: this.mastra.pubsub,
			stepExecutor: this.stepExecutor,
			step
		});
		else if (step?.type === "sleepUntil") return processWorkflowSleepUntil({
			workflow,
			workflowId,
			runId,
			executionPath,
			stepResults,
			activeStepsPath,
			resumeSteps,
			timeTravel,
			restart,
			prevResult,
			resumeData,
			parentWorkflow,
			requestContext,
			perStep,
			state: currentState,
			outputOptions
		}, {
			pubsub: this.mastra.pubsub,
			stepExecutor: this.stepExecutor,
			step
		});
		else if (step?.type === "foreach" && executionPath.length === 1) return processWorkflowForEach({
			workflow,
			workflowId,
			runId,
			executionPath,
			stepResults,
			activeStepsPath,
			resumeSteps,
			timeTravel,
			restart,
			prevResult,
			resumeData,
			parentWorkflow,
			requestContext,
			perStep,
			state: currentState,
			outputOptions,
			forEachIndex
		}, {
			pubsub: this.mastra.pubsub,
			mastra: this.mastra,
			step
		});
		return this.runLeafStep({
			...args,
			step
		});
	}
	/**
	* Shared leaf-step runner. Executes a single leaf entry - a plain `step`, a
	* declarative `agent` / `tool` / `mapping` entry, or a `loop` / `foreach`
	* body - and emits its lifecycle events (`workflow.step.end`, retries,
	* suspend, cancel). The per-kind interpretation happens in the step
	* executor's dispatch; here entries are only inspected via the `step-entry`
	* accessors.
	*/
	async runLeafStep(args) {
		const { workflow, workflowId, runId, executionPath, stepResults, activeStepsPath, resumeSteps, timeTravel, restart, prevResult, resumeData, parentWorkflow, retryCount = 0, perStep, state, outputOptions, forEachIndex, step } = args;
		let requestContext = args.requestContext;
		const streamFormat = this.runFormats.get(runId);
		const currentState = resolveCurrentState({
			stepResults,
			state
		});
		const stepGraph = workflow.stepGraph;
		const leaf = step.type === "loop" || step.type === "foreach" ? step.step : step;
		const leafId = getEntryId(leaf);
		if (!isExecutableStep(step)) return this.errorWorkflow({
			workflowId,
			runId,
			executionPath,
			stepResults,
			activeStepsPath,
			resumeSteps,
			prevResult,
			resumeData,
			parentWorkflow,
			requestContext
		}, new require_error.MastraError({
			id: "MASTRA_WORKFLOW",
			text: `Step is not executable: ${step?.type} -- ${JSON.stringify(executionPath)}`,
			domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
			category: require_error.ErrorCategory.SYSTEM
		}));
		activeStepsPath[leafId] = executionPath;
		const workflowsStore = await this.mastra?.getStorage()?.getStore("workflows");
		const nestedWorkflowStep = getEntryWorkflow(leaf);
		if (nestedWorkflowStep) {
			const nestedWorkflow = nestedWorkflowStep;
			if (resumeSteps?.length === 1 && resumeSteps[0] === leafId) {
				const nestedRunId = stepResults[leafId]?.suspendPayload?.__workflow_meta?.runId;
				if (!nestedRunId) return this.errorWorkflow({
					workflowId,
					runId,
					executionPath,
					stepResults,
					activeStepsPath,
					resumeSteps,
					prevResult,
					resumeData,
					parentWorkflow,
					requestContext
				}, new require_error.MastraError({
					id: "MASTRA_WORKFLOW",
					text: `Nested workflow run id not found for auto-detection: ${JSON.stringify(stepResults)}`,
					domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
					category: require_error.ErrorCategory.SYSTEM
				}));
				const snapshot = await workflowsStore?.loadWorkflowSnapshot({
					workflowName: leafId,
					runId: nestedRunId
				});
				const suspendedStepId = Object.keys(snapshot?.suspendedPaths ?? {})?.[0];
				if (!suspendedStepId) return this.errorWorkflow({
					workflowId,
					runId,
					executionPath,
					stepResults,
					activeStepsPath,
					resumeSteps,
					prevResult,
					resumeData,
					parentWorkflow,
					requestContext
				}, new require_error.MastraError({
					id: "MASTRA_WORKFLOW",
					text: `No suspended step found in nested workflow: ${leafId}`,
					domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
					category: require_error.ErrorCategory.SYSTEM
				}));
				const nestedExecutionPath = snapshot?.suspendedPaths?.[suspendedStepId];
				const nestedStepResults = snapshot?.context;
				const nestedPrevResult = {
					status: "success",
					output: (nestedStepResults?.[suspendedStepId])?.payload ?? prevResult?.output
				};
				await this.mastra.pubsub.publish("workflows", {
					type: "workflow.resume",
					runId,
					data: {
						workflowId: leafId,
						parentWorkflow: {
							stepId: leafId,
							workflowId,
							runId,
							stepGraph,
							executionPath,
							resumeSteps,
							stepResults,
							input: prevResult,
							parentWorkflow,
							activeStepsPath,
							resumeData
						},
						executionPath: nestedExecutionPath,
						runId: nestedRunId,
						resumeSteps: [suspendedStepId],
						stepResults: nestedStepResults,
						prevResult: nestedPrevResult,
						resumeData,
						activeStepsPath,
						requestContext,
						perStep,
						initialState: currentState,
						state: currentState,
						outputOptions
					}
				});
			} else if (resumeSteps?.length > 1 && resumeSteps[0] === leafId) {
				const nestedRunId = stepResults[leafId]?.suspendPayload?.__workflow_meta?.runId;
				if (!nestedRunId) return this.errorWorkflow({
					workflowId,
					runId,
					executionPath,
					stepResults,
					activeStepsPath,
					resumeSteps,
					prevResult,
					resumeData,
					parentWorkflow,
					requestContext
				}, new require_error.MastraError({
					id: "MASTRA_WORKFLOW",
					text: `Nested workflow run id not found: ${JSON.stringify(stepResults)}`,
					domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
					category: require_error.ErrorCategory.SYSTEM
				}));
				const snapshot = await workflowsStore?.loadWorkflowSnapshot({
					workflowName: leafId,
					runId: nestedRunId
				});
				const nestedStepResults = snapshot?.context;
				const nestedSteps = resumeSteps.slice(1);
				const nestedPrevResult = {
					status: "success",
					output: (nestedStepResults?.[nestedSteps[0]])?.payload ?? prevResult?.output
				};
				await this.mastra.pubsub.publish("workflows", {
					type: "workflow.resume",
					runId,
					data: {
						workflowId: leafId,
						parentWorkflow: {
							stepId: leafId,
							workflowId,
							runId,
							stepGraph,
							executionPath,
							resumeSteps,
							stepResults,
							input: prevResult,
							parentWorkflow,
							activeStepsPath,
							resumeData
						},
						executionPath: snapshot?.suspendedPaths?.[nestedSteps[0]],
						runId: nestedRunId,
						resumeSteps: nestedSteps,
						stepResults: nestedStepResults,
						prevResult: nestedPrevResult,
						resumeData,
						activeStepsPath,
						requestContext,
						perStep,
						initialState: currentState,
						state: currentState,
						outputOptions
					}
				});
			} else if (timeTravel && timeTravel.steps?.length > 1 && timeTravel.steps[0] === leafId) {
				const nestedRunId = stepResults[leafId]?.metadata?.nestedRunId ?? (0, crypto.randomUUID)();
				const snapshot = await workflowsStore?.loadWorkflowSnapshot({
					workflowName: leafId,
					runId: nestedRunId
				}) ?? { context: {} };
				const timeTravelParams = createTimeTravelExecutionParams({
					steps: timeTravel.steps.slice(1),
					inputData: timeTravel.inputData,
					resumeData: timeTravel.resumeData,
					context: timeTravel.nestedStepResults?.[leafId] ?? {},
					nestedStepsContext: timeTravel.nestedStepResults ?? {},
					snapshot,
					graph: nestedWorkflow.buildExecutionGraph(),
					perStep
				});
				const nestedPrevStepId = getStepId(nestedWorkflow, timeTravelParams.executionPath);
				const nestedPrevResult = timeTravelParams.stepResults[nestedPrevStepId ?? "input"];
				await this.mastra.pubsub.publish("workflows", {
					type: "workflow.start",
					runId,
					data: {
						workflowId: leafId,
						parentWorkflow: {
							stepId: leafId,
							workflowId,
							runId,
							stepGraph,
							executionPath,
							resumeSteps,
							stepResults,
							timeTravel,
							input: prevResult,
							parentWorkflow,
							activeStepsPath,
							resumeData
						},
						executionPath: timeTravelParams.executionPath,
						runId: nestedRunId,
						stepResults: timeTravelParams.stepResults,
						prevResult: {
							status: "success",
							output: nestedPrevResult?.payload
						},
						timeTravel: timeTravelParams,
						activeStepsPath,
						requestContext,
						perStep,
						initialState: currentState,
						state: currentState,
						outputOptions
					}
				});
			} else if (restart && !!restart.activeStepsPath?.[leafId]) {
				const nestedRunId = stepResults[leafId]?.metadata?.nestedRunId ?? (0, crypto.randomUUID)();
				const snapshot = await workflowsStore?.loadWorkflowSnapshot({
					workflowName: leafId,
					runId: nestedRunId
				}) ?? { context: {} };
				const restartParams = createRestartExecutionParams({
					snapshot,
					graph: nestedWorkflow.buildExecutionGraph()
				});
				const nestedPrevStepId = getStepId(nestedWorkflow, snapshot.activePaths);
				const nestedPrevResult = restartParams.stepResults[nestedPrevStepId ?? "input"];
				await this.mastra.pubsub.publish("workflows", {
					type: "workflow.start",
					runId,
					data: {
						workflowId: leafId,
						parentWorkflow: {
							stepId: leafId,
							workflowId,
							runId,
							stepGraph,
							executionPath,
							resumeSteps,
							stepResults,
							restart,
							input: prevResult,
							parentWorkflow,
							activeStepsPath,
							resumeData
						},
						executionPath: restartParams.activePaths,
						runId: nestedRunId,
						stepResults: restartParams.stepResults,
						prevResult: {
							status: "success",
							output: nestedPrevResult?.payload
						},
						restart: restartParams,
						activeStepsPath: restartParams.activeStepsPath,
						requestContext,
						perStep,
						initialState: restartParams.state,
						state: restartParams.state,
						outputOptions
					}
				});
			} else {
				const nestedRunId = (0, crypto.randomUUID)();
				const shouldPersist = nestedWorkflow?.options?.shouldPersistSnapshot?.({
					stepResults: {},
					workflowStatus: "pending"
				}) ?? true;
				const parentRun = await workflowsStore?.getWorkflowRunById({
					runId,
					workflowName: workflow.id
				});
				if (shouldPersist) {
					const pendingSnapshot = {
						runId: nestedRunId,
						status: "pending",
						value: {},
						context: {},
						activePaths: [],
						serializedStepGraph: nestedWorkflow.serializedStepGraph,
						activeStepsPath: {},
						suspendedPaths: {},
						resumeLabels: {},
						waitingPaths: {},
						result: void 0,
						error: void 0,
						timestamp: Date.now()
					};
					await workflowsStore?.persistWorkflowSnapshot({
						workflowName: nestedWorkflow.id,
						runId: nestedRunId,
						resourceId: parentRun?.resourceId,
						snapshot: nestedWorkflow?.options?.pruneSnapshot ? nestedWorkflow.options.pruneSnapshot({
							snapshot: pendingSnapshot,
							workflowStatus: "pending"
						}) : pendingSnapshot
					});
				}
				await this.mastra.pubsub.publish("workflows", {
					type: "workflow.start",
					runId,
					data: {
						workflowId: leafId,
						parentWorkflow: {
							stepId: leafId,
							workflowId,
							stepGraph,
							runId,
							executionPath,
							resumeSteps,
							stepResults,
							input: prevResult,
							parentWorkflow,
							activeStepsPath,
							resumeData
						},
						executionPath: [0],
						runId: nestedRunId,
						resumeSteps,
						prevResult,
						resumeData,
						activeStepsPath,
						requestContext,
						perStep,
						initialState: currentState,
						state: currentState,
						outputOptions
					}
				});
			}
			return;
		}
		if (isSingleStepEntry(step)) await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {
			type: "watch",
			runId,
			data: {
				type: "workflow-step-start",
				payload: {
					id: leafId,
					startedAt: Date.now(),
					payload: prevResult.status === "success" ? prevResult.output : void 0,
					status: "running"
				}
			}
		});
		new events.default().on("watch", async (event) => {
			await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {
				type: "watch",
				runId,
				data: event
			});
		});
		const rc = new require_request_context.RequestContext();
		for (const [key, value] of Object.entries(requestContext)) rc.set(key, value);
		const { resumeData: timeTravelResumeData, validationError: timeTravelResumeValidationError } = await validateStepResumeData({
			resumeData: timeTravel?.stepResults[leafId]?.status === "suspended" ? timeTravel?.resumeData : void 0,
			step: getEntrySchemas(leaf, this.mastra)
		});
		let resumeDataToUse;
		if (timeTravelResumeData && !timeTravelResumeValidationError) resumeDataToUse = timeTravelResumeData;
		else if (timeTravelResumeData && timeTravelResumeValidationError) this.mastra.getLogger()?.warn("Time travel resume data validation failed", {
			stepId: leafId,
			error: timeTravelResumeValidationError.message
		});
		else if (resumeSteps?.length > 0 && resumeSteps?.[0] === leafId) resumeDataToUse = resumeData;
		const abortController = this.getOrCreateAbortController(runId);
		let stepResult;
		if (this.stepExecutionStrategy) stepResult = await this.stepExecutionStrategy.executeStep({
			workflowId,
			runId,
			stepId: leafId,
			executionPath,
			stepResults,
			state: currentState,
			requestContext: Object.fromEntries(rc.entries()),
			input: prevResult?.output,
			resumeData: resumeDataToUse,
			retryCount,
			foreachIdx: step.type === "foreach" ? executionPath[1] : void 0,
			format: streamFormat,
			perStep,
			validateInputs: workflow.options.validateInputs,
			abortSignal: abortController.signal
		});
		else stepResult = await this.stepExecutor.execute({
			workflowId,
			entry: leaf,
			runId,
			stepResults,
			state: currentState,
			requestContext: rc,
			input: prevResult?.output,
			resumeData: resumeDataToUse,
			retryCount,
			foreachIdx: step.type === "foreach" ? executionPath[1] : void 0,
			validateInputs: workflow.options.validateInputs,
			abortController,
			format: streamFormat,
			perStep,
			tracingContext: this.resolveRunTracingContext(runId),
			tracingPolicy: workflow.options?.tracingPolicy
		});
		requestContext = Object.fromEntries(rc.entries());
		if (abortController?.signal?.aborted) {
			const updatedState = stepResult.__state ?? currentState;
			return this.mastra.pubsub.publish("workflows", {
				type: "workflow.cancel",
				runId,
				data: {
					parentWorkflow,
					workflowId,
					runId,
					executionPath,
					resumeSteps,
					timeTravel,
					stepResults: {
						...stepResults,
						[leafId]: stepResult,
						__state: updatedState
					},
					prevResult: {
						...stepResult,
						status: "canceled"
					},
					activeStepsPath,
					requestContext,
					perStep,
					state: updatedState,
					outputOptions
				}
			});
		}
		if (stepResult.status === "bailed") {
			stepResult.status = "success";
			await this.endWorkflow({
				workflow,
				resumeData,
				parentWorkflow,
				workflowId,
				runId,
				executionPath,
				resumeSteps,
				stepResults: {
					...stepResults,
					[leafId]: stepResult
				},
				prevResult: stepResult,
				activeStepsPath,
				requestContext,
				perStep,
				state: currentState,
				outputOptions
			});
			return;
		}
		if (stepResult.status === "failed") if (retryCount >= (getEntryRetries(leaf) ?? workflow.retryConfig.attempts ?? 0) || stepResult.nonRetryable) await this.mastra.pubsub.publish("workflows", {
			type: "workflow.step.end",
			runId,
			data: {
				parentWorkflow,
				workflowId,
				runId,
				executionPath,
				resumeSteps,
				stepResults,
				prevResult: stepResult,
				activeStepsPath,
				requestContext,
				state: currentState,
				outputOptions
			}
		});
		else return this.mastra.pubsub.publish("workflows", {
			type: "workflow.step.run",
			runId,
			data: {
				parentWorkflow,
				workflowId,
				runId,
				executionPath,
				resumeSteps,
				stepResults,
				timeTravel,
				restart,
				prevResult,
				activeStepsPath,
				requestContext,
				retryCount: retryCount + 1,
				state: currentState,
				outputOptions
			}
		});
		if (step.type === "loop" && stepResult.status === "suspended") {
			const updatedState = stepResult.__state ?? currentState;
			await this.mastra.pubsub.publish("workflows", {
				type: "workflow.step.end",
				runId,
				data: {
					parentWorkflow,
					workflowId,
					runId,
					executionPath,
					resumeSteps,
					timeTravel,
					restart,
					stepResults: {
						...stepResults,
						[leafId]: stepResult,
						__state: updatedState
					},
					prevResult: stepResult,
					activeStepsPath,
					requestContext,
					perStep,
					state: updatedState,
					outputOptions
				}
			});
			return;
		}
		if (step.type === "loop") await processWorkflowLoop({
			workflow,
			workflowId,
			prevResult: stepResult,
			runId,
			executionPath,
			stepResults,
			activeStepsPath,
			resumeSteps,
			resumeData,
			parentWorkflow,
			requestContext,
			retryCount: retryCount + 1
		}, {
			pubsub: this.mastra.pubsub,
			stepExecutor: this.stepExecutor,
			step,
			stepResult
		});
		else {
			const updatedState = stepResult.__state ?? currentState;
			await this.mastra.pubsub.publish("workflows", {
				type: "workflow.step.end",
				runId,
				data: {
					parentWorkflow,
					workflowId,
					runId,
					executionPath,
					resumeSteps,
					timeTravel,
					restart,
					stepResults: {
						...stepResults,
						[leafId]: stepResult,
						__state: updatedState
					},
					prevResult: stepResult,
					activeStepsPath,
					requestContext,
					perStep,
					state: updatedState,
					outputOptions,
					forEachIndex
				}
			});
		}
	}
	/**
	* Aggregate the results of all branches of a `parallel` / `conditional` entry once
	* every branch has reached a terminal state (`success` / `skipped`) or `suspended`.
	*
	* This runs once per branch completion. It only acts when every branch is accounted
	* for; otherwise it returns and lets a later branch finish the aggregation. Because
	* `stepResults` is the snapshot returned by the caller's `updateWorkflowResults`
	* call — which grows monotonically per branch — only the branch whose write landed
	* last observes the full set, so exactly one branch emits (no double emit).
	*
	* - if any branch is still suspended → re-emit `workflow.suspend` with the full set
	*   of suspended paths and persist the workflow state. This both fixes the race where
	*   each branch would overwrite `suspendedPaths` on its own, and lets the workflow
	*   stay suspended while only some branches have been resumed.
	* - otherwise → emit `workflow.step.end` for the parallel/conditional entry with the
	*   merged branch outputs (the existing behaviour).
	*/
	async aggregateBranchResults({ workflow, workflowId, runId, branchEntry, branchExecutionPath, latestBranchResult, resumeSteps, timeTravel, restart, parentWorkflow, stepResults, activeStepsPath, requestContext, state, outputOptions }) {
		const currentState = resolveCurrentState({
			stepResults,
			state
		});
		const parentIdx = branchExecutionPath[0];
		const finishedBranchIdx = branchExecutionPath.length > 1 ? branchExecutionPath[1] : void 0;
		let suspendedCount = 0;
		let skippedCount = 0;
		const allResults = {};
		const suspendedPaths = {};
		const resumeLabels = {};
		branchEntry.steps.forEach((branch, idx) => {
			if (!isSingleStepEntry(branch)) return;
			const branchId = getSingleStepEntryId(branch);
			const res = stepResults?.[branchId];
			if (!res || !res.status) return;
			if (res.status === "success") {
				const output = idx === finishedBranchIdx && latestBranchResult?.status === "success" ? latestBranchResult.output : res.output;
				allResults[branchId] = output;
			} else if (res.status === "skipped") skippedCount++;
			else if (res.status === "suspended") {
				suspendedCount++;
				suspendedPaths[branchId] = [parentIdx, idx];
				Object.assign(resumeLabels, res.suspendPayload?.__workflow_meta?.resumeLabels ?? {});
			}
		});
		if (Object.keys(allResults).length + skippedCount + suspendedCount < branchEntry.steps.length) return;
		if (suspendedCount > 0) {
			const workflowsStore = await this.mastra.getStorage()?.getStore("workflows");
			if (workflow?.options?.shouldPersistSnapshot?.({
				stepResults: stepResults ?? {},
				workflowStatus: "suspended"
			}) ?? true) {
				await workflowsStore?.updateWorkflowResults({
					workflowName: workflow.id,
					runId,
					stepId: "__state",
					result: currentState,
					requestContext
				});
				const suspendTracingContext = this.resolveSuspendTracingContext(runId);
				await workflowsStore?.updateWorkflowState({
					workflowName: workflowId,
					runId,
					opts: {
						status: "suspended",
						result: { status: "suspended" },
						suspendedPaths,
						resumeLabels,
						...suspendTracingContext ? { tracingContext: suspendTracingContext } : {}
					}
				});
				await this.pruneAndRepersistSnapshot({
					workflow,
					workflowId,
					runId
				});
			}
			await this.mastra.pubsub.publish("workflows", {
				type: "workflow.suspend",
				runId,
				data: {
					workflowId,
					runId,
					executionPath: branchExecutionPath,
					resumeSteps,
					parentWorkflow,
					stepResults,
					prevResult: { status: "suspended" },
					activeStepsPath,
					requestContext,
					timeTravel,
					restart,
					state: currentState,
					outputOptions
				}
			});
			return;
		}
		await this.mastra.pubsub.publish("workflows", {
			type: "workflow.step.end",
			runId,
			data: {
				parentWorkflow,
				workflowId,
				runId,
				executionPath: branchExecutionPath.slice(0, -1),
				resumeSteps,
				stepResults,
				prevResult: {
					status: "success",
					output: allResults
				},
				activeStepsPath,
				requestContext,
				timeTravel,
				restart,
				state: currentState,
				outputOptions
			}
		});
	}
	async processWorkflowStepEnd({ workflow, workflowId, runId, executionPath, resumeSteps, timeTravel, restart, prevResult, parentWorkflow, stepResults, activeStepsPath, parentContext, requestContext, perStep, state, outputOptions, forEachIndex, nestedRunId }) {
		const currentState = parentContext ? state ?? prevResult?.__state ?? stepResults?.__state ?? {} : prevResult?.__state ?? stepResults?.__state ?? state ?? {};
		const { __state: _removedState, ...cleanPrevResult } = prevResult;
		prevResult = cleanPrevResult;
		let step = workflow.stepGraph[executionPath[0]];
		if ((step?.type === "parallel" || step?.type === "conditional") && executionPath.length > 1) step = step.steps[executionPath[1]];
		if (!step) return this.errorWorkflow({
			workflowId,
			runId,
			executionPath,
			resumeSteps,
			prevResult,
			stepResults,
			activeStepsPath,
			requestContext
		}, new require_error.MastraError({
			id: "MASTRA_WORKFLOW",
			text: `Step not found: ${JSON.stringify(executionPath)}`,
			domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
			category: require_error.ErrorCategory.SYSTEM
		}));
		const stepId = getStepIds(step)[0];
		const workflowsStore = await this.mastra.getStorage()?.getStore("workflows");
		if (step.type === "foreach") {
			const snapshot = await workflowsStore?.loadWorkflowSnapshot({
				workflowName: workflowId,
				runId
			});
			const currentIdx = executionPath[1];
			const existingStepResult = (snapshot?.context)?.[getEntryId(step.step)];
			const currentResult = existingStepResult?.output;
			const originalPayload = existingStepResult?.payload;
			let newResult = prevResult;
			if (currentIdx !== void 0) {
				if (prevResult.status === "bailed") {
					const bailedResult = {
						status: "success",
						output: prevResult.output,
						startedAt: existingStepResult?.startedAt ?? Date.now(),
						endedAt: Date.now(),
						payload: originalPayload
					};
					await workflowsStore?.updateWorkflowResults({
						workflowName: workflow.id,
						runId,
						stepId: getEntryId(step.step),
						result: bailedResult,
						requestContext
					});
					return this.endWorkflow({
						workflow,
						parentWorkflow,
						workflowId,
						runId,
						executionPath: [executionPath[0]],
						resumeSteps,
						stepResults: {
							...stepResults,
							[getEntryId(step.step)]: bailedResult
						},
						prevResult: bailedResult,
						activeStepsPath,
						requestContext,
						perStep,
						state: currentState,
						outputOptions
					});
				}
				const iterationResult = prevResult.status === "suspended" ? prevResult : prevResult.output;
				if (currentResult) {
					currentResult[currentIdx] = iterationResult;
					newResult = {
						...existingStepResult,
						...prevResult,
						output: currentResult,
						payload: originalPayload,
						suspendPayload: existingStepResult?.suspendPayload ?? prevResult.suspendPayload,
						suspendedAt: existingStepResult?.suspendedAt ?? prevResult.suspendedAt,
						resumePayload: prevResult.resumePayload ?? existingStepResult?.resumePayload,
						resumedAt: prevResult.resumedAt ?? existingStepResult?.resumedAt
					};
				} else newResult = {
					...prevResult,
					output: [iterationResult],
					payload: originalPayload
				};
			}
			const newStepResults = await workflowsStore?.updateWorkflowResults({
				workflowName: workflow.id,
				runId,
				stepId: getEntryId(step.step),
				result: newResult,
				requestContext
			});
			if (currentState) await workflowsStore?.updateWorkflowResults({
				workflowName: workflow.id,
				runId,
				stepId: "__state",
				result: currentState,
				requestContext
			});
			stepResults = {
				...!newStepResults || Object.keys(newStepResults).length === 0 ? {
					...stepResults ?? {},
					[getEntryId(step.step)]: newResult
				} : newStepResults,
				__state: currentState
			};
			if (currentIdx !== void 0) {
				const foreachResult = readForeachResult(stepResults, getEntryId(step.step));
				const iterationResults = foreachResult?.output ?? [];
				const targetLen = foreachResult?.payload?.length ?? 0;
				const pendingCount = iterationResults.filter((r) => r === null).length;
				const suspendedCount = iterationResults.filter((r) => r && typeof r === "object" && r.status === "suspended").length;
				const iterationsStarted = iterationResults.length;
				const completedCount = iterationResults.filter((r) => r !== null && !(typeof r === "object" && r.status === "suspended")).length;
				const iterationStatus = prevResult.status === "suspended" ? "suspended" : prevResult.status === "success" ? "success" : "failed";
				await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {
					type: "watch",
					runId,
					data: {
						type: "workflow-step-progress",
						payload: {
							id: getEntryId(step.step),
							completedCount,
							totalCount: targetLen,
							currentIndex: currentIdx,
							iterationStatus,
							...prevResult.status === "success" ? { iterationOutput: prevResult.output } : {}
						}
					}
				});
				if (pendingCount > 0) return;
				if (iterationsStarted < targetLen) {
					await processWorkflowForEach({
						workflow,
						workflowId,
						prevResult: {
							status: "success",
							output: foreachResult.payload
						},
						runId,
						executionPath: [executionPath[0]],
						stepResults,
						activeStepsPath,
						resumeSteps,
						timeTravel,
						restart,
						resumeData: void 0,
						parentWorkflow,
						requestContext,
						perStep,
						state: currentState,
						outputOptions
					}, {
						pubsub: this.mastra.pubsub,
						mastra: this.mastra,
						step
					});
					return;
				}
				if (suspendedCount > 0) {
					const collectedResumeLabels = {};
					const suspendedPaths = { [getEntryId(step.step)]: [executionPath[0]] };
					let firstSuspendedIterationPayload;
					for (let i = 0; i < iterationResults.length; i++) {
						const iterResult = iterationResults[i];
						if (iterResult && typeof iterResult === "object" && iterResult.status === "suspended") {
							if (iterResult.suspendPayload?.__workflow_meta?.resumeLabels) Object.assign(collectedResumeLabels, iterResult.suspendPayload.__workflow_meta.resumeLabels);
							if (firstSuspendedIterationPayload === void 0) firstSuspendedIterationPayload = iterResult.suspendPayload;
						}
					}
					const foreachSuspendResult = {
						status: "suspended",
						output: iterationResults,
						payload: foreachResult.payload,
						suspendedAt: Date.now(),
						startedAt: foreachResult.startedAt ?? Date.now(),
						suspendPayload: {
							...firstSuspendedIterationPayload,
							__workflow_meta: {
								path: executionPath,
								resumeLabels: collectedResumeLabels
							}
						}
					};
					await workflowsStore?.updateWorkflowResults({
						workflowName: workflow.id,
						runId,
						stepId: getEntryId(step.step),
						result: foreachSuspendResult,
						requestContext
					});
					if (workflow?.options?.shouldPersistSnapshot?.({
						stepResults: stepResults ?? {},
						workflowStatus: "suspended"
					}) ?? true) {
						await workflowsStore?.updateWorkflowResults({
							workflowName: workflow.id,
							runId,
							stepId: "__state",
							result: currentState,
							requestContext
						});
						const suspendTracingContext = this.resolveSuspendTracingContext(runId);
						await workflowsStore?.updateWorkflowState({
							workflowName: workflowId,
							runId,
							opts: {
								status: "suspended",
								result: foreachSuspendResult,
								suspendedPaths,
								resumeLabels: collectedResumeLabels,
								activePaths: executionPath,
								activeStepsPath,
								...suspendTracingContext ? { tracingContext: suspendTracingContext } : {}
							}
						});
						await this.pruneAndRepersistSnapshot({
							workflow,
							workflowId,
							runId
						});
					}
					await this.mastra.pubsub.publish("workflows", {
						type: "workflow.suspend",
						runId,
						data: {
							workflowId,
							runId,
							executionPath: [executionPath[0]],
							resumeSteps,
							parentWorkflow,
							stepResults: {
								...stepResults,
								[getEntryId(step.step)]: foreachSuspendResult
							},
							prevResult: foreachSuspendResult,
							activeStepsPath,
							requestContext,
							timeTravel,
							restart,
							state: currentState,
							outputOptions
						}
					});
					return;
				}
				await processWorkflowForEach({
					workflow,
					workflowId,
					prevResult: {
						status: "success",
						output: foreachResult.payload
					},
					runId,
					executionPath: [executionPath[0]],
					stepResults,
					activeStepsPath,
					resumeSteps,
					timeTravel,
					restart,
					resumeData: void 0,
					parentWorkflow,
					requestContext,
					perStep,
					state: currentState,
					outputOptions
				}, {
					pubsub: this.mastra.pubsub,
					mastra: this.mastra,
					step
				});
				return;
			}
		} else if (isExecutableStep(step)) {
			delete activeStepsPath[stepId];
			if (parentContext) prevResult = stepResults[stepId] = {
				...prevResult,
				payload: parentContext.input?.output ?? {},
				...nestedRunId && { metadata: {
					...prevResult.metadata,
					nestedRunId
				} }
			};
			const newStepResults = await workflowsStore?.updateWorkflowResults({
				workflowName: workflow.id,
				runId,
				stepId,
				result: prevResult,
				requestContext
			});
			if (!newStepResults || Object.keys(newStepResults).length === 0) stepResults = {
				...stepResults ?? {},
				[stepId]: prevResult
			};
			else stepResults = newStepResults;
		}
		stepResults = {
			...stepResults,
			__state: currentState
		};
		if (!prevResult?.status || prevResult.status === "failed") {
			await this.mastra.pubsub.publish("workflows", {
				type: "workflow.fail",
				runId,
				data: {
					workflowId,
					runId,
					executionPath,
					resumeSteps,
					parentWorkflow,
					stepResults,
					timeTravel,
					restart,
					prevResult,
					activeStepsPath,
					requestContext,
					state: currentState,
					outputOptions
				}
			});
			return;
		} else if (prevResult.status === "suspended") {
			await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {
				type: "watch",
				runId,
				data: {
					type: "workflow-step-suspended",
					payload: {
						id: stepId,
						...prevResult,
						suspendedAt: Date.now(),
						suspendPayload: prevResult.suspendPayload
					}
				}
			});
			const parentEntry = workflow.stepGraph[executionPath[0]];
			if ((parentEntry?.type === "parallel" || parentEntry?.type === "conditional") && executionPath.length > 1) {
				await this.aggregateBranchResults({
					workflow,
					workflowId,
					runId,
					branchEntry: parentEntry,
					branchExecutionPath: executionPath,
					latestBranchResult: prevResult,
					resumeSteps,
					timeTravel,
					restart,
					parentWorkflow,
					stepResults,
					activeStepsPath,
					requestContext,
					state: currentState,
					outputOptions
				});
				return;
			}
			const suspendedPaths = {};
			const suspendedStepId = getStepId(workflow, executionPath);
			if (suspendedStepId) suspendedPaths[suspendedStepId] = executionPath;
			const resumeLabels = prevResult.suspendPayload?.__workflow_meta?.resumeLabels ?? {};
			if (workflow?.options?.shouldPersistSnapshot?.({
				stepResults: stepResults ?? {},
				workflowStatus: "suspended"
			}) ?? true) {
				await workflowsStore?.updateWorkflowResults({
					workflowName: workflow.id,
					runId,
					stepId: "__state",
					result: currentState,
					requestContext
				});
				const suspendTracingContext = this.resolveSuspendTracingContext(runId);
				await workflowsStore?.updateWorkflowState({
					workflowName: workflowId,
					runId,
					opts: {
						status: "suspended",
						result: prevResult,
						suspendedPaths,
						resumeLabels,
						activePaths: executionPath,
						activeStepsPath,
						...suspendTracingContext ? { tracingContext: suspendTracingContext } : {}
					}
				});
				await this.pruneAndRepersistSnapshot({
					workflow,
					workflowId,
					runId
				});
			}
			await this.mastra.pubsub.publish("workflows", {
				type: "workflow.suspend",
				runId,
				data: {
					workflowId,
					runId,
					executionPath,
					resumeSteps,
					parentWorkflow,
					stepResults,
					prevResult,
					activeStepsPath,
					requestContext,
					timeTravel,
					restart,
					state: currentState,
					outputOptions
				}
			});
			return;
		}
		if (step && isSingleStepEntry(step)) {
			await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {
				type: "watch",
				runId,
				data: {
					type: "workflow-step-result",
					payload: {
						id: stepId,
						...prevResult
					}
				}
			});
			if (prevResult.status === "success") await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {
				type: "watch",
				runId,
				data: {
					type: "workflow-step-finish",
					payload: {
						id: stepId,
						metadata: {}
					}
				}
			});
		}
		step = workflow.stepGraph[executionPath[0]];
		if (perStep) if (parentWorkflow && executionPath[0] < workflow.stepGraph.length - 1) {
			const { endedAt, output, status, ...nestedPrevResult } = prevResult;
			await this.endWorkflow({
				workflow,
				parentWorkflow,
				workflowId,
				runId,
				executionPath,
				resumeSteps,
				stepResults,
				prevResult: {
					...nestedPrevResult,
					status: "paused"
				},
				activeStepsPath,
				requestContext,
				perStep
			});
		} else await this.endWorkflow({
			workflow,
			parentWorkflow,
			workflowId,
			runId,
			executionPath,
			resumeSteps,
			stepResults,
			prevResult,
			activeStepsPath,
			requestContext,
			perStep
		});
		else if ((step?.type === "parallel" || step?.type === "conditional") && executionPath.length > 1) await this.aggregateBranchResults({
			workflow,
			workflowId,
			runId,
			branchEntry: step,
			branchExecutionPath: executionPath,
			latestBranchResult: prevResult,
			resumeSteps,
			timeTravel,
			restart,
			parentWorkflow,
			stepResults,
			activeStepsPath,
			requestContext,
			state: currentState,
			outputOptions
		});
		else if (step?.type === "foreach") {
			const originalArray = readForeachResult(stepResults, getEntryId(step.step))?.payload;
			await this.mastra.pubsub.publish("workflows", {
				type: "workflow.step.run",
				runId,
				data: {
					workflowId,
					runId,
					executionPath: executionPath.slice(0, -1),
					resumeSteps,
					parentWorkflow,
					stepResults,
					prevResult: {
						...prevResult,
						output: originalArray
					},
					activeStepsPath,
					requestContext,
					timeTravel,
					restart,
					state: currentState,
					outputOptions,
					forEachIndex
				}
			});
		} else if (executionPath[0] >= workflow.stepGraph.length - 1) await this.endWorkflow({
			workflow,
			parentWorkflow,
			workflowId,
			runId,
			executionPath,
			resumeSteps,
			stepResults,
			prevResult,
			activeStepsPath,
			requestContext,
			state: currentState,
			outputOptions
		});
		else {
			const nextExecutionPath = executionPath.slice(0, -1).concat([executionPath[executionPath.length - 1] + 1]);
			await this.mastra.pubsub.publish("workflows", {
				type: "workflow.step.run",
				runId,
				data: {
					workflowId,
					runId,
					executionPath: nextExecutionPath,
					resumeSteps,
					parentWorkflow,
					stepResults,
					prevResult,
					activeStepsPath,
					requestContext,
					timeTravel,
					restart,
					state: currentState,
					outputOptions
				}
			});
		}
	}
	async loadData({ workflowId, runId }) {
		return await (await this.mastra.getStorage()?.getStore("workflows"))?.loadWorkflowSnapshot({
			workflowName: workflowId,
			runId
		});
	}
	/**
	* Result of handling a single workflow event.
	*
	* - `ok: true` — event was processed; the transport should ack.
	* - `ok: false, retry: true` — transient failure, the transport should
	*   nack/redeliver (or, for HTTP push, return 5xx so the broker retries).
	* - `ok: false, retry: false` — terminal/poison failure, the transport
	*   should drop the event (or return 4xx for HTTP push).
	*/
	async handle(event) {
		const baseWorkflowData = event.data;
		const eventKey = event.id ?? JSON.stringify({
			type: event.type,
			runId: event.runId,
			workflowId: baseWorkflowData?.workflowId,
			executionPath: baseWorkflowData?.executionPath
		});
		if (this.deliveryAttempts.get(eventKey) === WorkflowEventProcessor.TERMINAL_SENTINEL) return {
			ok: false,
			retry: false
		};
		try {
			await this.#dispatch(event);
			this.deliveryAttempts.delete(eventKey);
			return { ok: true };
		} catch (err) {
			const attempts = (this.deliveryAttempts.get(eventKey) ?? 0) + 1;
			this.#setDeliveryAttempts(eventKey, attempts);
			const exhausted = attempts >= WorkflowEventProcessor.MAX_DELIVERY_ATTEMPTS;
			this.mastra.getLogger()?.error("WorkflowEventProcessor.handle: error processing event", {
				type: event.type,
				runId: event.runId,
				attempts,
				maxAttempts: WorkflowEventProcessor.MAX_DELIVERY_ATTEMPTS,
				terminal: exhausted,
				error: err
			});
			if (!exhausted) return {
				ok: false,
				retry: true
			};
			this.#setDeliveryAttempts(eventKey, WorkflowEventProcessor.TERMINAL_SENTINEL);
			try {
				const failWorkflowData = event.data;
				if (event.type !== "workflow.fail" && failWorkflowData && failWorkflowData.workflowId && failWorkflowData.runId) await this.errorWorkflow(failWorkflowData, require_error.getErrorFromUnknown(err));
			} catch (failErr) {
				this.mastra.getLogger()?.error("WorkflowEventProcessor.handle: failed to publish workflow.fail after retry exhaustion", {
					type: event.type,
					runId: event.runId,
					error: failErr
				});
			}
			return {
				ok: false,
				retry: false
			};
		}
	}
	/**
	* Set a deliveryAttempts entry and evict the oldest entries (FIFO via Map's
	* insertion-order iteration) if we've exceeded DELIVERY_ATTEMPTS_MAX_ENTRIES.
	* Re-setting an existing key first deletes then re-inserts so that the entry
	* moves to the tail of the iteration order; this keeps actively-retrying
	* events from being evicted while idle TERMINAL_SENTINEL entries age out.
	*/
	#setDeliveryAttempts(eventKey, value) {
		if (this.deliveryAttempts.has(eventKey)) this.deliveryAttempts.delete(eventKey);
		this.deliveryAttempts.set(eventKey, value);
		while (this.deliveryAttempts.size > WorkflowEventProcessor.DELIVERY_ATTEMPTS_MAX_ENTRIES) {
			const oldestKey = this.deliveryAttempts.keys().next().value;
			if (oldestKey === void 0) break;
			this.deliveryAttempts.delete(oldestKey);
		}
	}
	/**
	* @deprecated prefer {@link WorkflowEventProcessor.handle}, which returns a
	* structured result instead of relying on an ack callback. Kept as a thin
	* wrapper so existing pull-mode call sites continue to work.
	*/
	async process(event, ack) {
		if ((await this.handle(event)).ok) try {
			await ack?.();
		} catch (e) {
			this.mastra.getLogger()?.error("Error acking event", e);
		}
	}
	async #dispatch(event) {
		const { type, data } = event;
		const workflowData = data;
		const currentState = await this.loadData({
			workflowId: workflowData.workflowId,
			runId: workflowData.runId
		});
		if (currentState?.status === "canceled" && type !== "workflow.end" && type !== "workflow.cancel") return;
		if (type.startsWith("workflow.user-event.")) {
			const userEventWorkflow = this.#tryResolveWorkflow(workflowData.workflowId);
			if (!userEventWorkflow) return this.errorWorkflow(workflowData, new require_error.MastraError({
				id: "MASTRA_WORKFLOW",
				text: `Workflow not found: ${workflowData.workflowId}`,
				domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
				category: require_error.ErrorCategory.SYSTEM
			}));
			await processWorkflowWaitForEvent({
				...workflowData,
				workflow: userEventWorkflow
			}, {
				pubsub: this.mastra.pubsub,
				eventName: type.split(".").slice(2).join("."),
				currentState
			});
			return;
		}
		let workflow;
		if (this.mastra.__hasInternalWorkflow(workflowData.workflowId, workflowData.runId)) workflow = this.mastra.__getInternalWorkflow(workflowData.workflowId, workflowData.runId);
		else if (workflowData.parentWorkflow) workflow = getNestedWorkflow(this.mastra, workflowData.parentWorkflow);
		else workflow = this.#tryResolveWorkflow(workflowData.workflowId);
		if (!workflow) if (type === "workflow.fail" || type === "workflow.end" || type === "workflow.cancel") {} else return this.errorWorkflow(workflowData, new require_error.MastraError({
			id: "MASTRA_WORKFLOW",
			text: `Workflow not found: ${workflowData.workflowId}`,
			domain: require_error.ErrorDomain.MASTRA_WORKFLOW,
			category: require_error.ErrorCategory.SYSTEM
		}));
		if (type === "workflow.start" || type === "workflow.resume") {
			const { runId } = workflowData;
			await this.mastra.pubsub.publish(`workflow.events.v2.${runId}`, {
				type: "watch",
				runId,
				data: {
					type: "workflow-start",
					payload: { runId }
				}
			});
		}
		const workflowArg = workflow;
		switch (type) {
			case "workflow.cancel":
				await this.processWorkflowCancel({
					workflow: workflowArg,
					...workflowData
				});
				break;
			case "workflow.start":
				await this.processWorkflowStart({
					workflow: workflowArg,
					...workflowData
				});
				break;
			case "workflow.resume":
				await this.processWorkflowStart({
					workflow: workflowArg,
					...workflowData
				});
				break;
			case "workflow.end":
				await this.processWorkflowEnd({
					workflow: workflowArg,
					...workflowData
				});
				break;
			case "workflow.step.end":
				await this.processWorkflowStepEnd({
					workflow: workflowArg,
					...workflowData
				});
				break;
			case "workflow.step.run":
				await this.processWorkflowStepRun({
					workflow: workflowArg,
					...workflowData
				});
				break;
			case "workflow.suspend":
				await this.processWorkflowSuspend({
					workflow: workflowArg,
					...workflowData
				});
				break;
			case "workflow.fail":
				await this.processWorkflowFail({
					workflow: workflowArg,
					...workflowData
				});
				break;
			default: break;
		}
	}
};
//#endregion
Object.defineProperty(exports, "StepExecutor", {
	enumerable: true,
	get: function() {
		return StepExecutor;
	}
});
Object.defineProperty(exports, "WorkflowEventProcessor", {
	enumerable: true,
	get: function() {
		return WorkflowEventProcessor;
	}
});
Object.defineProperty(exports, "cleanStepResult", {
	enumerable: true,
	get: function() {
		return cleanStepResult;
	}
});
Object.defineProperty(exports, "collectTemplateStepIds", {
	enumerable: true,
	get: function() {
		return collectTemplateStepIds;
	}
});
Object.defineProperty(exports, "createDeprecationProxy", {
	enumerable: true,
	get: function() {
		return createDeprecationProxy;
	}
});
Object.defineProperty(exports, "createRestartExecutionParams", {
	enumerable: true,
	get: function() {
		return createRestartExecutionParams;
	}
});
Object.defineProperty(exports, "createTimeTravelExecutionParams", {
	enumerable: true,
	get: function() {
		return createTimeTravelExecutionParams;
	}
});
Object.defineProperty(exports, "createTripWireFromChunk", {
	enumerable: true,
	get: function() {
		return createTripWireFromChunk;
	}
});
Object.defineProperty(exports, "forwardAgentStreamChunk", {
	enumerable: true,
	get: function() {
		return forwardAgentStreamChunk;
	}
});
Object.defineProperty(exports, "getEntryId", {
	enumerable: true,
	get: function() {
		return getEntryId;
	}
});
Object.defineProperty(exports, "getEntryWorkflow", {
	enumerable: true,
	get: function() {
		return getEntryWorkflow;
	}
});
Object.defineProperty(exports, "getResumeLabelsByStepId", {
	enumerable: true,
	get: function() {
		return getResumeLabelsByStepId;
	}
});
Object.defineProperty(exports, "getSingleStepEntryId", {
	enumerable: true,
	get: function() {
		return getSingleStepEntryId;
	}
});
Object.defineProperty(exports, "getStepEntry", {
	enumerable: true,
	get: function() {
		return getStepEntry;
	}
});
Object.defineProperty(exports, "getStepId", {
	enumerable: true,
	get: function() {
		return getStepId;
	}
});
Object.defineProperty(exports, "getStepIds", {
	enumerable: true,
	get: function() {
		return getStepIds;
	}
});
Object.defineProperty(exports, "getStepResult", {
	enumerable: true,
	get: function() {
		return getStepResult;
	}
});
Object.defineProperty(exports, "getTextDeltaFromChunk", {
	enumerable: true,
	get: function() {
		return getTextDeltaFromChunk;
	}
});
Object.defineProperty(exports, "hydrateSerializedStepErrors", {
	enumerable: true,
	get: function() {
		return hydrateSerializedStepErrors;
	}
});
Object.defineProperty(exports, "isSingleStepEntry", {
	enumerable: true,
	get: function() {
		return isSingleStepEntry;
	}
});
Object.defineProperty(exports, "isTripwireChunk", {
	enumerable: true,
	get: function() {
		return isTripwireChunk;
	}
});
Object.defineProperty(exports, "resolveForeachConcurrency", {
	enumerable: true,
	get: function() {
		return resolveForeachConcurrency;
	}
});
Object.defineProperty(exports, "runAgentEntry", {
	enumerable: true,
	get: function() {
		return runAgentEntry;
	}
});
Object.defineProperty(exports, "runCountDeprecationMessage", {
	enumerable: true,
	get: function() {
		return runCountDeprecationMessage;
	}
});
Object.defineProperty(exports, "runMappingEntry", {
	enumerable: true,
	get: function() {
		return runMappingEntry;
	}
});
Object.defineProperty(exports, "runToolEntry", {
	enumerable: true,
	get: function() {
		return runToolEntry;
	}
});
Object.defineProperty(exports, "validateStepInput", {
	enumerable: true,
	get: function() {
		return validateStepInput;
	}
});
Object.defineProperty(exports, "validateStepRequestContext", {
	enumerable: true,
	get: function() {
		return validateStepRequestContext;
	}
});
Object.defineProperty(exports, "validateStepResumeData", {
	enumerable: true,
	get: function() {
		return validateStepResumeData;
	}
});
Object.defineProperty(exports, "validateStepStateData", {
	enumerable: true,
	get: function() {
		return validateStepStateData;
	}
});
Object.defineProperty(exports, "validateStepSuspendData", {
	enumerable: true,
	get: function() {
		return validateStepSuspendData;
	}
});
Object.defineProperty(exports, "validateTemplate", {
	enumerable: true,
	get: function() {
		return validateTemplate;
	}
});
Object.defineProperty(exports, "waitForSuspendedSnapshot", {
	enumerable: true,
	get: function() {
		return waitForSuspendedSnapshot;
	}
});

//# sourceMappingURL=workflow-event-processor-CkjVcesJ.cjs.map